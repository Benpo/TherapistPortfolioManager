/**
 * md-render.js — minimal Markdown → HTML renderer for the export preview pane.
 *
 * STUB — RED gate placeholder; implementation lands in the GREEN commit.
 */
window.MdRender = (function () {
  "use strict";

  function render(/* markdown */) {
    return "";
  }

  return {
    render: render,
  };
})();
