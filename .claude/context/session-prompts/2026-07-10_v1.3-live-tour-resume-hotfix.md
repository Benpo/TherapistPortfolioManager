# Session handoff — v1.3.0 IS LIVE; one open production bug: tour dies at step 2→3

**Created:** 2026-07-10 late evening, mid-incident-recovery. Delete this file once the tour bug is fixed and note completion in the dashboard.

## Where things stand (all verified, don't re-derive)

- **v1.3.0 went live today.** 367 commits pushed (`4c4b861..e6c1176`), CF Pages deployed. **Phase 43 is COMPLETE** (`phase.complete` run): GATE-04's live-ship proof captured from CI run 29122423243 — `Docs gate range: … (367 commit(s))`, `docs-gate OK`, zero waivers. UAT 2/2, VERIFICATION passed, SECURITY.md verified for phases 40/42/42.1/43.
- **Go-live incident (RESOLVED):** deploy.yml purges the CF cache BEFORE Pages promotion → edge served a MIXED deploy (new `app.js` + old `i18n-en.js`) → raw i18n keys, broken layout; installed PWAs precached the poison under the new cache token, force-quit couldn't heal. Fixed by: `gh run rerun` (re-purge post-promotion) → 16-asset byte sweep consistent → empty-commit token roll `9e45a37` (live, verified) → Ben's devices healed (PWA needed manual DevTools SW-unregister + `sessions-garden-*` cache delete; IndexedDB untouched).
- **Milestone close (`/gsd-audit-milestone` → `/gsd-complete-milestone`) is deliberately ON HOLD** — Ben: "no finish line until things work." Do not run it until the tour bug below is fixed and Ben confirms.

## THE JOB: tour cross-page resume bug (production, reproducible on clean profile)

**Repro (Ben, incognito, live https://sessionsgarden.app, EN):** start the tour → steps 1 and 2 fine (geometry fine) → clicking Next on step 2 navigates to the settings page → **step 3 never appears**; settings loads bare, tour dead.

**Prime suspects (verify, don't assume — memory: root_cause ≠ product intent):**
1. **AttentionCoordinator vs tour resume ordering** on the destination page: `initCommon()` boots the coordinator on settings.html; the Phase-42 guard (`window.Tour.isActive()` early-return, attention-coordinator.js:~101) is useless in this direction because on a fresh page load the tour is NOT yet active when the coordinator runs.
2. **Resume path never invoked on settings.html**: tour resumes via a sessionStorage marker read at page boot (see `assets/tour.js` + the `initCommon` wiring in `assets/app.js`, Phase 41-05 "resume after setLanguage=A7"). Check whether settings.html's boot actually reaches the resume call.
3. **Step-3 anchor missing on settings.html**: a missing `data-tour` anchor with the never-silent-fallback could kill the resume differently (distinguish "resume never ran" vs "ran but anchor missing" vs "raced").

**Investigation state:** a Playwright investigation agent was mid-flight in the previous session and its findings died with it. Re-investigate fresh: Playwright vs live prod (or local — but NOT `python3 -m http.server`, it false-passes SW routes; see tests/webkit/*.mjs for harness patterns; `window.Tour.start()` can be driven directly; demo-mode seam HIDES tour entry points so don't use it for this). Instrument: sessionStorage marker before/after nav, console errors on settings.html, coordinator vs resume execution order.

**Fix path (house rules):**
- Falsifying behavior test FIRST (RED), then fix, then suite green (`npm test`, 168 files). Route via `/gsd-debug` or a quick task — Ben confirms fix DIRECTION before dispatch.
- **Docs gate on the hotfix push:** `tour.js` is now CHANGELOG-ONLY (today's role-table revision) — the push demands a changelog entry. Options: (a) bump `APP_VERSION` 1.3.0→1.3.1 + a "Fixed" changelog entry ×4 locales (release-moment rule then demands the changelog anyway — this is the clean route), or (b) tip-commit `Changelog-Unaffected: <reason>` if Ben wants to fold the mention into a later entry. Ben's call.
- **⚠ DEPLOY WARNING — the purge race is NOT fixed yet** (todo `2026-07-10-deploy-purge-race-mixed-cache.md`, HIGH). Every deploy risks another mixed-cache window. After the hotfix deploy completes, ALWAYS run the consistency sweep, and if mixed, `gh run rerun <run-id>`:
  ```bash
  for f in assets/i18n-en.js assets/app.js assets/app.css assets/tour.js sw.js; do
    L=$(curl -sL "https://sessionsgarden.app/$f" | wc -c | tr -d ' '); R=$(wc -c < "$f" | tr -d ' ')
    [ "$L" = "$R" ] && echo "OK $f" || echo "MISMATCH $f live=$L repo=$R"
  done
  ```
  Consider fixing the pipeline todo in the same session (purge after promotion + post-deploy consistency step) — it's small and every deploy until then is exposed.

## Also open (not tonight's job)

- Todo `2026-07-10-help-language-popovers-both-open.md` (minor, Ben: tomorrow): "?" and language popovers can be open simultaneously; close-siblings fix + jsdom case sketched in the todo.
- Todo `2026-07-10-deploy-purge-race-mixed-cache.md` (HIGH): see deploy warning above.
- After tour fix + Ben's confirmation: `/gsd-audit-milestone` → `/gsd-complete-milestone` for v1.3.

## Context notes for the new session

- Project CLAUDE.md's DoD section was REWRITTEN today — trailer semantics changed (`Help-Unaffected` any-commit; `Changelog-Unaffected` + `Docs-Emergency-Skip` tip-only; changelog-only tier; reporting PoC carve-out). Trust the file, not memory of the old contract.
- Today's role-table decisions are Ben-approved and recorded (OD-3/OD-4 revision notes in 43-CONTEXT.md) — do not re-litigate.
- Local `main` == `origin/main` == deployed (plus any docs commits since). Suite: 168/168.

**Start here:** read this file, then reproduce the tour bug (agent or inline Playwright), present root cause + fix direction to Ben, and proceed on his confirmation.
