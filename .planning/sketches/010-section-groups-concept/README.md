# Sketch 010 — Section groups concept (Phase 47)

**Design question:** What is the session form's structural model — Model A (mixed: bare
sections + real groups only where fields genuinely belong together) or Model B (every
section inside a group, with cleaned-up default names/membership)? And where does the
"Issue severity" block live in the reorder model (free / glued to Session topics / pinned)?

**Why it exists:** During the Phase 47 discussion (2026-07-22) Ben identified the current
accordion grouping as a pain point — single-field groups, group headers colliding with
inner field names, Session topics living under "Heart-Wall Session". He wants to finalize
the group CONCEPT with Sapir before any scheduling/planning decision ("mockup needed —
not later, now").

**What it shows:** For each model: the resulting session form (collapsible, real app
section labels) and the corresponding Settings grouped reorder list (drag handles,
up/down arrows, rename, enable toggles). Bottom row: the three severity-placement
variants. EN/HE toggle (full RTL) so Sapir can read it natively.

**Already decided, NOT explored here:** two-level reorder (no cross-group moves in 47),
drag + arrows both, disabled rows keep their slot, one reset-order button, export
mirrors the saved order, groups are form-only (never exported), empty groups hide.

**Winner:** TBD — Ben + Sapir review.
