---
phase: 05.2-landing-page-polish
task: demo-db-isolation
status: done
last_updated: 2026-03-17T17:00:00Z
---

<current_state>
Demo DB isolation is COMPLETE. The demo iframe now uses a separate `demo_portfolio` IndexedDB that is cleared and re-seeded with sample data on every load. Visitor changes do NOT affect the real database.
</current_state>

<commits_done>
- `e1ad11e` — Interactive demo iframe replacing screenshots section
- `3de2c5e` — Pulsing dot hints with i18n (8 dots, 4 languages)
- (pending commit) — Demo DB isolation via window.name + demo-seed.js
</commits_done>

<what_works>
1. `demo.html` sets `window.name = 'demo-mode'` before any scripts load
2. `db.js` checks `window.name` — uses `demo_portfolio` when in demo mode, `emotion_code_portfolio` otherwise
3. `demo-seed.js` deletes and recreates `demo_portfolio` on every demo load (5 clients, 9 sessions)
4. `overview.js` awaits `demoSeedReady` promise before rendering dashboard
5. `window.name` persists across navigations within the iframe — add-client.html, add-session.html etc. all use `demo_portfolio`
6. `app.js` `initDemoMode()` injects demo banner + CSS on any page when in demo mode
7. Service worker cache bumped to v4, includes `demo-seed.js`
8. Seed data includes realistic therapist content: adults, child, animal client, various session types, emotions, Heart-Wall work
</what_works>

<seed_data_summary>
Clients: Ben D (adult), Rexi A (animal/dog), Linda R (adult), Tom S (child), Maya K (adult)
Sessions: 9 total — mix of in-person/remote, severity tracking, trapped emotions, Heart-Wall, Body Code
</seed_data_summary>

<design_language>
- Colors: forest green (#2d6a4f), warm cream (#fdf8f0)
- Target audience: energy healers and holistic therapists
- Demo dot color: #e07a5f (coral/orange)
- Tooltip: white card with subtle green border, 8s auto-dismiss
</design_language>
