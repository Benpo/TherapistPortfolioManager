---
phase: 05.1-landing-page-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing.html
  - assets/landing.css
autonomous: true
requirements:
  - DIST-05

must_haves:
  truths:
    - "Hero section has a dark green-to-cream gradient background creating visual separation from the rest of the page"
    - "Hero text (h1, tagline, subtitle, badge) is readable as white/cream on the dark green gradient"
    - "Hero botanical illustrations are hand-drawn doodle style with organic, slightly wobbly paths — not geometric/minimalist"
    - "Section divider between hero and features is a doodle-style botanical branch with leaves"
    - "All illustrations render as black lines in light mode and cream/white lines in dark mode"
    - "Illustrations are hidden on screens narrower than 820px (existing responsive behavior maintained)"
  artifacts:
    - path: "landing.html"
      provides: "Hero section with doodle-style inline SVGs and doodle section divider"
      contains: "stroke-linecap=\"round\" stroke-linejoin=\"round\""
    - path: "assets/landing.css"
      provides: "Hero gradient, hero text contrast overrides, dark mode illustration colors, typography refinements"
      contains: "linear-gradient"
  key_links:
    - from: "landing.html hero SVGs"
      to: "assets/landing.css .hero-botanical"
      via: "SVGs use stroke=\"currentColor\", CSS sets color via .hero-botanical"
      pattern: "stroke=\"currentColor\""
    - from: "assets/landing.css dark mode"
      to: "landing.html illustrations"
      via: "[data-theme=\"dark\"] .hero-botanical color override"
      pattern: "data-theme.*dark.*hero-botanical"
---

<objective>
Redesign the hero section and section divider of the landing page with a dark green-to-cream gradient background, premium typography refinements, and hand-drawn doodle-style botanical illustrations replacing the current geometric/minimalist SVGs.

Purpose: Transform the landing page from "functional template" to "premium, natural, hand-crafted" visual identity per Sapir's design direction (Option A: hero + section dividers).
Output: Updated hero section in landing.html with new doodle SVGs, updated landing.css with gradient + typography + dark mode.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-landing-page-visual-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-landing-page-visual-redesign/05.1-RESEARCH.md
@landing.html
@assets/landing.css
@assets/tokens.css

<interfaces>
<!-- Key CSS tokens and patterns the executor needs -->

From assets/tokens.css (primitive colors for hero gradient):
```css
--color-green-700: #1e5c3a;
--color-green-600: #2d6a4f;
--color-green-500: #3a7d5f;
--color-cream-warm-400: #fefaf3;
--color-cream-warm-100: #fefcf7;
--color-cream-warm-200: #fdf5e8;
```

Dark mode background: `--color-background: #1a2020;`

From assets/landing.css (existing patterns to extend):
```css
/* Hero botanical positioning — lines 83-98 */
.hero-botanical { position: absolute; top: -10px; color: var(--color-primary); opacity: 0.18; pointer-events: none; }
.hero-botanical-left { left: -15px; }
.hero-botanical-right { right: -15px; transform: scaleX(-1); }

/* Responsive hiding — lines 100-121 */
@media (max-width: 1100px) { .hero-botanical { opacity: 0.1; } ... }
@media (max-width: 820px) { .hero-botanical { display: none; } }

/* Section divider — lines 124-136 */
.botanical-divider { display: flex; align-items: center; justify-content: center; padding: 0.5rem 0 1.5rem; color: var(--color-primary); opacity: 0.45; }

/* Dark mode badge override — lines 152-155 */
[data-theme="dark"] .landing-hero-badge { background: var(--color-surface-alt); color: var(--color-primary-dark); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero gradient, typography refinements, and text contrast overrides</name>
  <files>assets/landing.css</files>
  <action>
Add hero gradient background and hero text contrast overrides to landing.css. Also refine typography hierarchy for premium feel.

**Hero gradient (add after existing `.landing-hero` rule, line ~72):**
- `.landing-hero` gets `background: linear-gradient(160deg, var(--color-green-700) 0%, var(--color-green-600) 30%, var(--color-green-500) 55%, var(--color-cream-warm-400) 100%);`
- Dark mode variant: `[data-theme="dark"] .landing-hero { background: linear-gradient(160deg, #0f2a1a 0%, #1a3528 40%, var(--color-background) 100%); }`

**Hero text contrast (CRITICAL — text must be readable on dark green):**
- `.landing-hero h1` — change `color` to `var(--color-cream-warm-100)` (white-cream on dark green)
- `.landing-hero .landing-tagline` — `color: var(--color-cream-warm-200)`
- `.landing-hero .landing-subtitle` — `color: color-mix(in srgb, var(--color-cream-warm-100) 85%, transparent)`
- `.landing-hero .landing-hero-badge` — `background: color-mix(in srgb, var(--color-cream-warm-100) 18%, transparent); color: var(--color-cream-warm-100);`
- `.landing-hero .landing-price-note` — `color: color-mix(in srgb, var(--color-cream-warm-100) 70%, transparent)`
- `.landing-hero .landing-btn-primary` — keep green button but ensure visibility: white text on green is fine

**Dark mode hero text:** In dark mode, hero text should also be light — the gradient is still dark. Add `[data-theme="dark"] .landing-hero h1 { color: var(--color-cream-warm-200); }` and similar for tagline/subtitle.

**Dark mode illustration colors (for Task 2's SVGs):**
- `[data-theme="dark"] .hero-botanical { color: var(--color-cream-warm-200); }` (cream lines on dark)
- `[data-theme="dark"] .botanical-divider { color: var(--color-cream-warm-200); }`

**Typography refinements:**
- Increase hero h1 letter-spacing from `-0.03em` to `-0.025em` for slightly more breathing room
- `.landing-hero-badge` letter-spacing from `0.04em` to `0.06em` for premium uppercase feel
- `.landing-tagline` — reduce font-weight from 500 to 400, increase letter-spacing to `0.01em` for elegance

**CONSTRAINTS:**
- All new properties must use CSS logical properties where applicable (inline-start, block-end etc.)
- All new color values must reference design tokens or color-mix() — no hardcoded hex except dark mode gradient stops (where no token exists)
- Do NOT modify any existing responsive media queries — they continue to work
- Scope all hero text overrides to `.landing-hero` selector to avoid leaking to other sections
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && grep -c "linear-gradient" assets/landing.css && grep -c "cream-warm" assets/landing.css | tail -1</automated>
  </verify>
  <done>Hero section has gradient background (dark green top to cream bottom). All hero text elements are white/cream and readable against the dark green gradient. Dark mode gradient uses deeper forest green. Dark mode illustration colors set to cream. Typography spacing refined for premium feel.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hero botanical SVGs and section divider with hand-drawn doodle style</name>
  <files>landing.html</files>
  <action>
Replace the two hero botanical SVGs and the section divider SVG in landing.html with new hand-drawn doodle-style illustrations.

**CRITICAL STYLE REQUIREMENTS (from Sapir's reference images):**
- Thin black outlines, organic hand-drawn feel
- Slightly wobbly coordinates — NOT perfectly symmetric (offset control points by 3-8 units)
- Vary stroke-width: main stems ~2-2.5, leaves ~1.4-1.8, leaf veins ~0.7-1.0
- Closed leaf shapes using path ending with `Z` (gives filled silhouette feel without using fill)
- `stroke-linecap="round" stroke-linejoin="round"` on every SVG root
- `fill="none" stroke="currentColor"` on every SVG root
- `aria-hidden="true"` on every decorative container div

**Hero left botanical (replace lines 54-86 of landing.html):**
Create a tall flowering plant doodle (keep similar viewBox ~210x370). Must include:
- A main stem with slight curves (NOT straight line)
- 5-7 alternating leaves — closed paths with inner vein detail
- A multi-petal flower at the top (5-6 petal paths, slightly overlapping)
- Optional: 1-2 small berries or buds along the stem (small circles, stroke only)
- Small root/grass marks at the base (2-3 short wavy strokes)
- Overall: should look like someone drew a garden plant in a notebook with a thin pen

**Hero right botanical (replace lines 88-128):**
Create a branching botanical with berries/buds (keep similar viewBox ~195x318). Must include:
- A central stem with 3-4 side branches
- Leaves on branches — closed path shapes
- Clusters of small berries (circles, stroke only, 2-3 per cluster)
- A flower bud or small bloom at the top
- Less symmetrical than the left plant — more wild/organic

**Section divider (replace lines 163-178):**
Replace the geometric stem-and-leaf divider with a doodle-style horizontal branch:
- Keep viewBox="0 0 320 38" and the two horizontal lines (but make them slightly wavy using curves instead of `<line>`)
- Center element: a small branch with 2-3 leaves and a tiny flower bud
- Should feel like a quick pen sketch — 3-4 elements, not cluttered
- Keep opacity controlled by parent CSS (`.botanical-divider { opacity: 0.45; }`)

**CONSTRAINTS:**
- Keep the same container div classes (`.hero-botanical`, `.hero-botanical-left`, `.hero-botanical-right`, `.botanical-divider`)
- Keep similar viewBox dimensions to existing SVGs so CSS sizing/positioning continues to work
- Do NOT use `fill` on any path (except `fill="none"` on SVG root)
- Do NOT hardcode colors — use `stroke="currentColor"` exclusively
- Keep `xmlns="http://www.w3.org/2000/svg"` on all SVGs
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && grep -c "stroke-linecap=\"round\"" landing.html && grep -c "currentColor" landing.html</automated>
  </verify>
  <done>Hero section displays two hand-drawn doodle-style botanical illustrations (left: flowering plant, right: branching plant with berries). Section divider shows a doodle-style branch with leaves. All use currentColor for theming. All containers maintain existing CSS classes. Visual feel is organic and hand-drawn, not geometric or computer-generated.</done>
</task>

</tasks>

<verification>
1. Open landing.html in browser — hero section shows dark green gradient fading to cream
2. Hero text (title, tagline, subtitle, badge, price note) is clearly readable on the gradient
3. Botanical illustrations appear as subtle dark line art on the left and right of the hero
4. Below the hero, a doodle-style branch divider separates hero from features section
5. Toggle dark mode — gradient shifts to deeper forest green, illustrations become cream/white lines
6. Resize to below 820px — botanical illustrations hide, hero text still readable
7. Switch to Hebrew (RTL) — layout doesn't break, right illustration mirrors correctly
</verification>

<success_criteria>
- Hero gradient renders correctly in both light and dark mode
- All hero text passes WCAG AA contrast against the gradient background
- Botanical SVGs use currentColor and have organic, hand-drawn appearance
- Section divider is doodle-style, not geometric
- No regressions in responsive behavior or RTL support
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-landing-page-visual-redesign/05.1-01-SUMMARY.md`
</output>
