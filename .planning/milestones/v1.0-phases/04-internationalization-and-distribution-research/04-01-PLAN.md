---
phase: 04-internationalization-and-distribution-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - assets/i18n.js
  - assets/overview.js
  - assets/sessions.js
  - assets/reporting.js
  - index.html
  - add-session.html
  - add-client.html
  - sessions.html
  - reporting.html
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03]

must_haves:
  truths:
    - "User can switch between EN, HE, DE, CS and all UI text updates instantly without page reload"
    - "App title shows 'Sessions Garden' in all languages (not 'Emotion Code Portfolio')"
    - "Brand subtitle updates per language when switching (HE shows Hebrew subtitle, EN shows English, etc.)"
    - "'Heart-Wall' term is replaced with 'Heart Shield' (EN), 'הגנת הלב' (HE), 'Herzschutz' (DE), 'Ochrana srdce' (CS) everywhere in the UI"
    - "'Clinic' is replaced with 'Practice' in EN and 'Praxe' in CS session location labels"
    - "Czech translations contain no English trademark terms (Heart-Wall, Emotion Code)"
    - "No console errors on any page after the i18n file split"
  artifacts:
    - path: "assets/i18n-en.js"
      provides: "English translations + English quotes"
      contains: "window.I18N.en"
    - path: "assets/i18n-he.js"
      provides: "Hebrew translations + Hebrew quotes"
      contains: "window.I18N.he"
    - path: "assets/i18n-de.js"
      provides: "German translations + German quotes"
      contains: "window.I18N.de"
    - path: "assets/i18n-cs.js"
      provides: "Czech translations + Czech quotes"
      contains: "window.I18N.cs"
    - path: "assets/i18n.js"
      provides: "Loader only — no translation content"
      contains: "I18N_DEFAULT"
  key_links:
    - from: "index.html (and 4 other HTML files)"
      to: "assets/i18n-*.js + assets/i18n.js"
      via: "script tags in correct order"
      pattern: "i18n-en\\.js.*i18n-he\\.js.*i18n-de\\.js.*i18n-cs\\.js.*i18n\\.js"
    - from: "assets/overview.js"
      to: "window.I18N[lang]"
      via: "App.t() calls with renamed heartShield keys"
      pattern: "heartShield"
    - from: "brand-subtitle div"
      to: "window.I18N[lang]['app.subtitle']"
      via: "data-i18n attribute"
      pattern: "data-i18n=\"app.subtitle\""
---

<objective>
Split the monolithic i18n.js (899 lines) into four per-language files plus a thin loader, applying all terminology renames (Heart-Wall to Heart Shield, Clinic to Practice, Emotion Code Portfolio to Sessions Garden) and adding brand subtitle i18n wiring.

Purpose: Establishes the per-language file architecture required by I18N-01, applies all locked terminology decisions from the user, and removes trademarked terms from all languages.
Output: 4 language files (i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js), updated i18n.js loader, updated HTML files with correct script loading and subtitle wiring, renamed Heart Shield JS variables in overview.js/sessions.js/reporting.js.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-internationalization-and-distribution-research/04-CONTEXT.md
@.planning/phases/04-internationalization-and-distribution-research/04-RESEARCH.md

@assets/i18n.js
@assets/app.js
@assets/overview.js
@assets/sessions.js
@assets/reporting.js
@index.html
@reporting.html
@sessions.html

<interfaces>
<!-- Current i18n.js structure (lines 1-2, 688): -->
<!-- window.I18N = { en: {...}, he: {...}, de: {...}, cs: {...} }; -->
<!-- window.I18N_DEFAULT = "en"; -->
<!-- window.QUOTES = { he: [...], en: [...], de: [...], cs: [...] }; -->

<!-- app.js translation function (does NOT need changes): -->
<!-- App.t(key) reads window.I18N[currentLang][key] with en fallback -->
<!-- setLanguage() reads window.I18N[lang], sets document.body.dir for RTL -->
<!-- Language stored in localStorage as portfolioLang -->

<!-- overview.js Heart-Wall references (lines 179, 182, 251, 254): -->
<!-- client.heartWall property, App.t("common.heartWall"), session.heartWallCleared -->

<!-- sessions.js Heart-Wall references (lines 117-143): -->
<!-- heartWallCell variable, session.heartWallCleared property -->

<!-- reporting.js Heart-Wall references (lines 16, 19, 42, 49): -->
<!-- heartWallCleared counter, reportHeartWall element ID -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-language files from i18n.js split with all terminology fixes</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, assets/i18n.js</files>
  <action>
Split assets/i18n.js (899 lines) into 4 per-language files + loader. Each language file follows this pattern:

```javascript
// assets/i18n-{lang}.js
window.I18N = window.I18N || {};
window.QUOTES = window.QUOTES || {};

window.I18N.{lang} = {
  // all translation keys for this language
};

window.QUOTES.{lang} = [
  // all quotes for this language
];
```

While splitting, apply these terminology fixes simultaneously:

**All 4 languages — app.title:**
- EN: "Emotion Code Portfolio" -> "Sessions Garden"
- HE: "תיק Emotion Code" -> "Sessions Garden"
- DE: "Emotion Code Portfolio" -> "Sessions Garden"
- CS: "Emotion Code Portfolio" -> "Sessions Garden"

**All 4 languages — app.subtitle (new translated key):**
- HE: "תיעוד ומעקב אחר מפגשים טיפוליים"
- EN: "Documentation and tracking of therapeutic sessions"
- DE: "Dokumentation und Begleitung therapeutischer Sitzungen"
- CS: "Dokumentace a sledování terapeutických sezení"

**Heart-Wall -> Heart Shield rename in translation KEYS (change key names AND values):**
Rename these i18n keys in ALL 4 language blocks:
- "overview.sessions.heartWallCleared" -> "overview.sessions.heartShieldCleared"
- "session.copy.heartWall" -> "session.copy.heartShield"
- "toast.heartWallRequired" -> "toast.heartShieldRequired"
- "sessions.table.heartWall" -> "sessions.table.heartShield"
- "reporting.heartWallCleared" -> "reporting.heartShieldCleared"
- "common.heartWall" -> "common.heartShield"

And update the VALUES to use Heart Shield terminology:
- EN: "Heart-Wall Cleared:" -> "Heart Shield Cleared:", "Heart-Wall" -> "Heart Shield", "Heart-Wall Cleared Sessions" -> "Heart Shield Cleared Sessions", "Select Heart-Wall cleared or not" -> "Select Heart Shield cleared or not"
- HE: "חומת לב" -> "הגנת הלב" throughout (all heartWall keys)
- DE: "Heart-Wall" -> "Herzschutz" throughout (all heartWall keys)
- CS: "Heart-Wall" -> "Ochrana srdce" throughout (all heartWall keys)

**Clinic -> Practice rename in translation values:**
- EN: session.form.clinic value "Clinic" -> "Practice"
- CS: session.form.clinic value "Klinika" -> "Praxe"
- DE: session.form.clinic value "Praxis" stays (already correct)
- HE: session.form.clinic value "קליניקה" stays (user-validated)

**Czech additional fixes from research audit:**
- CS app.subtitle: use "Dokumentace a sledování terapeutických sezení"
- CS session.form.issueName: consider "Důvod návštěvy / téma" (flag for user if changed from "Důvod referování / stížnost")

After split, reduce assets/i18n.js to loader-only:
```javascript
// assets/i18n.js — loader only, no translation content
// Language files (i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js)
// have already populated window.I18N and window.QUOTES.
window.I18N_DEFAULT = "en";
```

IMPORTANT: Preserve the exact quote arrays as-is during the split (quote fixes are in Plan 02). Only move them into the per-language files without modification, EXCEPT for quote #12 in Hebrew which must change "שכל" to "ראש" per locked decision.
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && node -e "
      // Load all files in order
      require('./assets/i18n-en.js');
      require('./assets/i18n-he.js');
      require('./assets/i18n-de.js');
      require('./assets/i18n-cs.js');
      require('./assets/i18n.js');
      const langs = ['en','he','de','cs'];
      let ok = true;
      // Check all langs exist
      for (const l of langs) {
        if (!window.I18N[l]) { console.error('Missing I18N.' + l); ok = false; }
        if (!window.QUOTES[l]) { console.error('Missing QUOTES.' + l); ok = false; }
        // Check Sessions Garden title
        if (window.I18N[l]['app.title'] !== 'Sessions Garden') { console.error(l + ' title not Sessions Garden'); ok = false; }
        // Check Heart Shield rename
        if (window.I18N[l]['sessions.table.heartWall']) { console.error(l + ' still has heartWall key'); ok = false; }
        if (!window.I18N[l]['sessions.table.heartShield']) { console.error(l + ' missing heartShield key'); ok = false; }
        // Check subtitle exists
        if (!window.I18N[l]['app.subtitle']) { console.error(l + ' missing subtitle'); ok = false; }
      }
      // Check Practice rename
      if (window.I18N.en['session.form.clinic'] !== 'Practice') { console.error('EN clinic not Practice'); ok = false; }
      if (window.I18N.cs['session.form.clinic'] !== 'Praxe') { console.error('CS clinic not Praxe'); ok = false; }
      // Check loader
      if (window.I18N_DEFAULT !== 'en') { console.error('I18N_DEFAULT not en'); ok = false; }
      console.log(ok ? 'PASS' : 'FAIL');
      process.exit(ok ? 0 : 1);
    "
    </automated>
  </verify>
  <done>4 per-language files created with all terminology fixes applied. i18n.js reduced to loader-only (3 lines). All 4 languages have: Sessions Garden title, translated subtitle, Heart Shield keys/values, Practice/Praxe for session location. No English trademark terms remain in any language.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML script tags, subtitle wiring, and JS Heart Shield variable renames</name>
  <files>index.html, add-session.html, add-client.html, sessions.html, reporting.html, assets/overview.js, assets/sessions.js, assets/reporting.js</files>
  <action>
**HTML files (all 5 pages):** Replace the single i18n script tag with the 4 language files + loader:

Replace:
```html
<script src="./assets/i18n.js"></script>
```
With:
```html
<script src="./assets/i18n-en.js"></script>
<script src="./assets/i18n-he.js"></script>
<script src="./assets/i18n-de.js"></script>
<script src="./assets/i18n-cs.js"></script>
<script src="./assets/i18n.js"></script>
```

**HTML files (all 5 pages):** Add data-i18n attribute to brand subtitle:

Replace:
```html
<div class="brand-subtitle">Your private practice journal</div>
```
With:
```html
<div class="brand-subtitle" data-i18n="app.subtitle">Documentation and tracking of therapeutic sessions</div>
```

**reporting.html:** Update Heart-Wall data-i18n attributes:
- `data-i18n="reporting.heartWallCleared"` -> `data-i18n="reporting.heartShieldCleared"`
- Update fallback text: "Heart-Wall Cleared Sessions" -> "Heart Shield Cleared Sessions"

**sessions.html:** Update Heart-Wall data-i18n attributes:
- `data-i18n="sessions.table.heartWall"` -> `data-i18n="sessions.table.heartShield"`
- Update fallback text: "Heart-Wall" -> "Heart Shield"

**assets/overview.js:** Rename JS references:
- `App.t("common.heartWall")` -> `App.t("common.heartShield")`
- `App.t("overview.sessions.heartWallCleared")` -> `App.t("overview.sessions.heartShieldCleared")`
- NOTE: `client.heartWall` and `session.heartWallCleared` are IndexedDB data properties -- do NOT rename these. They reference stored data field names. Only rename i18n key references and local variable names.

**assets/sessions.js:** Rename JS references:
- `heartWallCell` variable -> `heartShieldCell` (local variable rename)
- `App.t("overview.sessions.heartWallCleared")` -> `App.t("overview.sessions.heartShieldCleared")`

**assets/reporting.js:** Rename JS references:
- `heartWallCleared` variable -> `heartShieldCleared` (local variable rename)
- `heartWallEl` variable -> `heartShieldEl` (local variable rename)
- NOTE: `session.heartWallCleared` is a data property -- do NOT rename
- NOTE: `document.getElementById("reportHeartWall")` references an HTML element ID. Also rename the ID in reporting.html from `reportHeartWall` to `reportHeartShield`.

Do NOT modify assets/app.js -- it reads window.I18N[lang] generically and needs no changes.
Do NOT modify assets/db.js -- database field names stay as-is.
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && echo "=== Checking HTML script tags ===" && for f in index.html add-session.html add-client.html sessions.html reporting.html; do grep -c "i18n-en.js" "$f" | tr -d '\n' && echo " $f"; done && echo "=== Checking subtitle wiring ===" && grep -c 'data-i18n="app.subtitle"' index.html add-session.html add-client.html sessions.html reporting.html && echo "=== Checking no old heartWall i18n keys in JS ===" && ! grep -n 'App\.t.*heartWall' assets/overview.js assets/sessions.js assets/reporting.js && echo "PASS: No old heartWall App.t calls" && echo "=== Checking heartShield references exist ===" && grep -c 'heartShield' assets/overview.js assets/sessions.js assets/reporting.js && echo "=== Checking HTML data-i18n renames ===" && grep -c 'heartShield' reporting.html sessions.html</automated>
  </verify>
  <done>All 5 HTML files load 4 language scripts + loader in correct order. Brand subtitle has data-i18n="app.subtitle" on all pages. Heart Shield rename applied to all JS App.t() calls and local variable names in overview.js, sessions.js, reporting.js. HTML data-i18n attributes and element IDs updated in reporting.html and sessions.html. Database property names (client.heartWall, session.heartWallCleared) left untouched.</done>
</task>

</tasks>

<verification>
1. Open each of the 5 pages in browser -- no console errors
2. Switch language to each of EN/HE/DE/CS -- all text updates instantly, no page reload needed
3. Brand title shows "Sessions Garden" in all languages
4. Brand subtitle updates per language
5. Sessions table header shows "Heart Shield" (EN), "הגנת הלב" (HE), "Herzschutz" (DE), "Ochrana srdce" (CS)
6. Reporting page shows "Heart Shield Cleared Sessions" (EN) or equivalent per language
7. Add Session page shows "Practice" (EN) / "Praxe" (CS) for session location
8. No instances of "Heart-Wall", "Emotion Code Portfolio", or "Klinika" visible in the UI in any language
</verification>

<success_criteria>
- 4 per-language files exist and populate window.I18N and window.QUOTES correctly
- i18n.js is loader-only (under 5 lines)
- All 5 HTML pages load without console errors in all 4 languages
- "Sessions Garden" appears as app title in every language
- No trademarked terms (Heart-Wall, Emotion Code) appear anywhere in UI text
- Language switching works instantly with no reload
</success_criteria>

<output>
After completion, create `.planning/phases/04-internationalization-and-distribution-research/04-01-SUMMARY.md`
</output>
