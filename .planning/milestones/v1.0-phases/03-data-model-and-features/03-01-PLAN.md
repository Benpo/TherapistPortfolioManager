---
phase: 03-data-model-and-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/db.js
  - add-client.html
  - assets/add-client.js
  - add-session.html
  - assets/i18n.js
autonomous: true
requirements: [DATA-02, DATA-03]

must_haves:
  truths:
    - "User can create a client with type Adult, Child, Animal, or Other"
    - "Existing clients with type 'human' are migrated to 'adult' on DB upgrade"
    - "User can select a referral source from a dropdown when adding or editing a client"
    - "Selecting 'Other' referral source reveals a free text field that persists"
    - "The inline client form in add-session.html includes the 'Other' type option"
  artifacts:
    - path: "assets/db.js"
      provides: "MIGRATIONS[2] with human-to-adult migration"
      contains: "DB_VERSION = 2"
    - path: "add-client.html"
      provides: "4 client type toggle cards + referral source dropdown"
      contains: "clientReferralSource"
    - path: "assets/add-client.js"
      provides: "Referral source save/load logic with Other toggle"
    - path: "add-session.html"
      provides: "Inline client form with 4 type options"
    - path: "assets/i18n.js"
      provides: "i18n keys for new client types and referral source options in all 4 languages"
  key_links:
    - from: "assets/db.js"
      to: "IndexedDB"
      via: "MIGRATIONS[2] cursor update"
      pattern: "cursor\\.update.*type.*adult"
    - from: "assets/add-client.js"
      to: "PortfolioDB.addClient"
      via: "referralSource field in client object"
      pattern: "referralSource"
    - from: "add-client.html"
      to: "assets/add-client.js"
      via: "clientReferralSource select element"
      pattern: "clientReferralSource"
---

<objective>
Add DB migration v2 (human-to-adult type migration), expand client types to Adult/Child/Animal/Other, and add referral source dropdown with "Other" free text to the client form.

Purpose: The clinical data model needs expanded client categorization and referral tracking per Sapir's approved field list.
Output: Working client type expansion with DB migration, referral source field on client records.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-model-and-features/03-CONTEXT.md
@.planning/phases/03-data-model-and-features/03-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs -->

From assets/db.js:
```javascript
const DB_VERSION = 1; // Must become 2
const MIGRATIONS = {
  1: function initializeSchema(db) { /* ... */ },
  // Phase 3 will add version 2 here when schema changes are needed:
  // 2: function addReferralSource(db, transaction) { ... },
};
// Migration loop (lines 35-39): for (let v = oldVersion + 1; v <= newVersion; v++)
```

From add-client.html (lines 84-98):
```html
<div class="toggle-group" id="clientTypeGroup">
  <label class="toggle-card" data-type="child">
    <input type="radio" name="clientType" value="child" />
  </label>
  <label class="toggle-card active" data-type="adult">
    <input type="radio" name="clientType" value="adult" checked />
  </label>
  <label class="toggle-card" data-type="animal">
    <input type="radio" name="clientType" value="animal" />
  </label>
</div>
```

From add-session.html (inline client form, lines 138-148):
```html
<label class="toggle-card" data-type="child"> ... </label>
<label class="toggle-card active" data-type="adult"> ... </label>
<label class="toggle-card" data-type="animal"> ... </label>
```

From assets/add-client.js: Uses `setupToggleGroup("clientTypeGroup")` pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration v2 and client type expansion</name>
  <files>assets/db.js, add-client.html, add-session.html, assets/i18n.js</files>
  <action>
1. In `assets/db.js`:
   - Change `DB_VERSION` from 1 to 2
   - Add `MIGRATIONS[2]` function named `expandDataModel(db, transaction)` that:
     - Opens a cursor on the "clients" store via `transaction.objectStore("clients")`
     - For each record where `type === "human"`, calls `cursor.update({ ...record, type: "adult" })`
     - Does NOT create new indexes (referralSource is not searched by, importantPoints is a session property stored as-is)
   - CRITICAL: Use `transaction.objectStore("clients")` (the upgrade transaction), NOT `db.transaction()`

2. In `add-client.html`:
   - Add a 4th toggle card for "Other" type after the "Animal" card, following the exact same pattern:
     ```html
     <label class="toggle-card" data-type="other">
       <input type="radio" name="clientType" value="other" />
       <span class="icon-badge">&#10067;</span>
       <span data-i18n="client.form.type.other">Other</span>
     </label>
     ```
   - Choose an appropriate emoji/icon for "Other" (suggestion: question mark or generic person silhouette)

3. In `add-session.html` (inline client form, around lines 138-148):
   - Add the same "Other" toggle card after the "Animal" card in the inline client type toggle group
   - Use the same `data-type="other"` and `data-i18n="client.form.type.other"` attributes

4. In `assets/i18n.js`, add to ALL four language objects (en, he, de, cs):
   - `"client.form.type.other": "Other"` (en), `"אחר"` (he), `"Andere"` (de), `"Ostatni"` (cs)
   - `"common.type.other": "Other"` (en), `"אחר"` (he), `"Andere"` (de), `"Ostatni"` (cs)
   - Follow Hebrew text policy: use plural where natural

No changes to add-client.js are needed for type expansion -- `setupToggleGroup` already handles any number of toggle cards, and the type value is read from the checked radio input.
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && grep -c "DB_VERSION = 2" assets/db.js && grep -c "expandDataModel" assets/db.js && grep -c 'data-type="other"' add-client.html add-session.html && grep -c "client.form.type.other" assets/i18n.js</automated>
  </verify>
  <done>DB_VERSION is 2, MIGRATIONS[2] exists with human-to-adult cursor migration, "Other" toggle card present in both add-client.html and add-session.html inline form, i18n keys for "other" type exist in all 4 languages</done>
</task>

<task type="auto">
  <name>Task 2: Referral source dropdown with "Other" free text</name>
  <files>add-client.html, assets/add-client.js, assets/i18n.js</files>
  <action>
1. In `add-client.html`, add a referral source form field after the client type toggle group (or after notes/phone -- Claude decides placement):
   ```html
   <div class="form-field">
     <label class="label" data-i18n="client.form.referralSource">Referral Source</label>
     <select class="select-field" id="clientReferralSource">
       <option value="" data-i18n="client.form.referral.placeholder">Select...</option>
       <option value="facebook">Facebook</option>
       <option value="instagram">Instagram</option>
       <option value="wordOfMouth" data-i18n="client.form.referral.wordOfMouth">Word of mouth</option>
       <option value="colleague" data-i18n="client.form.referral.colleague">Colleague referral</option>
       <option value="internet" data-i18n="client.form.referral.internet">Internet search</option>
       <option value="other" data-i18n="client.form.referral.other">Other</option>
     </select>
     <input class="input" id="clientReferralOther" type="text" style="display:none;"
       data-i18n-placeholder="client.form.referral.otherPlaceholder" />
   </div>
   ```
   - The field is NOT required (no asterisk, no validation)
   - Facebook and Instagram options do NOT need i18n (brand names are universal)

2. In `assets/add-client.js`:
   - Add event listener on `#clientReferralSource` select: when value changes to "other", show `#clientReferralOther` input; otherwise hide it
   - On save (both add and update paths): read referral source value. If "other" is selected, save the free text value from `#clientReferralOther`. If a preset is selected, save the preset key (e.g., "facebook"). Store as `client.referralSource` (single string field).
   - On edit/load (when editing existing client): populate the select dropdown from `client.referralSource`. If the stored value does not match any preset option value, set select to "other" and populate the free text field with the stored value, showing it.
   - Do NOT add referral source to the inline client form in add-session.html (it's a quick-add flow; referral can be added later via edit)

3. In `assets/i18n.js`, add to ALL four language objects:
   - `"client.form.referralSource"` -- "Referral Source" / "מקור הפנייה" / "Empfehlungsquelle" / "Zdroj doporuceni"
   - `"client.form.referral.placeholder"` -- "Select..." / "...בחירה" / "Auswahlen..." / "Vyberte..."
   - `"client.form.referral.wordOfMouth"` -- "Word of mouth" / "המלצה מפה לאוזן" / "Mundpropaganda" / "Doporuceni od znameho"
   - `"client.form.referral.colleague"` -- "Colleague/therapist referral" / "המלצה מקולגה/מטפל אחר" / "Empfehlung von Kollegen" / "Doporuceni od kolegy"
   - `"client.form.referral.internet"` -- "Internet search" / "חיפוש אינטרנט" / "Internetsuche" / "Hledani na internetu"
   - `"client.form.referral.other"` -- "Other" / "אחר" / "Andere" / "Jine"
   - `"client.form.referral.otherPlaceholder"` -- "Please specify..." / "...נא לפרט" / "Bitte angeben..." / "Prosim upřesněte..."
   - Follow Hebrew text policy: plural form where natural
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && grep -c "clientReferralSource" add-client.html && grep -c "referralSource" assets/add-client.js && grep -c "client.form.referralSource" assets/i18n.js</automated>
  </verify>
  <done>Referral source dropdown appears in add-client form with 6 options, selecting "Other" reveals free text input, referral source persists through save/reload/edit cycle, i18n keys exist in all 4 languages</done>
</task>

</tasks>

<verification>
1. Open add-client.html -- 4 client type cards visible (Adult selected by default, Child, Animal, Other)
2. Create a new client with type "Other" -- saves successfully
3. Referral source dropdown visible with all 6 options + "Select..."
4. Select "Other" referral -- free text appears; type custom text, save, edit client -- custom text persists
5. Select "Facebook" referral, save, edit -- "Facebook" is re-selected
6. Switch language to Hebrew -- all new labels display correctly in Hebrew
7. Open add-session.html -- inline client form shows 4 type options (no referral source in inline form)
8. Clear browser data, reload app -- DB migration runs, no errors in console
</verification>

<success_criteria>
- DB_VERSION is 2 with working MIGRATIONS[2]
- 4 client types selectable in both add-client and add-session inline forms
- Referral source dropdown with "Other" free text works in add-client
- All new i18n keys present in en, he, de, cs
- No console errors on fresh DB or migrated DB
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-model-and-features/03-01-SUMMARY.md`
</output>
