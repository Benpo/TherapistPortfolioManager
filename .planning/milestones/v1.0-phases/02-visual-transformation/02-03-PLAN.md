---
phase: 02-visual-transformation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/app.css
autonomous: true
requirements:
  - DSGN-03

must_haves:
  truths:
    - "Switching the app to Hebrew (RTL) mode produces correct layout with no CSS overrides needed for spacing and alignment"
    - "No margin-left, margin-right, padding-left, padding-right, text-align: left/right (except centering tricks), left: X, right: X directional properties remain in app.css outside RTL override blocks"
    - "RTL override blocks for migrated properties are removed; semantic flex-direction overrides are preserved"
  artifacts:
    - path: "assets/app.css"
      provides: "CSS logical properties throughout — all spacing/alignment/positioning uses inline-start/end variants"
      contains: "margin-inline-start"
  key_links:
    - from: "app.css logical properties"
      to: "body[dir='rtl'] set by setLanguage() in app.js"
      via: "Browser automatic RTL flip of logical properties when dir=rtl"
      pattern: "margin-inline"
---

<objective>
Migrate all directional CSS in app.css to logical properties, and remove the RTL override blocks
that become redundant after migration. Semantic flex-direction overrides are preserved.

Purpose: Hebrew RTL mode requires zero CSS overrides after this migration. Phase 4 i18n work
can add Hebrew content without touching app.css.
Output: Updated app.css with logical properties replacing all physical directional properties.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-visual-transformation/02-CONTEXT.md
@.planning/phases/02-visual-transformation/02-RESEARCH.md

<interfaces>
<!-- Complete directional CSS audit from RESEARCH.md — all 17 instances: -->
```
Line 289:  text-align: left            → text-align: start          (.table th/td)
Line 297:  text-align: right           → REMOVE (was RTL override — replace with logical on base)
Line 431:  text-align: right           → text-align: end             (.session-actions-cell)
Line 621:  right: 0.75rem              → inset-inline-end: 0.75rem   (.issue-remove absolute position)
Line 705:  right: 2rem                 → inset-inline-end: 2rem      (.toast fixed position)
Line 775:  margin-left: 0              → margin-inline-start: 0      (body[dir="rtl"] .row-toggle — remove after logical on base)
Line 776:  margin-right: 0             → margin-inline-end: 0        (body[dir="rtl"] .row-toggle — remove after logical on base)
Line 791:  text-align: right           → REMOVE (RTL override)
Line 796:  left: 0.75rem               → inset-inline-start: 0.75rem (body[dir="rtl"] .issue-remove — remove after base gets inset-inline-end)
Line 800:  text-align: right           → REMOVE (RTL override)
Line 871:  margin-left: .35rem         → margin-inline-start: .35rem (.heart-badge)
Line 883:  margin-left: 0              → REMOVE (RTL override for .heart-badge — base uses logical)
Line 884:  margin-right: .35rem        → REMOVE (RTL override for .heart-badge — base uses logical)
Line 972:  right: 1rem                 → inset-inline-end: 1rem      (.modal-close absolute position)
Line 986:  left: 50%                   → KEEP (centering trick with transform: translateX(-50%))
Line 1066: left: 1rem                  → REMOVE (body[dir="rtl"] .modal-close — base gets inset-inline-end)
Line 1074: text-align: left            → REMOVE (body[dir="rtl"] .session-actions-cell → base gets text-align: end)
Line 1096: text-align: right           → REMOVE (body[dir="rtl"] .modal-card — add text-align: start on base instead)
```

IMPORTANT — preserve these semantic RTL overrides (NOT replaceable by logical properties):
```css
/* Keep lines 730-743 area: */
body[dir="rtl"] .app-header { align-items: flex-end; }  /* intentional RTL column header alignment */

/* Keep any body[dir="rtl"] flex-direction: row-reverse */
/* Keep body[dir="rtl"] .session-header flex overrides */
/* Keep body[dir="rtl"] .row-actions justify-content overrides */
/* Keep body[dir="rtl"] .heartwall-badge align-self overrides */
```

Migration mapping reference:
```
margin-left   → margin-inline-start
margin-right  → margin-inline-end
padding-left  → padding-inline-start
padding-right → padding-inline-end
text-align: left  → text-align: start
text-align: right → text-align: end  (or REMOVE if it's an RTL override of a base that gets start/end)
left: X       → inset-inline-start: X  (except centering tricks paired with transform: translateX)
right: X      → inset-inline-end: X
border-left   → border-inline-start
border-right  → border-inline-end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate directional CSS to logical properties and clean up RTL overrides</name>
  <files>assets/app.css</files>
  <action>
Make the following targeted changes to assets/app.css. Use the research audit line numbers as a
guide — read the file first to confirm exact line context before editing, as line numbers may shift
slightly from prior edits.

CHANGES TO MAKE (follow the audit from the research):

1. .table th, .table td (around line 289):
   Change `text-align: left` → `text-align: start`

2. body[dir="rtl"] .table td (around line 297):
   REMOVE the `text-align: right` rule (the base now has text-align: start which auto-flips in RTL)
   If this leaves the body[dir="rtl"] .table selector block empty, remove the whole block.

3. .session-actions-cell (around line 431):
   Change `text-align: right` → `text-align: end`

4. .issue-remove (absolute positioned, around line 621):
   Change `right: 0.75rem` → `inset-inline-end: 0.75rem`
   If there is a `position: absolute` + `right:` combo, ensure `left:` is also removed or absent.

5. .toast (fixed positioned, around line 705):
   Change `right: 2rem` → `inset-inline-end: 2rem`

6. body[dir="rtl"] .row-toggle (around lines 775-776):
   REMOVE `margin-left: 0` and `margin-right: 0` from the RTL override.
   Then find the BASE .row-toggle rule and ensure it uses logical properties if it has margin-left/right.
   If the base rule has margin-left/right values other than 0, convert them to logical too.
   If this leaves the body[dir="rtl"] .row-toggle block empty, remove the whole block.

7. body[dir="rtl"] table text-align (around line 791):
   REMOVE `text-align: right` RTL override (covered by logical base).

8. body[dir="rtl"] .issue-remove (around line 796):
   REMOVE `left: 0.75rem` RTL override (base now uses inset-inline-end which auto-flips).
   If this leaves the block empty, remove it.

9. body[dir="rtl"] generic text-align (around line 800):
   REMOVE `text-align: right` if it's an RTL override for something now using text-align: start/end.

10. .heart-badge (around line 871):
    Change `margin-left: .35rem` → `margin-inline-start: .35rem`

11. body[dir="rtl"] .heart-badge (around lines 883-884):
    REMOVE `margin-left: 0` and `margin-right: .35rem` RTL overrides (logical handles this).
    If this leaves the block empty, remove it.

12. .modal-close (around line 972):
    Change `right: 1rem` → `inset-inline-end: 1rem`

13. line ~986 (.modal-close::before, ::after centering):
    KEEP `left: 50%` — this is a centering trick paired with transform: translateX(-50%).
    Add a comment: `/* centering trick — not directional, keep as physical */`

14. body[dir="rtl"] .modal-close (around line 1066):
    REMOVE `left: 1rem` RTL override (base now uses inset-inline-end which flips automatically).
    If this leaves the block empty, remove it.

15. body[dir="rtl"] .session-actions-cell (around line 1074):
    REMOVE `text-align: left` RTL override (base now uses text-align: end which flips automatically).
    If this leaves the block empty, remove it.

16. body[dir="rtl"] .modal-card (around line 1096):
    REMOVE `text-align: right` RTL override. Instead, add `text-align: start` to the BASE .modal-card
    rule if it doesn't already have a text-align declaration.
    If this leaves the body[dir="rtl"] .modal-card block empty, remove it.

PRESERVE (do not touch):
- `left: 50%` on .modal-close pseudo-elements (centering trick)
- body[dir="rtl"] .app-header { align-items: flex-end } — intentional semantic override, add comment
- body[dir="rtl"] .session-header flex overrides (flex-direction, justify-content)
- body[dir="rtl"] .row-actions justify-content overrides
- body[dir="rtl"] .heartwall-badge align-self overrides
- All flex-direction overrides in RTL blocks
- Lines 730-743 area — review each carefully, keep semantic overrides

After all changes: remove the now-unused `font-weight: 800` from .brand-mark if it was only
used for the "EC" text brand mark (the SVG does not need it).
  </action>
  <verify>
    1. Switch app to Hebrew (עברית) via language selector
    2. Check layout on all 5 pages:
       - Table columns: text aligns to right (start in RTL = right)
       - Session action buttons: correctly positioned on the opposite side from LTR
       - Modal close button: appears on the left side (inline-end in RTL = left)
       - Toast notification: appears on the left side
       - Heart badge margin: appears on the correct side
    3. Switch back to English — all layout returns to LTR correctly
    4. No layout breaks in either direction
    Automated check: grep app.css for remaining physical directional properties:
    `grep -n "margin-left\|margin-right\|padding-left\|padding-right\|text-align: left\|text-align: right" assets/app.css`
    Expected: zero matches outside of any intentional exceptions (only centering `left: 50%` remains).
  </verify>
  <done>
    All spacing/alignment/positioning in app.css uses logical properties. RTL override blocks for
    migrated properties removed. Semantic flex overrides preserved. grep shows zero remaining
    physical directional properties (except the documented centering left: 50%).
  </done>
</task>

</tasks>

<verification>
Complete DSGN-03 verification:
1. Switch to Hebrew — all 5 pages render correctly without any CSS overrides
2. Modal close button on correct side in RTL
3. Table alignment correct in RTL
4. Badge/toggle spacing correct in RTL
5. grep confirms no physical directional properties outside documented exception
</verification>

<success_criteria>
- Zero margin-left/right, padding-left/right, text-align: left/right (except centering trick) in app.css
- All directional properties use inline-start/end logical equivalents
- RTL layout correct on all 5 pages in Hebrew mode
- Semantic flex overrides preserved (session-header, row-actions, heartwall-badge)
- grep command returns 0 physical directional properties
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-transformation/02-03-SUMMARY.md`
</output>
