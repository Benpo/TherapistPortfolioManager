---
phase: 02-visual-transformation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - assets/tokens.css
autonomous: true
requirements:
  - DSGN-02

must_haves:
  truths:
    - "Clicking the moon/sun toggle switches the app to dark mode and back"
    - "Dark mode shows deep forest green backgrounds (not generic dark grey)"
    - "Garden colors (muted green, readable cream text, warm orange accent) remain visible in dark mode"
    - "After toggling to dark and reloading the page, dark mode is active immediately with no flash of light theme"
    - "Theme preference persists to localStorage under key 'portfolioTheme'"
  artifacts:
    - path: "assets/tokens.css"
      provides: "Night-garden dark mode override block replacing Phase 1 placeholder values"
      contains: "[data-theme=\"dark\"]"
  key_links:
    - from: "assets/tokens.css [data-theme='dark'] block"
      to: "HTML <html> element data-theme attribute"
      via: "CSS custom property cascade override"
      pattern: "data-theme.*dark"
    - from: "No-flash inline script in HTML <head>"
      to: "localStorage 'portfolioTheme'"
      via: "localStorage.getItem check before first paint"
      pattern: "portfolioTheme"
---

<objective>
Replace the Phase 1 placeholder dark mode token values in tokens.css with a real "night garden"
palette: deep forest greens for backgrounds, readable cream text, visible garden colors throughout.
The theme toggle (added as a stub in Plan 01) becomes fully functional.

Purpose: Complete DSGN-02 — dark mode that feels atmospheric and garden-themed, not generic.
Output: Updated [data-theme="dark"] block in tokens.css with night-garden values.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-visual-transformation/02-CONTEXT.md
@.planning/phases/02-visual-transformation/02-RESEARCH.md
@.planning/phases/02-visual-transformation/02-01-SUMMARY.md

<interfaces>
<!-- Current [data-theme="dark"] block to replace (Phase 1 placeholder): -->
```css
[data-theme="dark"] {
  --color-background: #1a1825;               /* purple-dark — REPLACE */
  --color-background-gradient-start: #1a1825;
  --color-background-gradient-end: #1f1c2e;  /* purple-dark — REPLACE */
  --color-surface: #252235;                  /* purple-surface — REPLACE */
  --color-surface-alt: #2a2640;
  --color-surface-hover: #2e2a3e;
  /* ... many more purple-dark values ... */
  --color-primary: #9b88ff;                  /* purple-bright — REPLACE with garden green */
  --color-text: #f0eeff;                     /* near-white with purple tint — REPLACE */
  /* ... */
}
```

Theme toggle (added in Plan 01) in app.js — already functional, reads/writes portfolioTheme:
```javascript
function initThemeToggle() {
  // Reads: document.documentElement.getAttribute('data-theme') === 'dark'
  // Writes: document.documentElement.setAttribute('data-theme', 'dark')
  //         document.documentElement.removeAttribute('data-theme')
  //         localStorage.setItem('portfolioTheme', next)
}
```

No-flash script (added in Plan 01 to all 5 HTML heads):
```html
<script>
  (function(){try{var t=localStorage.getItem('portfolioTheme');if(t==='dark')document.documentElement.setAttribute('data-theme','dark');}catch(e){}})();
</script>
```
This script is already in place — no changes needed to HTML files.

Garden primitives available after Plan 01 (for reference in dark overrides):
- Greens: --color-green-700 (#1e5c3a), --color-green-600 (#2d6a4f), --color-green-500 (#3a7d5f)
- Oranges: --color-orange-500 (#f97316), --color-orange-400 (#fb923c)
- Darks: --color-dark-900 (#2f2d38), --color-dark-800 (#2e2a38), --color-dark-700 (#1a1825)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dark mode placeholder with night-garden palette in tokens.css</name>
  <files>assets/tokens.css</files>
  <action>
Replace the entire [data-theme="dark"] block in assets/tokens.css with night-garden values.

Design intent: "Night garden" — deep forest green backgrounds with garden colors still visible.
NOT generic dark grey. The user should feel they are in a garden at night.

Night-garden palette values:

BACKGROUNDS (deep forest greens):
- --color-background: #0d2818              (very deep forest green, near black but green)
- --color-background-gradient-start: #0d2818
- --color-background-gradient-end: #112e1e (slightly lighter forest green)

SURFACES (dark green panels):
- --color-surface: #162e1f                 (dark green card surface)
- --color-surface-alt: #1a3524            (slightly lighter for alternating rows)
- --color-surface-hover: #1f3d28          (hover state)
- --color-surface-subtle: #1a3524
- --color-surface-muted: #162e1f
- --color-surface-avatar: #1f3d28
- --color-surface-media: #1f3d28
- --color-surface-disabled: #162e1f
- --color-surface-secondary-btn: #1a3524
- --color-surface-toggle: #1a3524
- --color-surface-modal-close: #1a3524

TEXT (cream/light, readable on dark green):
- --color-text: #f0ede4                    (warm cream — not pure white, matches light mode cream feel)
- --color-text-muted: #9ab89f             (muted garden green-grey)
- --color-text-meta: #9ab89f

PRIMARY (lighter garden green — visible on dark backgrounds):
- --color-primary: #52a078               (medium garden green — lighter than light mode primary)
- --color-primary-soft: #1a3524          (dark green soft container)
- --color-primary-dark: #a8d4b8          (light cream-green for headings in dark)
- --color-primary-deeper: #9ab89f

BORDERS (subtle green-tinted):
- --color-border: rgba(82,160,120,0.25)
- --color-border-soft: rgba(82,160,120,0.12)
- --color-border-faint: rgba(82,160,120,0.08)
- --color-border-subtle: rgba(82,160,120,0.15)
- --color-border-ghost: rgba(82,160,120,0.3)

TOAST / MODAL:
- --color-toast-bg: #0d2818              (match background)
- (--color-modal-overlay-bg uses existing --color-modal-overlay which is rgba dark neutral — keep)

SHADOWS (deeper on dark):
- --shadow-soft: 0 18px 40px rgba(0,0,0,0.45)
- --shadow-nav: 0 10px 20px rgba(0,0,0,0.3)
- --shadow-card: 0 12px 26px rgba(0,0,0,0.5)
- --shadow-brand: 0 8px 24px rgba(82,160,120,0.4)
- --shadow-button: 0 4px 12px rgba(82,160,120,0.3)
- --shadow-danger: 0 4px 12px rgba(234,75,75,0.35)
- --shadow-modal: 0 20px 40px rgba(0,0,0,0.65)

PLACEHOLDER GRADIENT:
- --color-placeholder-gradient: linear-gradient(135deg, #3a7d5f, #52a078)

NOTE on orange accent: The orange (#f97316) has sufficient contrast on deep green backgrounds
(approximately 5:1 against #0d2818). Keep orange semantic tokens at their light-mode values —
they inherit from primitive tokens which are not overridden in dark mode. Only override if visual
testing shows contrast issues.

NOTE: Do NOT override --color-success, --color-danger, --color-heartwall-badge in dark mode —
these functional colors work on both backgrounds.

Remove the comment "Phase 1 placeholder values — Phase 2 will replace with garden palette" and
replace with "Night-garden dark mode palette".
  </action>
  <verify>
    1. Click the moon toggle button on any page — app switches to dark mode
    2. Verify backgrounds are deep green (not purple, not grey)
    3. Verify text is readable cream color (not purple-tinted)
    4. Verify nav active/hover shows garden green (not bright purple)
    5. Toggle back to light — returns to cream/garden green
    6. Reload page after toggling to dark — dark mode activates immediately (no flash of light)
    7. Inspect localStorage.getItem('portfolioTheme') — returns 'dark'
    8. Open browser DevTools Color Contrast checker: verify text (#f0ede4) on background (#0d2818)
       meets WCAG AA (4.5:1 minimum). Expected ~14:1 — well above threshold.
  </verify>
  <done>
    Dark mode shows night-garden atmosphere (deep forest green backgrounds, cream text, visible garden
    colors). Theme persists across reload with no flash. Toggle switches between light and dark.
  </done>
</task>

</tasks>

<verification>
Complete DSGN-02 verification:
1. Light mode: cream backgrounds, garden green primary — confirmed in Plan 01
2. Dark mode: deep green backgrounds, cream text, functional toggle
3. Persistence: toggle to dark → reload → still dark (no flash)
4. Toggle to light → reload → still light
5. Visual: all 5 pages look correct in both modes
</verification>

<success_criteria>
- [data-theme="dark"] block contains night-garden values (deep green, not purple/grey)
- Theme toggle functional on all 5 pages
- Preference persists in localStorage key 'portfolioTheme'
- No flash of wrong theme on reload in either direction
- Text readable in dark mode (WCAG AA minimum)
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-transformation/02-02-SUMMARY.md`
</output>
