---
phase: 13-review-and-fix-greeting-quotes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements:
  - QUOTE-01
  - QUOTE-02
  - QUOTE-03

must_haves:
  truths:
    - "Every quote sounds natural and idiomatic in its language — not like a translation"
    - "No quote is attributed to someone who did not say it"
    - "Hebrew quotes use neutral or plural phrasing — no masculine-only forms"
    - "All 4 languages have the same number of quotes with matching themes"
    - "Quote tone stays consistent: healing, inspiration, energy, motivation"
  artifacts:
    - path: "assets/i18n-en.js"
      provides: "English quotes (window.QUOTES.en)"
      contains: "window.QUOTES.en"
    - path: "assets/i18n-he.js"
      provides: "Hebrew quotes (window.QUOTES.he)"
      contains: "window.QUOTES.he"
    - path: "assets/i18n-de.js"
      provides: "German quotes (window.QUOTES.de)"
      contains: "window.QUOTES.de"
    - path: "assets/i18n-cs.js"
      provides: "Czech quotes (window.QUOTES.cs)"
      contains: "window.QUOTES.cs"
  key_links:
    - from: "assets/overview.js"
      to: "window.QUOTES"
      via: "getDailyQuote(lang) selects by day-of-year modulo array length"
      pattern: "QUOTES\\."
---

<objective>
Review and fix all greeting quotes across 4 languages (EN, HE, DE, CS).

Purpose: Ensure every daily greeting quote sounds natural in its language, has correct attribution (or none), follows Hebrew gender rules, and maintains the healing/inspirational tone. This is a content-quality pass -- no UI or code logic changes.

Output: Updated QUOTES arrays in all 4 i18n files with verified, natural-sounding content.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-review-and-fix-greeting-quotes/13-CONTEXT.md

@assets/i18n-en.js
@assets/i18n-he.js
@assets/i18n-de.js
@assets/i18n-cs.js
@assets/overview.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and rewrite English quotes + verify famous quote attributions</name>
  <files>assets/i18n-en.js</files>
  <action>
Read the full window.QUOTES.en array (lines 217-261 of assets/i18n-en.js). Perform these checks on every quote:

**Custom quotes (no author):**
1. Does it sound natural in English? Fix any awkward phrasing.
2. Does the tone fit (healing, inspiration, energy, motivation for therapists)? Flag if off-tone.
3. Is there redundancy across the set? If two quotes say essentially the same thing, replace one.

**Famous quotes (with author):**
1. Verify attribution accuracy. For each famous quote:
   - "Out beyond ideas of wrongdoing..." -- commonly attributed to Rumi. Verify.
   - "The real miracle of life is not about doing extraordinary things..." -- attributed to Thich Nhat Hanh. Verify this is an actual quote of his (not a paraphrase or misattribution).
   - "Smile, breathe, and go slowly" -- Thich Nhat Hanh. Verify.
   - "Who looks outside, dreams; who looks inside, awakes" -- Carl Jung. Verify.
   - "We cannot change anything until we accept it" -- Carl Jung. Verify.
   - "Don't ask what the world needs..." -- Howard Thurman. Verify.
   - "Compassion is the language of the world" -- Dalai Lama. Verify exact wording.
   - "Be the change you wish to see in the world" -- Gandhi. Known misquote -- fix or replace.
   - "All that we are is the result of what we have thought" -- Buddha. Verify.
   - "One light is enough to illuminate all darkness" -- Nelson Mandela. Verify this exact quote.
   - "Courage is not the absence of fear..." -- Brene Brown. Verify exact wording and attribution.

2. For any quote where attribution cannot be verified or is known to be wrong:
   - Option A: Fix the attribution to the correct person
   - Option B: Replace with an original (no-author) quote that captures the same spirit
   - Per user decision: "the person doesn't matter -- the message and natural phrasing matter"

**Quantity target:** Maintain at least 41 quotes. Can add up to ~48 if good new ones emerge during review. If removing a bad quote, replace it.

**Output:** Rewrite the window.QUOTES.en array in assets/i18n-en.js. Keep the same structure: custom quotes (no author) first, then famous quotes (with author). Keep all non-QUOTES content in the file unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('assets/i18n-en.js','utf8'); const m=c.match(/window\.QUOTES\.en\s*=\s*\[([\s\S]*?)\];/); if(!m){process.exit(1)} const quotes=m[1].match(/\{[^}]+\}/g); console.log('EN quotes:',quotes.length); if(quotes.length<41){console.error('Too few quotes');process.exit(1)} const authorQuotes=quotes.filter(q=>q.includes('author')); console.log('With author:',authorQuotes.length); console.log('PASS')"</automated>
  </verify>
  <done>English quotes array has 41+ quotes, all with natural phrasing, verified attributions, and consistent healing/inspirational tone. No misattributed famous quotes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Hebrew, German, and Czech quotes — native idiom + Hebrew gender rules</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
Using the updated English quotes from Task 1 as the reference set, update all three remaining language files. Each language's QUOTES array must have the same number of quotes in the same order as English.

**For ALL languages:**
1. Each quote should match the spirit/theme of its English counterpart but sound native -- NOT like a literal translation.
2. If a quote simply cannot work in a target language (cultural mismatch, idiom that doesn't translate), replace it with a culturally appropriate alternative that keeps the same theme.
3. Famous quotes: use the established translation of that quote in the target language if one exists. If the famous person's quote is not well-known in that culture, consider replacing with a local equivalent or making it an original (no-author) quote.
4. Keep the same structure: `{ text: "..." }` for custom, `{ text: "...", author: "..." }` for famous.

**Hebrew (i18n-he.js) specific rules -- CRITICAL:**
1. Read every Hebrew quote and check for masculine-only forms (e.g., "כשאתה", "אתה יכול", verb conjugations in masculine singular).
2. Apply the priority system:
   - Priority 1: Rephrase to avoid gendered forms entirely (infinitive constructs, impersonal phrasing, "ניתן ל...", "הדרך ל...")
   - Priority 2: Use plural form (לשון רבים) if neutral isn't possible
   - Priority 3: Use feminine form (לשון נקבה) only as last resort
3. Ensure Hebrew text reads naturally -- as if written originally in Hebrew, not translated.

**German (i18n-de.js) specific:**
1. Check for natural German phrasing (muttersprachlich).
2. Use proper German quotation style in the text content itself.
3. Famous quote translations should match established German translations where they exist.

**Czech (i18n-cs.js) specific:**
1. Check for natural Czech phrasing (prirozene).
2. Czech has grammatical gender -- ensure quotes use inclusive or neutral phrasing where possible.
3. Famous quote translations should match established Czech translations where they exist.

**Keep all non-QUOTES content in each file unchanged.** Only modify the window.QUOTES.xx array.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); let ok=true; for(const lang of ['he','de','cs']){const c=fs.readFileSync('assets/i18n-'+lang+'.js','utf8'); const m=c.match(/window\.QUOTES\.'+lang+'\s*=\s*\[([\s\S]*?)\];/); if(!m){console.error(lang+': QUOTES not found');ok=false;continue} const quotes=m[1].match(/\{[^}]+\}/g); console.log(lang+' quotes:',quotes.length); if(quotes.length<41){console.error(lang+': too few');ok=false}} if(!ok)process.exit(1); console.log('PASS')" 2>&1 || node -e "const fs=require('fs'); let ok=true; for(const lang of ['he','de','cs']){const c=fs.readFileSync('assets/i18n-'+lang+'.js','utf8'); const re=new RegExp('window\\.QUOTES\\.'+lang+'\\s*=\\s*\\[([\\s\\S]*?)\\];'); const m=c.match(re); if(!m){console.error(lang+': QUOTES not found');ok=false;continue} const quotes=m[1].match(/\\{[^}]+\\}/g); console.log(lang+' quotes:',quotes.length); if(quotes.length<41){console.error(lang+': too few');ok=false}} if(!ok)process.exit(1); console.log('PASS')"</automated>
  </verify>
  <done>All 4 languages have matching quote counts (41+). Hebrew quotes use neutral/plural/feminine forms with no masculine-only phrasing. German and Czech quotes read naturally in their respective languages. Quote order matches across all files.</done>
</task>

</tasks>

<verification>
1. All 4 i18n files load without syntax errors: `node -e "for(const f of ['en','he','de','cs']){require('./assets/i18n-'+f+'.js')}" 2>&1`
2. Quote counts match across languages
3. No masculine-only Hebrew forms (spot-check for כשאתה, אתה יכול, etc.)
4. The app loads and displays a daily quote: open index.html, verify quote appears on overview page
</verification>

<success_criteria>
- All 4 QUOTES arrays have 41-48 quotes each, same count across languages
- Zero misattributed famous quotes
- Hebrew quotes follow gender priority (neutral > plural > feminine)
- Each language's quotes sound native, not translated
- App still loads and displays daily quote correctly
</success_criteria>

<output>
After completion, create `.planning/phases/13-review-and-fix-greeting-quotes/13-01-SUMMARY.md`
</output>
