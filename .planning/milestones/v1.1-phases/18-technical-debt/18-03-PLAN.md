---
phase: 18-technical-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/app.js
  - .planning/phases/18-technical-debt/REFUND-SOP.md
autonomous: true

requirements:
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "Public App API functions in app.js have JSDoc comments"
    - "App API functions are logically grouped (i18n, nav, theme, utilities)"
    - "A refund SOP document exists with step-by-step Lemon Squeezy dashboard instructions"
    - "SOP documents the enforcement mechanism (deactivate key on refund)"
  artifacts:
    - path: "assets/app.js"
      provides: "Clean, documented public App API"
      contains: "@param|@returns"
    - path: ".planning/phases/18-technical-debt/REFUND-SOP.md"
      provides: "Step-by-step refund handling procedure"
      contains: "Lemon Squeezy"
  key_links:
    - from: ".planning/phases/18-technical-debt/REFUND-SOP.md"
      to: "Lemon Squeezy dashboard"
      via: "Manual SOP steps"
      pattern: "deactivate.*license"
---

<objective>
Clean up the App.js public API with JSDoc comments and logical grouping, and create a refund handling SOP document.

Purpose: DEBT-02 ensures the shared App API is well-documented for maintainability. DEBT-03 provides Sapir with a clear process for handling refund requests without needing technical knowledge.
Output: Improved app.js with JSDoc, REFUND-SOP.md document
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-technical-debt/18-CONTEXT.md

<interfaces>
From assets/app.js — current public API (returned object, ~line 396):
```javascript
return {
  t,                    // i18n translation
  applyTranslations,    // apply data-i18n attributes
  setLanguage,          // set language + dir + dispatch event
  getLanguage,          // get current language
  initCommon,           // init nav, theme, license link
  renderNav,            // render navigation
  initThemeToggle,      // init dark/light toggle
  initLicenseLink,      // add license SVG link to header
  showToast,            // show temporary toast notification
  confirmDialog,        // show confirm/cancel dialog
  formatDate,           // format date for display
  createSeverityScale,  // create 1-10 severity picker
  getSeverityValue,     // get value from severity picker
  exportData,           // export all data
  downloadJSON,         // download JSON file
  formatSessionType,    // format session type string
  setSubmitLabel,       // set form submit button label
  readFileAsDataURL,    // read file as data URL
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.js JSDoc comments and API grouping</name>
  <files>assets/app.js</files>
  <read_first>assets/app.js</read_first>
  <action>
Per D-07: Lightweight cleanup pass of existing `window.App` API — organize, ensure consistent naming, verify JSDoc comments on public functions.

Per D-08: Single-use functions stay in their respective files. No extraction, no new utils.js (D-06).

1. Add a section comment header above each logical group of functions within the IIFE. Use this grouping:

```
// ---------------------------------------------------------------------------
// i18n — translation and language management
// ---------------------------------------------------------------------------
```
Before: `t()`, `applyTranslations()`, `setLanguage()`, `getLanguage()`

```
// ---------------------------------------------------------------------------
// Navigation and chrome
// ---------------------------------------------------------------------------
```
Before: `renderNav()`, `initCommon()`, `initThemeToggle()`, `initLicenseLink()`

```
// ---------------------------------------------------------------------------
// UI utilities
// ---------------------------------------------------------------------------
```
Before: `showToast()`, `confirmDialog()`

```
// ---------------------------------------------------------------------------
// Data formatting and export
// ---------------------------------------------------------------------------
```
Before: `formatDate()`, `createSeverityScale()`, `getSeverityValue()`, `exportData()`, `downloadJSON()`

```
// ---------------------------------------------------------------------------
// Shared form helpers (extracted Phase 16)
// ---------------------------------------------------------------------------
```
Before: `formatSessionType()`, `setSubmitLabel()`, `readFileAsDataURL()`

2. Add JSDoc comments to each public function. Examples of the format to use:

```javascript
/**
 * Translate a key using the current language dictionary.
 * Falls back to English, then returns the key itself.
 * @param {string} key - i18n key (e.g., 'nav.overview')
 * @returns {string} Translated string
 */
function t(key) { ... }

/**
 * Apply translations to all elements with data-i18n and data-i18n-placeholder attributes.
 * @param {Document|Element} [root=document] - Root element to scan
 */
function applyTranslations(root = document) { ... }

/**
 * Set the active language, persist to localStorage, update dir attribute, and dispatch event.
 * @param {string} lang - Language code ('en', 'he', 'de', 'cs')
 */
function setLanguage(lang) { ... }

/**
 * Initialize page: render nav, apply translations, set up theme toggle and license link.
 * Call this in DOMContentLoaded on every app page.
 */
function initCommon() { ... }

/**
 * Show a temporary toast notification that auto-dismisses.
 * @param {string} message - Text to display
 * @param {number} [duration=2500] - Duration in ms
 */
function showToast(message, duration) { ... }

/**
 * Show a modal confirmation dialog with OK/Cancel buttons.
 * @param {string} message - Confirmation message
 * @param {Function} onConfirm - Called when user confirms
 * @param {Object} [options] - Optional button labels {ok, cancel}
 */
function confirmDialog(message, onConfirm, options) { ... }

/**
 * Format a date string for display in the current locale.
 * @param {string} dateStr - ISO date string
 * @returns {string} Formatted date
 */
function formatDate(dateStr) { ... }

/**
 * Format session type key to translated display string.
 * @param {string} type - Session type ('clinic', 'online', 'other')
 * @returns {string} Translated type label
 */
function formatSessionType(type) { ... }

/**
 * Set a submit button's label via i18n key.
 * @param {string} key - i18n key for the label
 * @param {HTMLElement} submitButton - The button element
 * @param {HTMLElement} [submitLabel] - Optional separate label element
 */
function setSubmitLabel(key, submitButton, submitLabel) { ... }

/**
 * Read a File object as a data URL string.
 * @param {File} file - File to read
 * @returns {Promise<string>} Base64 data URL
 */
function readFileAsDataURL(file) { ... }
```

Add similar JSDoc to ALL remaining public functions: `renderNav`, `initThemeToggle`, `initLicenseLink`, `createSeverityScale`, `getSeverityValue`, `exportData`, `downloadJSON`.

3. In the return object, add grouping comments:

```javascript
return {
  // i18n
  t,
  applyTranslations,
  setLanguage,
  getLanguage: function() { return currentLang; },

  // Navigation and chrome
  initCommon,
  renderNav,
  initThemeToggle,
  initLicenseLink,

  // UI utilities
  showToast,
  confirmDialog,

  // Data formatting and export
  formatDate,
  createSeverityScale,
  getSeverityValue,
  exportData,
  downloadJSON,

  // Shared form helpers
  formatSessionType,
  setSubmitLabel,
  readFileAsDataURL,
};
```

4. Do NOT change any function signatures, logic, or behavior. This is documentation-only.
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && grep -c "@param\|@returns" assets/app.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/app.js contains at least 15 `@param` annotations
    - assets/app.js contains at least 8 `@returns` annotations
    - assets/app.js contains "i18n" section comment header
    - assets/app.js contains "Navigation and chrome" section comment header
    - assets/app.js contains "UI utilities" section comment header
    - assets/app.js contains "Data formatting and export" section comment header
    - assets/app.js contains "Shared form helpers" section comment header
    - assets/app.js return object contains grouping comments (// i18n, // Navigation, etc.)
    - No function signatures changed (all existing function names remain)
    - `grep -c "return {" assets/app.js` still returns 1 (single return object)
  </acceptance_criteria>
  <done>All public App API functions have JSDoc comments. Functions are grouped by concern with section headers. No behavioral changes.</done>
</task>

<task type="auto">
  <name>Task 2: Refund handling SOP document</name>
  <files>.planning/phases/18-technical-debt/REFUND-SOP.md</files>
  <read_first>.planning/phases/18-technical-debt/18-CONTEXT.md</read_first>
  <action>
Create the SOP document per D-09, D-10, D-11. This is a process document, not code. Written for Sapir (non-technical) to follow when a customer requests a refund.

Write the following content to `.planning/phases/18-technical-debt/REFUND-SOP.md`:

```markdown
# Refund Handling SOP — Sessions Garden

**Last updated:** 2026-03-24
**Applies to:** Lemon Squeezy Store #324581 (Sessions Garden)

## When to Use This

A customer contacts contact@sessionsgarden.app requesting a refund for their Sessions Garden license (EUR 119 one-time purchase).

## Steps

### 1. Verify the Request

- Confirm the customer's email matches a Lemon Squeezy order
- Check how long ago they purchased (Lemon Squeezy dashboard > Orders)
- EU law (Widerrufsrecht): customers have 14 days for digital goods, BUT the right is waived if they accepted the terms and began using the product. Use your judgment.

### 2. Process the Refund in Lemon Squeezy

1. Go to https://app.lemonsqueezy.com/orders
2. Find the customer's order (search by email)
3. Click the order to open details
4. Click "Refund" button
5. Select full or partial refund
6. Confirm — Lemon Squeezy handles the payment reversal

### 3. Deactivate the License Key

1. Go to https://app.lemonsqueezy.com/licenses
2. Find the customer's license key (search by email or key)
3. Click the license to open details
4. Click "Deactivate All Instances" to remove all device activations
5. Optionally: disable the license key entirely so it cannot be re-activated

### 4. What Happens After Deactivation

- **Existing installs keep working.** The app is local — it cannot check the license server daily. The customer can continue using the app on devices where it's already activated.
- **New activations blocked.** The customer cannot activate on any new device or browser. If they clear their browser data, they lose access.
- **This is proportional.** At EUR 119 one-time purchase with low volume, building automated enforcement (webhooks, periodic revalidation) is not justified. This manual process works.

### 5. Respond to the Customer

Template:

> Hi [name],
>
> Your refund of EUR 119 has been processed. You should see it in your account within 5-10 business days depending on your payment provider.
>
> Your license key has been deactivated. If you'd like to use Sessions Garden again in the future, you're welcome to purchase a new license at any time.
>
> Best regards,
> Sessions Garden

## Edge Cases

| Situation | Action |
|-----------|--------|
| Customer purchased more than 14 days ago | Refund at your discretion — good customer service builds reputation |
| Customer says app doesn't work | Troubleshoot first (check browser compatibility, clear cache) before refunding |
| Customer wants to transfer license to new device | Not a refund — tell them to deactivate old device from within the app, then activate on new device |
| Customer lost access to old device | Deactivate their instances in LS dashboard (step 3), then they can re-activate |

## Future Consideration

If refund volume becomes significant (>5/month), consider building:
- Cloudflare Worker webhook to auto-deactivate on LS refund event
- Periodic license revalidation check (30-day ping)

For now, manual process is the right approach.
```
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && test -f .planning/phases/18-technical-debt/REFUND-SOP.md && grep -c "Lemon Squeezy" .planning/phases/18-technical-debt/REFUND-SOP.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/18-technical-debt/REFUND-SOP.md` exists
    - Contains "Refund Handling SOP" in heading
    - Contains "Deactivate the License Key" section
    - Contains "Lemon Squeezy" at least 5 times
    - Contains "EUR 119" price reference
    - Contains email template for customer response
    - Contains edge cases table
    - Does NOT mention webhook implementation or Cloudflare Worker code (only mentions as future consideration)
  </acceptance_criteria>
  <done>Refund SOP exists with step-by-step LS dashboard instructions, customer email template, edge cases, and documented enforcement mechanism (deactivate key on refund). No code implementation — SOP only per D-09.</done>
</task>

</tasks>

<verification>
1. `grep -c "@param" assets/app.js` returns at least 15
2. `grep "Shared form helpers" assets/app.js` returns a match
3. `test -f .planning/phases/18-technical-debt/REFUND-SOP.md` exits 0
4. `grep -c "Deactivate" .planning/phases/18-technical-debt/REFUND-SOP.md` returns at least 3
</verification>

<success_criteria>
- All public App functions have JSDoc with @param/@returns
- Functions grouped by concern with section headers
- No behavioral changes to app.js
- REFUND-SOP.md exists with complete step-by-step procedure
- SOP is written for non-technical person (Sapir)
</success_criteria>

<output>
After completion, create `.planning/phases/18-technical-debt/18-03-SUMMARY.md`
</output>
