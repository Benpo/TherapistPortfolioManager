---
phase: 12-launch-prerequisites
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified: []
autonomous: false
requirements:
  - LNCH-06

must_haves:
  truths:
    - "The app opens and functions correctly in Chrome, Firefox, Safari, and Edge"
    - "Hebrew RTL layout has no breakage across all pages"
    - "The layout is usable on mobile viewport"
    - "Dark mode works correctly across all pages"
    - "All 4 languages display correctly in both app and landing page"
    - "No TODO placeholders remain in visible UI"
  artifacts:
    - path: ".planning/phases/12-launch-prerequisites/12-04-QA-CHECKLIST.md"
      provides: "Structured QA checklist for Sapir to execute"
      min_lines: 50
  key_links:
    - from: "QA checklist"
      to: "Sapir's test execution"
      via: "Manual testing on MacBook + Windows PC + iPhones"
      pattern: "checklist"
---

<objective>
Create a comprehensive QA checklist and guide Sapir through manual testing across all browsers, devices, and languages. Fix any bugs found.

Purpose: This is the final quality gate before launch. The app and landing page must work correctly everywhere. Claude creates the checklist, Sapir tests, Claude fixes any issues found.

Output: QA checklist document + all bugs fixed
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-launch-prerequisites/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive QA checklist</name>
  <files>.planning/phases/12-launch-prerequisites/12-04-QA-CHECKLIST.md</files>
  <action>
Create a structured QA checklist document that Sapir can follow step-by-step. Organize by test area, not by browser (test one area across all browsers before moving on — less context switching).

Structure:

**Devices and Browsers:**
- MacBook: Chrome, Safari
- Windows PC: Chrome, Firefox, Edge
- iPhone: Safari

**Test Areas (each with checkboxes):**

1. **Landing Page — Visual and Content**
   - Page loads, hero visible, aurora animation plays
   - All sections render (features, screenshots, pricing, contact, footer)
   - Impressum shows real business details (not placeholder)
   - Datenschutzerklarung shows full privacy policy
   - Buy buttons work (link to Lemon Squeezy checkout)
   - Dark mode toggle works, all sections readable in dark mode
   - Language switcher: EN, HE, DE, CS — all text changes, no missing strings
   - Hebrew: full RTL layout, no overlapping or misaligned elements
   - Mobile: responsive layout, no horizontal scroll, touch targets adequate

2. **App — Core Flow**
   - License key entry works (or skip if testing locally)
   - Disclaimer acceptance flow works
   - Add client: fill all fields, save, appears in list
   - Add session: fill all fields including Heart Shield toggle, save
   - Edit client: change fields, save, changes persist
   - Photo upload and crop/reposition works
   - Client search filters correctly
   - Session history shows correct data
   - Heart Shield icon appears for relevant clients
   - Session type filter (all/Heart Shield/regular) works
   - Backup export (ZIP) downloads successfully
   - Backup import restores data

3. **App — Language and RTL**
   - Switch to each language: EN, HE, DE, CS
   - All UI text updates (no English fallbacks in DE/CS/HE)
   - Hebrew: RTL layout correct on all pages
   - Check tooltips appear in correct language

4. **App — Dark Mode**
   - Toggle dark mode on each page
   - All text readable (no white-on-white or black-on-black)
   - Botanical decorations visible in dark mode
   - Logo visible in dark mode

5. **App — Mobile**
   - iPhone Safari: app loads, navigation works
   - Add client and add session forms usable on small screen
   - Photo crop works on touch
   - Tables scroll horizontally if needed (not cut off)

6. **PWA / Offline**
   - "Add to Home Screen" prompt appears or manual add works
   - App icon shows (not generic browser icon)
   - After adding to home screen, open — loads without browser chrome
   - Turn off internet — app still loads and functions

For each test area, include:
- Clear pass/fail checkbox
- What to look for (expected behavior)
- "If broken" note — what to report (screenshot + browser + description)

Keep instructions extremely clear and non-technical — Sapir said she feels overwhelmed by technical things.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-launch-prerequisites/12-04-QA-CHECKLIST.md && wc -l .planning/phases/12-launch-prerequisites/12-04-QA-CHECKLIST.md | awk '{if ($1 >= 50) print "Checklist has sufficient detail ("$1" lines)"; else {print "FAIL: only "$1" lines"; exit 1}}'</automated>
  </verify>
  <done>
    - QA checklist exists with all 6 test areas
    - Each area has clear checkboxes and expected behavior
    - Instructions are in plain, non-technical language
    - "If broken" reporting guidance included
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sapir executes QA checklist and reports results</name>
  <files>.planning/phases/12-launch-prerequisites/12-04-QA-CHECKLIST.md</files>
  <action>
Present the QA checklist to Sapir. Wait for her to test across her devices (MacBook Chrome/Safari, Windows PC Chrome/Firefox/Edge, iPhone Safari). Fix any bugs she reports.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — Sapir confirms QA results"</automated>
  </verify>
  <done>Sapir has tested all areas and either all passed or all reported bugs have been fixed</done>
</task>

</tasks>

<verification>
- QA checklist document created and comprehensive
- Sapir has executed the checklist
- All reported bugs are fixed
- Final state: app works across all target browsers, devices, and languages
</verification>

<success_criteria>
A structured QA checklist has been created and executed by Sapir. All reported issues have been fixed. The app and landing page work correctly across Chrome, Firefox, Safari, Edge, mobile viewports, RTL Hebrew, dark mode, and all 4 languages.
</success_criteria>

<output>
After completion, create `.planning/phases/12-launch-prerequisites/12-04-SUMMARY.md`
</output>
