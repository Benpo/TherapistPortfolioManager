---
phase: 25-backup-architectural-rework
plan: 04
type: execute
wave: 4
depends_on: [25-02, 25-03]
files_modified:
  - assets/overview.js
  - assets/app.css
  - assets/backup.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/25-04-cloud-state.test.js
  - tests/25-04-schedule-interval.test.js
  - tests/25-04-banner-suppression.test.js
  - assets/app.js
autonomous: true
requirements: [D-08, D-13, D-14, D-15, D-19, D-27, D-28, D-30]
tags: [backup, header-icon, cloud-color, schedule-aware, i18n]

must_haves:
  truths:
    - "The header cloud icon button #backupCloudBtn (mounted by Plan 02 via App.mountBackupCloudButton) has its background color updated to encode last-backup recency on every relevant event (page load, post-export hook, post-import hook, visibilitychange === 'visible', schedule-change callback)."
    - "Color state class on #backupCloudBtn is one of: backup-cloud-btn--never / --fresh / --warning / --danger — exactly one at a time."
    - "Icon state is derived from getChipState(now, lastExportMs, scheduleIntervalMs) — a pure function exported on BackupManager for unit testing. (Function name retained from earlier draft for code-stability; it is the cloud-icon state derivation function regardless of name.)"
    - "Icon click opens the Backup & Restore modal via window.openBackupModal() — the click handler ships in Plan 02; this plan does not re-wire it."
    - "Icon's title attribute reads localized 'Last backup · {relative time}' (or 'Last backup · never') and is updated alongside the state class on every update tick — the textual a11y-safe equivalent of the color signal (D-13)."
    - "When schedule mode is OFF (no portfolioBackupScheduleMode key, or value 'off'): green ≤7 days, warning >7 ≤14, danger >14, never if no portfolioLastExport key."
    - "When schedule mode is ON (interval is a positive number ms via the shared BackupManager.getScheduleIntervalMs helper): green ≤ interval, warning > interval AND ≤ interval×2, danger > interval×2."
    - "BackupManager exposes getScheduleIntervalMs() (single source of truth used by icon updater + schedule fire + banner suppression — D-30)."
    - "BackupManager.computeBackupRecencyState (the Plan-02 stub) is REPLACED in this plan with a one-line delegation: `return getChipState({ now: Date.now(), lastExport: Number(localStorage.getItem('portfolioLastExport')), intervalMs: getScheduleIntervalMs() });` — so the icon's mount-time state class and the post-mount update tick share the same logic (D-30)."
    - "When schedule mode is ON, the existing 7-day banner at app.js:737 (checkBackupReminder) early-returns (D-15 / D-19)."
    - "The banner-suppression behavior is proven by tests/25-04-banner-suppression.test.js — schedule ON: showBackupBanner is NOT invoked; schedule OFF + last-export >7 days ago: showBackupBanner IS invoked. Closes the D-15/D-19 behavior gap per project memory feedback-behavior-verification.md."
    - "NO chip element exists anywhere in the DOM. The cloud icon's background color IS the status surface (D-13 updated 2026-05-15)."
  artifacts:
    - path: "assets/backup.js"
      provides: "getScheduleIntervalMs + getChipState helpers exposed on public API; computeBackupRecencyState body REPLACED to delegate to getChipState (single source of truth)"
      exports: ["getScheduleIntervalMs", "getChipState"]
    - path: "assets/app.js"
      provides: "App.updateBackupCloudState(buttonEl) — reads computeBackupRecencyState (which delegates to getChipState), removes all four .backup-cloud-btn--{state} classes from buttonEl, adds the matching one, updates buttonEl.title with the localized 'Last backup · {relative time}' string. Also: checkBackupReminder() — early-return when getScheduleIntervalMs() returns a positive number (D-15/D-19); checkBackupReminder + showBackupBanner exposed as test seams on the App return object."
    - path: "assets/overview.js"
      provides: "Wires updateBackupCloudState invocation into the post-export and post-import flows (already calling renderLastBackupSubtitle at those points); adds visibilitychange listener that calls updateBackupCloudState when the page becomes visible"
    - path: "assets/app.css"
      provides: "The four state-color modifier classes .backup-cloud-btn--{never|fresh|warning|danger} appended to the Phase 25 section. Plan 02 shipped the base .header-icon-btn / .backup-cloud-btn / .backup-cloud-btn--never default — this plan refines the --never tokens to match the four-state palette and adds the other three."
    - path: "tests/25-04-cloud-state.test.js"
      provides: "getChipState pure-function table test covering all OFF/ON × never/fresh/warning/danger combinations. Filename was tests/25-04-chip-state.test.js in the prior draft — RENAMED to reflect the cloud-icon surface (no chip element exists anymore). Test body asserts state STRING values from getChipState — same semantics, just no DOM chip-class assertions."
    - path: "tests/25-04-schedule-interval.test.js"
      provides: "getScheduleIntervalMs returns null/expected ms for each mode (off/daily/weekly/monthly/custom)"
    - path: "tests/25-04-banner-suppression.test.js"
      provides: "Behavior test for D-15/D-19 — loads assets/app.js in vm sandbox with BackupManager + showBackupBanner spies; asserts checkBackupReminder early-returns when getScheduleIntervalMs() returns a positive number, and still fires the banner in OFF + stale path. This is the falsifiable behavior gate for D-15/D-19 per project memory feedback-behavior-verification.md and matches VALIDATION.md (line 51) verbatim."
  key_links:
    - from: "#backupCloudBtn (header)"
      to: "App.updateBackupCloudState(#backupCloudBtn)"
      via: "called on page-load (DOMContentLoaded after mount), post-export, post-import, visibilitychange, and schedule-change"
      pattern: "updateBackupCloudState\\("
    - from: "App.updateBackupCloudState"
      to: "BackupManager.computeBackupRecencyState (delegating to getChipState + getScheduleIntervalMs)"
      via: "internal call inside the updater"
      pattern: "computeBackupRecencyState\\("
    - from: "BackupManager.computeBackupRecencyState (Plan-02 stub REPLACED here)"
      to: "BackupManager.getChipState + BackupManager.getScheduleIntervalMs"
      via: "single delegating call so the icon's mount-time state and the post-mount tick share the same logic (D-30)"
      pattern: "getChipState\\(\\{[^}]*intervalMs"
    - from: "checkBackupReminder (app.js:737)"
      to: "BackupManager.getScheduleIntervalMs"
      via: "early-return guard at top of function"
      pattern: "BackupManager\\.getScheduleIntervalMs\\(\\)"
---

<objective>
Make the header cloud icon (Plan 02 mount) actually encode last-backup recency by shipping the four state-color CSS modifier classes, the App.updateBackupCloudState updater that flips the class + the title text, the schedule-coupled threshold logic (getChipState + getScheduleIntervalMs), and the wiring that fires the updater on every relevant event. Also suppress the existing 7-day reminder banner when a schedule is active so the two channels never compete.

Purpose: D-08/D-13 (updated 2026-05-15) replaced the earlier "passive chip on overview" pattern with "color the cloud icon's background." One tap target, one status surface, no separate chip element. D-14 couples the color thresholds to schedule state. D-15/D-19 make the banner-and-schedule never compete: icon color remains visible across both modes, banner suppresses when schedule is on. D-30 demands a single-source-of-truth helper for schedule interval — the icon updater, the schedule fire (Plan 05), the banner suppression, AND the Plan-02 mount-time class assignment all consume the same `getScheduleIntervalMs` (via `computeBackupRecencyState` which delegates to `getChipState`).

Output: Two new pure helpers on BackupManager (getScheduleIntervalMs, getChipState), a one-line replacement of the Plan-02 computeBackupRecencyState stub body to delegate to those helpers, the App.updateBackupCloudState updater in app.js, the four state-color CSS modifier classes, the visibilitychange + post-export + post-import wiring in overview.js, the banner-suppression early-return in app.js, three unit tests (two pure-function for the helpers + one behavior test for the banner suppression), and 4 new i18n keys × 4 locales for the icon's title text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-backup-architectural-rework/25-CONTEXT.md
@.planning/phases/25-backup-architectural-rework/25-RESEARCH.md
@.planning/phases/25-backup-architectural-rework/25-UI-SPEC.md
@.planning/phases/25-backup-architectural-rework/25-VALIDATION.md
@.planning/phases/25-backup-architectural-rework/25-02-SUMMARY.md
@assets/backup.js
@assets/overview.js
@assets/app.js
@assets/app.css
@assets/i18n-en.js

<interfaces>
<!-- Existing identifiers and conventions. -->

From assets/app.js (Plan 02):
  - App.mountBackupCloudButton(headerActionsEl) — already mounts #backupCloudBtn into #headerActions before the gear, with an initial backup-cloud-btn--{state} class read from BackupManager.computeBackupRecencyState(). This plan does NOT modify the mount; it adds the post-mount updater (App.updateBackupCloudState) and wires it into the relevant events.
  - initSettingsLink (still exists; unchanged).
  - checkBackupReminder() at line 737 — current body checks portfolioBackupSnoozedUntil + portfolioLastExport vs 7 days. THIS PLAN ADDS the early-return guard at the top.
  - showBackupBanner() at line 750 — current body builds and prepends the .backup-banner DOM node. UNCHANGED.

From assets/backup.js (post-Plan-02):
  - BACKUP_CONTENTS_KEYS — the source-of-truth array
  - computeBackupRecencyState() — Plan-02 STUB body (schedule OFF only). THIS PLAN REPLACES the body with a one-line delegation to getChipState + getScheduleIntervalMs (the public API entry stays; consumers in Plan 02 — App.mountBackupCloudButton — keep working with no changes).

From assets/overview.js (post-Plan-02):
  - openExportFlow / openImportFlow — already call renderLastBackupSubtitle on success. THIS PLAN ADDS a parallel call to App.updateBackupCloudState(document.getElementById('backupCloudBtn')) at the same points.
  - formatRelativeTime — already hoisted to module top + exposed on window (Plan 02). REUSED here for the title-text format.

localStorage keys (read-only here — Plan 05 writes them):
  - portfolioLastExport (epoch ms; existing; written by triggerDownload + exportEncryptedBackup)
  - portfolioBackupScheduleMode (NEW — values 'off'|'daily'|'weekly'|'monthly'|'custom'; missing = 'off')
  - portfolioBackupScheduleCustomDays (NEW — number of days when mode='custom')

Schedule interval constants (the canonical map):
  off    → null (no interval)
  daily  → 24 * 60 * 60 * 1000
  weekly → 7 * 24 * 60 * 60 * 1000
  monthly → 30 * 24 * 60 * 60 * 1000
  custom → Number(localStorage.portfolioBackupScheduleCustomDays || 7) * 24 * 60 * 60 * 1000

Icon state derivation (pure function — for unit testability):
  getChipState({ now, lastExport, intervalMs }) returns one of:
    'never'   when lastExport is null/undefined/NaN
    'fresh'   when (now - lastExport) <= threshold_fresh
    'warning' when threshold_fresh < (now - lastExport) <= threshold_warning_max
    'danger'  when (now - lastExport) > threshold_warning_max

  Thresholds:
    schedule OFF (intervalMs is null): threshold_fresh = 7 days; threshold_warning_max = 14 days
    schedule ON (intervalMs > 0):       threshold_fresh = intervalMs; threshold_warning_max = intervalMs * 2
    The "warning" zone is (threshold_fresh, threshold_warning_max]; "danger" is > threshold_warning_max. CONTEXT D-14 (updated 2026-05-15) explicitly resolves the original (interval×1.5, interval×2] gap as 'warning' — Ben confirmed 3-color intent during plan review.

  Function name retained as `getChipState` for code-stability (it is referenced from VALIDATION.md and from the Plan 04 source-of-truth chain). The function returns the cloud-icon state string regardless of name; the legacy "chip" identifier reflects the helper's heritage, not a chip DOM element.

i18n keys to ADD (4 keys × 4 locales = 16 lines). NOTE: these were originally drafted as "chip" keys but are now used by the cloud icon's title attribute. The KEY NAMES are kept under the `overview.chip.*` namespace for code-stability (they are referenced from Plan 02's mountBackupCloudButton title fallback path AND will be referenced from this plan's updateBackupCloudState). The string VALUES are unchanged.

  - overview.chip.lastBackup → "Last backup" / "גיבוי אחרון" / "Letztes Backup" / "Poslední záloha"
  - overview.chip.never      → "never" / "מעולם לא" / "nie" / "nikdy"
  - overview.chip.separator  → " · "  (literal middle-dot with surrounding spaces — same in all 4 locales; this is a typographic constant, not a translatable string)
  - overview.chip.ariaLabel  → no longer used (the cloud icon's aria-label is `overview.backupRestore` from Plan 02). REMOVED from this plan's key list — it was only meaningful when the chip was a separate clickable element. The cloud icon does not need it.

  Updated key list (3 keys × 4 locales = 12 lines):
  - overview.chip.lastBackup
  - overview.chip.never
  - overview.chip.separator

Reusable formatRelativeTime — Plan 02 hoisted it to module top in overview.js and exposed it on window. Plan 04 calls window.formatRelativeTime from inside App.updateBackupCloudState (in app.js) via a defensive `typeof window.formatRelativeTime === 'function'` guard so non-overview pages still get a sensible fallback ("never").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BackupManager.getScheduleIntervalMs + getChipState pure helpers, swap computeBackupRecencyState body to delegate to them, and write the table-driven unit tests</name>
  <read_first>
    - assets/backup.js public API at the bottom of the IIFE (return object lines ~997-1010 post-Plans 01-03)
    - assets/backup.js — the Plan-02 stub body of computeBackupRecencyState (it lives near the public-API return; this plan replaces the body, NOT the entry)
    - assets/app.js lines 730-820 (existing checkBackupReminder + showBackupBanner — to plan the suppression edit in Task 3)
    - .planning/phases/25-backup-architectural-rework/25-CONTEXT.md (D-13, D-14, D-15, D-19, D-30)
    - .planning/phases/25-backup-architectural-rework/25-RESEARCH.md "Pattern 4: Foreground schedule check" (the canonical SCHEDULE_INTERVAL_MS map and getScheduleIntervalMs implementation)
    - tests/24-05-import-validator.test.js (vm sandbox pattern)
  </read_first>
  <behavior>
    RED — write the two pure-function tests first:

    a) tests/25-04-schedule-interval.test.js
       - vm-sandbox loads assets/backup.js with a Map-backed localStorage mock.
       - Test cases (table-driven):
         | localStorage state                                              | expected getScheduleIntervalMs() return |
         | (no key set)                                                    | null                                    |
         | portfolioBackupScheduleMode='off'                               | null                                    |
         | portfolioBackupScheduleMode='daily'                             | 86400000                                |
         | portfolioBackupScheduleMode='weekly'                            | 604800000                               |
         | portfolioBackupScheduleMode='monthly'                           | 2592000000                              |
         | portfolioBackupScheduleMode='custom' (no days)                  | 7*86400000  (default 7)                 |
         | portfolioBackupScheduleMode='custom', portfolioBackupScheduleCustomDays='3' | 3*86400000               |
         | portfolioBackupScheduleMode='custom', portfolioBackupScheduleCustomDays='0' | 7*86400000  (fall back to default for invalid 0) |
         | portfolioBackupScheduleMode='nonsense'                          | null                                    |
       - Assert each case via assert.strictEqual.

    b) tests/25-04-cloud-state.test.js  (RENAMED from tests/25-04-chip-state.test.js — no chip exists anymore; the helper still drives the cloud icon's color)
       - vm-sandbox loads assets/backup.js (no localStorage interaction needed — getChipState is pure).
       - Test cases (table-driven):
         | now             | lastExport      | intervalMs   | expected state |
         | 1700000000000   | null            | null         | 'never'        |
         | 1700000000000   | undefined       | 86400000     | 'never'        |
         | 1700000000000   | NaN             | null         | 'never'        |
         | 1700000000000   | 1700000000000 - 1*day  | null  | 'fresh'        |  (OFF: 1 day elapsed, fresh ≤7d)
         | 1700000000000   | 1700000000000 - 6*day  | null  | 'fresh'        |  (OFF: 6 days elapsed, still fresh)
         | 1700000000000   | 1700000000000 - 7*day  | null  | 'fresh'        |  (OFF: exactly 7d, boundary inclusive)
         | 1700000000000   | 1700000000000 - 8*day  | null  | 'warning'      |  (OFF: >7 ≤14)
         | 1700000000000   | 1700000000000 - 14*day | null  | 'warning'      |  (OFF: exactly 14d, boundary inclusive)
         | 1700000000000   | 1700000000000 - 15*day | null  | 'danger'       |  (OFF: >14)
         | 1700000000000   | 1700000000000 - 1*day  | 7*day | 'fresh'        |  (ON weekly: 1d elapsed ≤7d interval)
         | 1700000000000   | 1700000000000 - 7*day  | 7*day | 'fresh'        |  (ON weekly: exactly interval, fresh)
         | 1700000000000   | 1700000000000 - 8*day  | 7*day | 'warning'      |  (ON weekly: 8d > 7d but ≤ 7×2=14d)
         | 1700000000000   | 1700000000000 - 11*day | 7*day | 'warning'      |  (ON weekly: 11d > 7d but ≤ 14d — was 'danger' pre-2026-05-15 D-14 fix)
         | 1700000000000   | 1700000000000 - 14*day | 7*day | 'warning'      |  (ON weekly: exactly 7×2=14d, boundary inclusive)
         | 1700000000000   | 1700000000000 - 15*day | 7*day | 'danger'       |  (ON weekly: 15d > 14d)
         | 1700000000000   | 1700000000000 - 30*day | 24*60*60*1000  | 'danger' |  (ON daily: 30d > 1d×2=2d, far beyond)
         (day = 24*60*60*1000)
       - Assert each via assert.strictEqual.

       Additional case (D-30 delegation proof):
       - After loading backup.js, assert that calling computeBackupRecencyState() with localStorage cleared returns 'never' AND that calling it after seeding portfolioLastExport=Date.now()-3*day returns 'fresh' (proves the entry still works after the body swap; same observable behavior as the Plan-02 stub for the OFF path).

    GREEN — implement in assets/backup.js inside the IIFE, BEFORE the public-API return:

    var SCHEDULE_INTERVAL_MS = {
      daily:   24 * 60 * 60 * 1000,
      weekly:  7 * 24 * 60 * 60 * 1000,
      monthly: 30 * 24 * 60 * 60 * 1000
    };

    function getScheduleIntervalMs() {
      var mode;
      try { mode = localStorage.getItem('portfolioBackupScheduleMode') || 'off'; }
      catch (_) { return null; }
      if (mode === 'off') return null;
      if (mode === 'custom') {
        var raw = Number(localStorage.getItem('portfolioBackupScheduleCustomDays'));
        var days = (raw && raw > 0) ? raw : 7;
        return days * 24 * 60 * 60 * 1000;
      }
      return SCHEDULE_INTERVAL_MS[mode] || null;
    }

    function getChipState(opts) {
      opts = opts || {};
      var now = (typeof opts.now === 'number') ? opts.now : Date.now();
      var lastExport = opts.lastExport;
      if (lastExport === null || typeof lastExport === 'undefined' || isNaN(lastExport)) {
        return 'never';
      }
      var intervalMs = (typeof opts.intervalMs === 'number') ? opts.intervalMs : null;
      var elapsed = now - Number(lastExport);
      var DAY = 24 * 60 * 60 * 1000;
      var freshLimit, warningLimit;
      if (intervalMs === null) {
        freshLimit = 7 * DAY;
        warningLimit = 14 * DAY;
      } else {
        freshLimit = intervalMs;
        warningLimit = intervalMs * 2;
      }
      if (elapsed <= freshLimit) return 'fresh';
      if (elapsed <= warningLimit) return 'warning';
      return 'danger';
    }

    SWAP the Plan-02 computeBackupRecencyState body (find its declaration; the Plan-02 stub uses the OFF-only inline math) and REPLACE the body with the single delegating line:

    function computeBackupRecencyState() {
      try {
        var raw = localStorage.getItem('portfolioLastExport');
        var lastExport = raw ? Number(raw) : null;
        return getChipState({ now: Date.now(), lastExport: lastExport, intervalMs: getScheduleIntervalMs() });
      } catch (_) {
        return 'never';
      }
    }

    Add to public API: `getScheduleIntervalMs: getScheduleIntervalMs, getChipState: getChipState,` (computeBackupRecencyState was already exposed by Plan 02 — keep the line; only the body changes).
  </behavior>
  <action>
    Create the two test files using the vm-sandbox harness from tests/24-05-import-validator.test.js. Both tests are pure-function tests — no DOM, no crypto, no JSZip needed; provide minimal `window`, `document`, `console`, `localStorage` (Map-backed) stubs.

    Implement getScheduleIntervalMs and getChipState exactly as in <behavior>. Place them BEFORE the public-API return object in backup.js, grouped with similar helpers (after isAutoBackupActive at line ~921 is a clean home, alongside the Plan-02 computeBackupRecencyState declaration).

    REPLACE the Plan-02 computeBackupRecencyState body with the one-line delegation. Keep the function name and signature; the public API line `computeBackupRecencyState: computeBackupRecencyState,` from Plan 02 stays unchanged. (D-30: one source of truth — the icon's mount-time state class in Plan 02 and the post-mount updater tick in this plan now share identical logic.)

    Add both new helpers to the public-API return object alongside existing entries (testBackupPassword, BACKUP_CONTENTS_KEYS, computeBackupRecencyState, etc.):
      `getScheduleIntervalMs: getScheduleIntervalMs,`
      `getChipState: getChipState,`

    Do NOT add EN fallbacks to the _t map for these functions — they don't surface user-facing strings.

    The boundary semantics matter: <= is fresh, > is escalation. Verify the test table treats day-7 as still fresh and day-8 as warning (OFF mode). Same boundary discipline for ON mode.
  </action>
  <verify>
    <automated>node tests/25-04-schedule-interval.test.js</automated>
    <automated>node tests/25-04-cloud-state.test.js</automated>
    <automated>grep -E "getScheduleIntervalMs\s*:\s*getScheduleIntervalMs" assets/backup.js && grep -E "getChipState\s*:\s*getChipState" assets/backup.js && grep -E "computeBackupRecencyState\s*:\s*computeBackupRecencyState" assets/backup.js</automated>
    <automated>grep -E "function getScheduleIntervalMs|function getChipState" assets/backup.js | wc -l | grep -qx 2 && echo BOTH_HELPERS_DEFINED</automated>
    <automated>grep -A 6 "function computeBackupRecencyState" assets/backup.js | grep -q "getChipState\(.*intervalMs" && echo COMPUTE_DELEGATES_TO_CHIPSTATE</automated>
  </verify>
  <acceptance_criteria>
    - tests/25-04-schedule-interval.test.js exits 0 (covers all 9 table rows).
    - tests/25-04-cloud-state.test.js exits 0 (covers all 14 table rows including boundary cases plus the D-30 delegation proof).
    - assets/backup.js exports getScheduleIntervalMs + getChipState on the public API. computeBackupRecencyState entry is unchanged (still exposed; Plan 02 already added it).
    - getScheduleIntervalMs returns null for missing key, 'off', and unrecognized values; returns the correct interval ms for daily/weekly/monthly; handles custom with default 7 when days is missing/zero/invalid.
    - getChipState is a pure function (no localStorage reads, no Date.now() calls when `now` is provided in opts).
    - Boundary semantics: <= freshLimit returns 'fresh' (day-7 in OFF mode is fresh); > freshLimit && <= warningLimit returns 'warning'; > warningLimit returns 'danger'.
    - computeBackupRecencyState body is a single delegating call to getChipState + getScheduleIntervalMs (D-30 — verified by the grep on the function body).
  </acceptance_criteria>
  <done>
    Two pure helpers exist on BackupManager and are unit-tested. computeBackupRecencyState now delegates to them — the icon's mount-time class (Plan 02) and the post-mount updater (Task 2 of this plan) share the same logic. The schedule-interval map and cloud-icon thresholds are now the SINGLE source of truth for Plan 02 + Plan 04 + Plan 05 + the app.js banner suppression. D-30 satisfied for this surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship the four cloud-icon state-color CSS modifier classes; add App.updateBackupCloudState updater in app.js; wire visibilitychange + post-export + post-import refresh in overview.js; ship the 3 i18n title-text keys</name>
  <read_first>
    - assets/app.css — the Plan-02 base classes `.header-icon-btn`, `.backup-cloud-btn`, `.backup-cloud-btn--never` (in the Phase 25 section at the bottom of the file)
    - assets/app.js — the Plan-02 mountBackupCloudButton helper (the model for state class assignment and title format)
    - assets/overview.js (post-Plan-02): formatRelativeTime declared at module top + exposed on window; renderLastBackupSubtitle; openExportFlow + openImportFlow (this plan adds App.updateBackupCloudState calls right after the existing renderLastBackupSubtitle calls in those flows)
    - assets/i18n-en.js lines 25-40 (overview.* section)
    - .planning/phases/25-backup-architectural-rework/25-UI-SPEC.md "Cloud icon color thresholds (D-13, D-14, updated 2026-05-15)" (resolved icon CSS state map) + "Component Inventory" row "Header cloud icon button"
  </read_first>
  <action>
    1. assets/app.css — APPEND to the Phase 25 section the four state-color modifier classes (Plan 02 already shipped `.header-icon-btn`, `.backup-cloud-btn`, and `.backup-cloud-btn--never` base; this plan refines `--never` if needed and adds the other three):

       /* Phase 25 — cloud icon recency state colors (D-13/D-14, updated 2026-05-15) */
       /* The button background encodes recency; the icon glyph color flips alongside to maintain WCAG AA contrast. */
       .backup-cloud-btn--never {
         background-color: var(--color-surface-subtle, var(--color-surface-alt));
         color: var(--color-text-muted);
         border-color: var(--color-border);
       }
       .backup-cloud-btn--fresh {
         background-color: var(--color-success-bg);
         color: var(--color-success-text);
         border-color: transparent;
       }
       .backup-cloud-btn--warning {
         background-color: var(--color-warning-bg);
         color: var(--color-warning-text);
         border-color: transparent;
       }
       .backup-cloud-btn--danger {
         background-color: var(--color-danger-bg);
         color: var(--color-danger-text);
         border-color: transparent;
       }

       NOTE: the `--never` rule was provisionally shipped by Plan 02; redefining it here with the same selectors and the same tokens is intentional (the cascade keeps the later definition; both are identical so behavior is unchanged whether Plan 02 shipped the rule or not). Plan 04 owns the canonical state-color map; future edits land here.

    2. assets/app.js — ADD a new function `App.updateBackupCloudState(buttonEl)` IMMEDIATELY AFTER `mountBackupCloudButton`. Mirror the same defensive guards (BackupManager may not be loaded; localStorage may be unavailable):

       /**
        * Phase 25 (D-08/D-13/D-14, updated 2026-05-15) — update the cloud icon's
        * recency state class + title text on every relevant event (page load,
        * post-export, post-import, visibilitychange, schedule-change).
        *
        * Pure DOM update; no side-effects beyond the button element passed in.
        * If buttonEl is null/missing, no-op (defensive: pages without the icon).
        *
        * D-30: state derivation routes through BackupManager.computeBackupRecencyState,
        * which delegates to getChipState + getScheduleIntervalMs. Single source of truth.
        */
       function updateBackupCloudState(buttonEl) {
         if (!buttonEl) return;
         var STATES = ['never', 'fresh', 'warning', 'danger'];
         var state = 'never';
         try {
           if (typeof BackupManager !== 'undefined' && typeof BackupManager.computeBackupRecencyState === 'function') {
             state = BackupManager.computeBackupRecencyState() || 'never';
           }
         } catch (_) { /* keep default */ }
         if (STATES.indexOf(state) === -1) state = 'never';

         // Swap the state class.
         for (var i = 0; i < STATES.length; i++) {
           buttonEl.classList.remove('backup-cloud-btn--' + STATES[i]);
         }
         buttonEl.classList.add('backup-cloud-btn--' + state);

         // Update the title text (textual a11y-safe equivalent of the color).
         var labelLastBackup = (typeof t === 'function' ? t('overview.chip.lastBackup') : '') || 'Last backup';
         var labelNever      = (typeof t === 'function' ? t('overview.chip.never')      : '') || 'never';
         var sep = ' · ';
         var relText = labelNever;
         try {
           var raw = localStorage.getItem('portfolioLastExport');
           if (raw && typeof window.formatRelativeTime === 'function') {
             var rel = window.formatRelativeTime(Number(raw));
             if (rel) relText = rel;
           }
         } catch (_) { /* keep never */ }
         buttonEl.setAttribute('title', labelLastBackup + sep + relText);
       }

       Add to the App return object: `updateBackupCloudState: updateBackupCloudState,`.

    3. assets/app.js — at the end of `initCommon` (or wherever `mountBackupCloudButton()` is now invoked), ADD a call to refresh the icon's state right after mount:

       mountBackupCloudButton();
       initSettingsLink();
       // Phase 25 D-13/D-14: refresh cloud icon state on initial load (post-mount).
       updateBackupCloudState(document.getElementById('backupCloudBtn'));

       Also, in `initCommon` (or in a sibling helper if cleaner), ADD a single visibilitychange listener — registered ONCE per page (use a static-flag guard like the existing language-event listener pattern):

       if (!initCommon._backupVisibilityListenerInstalled) {
         document.addEventListener('visibilitychange', function () {
           if (document.visibilityState === 'visible') {
             updateBackupCloudState(document.getElementById('backupCloudBtn'));
           }
         });
         document.addEventListener('app:language', function () {
           // Re-render the title text when the language switches (state class is unchanged).
           updateBackupCloudState(document.getElementById('backupCloudBtn'));
         });
         initCommon._backupVisibilityListenerInstalled = true;
       }

       Note: the Plan-02 mountBackupCloudButton already installs an `app:language` listener that re-translates the aria-label. The listener added HERE additionally refreshes the title text (which depends on the locale-formatted relative time). Two listeners on the same event are fine — both run; both are idempotent.

    4. assets/overview.js — ADD post-export + post-import refresh calls.

       In openExportFlow's success path, AFTER the existing `renderLastBackupSubtitle()` call, ADD:
         App.updateBackupCloudState(document.getElementById('backupCloudBtn'));

       In openImportFlow's success path, AFTER the existing `renderLastBackupSubtitle()` call (and before closeBackupModal()), ADD:
         App.updateBackupCloudState(document.getElementById('backupCloudBtn'));

       These two insertions ensure the cloud icon flips its color/title immediately after the user completes an Export or Import — without waiting for the next visibilitychange.

    5. assets/i18n-{en,he,de,cs}.js — add the 3 new keys per the <interfaces> table (overview.chip.lastBackup, overview.chip.never, overview.chip.separator). Place adjacent to the existing overview.* keys (around line 25-40 in EN). The overview.chip.separator value is the literal " · " (space + middle-dot + space) in all four locales — same string. Use the literal middle-dot character (U+00B7); do not introduce bidi controls.

       (The earlier draft included `overview.chip.ariaLabel` — REMOVED. The cloud icon's aria-label is `overview.backupRestore` from Plan 02; there is no chip element to label separately.)

    6. Manual smoke-flow only (NO automated DOM-class test for the icon — the pure logic is already covered by Task 1):
       - On a fresh app install: icon background is the muted "never" state (surface-subtle bg, muted-text fg, 1px border outline); title attribute reads "Last backup · never".
       - After an Export: icon flips to green fresh state (success-bg + success-text); title updates to "Last backup · moments ago" / "Last backup · 1 minute ago".
       - 8 days after the last export (simulate by editing portfolioLastExport in DevTools + visibilitychange → switch tabs and back): icon is yellow warning state (warning-bg + warning-text); title reads "Last backup · 8 days ago".
       - 15 days after the last export (OFF mode): icon is red danger state (danger-bg + danger-text); title reads "Last backup · 15 days ago".
       - With portfolioBackupScheduleMode='daily' set + portfolioLastExport 3 days ago (visibilitychange to refresh): icon should be RED — 3d > 1d interval; 3d > 1d × 2 = 2d → danger. Confirm icon is red. (This is the schedule-coupling proof.)
  </action>
  <verify>
    <automated>grep -E "function updateBackupCloudState" assets/app.js</automated>
    <automated>grep -E "updateBackupCloudState\s*:\s*updateBackupCloudState" assets/app.js && echo UPDATER_EXPOSED</automated>
    <automated>grep -E "updateBackupCloudState\(" assets/overview.js | wc -l | grep -qE "^[2-9]$|^[1-9][0-9]+$" && echo UPDATER_INVOKED_FROM_EXPORT_AND_IMPORT</automated>
    <automated>grep -E "App\.updateBackupCloudState\(\s*document\.getElementById\(\s*['\"]backupCloudBtn" assets/overview.js | wc -l | grep -qE "^[2-9]$|^[1-9][0-9]+$" && echo UPDATER_TARGETS_CLOUD_BTN</automated>
    <automated>grep -E "visibilitychange" assets/app.js && echo VISIBILITYCHANGE_LISTENER_PRESENT</automated>
    <automated>grep -E "\.backup-cloud-btn--(never|fresh|warning|danger)" assets/app.css | wc -l | grep -qE "^[4-9]$|^[1-9][0-9]+$" && echo CLOUD_STATE_CSS_PRESENT</automated>
    <automated>for L in en he de cs; do for K in "overview.chip.lastBackup" "overview.chip.never" "overview.chip.separator"; do grep -F "\"$K\":" "assets/i18n-$L.js" >/dev/null || { echo "MISSING $K in $L"; exit 1; }; done; done; echo CLOUD_TITLE_KEYS_PRESENT</automated>
    <automated>grep -v '^//\|^ *\*' assets/i18n-en.js | grep -c '"overview.chip.ariaLabel"' | grep -qx 0 && echo ARIALABEL_KEY_NOT_REINTRODUCED</automated>
  </verify>
  <acceptance_criteria>
    - assets/app.js declares `function updateBackupCloudState(buttonEl)` and exposes it on the App public API as `App.updateBackupCloudState`.
    - updateBackupCloudState removes all four `.backup-cloud-btn--{never|fresh|warning|danger}` classes from the passed element and adds the matching one based on BackupManager.computeBackupRecencyState() (which delegates to getChipState — D-30).
    - updateBackupCloudState updates the element's `title` attribute to `{labelLastBackup} · {relativeTime}` or `{labelLastBackup} · {never}`, with literal-string fallbacks if the keys are missing.
    - assets/app.js calls updateBackupCloudState after mountBackupCloudButton in initCommon (initial post-mount refresh).
    - assets/app.js installs a visibilitychange listener (idempotent via a static flag) that calls updateBackupCloudState when the page becomes visible.
    - assets/app.js installs an `app:language` listener that re-renders the title text via updateBackupCloudState (alongside the Plan-02 listener that re-renders the aria-label).
    - assets/overview.js calls App.updateBackupCloudState(document.getElementById('backupCloudBtn')) in openExportFlow's success path and in openImportFlow's success path (verified by grep counting at least 2 occurrences with the canonical target).
    - assets/app.css contains all four `.backup-cloud-btn--{never|fresh|warning|danger}` selectors using existing semantic tokens (no new color literals; var() must come first).
    - All 3 new i18n keys (overview.chip.lastBackup, overview.chip.never, overview.chip.separator) ship in 4 locales.
    - The Hebrew strings use noun forms (e.g., "גיבוי אחרון" — "Last backup" as a phrase, NOT an imperative). The "never" string in HE is "מעולם לא" (adverb).
    - The earlier draft's `overview.chip.ariaLabel` key is NOT present in any locale file (the cloud icon uses overview.backupRestore for its aria-label; the chip-aria-label key is obsolete).
    - NO chip DOM element is created anywhere in the codebase (verified manually at checker pass; grep for `class="backup-chip` should return 0 matches).
  </acceptance_criteria>
  <done>
    The header cloud icon's background color encodes last-backup recency at all times. Color flips immediately after every Export/Import (overview.js wiring) and on page visibility change (app.js listener) and on language switch (re-renders the title). The schedule-coupling logic is correct because computeBackupRecencyState delegates to getChipState + getScheduleIntervalMs — single source of truth shared by Plan 02 (mount), this plan (post-mount updater), Plan 05 (schedule fire), and Task 3 (banner suppression). 3 i18n title-text keys ship in 4 locales. NO chip element exists anywhere — the icon IS the status surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suppress the existing 7-day reminder banner when a schedule is active + ship the D-15/D-19 banner-suppression behavior test</name>
  <read_first>
    - assets/app.js lines 730-820 (the existing checkBackupReminder + showBackupBanner; the early-return guard at line 738-739 for snooze)
    - assets/backup.js post-Task-1: BackupManager.getScheduleIntervalMs is now exposed
    - .planning/phases/25-backup-architectural-rework/25-CONTEXT.md D-15, D-19
    - .planning/phases/25-backup-architectural-rework/25-RESEARCH.md "Pattern 4 § Banner suppression" — the canonical guard
    - .planning/phases/25-backup-architectural-rework/25-VALIDATION.md (line 51 — the contracted filename `tests/25-04-banner-suppression.test.js` and the contracted unit-test mechanism for D-15/D-19)
    - tests/24-04-app-cache.test.js (canonical vm-sandbox-loading-app.js pattern; the same approach is reused here — load app.js with stubs, exercise `App.checkBackupReminder` if exposed OR extract `checkBackupReminder` body via regex into a fresh vm context)
    - $HOME/.claude/projects/-Users-ben-Claude-Code-Sandbox-TherapistPortfolioManager-app/memory/feedback-behavior-verification.md (project rule: runtime-behavior code requires falsifiable behavior tests — grep gates are NOT sufficient)
  </read_first>
  <behavior>
    RED — write tests/25-04-banner-suppression.test.js FIRST. This is the load-bearing D-15/D-19 gate per VALIDATION.md (line 51) and project memory `feedback-behavior-verification.md`. The existing `<verify>` grep + node-script order checks confirm SHAPE (guard appears before snooze in source); this test confirms BEHAVIOR (the guard actually short-circuits the banner render path at runtime).

    File: tests/25-04-banner-suppression.test.js

    Sandbox setup (modeled on tests/24-04-app-cache.test.js):
      - Map-backed localStorage stub.
      - Minimal `document` stub: getElementById returns null (so showBackupBanner's "already showing" guard does NOT short-circuit it for non-suppression reasons), createElement returns a chain-able stub, body.prepend captures the banner if rendered.
      - `BackupManager` stub on the sandbox window with a controllable `getScheduleIntervalMs` — initially returns null, the test reassigns per sub-case.
      - Spy: `let bannerRendered = 0;` — incremented inside a stubbed `showBackupBanner` OR by detecting body.prepend invocation with a `.backup-banner` className. Cleanest: stub `App.showBackupBanner` after app.js loads (if it's exposed) OR replace the inner createElement('div') path so the spy fires when prepend is called with a `.backup-banner` element. Use the inner-prepend detection — it's the most behavior-faithful spy because it observes the actual DOM-write side-effect.
      - Load assets/app.js into the vm sandbox via the same `rawSrc.replace(/^App\./gm, 'window.App.')` patch used in 24-04-app-cache.test.js. This exposes `App.checkBackupReminder` either directly (if Plan 04 Task 3 also exports it) OR indirectly (the test calls `App.initCommon()` which itself calls `checkBackupReminder()` at line 499, AND the test pre-arranges localStorage + BackupManager state before initCommon runs).

      Implementation choice: the test calls `checkBackupReminder` directly. To do that, Plan 04 Task 3 must add `checkBackupReminder: checkBackupReminder` to the public App return object. This is a tiny export (no behavior change) and is the cleanest test seam. Document the export in the SUMMARY.

    Sub-case A — schedule ON: banner is NOT rendered:
      - localStorage.setItem('portfolioBackupScheduleMode', 'daily'); (or directly stub BackupManager.getScheduleIntervalMs to return 86400000)
      - localStorage.setItem('portfolioLastExport', String(Date.now() - 30 * 24 * 60 * 60 * 1000)); (30 days ago — extremely stale; in OFF mode this WOULD trigger the banner)
      - bannerRendered = 0;
      - sandbox.window.BackupManager = { getScheduleIntervalMs: () => 86400000 };
      - App.checkBackupReminder();
      - assert.strictEqual(bannerRendered, 0, 'banner must NOT render when schedule is active (D-15/D-19)');

    Sub-case B — schedule OFF + last-export >7 days ago: banner IS rendered:
      - localStorage.removeItem('portfolioBackupSnoozedUntil');
      - localStorage.setItem('portfolioLastExport', String(Date.now() - 8 * 24 * 60 * 60 * 1000)); (8 days ago)
      - bannerRendered = 0;
      - sandbox.window.BackupManager = { getScheduleIntervalMs: () => null };  (schedule OFF)
      - App.checkBackupReminder();
      - assert.strictEqual(bannerRendered, 1, 'banner MUST render when schedule is OFF and last export is stale (legacy 7-day path preserved)');

    Sub-case C — schedule OFF + recent export: banner is NOT rendered (regression guard for the legacy path):
      - localStorage.setItem('portfolioLastExport', String(Date.now() - 1 * 24 * 60 * 60 * 1000)); (1 day ago — fresh)
      - bannerRendered = 0;
      - sandbox.window.BackupManager = { getScheduleIntervalMs: () => null };
      - App.checkBackupReminder();
      - assert.strictEqual(bannerRendered, 0, 'banner must NOT render when last export is recent (legacy behavior preserved)');

    Sub-case D — defensive: BackupManager throws inside getScheduleIntervalMs → fall through to legacy snooze+stale check (banner SHOULD render if stale):
      - localStorage.removeItem('portfolioBackupSnoozedUntil');
      - localStorage.setItem('portfolioLastExport', String(Date.now() - 10 * 24 * 60 * 60 * 1000)); (10 days ago — stale)
      - bannerRendered = 0;
      - sandbox.window.BackupManager = { getScheduleIntervalMs: () => { throw new Error('boom'); } };
      - App.checkBackupReminder();
      - assert.strictEqual(bannerRendered, 1, 'try/catch defensive shape must allow legacy banner path when BackupManager throws');

    Exit 0 on full pass, 1 on any assertion failure.

    GREEN — Plan 04 Task 3's existing implementation already adds the guard (per the <action> below). The behavior test confirms the guard short-circuits at runtime. The only NEW production-code change required by this task (beyond what Plan 04 already wrote in iteration 1) is:
      - Expose `checkBackupReminder` on the App public-return object so the vm-sandbox test can call it directly. ONE new line in the App return: `checkBackupReminder: checkBackupReminder,`. No behavior change in the browser; just a test seam.
  </behavior>
  <action>
    Modify assets/app.js's checkBackupReminder function (line 737):

    ADD a guard at the very top of the function body, BEFORE the existing snooze check at line 738:

      function checkBackupReminder() {
        // D-15 / D-19: when a backup schedule is active, the scheduled interval-end prompt is the reminder.
        // The 7-day banner is suppressed entirely so the two channels never compete.
        // BackupManager may not be loaded on every page (only pages that include backup.js); guard the call.
        try {
          if (typeof BackupManager !== 'undefined' && typeof BackupManager.getScheduleIntervalMs === 'function') {
            if (BackupManager.getScheduleIntervalMs() !== null) return;
          }
        } catch (_) { /* defensive: if BackupManager throws, fall through to legacy banner behavior */ }

        const snoozedUntil = localStorage.getItem("portfolioBackupSnoozedUntil");
        if (snoozedUntil && Date.now() < Number(snoozedUntil)) return;

        const lastExport = localStorage.getItem("portfolioLastExport");
        const sevenDays = 7 * 24 * 60 * 60 * 1000;

        if (!lastExport || Date.now() - Number(lastExport) > sevenDays) {
          showBackupBanner();
        }
      }

    Do NOT modify showBackupBanner. Do NOT change the call site at line 499.

    Additionally, expose `checkBackupReminder` on the App return object (NEW — required to make the behavior test in tests/25-04-banner-suppression.test.js runnable). Add a single line to the public-return:
      `checkBackupReminder: checkBackupReminder,`
    This is a test seam ONLY — the function is still called internally at line 499; this export gives the test a stable handle.

    Then create tests/25-04-banner-suppression.test.js per the BEHAVIOR section above (four sub-cases A–D). Use the canonical vm-sandbox pattern from tests/24-04-app-cache.test.js, including the `rawSrc.replace(/^App\./gm, 'window.App.')` patch. The spy detects banner render via either: (a) intercepting `body.prepend` and checking the prepended node's className includes 'backup-banner', OR (b) replacing `App.showBackupBanner` after load with `() => { bannerRendered++; }`. Option (b) is simpler and equally faithful — use it.

    For option (b), expose `showBackupBanner` on the App return object too (alongside checkBackupReminder). Both exports are test seams, no behavior change. Document both seams in the SUMMARY.

    Behavior consequences (verify at checker pass):
    - User with no schedule (default): banner appears 7 days after last export — UNCHANGED.
    - User who turns schedule ON in Settings (Plan 05): on the next page load, the banner does NOT appear — even if more than 7 days have passed. The cloud icon color alone signals state.
    - User who turns schedule OFF again: the banner re-appears as before (snooze key may still be present from a prior dismissal — D-15 accepts this; user can dismiss again).
  </action>
  <verify>
    <automated>node tests/25-04-banner-suppression.test.js</automated>
    <automated>grep -E "BackupManager\.getScheduleIntervalMs\(\)\s*!==\s*null" assets/app.js</automated>
    <automated>grep -B 2 -A 12 "function checkBackupReminder" assets/app.js | grep -q "D-15\|D-19\|schedule" && echo SUPPRESSION_GUARD_DOCUMENTED</automated>
    <automated>grep -E "checkBackupReminder\s*:\s*checkBackupReminder" assets/app.js && grep -E "showBackupBanner\s*:\s*showBackupBanner" assets/app.js && echo TEST_SEAMS_EXPORTED</automated>
    <automated>node -e "
      const fs=require('fs');
      const src=fs.readFileSync('assets/app.js','utf8');
      const idx=src.indexOf('function checkBackupReminder');
      const body=src.slice(idx, idx+800);
      // The guard MUST appear BEFORE the snoozedUntil check
      const guardIdx=body.indexOf('getScheduleIntervalMs');
      const snoozeIdx=body.indexOf('portfolioBackupSnoozedUntil');
      if (guardIdx < 0 || snoozeIdx < 0 || guardIdx > snoozeIdx) { console.error('FAIL: guard not before snooze'); process.exit(1); }
      console.log('GUARD_BEFORE_SNOOZE');
    "</automated>
  </verify>
  <acceptance_criteria>
    - tests/25-04-banner-suppression.test.js exits 0 with FOUR sub-cases passing:
      A. schedule ON + extremely stale last-export → banner NOT rendered (D-15/D-19 enforcement).
      B. schedule OFF + 8-day-stale last-export → banner IS rendered (legacy 7-day path preserved).
      C. schedule OFF + 1-day-fresh last-export → banner NOT rendered (no false positives).
      D. BackupManager.getScheduleIntervalMs throws → try/catch falls through, legacy stale path renders banner (defensive shape verified).
    - This behavior test is the load-bearing D-15/D-19 gate per VALIDATION.md (line 51) and project memory `feedback-behavior-verification.md`. It confirms BEHAVIOR (the guard short-circuits the render path), not just SHAPE (the guard exists in source).
    - checkBackupReminder's first non-comment statement is the BackupManager.getScheduleIntervalMs() guard (verified by grep + the order check).
    - The guard is wrapped in try/catch so pages that don't load backup.js (none currently — but defensive) don't error.
    - The existing snooze-key check, the lastExport check, and showBackupBanner are unchanged below the guard.
    - showBackupBanner itself is NOT touched (no behavior regression to the banner when it DOES appear in OFF mode).
    - Two new test seams are exported on App: `checkBackupReminder` and `showBackupBanner`. These are seams only — the in-browser call sites and behavior are unchanged.
    - No new dependencies on Plan 05's schedule UI — Plan 04 closes the loop on the "what happens if portfolioBackupScheduleMode is 'weekly'" path even before Plan 05 ships the UI to set that mode (manual via localStorage works for testing).
  </acceptance_criteria>
  <done>
    Banner-suppression guard lives at the top of checkBackupReminder. Behavior test tests/25-04-banner-suppression.test.js asserts the guard short-circuits the render path at runtime (schedule ON → no banner; schedule OFF + stale → banner; defensive try/catch path verified). When Plan 05 wires the Settings frequency selector and a user picks anything other than Off, the 7-day banner is silently muted. When the user reverts to Off, the banner returns. The cloud icon remains visible and color-coded across both modes. D-15/D-19 contract from VALIDATION.md (line 51) is satisfied with a falsifiable behavior gate per project memory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                                | Description                                                                  |
|-----------------------------------------|------------------------------------------------------------------------------|
| Cloud icon click → window.openBackupModal | Plain function call; no privilege change; same as the entry-point button (wired in Plan 02; this plan does not touch the click handler) |
| localStorage reads (portfolioBackup*)   | Pure reads; the keys are written by Plan 05 (schedule UI) and the existing export flow |
| visibilitychange listener → updateBackupCloudState | Pure DOM update on a single button element; no network, no storage writes |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                                       |
|------------|------------------------|----------------------------------------|-------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-25-04-01 | T Tampering            | localStorage tampering with portfolioBackupScheduleMode to 'off' | accept | Single-user app; user can edit their own localStorage. Worst case: they suppress their own schedule — purely self-inflicted, not a multi-tenant boundary breach. |
| T-25-04-02 | I Information Disclosure | Cloud icon title text reveals "8 days ago"        | accept      | Information is the user's own; they're the only viewer of this device's overview. No cross-tenant disclosure model exists. The title is the textual a11y-safe equivalent of the color signal that Plan 02 already exposes through the icon mount — no NEW disclosure beyond the color. |
| T-25-04-03 | R Repudiation          | Banner suppression masking a missed backup | mitigate | The cloud icon's background color is the constant-visibility signal — suppressing the banner does NOT remove user awareness; it changes the channel. The icon color escalates exactly when the banner would have. |
</threat_model>

<verification>
- Both new pure-function tests pass (cloud-state + schedule-interval).
- New banner-suppression behavior test passes (4 sub-cases — schedule ON, schedule OFF stale, schedule OFF fresh, defensive throw).
- Cloud icon CSS includes all four state modifier classes.
- 3 i18n title-text keys ship in 4 locales.
- Banner-suppression guard order is enforced by an inline node script in <verify>.
- Test seams (checkBackupReminder, showBackupBanner) exported on App for the behavior test.
- Manual smoke check: switch portfolioBackupScheduleMode in DevTools to 'weekly' / 'daily' / etc. and reload — the cloud icon background color changes per the threshold table; the banner does not appear regardless of staleness; the icon's title text matches the elapsed time in the active locale.
- No chip element exists in the DOM (grep `class="backup-chip` returns 0).
</verification>

<success_criteria>
- BackupManager.getScheduleIntervalMs + getChipState are pure helpers, unit-tested, and exposed on the public API (D-30).
- BackupManager.computeBackupRecencyState (Plan-02 stub) now delegates to getChipState + getScheduleIntervalMs — single source of truth shared by mount-time class assignment AND post-mount updater.
- The header cloud icon's background color encodes recency at all times; click-opens the modal (Plan 02 wiring); refreshes on Export/Import/visibilitychange/language-change.
- Cloud icon CSS uses only existing semantic tokens — no new colors.
- The 7-day banner is suppressed iff a schedule is active (D-15/D-19); the cloud icon remains in both modes (D-13).
- The banner-suppression behavior is proven by tests/25-04-banner-suppression.test.js (the contracted D-15/D-19 gate per VALIDATION.md line 51).
- 3 new i18n keys ship in 4 locales with Hebrew noun forms.
- Plan 05 can hook into the same getScheduleIntervalMs without redefining the interval map (single source of truth).
- NO chip DOM element exists anywhere in the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/25-backup-architectural-rework/25-04-SUMMARY.md` documenting:
- The two new pure helpers + their test coverage (cloud-state + schedule-interval)
- The body swap of computeBackupRecencyState (Plan-02 stub → one-line delegation to getChipState + getScheduleIntervalMs); single-source-of-truth proof for D-30
- The cloud icon updater (App.updateBackupCloudState) and its three invocation points: post-export, post-import, visibilitychange (+ the language-event re-render of the title text)
- The CSS state map (which token feeds which state — never/fresh/warning/danger)
- The 3 i18n title-text keys with one HE example
- The banner-suppression guard placement (file:line) + the try/catch defensive shape
- The new banner-suppression behavior test (tests/25-04-banner-suppression.test.js) — four sub-cases (schedule ON, schedule OFF stale, schedule OFF fresh, defensive throw); closes the D-15/D-19 gap flagged by `feedback-behavior-verification.md` and matches VALIDATION.md (line 51) verbatim
- Test seams added to App public return: `updateBackupCloudState`, `checkBackupReminder`, `showBackupBanner` (seams + production helper; no in-browser behavior change for the latter two)
- D-30 single-source-of-truth: getScheduleIntervalMs is consumed by computeBackupRecencyState (which is consumed by mountBackupCloudButton in Plan 02 + updateBackupCloudState in this plan), by Plan 05's schedule fire, and by checkBackupReminder's banner suppression — four callers, one definition
- Renaming note: `tests/25-04-chip-state.test.js` was renamed to `tests/25-04-cloud-state.test.js` (no chip exists anymore; helper still drives the cloud icon's color). VALIDATION.md does NOT reference this filename — only `tests/25-04-banner-suppression.test.js` is contracted by name (line 51), and that filename is unchanged.
- Removal note: the previously-drafted `overview.chip.ariaLabel` i18n key was removed (the cloud icon's aria-label is `overview.backupRestore` from Plan 02; the chip-aria-label key is obsolete). The other three `overview.chip.*` keys are RETAINED under their existing namespace for code-stability — they now drive the cloud icon's title text, not a chip element.
- D-14 thresholds: warning is (interval, interval×2] and danger is > interval×2 — Ben confirmed 3-color intent 2026-05-15; CONTEXT D-14 updated to remove the original (1.5×, 2×] gap
- Hand-off to Plan 05: getScheduleIntervalMs is the function Plan 05's interval-end-prompt scheduler calls; do not redefine it. Plan 05 may also call App.updateBackupCloudState(document.getElementById('backupCloudBtn')) after a scheduled export completes if it wants the icon to refresh outside the visibilitychange tick.
</output>
</content>
</invoke>