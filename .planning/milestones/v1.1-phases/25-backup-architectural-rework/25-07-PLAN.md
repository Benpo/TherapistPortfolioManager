---
phase: 25-backup-architectural-rework
plan: 07
type: execute
wave: 6
depends_on: [25-05, 25-06]
files_modified:
  - settings.html
  - assets/settings.js
  - assets/db.js
  - assets/app.css
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/25-07-photo-bytes-estimator.test.js
  - tests/25-07-delete-all-photos.test.js
autonomous: true
requirements: [D-24, D-25, D-26, D-27, D-28, D-30]
tags: [photos, settings, storage-display, optimize, destructive, i18n]

must_haves:
  truths:
    - "settings.html's Photos tab body (shell shipped in Plan 05) is populated with: storage-usage line, optimize-all section (with savings preview), delete-all section (destructive)."
    - "Storage usage uses navigator.storage.estimate() top-level usage AND a per-photo IDB walk (estimatePhotosBytes) — both displayed when available."
    - "Optimize-all walks every client with photoData, runs each through CropModule.resizeToMaxDimension (Plan 06 helper), and writes the smaller result back via PortfolioDB.updateClient. Shows estimated savings BEFORE running. Confirm dialog uses tone:'neutral' (not destructive)."
    - "Delete-all walks every client and clears photoData (sets to ''). Client and session records remain. Confirm dialog uses tone:'danger' (destructive)."
    - "Both bulk operations show success toast (with savings count for optimize) or partial-failure toast on errors."
    - "PortfolioDB exposes updateClient(client) (existing or extended) used by both bulk operations — single-source-of-truth (D-30): same write path as the existing edit-client save."
    - "Photos tab is hidden / shows empty state when no clients have photos."
  artifacts:
    - path: "settings.html"
      provides: "Photos tabpanel content (replaces the empty shell from Plan 05)"
    - path: "assets/settings.js"
      provides: "Photos tab handlers — usage display, estimate-savings preview, optimize-all loop, delete-all loop"
    - path: "assets/db.js"
      provides: "estimatePhotosBytes(clients) helper exposed on PortfolioDB; updateClient confirmed exposed (extend if missing)"
      exports: ["estimatePhotosBytes", "updateClient (verify)"]
    - path: "tests/25-07-photo-bytes-estimator.test.js"
      provides: "estimatePhotosBytes returns correct byte count for synthetic clients with data URLs"
    - path: "tests/25-07-delete-all-photos.test.js"
      provides: "deleteAllPhotos sets every client.photoData to '' and calls updateClient for each"
    - path: "assets/i18n-{en,he,de,cs}.js"
      contains: "photos.* keys for the Photos tab"
  key_links:
    - from: "Photos tab Optimize button"
      to: "CropModule.resizeToMaxDimension(blob, 800, 0.75)"
      via: "loop over clients with photoData; convert dataURL → Blob → resize → Blob → dataURL"
      pattern: "CropModule\\.resizeToMaxDimension"
    - from: "Photos tab Delete-all button"
      to: "PortfolioDB.updateClient({ ...c, photoData: '' })"
      via: "loop over clients"
      pattern: "PortfolioDB\\.updateClient"
    - from: "Storage usage line"
      to: "navigator.storage.estimate() + PortfolioDB.estimatePhotosBytes(clients)"
      via: "Promise.all; render via humanBytes formatter"
      pattern: "navigator\\.storage\\.estimate"
---

<objective>
Add the Photos Settings tab body (storage usage + optimize-all + delete-all) so users can reclaim browser storage for photos uploaded BEFORE Phase 25's resize-on-upload landed (D-24) AND nuke all photos in one stroke if needed (D-25).

Purpose: D-24/D-25 close the loop on the storage-management story: Phase 25 makes new uploads small (Plan 06), this plan makes existing uploads small via user-triggered migration. The Delete-all option exists for users who decide they don't want to retain photos at all (e.g., before sending the device to repair). Per D-30 the same CropModule.resizeToMaxDimension powers both new-upload (Plan 06) and bulk-optimize (this plan).

Output: The Photos tab body, two new PortfolioDB helpers (estimatePhotosBytes + verify-or-extend updateClient), the settings.js Photos handlers, two regression tests, and ~12 new i18n keys × 4 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-backup-architectural-rework/25-CONTEXT.md
@.planning/phases/25-backup-architectural-rework/25-RESEARCH.md
@.planning/phases/25-backup-architectural-rework/25-UI-SPEC.md
@.planning/phases/25-backup-architectural-rework/25-05-SUMMARY.md
@.planning/phases/25-backup-architectural-rework/25-06-SUMMARY.md
@settings.html
@assets/settings.js
@assets/db.js
@assets/crop.js
@assets/i18n-en.js

<interfaces>
<!-- Existing identifiers and patterns. -->

From settings.html (post-Plan-05):
  - #settingsTabPhotos — empty shell tabpanel with just the heading
  - .settings-tabs tablist already includes settingsTabPhotosBtn

From assets/db.js (verify the writers exist; extend if missing):
  - PortfolioDB.getAllClients() -> Promise<Client[]>           (existing)
  - PortfolioDB.getClient(id) -> Promise<Client | undefined>   (existing)
  - PortfolioDB.updateClient(client) -> Promise<void>          (verify — if missing, ADD as `db.put('clients', client)` wrapper)
  - PortfolioDB.addClient(client) -> Promise<id>               (existing)
  - PortfolioDB.deleteClient(id) -> Promise<void>              (existing)

From Plan 06:
  - CropModule.resizeToMaxDimension(blob, maxEdge, quality) -> Promise<Blob>
  - The same helper that powers new uploads. Plan 07 calls it in a loop over existing clients with photoData.

From assets/app.js:
  - App.confirmDialog({ titleKey, messageKey, confirmKey, cancelKey, tone }) -> Promise<boolean>
  - App.showToast(message, key) -> void

From RESEARCH.md "Per-photo IDB walk" pattern:
  - estimatePhotosBytes(clients) sums (data URL base64 length × 0.75) across all clients with photoData

From RESEARCH.md "Storage estimate" pattern:
  - navigator.storage.estimate() returns { usage, quota, usageDetails }
  - usageDetails is Chrome-only — use top-level usage as primary; per-store breakdown via estimatePhotosBytes

humanBytes formatter (RESEARCH Pattern):
  function humanBytes(n) {
    if (n == null || isNaN(n)) return '—';
    if (n < 1024) return n + ' B';
    if (n < 1024 * 1024) return (n / 1024).toFixed(1) + ' KB';
    return (n / (1024 * 1024)).toFixed(1) + ' MB';
  }

DataURL ↔ Blob conversion utilities (need both directions for the optimize loop):
  function dataURLToBlob(dataURL) {
    var [header, b64] = dataURL.split(',');
    var mime = (header.match(/data:([^;]+)/) || [])[1] || 'image/jpeg';
    var bytes = atob(b64);
    var arr = new Uint8Array(bytes.length);
    for (var i = 0; i < bytes.length; i++) arr[i] = bytes.charCodeAt(i);
    return new Blob([arr], { type: mime });
  }
  Use App.readFileAsDataURL(blob) for Blob → dataURL (already wired in add-client.js).

i18n keys to ADD (~13 keys × 4 locales):
  - photos.heading                → "Photos" / "תמונות" / "Fotos" / "Fotky"
  - photos.helper                 → "Manage how client photos use your browser storage." / "ניהול אופן השימוש של תמונות לקוחות באחסון הדפדפן." / "Verwalten Sie, wie Klientenfotos Ihren Browser-Speicher nutzen." / "Spravujte, jak fotky klientů využívají úložiště prohlížeče."
  - photos.usage.line             → "Photos use {size} of your browser storage." / "התמונות תופסות {size} מאחסון הדפדפן." / "Fotos nutzen {size} Ihres Browser-Speichers." / "Fotky využívají {size} úložiště vašeho prohlížeče."
  - photos.usage.unavailable      → "Storage usage is not available in this browser." / "נתוני שימוש באחסון אינם זמינים בדפדפן זה." / "Speichernutzung ist in diesem Browser nicht verfügbar." / "Využití úložiště není v tomto prohlížeči k dispozici."
  - photos.optimize.heading       → "Optimize existing photos" / "מיטוב תמונות קיימות" / "Vorhandene Fotos optimieren" / "Optimalizovat existující fotky"
  - photos.optimize.helper        → "Re-process photos that were uploaded before resize-on-upload was added. New uploads are already optimized automatically." / "עיבוד מחדש של תמונות שהועלו לפני הוספת שינוי גודל בעת ההעלאה. העלאות חדשות כבר ממוטבות אוטומטית." / "Verarbeiten Sie Fotos neu, die vor der automatischen Größenanpassung hochgeladen wurden. Neue Uploads werden bereits automatisch optimiert." / "Znovu zpracujte fotky nahrané před přidáním automatické úpravy velikosti. Nové fotky se již optimalizují automaticky."
  - photos.optimize.savingsPreview → "Estimated savings: ~{size}" / "חיסכון משוער: ~{size}" / "Geschätzte Einsparung: ~{size}" / "Odhadovaná úspora: ~{size}"
  - photos.optimize.action        → "Optimize all photos" / "מיטוב כל התמונות" / "Alle Fotos optimieren" / "Optimalizovat všechny fotky"
  - photos.optimize.confirm.title → "Optimize {n} photos?" / "מיטוב {n} תמונות?" / "{n} Fotos optimieren?" / "Optimalizovat {n} fotek?"
  - photos.optimize.confirm.body  → "This rewrites every photo to a smaller size in your browser. It cannot be undone, but the visual quality stays the same. Estimated savings: ~{size}." / "פעולה זו תכתוב מחדש כל תמונה בגודל קטן יותר בדפדפן. לא ניתן לבטל, אך איכות התצוגה נשמרת. חיסכון משוער: ~{size}." / "Dies schreibt jedes Foto in kleinerer Größe in Ihrem Browser neu. Kann nicht rückgängig gemacht werden, aber die visuelle Qualität bleibt gleich. Geschätzte Einsparung: ~{size}." / "Tato akce přepíše každou fotku v menší velikosti ve vašem prohlížeči. Nelze vrátit zpět, ale vizuální kvalita zůstane stejná. Odhadovaná úspora: ~{size}."
  - photos.optimize.confirm.yes   → "Yes, optimize" / "כן, למטב" / "Ja, optimieren" / "Ano, optimalizovat"
  - photos.optimize.success       → "Photos optimized. Saved {size}." / "התמונות מוטבו. נחסכו {size}." / "Fotos optimiert. {size} eingespart." / "Fotky optimalizovány. Ušetřeno {size}."
  - photos.optimize.partialFailure → "Optimized {success} photos. {failed} could not be processed and were left unchanged." / "מוטבו {success} תמונות. {failed} לא ניתן היה לעבד ונותרו ללא שינוי." / "{success} Fotos optimiert. {failed} konnten nicht verarbeitet werden und blieben unverändert." / "Optimalizováno {success} fotek. {failed} nebylo možné zpracovat a zůstaly beze změny."
  - photos.deleteAll.heading      → "Delete all photos" / "מחיקת כל התמונות" / "Alle Fotos löschen" / "Smazat všechny fotky"
  - photos.deleteAll.helper       → "Removes every client photo from your browser. Client records and sessions are kept. Cannot be undone." / "הסרת כל תמונות הלקוחות מהדפדפן. רשומות לקוחות ומפגשים נשמרות. לא ניתן לבטל." / "Entfernt jedes Klientenfoto aus Ihrem Browser. Klientendatensätze und Sitzungen bleiben erhalten. Kann nicht rückgängig gemacht werden." / "Odstraní všechny fotky klientů z prohlížeče. Záznamy klientů a sezení zůstanou. Nelze vrátit zpět."
  - photos.deleteAll.action       → "Delete all photos" / "מחיקת כל התמונות" / "Alle Fotos löschen" / "Smazat všechny fotky"
  - photos.deleteAll.confirm.title → "Delete all photos?" / "מחיקת כל התמונות?" / "Alle Fotos löschen?" / "Smazat všechny fotky?"
  - photos.deleteAll.confirm.body → "This permanently removes every client photo from this browser. Client and session records are kept. Cannot be undone." / "פעולה זו תסיר לצמיתות כל תמונת לקוח מדפדפן זה. רשומות לקוחות ומפגשים נשמרות. לא ניתן לבטל." / "Dies entfernt dauerhaft jedes Klientenfoto aus diesem Browser. Klienten- und Sitzungsdatensätze bleiben erhalten. Kann nicht rückgängig gemacht werden." / "Tato akce trvale odstraní všechny fotky klientů z tohoto prohlížeče. Záznamy klientů a sezení zůstanou. Nelze vrátit zpět."
  - photos.deleteAll.confirm.yes  → "Yes, delete all photos" / "כן, למחוק את כל התמונות" / "Ja, alle Fotos löschen" / "Ano, smazat všechny fotky"
  - photos.deleteAll.success      → "All photos deleted." / "כל התמונות נמחקו." / "Alle Fotos gelöscht." / "Všechny fotky smazány."
  - photos.empty                  → "You have no client photos yet." / "עדיין אין לך תמונות לקוחות." / "Sie haben noch keine Klientenfotos." / "Zatím nemáte žádné fotky klientů."

(That is 21 keys, not 13 — the count grew during writing. Verify all in the test gate.)

Hebrew strings: noun/infinitive throughout. "מיטוב" (noun), "מחיקה" (noun), "למטב"/"למחוק" (infinitive — acceptable per D-27 since infinitive is permitted alongside noun forms).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PortfolioDB.estimatePhotosBytes + verify/extend PortfolioDB.updateClient + write the two regression tests</name>
  <read_first>
    - assets/db.js full file (especially the public API exposure block — find via grep `window.PortfolioDB`)
    - .planning/phases/25-backup-architectural-rework/25-RESEARCH.md "Per-photo IDB walk" code example
    - tests/24-05-import-validator.test.js (vm sandbox pattern)
  </read_first>
  <behavior>
    Step 0 — verify updateClient. Open assets/db.js and grep for `updateClient` and `addClient`:
    - If `updateClient` already exists and uses an idempotent put: nothing to do.
    - If only `addClient` exists (which is `db.add('clients', c)` — would FAIL on existing id): ADD `updateClient(client)` as `db.put('clients', client)` and expose it on `window.PortfolioDB`.
    - If a different naming exists (e.g., `saveClient` or `putClient`): use the existing name and document the choice in the SUMMARY.

    RED — write the two tests first:

    a) tests/25-07-photo-bytes-estimator.test.js
       - vm-sandbox loads assets/db.js (provide minimal `indexedDB` stub — actually estimatePhotosBytes is PURE: it iterates an array of client objects, no IDB call. The test DOES NOT need indexedDB at all.)
       - Implementation: PortfolioDB.estimatePhotosBytes(clientsArray) returns the sum of (b64Length * 0.75) for every client whose photoData (or photo) starts with 'data:'.
       - Test cases:
         | clients                                                                   | expected bytes |
         | []                                                                        | 0              |
         | [{ photoData: null }]                                                     | 0              |
         | [{ photoData: 'data:image/jpeg;base64,AAAA' }]                            | 3              |  (4 chars × 0.75 = 3)
         | [{ photoData: 'data:image/jpeg;base64,' + 'A'.repeat(100) }]              | 75             |
         | [{ photoData: 'data:image/jpeg;base64,AAAA' }, { photoData: 'data:image/jpeg;base64,' + 'A'.repeat(100) }] | 78 |
         | [{ photo: 'data:image/png;base64,AAAA' }]                                 | 3              |  (handles legacy `photo` field too)
         | [{ photoData: 'photos/client-1.png' }]                                    | 0              |  (string but not data:)
         | [{ photoData: 12345 }]                                                    | 0              |  (non-string)

    b) tests/25-07-delete-all-photos.test.js
       - This is a behavior test for the bulk delete loop (which lives in settings.js, not db.js). The test verifies the LOOP shape, not settings.js's UI handler.
       - Define a pure helper INSIDE settings.js: `async function _deleteAllPhotosLoop(getAllClients, updateClient)` — accepts function dependencies for testability.
       - Loop: const clients = await getAllClients(); for each c with photoData → call updateClient({ ...c, photoData: '' }); return { success, failed }.
       - Expose for tests: `window.__PhotosTabHelpers = { _deleteAllPhotosLoop };` (mirror the Phase 24 `__SnippetEditorHelpers` pattern from settings.js).
       - Test:
         - vm-sandbox loads assets/settings.js (with minimal stubs).
         - Build clients = [{id:1, photoData:'data:image/jpeg;base64,AAAA'}, {id:2, photoData:''}, {id:3, photoData:'data:image/jpeg;base64,BBBB'}].
         - Call _deleteAllPhotosLoop(async()=>clients, async(c)=>{updates.push(c)}).
         - Assert: updates.length === 2 (only clients 1 and 3 had photos to clear).
         - Assert: updates[0].id === 1 && updates[0].photoData === '' && updates[1].id === 3 && updates[1].photoData === ''.
         - Assert: result.success === 2 && result.failed === 0.

    GREEN — implement:

    1. assets/db.js — add the helper and (if missing) updateClient:
       function estimatePhotosBytes(clients) {
         var total = 0;
         if (!Array.isArray(clients)) return 0;
         for (var i = 0; i < clients.length; i++) {
           var c = clients[i] || {};
           var photo = c.photoData || c.photo;
           if (typeof photo !== 'string') continue;
           if (!photo.startsWith('data:')) continue;
           var commaIdx = photo.indexOf(',');
           if (commaIdx < 0) continue;
           var b64 = photo.slice(commaIdx + 1);
           total += Math.floor(b64.length * 0.75);
         }
         return total;
       }
       (Optional updateClient if missing): function updateClient(client) { return _withDB(db => db.put('clients', client)); }
       Add to public API: `estimatePhotosBytes: estimatePhotosBytes,` (and `updateClient: updateClient,` if added).

    2. assets/settings.js — add the testable loop helper inside the Photos-tab IIFE (Task 2 wires the UI):
       async function _deleteAllPhotosLoop(getAllClients, updateClient) {
         var success = 0, failed = 0;
         var clients = await getAllClients();
         for (var i = 0; i < clients.length; i++) {
           var c = clients[i];
           if (!c || !c.photoData) continue;  // skip clients without photos
           try {
             var updated = Object.assign({}, c, { photoData: '' });
             await updateClient(updated);
             success++;
           } catch (_) { failed++; }
         }
         return { success: success, failed: failed };
       }

       async function _optimizeAllPhotosLoop(getAllClients, updateClient, resize, blobToDataURL, dataURLToBlob) {
         var success = 0, failed = 0, savedBytes = 0;
         var clients = await getAllClients();
         for (var i = 0; i < clients.length; i++) {
           var c = clients[i];
           if (!c || typeof c.photoData !== 'string' || !c.photoData.startsWith('data:')) continue;
           try {
             var origBytes = Math.floor((c.photoData.split(',')[1] || '').length * 0.75);
             var inBlob = dataURLToBlob(c.photoData);
             var outBlob = await resize(inBlob, 800, 0.75);
             var outDataURL = await blobToDataURL(outBlob);
             var newBytes = Math.floor((outDataURL.split(',')[1] || '').length * 0.75);
             // Only persist if it actually got smaller — avoid bloating already-optimized photos
             if (newBytes < origBytes) {
               var updated = Object.assign({}, c, { photoData: outDataURL });
               await updateClient(updated);
               savedBytes += (origBytes - newBytes);
               success++;
             }
           } catch (_) { failed++; }
         }
         return { success: success, failed: failed, savedBytes: savedBytes };
       }

       window.__PhotosTabHelpers = { _deleteAllPhotosLoop: _deleteAllPhotosLoop, _optimizeAllPhotosLoop: _optimizeAllPhotosLoop };
  </behavior>
  <action>
    Implement step 0-2 above. Place the new estimatePhotosBytes (and updateClient if needed) inside the existing PortfolioDB IIFE in assets/db.js, near the existing read helpers.

    Place the two _PhotosTab loop helpers inside a new IIFE in assets/settings.js (near the bottom, alongside Plan 05's Backups-tab IIFE). Expose via `window.__PhotosTabHelpers` for the test.

    Create the two test files using the vm-sandbox pattern. The estimator test needs zero IDB stubs (pure function). The delete-loop test mocks getAllClients + updateClient as injected function dependencies, so no IDB stub is needed.
  </action>
  <verify>
    <automated>node tests/25-07-photo-bytes-estimator.test.js</automated>
    <automated>node tests/25-07-delete-all-photos.test.js</automated>
    <automated>grep -E "function estimatePhotosBytes" assets/db.js</automated>
    <automated>grep -E "estimatePhotosBytes\s*:\s*estimatePhotosBytes" assets/db.js</automated>
    <automated>grep -E "function _deleteAllPhotosLoop|function _optimizeAllPhotosLoop" assets/settings.js | wc -l | grep -qx 2 && echo BOTH_LOOPS_DEFINED</automated>
    <automated>grep -E "window\.__PhotosTabHelpers" assets/settings.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/25-07-photo-bytes-estimator.test.js exits 0 (covers all 8 table rows including legacy `photo` field handling and non-data: strings).
    - tests/25-07-delete-all-photos.test.js exits 0 (verifies loop only processes clients with photoData, sets photoData:'', and counts success/failed).
    - PortfolioDB.estimatePhotosBytes is exposed on the public API.
    - PortfolioDB.updateClient exists (either pre-existing OR added with verification — document in SUMMARY which case applied).
    - settings.js exposes _deleteAllPhotosLoop AND _optimizeAllPhotosLoop via window.__PhotosTabHelpers (mirror of the existing __SnippetEditorHelpers pattern from Phase 24).
    - The optimize loop only writes when the new size is < original size (avoids bloating already-optimized photos that re-encode larger).
  </acceptance_criteria>
  <done>
    Two pure helpers + two testable loop functions exist. Both bulk operations are unit-testable via injected function dependencies. The same CropModule.resizeToMaxDimension powers Plan 06's new uploads AND Plan 07's bulk optimize (D-30 single-source-of-truth).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the Photos tab UI body, wire usage display + optimize-all + delete-all handlers, ship the i18n keys</name>
  <read_first>
    - settings.html (post-Plan-05): the empty #settingsTabPhotos shell
    - assets/settings.js (post-Plan-05 + Task 1): existing patterns + the _PhotosTabHelpers IIFE
    - assets/app.css existing .button.danger, .form-field, .helper-text patterns
    - .planning/phases/25-backup-architectural-rework/25-UI-SPEC.md "Settings — Photos tab" copywriting + "Component Inventory" row "Settings: Photos tab"
    - .planning/phases/25-backup-architectural-rework/25-CONTEXT.md (D-24, D-25, D-26)
  </read_first>
  <action>
    1. settings.html — REPLACE the empty #settingsTabPhotos body (currently just a heading from Plan 05) with the full body:

       <div id="settingsTabPhotos" role="tabpanel" class="settings-tabpanel" aria-labelledby="settingsTabPhotosBtn" hidden>
         <header class="page-header">
           <h3 class="section-title" data-i18n="photos.heading">Photos</h3>
           <p class="page-helper section-helper" data-i18n="photos.helper">Manage how client photos use your browser storage.</p>
         </header>

         <p class="photos-storage-usage helper-text" id="photosStorageUsage" data-i18n="photos.usage.unavailable">Storage usage is not available in this browser.</p>

         <p class="photos-empty helper-text is-hidden" id="photosEmpty" data-i18n="photos.empty">You have no client photos yet.</p>

         <section class="form-field" id="photosOptimizeSection">
           <h4 class="settings-subsection-title" data-i18n="photos.optimize.heading">Optimize existing photos</h4>
           <p class="helper-text" data-i18n="photos.optimize.helper">Re-process photos that were uploaded before resize-on-upload was added. New uploads are already optimized automatically.</p>
           <p class="helper-text photos-savings-preview" id="photosOptimizePreview" hidden></p>
           <button type="button" class="button" id="photosOptimizeBtn" data-i18n="photos.optimize.action">Optimize all photos</button>
         </section>

         <section class="form-field" id="photosDeleteAllSection">
           <h4 class="settings-subsection-title" data-i18n="photos.deleteAll.heading">Delete all photos</h4>
           <p class="helper-text" data-i18n="photos.deleteAll.helper">Removes every client photo from your browser. Client records and sessions are kept. Cannot be undone.</p>
           <button type="button" class="button danger" id="photosDeleteAllBtn" data-i18n="photos.deleteAll.action">Delete all photos</button>
         </section>
       </div>

    2. assets/settings.js — extend the _PhotosTabHelpers IIFE (or create a sibling block) to wire the UI:

       (function () {
         function $(id) { return document.getElementById(id); }

         function humanBytes(n) {
           if (n == null || isNaN(n)) return '—';
           if (n < 1024) return n + ' B';
           if (n < 1024 * 1024) return (n / 1024).toFixed(1) + ' KB';
           return (n / (1024 * 1024)).toFixed(1) + ' MB';
         }

         function dataURLToBlob(dataURL) {
           var commaIdx = dataURL.indexOf(',');
           var header = dataURL.slice(0, commaIdx);
           var b64 = dataURL.slice(commaIdx + 1);
           var mime = (header.match(/data:([^;]+)/) || [])[1] || 'image/jpeg';
           var bytes = atob(b64);
           var arr = new Uint8Array(bytes.length);
           for (var i = 0; i < bytes.length; i++) arr[i] = bytes.charCodeAt(i);
           return new Blob([arr], { type: mime });
         }
         function blobToDataURL(blob) {
           return new Promise(function (resolve, reject) {
             var fr = new FileReader();
             fr.onload = function () { resolve(fr.result); };
             fr.onerror = function () { reject(new Error('FileReader failed')); };
             fr.readAsDataURL(blob);
           });
         }

         async function refreshPhotosTab() {
           var usageEl = $('photosStorageUsage');
           var emptyEl = $('photosEmpty');
           var optimizeBtn = $('photosOptimizeBtn');
           var deleteAllBtn = $('photosDeleteAllBtn');
           var previewEl = $('photosOptimizePreview');
           var optimizeSection = $('photosOptimizeSection');
           var deleteSection = $('photosDeleteAllSection');
           if (!usageEl) return; // not on this page

           var clients = [];
           try { clients = await PortfolioDB.getAllClients(); } catch (_) {}

           var photoBytes = PortfolioDB.estimatePhotosBytes(clients);
           var hasPhotos = photoBytes > 0;

           if (emptyEl && optimizeSection && deleteSection) {
             if (hasPhotos) {
               emptyEl.classList.add('is-hidden');
               optimizeSection.removeAttribute('hidden');
               deleteSection.removeAttribute('hidden');
             } else {
               emptyEl.classList.remove('is-hidden');
               optimizeSection.setAttribute('hidden', '');
               deleteSection.setAttribute('hidden', '');
             }
           }

           // Storage usage line: prefer photo-specific number; fall back to navigator.storage.estimate top-level if photoBytes is 0 but estimate is available
           var displayBytes = photoBytes;
           if (navigator.storage && typeof navigator.storage.estimate === 'function') {
             try {
               var est = await navigator.storage.estimate();
               // For the line "Photos use {size}", photoBytes is the better number; the navigator total is shown only as fallback.
               if (!hasPhotos && est && typeof est.usage === 'number') {
                 displayBytes = est.usage; // total app storage, not photo-only
               }
             } catch (_) {}
           }

           if (hasPhotos) {
             usageEl.removeAttribute('data-i18n');
             usageEl.textContent = App.t('photos.usage.line').replace('{size}', humanBytes(displayBytes));
             // Estimated savings preview (heuristic: assume 60% reduction; only show if photoBytes > 100KB threshold)
             if (previewEl && photoBytes > 100 * 1024) {
               var estimated = Math.floor(photoBytes * 0.6);
               previewEl.removeAttribute('hidden');
               previewEl.textContent = App.t('photos.optimize.savingsPreview').replace('{size}', '~' + humanBytes(estimated));
             }
           } else {
             usageEl.setAttribute('data-i18n', 'photos.usage.unavailable');
             usageEl.textContent = App.t('photos.usage.unavailable');
             if (previewEl) previewEl.setAttribute('hidden', '');
           }
         }

         async function handleOptimize() {
           var btn = $('photosOptimizeBtn');
           if (!btn) return;
           var clients = [];
           try { clients = await PortfolioDB.getAllClients(); } catch (_) {}
           var photoBytes = PortfolioDB.estimatePhotosBytes(clients);
           if (photoBytes === 0) { App.showToast('', 'photos.empty'); return; }

           var photoCount = clients.filter(function (c) { return c && typeof c.photoData === 'string' && c.photoData.startsWith('data:'); }).length;
           var estimated = Math.floor(photoBytes * 0.6); // heuristic preview

           var confirmed = await App.confirmDialog({
             titleKey: 'photos.optimize.confirm.title',
             messageKey: 'photos.optimize.confirm.body',
             confirmKey: 'photos.optimize.confirm.yes',
             cancelKey: 'confirm.cancel',
             tone: 'neutral'  // UI-SPEC A7: neutral, not danger — non-destructive but irreversible
           });
           if (!confirmed) return;

           btn.disabled = true;
           try {
             var result = await window.__PhotosTabHelpers._optimizeAllPhotosLoop(
               function () { return PortfolioDB.getAllClients(); },
               function (c) { return PortfolioDB.updateClient(c); },
               function (b, m, q) { return CropModule.resizeToMaxDimension(b, m, q); },
               blobToDataURL,
               dataURLToBlob
             );
             var msgKey = (result.failed > 0) ? 'photos.optimize.partialFailure' : 'photos.optimize.success';
             var msg = App.t(msgKey)
               .replace('{success}', String(result.success))
               .replace('{failed}', String(result.failed))
               .replace('{size}', humanBytes(result.savedBytes));
             App.showToast(msg, '');
             await refreshPhotosTab();
           } catch (err) {
             console.error('Optimize all failed:', err);
             App.showToast('', 'toast.errorGeneric');
           } finally {
             btn.disabled = false;
           }
         }

         async function handleDeleteAll() {
           var btn = $('photosDeleteAllBtn');
           if (!btn) return;
           var confirmed = await App.confirmDialog({
             titleKey: 'photos.deleteAll.confirm.title',
             messageKey: 'photos.deleteAll.confirm.body',
             confirmKey: 'photos.deleteAll.confirm.yes',
             cancelKey: 'confirm.cancel',
             tone: 'danger'  // destructive
           });
           if (!confirmed) return;

           btn.disabled = true;
           try {
             var result = await window.__PhotosTabHelpers._deleteAllPhotosLoop(
               function () { return PortfolioDB.getAllClients(); },
               function (c) { return PortfolioDB.updateClient(c); }
             );
             App.showToast('', 'photos.deleteAll.success');
             await refreshPhotosTab();
           } catch (err) {
             console.error('Delete all failed:', err);
             App.showToast('', 'toast.errorGeneric');
           } finally {
             btn.disabled = false;
           }
         }

         function bindPhotosTab() {
           var optBtn = $('photosOptimizeBtn');
           var delBtn = $('photosDeleteAllBtn');
           if (!optBtn && !delBtn) return; // tab not present
           if (optBtn) optBtn.addEventListener('click', handleOptimize);
           if (delBtn) delBtn.addEventListener('click', handleDeleteAll);
           refreshPhotosTab();
         }

         document.addEventListener('DOMContentLoaded', bindPhotosTab);
       })();

    3. assets/app.css — append minimal new utility classes:
       .photos-storage-usage { font-size: 1rem; }
       .photos-savings-preview { color: var(--color-success-text, #1e6b3a); font-weight: 700; }
       .photos-empty { font-style: italic; }
       (Reuse .form-field, .helper-text, .button, .button.danger, .section-title — no new container classes needed.)

    4. assets/i18n-{en,he,de,cs}.js — add all 21 new photos.* keys (the full list from the <interfaces> table). Place them adjacent to existing toast.* / settings.* keys (around line 305-330 in EN).

       Hebrew strings: use noun/infinitive (D-27). "מיטוב" / "מחיקה" / "ניהול" — nouns. "למטב" / "למחוק" — infinitives. NOT imperatives like "מטב" / "מחק".

       The {n}, {size}, {success}, {failed} placeholders are literal substrings — handler does .replace() chain.
  </action>
  <verify>
    <automated>grep -E "id=\"photosStorageUsage\"" settings.html && grep -E "id=\"photosOptimizeBtn\"" settings.html && grep -E "id=\"photosDeleteAllBtn\"" settings.html && grep -E "id=\"photosOptimizePreview\"" settings.html && grep -E "id=\"photosEmpty\"" settings.html</automated>
    <automated>grep -E "function bindPhotosTab|function handleOptimize|function handleDeleteAll|function refreshPhotosTab" assets/settings.js | wc -l | grep -qx 4 && echo PHOTOS_HANDLERS_PRESENT</automated>
    <automated>grep -E "PortfolioDB\.estimatePhotosBytes\(" assets/settings.js && grep -E "PortfolioDB\.updateClient\(" assets/settings.js && grep -E "CropModule\.resizeToMaxDimension\(" assets/settings.js</automated>
    <automated>grep -E "tone:\s*['\"]danger['\"]" assets/settings.js | grep -A 1 -B 4 "photosDeleteAll\|photos\.deleteAll" | head -10</automated>
    <automated>grep -E "tone:\s*['\"]neutral['\"]" assets/settings.js | grep -A 1 -B 4 "photos\.optimize\|photosOptimize" | head -10</automated>
    <automated>for L in en he de cs; do for K in "photos.heading" "photos.helper" "photos.usage.line" "photos.usage.unavailable" "photos.optimize.heading" "photos.optimize.helper" "photos.optimize.savingsPreview" "photos.optimize.action" "photos.optimize.confirm.title" "photos.optimize.confirm.body" "photos.optimize.confirm.yes" "photos.optimize.success" "photos.optimize.partialFailure" "photos.deleteAll.heading" "photos.deleteAll.helper" "photos.deleteAll.action" "photos.deleteAll.confirm.title" "photos.deleteAll.confirm.body" "photos.deleteAll.confirm.yes" "photos.deleteAll.success" "photos.empty"; do grep -F "\"$K\":" "assets/i18n-$L.js" >/dev/null || { echo "MISSING $K in $L"; exit 1; }; done; done; echo PHOTOS_KEYS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - settings.html #settingsTabPhotos contains photosStorageUsage, photosEmpty, photosOptimizeBtn, photosOptimizePreview, photosOptimizeSection, photosDeleteAllBtn, photosDeleteAllSection elements.
    - assets/settings.js has refreshPhotosTab, handleOptimize, handleDeleteAll, bindPhotosTab functions (verified by grep).
    - handleOptimize uses tone:'neutral' (UI-SPEC A7); handleDeleteAll uses tone:'danger'.
    - Both handlers call the testable loop helpers from window.__PhotosTabHelpers.
    - The Optimize loop guard "only writes if newBytes < origBytes" is present (verified by reading _optimizeAllPhotosLoop in settings.js).
    - When no clients have photoData, the Photos tab shows photos.empty AND hides the Optimize / Delete-all sections (via [hidden] toggle).
    - All 21 new i18n keys ship in 4 locales.
    - Hebrew strings use noun/infinitive forms (manual confirm: מיטוב כל התמונות, מחיקת כל התמונות, כן למטב, כן למחוק).
  </acceptance_criteria>
  <done>
    Photos tab is fully wired. Storage usage line + photo count drive the UI state. Optimize-all loops through every client with photoData, runs each through the same CropModule.resizeToMaxDimension that powers new uploads (D-30), and reports savings + partial-failure. Delete-all clears every photoData via the same updateClient path used by edit-client (D-30). Tab hides Optimize/Delete sections + shows empty state when no photos exist. 21 i18n keys ship in 4 locales.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                                | Description                                                                          |
|-----------------------------------------|--------------------------------------------------------------------------------------|
| Photos tab UI → PortfolioDB.updateClient | Bulk write path; same code as edit-client save (D-30)                              |
| Optimize loop → CropModule.resizeToMaxDimension | Same code as add-client photo upload pipeline (D-30)                       |
| navigator.storage.estimate              | Read-only browser API; no privilege change                                          |

## STRIDE Threat Register

| Threat ID  | Category         | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
|------------|------------------|----------------------------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-25-07-01 | T6 EXIF leak (re-encode of existing photos) | Optimize-all loop                  | mitigate    | Existing photoData rows from before Phase 25 may have EXIF preserved (the legacy crop pipeline used toDataURL which strips EXIF too — but defensive: the optimize loop re-encodes through canvas, which guarantees EXIF is gone post-optimize). Side-benefit identical to T-25-06-01. |
| T-25-07-02 | D Denial of Service | Optimize loop blocks the page for many clients | accept     | Loop is sequential await; for ~100 clients × ~100ms per photo = ~10s blocking. Acceptable for a one-time user-initiated action. The button is disabled during the loop to prevent re-entry. Future: chunk + yield via setTimeout if large practices report slowness. |
| T-25-07-03 | T Tampering      | User accidentally deletes all photos | mitigate    | App.confirmDialog with tone:'danger' + explicit "Cannot be undone" body + capitalized confirm button "Yes, delete all photos" (4-locale). Mirrors the existing Phase 22-15 destructive pattern. |
| T-25-07-04 | I Information Disclosure | navigator.storage.estimate     | accept      | Returns the user's own storage usage; no cross-tenant model. |
</threat_model>

<verification>
- Both new tests pass.
- All grep gates pass.
- Manual smoke (DEFERRED to checker pass):
  1. Settings → Photos tab. With NO clients having photos: usage line says "Storage usage is not available..." OR shows the navigator total; Optimize/Delete sections hidden; empty-state visible.
  2. Add 3 clients with photos (use the existing add-client flow). Reload Photos tab: usage line says "Photos use ~250 KB"; Optimize section visible with savings preview; Delete-all visible.
  3. Click Optimize → confirm (neutral tone) → wait → toast "Photos optimized. Saved ~150 KB." → usage line refreshes.
  4. Click Delete-all → confirm (danger tone, "Yes, delete all photos") → toast "All photos deleted." → empty state appears; client records still in IDB (verify via overview).
  5. Re-run Optimize on a fresh-uploaded photo (already at 800px q=0.75): no-op (newBytes >= origBytes guard); usage line unchanged.
</verification>

<success_criteria>
- PortfolioDB.estimatePhotosBytes is pure and unit-tested.
- Photos tab UI displays usage, optimize savings preview, and tab-empty state correctly.
- Optimize-all uses CropModule.resizeToMaxDimension (D-30) and only persists when smaller (no bloat regression).
- Delete-all uses PortfolioDB.updateClient (D-30) — same write path as edit-client.
- Both bulk operations confirm via App.confirmDialog with correct tone (neutral / danger).
- 21 new i18n keys ship in 4 locales with Hebrew noun/infinitive forms.
- T6 mitigation reinforced: bulk-optimize re-encodes existing photos through canvas, removing any residual EXIF.
</success_criteria>

<output>
After completion, create `.planning/phases/25-backup-architectural-rework/25-07-SUMMARY.md` documenting:
- estimatePhotosBytes + the loop-helper test coverage
- The Photos tab UI markup + handlers
- The 21 i18n keys delivered (with one HE example)
- D-30 single-source-of-truth: CropModule.resizeToMaxDimension powers Plan 06 + Plan 07; PortfolioDB.updateClient is the same write path as edit-client
- The "only persist if smaller" guard rationale (avoids bloat regression on already-optimized photos)
- updateClient status: was it pre-existing or added? Document for Plan 08's audit
- Hand-off to Plan 08: PortfolioDB.estimatePhotosBytes is reusable in Plan 08's test (e.g., asserting backup photo count matches client count with photoData)
</output>
