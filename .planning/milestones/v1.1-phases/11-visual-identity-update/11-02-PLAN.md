---
phase: 11-visual-identity-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - add-client.html
  - add-session.html
  - sessions.html
  - reporting.html
  - demo.html
  - landing.html
  - assets/app.css
  - assets/icons/icon-192.png
  - assets/icons/icon-512.png
autonomous: false
requirements:
  - DSGN-06
  - LNCH-05

must_haves:
  truths:
    - "The watering can illustration appears as the logo in the app header across all 5 app pages + demo"
    - "The same watering can logo appears in the landing page header"
    - "The app icon on home screen / PWA install is a watering can on green rounded background, not a placeholder"
  artifacts:
    - path: "assets/icons/icon-192.png"
      provides: "192px PWA app icon with watering can on green background"
    - path: "assets/icons/icon-512.png"
      provides: "512px PWA app icon with watering can on green background"
    - path: "index.html"
      provides: "Updated brand-mark with watering can image instead of leaf SVG"
      contains: "brand-mark"
    - path: "landing.html"
      provides: "Updated landing-brand-mark with watering can image"
      contains: "landing-brand-mark"
  key_links:
    - from: "all HTML files brand-mark"
      to: "screenshots/new logo option.png or derived asset"
      via: "IMG src in brand-mark div"
      pattern: "brand-mark"
    - from: "manifest.json"
      to: "assets/icons/icon-192.png"
      via: "icons array src"
      pattern: "icon-192"
---

<objective>
Replace the leaf SVG logo with the new watering can illustration across all app pages and the landing page, and generate final PWA app icons from the watering can image.

Purpose: Unify the visual identity around the watering can motif (garden = nurturing = watering = growth) and replace the placeholder app icon.
Output: Updated logo in all HTML headers; final 192px and 512px app icons.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visual-identity-update/11-CONTEXT.md

<interfaces>
<!-- Current brand-mark structure in app HTML (same in all 5 pages + demo) -->
From index.html lines 25-34:
```html
<div class="brand-mark">
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="28" height="28" aria-hidden="true">
    <path d="M12 2 C14 2 18 6 18 11 ..." fill="currentColor" opacity="0.85"/>
    <!-- 5 more path elements for leaf veins -->
  </svg>
</div>
```

From landing.html lines 24-33:
```html
<span class="landing-brand-mark" aria-hidden="true">
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="32" height="32" aria-hidden="true">
    <!-- same leaf SVG -->
  </svg>
</span>
```

From assets/app.css lines 44-54:
```css
.brand-mark {
  width: 48px;
  height: 48px;
  border-radius: 16px;
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
  display: grid;
  place-items: center;
  font-weight: 800;
  box-shadow: var(--shadow-brand);
}
```

Source image: screenshots/new logo option.png (high-res watering can with flowers and hearts, line art on light background)
Existing smaller version: assets/illustrations/משפך.png (similar watering can, different style)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace leaf SVG with watering can logo in all HTML files</name>
  <files>index.html, add-client.html, add-session.html, sessions.html, reporting.html, demo.html, landing.html, assets/app.css</files>
  <action>
**Step 1: Prepare logo asset.**
Copy the source image `screenshots/new logo option.png` to `assets/logo.png` for use as the brand mark. This is the high-res watering can chosen by Sapir as the new logo.

**Step 2: Replace brand-mark SVG in all 5 app pages + demo.**
In each of: index.html, add-client.html, add-session.html, sessions.html, reporting.html, demo.html:

Find the `<div class="brand-mark">` block containing the leaf SVG (the entire SVG element with 6 path children). Replace the SVG with an IMG tag:

```html
<div class="brand-mark">
  <img src="./assets/logo.png" alt="Sessions Garden" width="36" height="36" class="brand-mark-img" />
</div>
```

**Step 3: Replace landing page brand mark.**
In landing.html, find the `<span class="landing-brand-mark">` block containing the leaf SVG. Replace the SVG with:

```html
<span class="landing-brand-mark" aria-hidden="true">
  <img src="./assets/logo.png" alt="" width="32" height="32" class="landing-brand-mark-img" />
</span>
```

**Step 4: Update CSS for brand-mark with image.**
In assets/app.css, update the `.brand-mark` rule. Keep existing dimensions and border-radius. Add:
- `overflow: hidden` to clip the image within rounded corners
- Remove `color` property (was for SVG currentColor, no longer needed)

Add a new rule for the image inside:
```css
.brand-mark-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

For dark mode, add subtle treatment so the line art stays visible:
```css
[data-theme="dark"] .brand-mark-img {
  filter: invert(1);
}

[data-theme="dark"] .brand-mark {
  background: var(--color-primary-dark);
}
```

Note: The `screenshots/new logo option.png` has a near-white background which works perfectly with the existing `var(--color-primary-soft)` background of brand-mark. For dark mode, invert makes the line art white-on-dark which pairs with a dark green background.
  </action>
  <verify>
    <automated>grep -l "brand-mark-img\|logo\.png" index.html add-client.html add-session.html sessions.html reporting.html demo.html landing.html | wc -l | grep -q "^7$" && echo "PASS: all 7 HTML files updated" || echo "FAIL"</automated>
  </verify>
  <done>All 7 HTML files use the watering can PNG as logo instead of leaf SVG; CSS updated for image-based brand mark with dark mode support; logo is visually consistent across app and landing page</done>
</task>

<task type="auto">
  <name>Task 2: Generate PWA app icons from watering can illustration</name>
  <files>assets/icons/icon-192.png, assets/icons/icon-512.png</files>
  <action>
Generate two PWA app icons using the Canvas API via a Node.js script (or using the `sips` macOS command-line tool for image processing).

**Approach: Use `sips` (macOS built-in, no dependencies).**

1. Create a temporary script or use sips commands to:
   - Start with `screenshots/new logo option.png` (the watering can illustration)
   - For each target size (192, 512):
     a. Create a square canvas with rounded-corner green background (`#2d6a4f` -- the app's theme_color from manifest.json)
     b. Place the watering can illustration centered, padded ~15% from edges
     c. Save as the respective icon file

2. Since sips cannot do compositing with backgrounds, use a Python script with Pillow (if available) or a simple HTML+Canvas approach:

**Preferred: Python with Pillow** (commonly available on macOS):
```python
from PIL import Image, ImageDraw
# Create green rounded-corner background
# Paste watering can centered with padding
# Save to assets/icons/
```

**Fallback: If no Pillow**, create a minimal Node.js script using the `canvas` npm package, OR use ImageMagick if installed (`convert` command).

**Fallback 2: Manual sips pipeline:**
- Resize source to fit within target with padding
- Use sips to set background (limited, may need creative approach)

The icons must:
- Be exactly 192x192 and 512x512 pixels
- Have a solid green (#2d6a4f) rounded-rectangle background (corner radius ~20% of size)
- Show the watering can illustration centered, filling ~70% of the icon area
- Be PNG format with no transparency (solid background for best home screen appearance)

After generating, verify manifest.json still points to the correct paths (it already does -- `/assets/icons/icon-192.png` and `/assets/icons/icon-512.png`).
  </action>
  <verify>
    <automated>test -f assets/icons/icon-192.png && test -f assets/icons/icon-512.png && python3 -c "from PIL import Image; i=Image.open('assets/icons/icon-192.png'); assert i.size==(192,192); i2=Image.open('assets/icons/icon-512.png'); assert i2.size==(512,512); print('PASS: icons correct size')" 2>/dev/null || (sips -g pixelWidth assets/icons/icon-192.png 2>/dev/null | grep -q 192 && echo "PASS: 192px icon exists" || echo "FAIL")</automated>
  </verify>
  <done>PWA app icons at 192px and 512px show watering can on green rounded background; manifest.json references are correct; icons replace previous placeholders</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual approval of logo and app icon</name>
  <files>index.html, landing.html, assets/icons/icon-192.png, assets/icons/icon-512.png</files>
  <action>
Present the completed logo replacement and app icons to Sapir for visual approval. The watering can illustration should appear as the brand mark in all app headers and the landing page header. The PWA app icons should show the watering can on a green rounded background.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: requires human visual approval"</automated>
  </verify>
  <done>Sapir approves the logo appearance in app and landing page headers, and the PWA app icon design</done>
  <what-built>Replaced leaf SVG logo with watering can illustration across all app pages and landing page. Generated final PWA app icons (192px + 512px) with watering can on green background.</what-built>
  <how-to-verify>
    1. Open the app (index.html) -- verify the header shows the watering can illustration as the logo (not the old leaf)
    2. Navigate to other pages (add-client, sessions, reporting) -- same watering can logo in all headers
    3. Open landing.html -- same watering can logo in the landing page header
    4. Toggle dark mode -- logo should remain clear and visible (inverted for dark background)
    5. Check the app icon: on mobile, try "Add to Home Screen" or check the PWA install prompt -- icon should show watering can on green background
    6. If on desktop, check assets/icons/icon-192.png and icon-512.png directly -- should show watering can on green rounded rectangle
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with logo appearance, sizing, or icon quality</resume-signal>
</task>

</tasks>

<verification>
1. All 7 HTML files (5 app + demo + landing) display watering can logo instead of leaf SVG
2. Logo looks good at 48px (app) and 32px (landing) -- recognizable watering can silhouette
3. Dark mode treatment keeps logo visible and attractive
4. PWA icons are 192x192 and 512x512 with green background
5. manifest.json paths still correct (unchanged)
</verification>

<success_criteria>
- The same watering can logo appears consistently in app header and landing page header
- App icons are final branded images replacing placeholders
- Dark mode logo treatment is clean
- Sapir approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-identity-update/11-02-SUMMARY.md`
</output>
