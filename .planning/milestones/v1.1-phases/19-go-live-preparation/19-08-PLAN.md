---
phase: 19-go-live-preparation
plan: 08
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05, 19-06, 19-07]
files_modified: []
autonomous: false
requirements: [LIVE-04, LIVE-07, LIVE-09]
must_haves:
  truths:
    - "Full purchase-activate-use flow works end-to-end on deployed site"
    - "LIVE-07 confirmed as zero-implementation (demo uses separate IndexedDB)"
    - "Innovator research output exists with v1.2 feature candidates"
  artifacts:
    - path: ".planning/phases/19-go-live-preparation/19-08-SUMMARY.md"
      provides: "E2E verification results and innovator research output"
      contains: "LIVE-04"
  key_links: []
---

<objective>
Verify the full end-to-end purchase-activate-use flow on the deployed site, confirm LIVE-07 needs zero implementation, run innovator research for v1.2 backlog, and do a final legal content review pass.

Purpose: This is the final verification wave — everything built in Waves 1-2 must work together. The E2E test catches integration issues. The innovator research captures v1.2 feature candidates while the codebase is fresh in context.
Output: E2E verification results, legal content review notes, and v1.2 feature backlog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-go-live-preparation/19-CONTEXT.md (D-17 — LIVE-07 zero implementation, D-28 — innovator research)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: E2E Purchase-Activate-Use Flow Verification (LIVE-04)</name>
  <action>Human verification checkpoint — follow the how-to-verify checklist below to test the full purchase-activate-use flow on the deployed site.</action>
  <what-built>
All Phase 19 plans have been executed:
- 12 per-language legal pages (Impressum, Datenschutz, Disclaimer in 4 languages each)
- Encrypted backup export/import via Web Crypto API
- License gate hardening (2-key check on 5 app pages)
- Context-aware license page chrome
- Landing page auto-detection for activated users
- SW cache updated with all 12 legal pages
- Cross-links updated to per-language file navigation
- Security guidance messaging at 3 touchpoints
- GitHub Action deploy workflow
- Enhanced _headers security headers
  </what-built>
  <how-to-verify>
**Pre-requisite:** Push to main and verify the GitHub Action runs successfully (check Actions tab).

**Then verify on the deployed site (or locally):**

1. **Legal pages (all 12 files):**
   - Visit impressum.html → German content with all 6 sections visible
   - Visit impressum-en.html → English content with courtesy banner at top
   - Visit impressum-he.html → Hebrew content, RTL layout, courtesy banner
   - Visit impressum-cs.html → Czech content with courtesy banner
   - Click globe language switcher on any → navigates to correct sibling file
   - Click footer links → navigate to correct language variant
   - Repeat quick check for datenschutz and disclaimer pages

2. **Disclaimer acceptance flow:**
   - Clear localStorage, visit disclaimer.html → checkbox and button appear
   - Accept terms → redirected to license page
   - Visit disclaimer.html?readonly=true → terms shown without acceptance UI

3. **License gate hardening:**
   - Set only `portfolioLicenseActivated=1` in DevTools → visit index.html → redirected to license.html
   - Set both `portfolioLicenseActivated=1` and `portfolioLicenseInstance=[any-base64]` → app loads

4. **License page chrome:**
   - With active license → app nav bar visible with "Back to app"
   - Without license → legal topbar with "Back to home"

5. **Landing page auto-detect:**
   - With active license, click "Already have a license?" → banner shows, redirects to app in 2s
   - Without license → navigates to license.html

6. **Encrypted backup:**
   - Click "Export Encrypted Backup" → passphrase modal → enter passphrase → .sgbackup file downloads
   - Import the .sgbackup → passphrase prompt → enter correct passphrase → data restores
   - Import with wrong passphrase → error message shown
   - Import a .zip file → works without passphrase prompt (backward compat)

7. **Security guidance:**
   - First load after activation → security note at top of overview page
   - Click "I understand" → note gone, doesn't reappear on refresh
   - Bottom of overview page → "Privacy & Data Security" section always visible
   - Wait for backup reminder → enhanced copy mentions data-loss risk

8. **Full purchase flow (if LS is set up):**
   - Landing page → Buy button → LS checkout → receive key → license.html → activate → app loads

9. **GH Action:**
   - Check Actions tab → deploy workflow ran on last push
   - Check deploy branch → contains only app files, no .planning/ or .claude/

  </how-to-verify>
  <resume-signal>Type "approved" with any issues found, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm LIVE-07 zero-implementation + Innovator research for v1.2 backlog (LIVE-09)</name>
  <files></files>
  <read_first>
    - .planning/phases/19-go-live-preparation/19-CONTEXT.md (D-17 — LIVE-07, D-28 — LIVE-09)
    - assets/demo-seed.js (confirm demo uses separate DB — verify `demo_portfolio` database name)
    - assets/db.js (confirm main app uses `portfolio` database name — separate from demo)
  </read_first>
  <action>
**LIVE-07 Confirmation:**
Verify that demo-seed.js uses a separate IndexedDB database name (`demo_portfolio` or equivalent) from the main app database (`portfolio` or `sessions_garden`). Read both files, confirm the database names are different. Document this confirmation in the summary. No code changes needed.

**LIVE-09 Innovator Research:**
Run an innovator-style analysis to generate v1.2 feature candidates. Consider:

1. **User pain points** from existing features — what's clunky or missing based on app structure?
2. **Natural extensions** — what would power users want next? (better search, data visualization, templates)
3. **Competitive landscape** — what do practice management tools offer that this app doesn't?
4. **Revenue opportunities** — features that could justify a premium tier or upsell
5. **Technical enablers** — what's now possible given the architecture (IndexedDB, Web Crypto, PWA)?

Create a backlog file: `.planning/research/v1.2-feature-backlog.md` with:
- Feature candidates (name, description, estimated complexity)
- Grouped by theme (UX improvements, data features, platform expansion, security)
- Priority recommendation (must-have for v1.2 vs nice-to-have)
- Dependencies (what needs to change architecturally)

Include the full IndexedDB encryption feature (already captured in todos) as the anchor item.

**Legal content review pass (per two-pass directive):**
Read through all 12 legal pages created in Plans 01 and 02. Check for:
- Completeness: all sections present in each language
- Consistency: same structure across language variants
- Legal accuracy: correct DDG (not TMG) citations, no ODR link, VSBG present
- Translation quality: no obvious machine-translation artifacts
- Cross-link correctness: all footer and globe links work
Document any issues found — if minor, fix inline. If significant, flag for follow-up.
  </action>
  <verify>
    <automated>test -f .planning/research/v1.2-feature-backlog.md && echo "backlog exists" && grep -c "IndexedDB encryption" .planning/research/v1.2-feature-backlog.md</automated>
  </verify>
  <acceptance_criteria>
    - LIVE-07 confirmed: demo-seed.js and db.js use different IndexedDB database names
    - .planning/research/v1.2-feature-backlog.md exists
    - Backlog contains at least 5 feature candidates
    - Backlog contains "IndexedDB encryption" as an item
    - Backlog features are grouped by theme
    - Legal content review notes are documented in summary
  </acceptance_criteria>
  <done>LIVE-07 confirmed as zero-implementation. v1.2 feature backlog created with prioritized candidates. Legal content review pass completed.</done>
</task>

</tasks>

<verification>
- All Phase 19 requirements addressed:
  - LIVE-01: Impressum has all legally required sections ✓ (Plan 01)
  - LIVE-02: 12 per-language legal pages exist ✓ (Plans 01, 02)
  - LIVE-03: GH Action deploy workflow exists ✓ (Plan 07)
  - LIVE-04: E2E flow verified ✓ (this plan - Task 1 checkpoint)
  - LIVE-05: License page chrome ✓ (Plan 04)
  - LIVE-06: Landing page auto-detect ✓ (Plan 04)
  - LIVE-07: Confirmed zero-implementation ✓ (this plan - Task 2)
  - LIVE-08: Encrypted backup + security guidance ✓ (Plans 03, 06)
  - LIVE-09: Innovator research completed ✓ (this plan - Task 2)
</verification>

<success_criteria>
E2E purchase-activate-use flow verified on deployed site. All 12 legal pages load correctly with language switching. Encrypted backup works. Gate hardening prevents simple bypass. LIVE-07 confirmed as zero-implementation. v1.2 feature backlog created.
</success_criteria>

<output>
After completion, create `.planning/phases/19-go-live-preparation/19-08-SUMMARY.md`
</output>
