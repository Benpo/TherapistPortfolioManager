---
phase: 19-go-live-preparation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - _headers
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "GitHub Action workflow exists that syncs app-only files to deploy branch on push to main"
    - "Deploy branch contains only app files (no .planning/, .claude/, .env, CLAUDE.md)"
    - "Concurrency block prevents race conditions from rapid pushes"
    - "_headers file includes security headers for production"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Action that syncs to deploy branch"
      contains: "deploy"
    - path: "_headers"
      provides: "Cloudflare Pages security headers"
      contains: "X-Frame-Options"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "deploy branch"
      via: "git push -f origin deploy"
      pattern: "push -f origin deploy"
---

<objective>
Create the GitHub Action workflow that syncs app-only files to a deploy branch on push to main, and enhance the _headers file with production security headers.

Purpose: Cloudflare Pages will watch the deploy branch. This pipeline ensures only app files (no planning docs, dev tools, or secrets) are deployed. The _headers file adds security headers that CF Pages serves automatically.
Output: .github/workflows/deploy.yml workflow file + enhanced _headers file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-go-live-preparation/19-CONTEXT.md (D-07 through D-10)
@.planning/phases/19-go-live-preparation/19-RESEARCH.md (Deployment Pipeline, GH Action workflow, _headers enhancement)

<interfaces>
From CONTEXT.md D-08 (files to include):
| Include | Reason |
|---------|--------|
| _headers | CF Pages headers config |
| All *.html files | App + legal + landing pages |
| assets/ (entire dir) | JS, CSS, fonts, images, demo data |
| manifest.json | PWA manifest |
| sw.js | Service worker |

From CONTEXT.md D-09 (files to exclude):
| Exclude | Reason |
|---------|--------|
| .claude/ | Dev tooling |
| .planning/ | Dev planning |
| .env, .env.* | Secrets |
| .gitignore | Dev-only |
| .DS_Store | macOS junk |
| CLAUDE.md | Dev instructions |

From _headers (current content):
```
/*.html
  Cache-Control: no-cache
/*.js
  Cache-Control: public, max-age=86400
/*.css
  Cache-Control: public, max-age=86400
```

From RESEARCH.md (enhanced _headers):
- Add X-Frame-Options: DENY
- Add X-Content-Type-Options: nosniff
- Add Referrer-Policy
- Add Permissions-Policy
- Keep /sw.js no-cache
- Do NOT add CSP here (already in HTML meta tags, _headers would be additive and break inline scripts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Action deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/19-go-live-preparation/19-RESEARCH.md (GH Action Workflow with Concurrency code example)
    - .planning/phases/19-go-live-preparation/19-CONTEXT.md (D-07 through D-10)
  </read_first>
  <action>
Create the directory structure and workflow file.

**Create `.github/workflows/` directory** (may not exist yet).

**Create `.github/workflows/deploy.yml`:**

```yaml
name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare deploy directory
        run: |
          mkdir -p deploy-staging
          # Include per D-08: _headers, *.html, assets/, manifest.json, sw.js
          cp _headers deploy-staging/
          cp *.html deploy-staging/
          cp -r assets deploy-staging/
          cp manifest.json deploy-staging/
          cp sw.js deploy-staging/

      - name: Verify no sensitive files
        run: |
          # Ensure no dev/planning files leaked into deploy
          if [ -d "deploy-staging/.planning" ] || [ -d "deploy-staging/.claude" ] || [ -f "deploy-staging/CLAUDE.md" ] || [ -f "deploy-staging/.env" ]; then
            echo "ERROR: Sensitive files found in deploy-staging!"
            ls -la deploy-staging/
            exit 1
          fi
          echo "Deploy directory is clean."
          echo "HTML files:"
          ls deploy-staging/*.html | wc -l
          echo "Asset files:"
          ls deploy-staging/assets/ | wc -l

      - name: Push to deploy branch
        run: |
          cd deploy-staging
          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b deploy
          git add -A
          git commit -m "Deploy from ${GITHUB_SHA::7}"
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push -f origin deploy
```

**Key design decisions:**
- `concurrency: { group: deploy, cancel-in-progress: true }` prevents race conditions when two pushes happen quickly (per Ben's directive and Pitfall 7)
- `permissions: contents: write` is required to push to the deploy branch
- `fetch-depth: 0` gives full history for branch operations
- Explicit `cp` commands (not rsync) — simple, auditable, matches D-08 include list exactly
- Verification step checks that no .planning/, .claude/, CLAUDE.md, or .env files leaked
- `git push -f origin deploy` force-pushes because the deploy branch is ephemeral (rebuilt from scratch each time)
- Uses `${{ secrets.GITHUB_TOKEN }}` which is auto-provided by GitHub Actions — no manual secret setup needed

**What this does NOT do:**
- Does NOT configure Cloudflare Pages (that's manual CF dashboard setup — LIVE-04)
- Does NOT use wrangler-action (the deploy-branch approach means CF Pages watches the branch natively)
- Does NOT need CF API token or account ID secrets
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy.yml && grep -c "concurrency" .github/workflows/deploy.yml && grep -c "deploy-staging" .github/workflows/deploy.yml && grep -c "push -f origin deploy" .github/workflows/deploy.yml && grep -c ".planning" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml exists
    - File contains `on: push: branches: [main]` trigger
    - File contains `concurrency:` block with `group: deploy` and `cancel-in-progress: true`
    - File contains `permissions: contents: write`
    - File contains `cp _headers deploy-staging/`
    - File contains `cp *.html deploy-staging/`
    - File contains `cp -r assets deploy-staging/`
    - File contains `cp manifest.json deploy-staging/`
    - File contains `cp sw.js deploy-staging/`
    - File contains verification step checking for `.planning` and `.claude`
    - File contains `git push -f origin deploy`
    - File contains `actions/checkout@v4`
    - File does NOT contain any CF API token or account ID references
    - File does NOT contain any wrangler-action reference (deploy-branch approach doesn't need it)
  </acceptance_criteria>
  <done>GitHub Action workflow syncs app-only files to deploy branch on push to main, with concurrency protection, sensitive file verification, and no external secrets required.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance _headers with production security headers</name>
  <files>_headers</files>
  <read_first>
    - _headers (current content — cache control rules only)
    - .planning/phases/19-go-live-preparation/19-RESEARCH.md (_headers File Enhancement section)
  </read_first>
  <action>
Replace the current _headers file with an enhanced version that includes security headers for production.

**New _headers content:**
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()

/*.html
  Cache-Control: no-cache

/*.js
  Cache-Control: public, max-age=86400

/*.css
  Cache-Control: public, max-age=86400

/sw.js
  Cache-Control: no-cache
```

**What changed vs current:**
- Added `/*` global rule with 4 security headers (applies to ALL responses)
- Kept existing cache rules for .html, .js, .css
- Added explicit `/sw.js` no-cache rule (service worker must never be long-cached — browser checks for updates)

**What is NOT in _headers:**
- CSP (Content Security Policy) — already in `<meta>` tags on every HTML page. Adding CSP via _headers would be ADDITIVE (more restrictive), potentially breaking inline scripts. Per RESEARCH.md: "meta tags take precedence for inline scripts, and _headers CSP would be additive."
- HSTS — CF Pages handles HTTPS automatically; no need for HSTS header
- X-XSS-Protection — deprecated header, removed from modern browsers

**Security headers explained:**
- `X-Frame-Options: DENY` — prevents the app from being embedded in iframes (clickjacking protection)
- `X-Content-Type-Options: nosniff` — prevents MIME type sniffing
- `Referrer-Policy: strict-origin-when-cross-origin` — only sends origin (not full URL) to external sites
- `Permissions-Policy: camera=(), microphone=(), geolocation=()` — explicitly denies access to device APIs the app doesn't use
  </action>
  <verify>
    <automated>grep -c "X-Frame-Options" _headers && grep -c "X-Content-Type-Options" _headers && grep -c "Referrer-Policy" _headers && grep -c "Permissions-Policy" _headers && grep -c "sw.js" _headers && grep -c "no-cache" _headers</automated>
  </verify>
  <acceptance_criteria>
    - _headers contains `X-Frame-Options: DENY`
    - _headers contains `X-Content-Type-Options: nosniff`
    - _headers contains `Referrer-Policy: strict-origin-when-cross-origin`
    - _headers contains `Permissions-Policy: camera=(), microphone=(), geolocation=()`
    - _headers contains `/*.html` with `Cache-Control: no-cache`
    - _headers contains `/*.js` with `Cache-Control: public, max-age=86400`
    - _headers contains `/*.css` with `Cache-Control: public, max-age=86400`
    - _headers contains `/sw.js` with `Cache-Control: no-cache`
    - _headers does NOT contain `Content-Security-Policy` (CSP is in HTML meta tags)
    - _headers does NOT contain `X-XSS-Protection` (deprecated)
  </acceptance_criteria>
  <done>_headers file includes production security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) plus existing cache rules and explicit sw.js no-cache rule.</done>
</task>

</tasks>

<verification>
- deploy.yml is valid YAML (yamllint or GH Actions syntax check)
- Locally simulate deploy: run the cp commands, verify deploy-staging/ contains only expected files
- _headers file is valid CF Pages headers syntax (key: value under path rules)
- No .planning/, .claude/, CLAUDE.md, .env in deploy-staging/
- sw.js, manifest.json, *.html, assets/* all present in deploy-staging/
</verification>

<success_criteria>
GitHub Action workflow exists and correctly syncs app-only files to deploy branch. Concurrency block prevents race conditions. Security headers configured for production. No CSP conflicts with existing meta tags.
</success_criteria>

<output>
After completion, create `.planning/phases/19-go-live-preparation/19-07-SUMMARY.md`
</output>
