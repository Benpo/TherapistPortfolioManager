# Phase 24 Plan 06 — Browser UAT script

Behavior the Node test (`tests/24-06-spotlight-session-info.test.js`) cannot cover:
runtime binding (does `PortfolioDB.getSessionsByClient` resolve from this script?),
DOM markup wiring, locale rendering, RTL flow, dark mode. Run this UAT before
marking Plan 06 task 1 `completed`.

Browser: open `add-session.html` in the running dev preview. Open DevTools console
to verify no warnings/errors.

## Scenario 1 — existing client with sessions and non-empty last summary

1. Pick an existing client from the dropdown who has ≥2 saved sessions and whose
   most recent session has a non-empty "Information for Next Session" value.
2. Expected DOM state:
   - `#clientSpotlight` visible (no `.is-hidden`).
   - `#clientSpotlightSessionInfo` visible (no `.is-hidden`).
   - The `<details>` is **collapsed by default** — only the `<summary>` "Recent activity"
     row is visible.
   - `#clientSpotlightLastSummary` NOT hidden (it's inside the collapsed body, but
     will appear when expanded).
3. Tap the `<summary>` to expand. Verify:
   - "Last session" label + date in the locale-aware format.
   - "Total sessions" label + count, where count === number of saved sessions for the client.
   - "Last note for next session" label + a `<blockquote>` containing the
     latest session's `customerSummary` text, verbatim.

**Pass:** all three rows render with correct values. The quote text matches the
latest session's customerSummary exactly (no escape artifacts, no HTML rendering).

## Scenario 2 — existing client whose latest session has empty customerSummary

1. Pick an existing client where the latest session was saved with an empty
   "Information for Next Session" field.
2. Expected DOM state when expanded:
   - "Last session" row visible.
   - "Total sessions" row visible.
   - `#clientSpotlightLastSummary` IS hidden (`.is-hidden` present) — no blockquote
     visible, no "Last note for next session" label.

**Pass:** the summary block is silently absent. No "—" or "(empty)" placeholder.

## Scenario 3 — new / empty client (zero sessions)

1. Pick a client with zero saved sessions.
2. Expected DOM state:
   - `#clientSpotlight` visible.
   - `#clientSpotlightSessionInfo` IS hidden (`.is-hidden` present). No divider,
     no expand-row, no strings.

**Pass:** the Customer-data section (photo + name + notes) renders normally; the
Session-info subsection is completely absent.

## Scenario 4 — race / rapid dropdown changes

1. With multiple clients in the dropdown, rapidly cycle through three clients.
2. Watch the Session-info subsection.

**Pass:** the final-shown client's data matches the dropdown's final value.
Mild flicker is acceptable for v1 (plan threat T-24-04-04 was scoped as optional
polish). **Fail:** the displayed quote belongs to a previous client and stays.

## Scenario 5 — four locales

1. With Scenario-1 client selected, change language via the settings gear.
2. Expected summary header text:
   - English: `Recent activity`
   - Hebrew (`he`): `פעילות אחרונה`
   - German (`de`): `Letzte Aktivität`
   - Czech (`cs`): `Poslední aktivita`
3. Expected labels in each locale per `24-06-PLAN.md` Task 2 Part A.

**Pass:** all 4 locales show the correct subsection header and row labels.
**Note:** Task 2 (i18n + CSS) must be shipped for this scenario to pass.

## Scenario 6 — RTL (Hebrew)

1. Switch to Hebrew.
2. Verify:
   - Subsection label text right-aligns naturally with the column flow.
   - Blockquote border (`border-inline-start`) appears on the right (RTL natural).
   - No layout breakage / clipping.

**Pass:** the subsection mirrors the rest of the spotlight in RTL.

## Scenario 7 — dark mode

1. Toggle theme to dark.
2. Verify:
   - Top divider visible against dark surface (uses `--color-border-subtle`).
   - Blockquote border visible.
   - Text contrast adequate (`--color-text` and `--color-text-muted` adapt).

**Pass:** subsection readable in both light and dark mode.

## Scenario 8 — console hygiene

While running scenarios 1–7, the DevTools console must show:
- **No** `TypeError` / `ReferenceError` / `Uncaught (in promise)`.
- **No** "Phase 24 Plan 06: failed to load sessions" warning (would indicate
  PortfolioDB.getSessionsByClient threw — investigate).

**Pass:** clean console across all scenarios.

## Scenario 9 — D-32 enforcement (negative test)

Inspect `#clientSpotlightSessionInfo`'s innerHTML. Verify NO child element
matches any of:
- An "open issues" list / counter.
- A `<svg>` sparkline / chart.
- Any text containing "open issue" or "severity trend".

**Pass:** only Last session / Total sessions / Last note rows present.

## Result template

```
Tester: Ben
Date: ____
Scenarios passed: 1 [ ]  2 [ ]  3 [ ]  4 [ ]  5 [ ]  6 [ ]  7 [ ]  8 [ ]  9 [ ]
Notes:
```
