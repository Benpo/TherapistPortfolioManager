---
milestone: v1.2
audited: 2026-07-07T12:30:15Z
status: passed
scores:
  requirements: 64/65
  phases: 11/11
  integration: 8/8
  flows: 8/8
gaps: {}
tech_debt:
  - phase: 30-test-harness-coverage
    items:
      - "WR-01 (advisory): fake-test-detector's readFileSync-literal check is evadable via a wrapper helper — future prevention weakness, not a current suite defect"
      - "WR-02 (advisory): stray unused `var jsdom` satisfies the marker check; narrow heuristic, current suite clean"
      - "WR-03 (advisory): mock-portfolio-db.getAllTherapistSettings returns a shared mutable reference instead of a deep copy — low risk, tests depending on it pass consistently"
      - "WR-04 (advisory): mock-portfolio-db sameId() coerces ids via String() comparison, more lenient than real IndexedDB — mitigated by Number.isInteger() gate in add-session.js"
      - "WR-05 (advisory, recommended fix before Phase 31 — was addressed): 25-11-toast-behavior.test.js lacked an EXPECTED_COUNT vacuous-green guard"
      - "WR-06 (advisory): app-stub.refreshSnippetCache mutates global.PortfolioDB with finally-only cleanup — mitigated by one-process-per-file execution"
  - phase: 29-reliability-observability
    items:
      - "WR-01 (advisory, open): onupgradeneeded catch nulls the pool only indirectly via onerror — OBS-03 reload recovers in production, not a blocker"
      - "WR-02 (advisory, open): export-modal.js re-derives getClientNameForCopy/sectionHasData independently of another module — no coupling test, deferred to follow-up"
  - phase: 36-code-comments-batch-2
    items:
      - "Deferred by design (D-01, 2026-07-01): backup.js, app.js, pdf-export.js (each 1,500L+) excluded from DOCS-03 banner coverage, deferred to a follow-up batch-3 — not a gap, explicit scope decision"
  - phase: 38-next-session-date-field-with-overview-column
    items:
      - "WR-03 (code review, deferred): mobile-accordion focus clipping — advisory, not a launch blocker"
      - "WR-04 (code review, deferred): error-tone toast adoption limited to the session form; not yet extended to every form in the app"
nyquist:
  compliant_phases: [29, 31, 33, 37]
  partial_phases: [30, 32, 34, 35, 36, 38]
  missing_phases: [28]
  overall: "discovery-only signal, not a milestone blocker — all 6 partial phases have a draft-status VALIDATION.md whose per-task compliance flag was never flipped to true, while the actual behavior-test suite for every phase is green and independently verified in each VERIFICATION.md (131/131 total). Phase 28 has no VALIDATION.md at all (predates the per-phase Nyquist artifact convention)."
---

# Milestone v1.2 Audit — Codebase Health & Reliability

**Audited:** 2026-07-07
**Scope:** Phases 28–38 (11 phases, 82 plans)
**Result:** ✅ PASSED — no critical gaps

## 1. Requirements Coverage (3-source cross-reference)

Cross-referenced REQUIREMENTS.md traceability table (65 REQ-IDs mapped to phases) against each phase's VERIFICATION.md requirements table and SUMMARY.md frontmatter.

- **65 requirements mapped**, spanning VER/OBS/TEST/RFCT/DOCS/I18N/PDFX/DEMO/DOCS-03/DATE/PERS/TERM/FILT/LEGAL/NEXT categories.
- **64 SATISFIED**, all with concrete evidence (test names, line numbers, on-device UAT quotes) in the corresponding VERIFICATION.md.
- **1 DESCOPED** — PDFX-03 (save-before-export guard), invalidated after implementation revealed the export button is only reachable in read-mode on an already-saved session, so no dirty-export path exists to guard. Descoping is documented with rationale and the guard code was removed cleanly; FN-1 ordinal correctness is fully covered by PDFX-02 instead. Treated as resolved, not a gap.
- **0 unsatisfied, 0 orphaned.** Every REQ-ID in the traceability table appears with a SATISFIED/VERIFIED status in at least one phase's VERIFICATION.md.

## 2. Phase Verification Status

| Phase | Status | Notes |
|-------|--------|-------|
| 28 Update Reliability & Versioning | passed | Field-verified live Safari PWA update fix (v1.2.1) |
| 29 Reliability & Observability | passed | Crash log + report flow + IDB reset hatch, all zero-network |
| 30 Test Harness & Coverage | passed | First package.json + npm test; 7 PDF tests fixed; RTL guard |
| 31 Refactor God Modules | passed | settings.js/add-session.js decomposed, suite stayed green throughout |
| 32 README + Code Comments | passed | Maintainer README + 5-file comment pilot |
| 33 DE/CS i18n Completion | passed | 13 export-modal keys, both languages |
| 34 Session PDF Export Visual Polish | passed | Redesigned PDF, RTL/bidi preserved, derived ordinal |
| 35 Demo System Refresh | passed | Demo chrome converged, seed refreshed, exposure locked down |
| 36 Code Comments Batch 2 | passed | ~22 modules banner-commented, 3 giants deferred to batch-3 (by design) |
| 37 Date Consistency + Personalization | passed | Canonical date engine, format picker, session-type list, terminology relabel |
| 38 Next Session Date + Overview Column | passed | Builds on Phase 37 engine; 2 UAT gap-closure rounds, all resolved on-device |

**11/11 phases verified passed.** No unverified phases.

## 3. Cross-Phase Integration & E2E Flows

Spawned `gsd-integration-checker` to independently verify wiring across the phases with the highest cross-cutting risk in this milestone — Phase 37's new date engine reaching every consumer, Phase 35's demo staying in sync with later UI additions, and Phase 31's extracted modules surviving subsequent edits.

**Result: zero broken wiring found.** Specifically confirmed by reading actual source (not just trusting prior claims):

1. Zero leftover `new Date("<calendar-date>")` UTC-parse call sites outside `date-format.js` itself — all remaining `new Date(` hits are legitimate wall-clock timestamps (`createdAt`/`updatedAt`/`exportedAt`).
2. `demo.html` has full parity with `index.html` for Phase 37/38 UI — Session Format filter, Heart-Wall toggle, Next Session sortable column all present (a common risk given Phase 35 shipped before Phase 37/38).
3. `assets/date-format.js` loads before `app.js`/`pdf-export.js`/`export-modal.js` on every app-facing HTML page.
4. `sw.js` PRECACHE_URLS includes all new Phase 31/37/38 assets; `CACHE_NAME` still auto-derives from the Phase 28 single-source integrity token.
5. Phase 31's extracted modules (`settings-snippets.js`, `settings-photos.js`, `settings-session-types.js`, `export-modal.js`) each self-guard their own boot — no duplicate-init regressions from later phases touching the same parent files.
6. `backup.js` round-trips `portfolioDateFormat`, `portfolioSessionTypes`, and `nextSessionDate` with zero backup.js code change needed, confirming the phases' "whole-object export" claims.
7. Overview's "Next Session" column uses the same shared `window.DateFormat`/`App.formatDate` machinery as the rest of the app — Phase 38 correctly builds on Phase 37's engine rather than a parallel implementation.
8. Demo seed's `nextSessionDaysAgo` → `nextSessionDate` runtime conversion (mirroring the existing `daysAgo`→`date` pattern) confirmed correct, not an omission.

**Full test suite: 131/131 passed, 0 failed** (`npm test` run fresh during this audit).

## 4. Nyquist Compliance (discovery only — not a gate)

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 28 | missing | — | predates per-phase Nyquist artifact convention |
| 29 | exists | true | — |
| 30 | exists | false | `/gsd-validate-phase 30` (optional) |
| 31 | exists | true | — |
| 32 | exists | false | `/gsd-validate-phase 32` (optional) |
| 33 | exists | true | — |
| 34 | exists | false | `/gsd-validate-phase 34` (optional) |
| 35 | exists | false | `/gsd-validate-phase 35` (optional) |
| 36 | exists | false | `/gsd-validate-phase 36` (optional) |
| 37 | exists | true | — |
| 38 | exists | false | `/gsd-validate-phase 38` (optional) |

This reflects incomplete per-task documentation in draft-status VALIDATION.md files (the `nyquist_compliant` flag was never flipped to `true`), not missing test coverage — every phase's actual behavior-test suite is independently green and verified in its VERIFICATION.md, and the full suite passes 131/131. Per the memory note `feedback-checker-nyquist-false-positive`, this gate has previously misfired; it is not treated as a milestone blocker here. Optional cleanup, not required for close.

## 5. Tech Debt Summary

All items below are **advisory**, already identified during phase-level code review, and explicitly judged non-blocking by the reviewing agent or Ben at the time:

- **Phase 30** (6 items): fake-test-detector evadability heuristics (WR-01/02), test-helper fidelity gaps (WR-03/04/06) — all low-risk/mitigated.
- **Phase 29** (2 items): IDB pool null-on-error indirection, export-modal.js helper duplication without a coupling test — both deferred, non-blocking.
- **Phase 36**: 3 giant modules (`backup.js`/`app.js`/`pdf-export.js`) explicitly deferred to a future batch-3 comment pass — a scope decision, not a defect.
- **Phase 38** (2 items): mobile-accordion focus clipping (WR-03), error-tone toast not yet adopted beyond the session form (WR-04) — both recorded as follow-ups at Ben's direction.

**Total: 13 advisory items across 4 phases.** None block milestone completion; all are candidates for the v1.2 backlog carry-over or a future "Codebase Health II" pass (already noted in ROADMAP.md Backlog).

## Conclusion

Milestone v1.2 achieves its definition of done. All 65 requirements are satisfied or deliberately descoped with rationale; all 11 phases passed verification; cross-phase integration was independently checked and found clean; the full test suite is green. Ready to close.
