---
phase: 36-code-comments-batch-2
plan: 04
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - assets/version.js
  - assets/add-client.js
  - assets/shared-chrome.js
  - assets/reporting.js
  - assets/globe-lang.js
  - assets/md-render.js
  - assets/demo-seed.js
  - assets/demo.js
  - sw.js
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "add-client.js and reporting.js gain brand-new banners (both header-less today — they start at `document.addEventListener`): add-client.js gets a four-slot banner; reporting.js (57 lines) gets a concise banner per D-05 (D-03, D-04)"
    - "version.js gets a LIGHT de-phase (D-05 — it is already well-headed): keep the header shape + the dual page/SW-scope explanation but strip its `VER-NN` requirement ids (7 in-comment occurrences: header `VER-02`, plus `VER-03`/`VER-06`) AND the `D-08` / `UI-SPEC` cross-refs to plain prose per D-07 — do not rewrite the whole header, just de-tag it"
    - "shared-chrome.js is refined to a four-slot banner; the trivial stubs (globe-lang.js, md-render.js, demo-seed.js 84L; demo.js 21L) get concise 1–3 line banners per D-05 — no fabricated invariants/deps sections (D-03, D-05). NOTE: i18n.js is already compliant (banner + no planning IDs) and is dropped from this plan's scope → 9 files"
    - "sw.js is de-phased + refined (precache list + token-derived cache strategy) as a COMMENT-ONLY edit — it touches no PRECACHE_URLS entry, so NO CACHE_NAME bump is required; the comments-only gate confirms zero code change (D-04, D-06)"
    - "All planning IDs are stripped from the 9 files per option 3 (D-07, supersedes the Phase-32 keep approach): phase/plan, D-NN, T-/CR-/WR-, date-prefixed bug tickets, ISO dates, `.planning/` cross-refs, AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST- become plain prose; only the KEEP allowlist (`AES-256`/`SHA-256`/`UTF-8`/`base64`/schema `v1–v6`/`file.js:NNN`) + design rationale + the sw.js CRITICAL SAFETY NOTE survive (D-04, D-07)"
    - "Every banner and de-phase decision follows `36-COMMENT-STYLE-GUIDE.md` — the canonical comment standard read FIRST by the executor (D-08)"
    - "The edits are provably comments-only: `npm test` stays green AND a deterministic comments-only strip-and-compare shows zero code lines changed across all 9 files (D-06)"
  prohibitions:
    - "The three deferred giants are NOT modified — `git diff --name-only` for this plan must contain none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01 scope fence)"
    - "No CACHE_NAME bump and no PRECACHE_URLS edit in sw.js — the banner change is comment-only; a version/cache bump would be a code change and fail the D-06 gate"
    - "Zero code lines change in any file — comment-only plan (D-06)"
  artifacts:
    - path: assets/add-client.js
      provides: "Brand-new four-slot banner (add-client page; new banner, header-less today)"
    - path: assets/reporting.js
      provides: "Brand-new concise banner (thin reporting page; new banner, header-less today) — D-05"
    - path: assets/version.js
      provides: "Light de-phase (well-headed; header shape + dual-scope rationale kept, VER-NN/D-08/UI-SPEC ids stripped to prose per D-07) — D-05"
    - path: assets/shared-chrome.js
      provides: "Refined four-slot banner (footer/nav chrome across pages)"
    - path: assets/globe-lang.js
      provides: "Concise banner (globe language selector) — D-05"
    - path: assets/md-render.js
      provides: "Concise banner (markdown → HTML/text renderer) — D-05"
    - path: assets/demo-seed.js
      provides: "Concise banner (demo seed loader) — D-05"
    - path: assets/demo.js
      provides: "1-line banner (demo-mode controller) — D-05"
    - path: sw.js
      provides: "De-phased + refined banner (precache + cache strategy); comment-only, no CACHE_NAME bump"
  key_links:
    - from: "the 10 banners"
      to: "assets/export-modal.js lines 1-19 (convention template) + .planning/codebase/ARCHITECTURE.md + CONVENTIONS.md"
      via: "each banner copies the four-slot shape, proportionate per D-05 for the stubs: owns · public window.* surface · cross-window.* deps · invariants"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-03 (batch-2 — the small/chrome/sw/stubs group) — Extend the Phase 32 convention to the remaining 9 modules: `assets/version.js`, `assets/add-client.js`, `assets/shared-chrome.js`, `assets/reporting.js`, `assets/globe-lang.js`, `assets/md-render.js`, `assets/demo-seed.js`, `assets/demo.js`, and root `sw.js`. (`assets/i18n.js` was in this group but is ALREADY compliant under D-07 — 3-line loader banner + no planning IDs, verified at current code — so it is dropped from active scope.) Two comment-only jobs per D-04: (a) header-less files (`add-client.js`, `reporting.js`) get brand-new banners; the rest get de-phased + refined banners, PROPORTIONATE per D-05 — trivial stubs (globe-lang/md-render/demo-seed/demo) get a concise 1–3 line banner (no fabricated sections), and version.js (already well-headed) gets a LIGHT de-phase only; (b) all planning IDs are stripped per option 3 (D-07 — incl. requirement IDs REQ-/OBS-/VER-). sw.js's banner refinement is comment-only and must NOT trigger a CACHE_NAME bump. PRIMARY template = `assets/settings.js:1-29`; export-modal:1-19 is a specialized variant only; every banner follows `36-COMMENT-STYLE-GUIDE.md` (D-08). Verify comment-only per D-06.

Depends on 36-01 for sequencing only (D-02); disjoint file set → no artifact dependency.

Purpose: this batch completes the 22-module core coverage — the app-page entry points, the shared chrome, the service worker, and the loader/renderer stubs. Proportionality (D-05) keeps the 4-line loader stub from getting a 4-section banner.
Output: 10 comment-only edited files; no new files; no new code symbols; no behavior change. The three giants (`backup.js`, `app.js`, `pdf-export.js`) are NOT touched (D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-comments-batch-2/36-CONTEXT.md
@.planning/phases/36-code-comments-batch-2/36-VALIDATION.md
@.planning/phases/32-readme-code-comments/32-03-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@assets/settings.js
@assets/export-modal.js
</context>

## Artifacts this phase produces (this plan)

- **No new file paths.** All nine targets already exist (i18n.js dropped — already compliant); this plan edits comments only (`add-client.js` and `reporting.js` gain a header banner, but no new code symbols).
- **No new code symbols** — no new functions, classes, flags, fields, `window.*` globals, or exports. The only artifacts are the banner comment blocks. Nothing here for a drift / source-grounding pass to flag.

<tasks>

<!-- The de-phase verify gate below greps for build-history literals that also appear in the <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: add-client.js + reporting.js (new banners) + shared-chrome.js (refine) + version.js (light de-phase)</name>
  <files>assets/add-client.js, assets/reporting.js, assets/shared-chrome.js, assets/version.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — canonical standard; the ≥3-of-4 reshape-vs-light-de-phase test governs version.js; the option-3 strip-ALL-IDs split incl. `VER-` — D-08)
    - assets/settings.js (lines 1-29 — the PRIMARY four-slot banner content model)
    - assets/add-client.js (starts at code `document.addEventListener("DOMContentLoaded", …)` — NO header today; ~264 lines — read for the add/edit-client form, referral-source options, the shared `CropModule`/`readFileAsDataURL` usage)
    - assets/reporting.js (starts at code `document.addEventListener("DOMContentLoaded", …)` — NO header today; ~57 lines — thin page module; read for what it renders)
    - assets/shared-chrome.js (JSDoc header `shared-chrome.js — Shared footer and navigation utilities for all pages`; ~185 lines — read for the `window.SharedChrome` surface, footer/nav/back-links, localized legal links, inline FOOTER_STRINGS)
    - assets/version.js (JSDoc header `version.js — Single source of truth for the app version (VER-02)`; ~353 lines — already well-headed; 7 in-comment `VER-NN` ids + a `D-08` tag (L34) + a `UI-SPEC` cross-ref (L136) to strip; read for the `AppVersion` object usable in BOTH page + SW scope, the integrity token, and the SW cache-mismatch detection)
    - assets/export-modal.js (lines 1-19 — SPECIALIZED-VARIANT reference only; do NOT model inline style on it)
    - .planning/codebase/ARCHITECTURE.md (the `AppVersion` + `SharedChrome` component rows; Page-Modules layer for add-client/reporting)
  </read_first>
  <action>
    add-client.js — BRAND-NEW four-slot banner (header-less today). owns (the add/edit-client form — fields, referral-source options, inline photo capture via the shared CropModule); public surface (page module, no `window.*` export; boots on `DOMContentLoaded`); dependencies (`window.App`, `window.PortfolioDB`, `window.CropModule`, `App.readFileAsDataURL`); invariants (user text via `textContent`/`.value`, never `innerHTML`). De-phase any body archaeology.

    reporting.js — BRAND-NEW CONCISE banner (header-less, ~57 lines → D-05: a concise banner, not a padded four-section block). State what the reporting page owns and its `App` + `PortfolioDB` deps in 1–3 lines.

    shared-chrome.js — DE-PHASE + REFINE (keep JSDoc style). Four-slot banner: owns (shared footer + nav chrome, back-links, localized legal links); public surface (`window.SharedChrome`); dependencies (used alongside app.js on app pages, and standalone on license/legal pages — note the inline FOOTER_STRINGS for portability where i18n.js is absent); invariants. De-phase the body.

    version.js — LIGHT DE-PHASE (D-05 — it is already well-headed; do NOT rewrite the whole header, just de-tag it). Keep the header shape + the dual page/SW-scope explanation (it assigns to `self`/`globalThis` so the SW can importScripts it), but STRIP its planning ids per D-07: all 7 in-comment `VER-NN` occurrences (restate the sentence in words, e.g. `(VER-02)` in the title → gone, "single source of truth for the app version" stays), the `D-08` tag (L34), and the `UI-SPEC` cross-ref (L136). Scan the body for any phase/plan tags and de-phase them; keep the design rationale.

    Use the SAME strip-ALL-IDs rule for ALL FOUR (option 3, D-07): strip phase/plan, D-NN, T-/CR-/WR-, acceptance-test, date-prefixed bug tickets, ISO dates, `.planning/` cross-refs (`UI-SPEC`, `NN-RESEARCH`), AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST- (Gap/round-N/Change-N are a MANUAL eyeball); PRESERVE only the KEEP allowlist + rationale. Scan EVERY comment incl. trailing comments on code lines; rewrite comment text only, never code. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION)|UI-SPEC)' assets/add-client.js assets/reporting.js assets/shared-chrome.js assets/version.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
    <automated>head -3 assets/add-client.js | grep -qE '^[[:space:]]*//' && head -3 assets/reporting.js | grep -qE '^[[:space:]]*//' && echo NEW_BANNERS_OK</automated>
    <automated>! grep -q 'VER-0' assets/version.js && echo VER_ID_STRIPPED</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `assets/add-client.js` and `assets/reporting.js` now open with a `//` banner (head grep passes) — both previously started with code.
    - The option-3 de-phase grep over all FOUR files returns empty — ZERO ID-shaped tokens remain (incl. requirement IDs REQ-/OBS-/VER- and the `UI-SPEC` cross-ref); the KEEP allowlist is NOT flagged.
    - version.js `VER-0` no longer appears (grep passes with `!`) — the requirement id is stripped to prose per D-07, but the header shape + dual page/SW-scope explanation are kept (NOT wholesale-rewritten) — D-05 light de-phase (manual).
    - MANUAL (class-(b) prose tags): no `Gap`/`round-N`/`Change N` prose tags survive.
    - reporting.js banner is concise (D-05 — not a padded four-section block); shared-chrome.js opens with the four labelled slots naming `window.SharedChrome` (manual).
    - Comments-only: covered by Task 3's strip-and-compare gate (all four files included).
  </acceptance_criteria>
  <done>add-client.js + reporting.js have new banners (reporting concise per D-05), shared-chrome.js is refined, version.js is lightly de-phased with VER-NN + dual-scope rationale preserved; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: sw.js (de-phase + refine, NO cache bump) + globe-lang/md-render/demo-seed/demo (concise D-05 banners)</name>
  <files>sw.js, assets/globe-lang.js, assets/md-render.js, assets/demo-seed.js, assets/demo.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — canonical standard; the D-05 Proportionality rule governs the four stubs; PRESERVE the sw.js CRITICAL SAFETY NOTE (it is an invariant, not archaeology) — D-08)
    - assets/settings.js (lines 1-29 — the PRIMARY four-slot banner content model, applied proportionately)
    - sw.js (JSDoc header `sw.js — Sessions Garden Service Worker` with a CRITICAL SAFETY NOTE; ~285 lines — read for the PRECACHE URL list, the `INTEGRITY_TOKEN`-derived CACHE_NAME, and the install/activate/fetch handlers; note it importScripts `/assets/version.js`)
    - assets/globe-lang.js (JSDoc header; ~84 lines — the globe language selector component `initGlobeLang({…})`)
    - assets/md-render.js (JSDoc header; ~81 lines — minimal Markdown → HTML renderer for the export preview; registers `window.MdRender`)
    - assets/demo-seed.js (thin `/* === DEMO SEED DATA === */` block; ~84 lines — fetches demo-seed-data.json, clears + seeds demo_portfolio on load; must run after db.js, before overview.js)
    - assets/demo.js (thin `/* === DEMO MODE CONTROLLER === */` block; ~21 lines — language sync from the parent landing page)
    - assets/export-modal.js (lines 1-19 — SPECIALIZED-VARIANT reference only; do NOT model inline style on it)
    - .planning/codebase/ARCHITECTURE.md (the Service Worker layer + "IndexedDB never touched by SW" constraint; `MdRender` component row; Demo Mode section)
    - reference-pre-commit-sw-bump (memory): editing sw.js comments does NOT require a CACHE_NAME bump; the pre-commit hook skips the bump when sw.js is in the diff
  </read_first>
  <action>
    sw.js — DE-PHASE + REFINE (keep JSDoc style; PRESERVE the CRITICAL SAFETY NOTE that the SW never touches IndexedDB — that is an invariant, not archaeology). Complete/refine the four-slot banner: owns (static-asset precache + cache-first fetch strategy); public surface (SW lifecycle — install/activate/fetch); dependencies (importScripts `/assets/version.js` for the `INTEGRITY_TOKEN` that derives CACHE_NAME); invariants (SW never touches IndexedDB; cache name is token-derived so a new deploy rolls the cache; the importScripts path must be a shipped path). De-phase any body tags. CRITICAL: this is a COMMENT-ONLY edit — do NOT touch any PRECACHE_URLS entry and do NOT bump CACHE_NAME/APP_VERSION. Because only comments change, no cache roll is needed (the comments-only gate in Task 3 confirms zero code change; the pre-commit hook also skips the bump when sw.js is in the diff). Adding a bump here would be a CODE change and would FAIL the D-06 gate.

    The four stubs — CONCISE banners per D-05 (do NOT fabricate invariants/deps sections where there is nothing to say):
    - globe-lang.js: 1–3 lines — the shared globe language-selector component (`initGlobeLang`), RTL-aware.
    - md-render.js: 1–3 lines — the minimal Markdown → HTML/text renderer for the export preview; registers `window.MdRender`; state the supported subset briefly.
    - demo-seed.js: 1–3 lines — fetches + seeds demo_portfolio on load; must run after db.js, before overview.js.
    - demo.js: 1 line — the demo-mode controller (language sync from the parent landing page; data comes from the isolated demo_portfolio DB).
    De-phase any build tags in all four. (i18n.js is NOT in scope — already compliant.)

    Use the SAME strip-ALL-IDs rule across all five files (option 3, D-07): strip phase/plan, D-NN, T-/CR-/WR-, acceptance-test, date-prefixed bug tickets, ISO dates, `.planning/` cross-refs, AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST- (Gap/round-N/Change-N are a MANUAL eyeball); PRESERVE only the KEEP allowlist + rationale AND the sw.js CRITICAL SAFETY NOTE (that is an invariant, not archaeology). Rewrite comment text only, never code. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION))' sw.js assets/globe-lang.js assets/md-render.js assets/demo-seed.js assets/demo.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';const a=strip(cp.execSync('git show '+base+':sw.js').toString());const b=strip(fs.readFileSync('sw.js','utf8'));if(a!==b){console.error('SW CODE CHANGED — no cache bump / no PRECACHE edit allowed');process.exit(1);}console.log('SW_COMMENTS_ONLY_OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The option-3 de-phase grep over all FIVE files returns empty — ZERO ID-shaped tokens remain (incl. requirement IDs); the KEEP allowlist is NOT flagged; the sw.js CRITICAL SAFETY NOTE (SW never touches IDB) is PRESERVED (manual).
    - MANUAL (class-(b) prose tags): no `Gap`/`round-N`/`Change N` prose tags survive.
    - sw.js is comment-only: the SW-specific strip-and-compare prints `SW_COMMENTS_ONLY_OK` — CACHE_NAME/APP_VERSION unchanged, no PRECACHE_URLS entry touched (D-06 + the no-bump prohibition).
    - The four stubs each open with a concise D-05 banner (1–3 lines; demo.js a single line) — not padded (manual).
    - Comments-only: also covered by Task 3's strip-and-compare gate (all five files included).
  </acceptance_criteria>
  <done>sw.js is de-phased + refined comment-only with no cache bump (SAFETY NOTE preserved); the five stubs carry concise D-05 banners; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 9-file edit is comments-only</name>
  <files>assets/version.js, assets/add-client.js, assets/shared-chrome.js, assets/reporting.js, assets/globe-lang.js, assets/md-render.js, assets/demo-seed.js, assets/demo.js, sw.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — the comment standard whose comments-only discipline this gate enforces)
    - .planning/phases/32-readme-code-comments/32-03-PLAN.md (Task 3 — the authoritative strip-and-compare gate this mirrors + the soundness argument)
    - .planning/phases/36-code-comments-batch-2/36-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove all nine files this plan touched changed comments only (i18n.js is out of scope — untouched). Run the strip-and-compare with the comment edits from Tasks 1–2 still UNCOMMITTED, so the baseline is git HEAD: for each file, strip block/line/trailing comments and collapse whitespace from BOTH the HEAD version (`git show HEAD:file`) and the working-tree version, then assert byte-equality. Any inequality means a code token changed → the gate FAILS; revert before committing. One-shot scratchpad Node script (not a committed test). If you committed content per-task, set `BASE` to the commit immediately before this plan's first edit and compare against that ref. Confirm the full suite is green. Confirm the scope fence: `git diff --name-only` lists none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01), and re-confirm sw.js's CACHE_NAME/APP_VERSION are unchanged. Only commit once all gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';let bad=0;for(const f of ['assets/version.js','assets/add-client.js','assets/shared-chrome.js','assets/reporting.js','assets/globe-lang.js','assets/md-render.js','assets/demo-seed.js','assets/demo.js','sw.js']){const a=strip(cp.execSync('git show '+base+':'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all nine files (D-06), run uncommitted against git HEAD (or against the pre-plan `BASE` ref).
    - If any file prints `CODE CHANGED`, a code line was modified — revert before committing (special attention: sw.js must show no CACHE_NAME/APP_VERSION change).
    - Scope fence (D-01): `git diff --name-only` excludes assets/backup.js, assets/app.js, assets/pdf-export.js.
  </acceptance_criteria>
  <done>The 9-file comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to existing browser-tier modules + the service worker. No new runtime code, network calls, or data flows. sw.js's precache/fetch strategy is unchanged — banner only, no cache roll. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04 | Tampering | comment edits silently altering code across the 9 files (esp. an accidental sw.js CACHE_NAME/PRECACHE change) | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + a dedicated sw.js SW-only strip-and-compare + green `npm test` prove no behavior change (D-06); no CACHE_NAME bump; edits isolated in their own commit(s). |

Honest framing: comment-only edits add no attack surface. No new schema push applies (schema-gate: N/A for comments). The one SW-specific risk (an errant cache/precache change) is caught mechanically by the dedicated sw.js gate and the no-bump prohibition.
</threat_model>

<verification>
- `npm test` green after the edits.
- Option-3 de-phase grep over all 9 files returns empty across EVERY ID-shaped dialect incl. requirement IDs REQ-/OBS-/VER- (D-04, D-07); the sw.js SAFETY NOTE preserved; version.js `VER-0` stripped; KEEP allowlist NOT flagged; class-(b) prose tags checked manually.
- add-client.js + reporting.js open with new banners; version.js lightly de-phased (ids stripped, shape kept); the four stubs carry concise D-05 banners; sw.js is comment-only with no cache bump. (i18n.js dropped — already compliant.)
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-06); sw.js SW-only gate prints `SW_COMMENTS_ONLY_OK`.
- Scope fence (D-01): the three giants are untouched.
</verification>

<success_criteria>
The 9 small/chrome/sw/stub modules carry Phase-32-convention banners (proportionate per D-05 — concise for stubs, light de-phase for version.js), all planning IDs stripped to prose per option 3 (D-07); sw.js is refined comment-only with no cache bump; the edits are provably comments-only (strip-and-compare + green suite). This completes the core coverage for DOCS-03 (i18n.js was already compliant).
</success_criteria>

<output>
Create `.planning/phases/36-code-comments-batch-2/36-04-SUMMARY.md` when done.
</output>
