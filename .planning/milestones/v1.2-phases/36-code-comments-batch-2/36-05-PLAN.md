---
phase: 36-code-comments-batch-2
plan: 05
type: execute
wave: 3
depends_on: ["36-01"]
files_modified:
  - assets/settings-snippets.js
  - assets/settings-photos.js
  - assets/export-modal.js
  - assets/settings.js
  - assets/add-session.js
autonomous: false
requirements: [DOCS-03]
must_haves:
  truths:
    - "The 5 already-shipped Phase-32 pilot files have their leftover planning IDs stripped from BANNER AND BODY per option 3 (D-07, D-09): requirement IDs (REQ-/OBS-/VER-/PDFX-/FN-/DOCS-/DEMO-/I18N-/TEST-/RFCT-), decision IDs (D-NN), phase/plan tags, `.planning/` cross-refs (`34-RESEARCH`, `UI-SPEC`), and dangling dated test-file refs become plain prose — the WHY each carried is kept"
    - "The existing banners are NOT rewritten — they are already good (settings.js:1–29 is the phase's PRIMARY exemplar; export-modal.js:1–19 the specialized variant): this plan is an ID/tag sweep + minimal prose fixes only, no banner reshape (D-09)"
    - "export-modal.js's body is swept specifically: the decorative divider `// ── FN-1 / D-03 / PDFX-02: … ──` (L23) → `// ── derived chronological session ordinal ──`; the `34-RESEARCH Pitfall 2` cross-ref (L31) is rewritten to the reason it encodes (parsing the ISO date drags in TZ/locale ambiguity); the `REQ-7…REQ-19` / `Phase 34 (34-09, D-08)` / `PDFX-01` / `D-03`/`D-08`/`D-09` body tags become prose; the dangling `tests/quick-260615-export-section-order.test.js` reference (a removed test) is de-phased (D-07)"
    - "Every de-phase decision follows `36-COMMENT-STYLE-GUIDE.md` — the canonical standard read FIRST by the executor; the KEEP allowlist (`AES-256`/`SHA-256`/`UTF-8`/`base64`/schema `v1–v6`/live `file.js:NNN` refs) is untouched (D-07, D-08)"
    - "The edits are provably comments-only: `npm test` stays green AND a deterministic comments-only strip-and-compare shows zero code lines changed across all 5 files (D-06)"
  prohibitions:
    - "The three deferred giants are NOT modified — `git diff --name-only` for this plan must contain none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01 scope fence)"
    - "The existing banners are NOT reshaped or rewritten — only IDs/tags are removed and minimal prose repaired (D-09)"
    - "Zero code lines change in any file — comment-only plan (D-06)"
  artifacts:
    - path: assets/settings-snippets.js
      provides: "Verified clean (Phase-32 de-phased) — ID sweep confirms no leftover planning IDs; no edit if already clean"
    - path: assets/settings-photos.js
      provides: "Body ID sweep — the two `UI-SPEC:` neutral-tone-confirm cross-refs (L232, L460) stripped to prose"
    - path: assets/export-modal.js
      provides: "Heavy body ID sweep — divider L23, `34-RESEARCH` L31, REQ-/PDFX-/FN-/D-NN/Phase-34 body tags, dangling test-file ref; banner (L1–19) untouched"
    - path: assets/settings.js
      provides: "Body ID sweep — `SPEC REQ-2` (L33), `OBS-01` (L562/L679), `UI-SPEC` (L817/L916) stripped to prose; banner (L1–29) untouched — it is the phase PRIMARY exemplar"
    - path: assets/add-session.js
      provides: "Body ID sweep — `REQ-3`/`REQ-5` section-visibility tags (L813/L881/L1235) stripped to prose; banner untouched"
  key_links:
    - from: "the 5 pilot-file ID sweeps"
      to: ".planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (Part B option-3 strip rules + the export-modal divider before→after example)"
      via: "each leftover ID/tag is de-phased to plain prose per the guide's class-(a) ID-shaped + class-(b) prose-shaped split"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-03 (batch-3 — the pilot-file ID sweep, D-09) — Bring the 5 already-shipped Phase-32 pilot files under option 3 (D-07) so the whole codebase is consistent: `assets/settings-snippets.js`, `assets/settings-photos.js`, `assets/export-modal.js`, `assets/settings.js`, `assets/add-session.js`. These files got their banners + de-phasing in Phase 32 under the OLD keep-`REQ`/`OBS`/`VER` rule — this plan strips the leftover planning IDs from BOTH banner and body to plain prose. **Their banners already exist and are good — do NOT rewrite them; this is an ID/tag removal + minimal prose fix only.** export-modal.js's body carries the heaviest residue (the `FN-1 / D-03 / PDFX-02` divider @L23, the `34-RESEARCH` cross-ref @L31, REQ-/PDFX-/Phase-34 body tags, and a dangling dated test-file reference) and is called out specifically. Sequenced last (D-09), after the round-1 human checkpoint. Verify comment-only per D-06.

Depends on 36-01 for sequencing only (D-02 — after the batch-1/round-1 checkpoint establishes the rhythm); its file set is disjoint from 36-01…36-04, so there is no artifact dependency.

Purpose: after 36-01…36-04 strip all planning IDs from the 22 core modules under D-07, these 5 pilot files are the only remaining place a `REQ-`/`OBS-`/`D-NN` survives in shipped code — this sweep closes the consistency gap so a single de-phase grep over the whole `assets/*.js` surface comes back clean.
Output: up to 5 comment-only edited files (settings-snippets.js may already be clean → no edit); no new files; no new code symbols; no behavior change. The three giants (`backup.js`, `app.js`, `pdf-export.js`) are NOT touched (D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-comments-batch-2/36-CONTEXT.md
@.planning/phases/36-code-comments-batch-2/36-VALIDATION.md
@.planning/phases/32-readme-code-comments/32-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@assets/settings.js
</context>

## Artifacts this phase produces (this plan)

- **No new file paths.** All five targets already exist and already have good banners; this plan removes leftover planning IDs from their comments only.
- **No new code symbols** — no new functions, classes, flags, fields, `window.*` globals, or exports. The only change is comment text (ID/tag removal). Nothing here for a drift / source-grounding pass to flag.

<tasks>

<!-- The de-phase verify gate below greps for planning-ID literals that also appear in the <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: settings-snippets.js + settings-photos.js + settings.js — body ID sweep (banners untouched)</name>
  <files>assets/settings-snippets.js, assets/settings-photos.js, assets/settings.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — Part B option-3 strip rules; the class-(a) ID-shaped/grep vs class-(b) prose-shaped/manual split; the KEEP allowlist; D-08)
    - assets/settings.js (the PRIMARY exemplar — its banner L1–29 is the model and stays UNCHANGED; the leftover IDs are all in the body: `SPEC REQ-2` at L33, `OBS-01` at L562 and L679, `UI-SPEC` at L817 and L916)
    - assets/settings-snippets.js (Phase-32-de-phased already — read to CONFIRM no leftover ID survives; if clean, make no edit)
    - assets/settings-photos.js (leftover `UI-SPEC:` cross-refs at L232 and L460, both on the optimize-all neutral-tone confirm — strip to prose)
  </read_first>
  <action>
    Sweep leftover planning IDs from the BODY of these three files per option 3 (D-07). **Do NOT touch the banners — settings.js:1–29 is the phase's PRIMARY exemplar and is already correct.** Only remove the ID/tag and keep the sentence's meaning:
    - settings-snippets.js — CONFIRM clean (Phase 32 already de-phased it). If the de-phase grep is already empty for this file, make zero edits; it is listed only to prove consistency.
    - settings-photos.js — rewrite the two `UI-SPEC:` neutral-tone-confirm cross-refs (L232, L460) into plain prose, e.g. `// UI-SPEC: irreversible but visual quality stays the same.` → `// Irreversible but visual quality stays the same.` Keep the neutral-tone rationale.
    - settings.js — body only: `// Three rows are disable-only per SPEC REQ-2: …` (L33) → drop `SPEC REQ-2`, keep "disable-only … their purpose is structurally fixed"; the two `OBS-01 surfacing:` comments (L562, L679) → drop `OBS-01`, keep "the Report-a-problem entry row"; the two `UI-SPEC` neutral-confirm notes (L817, L916) → drop `UI-SPEC`, keep the disabling-requires-a-neutral-confirm rationale.

    Strip per the guide: requirement IDs REQ-/OBS-/VER-/PDFX-/FN-/DOCS-/DEMO-/I18N-/TEST-/RFCT-, D-NN, T-N-N-N, CR-/WR-, phase/plan tags, `.planning/` cross-refs (`UI-SPEC`, `NN-RESEARCH`), date-prefixed tickets, ISO dates. The prose-shaped Gap/round-N/Change-N tags are a MANUAL eyeball. PRESERVE only the KEEP allowlist + security rationale. Scan trailing comments on code lines too. Rewrite comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR|FN)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION)|UI-SPEC)' assets/settings-snippets.js assets/settings-photos.js assets/settings.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
    <automated>head -3 assets/settings.js | grep -qE '^[[:space:]]*(/\*\*|//)' && echo SETTINGS_BANNER_INTACT</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The option-3 de-phase grep over all THREE files returns empty — ZERO ID-shaped tokens remain (settings.js's `REQ-2`/`OBS-01`/`UI-SPEC`, settings-photos.js's `UI-SPEC`); the KEEP allowlist is NOT flagged.
    - MANUAL (class-(b) prose tags): no `Gap`/`round-N`/`Change N` prose tags survive.
    - settings.js's banner (L1–29) is byte-unchanged (it is the PRIMARY exemplar — NOT reshaped); only body comments changed (manual + strip-and-compare confirms).
    - Comments-only: covered by Task 3's strip-and-compare gate (all three files included).
  </acceptance_criteria>
  <done>settings-snippets.js confirmed clean; settings-photos.js and settings.js have their body planning IDs stripped to prose with banners untouched; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: export-modal.js (heavy body sweep) + add-session.js — banners untouched</name>
  <files>assets/export-modal.js, assets/add-session.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — Part B has the EXACT before→after for the export-modal divider (L23) and the ⚠️ "do NOT model inline style on export-modal" note; option-3 strip rules + KEEP allowlist; D-08)
    - assets/export-modal.js (banner L1–19 is the SPECIALIZED-VARIANT reference and stays UNCHANGED; the body carries the residue: the divider `// ── FN-1 / D-03 / PDFX-02: … ──` (L23), the `34-RESEARCH Pitfall 2` cross-ref (L31), the `FN-1` mentions (L66, L575, L900), `REQ-7…REQ-19`/`REQ-16` (L282, L284), `Phase 34 (34-09, D-08)` (L372), `PDFX-01`/`D-03`/`D-08`/`D-09`/`34-09` (L574–L583), and the dangling `tests/quick-260615-export-section-order.test.js` reference (L258, L395 — that test file does NOT exist in tests/, so the reference dangles → de-phase it))
    - assets/add-session.js (banner is already a good four-slot `//` block and stays UNCHANGED; the body carries `REQ-3`/`REQ-5` section-visibility tags at L813, L881, L1235 — strip to prose)
    - .planning/codebase/ARCHITECTURE.md (the export/session-ordinal flow — confirm the WHY each stripped tag encoded so the prose that replaces it stays accurate)
  </read_first>
  <action>
    export-modal.js — HEAVY BODY SWEEP, banner (L1–19) untouched. Apply the guide's option-3 rules to every body comment:
    - L23 divider: `// ── FN-1 / D-03 / PDFX-02: derived chronological session ordinal ──` → `// ── derived chronological session ordinal ──` (strip the id-stack, keep the divider + label — this is the guide's worked example).
    - L31: rewrite `(34-RESEARCH Pitfall 2)` into the reason it encodes — the deliberate no-`new Date()` avoids TZ/locale ambiguity when parsing the ISO date (KEEP the live-code ref `db.js:225` on L28, per the cross-reference rule).
    - L66, L575, L900: drop the `FN-1` tag, keep the ordinal-derivation / test-seam explanation.
    - L282, L284: `// Export modal (REQ-7 to REQ-15, REQ-17, REQ-19)` and `// No Translate CTA (REQ-16 removed)` → plain prose describing what the block renders (no REQ ids).
    - L372: `// Phase 34 (34-09, D-08): the issues/severity section is NO LONGER emitted …` → keep the "no longer emitted" behavior statement, drop the phase/plan/decision tags.
    - L574–L583: the `buildSessionPDF` purity note — drop `PDFX-01`, `D-03`, `D-08`, `D-09`, `34-09`; keep the "pure function of its inputs / sessionNumber is the chronological ordinal / severity bars drawn from data / date is the card's session date" prose.
    - L258, L395: the `tests/quick-260615-export-section-order.test.js` pointer is DANGLING (that file was removed) → de-phase it (describe what the assertion guarantees, without the dead dated filename). NB: real test files with dated names that STILL exist (e.g. `tests/quick-260516-g7p-…`) would be live-code refs to KEEP — but this specific one dangles, so strip.

    add-session.js — BODY SWEEP, banner untouched. `// Section visibility (REQ-3, REQ-5)` (L813), `// REQ-5: visible, badge shown, inputs remain` (L881), `// … hide disabled sections from the form per REQ-3.` (L1235) → drop the REQ ids, keep the section-visibility behavior description.

    Strip per the guide (requirement IDs incl. FN-/PDFX-, D-NN, phase/plan, `.planning/` cross-refs, dangling dated test refs, ISO dates; Gap/round/Change = MANUAL). PRESERVE the KEEP allowlist + live-code refs (`db.js:225`) + security rationale. Rewrite comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR|FN)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION)|UI-SPEC)' assets/export-modal.js assets/add-session.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The option-3 de-phase grep over BOTH files returns empty — ZERO ID-shaped tokens remain (export-modal's FN-/D-/PDFX-/REQ-/34-RESEARCH/34-09/Phase-34 residue and the dangling test-file ref are all gone; add-session's REQ-3/REQ-5 gone); the KEEP allowlist is NOT flagged.
    - MANUAL (class-(b) prose tags): no `Gap`/`round-N`/`Change N` prose tags survive.
    - export-modal.js's L23 divider reads `// ── derived chronological session ordinal ──`; its banner (L1–19) is byte-unchanged; the live-code ref `db.js:225` is preserved (manual).
    - add-session.js's banner is byte-unchanged; only body comments changed (manual + strip-and-compare).
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>export-modal.js's heavy body residue and add-session.js's REQ tags are stripped to prose; both banners untouched; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 5-file pilot sweep is comments-only</name>
  <files>assets/settings-snippets.js, assets/settings-photos.js, assets/export-modal.js, assets/settings.js, assets/add-session.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — the comment standard whose comments-only discipline this gate enforces)
    - .planning/phases/32-readme-code-comments/32-02-PLAN.md (Task 3 — the authoritative strip-and-compare gate this mirrors + the soundness argument)
    - .planning/phases/36-code-comments-batch-2/36-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the five pilot files this plan touched changed comments only. Run the strip-and-compare with the comment edits from Tasks 1–2 still UNCOMMITTED, so the baseline is git HEAD (the pre-edit committed version): for each file, strip block/line/trailing comments and collapse whitespace from BOTH the HEAD version (`git show HEAD:file`) and the working-tree version, then assert byte-equality. Any inequality means a code token changed → the gate FAILS; revert before committing. One-shot scratchpad Node script (not a committed test). If you committed content per-task, set `BASE` to the commit immediately before this plan's first edit and compare against that ref. Confirm the full suite is green. Confirm the scope fence: `git diff --name-only` for this plan lists none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01). Only commit the comment edits once all gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';let bad=0;for(const f of ['assets/settings-snippets.js','assets/settings-photos.js','assets/export-modal.js','assets/settings.js','assets/add-session.js']){const a=strip(cp.execSync('git show '+base+':'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all five files (D-06), run uncommitted against git HEAD (or against the pre-plan `BASE` ref if interim-committed).
    - If any file prints `CODE CHANGED`, a code line was modified — revert that change before committing.
    - Scope fence (D-01): `git diff --name-only` for this plan excludes assets/backup.js, assets/app.js, assets/pdf-export.js.
  </acceptance_criteria>
  <done>The 5-file pilot ID sweep is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only ID-sweep edits to existing browser-tier IIFE modules that already shipped in Phase 32. No new runtime code, network calls, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05 | Tampering | an ID-sweep comment edit silently altering code in one of the 5 pilot files (esp. export-modal.js, the heaviest sweep) | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green `npm test` prove no behavior change (D-06); edits isolated in their own commit(s). |

Honest framing: comment-only ID removal adds no attack surface. No new schema push applies (schema-gate: N/A for comments). ASVS controls are untouched; the only risk is an accidental code edit, which the D-06 strip-and-compare gate catches mechanically.
</threat_model>

<verification>
- `npm test` green after the edits.
- Option-3 de-phase grep over all 5 pilot files returns empty across EVERY ID-shaped dialect incl. requirement IDs REQ-/OBS-/VER-/PDFX-/FN- and the `34-RESEARCH`/`UI-SPEC` cross-refs (D-07, D-09); the KEEP allowlist is NOT flagged; class-(b) prose tags checked manually.
- The existing banners are byte-unchanged (settings.js:1–29 PRIMARY exemplar; export-modal.js:1–19 variant; add-session.js banner) — this was an ID sweep, not a banner reshape (D-09).
- export-modal.js's L23 divider is de-phased; the dangling `quick-260615` test-file reference is gone.
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-06).
- Scope fence (D-01): the three giants are untouched.
</verification>

<success_criteria>
The 5 Phase-32 pilot files are brought under option 3 (D-07): every leftover planning ID in banner and body is stripped to plain prose, with the good existing banners left untouched (D-09) — so a single de-phase grep over the whole `assets/*.js` surface now comes back clean. The edits are provably comments-only (strip-and-compare + green suite).
</success_criteria>

<output>
Create `.planning/phases/36-code-comments-batch-2/36-05-SUMMARY.md` when done.
</output>
