---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/37-date-format.test.js
  - tests/34-date-locale.test.js
autonomous: true
requirements: [DATE-01, DATE-02, DATE-03, DATE-04, DATE-06, DATE-07]
must_haves:
  truths:
    - "A TZ-pinned falsifiable test exists that asserts App.formatDate('2026-07-02') === 'Jul 2, 2026' under TZ=America/New_York and FAILS against current code (returns 'Jul 1, 2026') — the D-20 spine"
    - "The test proves the fix by executing the REAL module via vm/require, never by asserting on source text (D-20, MEMORY feedback-behavior-verification)"
    - "One assertion per D-05 format option (auto/month-day-year/day-month-year/mm/dd/yyyy/dd/mm/yyyy/yyyy-mm-dd) plus the Hebrew numeric LTR-isolate unit assertion (D-04/D-07)"
    - "tests/34-date-locale.test.js is rewritten to assert the FIXED en-US behavior + raw-ISO export chain (D-19), replacing the old assertions of buggy behavior"
  artifacts:
    - "tests/37-date-format.test.js (NEW) — TZ-pinned engine spec"
    - "tests/34-date-locale.test.js (REWRITTEN) — asserts fixed behavior"
  key_links:
    - "TZ pin must take effect before any Date use — process.env.TZ set first line + re-exec (V8 caches TZ at startup)"
  prohibitions:
    - "No assertion on source text (must execute the real module via vm/require)"
    - "No watch-mode flags; each test file exits 0/1 in its own node child"
---

<objective>
Author the date-engine RED tests FIRST, before any implementation (Wave 0). These are the falsifiable, TZ-pinned behavior tests that prove the UTC-midnight off-by-one bug and gate the engine implementation (Plan 03) + PDF integration (Plan 04).

Purpose: Per 37-VALIDATION.md and MEMORY `feedback-behavior-verification`, the TZ-pinned behavior tests are a real gate, not a formality. They must EXIST and FAIL against the current tree, then go green as the engine lands.
Output: `tests/37-date-format.test.js` (new) and a rewritten `tests/34-date-locale.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-VALIDATION.md
@tests/34-date-locale.test.js
@tests/run-all.js
</context>

<artifacts_produced>
- `tests/37-date-format.test.js` (NEW) — TZ-pinned spec: parseLocal, 6 format options, Hebrew numeric isolate, month-boundary, age, input-default
- `tests/34-date-locale.test.js` (REWRITTEN) — asserts fixed en-US behavior + raw-ISO export chain
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/37-date-format.test.js (TZ-pinned falsifiable spine + 6 formats + boundary + age)</name>
  <files>tests/37-date-format.test.js</files>
  <read_first>
    - tests/34-date-locale.test.js (the `loadApp` vm-sandbox pattern at ~line 69 — replicate it; load assets into a vm sandbox with Intl/document/localStorage stubs)
    - tests/run-all.js (per-process env — a test file may set process.env.TZ for itself; each file self-reports and exits 0/1)
    - 37-RESEARCH.md §"Falsifiable TZ-Pinned Tests" (the exact TZ re-exec skeleton, the spine assertion table, and the boundary/age/input-default assertions) and §"Module API" (the public surface the test targets)
  </read_first>
  <action>
    Create tests/37-date-format.test.js as a bespoke zero-dep node test (self-reports PASS/FAIL, process.exit(failed?1:0)), authored BEFORE the engine exists.

    TZ pin: as the very first executable lines, if process.env.TZ !== 'America/New_York', set it and re-exec the file via child_process.execFileSync(process.execPath,[__filename],{stdio:'inherit',env:{...process.env,TZ:'America/New_York'}}) then process.exit(0). Add a self-check that new Date(2026,6,2).getTimezoneOffset() is the EDT offset (240) so a silent TZ miss fails loudly.

    Load strategy (falsifiable RED that flips cleanly): load assets/app.js into a vm sandbox (mirror the 34-date-locale `loadApp` helper). If assets/date-format.js is present, eval it into the SAME sandbox BEFORE app.js so App.formatDate can delegate to window.DateFormat. Assert the spine: App.formatDate('2026-07-02') === 'Jul 2, 2026' with lang 'en'. Against the current tree this returns 'Jul 2'-vs-'Jul 1' — it FAILS ('Jul 1, 2026'); after Plan 03 it PASSES.

    Unit assertions against window.DateFormat (these load date-format.js directly and are RED until Plan 03 creates the module): parseLocal('2026-07-02').getDate()===2 (NOT 1); the 6 D-05 format-option outputs for input '2026-07-02' lang 'en' per the RESEARCH table (auto→'Jul 2, 2026', month-day-year→'Jul 2, 2026', day-month-year→'2 Jul 2026', mm/dd/yyyy→'07/02/2026', dd/mm/yyyy→'02/07/2026', yyyy-mm-dd→'2026-07-02'); D-06 separators (slash for the two numeric slash formats, dash for yyyy-mm-dd); the Hebrew numeric LTR assertion (format('2026-07-02','dd/mm/yyyy','he') contains U+2066 and U+2069, and the isolate-stripped string equals '02/07/2026' with '02' preceding '2026' — LTR order preserved, D-04/D-07).

    Boundary + math (the real bugs, DATE-06/DATE-02): assert parseLocal('2026-07-01').getMonth()===6 (July — the countSessionsThisMonth month-boundary unit proof, pre-fix new Date('2026-07-01') lands in June in NY); assert parseLocal('2000-01-01') yields year 2000/month 0/day 1 (age-math proof — the old UTC parse crossed to 1999 in NY, throwing age off by one); assert todayLocalISO() equals the hand-composed local getFullYear/getMonth+1/getDate, NOT the UTC toISOString().slice(0,10) composition.

    Do NOT implement any production code here. This is a test-only task.
  </action>
  <verify>
    <automated>node tests/37-date-format.test.js; test $? -ne 0 && echo "RED as expected (pre-implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/37-date-format.test.js` exists and, run against the current tree, exits NON-ZERO (RED) — proving falsifiability before implementation
    - The file contains the literal spine assertion for `App.formatDate('2026-07-02')` expecting `Jul 2, 2026`
    - The file sets `process.env.TZ` to `America/New_York` on the first executable line and re-execs to make the pin take effect
    - The file executes the real module via a vm sandbox (no assertion on source text)
  </acceptance_criteria>
  <done>The TZ-pinned engine spec exists, is falsifiable (RED now), and asserts the D-20 spine + all 6 D-05 formats + the D-04/D-07 Hebrew isolate + the DATE-06/DATE-02 boundary and age unit proofs.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tests/34-date-locale.test.js to assert the fixed behavior (D-19)</name>
  <files>tests/34-date-locale.test.js</files>
  <read_first>
    - tests/34-date-locale.test.js (current file — it asserts the OLD split behavior: en-US UI + en-GB PDF, and drives the export chain via PRE-FORMATTED strings; lines ~69-181)
    - 37-RESEARCH.md §"PDF Golden-Baseline Regeneration" (the rewrite spec: load date-format.js into the vm sandbox, assert `auto` outputs still hold, assert the export chain now passes RAW ISO and pdf-export produces the en-US card date)
  </read_first>
  <action>
    Rewrite tests/34-date-locale.test.js so it asserts the FIXED behavior (D-19), not the current buggy behavior. Load assets/date-format.js into the vm sandbox alongside app.js. Assert: the `auto` per-locale outputs still hold (en→en-US 'Jul 2, 2026', de→de-DE long, cs→cs-CZ long, he→he-IL short) reproducing the current app.js rule for `auto`; the English default is now en-US month-first (D-04), NOT en-GB '2 July 2026'; and the export chain now passes RAW ISO into the PDF path (the pre-formatting is removed) so pdf-export produces the en-US card date.

    This file will FAIL until the engine (Plan 03) and PDF integration (Plan 04) land — that is the intended RED state. Keep it a behavior test (execute the real module), never a source-text assertion.
  </action>
  <verify>
    <automated>node tests/34-date-locale.test.js; test $? -ne 0 && echo "RED as expected (pre-implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/34-date-locale.test.js` no longer asserts en-GB '2 July 2026' for English; it asserts en-US 'Jul 2, 2026' (or the fixture reference date's en-US form)
    - The file loads `assets/date-format.js` into the vm sandbox
    - Run against the current tree it exits NON-ZERO (RED)
  </acceptance_criteria>
  <done>34-date-locale.test.js asserts the fixed en-US + raw-ISO export behavior and is RED until the engine and PDF plans land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend/network. This plan authors tests only — it introduces no runtime trust boundary.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01-01 | Tampering | test fixtures / TZ pin | low | mitigate | TZ pin re-exec + EDT-offset self-check prevents a silently-inert "falsifiable" test that passes in the local TZ (Pitfall 1) |

No package installs this phase (zero-dependency vanilla + existing devDependencies only) — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/37-date-format.test.js` exits non-zero (RED) against the current tree
- `node tests/34-date-locale.test.js` exits non-zero (RED) against the current tree
- Both files execute the real modules via vm (no source-text assertions)
</verification>

<success_criteria>
Both engine RED tests exist, are falsifiable, and fail against current code — establishing the Wave 0 gate that Plans 03 and 04 must turn green.
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-01-SUMMARY.md` when done
</output>
