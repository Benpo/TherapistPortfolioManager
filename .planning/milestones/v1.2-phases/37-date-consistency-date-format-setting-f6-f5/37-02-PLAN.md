---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/37-personalization.test.js
  - tests/30-settings-tabnav.test.js
autonomous: true
requirements: [PERS-08]
must_haves:
  truths:
    - "Behavior tests exist (RED) covering the whole Personalization surface before implementation (PERS-08): tab appears + ?tab=personalize deep-links; date-format picker persists to localStorage['portfolioDateFormat'] and survives reload; F4 add/rename/delete persist to localStorage['portfolioSessionTypes'] + locked-row delete rejection; global rename overrides the label app-wide (D-16); unknown-type graceful raw fallback (D-18); backup round-trips portfolioDateFormat + portfolioSessionTypes via the REAL backup export→restore path (D-09/D-17); birthdate native input persists + edits"
    - "Tests execute the real page/modules via jsdom + mock PortfolioDB round-trip (writes mirror into reads), asserting OBSERVABLE state — never source text (MEMORY feedback-behavior-verification). The backup round-trip block specifically exercises backup.js's REAL export→restore of the localStorage settings block (NOT a mock-DB mirror), so it cannot false-GREEN if a field is dropped (FIX 1)"
    - "The locked-row guard is tested BOTH ways (no delete button rendered AND the delete handler rejects a locked key) — a green no-button test alone won't catch a handler regression"
  artifacts:
    - "tests/37-personalization.test.js (NEW) — surface behavior spec"
    - "tests/30-settings-tabnav.test.js (EXTENDED) — adds the personalize deep-link case + bumps the end-of-file expected-count guard"
  key_links:
    - "The new session-type editor must NOT call App.initCommon (double-mount, settings-snippets.js:1318 note) — a test asserts single-mount"
  prohibitions:
    - "No assertion on source text"
    - "No watch-mode flags"
---

<objective>
Author the Personalization-surface RED tests FIRST (Wave 0), before any implementation. These cover PERS-08 and gate the surface plans (06 F5/tab/i18n, 07 F4 editor/resolver, 05 backup+birthdate, 08 add-session cards+birthdate).

Purpose: Tests-first per 37-VALIDATION.md §Wave 0 and MEMORY `feedback-behavior-verification` — falsifiable behavior tests authored before the features exist.
Output: `tests/37-personalization.test.js` (new) and an extended `tests/30-settings-tabnav.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-VALIDATION.md
@tests/30-settings-tabnav.test.js
@tests/30-settings-section-roundtrip.test.js
</context>

<artifacts_produced>
- `tests/37-personalization.test.js` (NEW) — picker persistence + app:dateformat event, F4 two-tier editor CRUD (persists to localStorage['portfolioSessionTypes']) + locked-row guard, formatSessionType resolver (override + raw fallback), REAL backup export→restore round-trip of portfolioDateFormat + portfolioSessionTypes, birthdate native input
- `tests/30-settings-tabnav.test.js` (EXTENDED) — `?tab=personalize` case + bumped count guard
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/30-settings-tabnav.test.js for the Personalization tab</name>
  <files>tests/30-settings-tabnav.test.js</files>
  <read_first>
    - tests/30-settings-tabnav.test.js (the jsdom real-page pattern: capture the DOMContentLoaded handler by stable-identity substring, assert observable state, end-of-file expected-count guard)
    - 37-RESEARCH-personalization.md §1 (tab whitelist at settings.js:729; the tablist reads tabs dynamically via querySelectorAll) and §9 (extend this test: add personalize case; bump EXPECTED_COUNT)
  </read_first>
  <action>
    Add a case asserting that a `?tab=personalize` deep-link activates the Personalization tab and panel, that an invalid `?tab=` still falls back to the first tab, and that keyboard/ARIA wiring covers the new tab. Bump the end-of-file expected tab-count guard to include the 5th tab. This test is RED until Plan 06 adds the tab button/panel + whitelist token.
  </action>
  <verify>
    <automated>node tests/30-settings-tabnav.test.js; test $? -ne 0 && echo "RED as expected (pre-implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - The file references `personalize` as a valid `?tab=` target and asserts the panel becomes visible
    - The end-of-file expected-count guard is raised by one
    - Run against the current tree it exits NON-ZERO (RED)
  </acceptance_criteria>
  <done>The tab-nav test covers the Personalization tab deep-link + count guard and is RED pre-implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/37-personalization.test.js (picker + F4 editor + resolver + backup + birthdate)</name>
  <files>tests/37-personalization.test.js</files>
  <read_first>
    - tests/30-settings-section-roundtrip.test.js (mock PortfolioDB mirrors writes into reads for a genuine round-trip — the model for the IDB clients/sessions parts; F4 session-type storage is now localStorage, not IDB)
    - tests/_helpers/app-stub.js, tests/_helpers/mock-portfolio-db.js (existing helpers to reuse)
    - assets/backup.js (the settings export object ~:602-613 and the settings restore block ~:1209-1229) — the REAL localStorage settings round-trip path the backup block must drive (set localStorage → backup export → clear → restore → assert), NOT a mock mirror (FIX 1)
    - 37-RESEARCH-personalization.md §2 (picker persistence + app:dateformat event), §3 (two-tier editor mechanics + two-layer delete guard), §4 (formatSessionType resolver override + D-18 raw fallback), §5/§6 (session-type storage is localStorage['portfolioSessionTypes'] JSON {overrides,custom} per FIX 1 — NOT IDB; and the backup settings-block round-trip), §7 (birthdate native input value plumbing)
  </read_first>
  <action>
    Create tests/37-personalization.test.js as a jsdom behavior spec (self-reports, exits 0/1), authored BEFORE the features exist so every block is RED now.

    Picker (PERS-02): selecting a date-format option writes localStorage['portfolioDateFormat'] to the chosen seam key; default reads 'auto' when unset; a reload (re-init) re-applies the stored value to the select; the `change` handler dispatches an `app:dateformat` CustomEvent (spy). No page reload path.

    F4 editor (PERS-03): rendering shows the 5 locked defaults in fixed order (clinic/online/remote/proxy/other) each with a rename input + lock icon and NO delete button; a custom type renders a rename input + a delete button; adding a custom type persists it to localStorage['portfolioSessionTypes'] (assert the written JSON's `custom` array, per FIX 1 — NOT a mock DB); renaming a locked default writes a global override string into the same key's `overrides` (D-16) that resolves app-wide; deleting a custom type removes it; the delete handler REJECTS a locked key even if invoked directly (assert both the missing-button and the handler-guard). Assert that after a mutation the editor dispatches an `app:session-types-changed` CustomEvent (spy) and re-renders. Assert the editor boots WITHOUT calling App.initCommon (single-mount).

    Resolver (PERS-04): App.formatSessionType returns the override label when present-and-non-empty (D-16), the i18n default when no override, and the RAW string for an unknown/deleted key (D-18) — no crash.

    Backup round-trip (PERS-05): drive the REAL backup path (FIX 1) — seed localStorage['portfolioDateFormat'] and localStorage['portfolioSessionTypes'] (with non-empty `overrides` AND `custom`), run backup.js export to build the manifest, clear those localStorage keys, run backup.js restore(manifest), and assert BOTH keys are restored with the exact seeded values (the portfolioSessionTypes overrides + custom survive intact). This must exercise backup.js's own settings export object + restore block — NOT a mock-DB mirror — so it fails RED now and cannot false-GREEN if a field is dropped.

    Birthdate (PERS-06): the native date input stores/reads a YYYY-MM-DD `.value`; populate-on-edit sets `.value`; read-on-save reads `.value` (no `.valueAsDate`).

    Include a stored/DOM-XSS guard (threat T-37-02-SEC): render a session-type label containing an HTML-injection payload and assert it appears as literal text (via textContent/input.value), i.e. no element node is parsed from the label string.

    Test-only task — implement no production code.
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js; test $? -ne 0 && echo "RED as expected (pre-implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and exits NON-ZERO (RED) against the current tree
    - It contains distinct assertion blocks for: picker persistence + app:dateformat event; F4 add/rename/delete (persisting to localStorage['portfolioSessionTypes']) + app:session-types-changed dispatch; locked-row two-layer delete guard; formatSessionType override + raw fallback; a REAL backup export→restore round-trip of portfolioDateFormat + portfolioSessionTypes (overrides+custom survive); birthdate `.value` plumbing; an XSS-as-literal-text guard
    - It uses jsdom + the mock PortfolioDB round-trip helper for page-module behavior, and backup.js's REAL export/restore for the settings round-trip (no source-text assertions)
  </acceptance_criteria>
  <done>The Personalization surface behavior spec exists, is falsifiable (RED now), and covers every PERS-08 behavior including the locked-row two-layer guard and the XSS-as-text guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA (localStorage + IndexedDB, no backend). The one in-scope boundary is user-entered session-type labels/rename values crossing into the DOM.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02-SEC | Tampering (stored/DOM XSS) | session-type label rendering | medium | mitigate | This plan authors the XSS-as-literal-text guard test (label with an injection payload must render via textContent/input.value, never parsed) — the falsifiable proof that Plans 07/08 honor the no-innerHTML contract |

No package installs this phase — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/37-personalization.test.js` exits non-zero (RED) against the current tree
- `node tests/30-settings-tabnav.test.js` exits non-zero (RED) against the current tree
- Tests execute the real page/modules via jsdom + mock DB round-trip for page behavior, and backup.js's REAL export/restore for the settings round-trip (FIX 1)
</verification>

<success_criteria>
The full Personalization-surface behavior suite exists, is falsifiable, and fails against current code — the Wave 0 gate that Plans 05/06/07/08 turn green.
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-02-SUMMARY.md` when done
</output>
