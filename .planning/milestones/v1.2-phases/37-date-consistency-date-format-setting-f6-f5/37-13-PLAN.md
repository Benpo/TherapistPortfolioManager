---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 13
type: execute
wave: 7
depends_on: [37-10, 37-11]
files_modified:
  - assets/overview.js
  - index.html
autonomous: true
requirements: [FILT-01, FILT-02]
must_haves:
  truths:
    - "Overview has a Session Format multi-select (pill 'All formats' opens a checkbox list) that filters CLIENTS to those with >=1 session whose resolved format is checked"
    - "The checkbox options are the getSessionTypes() list (5 defaults + custom, dynamic); legacy sessions with no sessionType count as clinic; zero checked = no filter"
    - "The old #clientHeartShieldFilter dropdown is gone; a Heart-Wall .toggle-switch replaces it and shows clients with >=1 isHeartShield===true session regardless of shieldRemoved"
    - "Clear Filters resets both new controls; the Clear button visibility accounts for them"
    - "All new DOM is built via DOM APIs + textContent (custom labels never innerHTML)"
  artifacts:
    - "index.html: #clientFormatFilter multi-select + #clientHeartWallToggle (replacing #clientHeartShieldFilter)"
    - "overview.js: format multi-select build + predicate + Heart-Wall toggle predicate, wired into applyFiltersAndSort + clear/updateClearButton"
  key_links:
    - "uses the i18n keys (filter.sessionFormat*, overview.filter.heartShield repurposed) + CSS classes from 37-11"
    - "must satisfy the pinned selectors from tests/37-overview-filters.test.js (37-10) — that file turns GREEN here"
---

<objective>
Replace the Overview filter bar's mislabeled heart dropdown with the two decided controls (DECISIONS D2/D2a): a Session Format multi-select (dropdown-with-checkboxes) and a Heart-Wall toggle. The Session Format filter filters CLIENTS (a client passes if it has >=1 session whose resolved format is among the checked formats); the Heart-Wall toggle shows clients with >=1 handled Heart-Wall (isHeartShield===true, released or not).

This turns the 37-10 RED filter tests GREEN for the Overview screen. Reuse the i18n keys + CSS classes 37-11 front-loaded; touch NO i18n bundle and NO app.css here.
Purpose: disambiguated, useful filters on the home overview.
Output: overview.js + index.html changes only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@assets/overview.js
@index.html
@tests/37-overview-filters.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session Format multi-select filter on Overview (build + predicate)</name>
  <files>index.html, assets/overview.js</files>
  <read_first>
    - tests/37-overview-filters.test.js (the pinned selectors + expected behavior for #clientFormatFilter and the union/legacy/custom cases — implement to these)
    - index.html lines 112-152 (the filter-bar markup; the Client Type / Year / Sort .filter-field siblings to match)
    - assets/overview.js lines 174-291 (applyFiltersAndSort, updateClearButton, clearFilters, the filter-change wiring) and 293-345 (loadOverview, _sessionsByClient) and 364-530 (renderClientRows)
    - assets/app.js lines 1324-1345 (App.getSessionTypes shape: [{key,label,locked}]) and formatSessionType (resolver)
    - 37-11 artifacts: CSS classes .multi-select/.multi-select-toggle/.multi-select-panel/.multi-select-option and i18n keys filter.sessionFormat / .all / .count
  </read_first>
  <action>
    In index.html, add a new .filter-field to the filter bar containing the Session Format multi-select: a container #clientFormatFilter.multi-select holding a pill button #clientFormatFilterToggle (label via data-i18n filter.sessionFormat, summary text via filter.sessionFormat.all) and a panel #clientFormatFilterPanel (initially .is-hidden). A label for the field uses filter.sessionFormat. Do not hand-author the option rows in HTML — overview.js builds them from App.getSessionTypes().

    In overview.js, build the checkbox options at load (and refresh on app:language) from App.getSessionTypes(): for each {key,label} append a .multi-select-option label containing an input[type=checkbox] with data-format-key=key and a span whose textContent is the resolved label (NEVER innerHTML — custom labels are user data). Toggle the panel open/closed on the pill button; close on outside-click and Escape; update the pill summary — zero checked: App.t('filter.sessionFormat.all'); N checked: String(App.t('filter.sessionFormat.count')).replace('{count}', String(N)) — applyTranslations does NO interpolation, the caller substitutes (established pattern: add-session.js:1334). The summary text node must NOT carry data-i18n (applyTranslations would clobber the substituted text on language switch) — set it programmatically and re-render it inside the app:language handler, where the option rebuild must also RESTORE the previously-checked set. Maintain a selected-set from the checked boxes.

    Add the predicate to applyFiltersAndSort: when the selected-set is non-empty, a client passes only if some session in _sessionsByClient.get(client.id) has a resolved format key in the set, where the resolved key = session.sessionType || "clinic" (legacy/undefined counts as clinic). Empty set = no format filtering. Wire the control into the existing onFilterChange path, into updateClearButton (treat any checked box as an active filter), and into the Clear Filters handler (uncheck all + reset summary). Keep the per-session entry (add-session cards) single-select and untouched.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');var h=fs.readFileSync('index.html','utf8');var o=fs.readFileSync('assets/overview.js','utf8');if(h.indexOf('clientFormatFilter')===-1){console.error('missing #clientFormatFilter in index.html');process.exit(1);}if(o.indexOf('getSessionTypes')===-1||o.indexOf('data-format-key')===-1){console.error('overview.js must build options from getSessionTypes via data-format-key');process.exit(1);}if((o.match(/innerHTML/g)||[]).length>9){console.error('innerHTML use in overview.js increased above the pre-plan baseline (9) - new DOM must be built via DOM APIs/textContent');process.exit(1);}console.log('OK: Overview Session Format control wired')"</automated>
  </verify>
  <acceptance_criteria>
    - index.html has #clientFormatFilter (.multi-select) with #clientFormatFilterToggle + #clientFormatFilterPanel; no hand-authored option rows
    - overview.js builds one input[type=checkbox][data-format-key] per App.getSessionTypes() entry, label via textContent only; a custom label containing HTML renders as literal text (no injected element)
    - Predicate: checked {online,remote} shows only clients with a matching session; a legacy-undefined session counts as clinic; zero checked = all clients
    - After Task 2, the Session Format cases in tests/37-overview-filters.test.js pass
    - No edit to any i18n bundle or app.css; per-session single-select entry untouched
  </acceptance_criteria>
  <done>Overview Session Format multi-select filters clients by resolved format, custom + legacy handled, XSS-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heart-Wall toggle replaces the old heart dropdown on Overview</name>
  <files>index.html, assets/overview.js</files>
  <read_first>
    - tests/37-overview-filters.test.js (the #clientHeartWallToggle contract + shieldRemoved-agnostic cases)
    - index.html lines 127-134 (the #clientHeartShieldFilter dropdown to REMOVE)
    - assets/overview.js lines 176, 183, 195-201, 237, 253, 282 (every reference to clientHeartShieldFilter / shieldVal to REMOVE or replace) and 406-417 (the existing badge path that already keys on isHeartShield — unchanged)
    - assets/app.css lines 1951-1996 (the .toggle-switch/.toggle-slider being reused) and the 37-11 .filter-toggle class
  </read_first>
  <action>
    In index.html, remove the #clientHeartShieldFilter select and its .filter-field, and add a .filter-field.filter-toggle containing a label (data-i18n overview.filter.heartShield — repurposed by 37-11 to read "Heart-Wall") and a label.toggle-switch wrapping input[type=checkbox]#clientHeartWallToggle + span.toggle-slider (mirror the settings.js toggle build). The toggle is unchecked by default.

    In overview.js, delete the clientHeartShieldFilter element lookup, its shieldVal read, and the "heartShield"/"regular" dropdown predicate; add a #clientHeartWallToggle lookup and, when checked, filter clients to those where some session in _sessionsByClient.get(client.id) has isHeartShield===true (regardless of shieldRemoved — D2a); unchecked = no heart filtering. Add the toggle to the filter-change wiring (change event), to updateClearButton (checked = active filter), and to the Clear Filters handler (uncheck). Leave the client-row Heart-Wall badge rendering (renderClientRows) unchanged.
  </action>
  <verify>
    <automated>node tests/37-overview-filters.test.js</automated>
  </verify>
  <acceptance_criteria>
    - index.html no longer contains #clientHeartShieldFilter; it contains #clientHeartWallToggle inside label.toggle-switch (+ span.toggle-slider) in a .filter-toggle field
    - overview.js has no remaining clientHeartShieldFilter/shieldVal reference; the toggle predicate matches isHeartShield===true regardless of shieldRemoved
    - A client whose only heart session is released still appears when the toggle is ON; a non-heart client disappears
    - `node tests/37-overview-filters.test.js` exits 0 (both Session Format and Heart-Wall Overview cases GREEN); Clear Filters resets the toggle
    - No edit to any i18n bundle or app.css (reuses repurposed key + existing/37-11 classes)
  </acceptance_criteria>
  <done>The Overview heart dropdown is gone; a Heart-Wall toggle filters shieldRemoved-agnostically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored custom session-type label → Session Format option render | user-authored text crosses into the checkbox list DOM |
| stored session records → filter predicate | untrusted session.sessionType / isHeartShield values drive client visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-13-SEC | Tampering (XSS) | #clientFormatFilterPanel option label | high | mitigate | option labels via textContent only; SVG/icons via createElementNS; guarded by the 37-10 XSS-as-literal-text test |
| T-37-13-01 | Tampering | filter predicate correctness for legacy/custom keys | medium | mitigate | resolved key = sessionType || 'clinic'; options sourced from getSessionTypes(); covered by union/legacy cases in tests/37-filters-sort.test.js |
| T-37-13-SC | Tampering | npm/pip/cargo installs | low | mitigate | no package installs; hooks on; never --no-verify |
</threat_model>

<verification>
- `node tests/37-overview-filters.test.js` exits 0 (Overview Session Format + Heart-Wall cases GREEN); tests/37-sessions-filters.test.js stays RED until 37-14 and tests/37-overview-sort.test.js stays RED until 37-15
- No i18n bundle or app.css modified; per-session single-select entry untouched; #clientHeartShieldFilter fully removed
</verification>

<success_criteria>
- Overview shows a Session Format multi-select + a Heart-Wall toggle, both wired into filtering + Clear Filters
- The Overview half of the 37-10 RED tests is GREEN; XSS-safe; no i18n/CSS churn here
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-13-SUMMARY.md` when done.
</output>
