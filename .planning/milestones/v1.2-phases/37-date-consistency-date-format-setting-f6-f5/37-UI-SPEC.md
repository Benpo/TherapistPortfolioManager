---
phase: 37
slug: date-consistency-date-format-setting-f6-f5
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-02
---

# Phase 37 — UI Design Contract

> Visual and interaction contract for the Personalization Settings surface (F5 date-format picker · F4 session-type editor · birthdate input swap) and the Hebrew-RTL date rendering rules the date engine must honor. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope reality:** This phase adds **no new visual language.** It composes entirely on the app's locked, mature design system (`assets/tokens.css` + `assets/app.css`) and four existing, tested UI patterns (settings tablist, `.form-field`/`.select-field`, `.snippets-list-row`, native `<input type="date">`). The contract below therefore *pins reuse* and specifies the **copywriting + interaction states + RTL rules** that are genuinely new, rather than re-inventing tokens. Divergence from an existing sibling pattern is the risk here — not missing capability.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (zero-build vanilla JS; no bundler, no `components.json`, no npm in production) |
| Preset | not applicable |
| Component library | none — hand-authored `window.X = (() => { 'use strict'; … })()` IIFE modules + CSS design tokens |
| Icon library | none (vendored) — inline SVG built via `createElementNS` DOM APIs; reuse `buildInfoIconSvg` (lock/info) and `TRASH_ICON_PATHS` (delete) from `settings.js` / `settings-snippets.js` |
| Font | **Rubik** (self-hosted woff2: 400 Regular, 600 SemiBold, 700 Bold) — `assets/fonts/`, declared in `tokens.css`. No new font. |

**shadcn initialization gate:** Not applicable — stack is not React/Next.js/Vite and the project is a locked zero-build vanilla app (a build step / bundler is explicitly Out-of-Scope per REQUIREMENTS.md). No design system to initialize; the token system in `tokens.css` is the design system. Registry safety gate: not applicable.

---

## Spacing Scale

The app does **not** define a `--space-*` scale in `:root` — the `--space-*` / `--text-*` / `--radius-*` custom properties are referenced with **literal fallbacks** throughout `app.css` (documented flag at `app.css:2200`). Effective spacing is `rem`-based. **This phase MUST NOT introduce a new scale** — it reuses the exact values the sibling patterns already ship.

Values this phase's new/edited elements use (at 16px root):

| Context | Value | Source (reuse verbatim) |
|---------|-------|-------------------------|
| Tablist gap between tabs | 6px (`var(--space-1, 6px)`) | `.settings-tabs` (`app.css:3855`) |
| Tab padding | 10px block / 16px inline | `.settings-tab` (`app.css:3864`) |
| `.form-field` internal gap (label→control) | 8px (`0.5rem`) | `.form-field` (`app.css:986`) |
| List of session-type rows — row gap | 6–8px (`var(--space-1, 6px)`) | `.snippets-list` (`app.css:3587`) |
| Session-type row padding | 8px block / 12px inline | `.snippets-list-row` (`app.css:3595`) |
| Section separation (picker ↔ editor) | 16–20px block | `<h3>` subsection headers, matches Snippets panel |
| `.toggle-group` gap (session-type cards) | 12px (`0.75rem`) | `.toggle-group` (`app.css:1060`) |

Exceptions (inherited, do NOT "fix" to an on-grid value — that would diverge the reused primitive): the shared `.input` / `.select-field` control padding is `0.7rem 0.9rem` (≈11px/≈14px) and border-radius `14px`; `.toggle-card` padding `0.9rem` / radius `16px`. These are the app's established control metrics and are reused verbatim.

---

## Typography

Rubik only. The app uses `rem`-based sizes; the new surface reuses inherited control/label/heading sizes — no new type ramp.

| Role | Size | Weight | Line Height | Reuse source |
|------|------|--------|-------------|--------------|
| Body / control text | 16px (`1rem`) | 400 | ~1.5 (inherited) | `.input`/`.select-field` (`app.css:1023`) |
| Label (field + row) | ~16px | **700** | inherit | `.label { font-weight: 700 }` (`app.css:1014`) |
| Tab label | inherit (~16px) | **500** (600 when active-ish) | inherit | `.settings-tab` (`app.css:3869`) |
| Section heading (`<h3>`) | ~1.15–1.25rem | 700 | ~1.2 | existing settings subsection headings |
| Row meta / helper text | ~0.875rem (14px) | 400 | ~1.4 | `.snippets-list-preview` / helper text |

Weights in play: **400 / 500 / 700** (all Rubik faces already loaded). Do not introduce 800 into this surface (reserved for landing/brand headings).

---

## Color

Inherit `tokens.css` semantic tokens verbatim — full light + dark mode already defined. The 60/30/10 split maps to the app's garden theme:

| Role | Token | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-background` (`#fdf8f0` warm cream / dark `#1a2020`) | Page + Settings background |
| Secondary (30%) | `--color-surface` (`#ffffff` / dark `#202828`), `--color-surface-toggle` | Panels, session-type rows, toggle-cards |
| Accent (10%) | `--color-primary` (`#2d6a4f` garden green / dark `#5aacac`) | see reserved list |
| Destructive | `--color-danger` (`#ea4b4b`) | delete-custom-type confirm dialog only |

**Accent (`--color-primary`) reserved for, in this surface:**
- Active Settings-tab underline + active tab label color (`.settings-tab.is-active`)
- Active session-type `.toggle-card` border + soft fill (`.toggle-card.active`)
- The primary **"Add type"** button (existing primary-button styling)
- Focus-visible outlines on tab / select / inputs (`:focus-visible` 2px primary)
- Lock-icon and rename-input accent tint on locked rows (matches `settings.js` locked-section treatment)

Never apply accent to: inactive tabs, row backgrounds, the delete affordance, or body text.

**Destructive (`--color-danger`) reserved for:** the `confirmDialog({ tone: "danger" })` when deleting a **custom** session type — and nothing else in this phase. Locked defaults render a lock icon (no delete button), so there is no destructive path for them. The date-format picker and birthdate input have no destructive actions.

---

## Copywriting Contract

All strings are i18n keys added across **en / he / de / cs** (PERS-07). English canonical values below; he/de/cs translations are `[ASSUMED — pending Sapir/Ben native review]`. Chrome strings only — the date-format example strings inside the picker options are **engine output** (`window.DateFormat`), never hand-typed i18n.

| Element | i18n key | Copy (EN) |
|---------|----------|-----------|
| Tab label | `settings.tab.personalize` | Personalization |
| Date-format field label | `settings.dateFormat.label` | Date format |
| Date-format "auto" option (only static option) | `settings.dateFormat.auto` | Automatic (follow language) |
| Date-format saved toast | `settings.dateFormat.savedToast` | Date format updated. |
| Session-types section heading | `settings.sessionTypes.heading` | Session types |
| Session-types helper (one line) | `settings.sessionTypes.helper` | Rename the built-in types or add your own. |
| **Primary CTA** (add a custom type) | `settings.sessionTypes.add.label` | Add type |
| Add-type input placeholder | `settings.sessionTypes.add.placeholder` | New session type |
| Locked-row lock tooltip | `settings.sessionTypes.locked.tooltip` | Built-in type — can be renamed but not deleted. |
| Rename input aria-label | `settings.sessionTypes.rename.aria` | Rename this session type |
| Delete button aria-label (custom rows) | `settings.sessionTypes.delete.aria` | Delete this session type |
| Rename/add saved toast | `settings.sessionTypes.savedToast` | Session types updated. |
| New default label — Remote | `session.type.remote` | Remote |
| New default label — Proxy | `session.type.proxy` | Proxy |

### States

| State | Where | Copy (EN) | key |
|-------|-------|-----------|-----|
| **Empty** (no custom types yet) | below the 5 locked rows | No custom types yet. Add one below. | `settings.sessionTypes.empty` |
| **Error — name too long** | rename / add input | That name is too long. Use 60 characters or fewer. | reuse `settings.rename.tooLong` shape |
| **Error — add duplicate/blank** | add-type input | Enter a name that isn't already in the list. | `settings.sessionTypes.add.invalid` |
| **Destructive confirmation** | delete a custom type | **Title:** Delete this session type? · **Body:** Existing sessions that used it will keep their saved value. · **Confirm:** Delete type · **Cancel:** Cancel | `settings.sessionTypes.confirm.delete.{title,body,confirm}` (+ shared `confirm.cancel`) |

- **CTA verbs are specific:** "Add type" / "Delete type" (verb + noun), not "Submit" / "OK".
- **No "optional" hints** under any field (MEMORY `feedback-optional-fields-no-hints`) — the date-format picker, add-type input, and birthdate input carry no "(optional)" helper text.
- The date-format picker **auto-saves on `change`** (mirrors language/theme) — there is no Save button and therefore no CTA for F5; its only user-visible confirmation is the saved toast.

---

## Component & Interaction Inventory

**Visual hierarchy (Personalization tab focal point):** The **Date format `<select>`** is the primary anchor — it sits first in reading order with its label at the top of the panel, and is the surface's headline action (F5). The Session-types editor (heading + list) is the secondary block below it; the "Add type" primary button is the strongest accent *within* that secondary block but does not outrank the date picker at the panel level.

Everything below is **reuse of an existing pattern**. The executor's job is composition; do not re-implement.

| New element | Pattern reused (source) | Interaction contract |
|-------------|-------------------------|----------------------|
| 5th "Personalization" tab (button + panel) | `.settings-tab` / `.settings-tabpanel` + tab-nav IIFE (`settings.js:721–799`) | WAI-ARIA tablist: `role="tab"`/`role="tabpanel"`, `aria-selected`, `aria-controls`, `tabindex="-1"` on inactive; Arrow/Home/End keyboard nav; `?tab=personalize` deep-link (add token to whitelist `settings.js:729`); default falls back to first tab on invalid `?tab=`. |
| Date-format `<select>` | `.form-field` + `.select-field` (Backups frequency select `settings.html:171`) | 6 `<option value>`= locked seam keys (`auto`/`month-day-year`/`day-month-year`/`mm/dd/yyyy`/`dd/mm/yyyy`/`yyyy-mm-dd`). Non-auto option **labels** filled at wire-time by calling `window.DateFormat` on a fixed reference date (SEAM — never hand-typed). `change` → persist `localStorage["portfolioDateFormat"]` → dispatch `app:dateformat` CustomEvent → saved toast. Live re-render, **never a page reload**. |
| Two-tier session-type editor | `.snippets-list` / `.snippets-list-row` (`settings-snippets.js`) + locked-row lock-icon (`settings.js` `LOCKED_RENAME` + `buildInfoIconSvg`) | **Locked rows (5 defaults):** rename `<input>` + **lock icon**, no delete button. **Custom rows:** rename `<input>` + **delete button** (`confirmDialog` tone=danger). Add-new input + "Add type" button at list bottom. Order: In-person, Online, Remote, Proxy, Other, then custom. Renames are global (one language-agnostic override per key). |
| Session-type cards (add/edit session) | `.toggle-group` / `.toggle-card` (`add-session.html:77`) | Rendered data-driven from `App.getSessionTypes()` (5 defaults + custom); first card `.active` default; radio `name="sessionType"`, value = stored key; label via `App.formatSessionType(key)`; re-resolve on `app:language`. |
| Birthdate input | native `<input type="date">` (`#sessionDate`, `add-session.html:94`) | Replaces 3 `<select>` dropdowns across add-client + add-session (inline + edit). Value always `YYYY-MM-DD`; browser owns locale ordering; zero custom JS. |

### Hebrew RTL rendering rules (date engine — DATE-04 / D-07)

| Rule | Contract |
|------|----------|
| Numeric date strings in Hebrew | Wrap in Unicode directional isolates `U+2066` (LRI) … `U+2069` (PDI) — a **bare-string** wrap, never `<bdo>` markup (most call sites use `.textContent`). Applies to `mm/dd/yyyy`, `dd/mm/yyyy`, `yyyy-mm-dd` in `he` only. |
| Named-month Hebrew dates | Not wrapped — the strong-RTL Hebrew month token anchors direction (matches current `auto` behavior). |
| Consumption contexts | DOM display, `document.title`, PDF (`doc.text` via `bidi.min.js`), markdown export — all four must render LTR digit order without flipping. PDF isolate-tolerance is **test-gated** (assert `2026` present / `6202` absent). |
| Chrome strings (tab, editor labels) | Plain text — no RTL special-casing beyond what existing settings tabs already handle. |

### Accessibility & theming (inherit — do not regress)

- All user-entered labels rendered via `.textContent` / `input.value` — **never `innerHTML`** (XSS + the data-i18n gate).
- Icons built via `createElementNS` DOM APIs with `aria-label` (lock tooltip, delete button).
- Focus-visible outlines: 2px `--color-primary`, offset 2px (existing `:focus-visible`).
- Both light and dark mode inherited automatically via semantic tokens — no per-theme code needed. Verify the new tab, select, list rows, and lock/delete icons in **both** themes.
- Toasts announce via existing `App.showToast(message, key)` `aria-live` path.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (no shadcn / no bundler in this project) |
| third-party | none | not applicable |

No component registries are used; all UI is hand-authored vanilla against `tokens.css`. Vetting gate: N/A.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS *(see accepted-exception 1)*
- [x] Dimension 2 Visuals: PASS *(focal point declared — see Component & Interaction Inventory)*
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS *(see accepted-exception 2)*
- [x] Dimension 5 Spacing: PASS *(see accepted-exception 3)*
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED (force-approved by Ben, 2026-07-02) — this is a **reuse-only** phase adding no new visual language. The gsd-ui-checker raised 3 BLOCKs by applying greenfield design-system rules; all three were verified against the live codebase and consciously accepted as false-positives for this phase.

### Accepted checker exceptions (verified against source — do NOT "fix")

1. **Copywriting — confirm dialog "Cancel".** The delete-custom-type dialog reuses the shared `confirm.cancel` i18n key (translated en/he/de/cs), used at ~15 call sites app-wide including the direct sibling snippets-delete dialog (`settings-snippets.js:933`). Renaming it here would *diverge* from every other confirm dialog — the exact risk this contract guards against. **Keep "Cancel".**
2. **Typography — 3 weights (400/500/700).** The app's real design system ships five weights (400/500/600/700/800 across `app.css`). `.settings-tab` genuinely declares `font-weight: 500` (`app.css:3869`). The spec is factually accurate; reducing to 2 weights would break the inherited type system. **Inherit as-is.**
3. **Spacing — off-grid inherited values (6/10px, 0.7rem/0.9rem).** Inherited verbatim from `.settings-tab`, `.snippets-list-row`, `.input`, `.toggle-card`. This phase MUST NOT change control-primitive metrics. **Reuse verbatim.**
