---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 11
type: execute
wave: 6
depends_on: [37-08]
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - assets/app.css
  - add-session.html
  - reporting.html
  - sessions.html
  - assets/demo-seed-data.json
autonomous: true
requirements: [TERM-01, TERM-02]
must_haves:
  truths:
    - "No i18n surface reads the retired heart term or the modality collision label; heart = Heart-Wall / חומת הלב, modality axis = Session Format / אופן הטיפול / Sitzungsart / Typ sezení"
    - "The 5 modality VALUE labels and keys are unchanged; Proxy stays distinct from Remote; Client Type unchanged"
    - "The HE inconsistencies are gone — one HE heart term (חומת הלב) everywhere"
    - "New i18n keys exist for the Session Format multi-select filter and both Heart-Wall toggle labels, all 4 languages"
    - "New CSS classes exist for the checkbox-dropdown, the filter-bar Heart-Wall toggle layout, and the sortable headers + arrows — RTL-aware, token-based"
    - "No data migration (isHeartShield/shieldRemoved/clinic intact); no PDF golden regeneration"
    - "The public demo's seeded clinical prose no longer says Heart Shield — the demo narrates the same Heart-Wall term as the chrome (Ben-approved in-phase fix, 2026-07-05)"
  artifacts:
    - "assets/i18n-{en,he,de,cs}.js relabeled + new keys filter.sessionFormat / .all / .count; overview.filter.heartShield + sessions.filter.type repurposed as Heart-Wall toggle labels"
    - "app.css: .multi-select, .multi-select-toggle, .multi-select-panel, .multi-select-option, .filter-toggle, .table th.sortable, .sort-arrow"
    - "assets/demo-seed-data.json: the 9 Heart Shield prose hits relabeled to Heart-Wall (data values isHeartShield/shieldRemoved/sessionType untouched)"
  key_links:
    - "the new i18n keys + CSS class names are the contract Plans 37-13/37-14/37-15 consume verbatim"
    - "getSectionLabel('heartShield','session.form.heartShield') now yields the Heart-Wall label in the export/PDF path — but no golden fixture renders it, so baselines do NOT shift"
---

<!-- planner-discipline-allow: Heart Shield -->
<!-- planner-discipline-allow: Session Type -->

<objective>
Land the terminology relabel + the shared i18n/CSS foundation the two screen plans build on. Two disambiguations per DECISIONS D1/D3:
1. The modality axis becomes Session Format (EN) / אופן הטיפול (HE) / Sitzungsart (DE, unchanged) / Typ sezení (CS, unchanged).
2. The heart term becomes Heart-Wall (EN/DE/CS) / חומת הלב (HE) on every surface, retiring the HE inconsistencies.

Also front-load ALL new i18n keys (Session Format filter + Heart-Wall toggle labels) and ALL new CSS classes (checkbox-dropdown, filter-bar toggle layout, sortable headers) so Plans 37-13/37-14/37-15 touch neither the i18n bundles nor app.css (parallel-safe).

Hard fences: i18n key names unchanged (values only); the 5 modality value labels + keys unchanged (Proxy kept distinct); Client Type unchanged; NO data migration; NO PDF golden regen.
Purpose: one consistent vocabulary across 4 languages + the reusable foundation.
Output: relabeled i18n ×4, new keys, new CSS, updated HTML fallback text on the surviving surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@assets/i18n-en.js
@assets/i18n-he.js
@assets/app.css
</context>

<relabel_tables>
Values are authoritative. he/de/cs are `[ASSUMED — Ben confirms HE+DE himself; CS = machine + spot-check, per D5]`; the executor writes them, the orchestrator routes confirmation. Key names never change; only values.

## Table A — Heart-term VALUE relabel (TERM-02). Apply to EVERY listed key across all 4 bundles.

| i18n key | EN | HE | DE | CS |
|---|---|---|---|---|
| overview.heartShield.active | Active Heart-Wall | חומת לב פעילה | Aktive Heart-Wall | Aktivní Heart-Wall |
| overview.heartShield.removed | Heart-Wall Removed | חומת הלב הוסרה | Heart-Wall entfernt | Heart-Wall odstraněna |
| session.form.heartShield | Heart-Wall Session | מפגש חומת הלב | Heart-Wall-Sitzung | Sezení s Heart-Wall |
| session.form.shieldRemoved | Heart-Wall Removed? | האם חומת הלב הוסרה? | Heart-Wall entfernt? | Heart-Wall odstraněna? |
| session.form.heartShieldEmotions | Emotions in the Heart-Wall | רגשות בחומת הלב | Emotionen in der Heart-Wall | Emoce v Heart-Wall |
| session.form.heartShieldEmotions.placeholder | List the emotions found in the Heart-Wall | רשמו את הרגשות שנמצאו בחומת הלב | Die in der Heart-Wall gefundenen Emotionen auflisten | Vypište emoce nalezené v Heart-Wall |
| session.copy.heartShield | Heart-Wall Cleared: | חומת הלב נוקתה: | Heart-Wall aufgelöst: | Heart-Wall odstraněna: |
| toast.heartShieldRequired | Select whether the Heart-Wall was cleared | יש לבחור אם חומת הלב נוקתה או לא | Bitte auswählen, ob die Heart-Wall aufgelöst wurde | Vyberte, zda byla Heart-Wall odstraněna |
| sessions.table.heartShield | Heart-Wall | חומת הלב | Heart-Wall | Heart-Wall |
| reporting.heartShieldCleared | Heart-Wall Cleared Sessions | מפגשי חומת הלב שנוקו | Sitzungen mit aufgelöster Heart-Wall | Sezení s odstraněnou Heart-Wall |
| common.heartShield | Heart-Wall | חומת הלב | Heart-Wall | Heart-Wall |
| settings.row.heartShield.description | Heart-Wall session toggle and controls | מתג ובקרות של מפגש חומת הלב | Heart-Wall-Sitzung: Schalter und Steuerung | Přepínač a ovládání sezení s Heart-Wall |
| settings.row.heartShieldEmotions.description | Emotions found inside the Heart-Wall | רגשות שנמצאו בתוך חומת הלב | In der Heart-Wall gefundene Emotionen | Emoce nalezené v Heart-Wall |
| sessions.filter.type.heartShield (doomed dropdown option; value relabeled so it carries no retired token; the .all/.heartShield/.regular sub-keys are removed by 37-15 Task 1.5 — only AFTER both old dropdowns are gone, since index.html AND sessions.html both reference them until wave 7) | Heart-Wall | חומת הלב | Heart-Wall | Heart-Wall |

## Table B — Repurpose the two modality-collision dropdown labels into Heart-Wall TOGGLE labels (used by 37-13/37-14). Value only.

| i18n key | EN | HE | DE | CS |
|---|---|---|---|---|
| overview.filter.heartShield (Overview Heart-Wall toggle label) | Heart-Wall | חומת הלב | Heart-Wall | Heart-Wall |
| sessions.filter.type (Sessions Heart-Wall toggle label) | Heart-Wall | חומת הלב | Heart-Wall | Heart-Wall |

## Table C — Modality axis relabel (TERM-01). Value only.

| i18n key | EN | HE | DE | CS |
|---|---|---|---|---|
| session.form.sessionType (add-session form section label) | Session Format | אופן הטיפול | Sitzungsart (unchanged) | Typ sezení (unchanged) |
| sessions.table.type (Sessions modality column header) | Session Format | אופן הטיפול | Typ (unchanged) | Typ (unchanged) |

## Table D — NEW keys for the Session Format multi-select filter (used by 37-13/37-14). Add to all 4 bundles.

| i18n key | EN | HE | DE | CS |
|---|---|---|---|---|
| filter.sessionFormat (filter label) | Session Format | אופן הטיפול | Sitzungsart | Typ sezení |
| filter.sessionFormat.all (pill summary, 0 selected) | All formats | כל האופנים | Alle Arten | Všechny typy |
| filter.sessionFormat.count (pill summary, N selected; {count} placeholder) | {count} selected | {count} נבחרו | {count} ausgewählt | Vybráno: {count} |

## DO NOT TOUCH: the 5 modality value labels + keys (session.form.clinic/online/other, session.type.clinic/online/remote/proxy/other; HE פרונטלי/מקוון/מרחוק/מיופה כוח/אחר); Client Type (overview.filter.type, common.type.*); the stored field names isHeartShield/shieldRemoved and value key clinic.
</relabel_tables>

<tasks>

<task type="auto">
  <name>Task 1: Heart-term relabel across i18n x4 + surviving HTML fallback surfaces</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, reporting.html, add-session.html, sessions.html</files>
  <read_first>
    - .planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md D1/D3 (naming table, no-migration fence)
    - the Relabel Tables A + B in THIS plan (authoritative values)
    - assets/i18n-en.js lines 41-42, 103-116, 142, 175, 235-267, 405-406 and the HE/DE/CS peers at the same keys
    - add-session.html lines 204-238, 341-343 (the accordion heart section + fallback text between the data-i18n tags)
    - reporting.html line 78 (the heart-cleared stat fallback text)
    - sessions.html line 85 (the Heart-Wall column header fallback text)
  </read_first>
  <action>
    Rewrite the VALUE of every key in Relabel Tables A and B, in all four i18n bundles, to the Heart-Wall term for that language (EN/DE/CS use the compound Heart-Wall; HE uses חומת הלב). Preserve each string's surrounding grammar and punctuation exactly as shown in the tables; do NOT change any key name. This retires every appearance of the retired EN, HE, DE, and CS heart tokens, including the inconsistent HE variants, leaving one heart term per language.

    Then update the shipped HTML fallback text (the text node between the opening and closing tags of a data-i18n element) so it matches the new value on the SURVIVING heart surfaces only: reporting.html (the reporting.heartShieldCleared stat label), add-session.html (the session.form.heartShield accordion header + toggle label, the session.form.shieldRemoved label, the session.form.heartShieldEmotions label + its placeholder), and sessions.html (ONLY the sessions.table.heartShield column header). Do NOT touch the filter-bar dropdowns in index.html / sessions.html — those are removed by 37-13/37-14.

    Do NOT modify assets/pdf-export.js, the export golden fixtures, or any .pdf.sha256 baseline — no golden regeneration (no fixture renders a relabeled string; modality value labels are unchanged). Before committing, grep tests/ for the retired heart tokens and fix any test expectation asserting an old label IN THE SAME COMMIT so npm test does not regress on a pre-existing test (the 37-10 RED filter tests stay red — expected).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['en','he','de','cs'].forEach(function(l){var s=fs.readFileSync('assets/i18n-'+l+'.js','utf8');if(/Heart Shield|Herzschutz|Ochrana srdce/.test(s)){console.error('retired latin heart token in '+l);process.exit(1);}if(s.indexOf('מגננת')!==-1||s.indexOf('הגנת הלב')!==-1){console.error('retired HE heart token in '+l);process.exit(1);}});console.log('OK: heart tokens retired in i18n x4')"</automated>
  </verify>
  <acceptance_criteria>
    - Every key in Relabel Tables A + B carries its new value in all 4 bundles; no key name changed
    - The verify node script exits 0 (no retired EN/DE/CS latin token and no HE מגננת / הגנת הלב remain in any i18n bundle)
    - reporting.html, add-session.html (heart section), and the sessions.html Heart-Wall column header fallback text match their new i18n values
    - Pre-verified at plan time: NO existing test asserts a retired heart LABEL literal (the 6 tests/ files matching "Heart Shield"/"heartShield" reference only the data keys heartShieldEmotions/isHeartShield, which are unchanged). Gate: plain `npm test` after the relabel — if any test DOES assert a relabeled string, update that expectation in this same commit
    - `npm test` shows no NEW failure vs the pre-plan baseline other than the still-RED 37-10 filter tests; no .pdf.sha256 baseline changed
  </acceptance_criteria>
  <done>One heart term per language across every i18n surface and surviving HTML fallback; no golden drift.</done>
</task>

<task type="auto">
  <name>Task 2: Modality axis relabel + new Session Format filter i18n keys</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, add-session.html, sessions.html</files>
  <read_first>
    - Relabel Tables C + D in THIS plan
    - assets/i18n-en.js line 103 (session.form.sessionType) + line 248 (sessions.table.type), and the HE/DE/CS peers
    - add-session.html line 76 (the session.form.sessionType section label fallback text)
    - .planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md Copywriting Contract (chrome-string conventions)
  </read_first>
  <action>
    Apply Table C: set session.form.sessionType to the Session Format axis label (EN Session Format, HE אופן הטיפול; DE Sitzungsart and CS Typ sezení are already the axis term — leave unchanged) and set sessions.table.type EN/HE to the axis label (Session Format / אופן הטיפול); leave DE Typ and CS Typ unchanged (short column header per D1). Update the add-session.html session.form.sessionType label fallback text to Session Format.

    Add the three Table D keys (filter.sessionFormat, filter.sessionFormat.all, filter.sessionFormat.count) to all four bundles with the values shown. filter.sessionFormat.count contains the {count} placeholder token. NOTE: applyTranslations (app.js:23-32) does NO interpolation — the CONSUMER (37-13/37-14) must substitute via .replace('{count}', String(n)), the established caller-side pattern (see add-session.js:1334). Insert the new keys in a sensible neighbourhood (near the other filter.* / overview.filter.* keys) and keep each bundle's key set identical across the 4 languages.

    Do not alter any modality VALUE label (session.form.clinic/online/other, session.type.*) or Client Type keys.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['en','he','de','cs'].forEach(function(l){var s=fs.readFileSync('assets/i18n-'+l+'.js','utf8');['filter.sessionFormat','filter.sessionFormat.all','filter.sessionFormat.count'].forEach(function(k){if(s.indexOf('\"'+k+'\"')===-1){console.error('missing '+k+' in '+l);process.exit(1);}});});console.log('OK: sessionFormat filter keys present x4')"</automated>
  </verify>
  <acceptance_criteria>
    - session.form.sessionType = Session Format (EN) / אופן הטיפול (HE); DE Sitzungsart + CS Typ sezení unchanged; the add-session.html fallback text reads Session Format
    - sessions.table.type = Session Format (EN) / אופן הטיפול (HE); DE Typ + CS Typ unchanged
    - filter.sessionFormat / .all / .count exist in all 4 bundles (verify script exits 0); the 4 bundles have identical key sets
    - `grep -c "Session Type" assets/i18n-en.js` returns 0 for the modality axis label context (the two doomed dropdown labels are already repurposed in Task 1); no modality VALUE label changed
  </acceptance_criteria>
  <done>The modality axis reads Session Format / אופן הטיפול consistently; the new filter keys exist in all 4 languages.</done>
</task>

<task type="auto">
  <name>Task 3: New CSS classes — checkbox-dropdown, filter-bar toggle layout, sortable headers</name>
  <files>assets/app.css</files>
  <read_first>
    - assets/app.css lines 1951-1996 (the existing .toggle-switch / .toggle-slider the Heart-Wall toggle REUSES — do not redefine)
    - assets/app.css .select-field / .select-modern / .input-pill and .filter-bar / .filter-field (the pill + filter-bar metrics the new controls must match) and .table th (existing header style)
    - .planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md Spacing/Color/Typography (reuse verbatim; introduce no new scale/token)
    - assets/tokens.css (semantic tokens: --color-surface, --color-border, --color-primary, --color-background)
  </read_first>
  <action>
    Add these new CSS class blocks to app.css, using existing tokens + the app's established control metrics (radius 14px, padding 0.7rem 0.9rem for the pill; row/panel metrics matching .snippets-list-row). All must be RTL-aware via logical properties (inset-inline-*, margin-inline-*, padding-inline-*), matching the existing .select-modern chevron approach. These classes are dead-but-valid until Waves 7-8 wire elements to them.

    - .multi-select — position:relative inline container that drops into a .filter-field (so the Session Format control lines up with the sibling selects).
    - .multi-select-toggle — the pill button that visually mirrors .select-field.select-modern (same border, radius, padding, background, focus-visible outline) with a trailing chevron on inset-inline-end that does not overlap the label; carries a hover/active affordance.
    - .multi-select-panel — an absolutely-positioned popover anchored under the toggle (inset-inline-start:0; top:100%), --color-surface background, 1px --color-border, 14px radius, subtle shadow, max-height with overflow-y:auto, z-index above the filter bar; a modifier or the .is-hidden utility toggles display:none.
    - .multi-select-option — a flex label row (checkbox + text, gap ~8px, padding 8px/12px) with a hover background; text truncates/wraps gracefully for long custom labels.
    - .filter-toggle — a .filter-field variant that lays out the Heart-Wall label + the reused .toggle-switch inline (align-items:center; gap), RTL-safe, matching the filter-bar baseline.
    - .table th.sortable — cursor:pointer; user-select:none; a hover color; and space for the arrow via padding-inline-end / an inline-flex wrapper. Style the aria-sort states: th[aria-sort="ascending"] and th[aria-sort="descending"] give their .sort-arrow the up/down appearance; th[aria-sort="none"] shows a neutral/low-opacity arrow.
    - .sort-arrow — an inline-flex indicator sized to the header text, its direction driven by the aria-sort state (the SVG itself is injected by 37-15 via createElementNS); RTL places it on the correct inline side.

    Do not change any existing selector's declarations; only ADD new blocks. No new CSS custom-property scale.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');var c=fs.readFileSync('assets/app.css','utf8');['.multi-select','.multi-select-toggle','.multi-select-panel','.multi-select-option','.filter-toggle','th.sortable','.sort-arrow'].forEach(function(s){if(c.indexOf(s)===-1){console.error('missing CSS selector '+s);process.exit(1);}});if(c.indexOf('inset-inline')===-1){console.error('new controls must be RTL-aware (logical props)');process.exit(1);}console.log('OK: filter/sort CSS classes present + RTL-aware')"</automated>
  </verify>
  <acceptance_criteria>
    - app.css defines .multi-select, .multi-select-toggle, .multi-select-panel, .multi-select-option, .filter-toggle, .table th.sortable (and aria-sort state rules), .sort-arrow — verify script exits 0
    - The new controls use logical properties (inset-inline / margin-inline / padding-inline) for RTL, never left/right
    - No existing selector's declarations were modified; no new --*-scale token introduced
    - The Heart-Wall toggle reuses the existing .toggle-switch/.toggle-slider (not redefined)
    - `npm test` shows no new failure vs baseline (CSS-only change; 37-10 stays RED)
  </acceptance_criteria>
  <done>The reusable checkbox-dropdown, filter-toggle, and sortable-header styles exist, RTL-aware, ready for Waves 7-8.</done>
</task>

<task type="auto">
  <name>Task 4: Relabel the demo seed's Heart Shield clinical prose to Heart-Wall (Ben-approved scope-in)</name>
  <files>assets/demo-seed-data.json</files>
  <read_first>
    - assets/demo-seed-data.json lines 260-320 (the Heart-Wall story arc: the ~9 "Heart Shield" occurrences in insights / customerSummary / comments prose)
    - tests/35-demo-seed.test.js lines 60-70 and 165-195 (the seed gate asserts ALLOWED_SESSION_TYPES, isHeartShield/shieldRemoved booleans and the .heart-badge-removed class — NOT the prose text; a text-only relabel cannot break it)
  </read_first>
  <action>
    In assets/demo-seed-data.json, replace every "Heart Shield" occurrence in the user-visible clinical PROSE (insights, customerSummary, comments — approx. 9 hits around lines 270, 273-274, 292, 295-296, 314, 317) with "Heart-Wall", preserving each sentence's grammar (e.g. "we identified a Heart-Wall — a protective energetic layer…", "Heart-Wall fully removed."). Change ONLY prose text values: the data fields isHeartShield/shieldRemoved/sessionType and all keys stay byte-identical. Ben approved this scope-in on 2026-07-05 (the public demo must not narrate the retired term next to Heart-Wall chrome).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');var s=fs.readFileSync('assets/demo-seed-data.json','utf8');if(/Heart[ -]Shield/i.test(s)){console.error('retired Heart Shield prose survives in demo-seed-data.json');process.exit(1);}JSON.parse(s);console.log('OK: demo prose relabeled, JSON valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "heart shield" assets/demo-seed-data.json` returns 0; the file parses as valid JSON
    - The isHeartShield/shieldRemoved/sessionType values and all object keys are unchanged (prose-only diff)
    - `node tests/35-demo-seed.test.js` exits 0 (the seed gate keys on data, not prose)
  </acceptance_criteria>
  <done>The public demo narrates Heart-Wall, consistent with the relabeled chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n bundle values → rendered chrome | static, developer-authored translation strings (no user input in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-11-01 | Tampering | relabel scope creep into modality VALUE labels or stored keys | medium | mitigate | fences enforced by verify: modality value labels + isHeartShield/shieldRemoved/clinic keys untouched; only label VALUES change |
| T-37-11-02 | Information disclosure | accidental PDF golden drift | low | accept | no fixture renders a relabeled string and modality value labels are unchanged; verify asserts no .pdf.sha256 changed |
| T-37-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | no package installs in this plan; hooks on; never --no-verify |
</threat_model>

<verification>
- The four verify node scripts exit 0 (retired heart tokens gone from i18n x4; sessionFormat keys present x4; CSS classes present + RTL-aware; demo-seed prose relabeled + JSON valid)
- `npm test` regresses nothing vs the pre-plan baseline except the intentionally-RED 37-10 filter tests; no .pdf.sha256 baseline changed
- The 4 i18n bundles keep identical key sets
</verification>

<success_criteria>
- One heart term per language (Heart-Wall / חומת הלב) and one modality axis label (Session Format / אופן הטיפול / Sitzungsart / Typ sezení) across every i18n surface + surviving HTML fallback
- New filter/toggle i18n keys + new filter/sort CSS classes exist and are RTL-aware, ready for the screen plans
- No data migration, no PDF golden regen
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-11-SUMMARY.md` when done.
</output>