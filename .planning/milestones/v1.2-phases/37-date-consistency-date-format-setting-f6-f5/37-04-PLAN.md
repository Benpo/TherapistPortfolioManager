---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 04
type: execute
wave: 2
depends_on: ["37-01", "37-03"]
files_modified:
  - assets/pdf-export.js
  - assets/export-modal.js
  - tests/pdf-digit-order.test.js
  - .planning/fixtures/phase-23/fixture-en.pdf.sha256
  - .planning/fixtures/phase-23/fixture-de.pdf.sha256
  - .planning/fixtures/phase-23/fixture-cs.pdf.sha256
  - .planning/fixtures/phase-23/fixture-he.pdf.sha256
  - .planning/fixtures/phase-23/fixture-he-mixed.pdf.sha256
autonomous: false
requirements: [DATE-04, DATE-05, DATE-07]
must_haves:
  truths:
    - "pdf-export.js formatDate delegates to window.DateFormat.format(sessionDate, getPreference(), uiLang) (the old en-GB T00:00:00 body becomes dead) so the PDF card date uses the chosen format (D-05/D-08)"
    - "export-modal.js STOPS pre-formatting: it passes the RAW ISO sessionDate into buildSessionPDF, and the footer 'Exported on' uses App.formatDate(DateFormat.todayLocalISO()) — local today, chosen format (DATE-05)"
    - "Hebrew numeric dates survive the jsPDF/bidi path — a he mm/dd/yyyy PDF asserts 2026 present / 6202 absent via the pdf-digit-order GID technique (DATE-04); if isolates break rendering, the specified strip-fallback is applied"
    - "TWO fixtures shift DELIBERATELY, not one (FIX 4): (a) the English baseline shifts en-GB→en-US ('2 July 2026'→'Jul 2, 2026', D-04/D-19); AND (b) the Hebrew fixtures (fixture-he + fixture-he-mixed) shift Hebrew LONG month→SHORT month, because old pdf-export.js:686 used month:'long' for he-IL while the new engine reproduces app.js's he=short-month rule (D-08 — the PDF finally matches the UI). de/cs are genuinely UNCHANGED (both old and new use month:'long'). The 5 SHA-256 baselines are regenerated only after real rendered PDFs are visually verified — never blind --regenerate (MEMORY reference-pdf-jsdom-inert-gates)"
    - "tests/34-date-locale.test.js (rewritten in Plan 01) goes GREEN"
  artifacts:
    - "assets/pdf-export.js formatDate (delegating)"
    - "assets/export-modal.js (raw-ISO pass-through + local exportedOn)"
    - "5 regenerated .planning/fixtures/phase-23/*.pdf.sha256 baselines"
    - "tests/pdf-digit-order.test.js Hebrew-numeric assertion (extended)"
  key_links:
    - "The only hash-affecting change per fixture is the session-card date string. Expected-changed: fixture-en (en-GB→en-US) AND fixture-he + fixture-he-mixed (Hebrew long→short month — intended, matches the UI per D-08). Expected-UNCHANGED: fixture-de + fixture-cs (both old and new use month:'long'). Confirm de/cs are byte-identical and that the he change is the long→short month form (not a regression) before accepting (FIX 4)"
  prohibitions:
    - "Never run --regenerate on an unopened/unverified PDF (a jsdom PDF build can hang→exit-0 silently)"
    - "Do not reroute wall-clock timestamps in these files — only the session-card date + footer exportedOn (a local calendar day) change"
---

<objective>
Unify the PDF date path on `window.DateFormat` and regenerate the golden baselines deliberately. Route `pdf-export.js formatDate` through the engine, stop `export-modal.js` pre-formatting (pass raw ISO), make the footer date local-today, verify Hebrew numeric survives jsPDF, and regenerate the 5 SHA-256 baselines with real-output visual review.

Purpose: Close DATE-05 (PDF parity via the helper) + DATE-07 (baseline regen with visual review) + the PDF half of DATE-04 (Hebrew LTR in jsPDF). The en-GB→en-US shift is the intended D-04 baseline change.
Output: delegating PDF formatter, raw-ISO export chain, extended Hebrew-numeric assertion, 5 regenerated baselines; `tests/34-date-locale.test.js` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/pdf-export.js
@assets/export-modal.js
@tests/pdf-digit-order.test.js
@tests/pdf-latin-regression.test.js
</context>

<artifacts_produced>
- `assets/pdf-export.js` `formatDate` delegating to `window.DateFormat`
- `assets/export-modal.js` passing raw ISO + `exportedOn = App.formatDate(DateFormat.todayLocalISO())`
- extended Hebrew-numeric PDF assertion in `tests/pdf-digit-order.test.js`
- 5 regenerated `.planning/fixtures/phase-23/*.pdf.sha256` baselines
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Delegate pdf-export.js formatDate + stop export-modal pre-formatting</name>
  <files>assets/pdf-export.js, assets/export-modal.js</files>
  <read_first>
    - 37-RESEARCH.md §"PDF Integration" (the current 4-step flow; where en-GB lives at pdf-export.js:685; the exact edits) and §"The Definitive UTC-Parse Sweep" rows 12/13/14 (pdf-export.js:680, export-modal.js:326-327, export-modal.js:602)
    - assets/pdf-export.js formatDate (~line 674-690) and where the card date + footer are drawn (~:877, :1846-1849)
    - assets/export-modal.js (~:326-330 pre-format, :602 exportedOn, :646-651/:709-714 buildSessionPDF args)
  </read_first>
  <action>
    Rework pdf-export.js formatDate to delegate its whole body to window.DateFormat.format(sessionDate, window.DateFormat.getPreference(), uiLang); the old en-GB / T00:00:00 body becomes dead and is removed. In export-modal.js, stop pre-formatting the session date: pass the RAW ISO sessionDate value into buildSessionPDF (the two call sites that currently pass the pre-formatted string) so pdf-export owns formatting; keep the on-screen Step-1 preview using App.formatDate if it exists. Change the footer exportedOn from App.formatDate(new Date()) to App.formatDate(window.DateFormat.todayLocalISO()) so the footer uses local today in the chosen format. Touch only the session-card date + footer exportedOn — leave every wall-clock timestamp untouched.
  </action>
  <verify>
    <automated>node tests/34-date-locale.test.js && node tests/pdf-bold-rendering.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DateFormat.format" assets/pdf-export.js` shows formatDate delegating to the engine
    - `node tests/34-date-locale.test.js` exits 0 (the rewritten fixed-behavior assertions pass, including the raw-ISO export chain producing the en-US card date)
    - export-modal passes raw ISO (no pre-formatted session date reaches buildSessionPDF); the footer uses `todayLocalISO()`
  </acceptance_criteria>
  <done>The PDF date path is unified on window.DateFormat; 34-date-locale is green; the export chain passes raw ISO.</done>
</task>

<task type="auto">
  <name>Task 2: Hebrew-numeric PDF assertion (isolate tolerance, DATE-04) + regenerate baselines</name>
  <files>tests/pdf-digit-order.test.js, .planning/fixtures/phase-23/fixture-en.pdf.sha256, .planning/fixtures/phase-23/fixture-de.pdf.sha256, .planning/fixtures/phase-23/fixture-cs.pdf.sha256, .planning/fixtures/phase-23/fixture-he.pdf.sha256, .planning/fixtures/phase-23/fixture-he-mixed.pdf.sha256</files>
  <read_first>
    - 37-RESEARCH.md §"Hebrew Numeric LTR Wrapping" (the PDF caveat: isolates may be handled, stripped, or render as .notdef — test-gated; the strip-fallback) and §"PDF Golden-Baseline Regeneration" (the safe regeneration procedure + the per-fixture drift table). NOTE (FIX 4): the drift is TWO fixtures wide — fixture-he AND fixture-he-mixed are expected-changed (Hebrew LONG→SHORT month, because old pdf-export.js:686 used month:'long' for he-IL and the new engine uses he=short), not just fixture-en; de/cs are unchanged. Read the RESEARCH drift table with that correction.
    - tests/pdf-digit-order.test.js (the GID-run extraction technique asserting '2026' present / '6202' absent)
    - tests/pdf-latin-regression.test.js (the --regenerate path at ~line 41; the 5 fixtures at ~:125-130)
  </read_first>
  <action>
    Add a Hebrew mm/dd/yyyy assertion: build a he PDF with portfolioDateFormat set to mm/dd/yyyy and sessionDate '2026-07-02', reuse the pdf-digit-order GID-run extraction, and assert the year group 2026 is present and the reversed 6202 is absent (and 07 present) — proving the U+2066/U+2069 isolates survive bidi.min.js. If the assertion shows the isolates break rendering (.notdef or stripping), apply the RESEARCH strip-fallback: pdf-export.js strips U+2066/U+2069 before shapeForJsPdf and relies on the known LTR draw position; re-run until the digit-order assertion passes.

    Regenerate the 5 SHA-256 baselines using the safe procedure: render each fixture PDF to a real file on disk, open/inspect each so a human confirms (a) the en card date reads exactly the en-US form ('Jul 2, 2026'), (b) the he + he-mixed card dates read the Hebrew SHORT-month form (the intended long→short shift, FIX 4), and (c) de + cs are unchanged, THEN run the regenerate path, then re-run without --regenerate and confirm 5/5 pass. Do not blind-regenerate.
  </action>
  <verify>
    <automated>node tests/pdf-digit-order.test.js && node tests/pdf-latin-regression.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/pdf-digit-order.test.js` includes a he mm/dd/yyyy assertion (2026 present / 6202 absent) and exits 0
    - `node tests/pdf-latin-regression.test.js` exits 0 with the regenerated baselines (5/5)
    - `git diff --stat` on the 5 `.sha256` files shows the intended change: fixture-en, fixture-he, AND fixture-he-mixed changed (en-US shift + Hebrew long→short month, all human-confirmed as correct per FIX 4); fixture-de and fixture-cs UNCHANGED (byte-identical)
  </acceptance_criteria>
  <done>Hebrew numeric survives jsPDF (or the strip-fallback is applied) and the 5 baselines are regenerated from real, visually-verified renders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual sign-off on the regenerated PDF baselines</name>
  <action>Blocking human checkpoint — no automated action. The developer performs the visual verification steps below before the 5 regenerated `.sha256` baselines are committed. Do not proceed on a green test alone (jsdom PDF builds can hang→exit-0 silently).</action>
  <what-built>The PDF date path now flows through window.DateFormat. TWO deliberate baseline shifts (FIX 4): (a) English shifted en-GB ("2 July 2026") → en-US ("Jul 2, 2026") per D-04/D-19; (b) Hebrew shifted LONG month → SHORT month, because the old PDF path used month:'long' for he-IL while the engine reproduces the UI's he=short-month rule — so the PDF finally matches the on-screen UI (D-08). German and Czech are UNCHANGED (both old and new use long month). The 5 SHA-256 golden baselines were regenerated from real rendered PDFs. Per MEMORY `reference-pdf-jsdom-inert-gates`, a jsdom PDF build can hang→exit-0 silently, so a green test alone is not proof — the bytes need human eyes.</what-built>
  <how-to-verify>
    1. Open the regenerated rendered PDFs (the real files produced during Task 2, before the .sha256 overwrite).
    2. Confirm the English fixture's session-card date reads exactly "Jul 2, 2026" (en-US), NOT "2 July 2026" (en-GB).
    3. Confirm the Hebrew fixtures (fixture-he + fixture-he-mixed) now show the Hebrew SHORT-month form (e.g. the abbreviated month), which is the CORRECT new output matching the UI — this is the intended change, NOT a regression to reject.
    4. Confirm the German (de-DE long) and Czech (cs-CZ long) fixtures' card dates are UNCHANGED from before (visually identical).
    5. In the Hebrew PDF, confirm a numeric date reads left-to-right (digits not flipped).
    6. Confirm no other visible content shifted (layout/glyphs).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the baseline regeneration, or describe any unintended drift to fix before the baselines are committed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend. The PDF is generated client-side from the user's own session data; no untrusted external input crosses a boundary here.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04-01 | Tampering (bidi spoofing in PDF) | Hebrew numeric date via bidi.min.js | low | mitigate | The Hebrew-numeric digit-order assertion (2026 present / 6202 absent) proves the isolate wrap renders LTR without flipping; strip-fallback specified if isolates break |
| T-37-04-02 | Repudiation (wrong golden bytes) | regenerated SHA-256 baselines | medium | mitigate | Blocking human visual verification before the .sha256 overwrite is committed — never blind --regenerate |

No package installs — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/34-date-locale.test.js` green (fixed en-US + raw-ISO chain)
- `node tests/pdf-digit-order.test.js` green (Hebrew numeric LTR)
- `node tests/pdf-latin-regression.test.js` green (5/5 regenerated baselines)
- Human visual sign-off on the regenerated PDFs (blocking checkpoint)
- `npm test` full suite green
</verification>

<success_criteria>
The PDF card + footer dates flow through window.DateFormat, the export chain passes raw ISO, Hebrew numeric renders LTR in the PDF, and the 5 golden baselines are regenerated from human-verified real renders (DATE-04 PDF half, DATE-05, DATE-07).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-04-SUMMARY.md` when done
</output>
