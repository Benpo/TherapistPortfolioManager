---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 15
type: execute
wave: 8
depends_on: [37-13, 37-14]
files_modified:
  - assets/overview.js
  - index.html
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: false
requirements: [FILT-03]
must_haves:
  truths:
    - "Clicking the Overview Name / Sessions / Last Session column headers sorts by that column; the header shows a direction arrow; a second click flips direction"
    - "The header click and the #clientSortSelect dropdown drive the SAME sort state and stay in sync both ways (RTL-aware, aria-sort correct)"
    - "The Type (Client Type) and Actions columns are not sortable"
    - "All new controls (both screens) render without horizontal overflow in LTR, Hebrew RTL, and ~500px mobile"
    - "The full test suite is green (all three 37-10 RED files now GREEN)"
  artifacts:
    - "index.html: sortable th (data-sort-key + aria-sort + .sort-arrow) on Name/Sessions/Last Session"
    - "overview.js: shared sort state, header click handlers, dropdown sync, direction arrow (createElementNS), aria-sort management"
    - "i18n x4: dead sub-keys sessions.filter.type.all/.heartShield/.regular removed (parent sessions.filter.type toggle label intact)"
  key_links:
    - "the #clientSortSelect options (name/lastSession/sessions) map 1:1 to the three sortable columns"
    - "turns tests/37-overview-sort.test.js GREEN and gates the whole work package on a full green suite"
---

<objective>
Deliver DECISIONS D2b (Sort = BOTH): add click-to-sort on the Overview table's Name / Sessions / Last Session column headers (direction arrow, RTL-aware, aria-sort) AND keep the existing #clientSortSelect dropdown, with both driving one shared sort state and staying in sync. Then run the final headless-Chrome visual verification of all the new controls across both screens (LTR + Hebrew RTL + ~500px mobile) and gate the work package on a fully green suite.

This turns tests/37-overview-sort.test.js GREEN and, with 37-13/37-14 already green, brings the whole suite green. Touch NO app.css (the sortable-header + arrow styles were front-loaded in 37-11). The ONLY i18n edit here is Task 1.5's removal of the three dead heart-dropdown sub-keys — safe only in this wave, after BOTH old dropdowns are gone.
Purpose: fast column sorting without losing the accessible dropdown.
Output: overview.js + index.html; a throwaway headless-render harness (not shipped).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@assets/overview.js
@index.html
@tests/37-overview-sort.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Click-to-sort headers + two-way sync with #clientSortSelect (+ direction arrows)</name>
  <files>index.html, assets/overview.js</files>
  <read_first>
    - tests/37-overview-sort.test.js (the pinned th[data-sort-key]/aria-sort/.sort-arrow contract + both sync directions + toggle-on-repeat)
    - index.html lines 158-172 (the table thead: Name/Type/Sessions/Last Session/Actions)
    - assets/overview.js lines 180-291 (applyFiltersAndSort — the sortVal read from #clientSortSelect and the name/lastSession/sessions branches; onFilterChange; updateClearButton) and 344-364 (render entry)
    - 37-11 artifacts: the .table th.sortable / aria-sort state rules / .sort-arrow CSS classes to attach to
  </read_first>
  <action>
    In index.html, mark the Name, Sessions, and Last Session <th> as sortable: add class "sortable", a data-sort-key ("name" | "sessions" | "lastSession"), an initial aria-sort="none" (Name may start "ascending" to reflect the default), and a child span.sort-arrow. Leave the Type (Client Type) and Actions headers plain (no data-sort-key). Keep #clientSortSelect and its options.

    In overview.js, introduce one shared sort state (key + direction), initialised from #clientSortSelect's current value (default key "name"). PER-KEY DEFAULT DIRECTIONS (pinned by tests/37-overview-sort.test.js — do not unify): name -> ascending; sessions and lastSession -> DESCENDING (most sessions / most recent first, matching the shipped dropdown behavior at overview.js ~211-226). Add a click handler on each sortable <th>: clicking a new key sets that key with ITS per-key default direction; clicking the already-active key flips direction; it then syncs #clientSortSelect.value to the key, sets aria-sort on the clicked <th> (ascending/descending) and resets the others to "none", updates the .sort-arrow direction (build/point the arrow SVG via createElementNS — no innerHTML), and re-runs applyFiltersAndSort. Extend applyFiltersAndSort to honor the shared direction for all three keys (name/sessions/lastSession ascending or descending). Add a #clientSortSelect change handler that updates the shared state key (default direction), mirrors aria-sort + arrow onto the matching header, resets the others, and re-renders — so the dropdown drives the same state. Keyboard: make the sortable headers focusable + Enter/Space activate (a11y).
  </action>
  <verify>
    <automated>node tests/37-overview-sort.test.js</automated>
  </verify>
  <acceptance_criteria>
    - index.html Name/Sessions/Last Session th carry class sortable + data-sort-key + aria-sort + span.sort-arrow; Type and Actions have no data-sort-key
    - Clicking a header sorts by it, sets its aria-sort direction, resets the others to none, updates the arrow, and sets #clientSortSelect.value to match
    - Clicking the active header flips direction and reverses row order; changing #clientSortSelect updates the header aria-sort + arrow + row order
    - The sort arrow is built via createElementNS (no innerHTML); headers are keyboard-activatable
    - `node tests/37-overview-sort.test.js` exits 0; no edit to any i18n bundle or app.css
  </acceptance_criteria>
  <done>Overview headers and the Sort dropdown drive one synced, bidirectional sort state.</done>
</task>

<task type="auto">
  <name>Task 1.5: Retire the dead heart-dropdown i18n sub-keys (post-dropdown cleanup)</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js lines 235-239 (the sessions.filter.type.* block; the HE/DE/CS peers carry the same keys at the same lines)
    - 37-11 Relabel Table B (the PARENT key sessions.filter.type is repurposed as the Sessions Heart-Wall toggle label — it MUST survive)
  </read_first>
  <action>
    Delete the three now-unreferenced dropdown-option keys "sessions.filter.type.all", "sessions.filter.type.heartShield", and "sessions.filter.type.regular" from all four i18n bundles. Both consuming dropdowns are already gone by this wave (#clientHeartShieldFilter removed by 37-13, #sessionTypeFilter removed by 37-14). Do NOT touch the parent key "sessions.filter.type" (the repurposed Heart-Wall toggle label from 37-11) or any other key. Commit as its own chore(37) commit.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');var bad=false;['en','he','de','cs'].forEach(function(l){var s=fs.readFileSync('assets/i18n-'+l+'.js','utf8');if(s.indexOf('sessions.filter.type.')!==-1){console.error('dead sub-key survives in i18n-'+l);bad=true;}if(s.indexOf('sessions.filter.type')===-1){console.error('parent toggle-label key missing in i18n-'+l);bad=true;}});['index.html','sessions.html'].forEach(function(f){if(fs.readFileSync(f,'utf8').indexOf('sessions.filter.type.')!==-1){console.error('dead sub-key still referenced in '+f);bad=true;}});if(bad)process.exit(1);console.log('OK: dead heart-dropdown sub-keys retired, parent key intact')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "sessions\.filter\.type\." assets/ index.html sessions.html` returns nothing (all three sub-keys gone, nothing references them)
    - The parent key "sessions.filter.type" survives in all 4 bundles with its Heart-Wall toggle value from 37-11
    - `npm test` stays green (plain invocation, no pipes)
  </acceptance_criteria>
  <done>The three dead dropdown sub-keys are gone from all four bundles; the repurposed parent toggle label is intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final headless-Chrome visual verification of all new controls + full-suite green gate</name>
  <files>(no shipped source files — a throwaway headless-render harness under the scratchpad + the full-suite gate)</files>
  <read_first>
    - memory reference-headless-chrome-ui-verification (the isolated-markup + self-measuring headless-Chrome harness; beware Chrome's ~500px min-viewport cropping narrower shots)
    - memory reference-rtl-select-value-alignment-headless (RTL: computed direction is not enough — verify the value/chevron/arrow inline side on the rendered popup)
    - index.html + sessions.html (the final filter bars + Overview table to render) and assets/app.css (the real stylesheet to load into the harness)
  </read_first>
  <action>Before the human step, the executor runs the automated headless-Chrome render + the full `npm test` gate described in how-to-verify, then pauses for Ben's eyeball. Do NOT mark the phase complete — Phase 37 stays PENDING for /gsd-verify-work 37.</action>
  <acceptance_criteria>
    - The headless render covers the Overview filter bar + table headers AND the Sessions filter bar, with the Session Format panel OPEN and the Heart-Wall toggle ON, in LTR + Hebrew RTL + ~500px
    - Each config self-measures document.documentElement.scrollWidth === window.innerWidth (zero horizontal overflow); the RTL chevron/toggle/sort-arrow are on the correct inline side
    - `npm test` is fully green (all three 37-10 files pass; no other regression; no .pdf.sha256 changed)
    - Ben approves the screenshots (or files issues); the phase is left PENDING (no completion step)
  </acceptance_criteria>
  <what-built>All new Phase-37 filter/sort controls: the Overview + Sessions Session Format multi-select (checkbox dropdown), the Heart-Wall toggles, and the Overview sortable headers — verified by an automated headless-Chrome render first, then a human eyeball.</what-built>
  <how-to-verify>
    1. Automated (executor runs before the human step): using the repo's headless-Chrome isolated-markup approach (per reference-headless-chrome-ui-verification), render the Overview filter bar + table headers AND the Sessions filter bar with the real assets/app.css, in three configs — LTR (en), Hebrew RTL (dir=rtl), and ~500px mobile — with the Session Format panel OPEN and the Heart-Wall toggle ON. Self-measure and assert document.documentElement.scrollWidth === window.innerWidth (zero horizontal overflow) in each; assert the RTL chevron/toggle/sort-arrow sit on the correct inline side. Save screenshots under the scratchpad.
    2. Run the full suite: `npm test` — it MUST be green (all three 37-10 files now pass; nothing else regressed; no .pdf.sha256 changed).
    3. Human: open the saved screenshots and confirm the checkbox dropdown, the green Heart-Wall toggle, and the sort arrows read correctly in LTR + Hebrew RTL + mobile (no clipping, correct RTL mirroring). The live in-app render behind the Terms/license gate is Ben's separate UAT — not this gate.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/RTL issues to fix</resume-signal>
  <verify><human-check>Ben confirms from the headless screenshots that the Session Format dropdown, Heart-Wall toggles, and Overview sort arrows render correctly (no clipping, correct RTL mirroring) in LTR + Hebrew RTL + ~500px, and that `npm test` is green.</human-check></verify>
  <done>Ben types "approved" after the headless render (zero overflow, correct RTL) and a green full suite are confirmed; Phase 37 remains PENDING for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session records -> sort comparison | untrusted date/count values drive row order (no injection surface; comparison only) |
| sort-arrow SVG -> DOM | app-controlled SVG built via createElementNS (no user data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-15-01 | Tampering | sort-arrow render path | low | mitigate | arrow built via createElementNS, no innerHTML, no user data |
| T-37-15-02 | Denial of Service | layout overflow in RTL/mobile from the new controls | medium | mitigate | headless-Chrome self-measuring gate asserts zero horizontal overflow in LTR/RTL/500px before human sign-off |
| T-37-15-SC | Tampering | npm/pip/cargo installs | low | mitigate | no package installs; hooks on; never --no-verify |
</threat_model>

<verification>
- `node tests/37-overview-sort.test.js` exits 0; `npm test` fully green (all three 37-10 RED files now GREEN; no other regression; no .pdf.sha256 changed)
- Headless-Chrome render of both screens' new controls shows zero horizontal overflow and correct RTL mirroring in LTR / Hebrew RTL / ~500px
- No app.css modified here; the only i18n change is Task 1.5's dead sub-key removal (`sessions.filter.type.*` gone from all 4 bundles + no HTML references it; parent `sessions.filter.type` intact)
</verification>

<success_criteria>
- Overview sort works from both the headers and the dropdown, synced and bidirectional, RTL-aware
- The full suite is green and the new controls pass the headless LTR/RTL/mobile visual gate + human sign-off
- Phase 37 stays PENDING (no completion step here) — Ben runs /gsd-verify-work 37 after pushing
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-15-SUMMARY.md` when done.
</output>
