---
phase: 33-de-cs-i18n-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
  - 33-02
files_modified:
  - tests/33-i18n-de-cs-completion.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - I18N-01
  - I18N-02
must_haves:
  truths:
    - "An automated test asserts ZERO // TODO i18n markers remain in assets/i18n-de.js AND assets/i18n-cs.js (D-06)"
    - "The same test asserts EXACT bidirectional key parity: DE key set === EN key set and CS key set === EN key set — no missing keys AND no extra keys (D-06)"
    - "The test is discovered and run by the existing single-command harness (tests/run-all.js via npm test) — no new runner is introduced (D-06)"
    - "The REQUIREMENTS.md I18N-01/I18N-02 note reflects the AI native-translation-panel source and carries no human-translator dependency; the stale (needs Sapir's strings) annotation is removed (D-02)"
    - "The D-06 automated check is EXHAUSTIVE — it verifies the COMPLETE finite key set for parity and the COMPLETE files for zero markers; full-population verification of a finite set is not a sampling problem, so parity + no-TODO fully satisfies validation coverage (no Nyquist/Dimension-8 gap)"
  artifacts:
    - "tests/33-i18n-de-cs-completion.test.js — parity + no-TODO gate over both target files"
    - ".planning/REQUIREMENTS.md — updated I18N-01/I18N-02 source note (D-02)"
  key_links:
    - "tests/run-all.js auto-discovers top-level tests/*.test.js and preserves each file's exit-0/1 contract — the new file plugs into `npm test` with no runner change"
    - "The test loads the i18n dictionaries in a vm sandbox (window.I18N.{en,de,cs}) exactly like tests/25-11-i18n-parity.test.js"
---

<objective>
Add the D-06 automated verification gate and correct the stale REQUIREMENTS.md source note. The gate proves — for the complete key set and complete files — that DE and CS no longer fall back to English and carry no placeholder markers. This closes the phase's goal-backward loop: the translations exist (Plans 33-01/33-02) AND a repeatable test enforces they stay complete.

Purpose: Make "no English fallbacks and no `// TODO i18n` markers remain" a standing, machine-checked invariant (D-06); align the requirement note with the AI-panel source that superseded the human-translator dependency (D-02).
Output: `tests/33-i18n-de-cs-completion.test.js` (green) and an updated I18N-01/I18N-02 note in `.planning/REQUIREMENTS.md`.

**Validation adequacy (Nyquist / Dimension 8):** research was intentionally skipped for this phase, so there is no VALIDATION.md — this is CORRECT and must NOT be flagged as a validation gap. The D-06 automated check is exhaustive, not a sample: it verifies the COMPLETE key set for parity and scans the COMPLETE files for markers. Full-population verification of a finite key set is not a sampling problem, so the parity + no-TODO test fully satisfies validation coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-de-cs-i18n-completion/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the parity + no-TODO verification test to the existing harness</name>
  <files>tests/33-i18n-de-cs-completion.test.js</files>

  <read_first>
    - `tests/25-11-i18n-parity.test.js` — the vm-sandbox pattern to follow: it loads the four i18n files into a sandbox with `{ window: { I18N: {} } }`, then reads `window.I18N.{en,he,de,cs}`. Reuse this loading approach. Note it only checks EN ⊆ {he,de,cs} (no missing keys); this new test adds the REVERSE direction (no EXTRA keys) plus the no-marker assertion.
    - `tests/run-all.js` — the single-command runner. It auto-discovers top-level `tests/*.test.js`, runs each in its own child process, and preserves each file's `process.exit(0|1)` contract. The new file must follow that contract (exit 0 on all-pass, exit 1 on any failure) and needs no runner change.
    - `assets/i18n-en.js`, `assets/i18n-de.js`, `assets/i18n-cs.js` — the dictionaries under test.
  </read_first>

  <action>
Create `tests/33-i18n-de-cs-completion.test.js`, following the vm-sandbox loading style of `tests/25-11-i18n-parity.test.js` and the exit-0/1 contract that `tests/run-all.js` expects. Load `i18n-en.js`, `i18n-de.js`, and `i18n-cs.js` into a sandbox and read `window.I18N.{en,de,cs}`. Do NOT add a new runner and do NOT modify `tests/run-all.js` — the runner discovers this file automatically.

The test must assert (each as a distinct named case so failures pinpoint the cause):

1. **No placeholder markers.** Read the raw source of `assets/i18n-de.js` and `assets/i18n-cs.js` and assert neither file contains the string `// TODO i18n`. (This is the D-06 no-marker gate; it complements the per-key checks in Plans 33-01/33-02.)

2. **Exact DE↔EN key parity.** The set of keys in `window.I18N.de` equals the set in `window.I18N.en` — assert BOTH directions: every EN key exists in DE (no missing) AND every DE key exists in EN (no extra). Report the offending keys on failure.

3. **Exact CS↔EN key parity.** Same bidirectional assertion for `window.I18N.cs` versus `window.I18N.en`.

Do NOT assert specific German/Czech string content (translation quality is the D-06 manual visual check, and cognates like "Export" make value-equality checks unreliable). Do NOT duplicate the RTL guard — one already exists from Phase 30 / TEST-02. Keep the file self-contained with no new dependencies (plain `fs` + `vm`, matching the existing test).
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `tests/33-i18n-de-cs-completion.test.js` exists and is discovered by `tests/run-all.js` (appears in the `npm test` output).
    - The full suite passes (`npm test` exits 0), including the new file's no-marker + bidirectional DE↔EN and CS↔EN parity cases.
    - The test genuinely gates: it would FAIL if a `// TODO i18n` marker were reintroduced or if a key were added to/removed from DE or CS without a matching EN key.
    - No change to `tests/run-all.js`; no new runtime dependency added.
  </acceptance_criteria>
  <done>`tests/33-i18n-de-cs-completion.test.js` exists, is discovered by `npm test`, and the full suite passes with the no-marker + bidirectional DE↔EN and CS↔EN parity cases green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the REQUIREMENTS.md I18N source note (D-02)</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the I18N-01 and I18N-02 bullets under "### DE/CS i18n Completion (Phase 33)". Each currently ends with the stale parenthetical "(needs Sapir's strings)" and references stale line numbers.
    - `.planning/phases/33-de-cs-i18n-completion/33-CONTEXT.md` — D-02 (the AI native-translation panel supersedes the human-translator dependency).
  </read_first>

  <action>
Edit the I18N-01 and I18N-02 bullets in `.planning/REQUIREMENTS.md` so the source note reflects reality per D-02: the 13 export-modal keys are translated by the AI native-translation panel (D-01), with NO human-translator ("Sapir's strings") dependency. Remove the trailing "(needs Sapir's strings)" annotation from both bullets and, since the line-number references are stale (the files have grown), phrase the requirement in terms of "the 13 export-modal keys" rather than a line range. Keep each bullet's checkbox state and requirement ID unchanged — only the descriptive text/parenthetical changes. Do not touch the Traceability table status (phase-completion workflow owns that) and do not edit any other requirement.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const seg=t.split('### DE/CS i18n Completion')[1].split('###')[0];if(/needs Sapir/i.test(seg)){console.error('FAIL: stale (needs Sapir) note still present in I18N section');process.exit(1)}if(!/I18N-01/.test(seg)||!/I18N-02/.test(seg)){console.error('FAIL: I18N bullets missing');process.exit(1)}console.log('OK: I18N note updated, no human-translator dependency remains')"</automated>
  </verify>

  <acceptance_criteria>
    - The I18N-01 and I18N-02 bullets no longer contain "(needs Sapir's strings)".
    - The bullets reflect the AI native-translation-panel source (D-02) and reference "the 13 export-modal keys" rather than a stale line range.
    - Requirement IDs and checkbox markers are unchanged; no other requirement bullet is modified.
  </acceptance_criteria>
  <done>The I18N-01/I18N-02 note in `.planning/REQUIREMENTS.md` reflects the AI native-translation-panel source with no human-translator dependency; the Task 2 automated check passes.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

- **New file:** `tests/33-i18n-de-cs-completion.test.js` (parity + no-TODO gate; plain `fs`+`vm`, exit-0/1 contract).
- **Edited docs:** `.planning/REQUIREMENTS.md` I18N-01/I18N-02 source note (D-02).
- No production code, no i18n key changes (those are Plans 33-01/33-02).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A test file and a docs edit. No runtime code path, no user-input, no DOM, no network surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Tampering | `tests/33-i18n-de-cs-completion.test.js` (dev-only) + `.planning/REQUIREMENTS.md` (planning doc) | low | accept | Neither artifact ships to users or executes in the app. The test reads repo files with `fs`/`vm` only; no network, no external input. No new attack surface. |

No package-manager installs occur, so no supply-chain (`T-33-SC`) checkpoint is required.
</threat_model>

<verification>
- **Automated (D-06 part 1):** `npm test` runs the full suite including the new file; the no-marker + bidirectional DE↔EN and CS↔EN parity cases pass. This is the exhaustive, standing gate for "no English fallbacks / no markers."
- **Manual (D-06 part 2 — human/UAT, NOT an autonomous executor task):** switch the app locale to DE and to CS and open the export modal; visually confirm steps 1–3 (stepper-label chips, step helpers, formatting tips) render in-language and fit — no overflow on the stepper-label chips, no clipped helpers. Capture screenshots. Track this as a phase-level UAT/manual verification item; it does not block the autonomous plans.
</verification>

<success_criteria>
- `npm test` enforces zero `// TODO i18n` markers and exact DE↔EN / CS↔EN key parity across both target files (D-06).
- The REQUIREMENTS.md I18N note describes the AI-panel source with no human-translator dependency (D-02).
</success_criteria>

<output>
Create `.planning/phases/33-de-cs-i18n-completion/33-03-SUMMARY.md` when done.
</output>
