---
phase: 38-next-session-date-field-with-overview-column
plan: 06
type: execute
wave: 2
depends_on: ["38-02"]
files_modified:
  - assets/export-modal.js
autonomous: true
requirements: [NEXT-06, NEXT-08]
must_haves:
  truths:
    - "The next-session date is rendered in the export markdown alongside the customerSummary note under the Information for Next Session heading, formatted via window.DateFormat (locale/RTL-aware, honoring portfolioDateFormat) - added to BOTH the clipboard-copy builder (export-modal.js:274) and the export builder (export-modal.js:411) per D-09 and orchestrator directive 1 (NEXT-06)"
    - "The nextSession block's empty-note gate becomes note-OR-date present: a date-only session (empty note, date set) still renders the block, and the nextSession content check (:142-144) returns true when a date is present so the include-toggle defaults on (D-09)"
    - "The per-section include-toggle still governs both note and date: if the nextSession section is excluded, both are omitted; if only one of note/date is present, the present one renders (D-09)"
    - "The date reaches the PDF automatically because pdf-export.js parses the export markdown; existing static PDF golden SHA-256 baselines are EXPECTED UNCHANGED - if any pdf-* hash changes, STOP and investigate, never blind --regenerate or force-pass (D-10; research Finding 3; MEMORY reference-pdf-jsdom-inert-gates)"
  artifacts:
    - "assets/export-modal.js (content check + both markdown builders emit the date line)"
  key_links:
    - "date formatted via window.DateFormat / App.formatDate exactly as the overview cell and export footer do (Phase 37 engine)"
    - "gate becomes (summaryValue.length > 0 || nextDatePresent) at :274 and :411; content-check :142-144 also keys off the date"
    - "tests/30-export-markdown.test.js + tests/30-section-visibility.test.js (Plan 38-02) flip GREEN when this lands"
    - "PDF path: pdf-export.js builds from sessionData.markdown - no direct pdf-export.js edit; static fixtures unchanged"
---

<objective>
Render the next-session date in the PDF/markdown export next to the note, gated by the same nextSession section toggle, and make a date-only session still export — NEXT-06. Confirm the static PDF golden baselines remain unchanged (D-10 discipline) — NEXT-08.

Purpose: D-09 puts the date in the export; the subtlety is the empty-note gate (both builders skip the block when the note is empty) must become note-OR-date so a date-only session renders. The PDF renders from the export markdown, so no pdf-export.js edit is needed — and per research Finding 3 the static fixtures should NOT change; the executor must treat any pdf-* hash change as a red flag, not a regeneration prompt.
Output: Export markdown (and therefore PDF) carrying the formatted next-session date, with unchanged golden baselines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-CONTEXT.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- Session-record field `nextSessionDate` (consumed by the export builders)
- Behavior: date line + date-only rendering in the nextSession export block
- (other phase-38 symbols: `#nextSessionDate`, `mostRecentSession`, `syncNextSessionMin`, `nextSession` sort key, overview `<th>`/`<option>`, `.next-session-cell`/`.is-overdue`/`.next-overdue-dot`, i18n keys, tests/38-*.test.js)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Emit the next-session date in both markdown builders + update the content check (export-modal.js)</name>
  <files>assets/export-modal.js</files>
  <read_first>
    - assets/export-modal.js (:142-144 nextSession content check keyed off #customerSummary; :208 copy-builder summaryValue read; :274-276 copy-builder gate `if (summaryValue.length > 0)`; :410 export-builder summaryValue read; :411-413 export-builder gate `if (selected.has("nextSession") && summaryValue.length > 0)`; :319 getSectionLabel fallback keys)
    - assets/date-format.js (window.DateFormat format API — honoring portfolioDateFormat) and how app.js App.formatDate delegates (the export footer already uses App.formatDate per Phase 37)
    - tests/30-export-markdown.test.js + tests/30-section-visibility.test.js (Plan 38-02 — the behavior contract)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Pitfall 4 empty-note gate; Code Examples for the export path)
  </read_first>
  <action>Read the next-session date once in each builder: nextDateRaw = document.getElementById("nextSessionDate")?.value or "" (same source the save path reads). Compute nextDateFormatted via the same locale/RTL-aware engine the overview + footer use (App.formatDate(nextDateRaw) or window.DateFormat honoring portfolioDateFormat) when nextDateRaw is non-empty. In the COPY builder at :274, change the gate to fire when (summaryValue.length > 0 || nextDateRaw) and, inside, push the nextSession heading followed by the formatted date line (when present) and the note (when present) — render whichever exists. In the EXPORT builder at :411, change the gate to `selected.has("nextSession") && (summaryValue.length > 0 || nextDateRaw)` and likewise emit the heading + date line + note for whichever is present. In the content check at :142-144, return true when EITHER the #customerSummary note has text OR #nextSessionDate has a value, so the include-toggle defaults on for a date-only session. Keep the existing heading source (getSectionLabel("nextSession", "session.form.nextSession")); do NOT introduce a new heading key. Do not use innerHTML anywhere; these are plain markdown string pushes.</action>
  <verify>
    <automated>node tests/30-export-markdown.test.js && node tests/30-section-visibility.test.js && echo EXPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` passes (was RED): the formatted next-session date appears in the export markdown and a date-only session still renders the nextSession block
    - `node tests/30-section-visibility.test.js` passes: the nextSession toggle defaults on for a date-only session; excluding the section drops both note and date
    - Both the copy builder (:274) and the export builder (:411) gates are `(summaryValue.length > 0 || nextDateRaw)` and emit the date via App.formatDate / window.DateFormat (not raw ISO), honoring portfolioDateFormat
    - The content check (:142-144) returns true when only a date is present
    - No new heading i18n key introduced; no innerHTML used
  </acceptance_criteria>
  <done>Both markdown builders render the formatted next-session date beside the note, date-only sessions export, and the toggle gates both. (D-09, NEXT-06)</done>
</task>

<task type="auto">
  <name>Task 2: Confirm PDF golden baselines are unchanged (D-10 guard) — full suite green, no pdf-* hash drift</name>
  <files>(verification only — no files modified; inspects .planning/fixtures/phase-23/ + pdf-* baselines)</files>
  <read_first>
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Finding 3 + Pitfall 3 — static fixtures; existing SHA-256 hashes should PASS unchanged; regenerate only if you deliberately edit a fixture JSON)
    - .planning/fixtures/phase-23/fixture-en.json (:6 static sessionData.markdown — proof the live builder is not invoked for the golden PDF baselines)
    - tests/pdf-latin-regression.test.js (:90 builds from fixture.sessionData; :130 hash assertion)
    - MEMORY reference-pdf-jsdom-inert-gates (verify real output, not exit code; PDF jsdom tests can hang→exit-0 silently)
  </read_first>
  <action>Run the full suite with node tests/run-all.js. Confirm all pdf-* / golden SHA-256 baseline tests PASS with their EXISTING baselines — these fixtures carry static pre-baked markdown, so Task 1's live-builder change must NOT perturb them. If any pdf-* or golden hash test FAILS or reports a changed hash, STOP: do not run any --regenerate, do not force-pass, do not edit a .sha256 baseline. Investigate why pdf-export.js output drifted (it should not have) and report. Do NOT deliberately add a nextSessionDate to any fixture JSON in this plan (D-09 coverage lives in the live-builder markdown tests from Task 1, not in the static PDF fixtures). Because the PDF jsdom harness can hang-then-exit-0 (inert false-GREEN), confirm the pdf test files actually ran assertions (non-trivial output), not just exited 0.</action>
  <verify>
    <automated>node tests/run-all.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/run-all.js` completes with all pdf-* / golden baseline tests GREEN against their EXISTING .sha256 baselines (no hash changed)
    - No .sha256 baseline file was regenerated or edited in this plan; no --regenerate/force-pass was run
    - The PDF regression test files produced real assertion output (not a silent exit-0 hang) — confirmed from the run-all tail
    - If any pdf-* hash had drifted, the plan halts and reports instead of regenerating
  </acceptance_criteria>
  <done>Existing PDF golden baselines pass unchanged, confirming the export-markdown change did not perturb pdf-export.js; no blind regeneration occurred. (D-10, NEXT-08)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session field → export markdown | The date is injected into the markdown string that feeds the .md download and the PDF parser |
| export markdown → PDF | pdf-export.js parses the markdown; a perturbation would change golden baselines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-03 | Tampering | export-modal.js builders | medium | mitigate | Date emitted as a plain markdown string line (no innerHTML); formatted via window.DateFormat; section toggle still gates it |
| T-38-04 | Repudiation | PDF golden baselines | high | mitigate | Static fixtures expected unchanged; any pdf-* hash drift halts the plan (no blind --regenerate, no force-pass, real-output check) per D-10 + reference-pdf-jsdom-inert-gates |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node tests/30-export-markdown.test.js` + `node tests/30-section-visibility.test.js` GREEN (date line, date-only render, toggle gating)
- `node tests/run-all.js` GREEN with all pdf-* baselines unchanged; PDF tests produced real assertion output (no inert exit-0)
</verification>

<success_criteria>
The next-session date renders in the export (markdown + PDF) gated by the section toggle, date-only sessions export, and golden PDF baselines remain unchanged — NEXT-06, NEXT-08 satisfied.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-06-SUMMARY.md` when done.
</output>
