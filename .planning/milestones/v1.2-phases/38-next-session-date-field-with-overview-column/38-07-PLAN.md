---
phase: 38-next-session-date-field-with-overview-column
plan: 07
type: execute
wave: 2
depends_on: ["38-02"]
files_modified:
  - assets/demo-seed.js
  - assets/demo-seed-data.json
autonomous: true
requirements: [NEXT-07]
must_haves:
  truths:
    - "applyRelativeDates (assets/demo-seed.js) converts an optional relative nextSessionDaysAgo helper (negative = future) into a self-freshening nextSessionDate = isoDaysAgo(nextSessionDaysAgo), deleting the helper key - mirroring the existing daysAgo->date conversion so the demo next-dates never drift all-overdue over time (D-12)"
    - "A few RECENT seeded sessions in demo-seed-data.json carry nextSessionDaysAgo with near-future values so the demo Next Session column reads populated and mostly upcoming, with at most one deliberately near-past value to showcase the overdue cue (D-12; NEXT-07)"
    - "Backup/restore carries nextSessionDate automatically as part of the whole session object in the ZIP export/import - sessions are exported wholesale (backup.js:647,675) and restored via db.addSession (backup.js:1122-1123); BACKUP_CONTENTS_KEYS is top-level, not a per-field allowlist - so NO backup.js change is expected (D-11)"
  artifacts:
    - "assets/demo-seed.js (applyRelativeDates handles nextSessionDaysAgo)"
    - "assets/demo-seed-data.json (nextSessionDaysAgo on a few recent sessions)"
  key_links:
    - "nextSessionDaysAgo -> nextSessionDate mirrors daysAgo -> date (assets/demo-seed.js:30-34); keeps DEMO-06 self-freshening invariant"
    - "seeded next-dates land on the MOST-RECENT session per client so the overview column (clientSessions[0].nextSessionDate) reads populated"
    - "tests/35-demo-seed.test.js (Plan 38-02) flips GREEN when this lands"
    - "backup round-trip is verification-only; expect zero backup.js edits (D-11)"
---

<objective>
Seed a few near-future next-session dates into the demo so the new column reads naturally (self-freshening via the relative-date seam), and spot-check that backup/restore carries the field automatically with no backup.js change — NEXT-07 (D-11, D-12).

Purpose: D-12 requires the demo column to look populated and mostly upcoming, not all-overdue. Because the demo self-freshens session dates from a relative daysAgo model, the next-dates must be relative too (a static absolute date would drift into the past). D-11 is a verification: sessions are backed up as whole objects, so the new field rides along for free.
Output: Self-freshening demo next-dates + a confirmed automatic backup round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-CONTEXT.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- Demo seed helper key `nextSessionDaysAgo` (relative, JSON-only; converted to `nextSessionDate` by applyRelativeDates)
- Session-record field `nextSessionDate` (populated on seeded demo sessions post-seam)
- (other phase-38 symbols: `#nextSessionDate`, `mostRecentSession`, `syncNextSessionMin`, `nextSession` sort key, overview `<th>`/`<option>`, `.next-session-cell`/`.is-overdue`/`.next-overdue-dot`, i18n keys, tests/38-*.test.js)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Convert nextSessionDaysAgo in applyRelativeDates + seed a few recent demo sessions</name>
  <files>assets/demo-seed.js, assets/demo-seed-data.json</files>
  <read_first>
    - assets/demo-seed.js (:8-14 computed-date model note; :18-26 isoDaysAgo(n,now); :28-40 applyRelativeDates: copy.date = isoDaysAgo(s.daysAgo, now) then delete copy.daysAgo, then window.__demoSeedHelpers export; :64-65 seedData applies it)
    - assets/demo-seed-data.json (per-session integer daysAgo; recent sessions have small daysAgo; the customerSummary notes at ~:125 "Next session possible in 8 days" / ~:151 "Next session in 10 days" name plausible offsets)
    - tests/35-demo-seed.test.js (Plan 38-02: the permitted-key allowlist + the post-seam near-future assertion this must satisfy; DEMO-06 daysAgo pattern)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-CONTEXT.md (D-12)
  </read_first>
  <action>In assets/demo-seed.js applyRelativeDates (:30-34), after the existing daysAgo-to-date conversion, add: if s.nextSessionDaysAgo is not null/undefined, set copy.nextSessionDate = isoDaysAgo(s.nextSessionDaysAgo, now) and delete copy.nextSessionDaysAgo (mirror the daysAgo handling exactly; negative values yield a future date because isoDaysAgo(-6) is 6 days ahead of now). Leave sessions without the helper untouched (they simply have no nextSessionDate). In assets/demo-seed-data.json, add a nextSessionDaysAgo integer to a few of the MOST-RECENT seeded sessions (smallest daysAgo, i.e. the ones that are clientSessions[0] for their client so the overview column reads populated). Use near-future negatives (for example around -6 to -12, aligning with the "in 8/10 days" notes where those sessions exist), and include exactly ONE deliberately near-past value (a small positive daysAgo, so its computed nextSessionDate is a few days before today) to showcase the overdue cue — keep the majority upcoming. Do not add nextSessionDaysAgo to every session; a few recent ones is enough.</action>
  <verify>
    <automated>node tests/35-demo-seed.test.js && grep -q 'nextSessionDaysAgo' assets/demo-seed.js && grep -q 'nextSessionDate' assets/demo-seed.js && grep -c 'nextSessionDaysAgo' assets/demo-seed-data.json && echo SEED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/35-demo-seed.test.js` passes (was RED): the allowlist permits nextSessionDaysAgo, recent sessions carry it, and post-applyRelativeDates each nextSessionDate is >= its session's own computed date (near-future), with at most one past
    - assets/demo-seed.js applyRelativeDates converts nextSessionDaysAgo to nextSessionDate and deletes the helper key, mirroring the daysAgo-to-date conversion (isoDaysAgo reused, no new date math)
    - demo-seed-data.json has nextSessionDaysAgo on a few recent sessions (majority near-future negatives, at most one near-past positive); the JSON still parses
    - Sessions without the helper are unchanged (no nextSessionDate injected)
  </acceptance_criteria>
  <done>The demo Next Session column is populated and self-freshening (relative model), mostly upcoming with one overdue example. (D-12, NEXT-07)</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check backup/restore carries nextSessionDate automatically (no backup.js change)</name>
  <files>(verification only — assets/backup.js inspected, not modified)</files>
  <read_first>
    - assets/backup.js (:647 + :675 wholesale db.getAllSessions() export into the ZIP; :1122-1123 restore via db.addSession(manifest.sessions[j]); :1373 BACKUP_CONTENTS_KEYS is top-level content keys, NOT a per-field allowlist)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Runtime State Inventory — Backup payload row; D-11 rides-along verification)
    - any backup round-trip test in tests/ (the whole-object session round-trip pattern) to confirm the invariant by execution
  </read_first>
  <action>Verify by reading that sessions are exported and restored as WHOLE objects in backup.js (getAllSessions into the ZIP at :647/:675; db.addSession(manifest.sessions[j]) at :1122-1123) and that BACKUP_CONTENTS_KEYS (:1373) is a top-level content-key list, not a per-field allowlist that would strip nextSessionDate. Confirm NO code change is required in backup.js for the field to round-trip. Run the existing backup round-trip test(s) to confirm the suite is green and that a session object with nextSessionDate would survive export-then-restore. Do NOT add per-field backup serialization and do NOT edit backup.js. Record the spot-check result in the SUMMARY (files inspected, why no change needed).</action>
  <verify>
    <automated>git diff --quiet -- assets/backup.js && echo BACKUP_UNCHANGED</automated>
  </verify>
  <acceptance_criteria>
    - assets/backup.js is UNCHANGED (git diff --quiet passes; prints BACKUP_UNCHANGED) — the field rides along automatically
    - The SUMMARY records the inspected lines (:647/:675 wholesale export, :1122-1123 restore, :1373 top-level content keys) and why no change is needed
    - The backup round-trip test(s) are green in the full suite
  </acceptance_criteria>
  <done>Backup/restore carries nextSessionDate automatically as part of the whole session object; no backup.js change. (D-11, NEXT-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed JSON → demo render | Seeded relative offsets flow through applyRelativeDates into the demo overview column |
| session object → ZIP backup | The whole session object (incl. nextSessionDate) is serialized to and from the backup archive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-09 | Tampering | demo-seed relative dates | low | mitigate | Relative nextSessionDaysAgo via the existing isoDaysAgo seam keeps the demo self-freshening (no absolute-date drift into all-overdue); seed values are static JSON, no runtime input |
| T-38-10 | Information Disclosure | backup round-trip | low | accept | Field is client-local, non-secret; rides the existing whole-object session export with no new serialization surface (D-11) |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node tests/35-demo-seed.test.js` GREEN (allowlist + post-seam near-future assertion)
- `git diff --quiet -- assets/backup.js` passes (backup.js untouched)
- `node tests/run-all.js` GREEN including the backup round-trip test
- Manual (phase gate): open demo.html, confirm the Next Session column is populated and mostly upcoming with one overdue example
</verification>

<success_criteria>
The demo Next Session column reads naturally via a self-freshening relative model and the field round-trips through backup with zero backup.js change — NEXT-07 satisfied.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-07-SUMMARY.md` when done.
</output>
