---
phase: 38-next-session-date-field-with-overview-column
plan: 05
type: execute
wave: 2
depends_on: ["38-02", "38-03"]
files_modified:
  - index.html
  - demo.html
  - assets/app.css
  - assets/overview.js
autonomous: true
requirements: [NEXT-03, NEXT-04, NEXT-05]
must_haves:
  truths:
    - "A Next Session column appears in the overview table in BOTH index.html and demo.html, immediately after Last Session and before Actions (order Name, Type, Sessions, Last Session, Next Session, Actions), and a matching nextSession sort option is added to #clientSortSelect in both files (D-02)"
    - "The cell shows the MOST-RECENT session's nextSessionDate (clientSessions[0].nextSessionDate) formatted via App.formatDate; empty/missing renders literal dash exactly like Last Session; built via textContent/createTextNode/createElement, never innerHTML (D-01)"
    - "A subtle overdue cue (dimmed date text via .is-overdue plus a 0.6em amber dot .next-overdue-dot with aria-label/title from overview.table.nextSession.overdue) shows only when window.DateFormat.parseLocal(value) is strictly before today-local; today is NOT overdue; empty cells show dash with no marker (D-04, NEXT-05)"
    - "The nextSession sort key defaults to ascending (soonest first), derives the most-recent value the same way the render does (mostRecentSession helper, NOT lastSession reduce-max), sorts empty/missing next-dates to the BOTTOM under BOTH directions, and the detail-row colSpan is bumped 5 to 6 (D-01, D-03, NEXT-04)"
  artifacts:
    - "index.html (Next Session th + option)"
    - "demo.html (Next Session th + option)"
    - "assets/app.css (.next-session-cell / .is-overdue / .next-overdue-dot rule)"
    - "assets/overview.js (mostRecentSession helper, nextSessionCell render + overdue cue, colSpan 6, SORT_DEFAULT_DIR + nextSession comparator branch)"
  key_links:
    - "SORT_DEFAULT_DIR.nextSession = ascending (overview.js:260) drives soonest-first default (D-03)"
    - "mostRecentSession() reuses the date/createdAt/id tiebreak from the render sort (overview.js:619-626) so sort matches display (D-01)"
    - "blanks escape `return dir * base` via early-return (+1/-1) in the nextSession branch (D-03)"
    - "overdue tokens --color-text-muted + --color-warning-text exist in both light+dark (tokens.css) - executor greps to confirm before shipping"
    - "tests/37-overview-sort.test.js (Plan 38-02) flips GREEN when this lands"
---

<objective>
Add the sortable "Next Session" overview column (both index.html and demo.html), the most-recent-session cell render with the subtle overdue cue, and the ascending/blanks-to-bottom sort branch — NEXT-03, NEXT-04, NEXT-05.

Purpose: Surfaces the next-session date in the client overview mirroring Last Session, with two correctness traps handled: the sort must derive the most-recent value itself (not lastSession's reduce-max) and blanks must sink under both directions. The overdue cue is the one new visual treatment, locked exactly by the UI-SPEC.
Output: A sortable Next Session column with an accessible overdue indicator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-CONTEXT.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- Sort key `nextSession` (value `ascending` in `SORT_DEFAULT_DIR`)
- Helper `mostRecentSession(sessions)` (overview.js)
- CSS `.next-session-cell`, `.next-session-cell.is-overdue`, `.next-overdue-dot`
- DOM `<th data-sort-key="nextSession">` + `<option value="nextSession">` in index.html and demo.html
- i18n keys `overview.table.nextSession`, `overview.filter.sort.nextSession`, `overview.table.nextSession.overdue` (added Plan 38-03)
- Session-record field `nextSessionDate`
- (other phase-38 symbols: `#nextSessionDate`, `syncNextSessionMin`, tests/38-*.test.js)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the Next Session header, sort option, and overdue CSS rule (index.html, demo.html, app.css)</name>
  <files>index.html, demo.html, assets/app.css</files>
  <read_first>
    - index.html (:151-155 #clientSortSelect with name/lastSession/sessions options; :164-178 overview thead: name :167, type :170, sessions :171-173, lastSession :174-176, col-actions :177)
    - demo.html (:140-144 #clientSortSelect; :154-166 overview thead — identical 5-column structure ending at col-actions :166)
    - assets/app.css (:480 .table th/.table td padding; :1406 .table td[data-label]::before mobile stacked-label rule — the new cell inherits both)
    - assets/tokens.css (confirm --color-text-muted and --color-warning-text are defined for both the light and [data-theme="dark"] scopes)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md (§1 header markup, §2 overdue CSS rule, §3 sort option)
  </read_first>
  <action>In BOTH index.html and demo.html: (a) add a new sortable header th between the Last Session th and the col-actions th, mirroring the Last Session th exactly but with data-sort-key="nextSession", aria-sort="none", and an inner sort-label span whose label span carries data-i18n="overview.table.nextSession" plus the sort-arrow span — final header order Name, Type, Sessions, Last Session, Next Session, Actions; (b) add an option with value="nextSession" data-i18n="overview.filter.sort.nextSession" to #clientSortSelect immediately after the lastSession option. In assets/app.css add ONE new rule block per UI-SPEC §2: .next-session-cell.is-overdue sets color to var(--color-text-muted); and .next-session-cell .next-overdue-dot sets color to var(--color-warning-text), font-size 0.6em, line-height 1, margin-inline-end 0.35em, vertical-align middle (margin-inline-end so it flips in RTL). First grep tokens.css to confirm both tokens exist in light and dark; if either is missing, stop and report rather than inventing a token.</action>
  <verify>
    <automated>grep -c 'data-sort-key="nextSession"' index.html demo.html && grep -c 'value="nextSession"' index.html demo.html && grep -q 'next-overdue-dot' assets/app.css && grep -q 'next-session-cell' assets/app.css && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - index.html and demo.html each contain exactly one th with data-sort-key="nextSession" (data-i18n="overview.table.nextSession"), positioned between Last Session and Actions
    - index.html and demo.html each contain an option value="nextSession" (data-i18n="overview.filter.sort.nextSession") after the lastSession option in #clientSortSelect
    - assets/app.css contains the .next-session-cell.is-overdue and .next-overdue-dot rules using var(--color-text-muted) and var(--color-warning-text); both tokens confirmed present in light and dark scopes of tokens.css
    - No new design token is invented; only the two existing tokens are referenced
  </acceptance_criteria>
  <done>The Next Session column header + sort option exist in both HTML files and the single overdue CSS rule is added, reusing shipped tokens. (D-02, D-04)</done>
</task>

<task type="auto">
  <name>Task 2: Render the Next Session cell with the overdue cue + bump colSpan + add mostRecentSession helper (overview.js)</name>
  <files>assets/overview.js</files>
  <read_first>
    - assets/overview.js (:619-626 render most-recent tiebreak; :630 lastSession value + dash-empty pattern; :667-669 lastSessionCell build; :692-697 row.append order ending lastSessionCell then actionCell; :702 detailCell.colSpan = 5; :704-709 textContent-only render note locked by 31-overview-render-hardening.test.js)
    - assets/date-format.js (window.DateFormat.parseLocal + todayLocalISO — for the overdue comparison and today-local Date)
    - tests/31-overview-render-hardening.test.js (textContent-only lock — no innerHTML)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Cell render + one-new-CSS examples; Pitfall 5 colSpan)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md (§2 overdue cue treatment)
  </read_first>
  <action>Add a module-level mostRecentSession(sessions) helper that returns the single most-recent session by the SAME tiebreak used in the render sort at :619-626 (date desc, then createdAt desc, then id desc) — this is reused by the sort branch in Task 3 (in the render, clientSessions[0] is already most-recent post-sort, so the cell reads clientSessions[0]). Build a nextSessionCell (a td) with className "next-session-cell" and data-label = App.t("overview.table.nextSession"). Read nextRaw = clientSessions[0]?.nextSessionDate or "". If empty, set textContent to the literal dash exactly like Last Session (:630). Otherwise: compute todayLocalDate = window.DateFormat.parseLocal(window.DateFormat.todayLocalISO()); if window.DateFormat.parseLocal(nextRaw) is strictly less than todayLocalDate, add the "is-overdue" class and prepend a span.next-overdue-dot whose textContent is the filled-dot glyph and whose title and aria-label are App.t("overview.table.nextSession.overdue"); then append a text node of App.formatDate(nextRaw). Build everything via createElement/createTextNode/textContent — never innerHTML (locked by 31-overview-render-hardening.test.js). Insert row.appendChild(nextSessionCell) between the lastSessionCell append (:696) and the actionCell append (:697). Change detailCell.colSpan from 5 to 6 (:702) since the table now has 6 columns.</action>
  <verify>
    <automated>node tests/31-overview-render-hardening.test.js && node tests/37-overview-sort.test.js 2>&1 | tail -5; grep -n 'colSpan = 6' assets/overview.js && grep -q 'next-overdue-dot' assets/overview.js && grep -q 'mostRecentSession' assets/overview.js && echo RENDER_OK</automated>
  </verify>
  <acceptance_criteria>
    - overview.js defines mostRecentSession(sessions) using the date/createdAt/id tiebreak from :619-626
    - The nextSessionCell renders App.formatDate(clientSessions[0].nextSessionDate), or the literal dash when empty, via textContent/createTextNode (no innerHTML) and is inserted between lastSessionCell and actionCell
    - The overdue path adds .is-overdue + a .next-overdue-dot with title/aria-label = App.t("overview.table.nextSession.overdue"), only when parseLocal(value) < today-local (today NOT overdue)
    - detailCell.colSpan is 6 (grep confirms `colSpan = 6`)
    - tests/31-overview-render-hardening.test.js still passes (textContent-only invariant intact)
  </acceptance_criteria>
  <done>The Next Session cell renders the most-recent value with the accessible overdue cue and the detail-row spans the full 6 columns. (D-01, D-04, NEXT-03, NEXT-05)</done>
</task>

<task type="auto">
  <name>Task 3: Add the nextSession sort branch (ascending default, blanks-to-bottom both directions) (overview.js)</name>
  <files>assets/overview.js</files>
  <read_first>
    - assets/overview.js (:260 SORT_DEFAULT_DIR object; :297-319 the filtered.sort comparator with the lastSession/sessions/name branches and `return dir * base` at :318; :442-518 header↔#clientSortSelect two-way sync, incl. :490 hasOwnProperty(key) guard and :495 SORT_DEFAULT_DIR[key])
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Pattern 1 most-recent derivation ≠ reduce-max; Pattern 2 blanks escape dir*base via early-return; Anti-Patterns list)
    - tests/37-overview-sort.test.js (Plan 38-02 — the sort contract this must satisfy)
  </read_first>
  <action>Add nextSession: "ascending" to the SORT_DEFAULT_DIR object at :260 (soonest-due first — opposite of lastSession's descending). Add a nextSession branch to the filtered.sort comparator, placed as the FIRST if-branch (before lastSession) so its blank early-returns escape the shared `return dir * base` at :318. In the branch compute aNext = mostRecentSession(_sessionsByClient.get(a.id))?.nextSessionDate or "" and bNext likewise (use the Task 2 helper — do NOT copy lastSession's reduce-max, which returns the max across sessions and contradicts D-01; and do NOT read clientSessions[0] here because at sort time the per-client arrays are raw IDB order, not yet sorted). Then: if both blank return 0; if aNext blank return 1; if bNext blank return -1 (these early-returns push blanks to the bottom in BOTH directions); otherwise set base = aNext.localeCompare(bNext) and let it fall through to `return dir * base`. No new sync code is needed — the :442-518 two-way sync handles any key via SORT_DEFAULT_DIR.hasOwnProperty; confirm the new option is picked up.</action>
  <verify>
    <automated>node tests/37-overview-sort.test.js && grep -q 'nextSession: "ascending"' assets/overview.js && echo SORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/37-overview-sort.test.js` passes (was RED in Plan 38-02): allSortKeys includes nextSession, ascending default, blanks-to-bottom under both directions, most-recent derivation, header↔select sync
    - SORT_DEFAULT_DIR contains `nextSession: "ascending"`
    - The nextSession branch uses mostRecentSession() (not a reduce-max and not clientSessions[0]) and early-returns +1/-1 for blank cases so they bypass `dir * base`
    - No new two-way-sync code was added (the existing sync covers the new key)
  </acceptance_criteria>
  <done>The Next Session column sorts ascending by default with blanks pinned to the bottom in both directions, matching the displayed most-recent value. (D-01, D-03, NEXT-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session record → overview DOM | Stored date values are rendered into table cells and a marker |
| stored date → overdue comparison | The overdue decision is wall-clock-local and must use the local engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Tampering | nextSessionCell render | high | mitigate | Cell + overdue dot built via textContent/createTextNode/createElement, never innerHTML (locked by 31-overview-render-hardening.test.js); value formatted via App.formatDate |
| T-38-07 | Information Disclosure | overdue cue (a11y) | low | mitigate | Dot is not color-only — dimmed text is a second channel and the marker carries title + aria-label (overview.table.nextSession.overdue), WCAG 1.4.1 |
| T-38-08 | Repudiation | nextSession sort branch | medium | mitigate | Sort derives most-recent via mostRecentSession() so it matches the displayed value (no reduce-max drift); guarded by tests/37-overview-sort.test.js |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node tests/37-overview-sort.test.js` GREEN (key, ascending, blanks-both-directions, most-recent, sync)
- `node tests/31-overview-render-hardening.test.js` GREEN (textContent-only intact)
- Manual (phase gate, /gsd-verify-work): headless-Chrome screenshot of the overdue cell in LTR, Hebrew RTL, and dark theme — dimmed text + amber dot, marker flips via margin-inline-end (per MEMORY reference-headless-chrome-ui-verification)
</verification>

<success_criteria>
The sortable Next Session column renders the most-recent value with an accessible overdue cue and correct blanks-to-bottom sort — NEXT-03, NEXT-04, NEXT-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-05-SUMMARY.md` when done.
</output>
