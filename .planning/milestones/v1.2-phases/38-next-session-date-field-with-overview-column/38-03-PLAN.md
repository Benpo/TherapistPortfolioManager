---
phase: 38-next-session-date-field-with-overview-column
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [NEXT-01, NEXT-03, NEXT-04, NEXT-05]
must_haves:
  truths:
    - "All four new i18n keys exist in EN, HE, DE and CS with the exact values from the UI-SPEC copywriting contract: overview.table.nextSession, overview.filter.sort.nextSession, session.form.nextSessionDate, overview.table.nextSession.overdue (D-02 column header, D-04 overdue label, D-07 form field label)"
    - "Key placement mirrors the existing families: overview.table.nextSession right after overview.table.lastSession, overview.filter.sort.nextSession after overview.filter.sort.lastSession, session.form.nextSessionDate after session.form.nextSession (D-02, D-07)"
    - "Hebrew values are RTL-correct; the overdue key carries a real translated word so the marker's aria-label/title is not color-only (D-04, WCAG 1.4.1)"
    - "Key parity holds across all 4 language files (no key present in one file and missing in another)"
  artifacts:
    - "assets/i18n-en.js (4 keys added)"
    - "assets/i18n-he.js (4 keys added)"
    - "assets/i18n-de.js (4 keys added)"
    - "assets/i18n-cs.js (4 keys added)"
  key_links:
    - "overview.table.nextSession → data-label + <th> for the new column (Plan 38-05)"
    - "overview.filter.sort.nextSession → #clientSortSelect <option> (Plan 38-05)"
    - "session.form.nextSessionDate → #nextSessionDate <label> (Plan 38-04)"
    - "overview.table.nextSession.overdue → aria-label/title on the overdue dot (Plan 38-05)"
---

<objective>
Add the four new i18n keys across all four languages so the form field label, overview column header, sort-select option, and overdue accessible label all resolve. Foundation for the Wave 2 form + overview plans.

Purpose: The keys are static config that the display surfaces reference by exact name. Adding them first (their own wave) lets the form (38-04) and overview (38-05) plans render real labels without a cross-file dependency, and keeps the 4-language parity invariant intact.
Output: 4 keys × 4 languages, placed in their existing families.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- i18n keys: `overview.table.nextSession`, `overview.filter.sort.nextSession`, `session.form.nextSessionDate`, `overview.table.nextSession.overdue`
- (other phase-38 symbols: `nextSessionDate` field, `#nextSessionDate`, `mostRecentSession`, `syncNextSessionMin`, `nextSession` sort key, `.next-session-cell`/`.is-overdue`/`.next-overdue-dot`, new `<th>`/`<option>`, tests/38-*.test.js)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the four next-session i18n keys to all four language files</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (:22 overview.table.lastSession, :50 overview.filter.sort.lastSession, :135 session.form.nextSession, :136 session.form.nextSession.placeholder — the exact anchor lines + family style)
    - assets/i18n-he.js (the RTL Hebrew counterparts of the same three anchor keys)
    - assets/i18n-de.js (the German counterparts)
    - assets/i18n-cs.js (the Czech counterparts)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md (§Copywriting Contract table — the exact EN/HE/DE/CS values to use)
  </read_first>
  <action>Add these four keys to each of assets/i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js, placed immediately after the matching existing family member in each file (overview.table.nextSession after overview.table.lastSession; overview.filter.sort.nextSession after overview.filter.sort.lastSession; session.form.nextSessionDate after session.form.nextSession; overview.table.nextSession.overdue grouped with the overview.table.* family). Use exactly these values from the UI-SPEC: overview.table.nextSession = EN "Next Session" / HE "מפגש הבא" / DE "Nächste Sitzung" / CS "Příští sezení"; overview.filter.sort.nextSession = same four values as overview.table.nextSession; session.form.nextSessionDate = EN "Next session date" / HE "תאריך המפגש הבא" / DE "Datum der nächsten Sitzung" / CS "Datum příštího sezení"; overview.table.nextSession.overdue = EN "Overdue" / HE "באיחור" / DE "Überfällig" / CS "Po termínu". Match each file's existing quoting/trailing-comma style. Do NOT add a placeholder/optional hint for the date field (per MEMORY feedback-optional-fields-no-hints).</action>
  <verify>
    <automated>for k in overview.table.nextSession overview.filter.sort.nextSession session.form.nextSessionDate overview.table.nextSession.overdue; do for f in en he de cs; do grep -q "\"$k\"" assets/i18n-$f.js || { echo "MISSING $k in $f"; exit 1; }; done; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four keys present in all four files (16 additions); the verify loop prints OK
    - EN values are exactly: "Next Session", "Next Session", "Next session date", "Overdue"
    - HE values are the RTL Hebrew strings from the UI-SPEC; overdue is a real word ("באיחור"), not empty
    - No new placeholder/"(optional)" hint key added for the date field
    - Each file still parses (its module still loads under node without syntax error)
  </acceptance_criteria>
  <done>The four new keys resolve in all four languages with the UI-SPEC values, placed in their existing families. (D-02, D-04, D-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n value → rendered DOM | Key values are rendered downstream; they must be plain strings surfaced via textContent/data-i18n, not markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-05 | Tampering | assets/i18n-*.js | low | mitigate | Values are literal strings only (no HTML); consumers render via data-i18n/textContent (Plans 38-04/05), never innerHTML |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- The verify loop confirms all 4 keys in all 4 files
- Each i18n file loads under node without a syntax error
</verification>

<success_criteria>
Four next-session i18n keys exist across EN/HE/DE/CS with the exact UI-SPEC values and correct family placement.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-03-SUMMARY.md` when done.
</output>
