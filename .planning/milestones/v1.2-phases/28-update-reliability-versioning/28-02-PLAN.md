---
phase: 28-update-reliability-versioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - _headers
autonomous: true
requirements: [VER-04, VER-05, VER-06]
must_haves:
  truths:
    - "CSP is delivered via an HTTP Content-Security-Policy header in _headers, with content byte-for-byte equivalent to the per-page <meta> CSP (no silent weakening of script-src/style-src/connect-src/default-src/img-src/font-src; unsafe-inline kept verbatim)"
    - "Static JS/CSS in _headers is served with max-age=86400 (raised from 3600); the service worker still owns freshness for installed users"
    - "HTML and sw.js remain no-cache; landing.html's same-origin demo iframe and the existing X-Frame-Options: SAMEORIGIN keep working under the new header"
  artifacts:
    - path: "_headers"
      provides: "HTTP CSP header (global), raised JS/CSS cache TTL, preserved no-cache for HTML/sw.js"
      contains: "Content-Security-Policy"
  key_links:
    - from: "_headers"
      to: "the deleted per-page <meta> CSP (Plan 03)"
      via: "the header content must be verified equivalent BEFORE Plan 03 deletes the meta tags"
      pattern: "Content-Security-Policy:"
  prohibitions:
    - "Do NOT weaken any CSP directive — script-src/style-src/connect-src/default-src/img-src/font-src must match the <meta> content exactly, including 'unsafe-inline' (removing unsafe-inline is OUT OF SCOPE for v1.2)"
    - "Do NOT add any connect-src origin beyond the existing https://api.lemonsqueezy.com (VER-06 — no new network surface)"
    - "Do NOT change HTML or sw.js from no-cache; the SW owns freshness (VER-05)"
---

<objective>
Migrate CSP from the 21 per-page `<meta http-equiv="Content-Security-Policy">` tags to a single
HTTP `Content-Security-Policy` header in `_headers` (VER-04), with content byte-for-byte equivalent
to the existing meta (verified identical across all 21 pages — `unsafe-inline` kept verbatim, removal
out of scope). Raise the static JS/CSS cache TTL from `max-age=3600` to `max-age=86400` (VER-05),
keeping HTML and sw.js `no-cache` so the service worker still owns freshness for installed users.
Reconcile `landing.html`'s same-origin demo iframe with the existing `X-Frame-Options: SAMEORIGIN`.

Purpose: One CSP source instead of 21 (kills drift), a stronger delivery mechanism (headers protect
navigations that `<meta>` cannot), and a sane CDN cache TTL. The `<meta>` deletion happens in Plan 03
ONLY after this header is verified equivalent — this plan establishes the header that Plan 03 depends on.
Output: modified `_headers` (header CSP added, JS/CSS TTL raised, HTML/sw.js untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-update-reliability-versioning/28-CONTEXT.md
@.planning/phases/28-update-reliability-versioning/28-PATTERNS.md
@.planning/codebase/CONCERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols created in THIS plan (downstream drift checks must EXCLUDE these):

- The `Content-Security-Policy:` header line in `_headers` (NEW header entry — `_headers` had no CSP before)
- Possibly a `/landing.html` per-path block in `_headers` IF the demo-iframe reconciliation requires one

The per-page `<meta>` CSP deletion is owned by Plan 03 (Wave 2), gated on this header being verified equivalent.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the HTTP CSP header to _headers (verbatim-equivalent to the meta CSP)</name>
  <files>_headers</files>
  <read_first>
    - _headers (the whole file — the global `/*` block at lines 1-5, the `/*.html`, `/*.js`, `/*.css`, `/sw.js` per-path blocks)
    - index.html (line 23 — the exact `<meta http-equiv="Content-Security-Policy" content="...">` value to copy verbatim)
    - .planning/phases/28-update-reliability-versioning/28-PATTERNS.md (the "_headers (MODIFIED)" section, points 1-2 — the verbatim CSP string and the X-Frame nuance)
    - .planning/codebase/CONCERNS.md (the "CSP Delivered as <meta> Tag" entry)
  </read_first>
  <action>
    Add a `Content-Security-Policy:` header to the global `/*` block in `_headers` whose value is the meta CSP content copied VERBATIM from index.html:23 — confirmed byte-identical across all 21 pages: `default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' https://api.lemonsqueezy.com; font-src 'self'`. Keep `'unsafe-inline'` exactly as-is in both script-src and style-src (removal is OUT OF SCOPE for v1.2 — D-Discretion). Do not add, remove, or reorder any directive or source. Leave the existing `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` lines unchanged. Note: the CSP has no `frame-src`/`frame-ancestors`; `default-src 'self'` already governs the same-origin `./demo.html` iframe on landing.html, and `X-Frame-Options: SAMEORIGIN` already permits same-origin framing — so the existing landing demo iframe keeps working with NO page-specific block needed. Verify this reasoning against the landing iframe (src="./demo.html", same origin) and document it; only add a `/landing.html` per-path block if a directive would actually break same-origin framing (it does not, per this analysis).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('_headers','utf8');const idx=fs.readFileSync('index.html','utf8');const m=idx.match(/content=\"([^\"]*)\"\s*>/);const meta=m&&m[1].trim();const cspLine=(h.match(/Content-Security-Policy:\s*(.+)/)||[])[1];if(!cspLine)throw new Error('no CSP header in _headers');const norm=s=>s.replace(/\s+/g,' ').trim();if(norm(cspLine)!==norm(meta))throw new Error('CSP header != meta content\\nheader: '+norm(cspLine)+'\\nmeta:   '+norm(meta));if(!/'unsafe-inline'/.test(cspLine))throw new Error('unsafe-inline must be kept verbatim');console.log('OK CSP header matches meta verbatim')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_headers` contains a `Content-Security-Policy:` line under the global `/*` block
    - Behavior: the header CSP value (whitespace-normalized) is byte-equivalent to the `<meta>` content in index.html:23 — verified by the automated diff above
    - Source: `'unsafe-inline'` appears in BOTH script-src and style-src of the header (kept verbatim, NOT removed)
    - Source: connect-src lists exactly `'self' https://api.lemonsqueezy.com` — no new origin added (VER-06)
    - Source: the existing `X-Frame-Options: SAMEORIGIN` line is unchanged; landing's same-origin demo iframe reasoning documented (no page-specific block needed)
  </acceptance_criteria>
  <done>_headers serves a CSP HTTP header byte-equivalent to the per-page meta (unsafe-inline kept), with the landing demo-iframe / X-Frame-Options interaction verified and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Raise static JS/CSS cache TTL to 86400; keep HTML and sw.js no-cache</name>
  <files>_headers</files>
  <read_first>
    - _headers (the `/*.js`, `/*.css` blocks currently at `max-age=3600`, and the `/*.html` + `/sw.js` no-cache blocks)
    - .planning/codebase/CONCERNS.md (the "_headers Cache TTL for JS/CSS Is Only 1 Hour" entry)
    - .planning/phases/28-update-reliability-versioning/28-PATTERNS.md (the "_headers (MODIFIED)" section, point 3)
  </read_first>
  <action>
    In `_headers`, change the `Cache-Control` for both `/*.js` and `/*.css` from `public, max-age=3600` to `public, max-age=86400` (VER-05). Leave `/*.html` and `/sw.js` at `no-cache` UNCHANGED — the service worker owns freshness for installed users, and HTML must revalidate so the SW-registration script and CSP-header-bearing navigations are never stale. Do not change any other directive.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('_headers','utf8');const jsBlock=h.split(/\n(?=\/)/).find(b=>b.startsWith('/*.js'));const cssBlock=h.split(/\n(?=\/)/).find(b=>b.startsWith('/*.css'));if(!/max-age=86400/.test(jsBlock||''))throw new Error('/*.js not raised to 86400');if(!/max-age=86400/.test(cssBlock||''))throw new Error('/*.css not raised to 86400');const htmlBlock=h.split(/\n(?=\/)/).find(b=>b.startsWith('/*.html'));const swBlock=h.split(/\n(?=\/)/).find(b=>b.startsWith('/sw.js'));if(!/no-cache/.test(htmlBlock||''))throw new Error('/*.html must stay no-cache');if(!/no-cache/.test(swBlock||''))throw new Error('/sw.js must stay no-cache');console.log('OK TTL raised, HTML+sw.js still no-cache')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `/*.js` block has `Cache-Control: public, max-age=86400`
    - Source: `/*.css` block has `Cache-Control: public, max-age=86400`
    - Source: `/*.html` block remains `no-cache`
    - Source: `/sw.js` block remains `no-cache`
    - No max-age=3600 remains for JS/CSS
  </acceptance_criteria>
  <done>JS/CSS served with max-age=86400; HTML and sw.js stay no-cache so the SW still owns freshness (VER-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/edge → browser | CSP and Cache-Control are delivered by Cloudflare Pages from `_headers`; a weakened CSP or over-long HTML TTL would degrade the security/freshness posture |
| landing.html → demo.html iframe | same-origin framing must keep working under the new global CSP + X-Frame-Options |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering / Elevation | CSP header migration | mitigate | Header value is verified byte-equivalent to the meta CSP by an automated diff before the meta tags are deleted (Plan 03); no directive weakened; unsafe-inline kept verbatim (in scope) |
| T-28-05 | Spoofing / Clickjacking | landing demo iframe | mitigate | default-src 'self' governs the same-origin ./demo.html iframe and X-Frame-Options: SAMEORIGIN is preserved; reasoning documented; no cross-origin framing enabled |
| T-28-06 | Information Disclosure (stale security) | longer JS/CSS TTL | mitigate | Only JS/CSS get max-age=86400; HTML + sw.js stay no-cache, so the SW (which owns freshness) and the CSP-bearing navigations are never served stale to installed users (VER-05) |
| T-28-07 | Information Disclosure | connect-src scope | accept | connect-src unchanged (`'self' https://api.lemonsqueezy.com`); no new network egress (VER-06) |

No high-severity threat is left unmitigated. (Removing `unsafe-inline` would further harden against XSS but is explicitly OUT OF SCOPE for v1.2 per REQUIREMENTS "Out of Scope" and 28-CONTEXT deferred ideas — tracked, not a Phase 28 defect.)
</threat_model>

<verification>
- `_headers` serves a CSP HTTP header byte-equivalent to the per-page meta (Task 1 automated diff passes).
- JS/CSS TTL raised to 86400; HTML + sw.js stay no-cache (Task 2 automated check passes).
- The landing same-origin demo iframe keeps rendering under the new header (manual spot-check on deploy; default-src 'self' + X-Frame-Options SAMEORIGIN cover it).
- This header MUST be verified equivalent before Plan 03 deletes the 21 `<meta>` CSP tags (the depends_on gate).
</verification>

<success_criteria>
- CSP delivered via HTTP header, content verbatim-equivalent to the meta CSP, unsafe-inline kept (VER-04 header side).
- Static JS/CSS cached for 86400s; HTML + sw.js no-cache; SW still owns freshness (VER-05).
- No new network surface; connect-src unchanged (VER-06).
- Landing demo iframe + X-Frame-Options reconciled (no page-specific block needed; documented).
</success_criteria>

<output>
Create `.planning/phases/28-update-reliability-versioning/28-02-SUMMARY.md` when done. Record: the exact CSP header string written, confirmation it is byte-equivalent to the meta, the landing-iframe reasoning, and an explicit "SAFE TO DELETE per-page <meta> CSP in Plan 03" sign-off.
</output>
