---
phase: 32-readme-code-comments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .github/workflows/deploy.yml
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "A maintainer or agent can run, deploy, and ship a change to the app from the README alone (DOCS-01), calibrated for a technically-comfortable owner + his AI agents — not a non-technical first-timer (D-01)"
    - "The README lives in-repo so a cloud agent can read it as project context; it keeps local-serve instructions (localhost is required for the Web Crypto secure-context backup features) and drops the multi-machine-collaboration framing (D-02)"
    - "There is exactly ONE in-repo README, rewriting the existing root README.md (not split, not a separate MAINTAINING.md) (D-03)"
    - "The README is operational-first — center of gravity is run / deploy / make-a-change, not an architecture treatise (D-05) — and carries ~6 verified how-do-I recipes (D-06)"
    - "The README has a 'Rules an agent must not break' section (zero-runtime-dependency production rule; the IIFE script-load-order / cross-window.* resolution chain; the pre-commit service-worker bump gotcha; PRECACHE_URLS upkeep; no external network calls) (D-07)"
    - "The README carries its OWN self-contained current file-map reflecting the post-refactor structure, and POINTS TO .planning/codebase/*.md for deeper architecture rather than duplicating it (D-08)"
    - "The maintainer README is no longer published at the product URL — the deploy step that staged README.md into deploy-staging is removed (D-04); this is the only production-adjacent change in the phase and it reduces information disclosure"
  artifacts:
    - path: README.md
      provides: "Rewritten in-repo maintainer guide (run / deploy / file-map / recipes / agent-rules / troubleshoot)"
      contains: "Rules an agent must not break"
    - path: .github/workflows/deploy.yml
      provides: "Deploy pipeline with the README staging step removed (README repo-only)"
  key_links:
    - from: "README.md recipes"
      to: ".github/workflows/deploy.yml, assets/version.js, sw.js (PRECACHE_URLS), package.json"
      via: "every recipe truth-checked against the live file it documents (no from-memory drift)"
    - from: "README.md architecture section"
      to: ".planning/codebase/*.md"
      via: "pointer reference, not a duplicated copy (D-08)"
---

<objective>
DOCS-01 — Rewrite the root `README.md` in place into a single in-repo maintainer guide for a technically-comfortable owner + his AI agents (D-01), kept in-repo as agent context with local-serve instructions retained (D-02), as ONE README rewriting the existing file (D-03), operational-first (D-05), with ~6 verified how-do-I recipes (D-06), a precise "Rules an agent must not break" section (D-07), and a self-contained current file-map that points to the refreshed `.planning/codebase/*.md` for deeper architecture (D-08). Then stop publishing the README at the product URL by removing the deploy-staging copy step for it (D-04).

Purpose: With cloud Claude Code now a feasible workflow, an in-repo README whose agent-rules section is precise is the single highest-leverage maintenance artifact in this milestone. The old "for Sapir, multi-machine collaboration" framing is superseded — write denser, agent-readable docs.
Output: a rewritten `README.md` and a one-line-lighter `.github/workflows/deploy.yml`. No new files; no new code symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-code-comments/32-CONTEXT.md
@.planning/phases/32-readme-code-comments/32-RESEARCH.md
@.planning/phases/32-readme-code-comments/32-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- `README.md` — **rewrite in place** (not a new file; no new code symbols).
- `.github/workflows/deploy.yml` — edit only (one line removed; no new symbols).
- No new file paths and no new exported identifiers are introduced by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md into the in-repo maintainer guide</name>
  <files>README.md</files>
  <read_first>
    - README.md (the file being rewritten — current public-leaning skeleton; keep the value-prop/privacy framing, replace the stale Project-Structure block)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"Code Examples / Verified Mechanics" recipes 1-6 with the exact verified facts; §"Architecture Patterns" Pattern 1 section order; Pitfalls 2 and 3 — stale file-map + recipe drift)
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the README rewrite-in-place analog + the file-map tree style from STRUCTURE.md)
    - .planning/codebase/STRUCTURE.md (the current asset/file layout — source for the self-contained file-map)
    - .planning/codebase/ARCHITECTURE.md (§Architectural Constraints + §Anti-Patterns — source for the agent-rules section)
    - .planning/codebase/CONVENTIONS.md (the IIFE module pattern, window.* registration, ordered script loading — agent-rules + recipe 6)
    - assets/version.js (the version-bump single source of truth: APP_VERSION constant + INTEGRITY_TOKEN; do NOT hardcode the number, describe the constant)
    - sw.js (PRECACHE_URLS + the CACHE_NAME derivation from INTEGRITY_TOKEN — recipes 3, 4, 6)
    - package.json (the test recipe: scripts.test → node tests/run-all.js)
    - .github/workflows/deploy.yml (the ship-a-change recipe mechanics)
  </read_first>
  <action>
    Rewrite README.md in place. KEEP the existing one-paragraph value prop, the "What It Does" list, and the "Privacy by Design" framing (still accurate, audience-agnostic). REPLACE the stale Project-Structure block — it predates the prior refactor's extractions and omits the three extracted modules (settings-snippets, settings-photos, export-modal).

    Use this operational-first section order (D-05; refine wording within these slots): (1) What it is — one short paragraph keeping the privacy/offline value prop; (2) Run locally; (3) Deploy and ship a change; (4) Current file-map; (5) How do I... (recipes); (6) Rules an agent must not break; (7) Troubleshoot / get help; (8) a short pointer to .planning/codebase/*.md for deeper architecture.

    Write ~6 how-do-I recipes (D-06), copying the EXACT mechanics from RESEARCH §"Code Examples / Verified Mechanics" — never from memory (Pitfall 3): (1) run locally via python3 -m http.server and WHY localhost matters (Web Crypto secure-context for the encrypted-backup features); (2) run the tests via npm test (node tests/run-all.js); (3) ship a change (push main → the GitHub Action stages app-only files and sed-stamps the build token into the STAGING copy of version.js → force-pushes the deploy branch → Cloudflare Pages serves it → purges the CF cache) including the PRECACHE_URLS upkeep habit and the pre-commit service-worker-bump gotcha; (4) bump the app version — describe the APP_VERSION constant in assets/version.js as the single source that drives the footer, the SW CACHE_NAME (via INTEGRITY_TOKEN), and the runtime integrity self-check; describe the CONSTANT, do not hardcode the current number; note INTEGRITY_TOKEN is deploy-stamped, not hand-set; (5) add/edit a translation string — add the key to all four dictionaries (i18n-en/he/de/cs), use data-i18n in HTML or App.t() in JS, and the untranslated-key TODO-i18n marker convention; (6) add a new JS module — IIFE registering on window.*, add a script tag at the correct load-order position in each page that uses it, add its path to PRECACHE_URLS in sw.js, mind the SW-bump follow-up.

    Write the "Rules an agent must not break" section (D-07) sourced from ARCHITECTURE.md §Architectural Constraints + §Anti-Patterns and CONVENTIONS.md: zero-runtime-dependency production (dev/test tooling such as package.json and jsdom is dev-only and never ships); the IIFE script-load-order and cross-window.* identifier-resolution chain (App.*, PortfolioDB.*, BackupManager.*, Snippets.*/SNIPPETS_SEED/I18N); the pre-commit service-worker bump gotcha and PRECACHE_URLS upkeep; no external network calls (the offline/privacy promise); never use innerHTML for user/i18n content (use textContent/.value).

    Build the Current file-map (D-08) FRESH from STRUCTURE.md and the actual assets/ listing so it reflects the post-refactor structure (must list settings-snippets.js, settings-photos.js, export-modal.js). Use the annotated directory-tree style. Do NOT duplicate the codebase maps — point to .planning/codebase/*.md for deeper background.

    Do not include any deploy step that stages the README into the published artifact in your recipes (that step is being removed in Task 2). Do not put secrets, tokens, or private business data into the README — although it becomes repo-only, treat it as readable by anyone with repo access.
  </action>
  <verify>
    <automated>test -f README.md && grep -qiE '^#+.*[Rr]un locally' README.md && grep -qiE '^#+.*[Dd]eploy' README.md && grep -qiE 'file.?map' README.md && grep -qiE '^#+.*[Rr]ecipe|^#+.*[Hh]ow do I' README.md && grep -q 'Rules an agent must not break' README.md && grep -qiE '^#+.*[Tt]roubleshoot' README.md && grep -q '.planning/codebase' README.md && grep -q 'npm test' README.md && grep -q 'python3 -m http.server' README.md && grep -q 'APP_VERSION' README.md && grep -q 'PRECACHE_URLS' README.md && grep -qi 'settings-snippets.js' README.md && grep -qi 'settings-photos.js' README.md && grep -qi 'export-modal.js' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists and is a rewrite-in-place (still the root README, not a new file).
    - README.md contains the literal heading text `Rules an agent must not break`.
    - README.md contains a "Run locally" section, a "Deploy"/ship section, a current file-map, a recipes/"How do I" section, and a "Troubleshoot" section (heading greps pass).
    - The file-map names all three extracted modules: `settings-snippets.js`, `settings-photos.js`, `export-modal.js` (grep passes) — proving it is not the stale pre-refactor block (Pitfall 2).
    - The version recipe references the `APP_VERSION` constant (grep passes) and does NOT hardcode a version number as the source of truth (manual: describes the constant, per Pitfall 3).
    - README.md references `.planning/codebase` (pointer, not a duplicated copy — D-08).
    - The recipes reference live mechanics: `npm test`, `python3 -m http.server`, `PRECACHE_URLS` (greps pass).
    - Manual: each of the 6 recipes matches the live file it documents (cross-check against RESEARCH §Verified Mechanics).
  </acceptance_criteria>
  <done>README.md is the operational, agent-readable in-repo maintainer guide with all required sections, ~6 verified recipes, the agent-rules section, and a fresh post-refactor file-map pointing to the codebase maps.</done>
</task>

<task type="auto">
  <name>Task 2: Stop publishing the README at the product URL</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the Prepare-deploy-directory step; the README staging line is currently line 28; the "Verify no sensitive files" step stays intact)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§Runtime State Inventory "Live service config" row + §Security Domain — confirms this only stops publishing a file, changes no service config, and reduces information disclosure)
  </read_first>
  <action>
    In .github/workflows/deploy.yml, delete the single line in the "Prepare deploy directory" step that copies README.md into the deploy-staging directory (currently line 28). Leave every other staging line and the "Verify no sensitive files" step unchanged. This makes the maintainer README repo-only (D-04) — the only production-adjacent change in the phase, and it ships strictly less.
  </action>
  <verify>
    <automated>! grep -q 'cp README.md' .github/workflows/deploy.yml && grep -q 'cp _headers' .github/workflows/deploy.yml && grep -q 'sed -i' .github/workflows/deploy.yml && grep -q 'Verify no sensitive files' .github/workflows/deploy.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` no longer contains `cp README.md` (negative grep returns empty / exit non-zero).
    - The rest of the staging step is intact: `cp _headers` still present, the version.js `sed` token-stamp still present, and the "Verify no sensitive files" step still present (greps pass).
    - No other line in the workflow changed (manual: diff shows exactly one deleted line).
  </acceptance_criteria>
  <done>The deploy pipeline no longer stages the README into the published artifact; the README is repo-only and the rest of the pipeline is byte-for-byte unchanged except the one removed line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published deploy artifact (Cloudflare Pages) | Files staged by the GitHub Action become publicly served at the product URL. This phase REMOVES the README from that set. |

No new runtime code, network calls, or data flows are introduced. This is documentation plus one CI line removal.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Information Disclosure | README published at product URL / deploy.yml staging step | medium | mitigate | D-04 removes the README staging line so maintainer/deploy detail is no longer served at the product URL (reduces surface). Additionally: do not place secrets, tokens, or private business data in the now-repo-only README. |
| T-32-02 | Tampering (downstream) | "Rules an agent must not break" + file-map accuracy | low | mitigate | Source agent-rules + file-map from the freshly-refreshed .planning/codebase/* maps and verify all recipes against live files, so a future agent does not violate an invariant (no fetch, no innerHTML for i18n, zero-runtime-deps). Wrong docs are the only real risk here. |

Honest framing: this plan adds no attack surface. The single production-adjacent change (D-04) is a removal that reduces information disclosure (ASVS V14, positive). No V5/V6/V7 controls are touched.
</threat_model>

<verification>
- `README.md` heading + content greps pass (Task 1 automated check) — all required sections present, file-map is post-refactor, recipes reference live mechanics.
- `.github/workflows/deploy.yml` no longer contains `cp README.md`; the rest of the pipeline intact (Task 2 automated check).
- Manual recipe accuracy review against RESEARCH §Verified Mechanics.
</verification>

<success_criteria>
A maintainer or AI agent can run, deploy, and ship a change to the app using only README.md; the README is in-repo and no longer published at the product URL; every recipe matches the live files; the agent-rules section and post-refactor file-map are present and accurate.
</success_criteria>

<output>
Create `.planning/phases/32-readme-code-comments/32-01-SUMMARY.md` when done.
</output>
