---
phase: 30-test-harness-coverage
plan: 05
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-export-markdown.test.js
  - tests/30-export-stepper.test.js
  - tests/30-section-visibility.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The filtered export markdown (observed via #exportEditor.value after step-1→Next) has the correct section order, inclusion, scale labels, and headings; the full-copy builder is observed via a navigator.clipboard.writeText spy — captured by an EXECUTING test, replacing the source-slicing anti-pattern (D-08, D-09, TEST-03c, F-C)"
    - "The export-modal stepper drives active-step class 1→2→3; toggling sections in step 1 then advancing to step 2 produces a filtered #exportEditor.value reflecting the selected set (TEST-03d, F-I)"
    - "A custom section title renders into the live form label and an empty/disabled section is hidden — the settings→add-session cross-module link (F-E, D-13c)"
    - "All three tests execute the real add-session.js under jsdom via the async capture-and-await handler pattern (no vacuous green) and survive an internal rename but fail on an observable-output change (D-08, D-12, F-A)"
  artifacts:
    - path: tests/30-export-markdown.test.js
      provides: "Executing markdown-builder characterization via #exportEditor.value (filtered builder) + a clipboard spy (full builder)"
    - path: tests/30-export-stepper.test.js
      provides: "Executing export-modal stepper state-machine + filtered-markdown characterization"
    - path: tests/30-section-visibility.test.js
      provides: "Thin executing characterization of applySectionVisibility (empty→hidden) + applySectionLabels (custom title→label) — the settings→add-session cross-module link"
  key_links:
    - from: tests/30-export-markdown.test.js
      to: assets/add-session.js
      via: "loads add-session.html + add-session.js, drives step-1→Next and reads #exportEditor.value (filtered builder); drives copySessionBtn with a clipboard spy (full builder)"
      pattern: "exportEditor"
    - from: tests/30-export-stepper.test.js
      to: tests/_helpers/mock-navigator-share.js
      via: "injects createShareMock as navigator.canShare/share, asserts files-only share dispatch"
      pattern: "createShareMock"
    - from: tests/30-section-visibility.test.js
      to: assets/add-session.js
      via: "drives applySectionVisibility/applySectionLabels via App.isSectionEnabled/getSectionLabel + the app:settings-changed event, asserts label textContent and is-hidden"
      pattern: "applySectionLabels"
---

<objective>
Characterize the highest-risk, lowest-covered region of `assets/add-session.js` — the export modal (Phase 31 RFCT-02's exact extraction target) — before the refactor, via EXECUTING jsdom real-page tests (D-09) that replace the existing source-slicing "tests" (the D-08/D-12 false-confidence anti-pattern):
1. `tests/30-export-markdown.test.js` — the markdown builders (the copy/export output a therapist sees): correct section order (insights after trapped, per the 260615 fix), included/excluded sections, scale labels, headings (TEST-03c). The FILTERED builder (`buildFilteredSessionMarkdown`) is observed via `#exportEditor.value` after step-1→Next (add-session.js:1500); the FULL builder (`buildSessionMarkdown`) is observed via a `navigator.clipboard.writeText` spy on the copy path (F-C).
2. `tests/30-export-stepper.test.js` — the 3-step export stepper state machine (active-step 1→2→3); toggling sections in step 1 then advancing produces a filtered `#exportEditor.value`; plus the files-only share dispatch (TEST-03d, F-I).
3. `tests/30-section-visibility.test.js` — a THIN guard for the settings→add-session cross-module link: a custom section title renders into the live form label (`applySectionLabels`) and an empty/disabled section is hidden (`applySectionVisibility`) (F-E, D-13c). This mechanism sits in the markdown band Phase 31 will extract and is NOT covered by the step-1-checkbox filter test.

All assert OBSERVABLE output only (D-08): `#exportEditor.value`, the clipboard-spy arg, the active-step class, the label textContent, the `is-hidden` class, the share-mock calls — never internal function names.

Decision (resolves RESEARCH Open Question #1, for the D-12 architect pass): this phase changes NO production code, so the markdown-builder output is observed through the REAL editor value / clipboard / share path rather than by widening `window.__addSessionTestHooks`. `add-session.js`/`add-session.html` are loaded read-only.

Purpose: Establish the pre-refactor green baseline for the add-session export modal — the riskiest Phase 31 extraction (RFCT-02) — guarded by tests that execute the module and survive its internal rearrangement.
Output: `tests/30-export-markdown.test.js`, `tests/30-export-stepper.test.js`, `tests/30-section-visibility.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export markdown characterization — EXECUTING, not sliced (TEST-03c, D-08/D-09)</name>
  <files>tests/30-export-markdown.test.js</files>
  <read_first>
    - assets/add-session.js (markdown builders, lines 730-1180 — `buildSessionMarkdown`, `buildFilteredSessionMarkdown`, `getCurrentSessionDataForExport`; the step-1→Next path at :1493-1504 calls `buildFilteredSessionMarkdown(selected)` and assigns the result to `#exportEditor.value`; the full-copy path at :1582-1587 (`copySessionBtn` → `buildSessionMarkdown` → `copyTextToClipboard`); `copyTextToClipboard` at :738-762; read-only, do NOT modify)
    - add-session.html (the session form fragment + the export dialog container the handlers bind to, incl. the data-section-key elements, the `#exportStep1Rows` checkboxes, `#exportEditor`, and `#exportNextBtn`)
    - tests/25-06-crop-only.test.js (THE async analog — `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE; the `docListeners` capture-and-await pattern for the async `DOMContentLoaded` handler; the end-of-file `process.exit`)
    - tests/_helpers/app-stub.js (inject as `win.App`; the module calls App.t/getSectionLabel/showToast/etc.)
    - tests/_helpers/jsdom-pdf-env.js (use only if add-session.js references the jsPDF global at eval time; otherwise omit for the lighter markdown test)
    - tests/quick-260615-export-section-order.test.js (the ANTI-PATTERN being replaced — it `fs.readFileSync`+regex-matches function bodies; D-08 violation)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-export-markdown.test.js + §Anti-Patterns to Avoid)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Markdown builders row; §D-09 verdict for add-session.js)
  </read_first>
  <action>
    Build a jsdom real-page test that EXECUTES the export markdown path (NOT source-slicing), using the ASYNC runner from `tests/25-06-crop-only.test.js`. The page handler is `document.addEventListener("DOMContentLoaded", async () => { await App.initCommon(); ... })` — a synchronous `dispatchEvent` returns before the async continuation runs and yields a vacuous green (F-A). So: BEFORE evaling add-session.js, override `win.document.addEventListener` to record listeners into a `docListeners` map; after eval, `await` the captured `DOMContentLoaded` handler; after any async-driven event, `await new Promise(r => setTimeout(r, 0))`. Wrap every case in `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE.
    Setup: load the real `add-session.html` body, inject `win.App = createAppStub()` and a no-op `BroadcastChannel` stub, eval `assets/add-session.js`, run the captured handler, and populate the session form DOM with a known dataset (client, session fields, a couple of included sections and an excluded one).
    Observe the emitted markdown through OBSERVABLE output, with the two builders anchored explicitly (F-C — do NOT read `#exportPreview.textContent`, which `exportUpdatePreview` fills with `MdRender.render(...)` HTML that consumes the markdown markers and only degrades to `editor.value` in the fragile MdRender-absent else-branch):
    (a) FILTERED builder (`buildFilteredSessionMarkdown`): tick/untick the `#exportStep1Rows` section checkboxes, click `#exportNextBtn` (the step-1→Next path at :1499-1500 assigns the built markdown to `#exportEditor.value`), then assert on `#exportEditor.value` — the section ORDER mirrors the form DOM order (notably `insights` appears AFTER `trapped`, per the 260615 fix), excluded sections are absent and included present, scale labels render, headings correct.
    (b) FULL builder (`buildSessionMarkdown`): this is reachable ONLY via `copySessionBtn` → `copyTextToClipboard`, and under jsdom `window.isSecureContext` is false so it falls to the `execCommand("copy")` jsdom NO-OP (nothing observable). So make the secure path observable: `Object.defineProperty(win, 'isSecureContext', { value: true })` and install a capturing `win.navigator.clipboard = { writeText: (s) => { captured = s; return Promise.resolve(); } }` spy, click `copySessionBtn`, `await` the flush, and assert the captured argument is the full markdown (correct order/inclusion/labels/headings). Add this as a SEPARATE explicit assertion — do not let "preview/copy output" hand-wave the two different observability costs into one.
    CRITICAL (D-08/D-12): the test must jsdom-LOAD and EXECUTE add-session.js — it must NOT `fs.readFileSync` the asset and regex-match function bodies, and must NOT assert on function names or `indexOf('function ')`. Do NOT widen `window.__addSessionTestHooks` or otherwise modify add-session.js.
    End-of-file guard (F-A): after the IIFE resolves, assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` (literal count = number of `test(...)` cases) so a vacuous run cannot exit green; then `process.exit(failed === 0 ? 0 : 1)`.
    Open with the root-cause / falsifiability doc block. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-export-markdown.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` exits 0 against the current `assets/add-session.js`.
    - The FILTERED builder is asserted via `#exportEditor.value` after driving step-1 checkboxes → `#exportNextBtn` (NOT `#exportPreview.textContent`); it has the correct section order (insights after trapped), correct inclusion/exclusion, scale labels, and headings (F-C).
    - The FULL builder is asserted via a `navigator.clipboard.writeText` spy with `window.isSecureContext` forced true, capturing the `copySessionBtn` payload — a SEPARATE explicit assertion, not folded into the filtered case (F-C).
    - The test jsdom-LOADS and EXECUTES add-session.js — it performs no source-slicing and has no `indexOf('function')`/regex-on-source assertions (falsifiable: a reviewer can confirm the test never `fs.readFileSync`s the asset for assertion).
    - The async handler is captured-and-awaited (no blanket `dispatchEvent`); the end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard fails the run (non-zero) if the async assertions never executed (F-A).
    - The test FAILS if the emitted markdown changes (e.g. section order reverts) but SURVIVES an internal rename of a builder function (D-08/D-12).
    - No `assets/*` file modified; `window.__addSessionTestHooks` not widened.
  </acceptance_criteria>
  <done>The export markdown output (filtered builder via #exportEditor.value, full builder via clipboard spy) is pinned by an executing observable-behavior test that replaces the source-slicing anti-pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Export-modal stepper state machine + preview (TEST-03d)</name>
  <files>tests/30-export-stepper.test.js</files>
  <read_first>
    - assets/add-session.js (export modal stepper, lines 1180-1835 — `openExportDialog`, `exportSetActiveStep`; the step-1→Next `onNext` at :1493-1504 collects `#exportStep1Rows` checkboxes, builds `buildFilteredSessionMarkdown(selected)`, assigns `#exportEditor.value`, advances to step 2; `exportApplyMobileTabs`, `exportHandleDownloadPdf`/`exportHandleDownloadMd`/`exportHandleShare`, `exportProbeShareSupport`; read-only, do NOT modify)
    - add-session.html (the export dialog markup — the stepper step elements, `#exportNextBtn`, the `#exportStep1Rows` section checkboxes, `#exportEditor`)
    - tests/25-06-crop-only.test.js (the async capture-and-await analog for the `DOMContentLoaded` handler)
    - tests/_helpers/app-stub.js (inject as `win.App`)
    - tests/_helpers/jsdom-pdf-env.js (the PDF download path needs the jsPDF global — load it via the shared helper)
    - tests/_helpers/mock-navigator-share.js (lines 24-30 — `createShareMock({canShareReturns, shareReturns})`; assert on `mock.calls`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-export-stepper.test.js)
  </read_first>
  <action>
    Build a jsdom real-page test for the export-modal stepper state machine, using the ASYNC capture-and-await handler pattern from `tests/25-06-crop-only.test.js` (F-A — capture `DOMContentLoaded` into a `docListeners` map before eval, `await` the handler, `await new Promise(r => setTimeout(r, 0))` after async-driven events; wrap cases in `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE). Load `add-session.html` + `assets/add-session.js`, inject `win.App = createAppStub()`, a no-op `BroadcastChannel`, the jsPDF global (via the shared helper, for the PDF dispatch path), and `createShareMock()` as `navigator.canShare`/`navigator.share`.
    Open the export dialog (`openExportDialog`) and assert the OBSERVABLE stepper state machine: the active-step class transitions 1→2→3 as the user advances through the steps via `#exportNextBtn`. For the filtering behavior (F-I — section toggles are step-1 checkboxes; the markdown is (re)built on Next into step 2, NOT live on toggle): toggle sections in step 1, click Next, and assert `#exportEditor.value` reflects the filtered set (the selected sections present, the deselected absent) — do NOT model it as a live preview-updates-on-toggle. Optionally assert the share dispatch calls the share mock with files-only (per the 260615 share fix) by inspecting `shareMock.calls`.
    End-of-file guard (F-A): assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` (literal count) then `process.exit(failed === 0 ? 0 : 1)`.
    Assert observable active-step class + `#exportEditor.value` + share-mock calls ONLY (D-08) — never internal function names. Open with the falsifiability doc block. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-export-stepper.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-stepper.test.js` exits 0 against the current `assets/add-session.js`.
    - Opening the dialog and advancing via `#exportNextBtn` yields the active-step class transitioning 1→2→3 (observable); toggling sections in step 1 then clicking Next yields a `#exportEditor.value` reflecting the filtered set (F-I — not a live preview-on-toggle); if asserted, the share dispatch passes files-only to the share mock.
    - The async handler is captured-and-awaited; the end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard fails the run if cases were silently skipped (F-A).
    - The test EXECUTES the stepper under jsdom and FAILS if the step transitions or the filtered editor value break, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` file modified; `window.__addSessionTestHooks` not widened.
  </acceptance_criteria>
  <done>The export-modal stepper state machine and the step-1→Next filtered editor value are pinned by an executing observable-behavior test.</done>
</task>

<task type="auto">
  <name>Task 3: Thin section-visibility + custom-label cross-module guard (TEST-03, F-E/D-13c)</name>
  <files>tests/30-section-visibility.test.js</files>
  <read_first>
    - assets/add-session.js (lines 942-1023 — `applySectionVisibility(isPastSession)` toggles `is-hidden` on `[data-section-key]` wrappers via `App.isSectionEnabled`; `applySectionLabels` writes `App.getSectionLabel(sectionKey, defaultI18nKey)` into each wrapper's `.label[data-i18n]` textContent; the `app:settings-changed` listener at :1020 re-runs both — this whole block is registered INSIDE the async `DOMContentLoaded` handler at :44, after `await App.initCommon()`; read-only, do NOT modify)
    - add-session.html (the `[data-section-key]` section wrappers and their `.label[data-i18n]` elements and `.disabled-indicator-badge`)
    - tests/25-06-crop-only.test.js (the async capture-and-await handler pattern + manual invocation of captured document-level listeners)
    - tests/_helpers/app-stub.js (inject as `win.App`; here the test SUPPLIES `isSectionEnabled` and `getSectionLabel` return values via `overrides` to drive the two cases)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Section visibility/labels ~901-1054 — the Med×High "DO NOW" row; the settings→add-session cross-module link)
  </read_first>
  <action>
    Build a THIN (D-10 — broad ≠ unbounded) jsdom real-page test for the settings→add-session cross-module link, using the async capture-and-await pattern (F-A). Capture `document.addEventListener` into a `docListeners` map before eval; inject `win.App = createAppStub({ isSectionEnabled, getSectionLabel })` with test-controlled returns, plus a no-op `BroadcastChannel`; load `add-session.html`, eval `assets/add-session.js`, and `await` the captured `DOMContentLoaded` handler (so `applySectionVisibility`/`applySectionLabels` and the `app:settings-changed` listener are registered and the initial render runs). After the handler, force a re-render by invoking the captured `app:settings-changed` handler from the `docListeners` map (it is a document-level listener — invoke it directly rather than relying on a real dispatch), then `await new Promise(r => setTimeout(r, 0))`.
    Case 1 (custom title → label): make `App.getSectionLabel(sectionKey, key)` return a distinctive custom title for one section key; assert that wrapper's `.label[data-i18n]` `textContent` equals that custom title (observable render).
    Case 2 (empty/disabled → hidden): make `App.isSectionEnabled(sectionKey)` return false for one section, with a non-past session (editingSession absent); assert that wrapper carries the `is-hidden` class (observable visibility).
    F-J caveat (state in the doc block AND success criteria): the section labels come from the App STUB and `initCommon` is a no-op, so this test pins add-session's ORCHESTRATION/ORDERING (that the renamed label flows from `getSectionLabel` into the right element, and disabled→hidden), NOT the therapist-visible custom-title TEXT produced by the real settings.js writer. The Phase 31 D-13c glue cleanup could regress the real end-to-end output while this stays green.
    End-of-file guard (F-A): assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` (literal count) then `process.exit(failed === 0 ? 0 : 1)`. Assert observable label textContent + `is-hidden` ONLY (D-08) — never internal function names. Open with the falsifiability doc block. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-section-visibility.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-section-visibility.test.js` exits 0 against the current `assets/add-session.js`.
    - A custom `getSectionLabel` return renders into the matching `[data-section-key] .label[data-i18n]` textContent; a disabled section (`isSectionEnabled` false, non-past session) gets the `is-hidden` class — both observable.
    - The async handler is captured-and-awaited and the `app:settings-changed` re-render is driven via the captured handler; the end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard fails a vacuous run (F-A).
    - The test EXECUTES add-session.js under jsdom (no source-slicing) and SURVIVES an internal rename but FAILS if the label-render or visibility wiring breaks (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The settings→add-session section-visibility and custom-label wiring is pinned by a thin executing observable-behavior test (with the stub-masking limitation named).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | Both tests LOAD `add-session.html` + `assets/add-session.js` (+ jsPDF) into jsdom read-only; no write-back; `__testExports` NOT widened |
| test → share/clipboard | `navigator.share`/clipboard are in-repo mocks; nothing leaves the test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test files shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | export tests assert internal shape (false confidence, the D-12 risk) | medium | mitigate | D-08/D-12: tests jsdom-EXECUTE add-session.js and assert observable output (preview text, active-step class, share-mock calls); acceptance requires "no source-slicing, survives internal rename, fails on observable change" |
| T-30-06 | Tampering | widening `__addSessionTestHooks` enlarges the surface Phase 31 must preserve | low | mitigate | Decision: do NOT widen the test-hook surface; observe via the real preview/copy/share path (resolves RESEARCH Open Q#1) |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-export-markdown.test.js` (NEW — TEST-03c, D-08/D-09): executing markdown-builder characterization via `#exportEditor.value` (filtered builder) + a `navigator.clipboard.writeText` spy (full builder) (replaces source-slicing; F-C)
- `tests/30-export-stepper.test.js` (NEW — TEST-03d): executing export-modal stepper state-machine + step-1→Next filtered-`#exportEditor.value` characterization (F-I)
- `tests/30-section-visibility.test.js` (NEW — TEST-03 / F-E / D-13c): thin executing characterization of `applySectionVisibility` (empty/disabled→hidden) + `applySectionLabels` (custom title→label) — the settings→add-session cross-module link

Scope note (D-13b/D-14): the export modal is `consolidate-in-P31` (RFCT-02 extraction target) — this plan pins its behavior, it does NOT refactor it. (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-export-markdown.test.js` exits 0; executes the module (no slicing); filtered builder asserted via `#exportEditor.value`, full builder via the clipboard spy; falsifiable on a section-order regression (Task 1).
- `node tests/30-export-stepper.test.js` exits 0; falsifiable on a broken step transition or a wrong step-1→Next filtered `#exportEditor.value` (Task 2).
- `node tests/30-section-visibility.test.js` exits 0; falsifiable on a broken label-render or visibility wiring (Task 3).
- All three use the async capture-and-await handler pattern with the end-of-file assertion-count guard (no vacuous green — F-A); all survive an internal rename (D-08/D-12); `add-session.js` and `__testExports` unchanged.
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (add-session export modal): the markdown builders and the export stepper are captured as a pre-refactor green baseline that guards Phase 31's export-modal extraction (RFCT-02), with executing tests that replace the source-slicing anti-pattern.
- Stub-masking limitation (F-J): the section labels/headings these tests observe come from the App STUB and `initCommon` is a no-op, so green here means add-session's ORCHESTRATION/ORDERING is preserved — NOT that the therapist-visible custom-title export TEXT is correct. The Phase 31 D-13c glue cleanup could regress the real end-to-end output while these tests stay green; that text is out of Phase 30's observable reach without running the real settings.js writer.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-05-SUMMARY.md` when done.
</output>
