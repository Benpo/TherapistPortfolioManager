---
phase: 30-test-harness-coverage
plan: 13
type: execute
wave: 3
depends_on: ["30-07", "30-08", "30-09", "30-10", "30-11", "30-12"]
files_modified:
  - tests/30-fake-test-detector.test.js
  - tests/run-all.js
  - tests/_helpers/jsdom-pdf-env.js
  - .planning/phases/30-test-harness-coverage/30-RESEARCH.md
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-04]
must_haves:
  truths:
    - "tests/30-fake-test-detector.test.js is a permanent self-check scoped to EXECUTABLE assets only: it FAILS any tests/*.test.js that reads an assets/*.js file as text (detecting BOTH the path.join(ROOT,'assets','x.js') form AND a literal 'assets/x.js' string) and either (a) has zero execution markers after comments+string-literals are STRIPPED, OR (b) feeds the asset's SOURCE TEXT into an assertion / .indexOf( / .slice( / regex (source-under-test) regardless of markers — with an explicit allowlist of exactly {25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed, and itself}; reading .css/.html as text is NEVER flagged; it exits 0 on the post-cleanup tree (Prevention #1)"
    - "the detector's own falsifiability is DEMONSTRATED (its own mutation-kill): it FAILS a scratch fake that evals a module then asserts on its source, FAILS a scratch fake that reads via path.join with only a dead comment marker, and PASSES the real 30-* analog tests + the 3 allowlisted static guards"
    - "tests/run-all.js passes a bounded timeout + killSignal to its per-file spawnSync so one hung async test cannot stall the green gate forever (WR-01)"
    - "tests/_helpers/jsdom-pdf-env.js WrappedJsPDF forwards ALL constructor args to OriginalJsPDF, not just the first (WR-02)"
    - "the 5 documented mis-credits in 30-RESEARCH.md's behavior inventory are corrected per the Documentation corrections table in 30-VERIFICATION.md (D)"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-fake-test-detector.test.js
  key_links:
    - "the fake-detector gate runs inside npm test (discovered by run-all.js), so the single documented command (TEST-04) enforces it on every run"
    - "the gate depends on the 30-12 fake-cleanup having removed the pure source-slicers; the allowlist carves out 25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed, and the detector file itself"
  prohibitions:
    - "the allowlist must NOT hide a real fake — only the named legit static removal/audit guards (and the detector itself) may be allowlisted; every other flagged file must be a genuine executing test or be removed"
    - "do not change PDF byte-output determinism: the WrappedJsPDF change must keep applying the deterministic setCreationDate/setFileId pins per instance"
    - "the pre-commit hook (if added) must not introduce a network call and must not disturb the existing sw.js CACHE_NAME bump machinery"
---

<objective>
Bake in the prevention + robustness fixes that make the net durable, and correct the research record:

- Prevention #1 (B): a PERMANENT fake-test detector gate scoped to EXECUTABLE assets only — it fails any test file that reads an assets/*.js file as text but does not genuinely execute it, AND it catches the subtle fake (eval the module yet assert on its SOURCE text). It is itself rebuilt to not be a token-counting fake: it detects the path.join form (not just a literal 'assets/' substring), strips comments + string literals before scanning for execution markers, and never flags .css/.html readers. Runs inside `npm test`. Includes an explicit allowlist of exactly the 3 legitimate static removal/audit guards + itself.
- WR-01: add a bounded `timeout` + `killSignal` to the per-file `spawnSync` in tests/run-all.js so one hung async test (a realistic Phase-31 failure mode) cannot stall the whole gate forever.
- WR-02: make WrappedJsPDF in the shared PDF helper forward ALL constructor args, not just the first (latent trap for positional jsPDF construction).
- D: fix the 5 mis-credits in 30-RESEARCH.md's behavior inventory so the next planner does not re-inherit them.

This plan runs LAST (wave 3): the gate validates the fully-cleaned, fully-extended tree from waves 1-2 (the shared-helper extension in 30-07 and the fake-cleanup in 30-12 must both have landed).

Purpose: prevent the failure class from recurring and harden the runner/helper the whole suite depends on.
Output: 1 new gate test + 2 hardened infra files + corrected research doc, all green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@.planning/phases/30-test-harness-coverage/30-REVIEW.md
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@tests/run-all.js
@tests/_helpers/jsdom-pdf-env.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prevention #1 — permanent fake-test detector gate (B)</name>
  <files>tests/30-fake-test-detector.test.js</files>
  <read_first>
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (Prevention section #1 + the Anti-patterns; the warning sign is "a test file that reads the asset with fs.readFileSync and never eval/vm.runInContext/jsdom-loads it")
    - tests/run-all.js (how tests/*.test.js are discovered, so the new file is picked up automatically)
    - tests/25-08-single-source-audit.test.js (readSrc('assets/*.js') via a literal-string helper, regex/indexOf over source — legit), tests/25-11-hardcoded-english-removed.test.js (readSource('assets/settings.js') literal-string helper, source grep — legit), tests/25-12-folder-picker-removed.test.js (path.join(...,'assets','settings.js') + source assertions — legit) — these THREE are the only legit assets/*.js-reading static guards and must be allowlisted
    - tests/quick-260615-export-section-order.test.js:39 and tests/quick-260516-g7p-save-returns-to-session.test.js:40 (BOTH real fakes use the `fs.readFileSync(path.join(ROOT,'assets','x.js'))` form + SRC.indexOf/SRC.slice over source — these are DELETED by 30-12, so the post-cleanup tree must be clean; the detector must catch THIS shape)
    - tests/25-12-custom-days-effective-visibility.test.js, tests/25-12-password-ack-full-width.test.js, tests/25-12-password-ack-stacking.test.js (these read assets/app.css / settings.html as text — they must NEVER be flagged; reading .css/.html is not a fake), and tests/25-13-css-audit.test.js:17,31 (reads app.css; has the word "jsdom" only in a comment — proves why comment-stripping matters before the marker scan)
  </read_first>
  <behavior>
    - Scope = EXECUTABLE assets only: a tests/*.test.js is a CANDIDATE only if it reads an assets/*.js file as text. The detector recognizes BOTH read forms — `fs.readFileSync(path.join(...,'assets','x.js'))` AND a literal `'assets/x.js'` string passed to a read helper. Reading `.css` / `.html` as text is NEVER a candidate (removes the false-trips on the CSS audits).
    - A candidate is FLAGGED when EITHER: (a) after comments and string literals are STRIPPED from the file text, it has NO execution marker matching `vm|eval|jsdom|runInContext` (so a "jsdom"/"retrieval"/"level" in a comment or string cannot satisfy the check); OR (b) the asset's SOURCE TEXT is fed into an assertion / `.indexOf(` / `.slice(` / regex test (i.e. the module's source string is the thing under test) — flagged regardless of markers, to catch the subtle fake that eval's the module AND asserts on its source.
    - A flagged file passes ONLY if its basename is on the explicit ALLOWLIST.
    - On the post-cleanup tree (after 30-12 deletes the two path.join fakes) the gate exits 0: the only remaining assets/*.js-readers without genuine execution are the 3 allowlisted static guards.
    - It never flags itself (it reads tests/*.test.js, not assets/*.js) and is self-allowlisted defensively.
  </behavior>
  <action>
    Create tests/30-fake-test-detector.test.js following the project test scaffold (PASS/FAIL counter, exit 0/1, doc block). It reads each tests/*.test.js (fs.readdirSync + readFileSync of the TEST files — it does NOT read assets/*). For each test file:
    1. CANDIDATE detection (executable-asset scope): true if the file text contains a readFileSync AND references an assets/*.js path in either form — the path.join form (a join call whose segments include 'assets' and end in a '*.js' literal) OR a literal string matching `assets/...\.js`. Do NOT match on the bare 'assets/' substring (it false-trips CSS/HTML readers); require the `.js` extension.
    2. For a candidate, build a STRIPPED copy of the file with comments (// and block) and string/template literals removed, then test for an execution marker `vm|eval|jsdom|runInContext` against the STRIPPED text.
    3. FLAG the candidate if (a) no execution marker in the stripped text, OR (b) the raw text feeds an asset-source variable into an assertion or `.indexOf(` / `.slice(` / a regex `.test(`/`.match(` (source-under-test heuristic) — flag (b) even when a marker is present.
    4. A flagged candidate PASSES only if its basename ∈ ALLOWLIST = { 25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed, 30-fake-test-detector } — hardcoded with a one-line justification comment per entry (under the .js-only scope these are the only legit readers, so the suite stays green).
    CRITICAL self-validation the executor MUST do before flipping the gate to failing-on-violation: run the detector in report mode over the post-cleanup tree and ENUMERATE every flagged file; each must be either (i) deleted by 30-12, or (ii) one of the 3 named legit guards on the allowlist. Only then make violations fail. The gate MUST exit 0 on the current tree. Optionally add a pre-commit hook invocation of this same check (no network; must not disturb the sw.js CACHE_NAME bump machinery) — the npm-test self-check is the primary, required deliverable.
  </action>
  <verify>
    <automated>node tests/30-fake-test-detector.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-fake-test-detector.test.js` exits 0 on the current post-cleanup tree
    - SCOPE: the detector flags only assets/*.js readers; it does NOT flag the .css/.html readers (25-12-custom-days-effective-visibility, 25-12-password-ack-full-width, 25-12-password-ack-stacking, 25-13-css-audit) — confirm these stay green
    - DETECTS BOTH FORMS: the candidate check matches the `fs.readFileSync(path.join(ROOT,'assets','x.js'))` form, not just a literal 'assets/' substring
    - COMMENT/STRING STRIPPING: the execution-marker scan runs on a comments+string-literals-stripped copy (so a "jsdom" comment like 25-13-css-audit:17 cannot act as a marker)
    - STRONGER RULE: a file that eval's a module AND feeds its SOURCE TEXT into an assertion / .indexOf( / .slice( / regex is flagged even though it has a marker
    - ALLOWLIST is exactly the four named entries (25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed, and itself), each with a justification comment
    - DEMONSTRATED KILLS (the detector's own mutation-kill, recorded in the SUMMARY): (a) a scratch fake that eval's a module then asserts on its source → detector exits non-zero; (b) a scratch fake that reads via path.join(...,'assets','x.js') with only a dead comment marker → detector exits non-zero; (c) the detector PASSES the real 30-* analog tests AND the 3 allowlisted static guards (scratch files removed afterward)
    - the file is discovered by tests/run-all.js (matches `30-*.test.js`)
    - `npm test` stays green (exit 0)
  </acceptance_criteria>
  <done>A permanent, single-command-enforced gate fails any future source-slicing test (assets/*.js read without genuine execution, including the eval-then-assert-on-source subtle fake), scoped to executable assets so CSS/HTML audits stay green, with the 3 legit static guards allowlisted; the exact failure that caused this round cannot recur silently, and the detector is itself proven non-vacuous via demonstrated kills.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 + WR-02 — harden the runner timeout and the PDF wrapper arg forwarding</name>
  <files>tests/run-all.js, tests/_helpers/jsdom-pdf-env.js</files>
  <read_first>
    - tests/run-all.js:62-78 (the per-file spawnSync with no timeout; the existing result.signal != null branch already classifies signal termination as FAIL, so a timeout kill integrates cleanly)
    - tests/_helpers/jsdom-pdf-env.js:98-110 (WrappedJsPDF forwards only the first arg `args`; the deterministic setCreationDate/setFileId pins must stay applied per instance)
    - .planning/phases/30-test-harness-coverage/30-REVIEW.md (WR-01 and WR-02 with the suggested fixes)
  </read_first>
  <action>
    WR-01: add a bounded `timeout` (a value comfortably above the slowest legitimate PDF test, e.g. 120000 ms) and `killSignal` to the spawnSync options in tests/run-all.js so a hung child is killed and counted as FAIL by the existing signal branch. Keep the existing PASS/FAIL aggregation and exit contract. WR-02: change WrappedJsPDF to forward ALL constructor arguments to OriginalJsPDF (use Reflect.construct(OriginalJsPDF, arguments-as-array) or the Function.prototype.bind.apply form), while still applying setCreationDate(PINNED_DATE) + setFileId(PINNED_FILE_ID) and the optional onJsPDF hook per instance. Do not change byte-output determinism.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'timeout|killSignal' tests/run-all.js` shows both options passed to the per-file spawnSync; the existing PASS/FAIL/exit aggregation is unchanged
    - WrappedJsPDF forwards all args (`grep -nE 'Reflect.construct|bind.apply|arguments' tests/_helpers/jsdom-pdf-env.js` is non-empty in the wrapper) AND still calls setCreationDate(PINNED_DATE) + setFileId(PINNED_FILE_ID) per instance
    - all 7 migrated PDF tests still pass with byte-deterministic output (TEST-01 preserved): `npm test` exits 0
    - a deliberately-hung scratch test is killed by the timeout and counted FAIL (demonstrated, then removed) — record in SUMMARY
  </acceptance_criteria>
  <done>The runner can no longer be stalled forever by one hung test, and the shared PDF wrapper is safe against positional jsPDF construction — both without changing the green outcome or PDF determinism.</done>
</task>

<task type="auto">
  <name>Task 3: D — correct the 5 mis-credits in 30-RESEARCH.md's behavior inventory</name>
  <files>.planning/phases/30-test-harness-coverage/30-RESEARCH.md</files>
  <read_first>
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md ("Documentation corrections for 30-RESEARCH.md" table — the 5 claim→reality rows)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md:195-236 (the God-Module Behavior Inventory tables carrying the mis-credits: IIFE-4 Backups "EXTENSIVELY" via 25-04/25-05; IIFE-5 Photos "EXTENSIVELY" incl. 25-06; IIFE-2 Snippets "EXTENSIVE"; buildReportRow "shape only? via 29-03"; B8 bottom fns "DONE: 24-06; quick-260516-g7p")
  </read_first>
  <action>
    Edit ONLY the affected inventory rows/notes in 30-RESEARCH.md to reflect reality per the 5-row corrections table in 30-VERIFICATION.md: (1) IIFE-4 Backups — 25-04/25-05 load assets/backup.js (the engine), not settings.js screen wiring; helper-text + password-gate are gaps. (2) IIFE-5 Photos — 25-06 loads crop.js, 25-07 loads db.js; the settings.js _optimizeAllPhotosLoop body is a gap. (3) IIFE-2 Snippets — EXTENSIVE at LEAF level only; screen wiring uncovered. (4) buildReportRow — 29-03 is behavioral but leaf-only; mountReportRow boot wiring uncovered. (5) B8 bottom fns — 24-06 pins only renderSpotlightSessionInfo; quick-260516-g7p was a fake (now removed); rest of B8 uncovered. Keep edits small and surgical (scoped Edit, not a rewrite); add a dated note that the inventory was corrected post-re-audit. This is a documentation correction, not test work.
  </action>
  <verify>
    <automated>grep -nE 'backup.js|crop.js|db.js|leaf level|leaf-only' .planning/phases/30-test-harness-coverage/30-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - the 5 inventory rows/notes are corrected to match the 30-VERIFICATION.md corrections table (no remaining "EXTENSIVELY covered" claim for the mis-credited IIFE-4/IIFE-5, no "DONE: ...quick-260516-g7p" claim for B8)
    - a dated "corrected post-re-audit (2026-06-27)" note is present near the inventory
    - the rest of 30-RESEARCH.md is unchanged (surgical edits only)
    - no test files are modified by this task
  </acceptance_criteria>
  <done>The research behavior inventory no longer carries the 5 mis-credits, so a future planner won't re-inherit them.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-fake-test-detector.test.js (Prevention #1 / B)

**Modified files:**
- tests/run-all.js (WR-01: spawnSync timeout + killSignal)
- tests/_helpers/jsdom-pdf-env.js (WR-02: WrappedJsPDF forwards all args)
- .planning/phases/30-test-harness-coverage/30-RESEARCH.md (D: 5 behavior-inventory corrections)
- (optional) .git/hooks/pre-commit — secondary fake-detector invocation, if added (no network; must not disturb sw.js bump)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules; no untrusted input |
| pre-commit hook → shell | if a pre-commit hook is added, it executes a node check at commit time (local dev only) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-13 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. Schema: N/A. |
| T-30-13-SH | Tampering | optional pre-commit hook (shell/node execution at commit) | low | mitigate | If added, the hook only runs the in-repo node fake-detector against tests/*.test.js — no network, no install, no external input; reuses the existing local-only hook pattern and must not disturb the sw.js CACHE_NAME bump machinery. |
</threat_model>

<verification>
- `node tests/30-fake-test-detector.test.js` exits 0 on the cleaned tree; falsified to fail on a scratch source-slicer
- `grep -nE 'timeout|killSignal' tests/run-all.js` non-empty; WrappedJsPDF forwards all args and keeps the deterministic pins
- the 5 RESEARCH.md mis-credits are corrected
- `npm test` → full suite green, exit 0 (TEST-04 single-command + TEST-01 PDF determinism preserved; the gate now protects TEST-02's RTL guard and every other test from source-slicing)
</verification>

<success_criteria>
A permanent fake-test gate runs inside the single documented command and blocks future source-slicing (with the legit static guards allowlisted); the runner cannot be stalled by a hung test; the PDF wrapper is positional-arg safe; the research record is corrected; the whole suite stays green.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-13-SUMMARY.md` when done.
</output>
