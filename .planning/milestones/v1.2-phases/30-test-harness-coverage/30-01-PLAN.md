---
phase: 30-test-harness-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/run-all.js
autonomous: true
requirements: [TEST-04, TEST-01]
must_haves:
  truths:
    - "Running `npm test` executes every top-level tests/*.test.js and exits 0 on the current green suite (D-01, TEST-04)"
    - "`npm test` exits non-zero if any single test file exits non-zero — the gate that will guard the Phase 31 refactor"
    - "All 8 jsdom-dependent tests resolve jsdom from the installed devDependency with no /tmp install (D-01)"
    - "Production stays genuinely zero-runtime-dependency: package.json carries devDependencies only (D-02, D-03)"
    - "The Node engines floor guarantees native Blob.prototype.arrayBuffer so the PDF blob path cannot regress (D-05, TEST-01)"
  artifacts:
    - path: package.json
      provides: "First-ever root package.json — private:true, devDependencies={jsdom ^29.1.1}, engines.node >=18.0.0, scripts.test, name sessions-garden"
      contains: "private"
    - path: tests/run-all.js
      provides: "Suite runner — discovers tests/*.test.js, continue-on-fail, aggregates non-zero exit, default JSDOM_PATH bridge"
  key_links:
    - from: package.json
      to: tests/run-all.js
      via: "scripts.test = node tests/run-all.js"
      pattern: "run-all"
    - from: tests/run-all.js
      to: tests/*.test.js
      via: "spawns node per file, preserves the per-file exit-0/1 contract, fails suite if any file fails"
      pattern: "readdirSync"
    - from: tests/run-all.js
      to: node_modules/jsdom
      via: "sets JSDOM_PATH unconditionally to the installed devDependency (overriding any stale inherited export) to bridge the 8 legacy jsdom tests"
      pattern: "JSDOM_PATH"
---

<objective>
Establish the project's first `package.json` and a single-command suite runner so the whole test suite runs green via `npm test` (TEST-04), and pin the Node `engines` floor that permanently fixes the PDF tests' old-Node `blob.arrayBuffer` root cause (D-05, TEST-01 root cause (b)).

This is the foundation wave: it introduces the npm dev/test boundary (D-01/D-02/D-03) and the runner that every later test task is gated by. It changes NO production code — only a new root config file and a new test runner.

Purpose: Give the phase a single documented green command (TEST-04) and the npm boundary that lets jsdom be a reproducibly-installed devDependency, replacing the fragile `/tmp` jsdom convention. The green `npm test` produced here is the seed of the Phase 31 guardrail.
Output: `package.json`, `tests/run-all.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root package.json (npm dev/test boundary) and install jsdom</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§TEST-04 — exact package.json shape; §Package Legitimacy Audit — jsdom OK; Pitfall 3 — do not over-pin engines)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§No Analog Found — first-ever package.json guidance)
    - .gitignore (confirm the node-modules ignore line at line 19 is already present — verify only, do NOT edit)
    - .planning/PROJECT.md (§Key Decisions — the "zero npm dependencies / no build" production identity that D-02/D-03 reconcile)
  </read_first>
  <action>
    Create a root `package.json` with the exact shape from RESEARCH §TEST-04, implementing D-01/D-02/D-03/D-05:
    fields `private` true, `name` "sessions-garden", `version` "0.0.0", a `description` stating it is the dev/test workbench only (production ships static /assets/* with zero runtime deps), `scripts.test` set to "node tests/run-all.js", `devDependencies` containing ONLY "jsdom" pinned to "^29.1.1", and `engines.node` set to ">=18.0.0".
    The `engines.node` floor ">=18.0.0" is the D-05 fix: Node 18 is the exact version where native global Blob and Blob.prototype.arrayBuffer became available, so the PDF tests' blob path (TEST-01 root cause b) cannot recur. Do not raise the floor higher (Pitfall 3 — over-pinning blocks contributors).
    Do NOT add any `dependencies` key and do NOT add any runtime/production dependency — production must stay genuinely zero-runtime-dependency (D-02/D-03). jsdom is audited clean per RESEARCH §Package Legitimacy Audit (verdict OK, no postinstall) so no human legitimacy checkpoint is required.
    Run `npm install` once to populate `node_modules/`, which is already gitignored (confirm the ignore entry exists; no edit needed).
  </action>
  <verify>
    <automated>npm install && node -e "const p=require('./package.json');const a=require('assert');a.strictEqual(p.private,true);a.ok(p.devDependencies&&p.devDependencies.jsdom);a.ok(!p.dependencies);a.strictEqual(p.engines.node,'>=18.0.0');a.strictEqual(p.scripts.test,'node tests/run-all.js');console.log('ok')" && grep -c 'node_modules/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root with `private:true`, `devDependencies.jsdom` "^29.1.1", `engines.node` ">=18.0.0", `scripts.test` "node tests/run-all.js", and NO `dependencies` key (falsifiable: the node assert above exits non-zero if any of these is wrong or if a runtime dependency was added).
    - `npm install` exits 0 and `node_modules/` is present and ignored by git (`grep` returns >=1).
    - No `assets/*` file is modified; no production runtime dependency introduced.
  </acceptance_criteria>
  <done>package.json present and valid per the assert; jsdom installed under node_modules/ (gitignored); zero production-dependency footprint preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/run-all.js suite runner with the JSDOM_PATH bridge</name>
  <files>tests/run-all.js</files>
  <read_first>
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§TEST-04 — run-all.js vs shell glob recommendation; the per-file exit-0/1 contract; "fail the suite if any file fails")
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§No Analog Found — run-all.js idiom: readdirSync filter .test.js, spawn node, continue-on-fail, aggregate)
    - tests/quick-260620-q8m-pdf-paragraph-linebreaks.test.js (lines 61-69 — the legacy `JSDOM_PATH` env fallback the 8 jsdom tests rely on; the runner must bridge it to the installed devDependency)
    - .planning/codebase/TESTING.md (§Test Structure — Node built-in runner, exit 0/1 convention)
  </read_first>
  <action>
    Create `tests/run-all.js` (D-01 TEST-04 runner) that:
    discovers top-level test files via `fs.readdirSync('tests').filter(f => f.endsWith('.test.js'))` (this is top-level only, so the `tests/_helpers/` directory and its non-`.test.js` helpers are excluded automatically);
    spawns each file with `node` in a continue-on-fail loop (run ALL files, do not stop at the first failure), captures each child's exit code, prints a per-file PASS/FAIL line plus a final summary, and exits non-zero if ANY child exited non-zero — preserving each file's exit-0/1 contract and failing the whole suite if any single file fails.
    Set JSDOM_PATH for the spawned children so the 8 legacy jsdom tests resolve jsdom from the installed devDependency instead of the unwritten `/tmp` location: before spawning, set the child environment's `JSDOM_PATH` UNCONDITIONALLY to the absolute path of the installed jsdom under the repo's node_modules, i.e. `path.join(__dirname, '..', 'node_modules', 'jsdom')` (F-G — do NOT use a "default only if unset" guard: a developer who still has a stale `export JSDOM_PATH=/tmp/...` from the legacy convention would otherwise defeat the bridge and FATAL all 8 jsdom tests). If you choose to honor a caller-supplied override at all, honor it ONLY when that override path actually resolves on disk (`fs.existsSync`); otherwise fall back to the repo node_modules path. This bridges the legacy env fallback to the real installed dependency until plan 30-03 migrates the 7 PDF tests onto the shared helper.
    Run once and exit — NO watch mode, no long-lived process.
  </action>
  <verify>
    <automated>npm test; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` discovers and runs every top-level `tests/*.test.js`, prints per-file PASS/FAIL, and exits 0 against the current all-green suite (all ~87 existing files green, INCLUDING the 8 jsdom tests which now resolve jsdom via the bridge — falsifiable: before the bridge they fail to require jsdom).
    - `npm test` exits non-zero if any single file fails — falsifiable: temporarily making one test file call `process.exit(1)` makes `npm test` exit 1.
    - The JSDOM_PATH bridge survives a stale inherited export (F-G): with `JSDOM_PATH=/tmp/does-not-exist` exported in the environment, `npm test` still resolves jsdom from `node_modules` and the 8 jsdom tests stay green (falsifiable: a "default only if unset" guard would let the stale `/tmp` value through and FATAL those 8 tests).
    - No file under `tests/_helpers/` is executed as a test.
    - The runner runs once and exits (no watch mode).
  </acceptance_criteria>
  <done>`npm test` runs the full suite green, fails fast-aggregate on any single-file failure, and bridges the 8 legacy jsdom tests to the installed devDependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev workbench | jsdom devDependency is fetched and installed locally; never deployed |
| dev workbench → production (Cloudflare Pages) | Only static `/assets/*` ships; `node_modules/`, `package.json`, `tests/**` never reach the customer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | jsdom devDependency (npm install) | low | mitigate | Pin `^29.1.1` (audited OK, ~30M/wk, no postinstall per RESEARCH §Package Legitimacy Audit); review the generated lockfile; it is the sole new dependency |
| T-30-02 | Information disclosure | test infra (`node_modules/`, `package.json`, `tests/**`) | low | mitigate | `node_modules/` gitignored (verified line 19); Cloudflare deploy serves static `/assets/*` only; D-02 keeps production zero-runtime-dependency |
| T-30-SC | Tampering | npm install of jsdom | low | mitigate | Legitimacy audited (RESEARCH §Package Legitimacy Audit verdict OK); no `[ASSUMED]`/`[SUS]` packages → no blocking-human checkpoint required |

No `high` severity items; no new production attack surface (D-02, D-14).
</threat_model>

<artifacts>
## Artifacts this phase produces (full manifest — produced across plans 30-01 … 30-06)

- `package.json` (root, NEW — 30-01): `private:true`, `devDependencies={jsdom ^29.1.1}`, `engines.node ">=18.0.0"`, `scripts.test "node tests/run-all.js"`
- `tests/run-all.js` (NEW — 30-01): suite runner + JSDOM_PATH bridge
- `tests/_helpers/jsdom-pdf-env.js` (NEW — 30-02): exports `buildJsdomEnv()` → `{ dom, win }`; `getContext`→null stub; deterministic `setCreationDate`/`setFileId` pinning
- `tests/_helpers/app-stub.js` (NEW — 30-02): exports `createAppStub()` → the `App.*` stub surface with a `__calls` spy; `initCommon()` resolves
- `tests/30-rtl-guard.test.js` (NEW — 30-03, TEST-02)
- `tests/30-settings-section-roundtrip.test.js` (NEW — 30-04, TEST-03a)
- `tests/30-settings-tabnav.test.js` (NEW — 30-04, TEST-03b)
- `tests/30-export-markdown.test.js` (NEW — 30-05, TEST-03c)
- `tests/30-export-stepper.test.js` (NEW — 30-05, TEST-03d)
- `tests/30-section-visibility.test.js` (NEW — 30-05, TEST-03 / D-13c cross-module link): thin observable section-visibility (empty→hidden) + custom-label render
- `tests/30-issue-delta.test.js` (NEW — 30-06, TEST-03e)
- 7 MODIFIED PDF tests adopting the shared helper (30-03): `pdf-bold-rendering`, `pdf-digit-order`, `pdf-glyph-coverage`, `pdf-latin-regression`, `quick-260522-iwr-ordered-list-export`, `quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix`, `quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression`
- The `npm test` script (`scripts.test`) + the `engines.node` floor (package.json)

This plan (30-01) produces: `package.json`, `tests/run-all.js`, the `npm test` script, and the `engines` floor.
</artifacts>

<verification>
- `npm install` exits 0; `package.json` validates (Task 1 assert).
- `npm test` runs the full existing suite green and exits 0 (Task 2).
- `npm test` exits non-zero on any single-file failure (Task 2 falsifiability check).
- No `assets/*` production file touched.
</verification>

<success_criteria>
- A single documented command (`npm test`) runs the whole suite green (TEST-04).
- The npm dev/test boundary exists with devDependencies only and an `engines` floor (D-01/D-02/D-03/D-05); production stays zero-runtime-dependency.
- The 8 jsdom-dependent tests resolve jsdom from the installed devDependency (no `/tmp` install).
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-01-SUMMARY.md` when done.
</output>
