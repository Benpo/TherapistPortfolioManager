---
phase: 30-test-harness-coverage
plan: 04
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-settings-section-roundtrip.test.js
  - tests/30-settings-tabnav.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Editing a section title, saving, and reloading persists and re-renders the value — the documented save/load round-trip gap is closed (D-07, D-08, D-09, TEST-03a)"
    - "Editing a section title then Discarding reverts the input and writes nothing (assertNoWrites) — the onDiscard half of the round-trip (F-H, TEST-03a)"
    - "The ?tab= URL param selects the active settings tab, switching tabs writes the URL, and an invalid tab falls back to default (TEST-03b)"
    - "Each test drives ONLY its target settings.js DOMContentLoaded handler (roundtrip→IIFE-1, tabnav→IIFE-3 boot) — settings.js boots 5 handlers (:643 IIFE-1 async, :2016 snippets, :2111 tab-nav, :2342 backups, :2949 photos) and an unrelated one must not flake the run (F-F)"
    - "Both tests EXECUTE the real settings.js under jsdom and survive an internal rename but fail on an observable-output change (D-08, D-12)"
    - "Phase 30 characterizes ONLY the two god modules (settings.js, add-session.js) — the conscious scope recorded by the D-13a full-file coverage map and the D-13b per-finding forward-classification in 30-RESEARCH.md; every other assets/*.js is listed there with an out-of-scope reason and routed to backlog, not tested here (D-13a, D-13b, D-14)"
  prohibitions:
    - statement: "No assets/*.js file other than settings.js and add-session.js is the SUBJECT of a Phase 30 characterization test. Out-of-scope files (app.js, license.js, backup.js, pdf-export.js, db.js) may be loaded read-only as support (e.g. the real App.setLanguage for the RTL guard, the real severity pair for the issue-delta test) but are never characterized here — they are routed to backlog per the D-13a coverage map."
      status: resolved
      verification: "Inspect every tests/30-*.test.js: the module under characterization (the file whose observable behavior is asserted) is only settings.js or add-session.js; app.js appears solely as a read-only support import, never as the assertion subject (D-14)."
  artifacts:
    - path: tests/30-settings-section-roundtrip.test.js
      provides: "jsdom real-page save→reload round-trip over settings.js IIFE-1"
    - path: tests/30-settings-tabnav.test.js
      provides: "jsdom/vm tab-nav observable behavior over settings.js IIFE-3"
  key_links:
    - from: tests/30-settings-section-roundtrip.test.js
      to: tests/_helpers/mock-portfolio-db.js
      via: "asserts mockDb.__calls.get('setTherapistSetting') captured the persisted record"
      pattern: "setTherapistSetting"
    - from: tests/30-settings-section-roundtrip.test.js
      to: assets/settings.js
      via: "loads settings.html + settings.js, fires DOMContentLoaded, drives the real onSave handler"
      pattern: "DOMContentLoaded"
---

<objective>
Characterize the genuinely-uncovered observable behavior of `assets/settings.js` before the Phase 31 refactor (D-07 "go broad", scoped by D-10 to the NONE/uncovered rows only):
1. `tests/30-settings-section-roundtrip.test.js` — the section-title save/load round-trip (IIFE-1), the documented gap (TESTING.md "What Is NOT Tested: Settings save/load round-trips"), via the jsdom real-page approach (D-09).
2. `tests/30-settings-tabnav.test.js` — the `?tab=` URL-param tab navigation (IIFE-3), currently uncovered.

Both tests assert OBSERVABLE behavior only (D-08): what is persisted, what re-renders, which tab is active, what the URL becomes — never internal function names. They EXECUTE the real module under jsdom (D-09), so they fail on an observable-output change but survive the Phase 31 internal rename (D-08/D-12). Per D-10 the already-pinned IIFE-2/4/5 (snippets/backups/photos, ~2,200 lines covered by the 25-xx suite) are NOT re-tested. No `assets/*` file is modified — settings.js/settings.html are loaded read-only.

Purpose: Establish the pre-refactor green baseline for settings.js that the Phase 31 extraction (RFCT-01) is guarded by.
Output: `tests/30-settings-section-roundtrip.test.js`, `tests/30-settings-tabnav.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section-title save/load round-trip (TEST-03a, D-09 jsdom real-page)</name>
  <files>tests/30-settings-section-roundtrip.test.js</files>
  <read_first>
    - assets/settings.js (IIFE-1 SettingsPage, lines 14-688 — the ASYNC `DOMContentLoaded` handler at :643 runs `loadAndRender`; `onSave` (async) at :448 persists via `PortfolioDB.setTherapistSetting`; `onDiscard` (async) at :525 calls `loadAndRender` to revert; reads via `PortfolioDB.getAllTherapistSettings`; read-only, do NOT modify)
    - settings.html (the section-title editor form fragment — the inputs and Save/Discard controls the handlers bind to)
    - tests/25-06-crop-only.test.js (THE async analog — `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE; the `docListeners` capture-and-await pattern: override `document.addEventListener` before load to record listeners into a map, then `await` the captured handler; the end-of-file `process.exit(failed===0?0:1)`)
    - tests/_helpers/mock-portfolio-db.js (lines 50-128 — `createMockPortfolioDB`, the `__calls.get('setTherapistSetting')` arg-array spy, `getAllTherapistSettings` read-stub, `assertNoWrites`)
    - tests/_helpers/app-stub.js (the App.* stub from 30-02 — inject as `win.App`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-settings-section-roundtrip.test.js — the mock-DB wiring excerpt + the settings.js anchors)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Settings save/load round-trip code example; §D-09 verdict for settings.js IIFE-1)
  </read_first>
  <action>
    Build a jsdom real-page test (Pattern 2) using the ASYNC runner from `tests/25-06-crop-only.test.js`. The :643 handler is `async () => { await App.initCommon(); ... }` and `loadAndRender`/`onSave`/`onDiscard` are all async — a synchronous `dispatchEvent('DOMContentLoaded')` returns BEFORE the async continuation runs, so the file would exit green with zero assertions executed (F-A vacuous-green trap). Avoid it: BEFORE evaling settings.js, override `win.document.addEventListener` to record every listener into a `docListeners` map (the 25-06 pattern); after eval, `await` the SPECIFIC IIFE-1 `DOMContentLoaded` handler (the FIRST-registered one, at :643 — do NOT blanket-`dispatchEvent`, which would also boot the snippets/tabnav/backups/photos handlers and let an unrelated async rejection flake this test — F-F). After any async-driven event (handler, save click, discard click), `await new Promise(r => setTimeout(r, 0))` so microtasks settle. Wrap every case in `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE.
    Setup: load the real `settings.html` body fragment, inject `win.PortfolioDB = createMockPortfolioDB({ therapistSettings: [seeded section labels] })` and `win.App = createAppStub()`, then run the captured IIFE-1 handler so `loadAndRender` renders the section-title rows from `getAllTherapistSettings`.
    Save round-trip case: edit a section-title input's value, fire the Save control (the async `onSave` at :448), `await` the microtask flush, and assert the mock DB captured the persisted record — `mockDb.__calls.get('setTherapistSetting')` has at least one call whose argument carries the edited value. Then re-render (re-run the load path or re-fire the captured load handler) and assert the rendered input shows the saved value (the reload half of the round-trip).
    Discard/revert case (F-H): re-seed, render, edit a section-title input to a new value, then fire the Discard control (the async `onDiscard` at :525, which re-runs `loadAndRender`), `await` the flush, and assert (a) the input REVERTS to the seeded value and (b) `assertNoWrites(mockDb)` — no `setTherapistSetting` write fired for the discarded edit.
    End-of-file guard (F-A): after the IIFE resolves, assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` with a literal expected count equal to the number of `test(...)` cases in this file, so a vacuous run (async assertions never executed) CANNOT exit green; then `process.exit(failed === 0 ? 0 : 1)`.
    Assert OBSERVABLE persistence and re-render ONLY (D-08): the captured DB record, the rendered value, the reverted value, the no-write — never which internal function wrote it, and never a function name. Open the file with the root-cause / falsifiability doc block per `feedback-behavior-verification`. Load settings.js/settings.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-settings-section-roundtrip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-section-roundtrip.test.js` exits 0 against the current `assets/settings.js`.
    - Editing a section title and saving persists a record observable via `mockDb.__calls.get('setTherapistSetting')` carrying the edited value; a reload re-renders that value (the round-trip).
    - The Discard case reverts the edited input to the seeded value and `assertNoWrites(mockDb)` passes — no write fired for the discarded edit (F-H).
    - The test drives ONLY the IIFE-1 `DOMContentLoaded` handler (captured via the `docListeners` map), not a blanket `dispatchEvent` — an unrelated settings.js handler cannot run or flake this test (F-F).
    - The test FAILS (non-zero) if the async handler's assertions never run — enforced by the end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard (F-A: a synchronous-`dispatchEvent` mis-composition that skips the async continuation trips this guard instead of exiting vacuously green).
    - The test jsdom-LOADS and EXECUTES settings.js (it does NOT `fs.readFileSync`+regex the source) and would FAIL if the saved value were dropped or not re-rendered, but SURVIVE an internal rename of the writing/render function (D-08/D-12 — verifiable by renaming an internal helper in a scratch copy).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The settings section-title save→reload round-trip (plus discard/revert) is pinned by an executing observable-behavior test, closing the documented gap.</done>
</task>

<task type="auto">
  <name>Task 2: Settings tab navigation via ?tab= (TEST-03b)</name>
  <files>tests/30-settings-tabnav.test.js</files>
  <read_first>
    - assets/settings.js (IIFE-3 tab nav, lines 2038-2113 — `readUrlTab`, `writeUrlTab`, `boot` (SYNCHRONOUS), `activate`; its `DOMContentLoaded` handler is registered at :2111; read-only, do NOT modify)
    - settings.html (the tab markup — the tab buttons/panels and their identifying attributes)
    - tests/_helpers/app-stub.js (inject as `win.App`)
    - tests/25-06-crop-only.test.js (the `docListeners` capture pattern — used here only to SELECT the right handler, not for async/await since boot is sync)
    - tests/25-11-i18n-parity.test.js (the vm/jsdom loader + PASS/FAIL + exit-0/1 scaffold)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-settings-tabnav.test.js — assert observable active-tab class + window.location.search, never internal fn names)
  </read_first>
  <action>
    Build a test (jsdom real-page preferred) for settings.js IIFE-3. `boot()` is SYNCHRONOUS (reviewer-validated) so no async/await machinery is needed — but settings.js registers 5 `DOMContentLoaded` handlers (:643 IIFE-1 async, :2016 snippets, :2111 tab-nav, :2342 backups, :2949 photos), so do NOT blanket-`dispatchEvent` (that boots IIFE-1's async loadAndRender, snippets, backups, photos too, and an unrelated rejection could flake this test — F-F). Instead capture handlers into a `docListeners` map (override `win.document.addEventListener` before eval, the 25-06 pattern) and invoke ONLY the IIFE-3 tab-nav `boot` handler (the one registered at :2111 — the tab-nav `boot`, registered after the snippets `boot`).
    Load `settings.html` + `assets/settings.js` into a jsdom window whose URL carries `?tab=<valid-tab>`, run ONLY the captured tab-nav boot handler, and assert the OBSERVABLE active-tab state — the active class / `aria-selected` on the tab element matching the param. Then simulate switching to a different tab (activate it) and assert `window.location.search` was updated to the new `?tab=` value. Then load with an INVALID `?tab=` value and assert it falls back to the default tab (observable active class).
    End-of-file guard (F-A): after all cases, assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` with a literal expected count equal to the number of `test(...)` cases, then `process.exit(failed === 0 ? 0 : 1)` — so a mis-wired run that silently skips cases cannot exit green.
    Assert observable active-tab and URL state ONLY (D-08) — never internal function names. Open with the falsifiability doc block. Load settings.js/settings.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-settings-tabnav.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-tabnav.test.js` exits 0 against the current `assets/settings.js`.
    - A valid `?tab=` selects the matching active tab; switching tabs writes the new `?tab=` to `window.location.search`; an invalid value falls back to the default tab — all asserted via observable DOM/URL state.
    - The test invokes ONLY the IIFE-3 tab-nav boot handler (captured via the `docListeners` map), not a blanket `dispatchEvent` that would also boot IIFE-1/backups/photos (F-F).
    - The end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard fails the run if any case was silently skipped (F-A).
    - The test EXECUTES settings.js under jsdom and FAILS if tab selection or URL write breaks, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>Settings tab navigation behavior (?tab= select, URL write, invalid fallback) is pinned by an executing observable-behavior test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | Both tests LOAD `settings.html` + `assets/settings.js` into jsdom read-only; no write-back |
| test → storage | `PortfolioDB` is the in-repo mock spy; no real IndexedDB; no data leaves the test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test files shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | a characterization test that asserts internal shape (false confidence) | medium | mitigate | D-08/D-12: tests jsdom-EXECUTE settings.js and assert observable output only; acceptance requires "survives internal rename, fails on observable change" |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-settings-section-roundtrip.test.js` (NEW — TEST-03a, D-09): jsdom real-page save→reload round-trip over settings.js IIFE-1
- `tests/30-settings-tabnav.test.js` (NEW — TEST-03b): observable tab-nav behavior over settings.js IIFE-3

Scope note (D-10/D-13a/D-13c/D-14): settings.js IIFE-2 (snippets), IIFE-4 (backups), IIFE-5 (photos) are already pinned by the 25-xx/24-xx suite and are NOT re-tested; out-of-scope files (app.js, license.js, etc.) are not touched — those are the v1.3 backlog per D-14. The settings.js glue duplication (`t`/`showToast`/`getCurrentLang`) surfaced by research (D-13c) is a Phase 31 consolidation candidate, preserved here as context only — NOT Phase 30 work (this plan pins behavior; it refactors nothing). (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-settings-section-roundtrip.test.js` exits 0 and is falsifiable on a dropped/altered saved value (Task 1).
- `node tests/30-settings-tabnav.test.js` exits 0 and is falsifiable on broken tab selection/URL write (Task 2).
- Both tests execute settings.js under jsdom (not source-sliced) and survive an internal rename (D-08/D-12).
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (settings.js): the section-title save/load round-trip and the tab navigation are captured as a pre-refactor green baseline that guards Phase 31's settings extraction (RFCT-01).
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-04-SUMMARY.md` when done.
</output>
