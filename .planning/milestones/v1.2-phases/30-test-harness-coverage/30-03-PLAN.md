---
phase: 30-test-harness-coverage
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/pdf-bold-rendering.test.js
  - tests/pdf-digit-order.test.js
  - tests/pdf-glyph-coverage.test.js
  - tests/pdf-latin-regression.test.js
  - tests/quick-260522-iwr-ordered-list-export.test.js
  - tests/quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix.test.js
  - tests/quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression.test.js
  - tests/30-rtl-guard.test.js
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "All 7 previously-unrunnable PDF tests run green via the shared helper, with getContext stubbed and no /tmp jsdom dependency (D-04, D-06, TEST-01)"
    - "An automated RTL guard fails if dir=rtl is applied to a non-Hebrew locale (he→rtl, en/de/cs→ltr) (D-11, TEST-02)"
    - "Helper adoption turned no green test red (D-06)"
  artifacts:
    - path: tests/30-rtl-guard.test.js
      provides: "4-locale guard executing the real App.setLanguage dir code path"
    - path: tests/pdf-bold-rendering.test.js
      provides: "Migrated to the shared jsdom helper (representative of all 7)"
  key_links:
    - from: tests/pdf-bold-rendering.test.js
      to: tests/_helpers/jsdom-pdf-env.js
      via: "require('./_helpers/jsdom-pdf-env.js') replaces the inline buildJsdomEnv + legacy env fallback"
      pattern: "jsdom-pdf-env"
    - from: tests/30-rtl-guard.test.js
      to: assets/app.js
      via: "loads the real setLanguage (app.js:124) and asserts document.documentElement dir per locale"
      pattern: "setLanguage"
---

<objective>
Deliver the two TEST-01 / TEST-02 wins now that the shared helper exists:
1. Migrate the 7 currently-unrunnable PDF tests onto `tests/_helpers/jsdom-pdf-env.js` so they acquire the `getContext`→null stub and drop the fragile `/tmp` jsdom convention (D-04, D-06, TEST-01).
2. Add `tests/30-rtl-guard.test.js` — an automated regression guard that executes the REAL `App.setLanguage` dir code path across all 4 locales and FAILS if `dir="rtl"` is ever applied to a non-Hebrew locale (D-11, TEST-02).

No production code changes: the PDF tests are test-file edits; the RTL guard loads `assets/app.js` read-only.

Purpose: Close TEST-01 (the 7 PDF tests green under `npm test`) and TEST-02 (the RTL regression guard) — two of the four phase requirements, both small and well-bounded.
Output: 7 migrated PDF tests + `tests/30-rtl-guard.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<!-- planner-discipline-allow: /tmp/node_modules/jsdom -->

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 7 PDF tests onto the shared jsdom helper (TEST-01, D-04/D-06)</name>
  <files>tests/pdf-bold-rendering.test.js, tests/pdf-digit-order.test.js, tests/pdf-glyph-coverage.test.js, tests/pdf-latin-regression.test.js, tests/quick-260522-iwr-ordered-list-export.test.js, tests/quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix.test.js, tests/quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression.test.js</files>
  <read_first>
    - tests/quick-260620-q8m-pdf-paragraph-linebreaks.test.js (the already-green reference that stubs getContext — the target shape)
    - tests/_helpers/jsdom-pdf-env.js (the shared helper built in 30-02; its `buildJsdomEnv()` return shape)
    - tests/pdf-bold-rendering.test.js (and each of the other 6 — the inline buildJsdomEnv + the legacy jsdom env fallback block each currently carries)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§7 MODIFIED PDF tests — the 3-step migration recipe and the D-06 "do not turn green red" guardrail; Pitfall 2 — verify exit code, not stderr volume)
  </read_first>
  <action>
    In each of the 7 PDF test files, replace the test's own jsdom bootstrap with the shared helper:
    delete the file's inline `buildJsdomEnv` function and the legacy jsdom-acquisition block (the `JSDOM_PATH` environment fallback that resolves jsdom from the `/tmp` node-modules location), and instead `require('./_helpers/jsdom-pdf-env.js')` and call its exported `buildJsdomEnv()`.
    Keep every existing behavior assertion in each file UNCHANGED — the only functional change is each test acquiring the `getContext`→null stub (today `getContext` count is 0 in all 7) and resolving jsdom from the installed devDependency via the helper.
    D-06 guardrail: do NOT let adoption turn any green test red. If a file's inline env differs materially from the reference shape (extra font, custom URL), adapt the call site to the helper rather than weakening the assertions; if a file genuinely cannot use the shared helper without changing behavior, leave it on its inline env and note why in the SUMMARY — never break a green test to consolidate.
    Ensure no occurrence of the legacy `/tmp` jsdom path string remains in any of the 7 files (including in comments).
  </action>
  <verify>
    <automated>for f in tests/pdf-bold-rendering.test.js tests/pdf-digit-order.test.js tests/pdf-glyph-coverage.test.js tests/pdf-latin-regression.test.js tests/quick-260522-iwr-ordered-list-export.test.js tests/quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix.test.js tests/quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression.test.js; do node "$f" || exit 1; done; echo "all 7 green"; grep -rl '/tmp/node_modules/jsdom' tests/pdf-bold-rendering.test.js tests/pdf-digit-order.test.js tests/pdf-glyph-coverage.test.js tests/pdf-latin-regression.test.js tests/quick-260522-iwr-ordered-list-export.test.js tests/quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix.test.js tests/quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression.test.js && echo "LEAK" || echo "no /tmp leak"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 `node tests/<file>.test.js` exits 0 with its original behavior assertions intact (falsifiable: a broken migration changes an assertion outcome → non-zero exit).
    - No occurrence of the legacy `/tmp` jsdom path remains in any of the 7 files — the grep over all 7 returns no matches ("no /tmp leak").
    - Each of the 7 requires `./_helpers/jsdom-pdf-env.js` and the prior `Not implemented: HTMLCanvasElement's getContext()` stderr noise is gone (Pitfall 2 — exit code, not stderr volume, is the pass signal).
    - No `assets/*` production file modified. `npm test` stays green.
  </acceptance_criteria>
  <done>All 7 PDF tests run green via the shared helper, with the getContext stub acquired and the /tmp jsdom dependency removed; no green test turned red.</done>
</task>

<task type="auto">
  <name>Task 2: Add the RTL regression guard across 4 locales (TEST-02, D-11)</name>
  <files>tests/30-rtl-guard.test.js</files>
  <read_first>
    - assets/app.js (lines 116-126 — `setLanguage` and the `document.documentElement.setAttribute("dir", currentLang === "he" ? "rtl" : "ltr")` code path at :124; lines 1346-1347 — the `window.App = { setLanguage, getLanguage, ... }` export; read-only, do NOT modify)
    - tests/29-02-migration-escape-hatch.test.js (case 5 — the existing `dir=rtl` assertion shape and the minimal DOM stub)
    - tests/25-11-i18n-parity.test.js (lines 25-43 — the vm 4-locale loader; lines 53-65 — the PASS/FAIL counter + exit-0/1 scaffold)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-rtl-guard.test.js — analogs, the 4-locale sweep, the falsifiability note)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Code Examples → RTL guard)
  </read_first>
  <action>
    Create `tests/30-rtl-guard.test.js` implementing D-11. Load the REAL dir-applying code path from `assets/app.js` (the `setLanguage` function that sets `document.documentElement`'s `dir` to `rtl` only when the language is `he`, exported as `window.App.setLanguage`/`getLanguage`). Prefer jsdom (a real `document.documentElement`) since the behavior under test is a DOM attribute; the `29-02` minimal-DOM stub recording `setAttribute` is the acceptable zero-jsdom fallback if app.js loads cleanly in a stub.
    Sweep all 4 locales: for `he` assert the resulting `document.documentElement` `dir` is `rtl`; for `en`, `de`, and `cs` assert it is `ltr`. The test MUST fail if `rtl` is applied to any non-Hebrew locale — this is the regression it guards (falsifiable: mutating the production condition to e.g. `!== 'en'` flips de/cs to rtl and the de/cs assertions fail).
    Use the repo's PASS/FAIL counter + `process.exit(failed>0?1:0)` scaffold. Open the file with the root-cause / fix / falsifiability doc block per `feedback-behavior-verification`. Load `assets/app.js` unmodified.
  </action>
  <verify>
    <automated>node tests/30-rtl-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-rtl-guard.test.js` exits 0 against the current `assets/app.js`, asserting he→`rtl` and en/de/cs→`ltr` by reading the observable `document.documentElement` `dir` after calling the real `App.setLanguage` for each locale.
    - The test EXECUTES the real `setLanguage` (not a re-implementation) and FAILS if `rtl` is applied to a non-Hebrew locale (falsifiable by mutating the locale condition in a scratch copy).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The RTL guard executes the real App.setLanguage dir path across 4 locales and fails on any non-Hebrew rtl application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | The RTL guard and the PDF tests LOAD `assets/app.js` / `assets/pdf-export.js` / jsPDF into jsdom read-only; no write-back |
| dev workbench → production | All edited/new files are `tests/*`; never deployed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | test files shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-04 | Tampering | helper consolidation silently weakens a PDF test | low | mitigate | D-06 guardrail: behavior assertions kept unchanged; the verify runs all 7 and a /tmp-leak grep; any test that cannot migrate cleanly stays on its inline env |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-rtl-guard.test.js` (NEW — TEST-02, D-11): 4-locale guard over the real `App.setLanguage` dir path
- 7 MODIFIED PDF tests adopting `tests/_helpers/jsdom-pdf-env.js` (TEST-01, D-04/D-06): `pdf-bold-rendering`, `pdf-digit-order`, `pdf-glyph-coverage`, `pdf-latin-regression`, `quick-260522-iwr-ordered-list-export`, `quick-260608-c8x-pdf-list-typed-ordinal-and-rtl-prefix`, `quick-260608-cx5-pdf-rtl-ordered-list-unified-row-regression`

(Full phase manifest is listed in 30-01-PLAN.md.)
</artifacts>

<verification>
- All 7 migrated PDF tests exit 0; no `/tmp` jsdom path remains (Task 1).
- `node tests/30-rtl-guard.test.js` exits 0 and is falsifiable on a non-Hebrew rtl mutation (Task 2).
- `npm test` stays green.
- No `assets/*` production file modified.
</verification>

<success_criteria>
- TEST-01: the 7 PDF tests run green via the shared helper, getContext stubbed, no /tmp dependency, no green test turned red (D-04/D-06).
- TEST-02: an automated RTL guard fails if dir=rtl is applied to a non-Hebrew locale (D-11).
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-03-SUMMARY.md` when done.
</output>
