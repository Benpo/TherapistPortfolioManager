---
phase: 30-test-harness-coverage
plan: 09
type: execute
wave: 2
depends_on: ["30-07"]
files_modified:
  - tests/30-backups-helper-gate.test.js
  - tests/30-photos-optimize-loop.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-backups-helper-gate.test.js executes the REAL assets/settings.js IIFE-4 under jsdom and forces the REAL D-18 gate by stubbing BackupManager.canEnableSchedule→false (settings.js:2196-2198), then asserts the rejection wiring — schedulePasswordError shown (:2204), select reverted to prior (:2205), returns false, and NO schedule.savedToast (:2264-2266) — plus the frequency helper-text wiring (helperOff/helperOn) and the ack un-check force-off; the defensive readPasswordAcked fallback at :2201 is explicitly NOT the target (closes GAP-08 / region A4)"
    - "tests/30-photos-optimize-loop.test.js executes the REAL _optimizeAllPhotosLoop body from assets/settings.js (via window.__PhotosTabHelpers, NO monkey-patch) using the 30-07 Task-0 faithful base64 codec, and asserts persist-only-if-strictly-smaller with an EXACT savedBytes computed from real base64 lengths (not >0), the no-shrink no-op, the real dataURLToBlob/blobToDataURL round-trip, and one handleOptimize success path exercising the real dependency assembly (settings.js:2834-2839 → inline preview pill + success toast) (closes GAP-09 / region A5)"
    - "every new test in this plan has a recorded mutation-kill (G1): the guarded behavior reverted in a scratch copy makes the test exit non-zero, restored → exit 0, noted in verify + SUMMARY"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-backups-helper-gate.test.js
    - tests/30-photos-optimize-loop.test.js
  key_links:
    - "GAP-08 forces the REAL gate by stubbing BackupManager.canEnableSchedule→false (the roundtrip buildEnv stubs it true, making the rejection branch unreachable; passwordAcked='false' only reaches the defensive fallback at :2201 that production never executes because BackupManager is present)"
    - "GAP-09 uses vm-exec calling the REAL loop helper directly with injected stubs (resize returns a strictly-smaller blob) and the REAL dataURLToBlob/blobToDataURL fed by the 30-07 Task-0 base64 codec — never the 25-11 constant-output fakes"
    - "both assert observable output (DOM text/visibility, EXACT persisted bytes, returned counts), never internal function names"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without also executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "do NOT monkey-patch the photos optimize-loop helper to reject (that is the existing 25-11 gap); EXECUTE the real loop body"
    - "do NOT copy the 25-11 FileReader/atob/btoa/Blob fakes (they return CONSTANT output, making savedBytes meaningless) — use the 30-07 Task-0 faithful base64 codec (or real-page jsdom)"
    - "GAP-08 must NOT target the defensive readPasswordAcked fallback at settings.js:2201 — it must force the real BackupManager gate"
    - "do not re-test the already-pinned A4 custom-days-visibility / schedule-saved-toast or A5 render/verdict/language-rerender behaviors; do not modify any assets/* production file"
---

<objective>
Strengthen the two `assets/settings.js` regions whose named behaviors never actually execute: GAP-08 (A4 backups — the frequency helper-text wiring, the password-ack gate-block REJECTION path, and the ack un-check force-off; both existing covering tests pre-seed passwordAcked='true' so the rejection branch is unhit) and GAP-09 (A5 photos — the real `_optimizeAllPhotosLoop` body, the dataURL adapters, and the optimize SUCCESS path; every existing test either declines the confirm or monkey-patches the loop to reject, so the body never runs).

GAP-08 follows D-09 (real-page jsdom, invoke ONLY the IIFE-4 handler) AND forces the REAL gate by stubbing BackupManager.canEnableSchedule→false — because settings.js:2196-2198 gates on BackupManager.canEnableSchedule whenever BackupManager exists, the roundtrip buildEnv (which stubs it true) makes the rejection branch unreachable, and passwordAcked='false' only reaches the defensive fallback at :2201 that production never executes. GAP-09 follows the vm-exec pattern (call the exported real loop helper directly with injected stubs and the REAL dataURL adapters fed by the 30-07 Task-0 faithful base64 codec) so the loop body genuinely executes and savedBytes is truthful.

This plan is **wave 2** and depends on 30-07 Task 0 for the faithful base64 codec.

Purpose: pin the backup gating and the photo-optimize loop so Phase 31 cannot silently break them.
Output: 2 new `tests/30-*.test.js` files, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-settings-section-roundtrip.test.js
@tests/25-11-toast-behavior.test.js
@tests/_helpers/base64-codec.js
@assets/settings.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-08 — characterize backups helper-text + password-gate rejection (region A4)</name>
  <files>tests/30-backups-helper-gate.test.js</files>
  <read_first>
    - assets/settings.js:2189-2226 (applyFrequencyChange: the D-18 gate at :2194-2202 — `if (BackupManager && typeof BackupManager.canEnableSchedule === 'function') gateAllowed = BackupManager.canEnableSchedule(newMode); else gateAllowed = (newMode==='off') || readPasswordAcked();` — so when BackupManager exists the REAL path uses canEnableSchedule and the readPasswordAcked branch at :2201 is dead; rejection at :2203-2226 shows schedulePasswordError + reverts the select + returns false), :2257-2266 (on success: refreshFrequencyHelper + App.showToast('', 'schedule.savedToast'))
    - assets/settings.js:2166-2173 (refreshFrequencyHelper mode→helperOff/helperOn); ack-checkbox un-ack force-off at :2316-2329
    - tests/30-settings-section-roundtrip.test.js:94-130 (settings.html buildEnv; capture handlers, invoke ONLY captured[3]; the buildEnv's BackupManager stub returns canEnableSchedule:true — this test must OVERRIDE it to false)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-08 entry — note 25-04/25-05 load assets/backup.js, a DIFFERENT module; do not rely on them)
  </read_first>
  <behavior>
    - Helper text: after a frequency mode change, the schedule frequency helper element's text/data-i18n reflects helperOff vs helperOn for the off vs non-off modes.
    - Password-gate REJECTION (the real production path): with BackupManager.canEnableSchedule stubbed to return false, dispatching a change to a non-off frequency shows schedulePasswordError (:2204), reverts the select to its prior value (:2205), the change returns false, and produces NO schedule.savedToast (:2264-2266). The defensive readPasswordAcked fallback at :2201 is NOT the target — BackupManager must be present so the real gate runs.
    - Ack un-check force-off: un-checking the ack checkbox forces the schedule back off (observable select/state).
  </behavior>
  <action>
    Build the settings.html env; capture handlers (keep the captured.length===5 self-check) and invoke ONLY captured[3] (IIFE-4 backups). CRITICAL: override the buildEnv BackupManager stub so `canEnableSchedule(newMode)` returns FALSE for non-off modes (this forces the real gate at settings.js:2196-2198 to reject, instead of the buildEnv default of true). Case 1: change the frequency select mode and assert the helper element's resolved text/data-i18n (helperOff for off, helperOn for non-off). Case 2 (the real rejection): dispatch a change to a non-off frequency → assert schedulePasswordError is un-hidden, the select value reverts to its prior value, and App.showToast did NOT record 'schedule.savedToast'. Case 3: un-check the ack checkbox → assert the schedule is forced off. Assert observable DOM / toast-spy only. Standard doc block (root-cause: the rejection branch was unreachable because the gate was stubbed permissive) + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-backups-helper-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-backups-helper-gate.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/settings.js'))` AND invokes captured[3] AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-backups-helper-gate.test.js` is non-empty
    - FORCES THE REAL GATE: BackupManager.canEnableSchedule is stubbed to return false (so the gate at settings.js:2196-2198 rejects via the real path, NOT the :2201 readPasswordAcked fallback)
    - BEHAVIORAL: assertion on the frequency helper element text/data-i18n for off vs non-off; assertion that the gate rejection shows schedulePasswordError + reverts the select + emits NO 'schedule.savedToast'; assertion of the ack un-check force-off
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of settings.js for assertion, assertions on internal function names; must NOT assert against assets/backup.js; must NOT rely on the :2201 fallback path
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, drop the `if (!gateAllowed) { ... return false; }` rejection block → the rejection case exits non-zero (savedToast now fires / select not reverted); restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>A4 helper-text wiring + the REAL password-gate rejection (forced via canEnableSchedule→false) + ack force-off are pinned by observable DOM/toast; a Phase-31 break of the gate would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-09 — execute the photos optimize-loop body + dataURL adapters + success path (region A5)</name>
  <files>tests/30-photos-optimize-loop.test.js</files>
  <read_first>
    - assets/settings.js:2451-2459 (dataURLToBlob: atob + Blob), :2461-2468 (blobToDataURL: FileReader.readAsDataURL), :2512-2541 (the optimize loop body: origBytes = floor(base64Len*0.75), resize→outBlob→outDataURL, newBytes = floor(newBase64Len*0.75), persist + savedBytes += (origBytes-newBytes) ONLY when newBytes < origBytes), exports on window.__PhotosTabHelpers at :2544-2552; handleOptimize dependency assembly at :2831-2840 (passes PortfolioDB.getAllClients/updateClient, CropModule.resizeToMaxDimension, h.blobToDataURL, h.dataURLToBlob) → inline preview pill + success toast at :2856-2883
    - tests/25-11-toast-behavior.test.js:38,329-334 (the vm.runInContext load of settings.js + access to ctx.sandbox.window.__PhotosTabHelpers — copy ONLY the vm-load mechanics) — WARNING: 25-11:273-287 defines FAKE Blob/FileReader/atob/btoa that return CONSTANT output; do NOT copy those. 25-11:434 monkey-patches the loop; do NOT copy that either.
    - tests/_helpers/base64-codec.js (30-07 Task 0 — faithful Buffer-backed atob/btoa/Blob/FileReader; install THESE on the vm sandbox so dataURLToBlob/blobToDataURL produce truthful base64 lengths)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-09 entry — note 25-06 loads crop.js, 25-07 loads db.js; both off-region)
  </read_first>
  <behavior>
    - Strictly-smaller persistence with EXACT savedBytes: with resize returning a blob strictly smaller than the input, the loop persists the optimized photo (updateClient called with the smaller dataURL) and returns savedBytes EXACTLY equal to (origBytes − newBytes) computed from the real base64 lengths via the faithful codec (assert the exact integer, NOT savedBytes > 0).
    - No-shrink no-op: with resize returning a blob the same size or larger, the loop does NOT persist (no updateClient write for that client) and savedBytes stays 0.
    - dataURL round-trip: the REAL dataURLToBlob/blobToDataURL pair is exercised inside the loop (fed by the faithful codec, not the constant-output fakes).
    - Success path (Case 3 — keep, do not drop "within budget"): a real handleOptimize run with confirmDialog→true wires the real dependency assembly (settings.js:2831-2840) and renders the inline preview pill text + success toast — this catches a wrong-args wiring break in handleOptimize that calling the loop directly would miss.
  </behavior>
  <action>
    Load settings.js via vm.createContext + vm.runInContext (the 25-11 load mechanics) so window.__PhotosTabHelpers is available WITHOUT monkey-patching the loop, and install the 30-07 Task-0 FAITHFUL base64 codec (atob/btoa/Blob/FileReader) on the sandbox so the real adapters compute truthful bytes. Seed clients with real data-URL photoData of known base64 length. Case 1: call the real loop helper h with (getAllClients, updateClient, resize→a strictly-smaller blob of known bytes, h.blobToDataURL, h.dataURLToBlob); compute the EXPECTED savedBytes the same way the loop does (floor(origBase64Len*0.75) − floor(newBase64Len*0.75)) and assert updateClient received the smaller dataURL AND result.savedBytes === that exact integer AND result.success === 1. Case 2: resize→a same-or-larger blob → assert NO updateClient write for that client + result.savedBytes === 0 (no-shrink no-op). Use the REAL h.dataURLToBlob/h.blobToDataURL (do not stub those two). Case 3: drive the real handleOptimize success path (confirmDialog→true, CropModule.resizeToMaxDimension returning a strictly-smaller blob) so the real dependency assembly at settings.js:2831-2840 runs, and assert the inline preview pill text + success toast (use whichever exposed handler / real-page jsdom the load supports). Standard doc block (root-cause: the loop body never executed before; the prior fakes made savedBytes meaningless) + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-photos-optimize-loop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-photos-optimize-loop.test.js` exits 0
    - STRUCTURAL: loads settings.js via `vm.runInContext` (or win.eval) AND calls the REAL loop helper on `window.__PhotosTabHelpers` (not a monkey-patched replacement) AND installs the faithful base64 codec (NOT the 25-11 constant-output Blob/FileReader/atob/btoa) AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-photos-optimize-loop.test.js` is non-empty
    - BEHAVIORAL: assertion that a strictly-smaller resize triggers an updateClient persist with the smaller dataURL AND result.savedBytes equals the EXACT integer computed from the real base64 lengths (not >0); assertion that a no-shrink resize results in NO persist + savedBytes === 0; assertion exercising the REAL dataURLToBlob/blobToDataURL (no stub of those two); Case 3 assertion that the real handleOptimize dependency assembly renders the inline preview pill text + success toast
    - the test does NOT overwrite/monkey-patch the loop helper (`grep -c '__PhotosTabHelpers._optimizeAllPhotosLoop =' tests/30-photos-optimize-loop.test.js` is 0)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, regex-over-source assertions, assertions on internal function names; the 25-11 constant-output FileReader/atob/btoa/Blob fakes; must NOT assert against assets/crop.js or assets/db.js
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, change the loop's `newBytes < origBytes` guard to always-persist (or drop the savedBytes accumulation) → the exact-savedBytes / no-shrink-no-op case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>The photos optimize-loop body (with truthful byte math), the real dataURL adapters, and the real handleOptimize success-path dependency assembly actually execute and are pinned; a Phase-31 break of the shrink-only persist, the savedBytes math, or the handleOptimize arg wiring would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-backups-helper-gate.test.js (GAP-08, region A4)
- tests/30-photos-optimize-loop.test.js (GAP-09, region A5)

**Consumes (from 30-07 Task 0, not modified here):** tests/_helpers/base64-codec.js (faithful codec for the GAP-09 vm sandbox)

**Modified files:** none (no assets/* changes; 25-04/25-05/25-06/25-07/25-12 backup/photo tests retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval/vm-load trusted local production modules into an isolated context; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-09 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A. |
</threat_model>

<verification>
- `node tests/30-backups-helper-gate.test.js` and `node tests/30-photos-optimize-loop.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file; the optimize-loop test does not monkey-patch the loop
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
A4 backups gating and A5 photos optimize-loop are guarded by tests that execute the real code (not a stubbed/declined path) and assert observable behavior; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-09-SUMMARY.md` when done.
</output>
