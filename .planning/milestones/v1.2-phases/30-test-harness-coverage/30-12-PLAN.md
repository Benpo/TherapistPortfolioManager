---
phase: 30-test-harness-coverage
plan: 12
type: execute
wave: 2
depends_on: ["30-07"]
files_modified:
  - tests/quick-260615-export-section-order.test.js
  - tests/quick-260615-share-files-only.test.js
  - tests/25-12-optimize-placeholders.test.js
  - tests/quick-260516-g7p-save-returns-to-session.test.js
  - tests/30-save-redirect.test.js
  - tests/25-11-toast-behavior.test.js
  - tests/30-settings-save-failed-toast.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "the two confirmed FAKE source-slicing tests are deleted: tests/quick-260615-export-section-order.test.js (GAP-05) and tests/quick-260516-g7p-save-returns-to-session.test.js (GAP-06)"
    - "the two structural-redundant tests are retired: tests/quick-260615-share-files-only.test.js and tests/25-12-optimize-placeholders.test.js (GAP-16)"
    - "tests/30-save-redirect.test.js executes the REAL add-session.js and asserts the post-save redirect DESTINATION URL (./add-session.html?sessionId=<id>, add-session.js:1768) captured via a settable window.location.href setter (25-01-share-fallback:108 / 29-03-report-wiring:266-268 pattern), advancing the 600ms setTimeout (add-session.js:1766-1769) with fake timers — NOT satisfied by asserting the toast.sessionSaved toast (GAP-06 replacement)"
    - "tests/25-11-toast-behavior.test.js Scenario 5 (hand-rewritten try/catch source-slice) is removed and replaced by tests/30-settings-save-failed-toast.test.js, which forces PortfolioDB.setTherapistSetting to throw and asserts the REAL onSave catch shows settings.save.failed; scenarios 1-4,6 are kept (GAP-15)"
    - "each replacement test (30-save-redirect, 30-settings-save-failed-toast) has a recorded mutation-kill (G1): the guarded behavior reverted in a scratch copy makes the test exit non-zero, restored → exit 0, noted in verify + SUMMARY"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-save-redirect.test.js
    - tests/30-settings-save-failed-toast.test.js
  key_links:
    - "this fake-cleanup plan is wave 2 and MUST land before the wave-3 fake-detector gate in 30-13 (the gate fails the pure source-slicers until they are gone); 30-13 depends_on 30-12"
    - "the replacement tests execute the real module and assert observable output (the captured redirect DESTINATION URL via a settable window.location, toast text), never source text"
  prohibitions:
    - "the two replacement tests must NOT reintroduce source-slicing — no SRC.indexOf( / SRC.slice( / regex-over-source"
    - "do not delete or weaken the legit static removal/audit guards (25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed) — those are valid and stay"
    - "keep 25-11-toast-behavior scenarios 1-4 and 6 (real photos-tab tests) intact; only remove the fake Scenario 5"
---

<objective>
Remove the two confirmed fake (source-slicing) tests and the two structural-redundant tests, and replace the two pieces of REAL coverage they pretended to provide with executing tests. This is the cleanup the whole gap round exists to enable, and it MUST precede the permanent fake-detector gate (30-13) — the gate will fail the pure source-slicers until they are gone. This plan is **wave 2** (depends on 30-07 Task 0 for the shared helpers) and lands before the **wave-3** detector gate in 30-13.

- GAP-05: DELETE tests/quick-260615-export-section-order.test.js (regex over source; superseded by the real tests/30-export-markdown.test.js).
- GAP-06: DELETE tests/quick-260516-g7p-save-returns-to-session.test.js (re-implements the redirect from a source regex); REPLACE with a real post-save redirect test.
- GAP-15: remove the fake Scenario 5 from tests/25-11-toast-behavior.test.js (hand-rewritten try/catch over a source-slice) and REPLACE with a real onSave-catch test; keep scenarios 1-4,6.
- GAP-16: retire tests/quick-260615-share-files-only.test.js and tests/25-12-optimize-placeholders.test.js (redundant structural guards, superseded by the 30-* suite).

Purpose: eliminate the false-confidence tests and replace their real intent with execution.
Output: 4 files removed/edited + 2 new executing tests, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-export-markdown.test.js
@tests/30-settings-section-roundtrip.test.js
@tests/25-11-toast-behavior.test.js
@assets/add-session.js
@assets/settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAP-05 + GAP-16 — delete the fake + redundant structural tests</name>
  <files>tests/quick-260615-export-section-order.test.js, tests/quick-260615-share-files-only.test.js, tests/25-12-optimize-placeholders.test.js</files>
  <read_first>
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-05 and GAP-16 entries — GAP-05 is the confirmed fake superseded by 30-export-markdown; GAP-16 retires two redundant structural guards)
    - tests/30-export-markdown.test.js (the real test that supersedes the section-order fake)
    - tests/30-export-stepper.test.js (case C covers files-only share, superseding quick-260615-share-files-only)
  </read_first>
  <action>
    Delete the three files: tests/quick-260615-export-section-order.test.js (GAP-05), tests/quick-260615-share-files-only.test.js and tests/25-12-optimize-placeholders.test.js (GAP-16). Confirm each is a pure source-slicer / structural guard whose behavior is already pinned by a real 30-* test before removing (section order → 30-export-markdown; files-only share → 30-export-stepper case C; optimize placeholders → the floor/stale-estimate runtime tests). Do not touch any other file.
  </action>
  <verify>
    <automated>test ! -e tests/quick-260615-export-section-order.test.js && test ! -e tests/quick-260615-share-files-only.test.js && test ! -e tests/25-12-optimize-placeholders.test.js && echo DELETED</automated>
  </verify>
  <acceptance_criteria>
    - the three files no longer exist (the verify command prints DELETED)
    - the real superseding coverage is present and green: tests/30-export-markdown.test.js and tests/30-export-stepper.test.js still pass
    - the legit static guards (25-08-single-source-audit, 25-11-hardcoded-english-removed, 25-12-folder-picker-removed) are NOT deleted and remain green
    - `npm test` stays green (exit 0) with the reduced file count
  </acceptance_criteria>
  <done>The section-order fake and the two redundant structural guards are gone; their behavior remains covered by the real 30-* suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-06 — delete the g7p save-redirect fake + add a real redirect test</name>
  <files>tests/quick-260516-g7p-save-returns-to-session.test.js, tests/30-save-redirect.test.js</files>
  <read_first>
    - tests/quick-260516-g7p-save-returns-to-session.test.js (the fake: source-slices the save handler at :65-72 and re-implements the navigation from a regex at :85-100 — confirm it never eval/vm/jsdom-loads the module)
    - assets/add-session.js:1751-1769 (the real save handler: addSession → savedId → App.showToast("", "toast.sessionSaved") at :1764 → `setTimeout(() => { window.location.href = \`./add-session.html?sessionId=${savedId}\`; }, 600)` at :1766-1769 — the redirect destination to assert is `./add-session.html?sessionId=<savedId>`; note it fires in a 600ms timer, NOT a 0ms one)
    - tests/25-01-share-fallback.test.js:108-118 and tests/29-03-report-wiring.test.js:266-268 (the SETTABLE window.location pattern: delete win.location then Object.defineProperty an href getter/setter that records assignments into an array)
    - tests/30-issue-delta.test.js:81-135 (buildEnv real-page pattern; seedClientAndDate so submit reaches the save) and tests/_helpers/mock-portfolio-db.js (seed so addSession resolves a savedId)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-06 entry)
  </read_first>
  <behavior>
    - Post-save redirect (DESTINATION URL): after a real form submit that successfully persists the session (addSession resolves savedId), advancing the 600ms timer causes window.location.href to be set to `./add-session.html?sessionId=<savedId>` — the test captures and asserts that exact destination.
  </behavior>
  <action>
    Delete tests/quick-260516-g7p-save-returns-to-session.test.js. Create tests/30-save-redirect.test.js using the real-page jsdom buildEnv. BEFORE eval, install a SETTABLE window.location: `delete win.location` then Object.defineProperty(win, 'location', {...}) (or redefine win.location.href) with an href setter that pushes each assigned value into a captured array (the 25-01/29-03 pattern) — jsdom's default location is not writable, so this redefinition is required to observe the redirect. Make the 600ms timer deterministic: override win.setTimeout so the redirect callback can be invoked on demand (capture-and-run, or a controllable fake timer) — the existing settle() only flushes 0ms timers and will NOT fire the 600ms redirect. Seed mockDb + a valid client + date so the real submit reaches addSession; submit via the real submit control, settle(), advance/run the 600ms timer, then assert the captured location.href destination equals `./add-session.html?sessionId=<savedId>` (savedId from the addSession spy). The save toast (toast.sessionSaved) MUST NOT be the thing asserted — asserting the destination URL is the required artifact. Standard doc block (root-cause: replaces the g7p source-slice) + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>test ! -e tests/quick-260516-g7p-save-returns-to-session.test.js && node tests/30-save-redirect.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/quick-260516-g7p-save-returns-to-session.test.js no longer exists; `node tests/30-save-redirect.test.js` exits 0
    - STRUCTURAL: tests/30-save-redirect.test.js contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-save-redirect.test.js` is non-empty
    - REDIRECT CAPTURE: the test redefines a settable window.location.href setter (records assignments) AND advances/runs the 600ms timer (does not rely on settle()'s 0ms flush)
    - BEHAVIORAL (checkable artifact): asserts the captured destination URL equals `./add-session.html?sessionId=<savedId>` (the savedId from the addSession spy) — NOT merely that the save toast fired
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, regex-over-source, re-implemented navigation logic, satisfying the test by asserting toast.sessionSaved
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, change the redirect target (e.g. drop the `?sessionId=` query or point elsewhere) → the destination assertion exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>The g7p source-slicing fake is replaced by a test that executes the real save+redirect and asserts the captured destination URL after advancing the real 600ms timer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-15 — replace 25-11 Scenario 5 with a real settings save-failed-toast test</name>
  <files>tests/25-11-toast-behavior.test.js, tests/30-settings-save-failed-toast.test.js</files>
  <read_first>
    - tests/25-11-toast-behavior.test.js:569-592 (Scenario 5: source-slices App.showToast("", "settings.save.failed") then replays a hand-written try/catch — the fake sub-case to remove; scenarios 1-4,6 are real photos-tab tests to KEEP)
    - assets/settings.js:518-522 (the REAL onSave catch that shows settings.save.failed)
    - tests/30-settings-section-roundtrip.test.js:94-130 (settings.html buildEnv; invoke ONLY captured[0] = IIFE-1; the mock-DB write-mirror wrapper)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-15 entry)
  </read_first>
  <behavior>
    - Real onSave catch: when PortfolioDB.setTherapistSetting throws during Save, the REAL onSave catch shows the settings.save.failed toast (App.showToast spy records the failed key).
  </behavior>
  <action>
    Edit tests/25-11-toast-behavior.test.js to remove ONLY Scenario 5 (the hand-rewritten try/catch source-slice and its narration) and adjust that file's scenario count/guard accordingly; leave scenarios 1-4 and 6 intact and green. Create tests/30-settings-save-failed-toast.test.js using the settings.html real-page buildEnv: inject a mock PortfolioDB whose setTherapistSetting REJECTS/THROWS, invoke ONLY the IIFE-1 handler, perform a Save via the real onSave path, settle(), and assert App.showToast recorded the settings.save.failed key (observable). Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-settings-save-failed-toast.test.js && node tests/25-11-toast-behavior.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-save-failed-toast.test.js` exits 0 and `node tests/25-11-toast-behavior.test.js` exits 0 (scenarios 1-4,6 still pass)
    - 25-11-toast-behavior.test.js no longer contains the Scenario 5 hand-rewritten try/catch source-slice (`grep -c "settings.save.failed" tests/25-11-toast-behavior.test.js` reflects removal of the source-sliced sub-case; the file's scenario count guard is updated)
    - STRUCTURAL: tests/30-settings-save-failed-toast.test.js contains `win.eval(readAsset('assets/settings.js'))` AND invokes captured[0] AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-settings-save-failed-toast.test.js` is non-empty
    - BEHAVIORAL: assertion that the REAL onSave catch (DB throws) shows settings.save.failed via the App.showToast spy
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, hand-rewritten try/catch over source text
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, remove the settings.save.failed toast from the onSave catch (or swallow the error) → the test exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>The fake Scenario 5 is gone; the real onSave error path is pinned by executing the module with a throwing DB and asserting the failed toast; the other 25-11 scenarios remain green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-save-redirect.test.js (GAP-06 replacement)
- tests/30-settings-save-failed-toast.test.js (GAP-15 replacement)

**Deleted files:**
- tests/quick-260615-export-section-order.test.js (GAP-05)
- tests/quick-260615-share-files-only.test.js (GAP-16)
- tests/25-12-optimize-placeholders.test.js (GAP-16)
- tests/quick-260516-g7p-save-returns-to-session.test.js (GAP-06)

**Modified files:**
- tests/25-11-toast-behavior.test.js (GAP-15: remove fake Scenario 5, keep 1-4,6)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-12 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A. |
</threat_model>

<verification>
- the four deleted files are gone; `node tests/30-save-redirect.test.js` and `node tests/30-settings-save-failed-toast.test.js` exit 0
- `node tests/25-11-toast-behavior.test.js` exits 0 with Scenario 5 removed and 1-4,6 retained
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for both new files
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
The two fakes and two redundant structural guards are removed; their real intent (post-save redirect; settings save-failed toast) is pinned by executing tests; the legit static guards remain; the suite stays green via the single command. The tree is now clean for the fake-detector gate (30-13).
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-12-SUMMARY.md` when done.
</output>
