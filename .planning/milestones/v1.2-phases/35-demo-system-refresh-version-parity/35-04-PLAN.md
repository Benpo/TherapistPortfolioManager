---
phase: 35-demo-system-refresh-version-parity
plan: 04
type: execute
wave: 2
depends_on: [35-02]
files_modified:
  - assets/demo-seed-data.json
  - assets/demo-seed.js
autonomous: false
requirements: [DEMO-05, DEMO-06, DEMO-07]
nyquist_compliant: true

must_haves:
  truths:
    - "The seed showcases the flagship Heart Shield: one client has a coherent removal/progression arc (≥2 isHeartShield sessions, final shieldRemoved:true) so the dashboard shows the removed badge and each session its stage (D-03 / DEMO-05)."
    - "Session/client dates are computed at seed time from relative `daysAgo` offsets (noon-anchored), guaranteeing ≥1 session in the current calendar month forever — the demo never looks abandoned (D-06 / DEMO-06)."
    - "Every seeded session conforms exactly to the current db.js schema; client-type variety is kept and an `other`-type client/session is added (D-04 / DEMO-07)."
    - "Ben + Sapir approve the drafted seed on Ben's machine before it ships (D-05)."
  artifacts:
    - "assets/demo-seed-data.json (modified) — Heart-Shield arc, +1 `other`-type client + an `other` session-type, per-session integer `daysAgo` (one === 0), absolute `date` dropped"
    - "assets/demo-seed.js (modified) — new helpers isoDaysAgo(n, now?) + applyRelativeDates(sessions, now?), exposed as window.__demoSeedHelpers BEFORE the demo-mode early-return; seedData() applies the transform"
  key_links:
    - "Author the arc against overview.js's `.some()` client-badge logic (a single shieldRemoved:true flips the client row), NOT `.every()` — Pitfall 2."
    - "countSessionsThisMonth uses local `new Date(session.date)` — noon-anchored ISO dates avoid the month-edge timezone slip (Pitfall 5); set the newest session daysAgo:0."
  prohibitions:
    - "Never render seed strings via innerHTML — overview.js renders seed via textContent; keep it that way (T-XSS)."
    - "Never write seed into the real DB — preserve `demo_portfolio` vs `sessions_garden` window.name isolation (T-DBLEAK); the relative-date helpers must stay reachable in non-demo context for tests WITHOUT triggering a seed/write."
    - "No v1/simplified/placeholder seed — deliver the full Heart-Shield-forward dataset; the only gate is Ben+Sapir's clinical sign-off (D-05)."
---

<objective>
Refresh the demo seed (`assets/demo-seed-data.json` + the seeder `assets/demo-seed.js`) so the demo tells the current product's story: a Heart Shield removal arc (D-03), self-freshening relative dates (D-06), exact current-schema conformance plus an `other`-type client/session for variety (D-04/D-07) — drafted by Claude and signed off by Ben + Sapir before ship (D-05).

Purpose: The 4-month-stale dataset with zero Heart-Shield flags is the worst conversion-killer; this makes the demo showcase the flagship feature and never look abandoned again.
Output: A refreshed seed + a testable relative-date seam that turns the 35-02 seed gates GREEN — gated on human clinical approval.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md
@.planning/phases/35-demo-system-refresh-version-parity/35-CONTEXT.md

# Source of truth for schema + render + the seeder being changed:
@assets/db.js
@assets/overview.js
@assets/demo-seed.js
@assets/demo-seed-data.json
</context>

<artifacts_produced>
- `assets/demo-seed-data.json` — refreshed clients/sessions: Heart-Shield arc on one client, +1 `other`-type client, ≥1 `other` sessionType, per-session `daysAgo` (newest = 0), absolute `date` removed.
- `assets/demo-seed.js` — `isoDaysAgo(n, now?)`, `applyRelativeDates(sessions, now?)`, exposed via `window.__demoSeedHelpers` BEFORE the `window.name !== 'demo-mode'` early-return; `seedData()` applies `applyRelativeDates` before `addSession`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relative-date seam in demo-seed.js (DEMO-06)</name>
  <files>assets/demo-seed.js</files>
  <behavior>
    - isoDaysAgo(n, now): returns a YYYY-MM-DD string for `n` days before `now` (default new Date()), anchored at local noon so getMonth()/getFullYear() never slip across a UTC boundary.
    - applyRelativeDates(sessions, now): returns copies with `date = isoDaysAgo(s.daysAgo, now)` and `daysAgo` removed; pure, no IndexedDB.
    - window.__demoSeedHelpers exposes both, available even when window.name !== 'demo-mode' (so tests can call them without seeding).
    - For now at the 1st and last day of a month, applying the transform to the seed yields ≥1 session whose computed date is in `now`'s month (because newest daysAgo:0).
  </behavior>
  <read_first>
    - assets/demo-seed.js (the IIFE: demo-mode early-return L8; seedData() L28-44; addClient/addSession loop)
    - assets/overview.js (countSessionsThisMonth ~L562 — the consumer the dates must satisfy)
    - 35-RESEARCH.md ("Relative-date model" — the isoDaysAgo noon-anchor recommendation)
    - tests/35-demo-seed.test.js (the seam contract authored in 35-02)
  </read_first>
  <action>
    Add `isoDaysAgo(n, now)` and `applyRelativeDates(sessions, now)` to demo-seed.js as described in <behavior>. Expose `window.__demoSeedHelpers = { isoDaysAgo, applyRelativeDates }` at the TOP of the file, BEFORE the `if (window.name !== 'demo-mode') return` gate, so the helpers exist in any context and the test can exercise them without triggering a deleteDatabase/seed. In `seedData()`, run `sessions = applyRelativeDates(sessions)` (no explicit `now` → live Date) before the addSession loop, so each seeded session gets a freshly computed `date`. Keep `Date.now()`/`new Date()` here (normal runtime app code per D-06). Do not change the deleteDatabase-then-fetch flow.
  </action>
  <verify>
    <automated>node tests/35-demo-seed.test.js</automated>
  </verify>
  <acceptance_criteria>
    - DEMO-06 month-edge assertions GREEN for both 1st-of-month and last-of-month `now` fixtures.
    - __demoSeedHelpers is reachable with window.name unset and never opens IndexedDB in that path.
  </acceptance_criteria>
  <done>The seeder computes self-freshening noon-anchored dates and exposes a testable transform seam.</done>
</task>

<task type="auto">
  <name>Task 2: Author the refreshed seed dataset (DEMO-05/07, D-03/D-04)</name>
  <files>assets/demo-seed-data.json</files>
  <read_first>
    - assets/demo-seed-data.json (current 7 clients / 11 sessions; field shapes; existing client types adult×3/child×3/animal×1, no `other`)
    - assets/db.js (current schema: session keys, sessionType enum clinic|online|other, issues[{name,before,after}], isHeartShield/shieldRemoved)
    - assets/overview.js (client-badge .some() L393-408; per-session badge L499-508 — author the arc to these)
    - 35-RESEARCH.md ("Recommended Heart Shield arc", "Current Seed Schema")
  </read_first>
  <action>
    Rework the seed JSON (drafting clinically coherent English sample content per D-07):
    - Heart-Shield arc (D-03): convert one client (research suggests Anita C, adult, 3 sessions) to a coherent progression — session 1 `isHeartShield:true,shieldRemoved:false` (detected), session 2 `isHeartShield:true,shieldRemoved:false` (work in progress), session 3 `isHeartShield:true,shieldRemoved:true` (removed). Optionally add a second heart-shield client of a different type for variety. Author against `.some()` (final removed flips the client badge to ✅) — Pitfall 2.
    - Variety (D-04): keep adult/child/animal and ADD one `other`-type client, and include ≥1 session with `sessionType:"other"` (currently unshowcased). Keep "mostly just unstale it" — a few more sessions, not a brand-new dataset.
    - Dates (D-06): give every session an integer `daysAgo` and REMOVE the absolute `date` field; set the most-recent session `daysAgo:0`; spread the rest in chronological order up to ~90d (e.g. 0,4,12,23,38,55,70,84) preserving each client's order.
    - Schema (D-07): each session uses only allowed keys; sessionType ∈ {clinic,online,other}; issues[] entries only {name,before,after} with believable before→after drops; no legacy `heartWallCleared`/`heartWall`. Emotion names realistic (existing "Pressure — age 32\nOverwhelm…" shape is good). Keep clinical text English sample (localization deferred).
    - Keep `photoData` values as-is (do not regenerate base64).
  </action>
  <verify>
    <automated>node tests/35-demo-seed.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - DEMO-05 GREEN: a client owns ≥2 isHeartShield sessions with mixed shieldRemoved + final true; renderClientRows produces `.heart-badge-removed`; ≥1 active + ≥1 removed session present.
    - DEMO-07 GREEN: schema-union/enum/issues-shape/no-legacy + every session has `daysAgo` (no absolute `date`); an `other`-type client and an `other` sessionType exist.
    - Full suite green except this remains gated by Task 3 approval before ship.
  </acceptance_criteria>
  <done>The seed showcases a Heart-Shield arc, keeps type variety (+other), and is schema-clean with relative dates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Ben + Sapir clinical sign-off on the drafted seed (D-05)</name>
  <files>assets/demo-seed-data.json</files>
  <action>
    Pause execution before shipping the seed and request a BLOCKING human sign-off (D-05) — this checkpoint is never auto-approvable, regardless of auto_advance. Tasks 1-2 produced the drafted seed; present it to Ben (product) and Sapir (clinical domain owner) on Ben's machine using the steps below, and only proceed once BOTH approve. If revisions are requested, edit the sample wording/arc in `assets/demo-seed-data.json` (structure stays; the 35-02 seed gates stay GREEN) and re-request sign-off.
  </action>
  <what-built>
    A refreshed demo seed (`assets/demo-seed-data.json`) with a Heart-Shield removal arc, an added `other`-type client/session, self-freshening relative dates, and current-schema-clean English sample clinical content; the seeder now computes dates at load time.
  </what-built>
  <how-to-verify>
    On Ben's machine (real browser), open the demo (landing.html → #demo iframe, or demo.html directly) and review the seeded data as a prospective buyer + as the clinical domain owner (Sapir):
    1. Dashboard shows ≥1 "Session This Month" (not abandoned) and the client list renders.
    2. The Heart-Shield client shows the removed (✅) badge; opening that client shows sessions with active→removed stages telling a believable arc.
    3. The emotion names, before→after severity drops, insights, and comments read as clinically authentic sample content (Sapir signs off on authenticity).
    4. Client-type variety incl. the new `other`-type client and an `other`-type session is present.
    Approve only if both Ben (product) and Sapir (clinical) are satisfied; otherwise list the specific content to revise (structure is fine, only the sample wording/arc changes).
  </how-to-verify>
  <resume-signal>Type "approved" to ship the seed, or describe the specific clinical/content revisions needed.</resume-signal>
  <verify>
    <human-check>Ben (product) + Sapir (clinical) both confirm the Heart-Shield arc, dashboard "this month" count, type variety, and clinical authenticity in a real browser on Ben's machine.</human-check>
  </verify>
  <done>Both Ben and Sapir have approved the seed; the Heart-Shield arc renders the removed badge and the clinical sample content is signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed JSON → overview render | Author-controlled seed strings rendered into the dashboard |
| demo context → DB | Seed must only ever populate `demo_portfolio` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-XSS | Tampering | overview.js seed render | low | accept | Seed is author-controlled and rendered via `textContent` (overview.js); never switch seed render to innerHTML |
| T-DBLEAK | Tampering | demo-seed.js / db.js | low | mitigate | window.name keeps DB on `demo_portfolio`; the new __demoSeedHelpers stay pure (no IndexedDB) and reachable in non-demo context for tests without writing |
| T-35-SC | Tampering (supply chain) | installs | low | accept | No new packages (zero-dependency rule) |
</threat_model>

<verification>
- 35-02 seed gates (DEMO-05/06/07) GREEN; `node tests/run-all.js` green.
- D-05 blocking human checkpoint approved by Ben + Sapir before ship (this checkpoint is never auto-advanced).
</verification>

<success_criteria>
The demo seed showcases Heart Shield (D-03), self-freshens via relative dates (D-06), conforms to the current schema with kept+expanded variety (D-04/D-07), and is human-approved for clinical authenticity (D-05).
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-04-SUMMARY.md` when done.
</output>
