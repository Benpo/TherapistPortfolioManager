---
phase: 35-demo-system-refresh-version-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/35-demo-chrome.test.js
  - tests/35-demo-static.test.js
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-08, DEMO-09]
nyquist_compliant: true

must_haves:
  truths:
    - "A jsdom test boots the demo home, runs the REAL App.initCommon, and asserts the converged chrome (single lang picker, footer with v{APP_VERSION}, banner) — RED until 35-03 (DEMO-01/03/04)."
    - "A static/source test asserts demo.html's convergence markers and absence of the dead native picker + 'therapeutic' literal + demo-hints references — RED until 35-03/35-05 (DEMO-02/08/09)."
  artifacts:
    - "tests/35-demo-chrome.test.js (new) — behavioral chrome render gate"
    - "tests/35-demo-static.test.js (new) — source/grep gate for DEMO-01/02/08/09"
  key_links:
    - "Tests must assert real DOM/file content and FAIL on a concrete assertion (never exit-0 vacuously) — Pitfall 4 / reference-pdf-jsdom-inert-gates."
  prohibitions:
    - "No test may pass via process exit code alone; each must assert observable DOM nodes or file-content strings."
---

<objective>
Author the Wave-0 RED validation scaffolds for the demo home **chrome convergence** (D-01) and the **static source gates** (D-02-removed-native-picker, D-07 terminology, D-08 dead-code). These tests assert the *desired end state* and therefore FAIL now; the Wave-2 implementation plans (35-03, 35-05) turn them GREEN.

Purpose: Lock falsifiable acceptance for DEMO-01/02/03/04/08/09 BEFORE any code changes, so the chrome convergence and demo-hints deletion cannot silently regress or vacuously pass (this repo has shipped false-GREEN jsdom tests before — see reference-pdf-jsdom-inert-gates).
Output: Two new test files under tests/, runnable via `node tests/run-all.js`, both RED today.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-VALIDATION.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md

# Test-harness patterns to mirror (read before writing):
@tests/31-overview-render-hardening.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@tests/run-all.js
</context>

<artifacts_produced>
- `tests/35-demo-chrome.test.js` — NEW. Asserts converged demo-home chrome via real `App.initCommon`.
- `tests/35-demo-static.test.js` — NEW. Asserts demo.html source markers + repo-wide demo-hints absence.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Chrome render gate — tests/35-demo-chrome.test.js (DEMO-01/03/04)</name>
  <files>tests/35-demo-chrome.test.js</files>
  <read_first>
    - demo.html (the page under test; note its inline scripts do NOT run under jsdom runScripts:'outside-only')
    - assets/app.js (initCommon ~L584, initDemoMode ~L263, header injectors L137-503, renderFooter call ~L689)
    - assets/shared-chrome.js (renderFooter, APP_VERSION resolution)
    - assets/version.js (APP_VERSION = '1.2.3')
    - tests/31-overview-render-hardening.test.js (the eval-into-jsdom + App-stub pattern to mirror)
    - tests/_helpers/mock-portfolio-db.js (createMockPortfolioDB — satisfies initCommon's getAllTherapistSettings/getAllSnippets awaits)
  </read_first>
  <action>
    Build a jsdom window from demo.html with runScripts:'outside-only' and url 'https://localhost/demo.html'. Stub matchMedia (mirror 31-overview) and any navigator API initCommon touches (serviceWorker/BroadcastChannel are absent in jsdom and are guarded — confirm they no-op). Eval, in order, into that window: version.js, i18n-en/he/de/cs.js, i18n.js, shared-chrome.js, app.js. Set win.name = 'demo-mode' BEFORE calling initCommon. Inject window.PortfolioDB = createMockPortfolioDB({}) so the two awaits resolve. Then `await win.App.initCommon()`.

    Assert (all on real DOM, with a completion sentinel to kill vacuous-green):
    - Completion sentinel: `.app-footer` exists (renderFooter runs at the END of initCommon — if it threw early the footer is absent → FAIL). This guards the async path.
    - DEMO-01: `#headerActions` (or the .header-actions container) contains injected controls (childElementCount > 0).
    - DEMO-02/01: exactly ONE language picker — assert `select#languageSelect` is ABSENT and the injected globe popover/control is present exactly once. (At scaffold time the native select still ships in demo.html, so this is RED.)
    - DEMO-04: the `.app-footer-copy` text contains 'v' + win.AppVersion.APP_VERSION.
    - DEMO-03: a `.demo-banner` with non-empty `.demo-banner-text` is present.

    Use a per-assertion counter and a final guard asserting every case executed. Exit 0 only if all pass, 1 otherwise. Do NOT gate on process exit alone — assert nodes/text (Pitfall 4).
  </action>
  <verify>
    <automated>node tests/35-demo-chrome.test.js; echo "exit=$? (RED/non-zero EXPECTED at this wave — must fail on the native-select assertion, not vacuously)"</automated>
  </verify>
  <acceptance_criteria>
    - The test executes the REAL initCommon and fails TODAY on a concrete assertion (the duplicate/native lang picker), printing which assertion failed — proving it is wired to live DOM, not a stub.
    - When 35-03 lands (native select removed, shared-chrome script + id=headerActions added), the file goes GREEN with no test edits.
    - File contains no `process.exit(0)` shortcut that bypasses assertions; the `.app-footer` completion sentinel is asserted.
  </acceptance_criteria>
  <done>RED gate for DEMO-01/03/04 exists, asserts real injected DOM + footer version, and fails on a real assertion at this wave.</done>
</task>

<task type="auto">
  <name>Task 2: Source/grep gate — tests/35-demo-static.test.js (DEMO-01/02/08/09)</name>
  <files>tests/35-demo-static.test.js</files>
  <read_first>
    - demo.html (native select L55-65; brand-subtitle literal L51; script block L175-190)
    - index.html (convergence reference: id="headerActions" + shared-chrome.js script tag)
    - assets/i18n-en.js (app.subtitle resolves to the 'energy' copy), i18n-he/de/cs.js app.subtitle
    - sw.js (PRECACHE_URLS demo-hints entry ~L63)
    - 35-RESEARCH.md (DEMO-09 reference map: demo-hints lives in assets/demo-hints.js, app.js injection, sw.js precache)
  </read_first>
  <action>
    Use fs.readFileSync over the repo (REPO_ROOT = path.resolve(__dirname, '..')) — pure string/regex assertions, no jsdom. Assert:
    - DEMO-01: demo.html contains `id="headerActions"` AND a `<script src="./assets/shared-chrome.js">` reference (both absent today → RED).
    - DEMO-02: demo.html does NOT contain `id="languageSelect"` (present today → RED).
    - DEMO-08: demo.html does NOT contain the literal "therapeutic" (the brand-subtitle fallback says it today → RED); AND demo.html still carries `data-i18n="app.subtitle"`; AND each of i18n-en/he/de/cs.js app.subtitle resolves to the current 'energy' copy (regression guard — green now, must stay green).
    - DEMO-09: across the shipped surface — every file in assets/ plus sw.js plus the root *.html files — there is NO occurrence of the string `demo-hints` (EXCLUDE .planning/sketches/** and .claude/worktrees/** per the research reference map); AND `fs.existsSync('assets/demo-hints.js') === false`. (Both fail today → RED.)

    Filter comment/self-reference noise the same way the gate hygiene rules require (read the files you scan; do not let this test's own filename or strings self-match — scope the scan to assets/ + sw.js + root *.html, never to tests/). Count every assertion and guard that all ran. Exit 1 on any failure.
  </action>
  <verify>
    <automated>node tests/35-demo-static.test.js; echo "exit=$? (RED/non-zero EXPECTED at this wave)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of DEMO-01/02/08/09 maps to a distinct, named assertion that fails TODAY for the documented reason.
    - DEMO-09 scan excludes sketches/worktrees and the tests/ dir, and asserts BOTH zero `demo-hints` references AND the file's absence.
    - When 35-03 (chrome) and 35-05 (demo-hints removal) land, the file goes GREEN with no edits.
  </acceptance_criteria>
  <done>RED source-gate for DEMO-01/02/08/09 exists, scoped correctly, failing on real file contents at this wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/test harness → repo files | Tests only read source; they ship nothing to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01-FG | Tampering (false-GREEN) | jsdom scaffolds | medium | mitigate | Each test asserts real DOM/file content + a completion sentinel; never gates on exit-0 (Pitfall 4 / reference-pdf-jsdom-inert-gates) |
| T-35-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No new packages this phase (zero-dependency rule); jsdom already a devDependency — no install task |
</threat_model>

<verification>
- `node tests/run-all.js` discovers both new files (top-level tests/*.test.js).
- Both new files are RED at this wave (expected); pre-existing suite remains otherwise green.
- Each test prints which assertion failed (no silent exit-0).
</verification>

<success_criteria>
Two new RED gates exist covering DEMO-01/02/03/04/08/09, each falsifiable against live DOM/file content, ready to flip GREEN when 35-03 and 35-05 land.
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-01-SUMMARY.md` when done.
</output>
