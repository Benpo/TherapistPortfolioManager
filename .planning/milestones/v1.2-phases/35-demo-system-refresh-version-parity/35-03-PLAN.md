---
phase: 35-demo-system-refresh-version-parity
plan: 03
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - demo.html
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-08]
nyquist_compliant: true

must_haves:
  truths:
    - "The demo home header/nav/lang-picker/footer is injected from the single source (app.js initCommon + shared-chrome.js renderFooter), identical to index.html by construction — it cannot drift again (D-01)."
    - "The dead native <select id=languageSelect> is gone; the demo home shows exactly one (injected globe) language picker (DEMO-02)."
    - "The app version footer renders on the demo home, single-sourced from version.js (DEMO-04 — the on-screen version-parity signal)."
    - "The 'live demo' banner still renders and stays language-synced (DEMO-03)."
    - "Demo-specific hand-typed copy uses current terminology; the stale 'therapeutic' subtitle literal is gone (D-07 / DEMO-08)."
  artifacts:
    - "demo.html (modified) — header-actions block replaced with empty #headerActions; shared-chrome.js script added; subtitle literal corrected"
  key_links:
    - "Injection uses `getElementById('headerActions') || querySelector('.header-actions')` — adding id=headerActions makes demo.html match index.html's convergence target (Pattern 1)."
    - "shared-chrome.js needs NO demo-specific change; it renders the footer with v{APP_VERSION} from version.js, already loaded before it."
  prohibitions:
    - "Do NOT hand-copy chrome HTML into demo.html — that is how the drift happened; inject from the single source (anti-pattern)."
    - "Do NOT pursue D-02 (collapse demo.html into index.html via iframe name='demo-mode'): research recommended it but Ben chose D-01 (minimal blast radius) — keep demo.html as the entry, keep the overview body as-is this phase."
    - "Do NOT alter the landing iframe sandbox or the demo-mode inline gate scripts in demo.html (T-IFRAME / T-DBLEAK)."
---

<objective>
Converge the demo home shell (`demo.html`) onto the single-sourced chrome (D-01), so its header/nav/language-picker/footer come from `app.js` + `shared-chrome.js` exactly as `index.html` does — making the demo home current and drift-proof — and sweep the stale hand-typed subtitle literal to current terminology (D-07).

Purpose: Kill the one un-converged page that is the entire chrome-staleness surface, add the missing on-screen version footer (DEMO-04 version parity), and remove the dead double language picker — without touching the demo overview body (deferred) and without the D-02 collapse Ben declined.
Output: A modified `demo.html` that turns the 35-01 chrome + static gates GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md
@.planning/phases/35-demo-system-refresh-version-parity/35-CONTEXT.md

# Convergence reference + the file being changed:
@index.html
@demo.html
@assets/shared-chrome.js
</context>

<artifacts_produced>
- `demo.html` — header-actions block → empty `<div class="header-actions" id="headerActions"></div>`; `<script src="./assets/shared-chrome.js"></script>` added before app.js; brand-subtitle fallback literal corrected (no "therapeutic").
- No new symbols/files.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Single-source the demo home chrome + version footer (DEMO-01/02/03/04)</name>
  <files>demo.html</files>
  <read_first>
    - demo.html (header-actions + native select L55-65; script block L175-190; brand L37-53; demo-mode inline gate L8-14 — DO NOT touch the gate)
    - index.html (convergence target: the empty `#nav-placeholder` + `id="headerActions"` placeholders and the shared-chrome.js script-tag ordering)
    - assets/shared-chrome.js (renderFooter; APP_VERSION from version.js)
    - assets/app.js (initCommon header injectors fill #headerActions; renderFooter called at end of initCommon)
  </read_first>
  <action>
    In demo.html, replace the ENTIRE `<div class="header-actions"> … <div class="lang-picker"> … native <select id="languageSelect"> … </div></div>` block (the header-actions container that currently holds the dead native picker) with a single empty `<div class="header-actions" id="headerActions"></div>`, mirroring index.html. This removes the dead native select (no JS references it) and gives the injectors the `id="headerActions"` target; `app.js initCommon` then fills it with the globe lang-picker + theme + cloud + gear, and `shared-chrome.js renderFooter` appends the footer.

    Add `<script src="./assets/shared-chrome.js"></script>` to demo.html's script block immediately BEFORE `<script src="./assets/app.js"></script>` (mirror index.html ordering: version.js is already first, so APP_VERSION resolves). Do NOT reorder or remove the existing demo scripts (db.js, demo-seed.js, app.js, overview.js, demo.js) or the demo-mode inline gate.

    Leave `#nav-placeholder` as-is (already present). Do NOT add index.html's backup modal / iOS install banner / overview filter bar — D-01 keeps the demo overview body unchanged this phase. Keep the existing `.demo-banner` markup (DEMO-03 banner is owned by app.js initDemoMode and re-rendered idempotently; the static one in demo.html is fine to keep).
  </action>
  <verify>
    <automated>node tests/35-demo-chrome.test.js && node tests/35-demo-static.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 35-demo-chrome.test.js GREEN: after real initCommon, `#headerActions` is populated, exactly one language picker (no `select#languageSelect`), `.app-footer` text contains v{APP_VERSION}, `.demo-banner` present.
    - 35-demo-static.test.js GREEN for DEMO-01 (id=headerActions + shared-chrome.js script present) and DEMO-02 (no languageSelect).
    - Full suite `node tests/run-all.js` shows no NEW failures beyond the still-pending 35-seed/35-exposure RED gates owned by 35-04/35-06.
  </acceptance_criteria>
  <done>Demo home chrome is single-sourced; native picker gone; version footer renders; banner preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Terminology sweep — fix the stale subtitle literal (D-07 / DEMO-08)</name>
  <files>demo.html</files>
  <read_first>
    - demo.html (brand-subtitle L51: `data-i18n="app.subtitle"` with hardcoded fallback "Documentation and tracking of therapeutic sessions")
    - assets/i18n-en.js (app.subtitle = "Documentation and tracking of energy sessions" — the value to mirror)
  </read_first>
  <action>
    Update the brand-subtitle's hardcoded fallback text in demo.html so the literal matches the current i18n value ("energy", not "therapeutic") — keep the `data-i18n="app.subtitle"` attribute (it self-corrects at runtime; this fixes the dishonest static fallback). Do NOT touch the seed clinical sample text (stays English per D-07) and do NOT sweep landing.html's Hebrew-only #demo copy (out of scope this phase per research).

    Comment-text discipline: 35-demo-static.test.js negative-greps demo.html for the literal "therapeutic" — ensure no remaining occurrence of that literal anywhere in demo.html (including comments).
  </action>
  <verify>
    <automated>node tests/35-demo-static.test.js</automated>
  </verify>
  <acceptance_criteria>
    - DEMO-08 assertion GREEN: demo.html contains no "therapeutic" literal and still carries `data-i18n="app.subtitle"`; the four i18n dicts' app.subtitle resolve to the 'energy' copy (unchanged).
  </acceptance_criteria>
  <done>The demo home subtitle literal reflects current terminology; DEMO-08 gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landing.html → demo iframe (`src="./demo.html"`) | The entry point; its sandbox + demo-mode gate must keep working |
| demo browsing context → DB | `window.name==='demo-mode'` keeps the demo on `demo_portfolio` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-IFRAME | Elevation | demo.html in landing iframe | low | accept | No change to the iframe sandbox or demo.html's inline gate scripts; only the header-actions block + a script tag + a literal change |
| T-XSS | Tampering | shared-chrome footer render | low | accept | Footer markup is static/author-controlled (legal links + v{APP_VERSION}); no seed/user data interpolated |
| T-35-SC | Tampering (supply chain) | installs | low | accept | No new packages (zero-dependency rule) |
</threat_model>

<verification>
- 35-01 chrome + static gates GREEN.
- Manual confirmation of the landing iframe entry + language sync is covered by the DEMO-10 regression checkpoint in 35-06 (after all demo changes land).
- `node tests/run-all.js`: no regressions in the broader suite.
</verification>

<success_criteria>
Demo home chrome comes from one source (D-01), version footer renders (DEMO-04), dead native picker gone (DEMO-02), banner preserved (DEMO-03), subtitle terminology current (DEMO-08); 35-01 gates GREEN.
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-03-SUMMARY.md` when done.
</output>
