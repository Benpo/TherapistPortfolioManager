---
phase: 35-demo-system-refresh-version-parity
plan: 05
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - assets/demo-hints.js
  - assets/app.js
  - sw.js
autonomous: true
requirements: [DEMO-09]
nyquist_compliant: true

must_haves:
  truths:
    - "The orphaned guided-tour file `assets/demo-hints.js` is deleted and referenced by zero live files — its app.js dynamic-injection block and its sw.js precache entry are both removed (D-08 / DEMO-09)."
    - "The SW still installs cleanly and the removed asset is never served from a stale precache — cache invalidation is guaranteed by the INTEGRITY_TOKEN-derived CACHE_NAME on the next deploy."
  artifacts:
    - "assets/demo-hints.js — DELETED"
    - "assets/app.js (modified) — the `window !== window.top` demo-hints injection block removed"
    - "sw.js (modified) — `/assets/demo-hints.js` removed from PRECACHE_URLS"
  key_links:
    - "Locate the app.js injection block by CONTENT (the `window !== window.top` block that creates a <script> with src './assets/demo-hints.js'), not by line number — Phase 34 may have shifted lines (Pitfall 3)."
    - "CACHE_NAME = 'sessions-garden-' + self.AppVersion.INTEGRITY_TOKEN (sw.js:19) — token-derived, so the deploy's git-SHA rolls the cache; the pre-commit hook's v<N> auto-bump no-ops on this name and on a staged sw.js (reference-pre-commit-sw-bump)."
  prohibitions:
    - "Do NOT delete the file without removing BOTH live references (app.js injection + sw.js precache) — a dangling injection 404s on every demo page and the precache logs a permanent warning (Pitfall 1)."
    - "Do NOT touch the .planning/sketches demo-hints reference or .claude/worktrees copies (out of scope)."
---

<objective>
Remove the dead 372-line guided-tour file `assets/demo-hints.js` cleanly — the three coordinated edits research proved are required (D-08): delete the file, remove the `app.js` iframe-injection block, and remove the `sw.js` precache entry — then reconcile the SW cache so the removed asset is never served stale.

Purpose: DEMO-09. The CONTEXT assumption that demo-hints was "loaded by no page" was wrong — it is dynamically injected in iframe context and precached; a naive delete leaves a 404 injection + a stale precache entry.
Output: File gone, zero live references, SW cache-safe; the 35-01 DEMO-09 static gate GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md

# The references being removed + cache mechanism:
@assets/app.js
@sw.js
@assets/version.js
@.claude/hooks/pre-commit
</context>

<artifacts_produced>
- `assets/demo-hints.js` — deleted (recoverable from git history).
- `assets/app.js` — demo-hints injection block removed.
- `sw.js` — demo-hints precache entry removed.
- No new symbols.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Delete demo-hints.js + remove the app.js injection block (DEMO-09 part A)</name>
  <files>assets/demo-hints.js, assets/app.js</files>
  <read_first>
    - assets/app.js (the demo-hints injection block lives inside initCommon — the `if (window !== window.top) { … s.src = './assets/demo-hints.js'; … }` block; locate by CONTENT, ~end of initCommon)
    - assets/demo-hints.js (the file to delete)
    - 35-RESEARCH.md ("demo-hints.js reference map")
  </read_first>
  <action>
    Delete `assets/demo-hints.js`. In `assets/app.js`, remove the entire demo-hints dynamic-injection block — the `if (window !== window.top) { var s = document.createElement('script'); s.src = './assets/demo-hints.js'; document.body.appendChild(s); }` block near the end of `initCommon`. Locate it by its content (the `window !== window.top` guard + the `demo-hints.js` src), NOT by line number, because Phase 34 shifted app.js lines. Remove the block cleanly (including its preceding "Load demo hints…" comment) so no `demo-hints` token remains in app.js. Change nothing else in initCommon.
  </action>
  <verify>
    <automated>node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/demo-hints.js` no longer exists; `grep -n demo-hints assets/app.js` returns nothing.
    - initCommon otherwise unchanged (the 35-01 chrome test still boots and the suite shows no new failures).
  </acceptance_criteria>
  <done>The file is deleted and the app.js dynamic injection is gone (located by content).</done>
</task>

<task type="auto">
  <name>Task 2: Remove sw.js precache entry + reconcile SW cache (DEMO-09 part B + cache acceptance)</name>
  <files>sw.js</files>
  <read_first>
    - sw.js (PRECACHE_URLS demo-hints entry ~L63; CACHE_NAME ~L19 = 'sessions-garden-' + INTEGRITY_TOKEN; install allSettled tolerance ~L152)
    - assets/version.js (INTEGRITY_TOKEN derivation — deploy git-SHA stamp)
    - .claude/hooks/pre-commit (auto-bump logic: skips when sw.js is already staged; greps CACHE_NAME for v<N>)
    - reference note: project memory `reference-pre-commit-sw-bump`
  </read_first>
  <action>
    Remove the `'/assets/demo-hints.js',` line from sw.js PRECACHE_URLS. Do NOT touch PRECACHE_HTML's `/demo` entry (D-01 keeps demo.html, so /demo stays).

    SW cache reconciliation (the decision-4 acceptance for DEMO-09): confirm in the SUMMARY that CACHE_NAME derives from `self.AppVersion.INTEGRITY_TOKEN`, so the next deploy's git-SHA rolls the cache and a fresh install never lists the removed `demo-hints.js`. Note that the pre-commit hook's v<N> auto-bump is a no-op here (the token-based CACHE_NAME has no `v<N>` to parse, AND the hook skips when sw.js is itself staged) — so there is no manual cache-number to bump and no follow-up `chore` commit required. The APP_VERSION semver bump (version.js) is a phase-close decision for Ben per the recurring bump habit (project-version-bump-convention) and is intentionally NOT done in this plan (the demo footer's version parity is already delivered by 35-03 reading the live APP_VERSION).
  </action>
  <verify>
    <automated>node tests/35-demo-static.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - 35-demo-static.test.js DEMO-09 GREEN: no `demo-hints` token across assets/ + sw.js + root *.html (excluding sketches/worktrees) and the file is absent.
    - sw.js PRECACHE_HTML still lists `/demo`; CACHE_NAME still token-derived (cache rolls on deploy).
    - SUMMARY records the cache reconciliation + the deferred APP_VERSION-bump decision.
  </acceptance_criteria>
  <done>The precache entry is gone, the SW is cache-safe, and the cache/version reconciliation is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW precache → installed PWA | A stale precache entry would 404 forever; a stale cache could serve a deleted asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05-STALE | Tampering (stale cache) | sw.js precache | low | mitigate | Remove the precache entry; INTEGRITY_TOKEN-derived CACHE_NAME rolls on deploy so no stale serve; install uses allSettled (a missing asset never breaks install) |
| T-35-05-DANGLE | Denial of Service (console noise/404) | app.js injection | low | mitigate | Remove the injection block in the same plan as the file delete (Pitfall 1) |
| T-35-SC | Tampering (supply chain) | installs | low | accept | No new packages (zero-dependency rule) |
</threat_model>

<verification>
- 35-01 DEMO-09 static gate GREEN; full suite green.
- `grep -rn demo-hints assets/ sw.js *.html` (excluding sketches/worktrees) returns nothing.
</verification>

<success_criteria>
demo-hints.js deleted with all three live references removed (D-08), SW cache-safe, DEMO-09 gate green.
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-05-SUMMARY.md` when done.
</output>
