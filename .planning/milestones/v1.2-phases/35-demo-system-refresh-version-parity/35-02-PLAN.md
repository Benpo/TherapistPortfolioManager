---
phase: 35-demo-system-refresh-version-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/35-demo-seed.test.js
  - tests/35-demo-exposure.test.js
autonomous: true
requirements: [DEMO-05, DEMO-06, DEMO-07, DEMO-11]
nyquist_compliant: true

must_haves:
  truths:
    - "A data+render test asserts the seed tells a Heart-Shield removal arc (≥2 isHeartShield sessions on one client, mixed shieldRemoved, final true) and renders a removed badge — RED until 35-04 (DEMO-05)."
    - "A date test asserts every session carries a relative `daysAgo` offset and the seed's relative-date transform yields ≥1 session in the current month at month edges — RED until 35-04 (DEMO-06)."
    - "A schema test asserts every seed session conforms to the current db.js schema (allowed keys, sessionType enum, issues[] shape, no legacy fields) — RED until 35-04 (DEMO-07)."
    - "An exposure test asserts the backup cloud button, export/import, and license activate/deactivate are hidden/disabled in demo mode AND present in normal mode — RED until 35-06 (DEMO-11 / D-09)."
    - "An exposure test asserts the backup Export flow is blocked in demo mode and runs normally outside demo — RED until 35-06 (DEMO-11)."
  artifacts:
    - "tests/35-demo-seed.test.js (new) — DEMO-05/06/07"
    - "tests/35-demo-exposure.test.js (new) — DEMO-11"
  key_links:
    - "The seed date transform must be reachable from the test via a window.__demoSeedHelpers seam that demo-seed.js exposes BEFORE its demo-mode early-return (authored in 35-04)."
    - "Render assertions use the real overview.js (renderClientRows) per the 31-overview pattern; the .some() client-badge semantics (not .every()) is the rule — Pitfall 2."
  prohibitions:
    - "No exit-0 vacuous pass — assert nodes/data (Pitfall 4)."
    - "The exposure test MUST also assert controls are PRESENT in normal (non-demo) mode, so it catches a regression that breaks the real app."
---

<objective>
Author the Wave-0 RED scaffolds for the **seed refresh** (D-03/D-06/D-07 → DEMO-05/06/07) and the **demo exposure lock-down** (D-09 → DEMO-11). They assert the desired end state and FAIL now; 35-04 and 35-06 turn them GREEN.

Purpose: Pin falsifiable acceptance for the Heart-Shield arc, the self-freshening relative dates (incl. the timezone month-edge, Pitfall 5), schema conformance, and the demo control-hiding — before any seed or guard code is written.
Output: Two new test files under tests/, RED today.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-VALIDATION.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md

# Patterns + sources to mirror/read:
@tests/31-overview-render-hardening.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@assets/overview.js
@assets/demo-seed-data.json
</context>

<artifacts_produced>
- `tests/35-demo-seed.test.js` — NEW. DEMO-05 (arc render), DEMO-06 (relative-date month-edge), DEMO-07 (schema).
- `tests/35-demo-exposure.test.js` — NEW. DEMO-11 (backup-cloud/export/import/license controls hidden in demo, present in normal; plus the backup Export flow blocked-in-demo / runs-in-normal).
- Declares the contract the 35-04 seed must satisfy: `window.__demoSeedHelpers = { isoDaysAgo(n, now?), applyRelativeDates(sessions, now?) }` and per-session integer `daysAgo` (one === 0).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Seed gate — tests/35-demo-seed.test.js (DEMO-05/06/07)</name>
  <files>tests/35-demo-seed.test.js</files>
  <read_first>
    - assets/demo-seed-data.json (current 7 clients / 11 sessions — note ZERO isHeartShield today, absolute `date`, no `daysAgo`)
    - assets/overview.js (renderClientRows ~L351; client-badge .some() logic ~L393-408 — heart-badge-removed; countSessionsThisMonth ~L562)
    - assets/db.js (DB_VERSION=6 schema; session keys union — RESEARCH "Current Seed Schema")
    - tests/31-overview-render-hardening.test.js (eval overview.js into jsdom from index.html + App-stub + call renderClientRows directly — mirror exactly)
    - tests/_helpers/app-stub.js (createAppStub — key-returning t)
    - 35-RESEARCH.md ("Current Seed Schema", "Recommended Heart Shield arc", "Relative-date model")
  </read_first>
  <action>
    Three named assertion groups over the seed loaded from disk (fs read of assets/demo-seed-data.json), all RED today:

    DEMO-05 (Heart-Shield arc + render): assert at least one client owns ≥2 sessions with `isHeartShield === true`, with MIXED `shieldRemoved` across them and the chronologically-final one `shieldRemoved === true`. Then render that client's row: eval overview.js into a jsdom window built from index.html (per 31-overview), inject the App stub (key-returning t), build `clients` + `sessionsByClient`, call `win.renderClientRows([client], {[client.id]: sessions})`, and assert a `.heart-badge.heart-badge-removed` node exists in #clientTableBody (because the final session is removed and the badge uses `.some()`, Pitfall 2). Also assert per-session: ≥1 rendered/flagged active and ≥1 removed.

    DEMO-06 (relative dates, month-edge): assert every session object has an integer `daysAgo` and at least one session has `daysAgo === 0`. Then require the seed's transform seam — `window.__demoSeedHelpers.applyRelativeDates(sessions, now)` (exposed by 35-04 in demo-seed.js BEFORE its demo-mode early-return) — and, for `now` fixed at BOTH the 1st and the last day of a month, assert `countSessionsThisMonth(applyRelativeDates(copy, now)) >= 1` (use the real overview.js countSessionsThisMonth). Anchor expectations to local-noon dates (Pitfall 5). At this wave the JSON has no `daysAgo` and the seam does not exist → RED.

    DEMO-07 (schema conformance): for every session assert keys are a subset of the allowed union {clientId,date,daysAgo,sessionType,issues,trappedEmotions,insights,limitingBeliefs,additionalTech,customerSummary,comments,isHeartShield,shieldRemoved,createdAt,updatedAt,id}; `sessionType` ∈ {clinic,online,other}; each issues[] entry has only {name,before,after}; NO legacy keys (`heartWallCleared`,`heartWall`); and require a `daysAgo` (computed-date model) rather than a hardcoded absolute `date`. RED today (current sessions have `date`, no `daysAgo`).

    Guard every group ran with a counter. Assert data/DOM, never exit-0.
  </action>
  <verify>
    <automated>node tests/35-demo-seed.test.js; echo "exit=$? (RED/non-zero EXPECTED at this wave)"</automated>
  </verify>
  <acceptance_criteria>
    - DEMO-05 fails today because the current seed has zero isHeartShield; the render assertion uses the REAL renderClientRows and the real .some() badge path.
    - DEMO-06 fails today (no daysAgo / no seam) and, once 35-04 lands, passes for both the 1st-of-month and last-of-month `now` fixtures.
    - DEMO-07 fails today (no daysAgo) and pins the schema union + sessionType enum + issues[] shape + legacy-field absence.
    - No assertion is satisfied by process exit alone.
  </acceptance_criteria>
  <done>RED gate for DEMO-05/06/07 exists, asserting arc render, month-edge relative dates, and schema; defines the __demoSeedHelpers seam contract for 35-04.</done>
</task>

<task type="auto">
  <name>Task 2: Exposure gate — tests/35-demo-exposure.test.js (DEMO-11 / D-09)</name>
  <files>tests/35-demo-exposure.test.js</files>
  <read_first>
    - assets/app.js (mountBackupCloudButton ~L425 → #backupCloudBtn; initDemoMode ~L263; initCommon ~L584)
    - demo.html (overview body export/import controls: #exportBtn L101, .import-label + #importInput L102-105)
    - index.html (the NON-demo home — its inline script does NOT force demo-mode; use it for the present-in-normal-mode assertions)
    - license.html (#license-activate-btn L428, #license-deactivate-btn L455; loads shared-chrome.js + license.js, NOT app.js)
    - assets/license.js (DOMContentLoaded init ~L339; binds click handlers on license-activate-btn ~L417 / license-deactivate-btn ~L466)
    - assets/backup-modal.js (openExportFlow ~L236 — currently NO demo guard; openImportFlow demo guard ~L295 is the pattern; `window.openExportFlow` is exposed unconditionally on module load ~L345-350; calls `BM.exportEncryptedBackup()` first, then `BM.exportBackup()` on skip)
    - tests/_helpers/app-stub.js (createAppStub — key-returning t; extend/wrap it with a showToast spy for the export half)
    - tests/_helpers/mock-portfolio-db.js
  </read_first>
  <action>
    Three halves, all RED today, each also asserting NON-demo presence/behavior (no-regression guard):

    Home controls (demo): jsdom from demo.html (runScripts:'outside-only', stub matchMedia), eval version.js + i18n dicts + i18n.js + shared-chrome.js + app.js, set win.name='demo-mode', inject createMockPortfolioDB({}), `await win.App.initCommon()`. Assert: `#backupCloudBtn` is ABSENT; `#exportBtn` is hidden (`.hidden === true`) or absent; the import control (`#importInput` / `.import-label`) is hidden or disabled. (Use `.app-footer` as the initCommon-completion sentinel.)
    Home controls (normal): jsdom from index.html, win.name left default (''), inject mock PortfolioDB, `await App.initCommon()`. Assert: `#backupCloudBtn` IS present and `#exportBtn` is NOT hidden — proving the guard is demo-scoped and the real app is untouched.

    License controls (demo): jsdom from license.html, eval version.js + i18n dicts + i18n.js + shared-chrome.js + license.js, set win.name='demo-mode', dispatch DOMContentLoaded to run license.js init. Assert `#license-activate-btn` AND `#license-deactivate-btn` are disabled (`.disabled === true`) or hidden. License controls (normal): same boot with win.name='' → assert the activate button is enabled/visible.

    Backup export flow (demo vs normal): eval `assets/backup-modal.js` into a jsdom window (it exposes `window.openExportFlow` unconditionally on module load ~L345-350, so no wiring/DOM is required). Inject `win.App` exposing a `showToast` spy (plus a key-returning `t`), and a `win.BackupManager` mock whose `exportEncryptedBackup` and `exportBackup` are spies (so a real export is never triggered). Demo half: set `win.name='demo-mode'`, `await win.openExportFlow({})`, and assert NEITHER export-primitive spy ran (`exportEncryptedBackup` not called, `exportBackup` not called) and — if feasible — that `App.showToast` was requested with the `toast.exportDisabledDemo` key. Normal half: set `win.name=''`, `await win.openExportFlow({})`, and assert `exportEncryptedBackup` WAS called — the no-regression half. RED today: no export guard exists in `openExportFlow`, so the demo half currently reaches the primitive.

    At this wave none of the demo-mode guards exist → the demo halves FAIL while the normal halves pass; that asymmetry is the RED signal. Count every assertion; assert real DOM / real spy calls, never exit-0.
  </action>
  <verify>
    <automated>node tests/35-demo-exposure.test.js; echo "exit=$? (RED/non-zero EXPECTED at this wave)"</automated>
  </verify>
  <acceptance_criteria>
    - Demo-mode assertions FAIL today (controls still present/enabled, export primitive still reached) while normal-mode assertions PASS — a falsifiable asymmetry, not a blanket fail.
    - The normal-mode half guarantees 35-06 cannot satisfy the gate by breaking the real app.
    - License half drives the REAL license.js init (DOMContentLoaded), not a stub.
    - Backup-export half drives the REAL `window.openExportFlow` from backup-modal.js with spied export primitives; demo half asserts the primitive is NOT called, normal half asserts it IS — RED until the 35-06 `openExportFlow` guard lands.
  </acceptance_criteria>
  <done>RED gate for DEMO-11 exists: backup-cloud/export/import/license hidden-in-demo + present-in-normal (real DOM across demo.html, index.html, license.html) plus the backup Export flow blocked-in-demo / runs-in-normal (real `openExportFlow` with spied primitives).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo browsing context → real DB | Demo writes must never reach `sessions_garden` (the test encodes the UX-level control-hiding that sits atop the DB-name isolation) |
| dev/test harness → repo files | Tests read source + render in jsdom; ship nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-02-FG | Tampering (false-GREEN) | jsdom scaffolds | medium | mitigate | Assert real DOM + completion sentinel; demo-vs-normal asymmetry prevents a blanket-fail masquerade (Pitfall 4) |
| T-DBLEAK | Tampering | demo control exposure | low | mitigate | Exposure test encodes DEMO-11; documents that hiding is UX-level and the real boundary stays `window.name` DB-name isolation |
| T-35-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No new packages (zero-dependency rule) |
</threat_model>

<verification>
- `node tests/run-all.js` discovers both new files; both RED at this wave (expected).
- Demo-mode halves fail, normal-mode halves pass (falsifiable asymmetry).
</verification>

<success_criteria>
Two new RED gates exist covering DEMO-05/06/07/11, each falsifiable against live data/DOM, defining the __demoSeedHelpers seam contract for 35-04 and the demo-vs-normal control contract for 35-06.
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-02-SUMMARY.md` when done.
</output>
