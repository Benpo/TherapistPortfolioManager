---
phase: 31-refactor-god-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/31-overview-render-hardening.test.js
  - tests/31-sessions-render-hardening.test.js
  - assets/overview.js
  - assets/sessions.js
autonomous: true
requirements: [RFCT-03]
must_haves:
  truths:
    - "overview.js renders the per-row view button with label text App.t('overview.table.view') and the empty-state helper text App.t('overview.sessions.none') identically before and after the innerHTML->textContent/DOM swap — proven by tests/31-overview-render-hardening.test.js"
    - "sessions.js renders the per-row view button with label text App.t('sessions.table.view') identically before and after the swap — proven by tests/31-sessions-render-hardening.test.js"
    - "No interpolated-i18n innerHTML assignment remains at the hardened sites (overview.js:456,510; sessions.js:147) — replaced by textContent + DOM-built nodes"
  artifacts:
    - tests/31-overview-render-hardening.test.js
    - tests/31-sessions-render-hardening.test.js
  key_links:
    - "overview.js:456 (empty-state) and :510 (view button) build nodes via textContent/createElement instead of innerHTML string interpolation"
    - "sessions.js:147 (view button) builds nodes via textContent/createElement instead of innerHTML string interpolation"
---

<objective>
RFCT-03 — Harden the two interpolated-i18n `innerHTML` sites that the requirement explicitly names: `overview.js` (empty-state at :456, view-button at :510) and `sessions.js` (view-button at :147). Per D-03/D-06 this is test-first: write a characterization test for each render BEFORE swapping `innerHTML` string interpolation to `textContent` + DOM-built nodes, so the rendered output is provably identical.

Purpose: Defensive output-encoding consistency (V5) — match the `textContent`-only discipline `settings.js` already enforces. i18n strings are app-controlled so XSS risk is low; this is hygiene, not a live vuln fix. `sessions.js` has ZERO tests today, so the characterization test also closes a net gap.
Output: two new characterization tests + the hardened render sites.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files: `assets/settings-snippets.js` (plan 03), `assets/settings-photos.js` (plan 04), `assets/export-modal.js` (plan 05); tests `tests/31-openDB-pooling.test.js` (plan 01), `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js` (this plan). NEW symbol `window.__exportModalInit` (plan 05). This plan adds NO new public global and NO new file beyond the two tests.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-RESEARCH.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/overview.js
@assets/sessions.js
@tests/_helpers/app-stub.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize then harden overview.js view-button + empty-state renders</name>
  <files>tests/31-overview-render-hardening.test.js, assets/overview.js</files>
  <read_first>
    - assets/overview.js:456 (empty-state: `detailCell.innerHTML = `<div class="helper-text">${App.t("overview.sessions.none")}</div>`` ) and :510 (view button: innerHTML with a `button-label` span text `App.t("overview.table.view")` + a static `button-icon` SVG span)
    - assets/overview.js:316,355,595 (SAFE clears `= ""` — DO NOT change) and :430 (static SVG, no interpolation — DO NOT change)
    - tests/_helpers/app-stub.js (App.t stub used to drive jsdom real-page renders) and any existing overview test for the jsdom load pattern
    - .planning/phases/31-refactor-god-modules/31-RESEARCH.md §"Validation Architecture → RFCT-03 cleanups"
  </read_first>
  <behavior>
    - Empty-state: when the sessions list is empty, the detail cell renders a `.helper-text` element whose textContent equals App.t("overview.sessions.none"). Assert the rendered text AND that a `.helper-text` element exists.
    - View button: each session row renders a button containing a `.button-label` whose textContent equals App.t("overview.table.view"), a `data-i18n="overview.table.view"` attribute is present, and a `.button-icon` span with an `<svg>` child exists. Assert label text + structure.
    - These assertions must pass on the CURRENT innerHTML implementation first (characterization), then still pass after the DOM-node swap.
  </behavior>
  <action>First create tests/31-overview-render-hardening.test.js: load overview.html + assets/overview.js into jsdom (reuse the existing overview test's load pattern; inject the App.t stub), trigger the empty-state render and a populated-row render, and assert the observable output from the behavior block (label textContent, `.helper-text` text, `data-i18n` attribute, `.button-icon svg` presence). Confirm it passes on the UNCHANGED overview.js. THEN harden overview.js:456 and :510: replace the `innerHTML = `...${App.t(...)}...`` assignments with explicit DOM construction — create the element, set `.textContent` to the i18n string, preserve the `data-i18n` attribute and the `button-icon` SVG span (the SVG markup is static/app-controlled and may be built once via a small helper or assigned to the icon span's innerHTML since it contains no interpolation). Leave :316/:355/:595 (`= ""`) and :430 (static SVG) unchanged. Apply opportunistic RFCT-03 touched-region cleanups (any `var`->`const`/`let`; overview.js already has 0 `var`, so likely none) and a tagged catch-log only if a genuinely-silent catch sits in the touched region.</action>
  <verify>
    <automated>node tests/31-overview-render-hardening.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/31-overview-render-hardening.test.js` exits 0 BEFORE the swap (characterization baseline) and AFTER the swap.
    - `grep -n 'innerHTML' assets/overview.js` shows NO interpolated-i18n assignment remaining at the former :456/:510 sites (the `${App.t(...)}` interpolation is gone); the safe clears at :316/:355/:595 and the static SVG at :430 may remain.
    - Rendered `.button-label` textContent equals `App.t("overview.table.view")` and `.helper-text` textContent equals `App.t("overview.sessions.none")` in the test assertions.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>overview.js empty-state and view-button render via textContent/DOM nodes with byte-identical observable output, locked by a new characterization test, full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize then harden sessions.js view-button render (file had 0 tests)</name>
  <files>tests/31-sessions-render-hardening.test.js, assets/sessions.js</files>
  <read_first>
    - assets/sessions.js:147 (view button: innerHTML with `button-label` span text `App.t("sessions.table.view")` + a `data-i18n="sessions.table.view"` attr + static `button-icon` SVG span)
    - assets/sessions.js:16,79 (SAFE clears `= ""` — DO NOT change)
    - tests/_helpers/app-stub.js for the App.t stub; tests/31-overview-render-hardening.test.js (Task 1) as the structural analog for the jsdom load + assertion shape
    - .planning/phases/31-refactor-god-modules/31-RESEARCH.md §"Validation Architecture → RFCT-03 cleanups"
  </read_first>
  <behavior>
    - View button: each session row renders a button containing a `.button-label` whose textContent equals App.t("sessions.table.view"), a `data-i18n="sessions.table.view"` attribute is present, and a `.button-icon` span with an `<svg>` child exists.
    - Passes on the CURRENT innerHTML implementation first, then still passes after the DOM-node swap.
  </behavior>
  <action>Create tests/31-sessions-render-hardening.test.js (sessions.js has 0 tests today): load sessions.html + assets/sessions.js into jsdom mirroring Task 1's pattern, inject the App.t stub, render a populated session row, and assert the view-button label textContent, `data-i18n` attribute, and `.button-icon svg` presence. Confirm green on UNCHANGED sessions.js. THEN harden sessions.js:147: replace the interpolated `innerHTML` with DOM construction (createElement + textContent for the label, preserve `data-i18n`, keep the static SVG icon span). Leave :16/:79 (`= ""`) unchanged. Apply RFCT-03 touched-region cleanups if applicable (sessions.js has 0 `var`).</action>
  <verify>
    <automated>node tests/31-sessions-render-hardening.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/31-sessions-render-hardening.test.js` exits 0 BEFORE and AFTER the swap.
    - `grep -n 'innerHTML' assets/sessions.js` shows NO interpolated-i18n assignment remaining at the former :147 site; the safe clears at :16/:79 may remain.
    - Rendered `.button-label` textContent equals `App.t("sessions.table.view")` in the assertions.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>sessions.js view-button renders via textContent/DOM nodes with identical observable output, now covered by its first characterization test, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM render of app-controlled i18n strings (overview/sessions tables) | i18n values originate from in-repo `i18n-*.js`; no user-supplied data is interpolated. The swap REDUCES XSS surface by moving from string-interpolated innerHTML to textContent/DOM nodes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-02-T | Tampering / XSS | Interpolated i18n in innerHTML (overview.js:456,510; sessions.js:147) | low | mitigate | Swap to textContent + DOM nodes; characterization tests assert identical rendered output first (D-03). App-controlled strings → low residual risk; this is defensive consistency, not a live vuln. |

No new high-severity threats; the change REDUCES existing (low) XSS surface. No new inputs or trust boundaries (security_block_on: high — nothing at threshold).
</threat_model>

<verification>
- `node tests/31-overview-render-hardening.test.js` and `node tests/31-sessions-render-hardening.test.js` both exit 0 before AND after their respective swaps.
- `grep -n 'innerHTML' assets/overview.js assets/sessions.js` shows no interpolated-i18n assignments remaining at the three hardened sites.
- `npm test` full suite exits 0.
</verification>

<success_criteria>
The three named interpolated-i18n innerHTML sites are hardened to textContent/DOM nodes with provably identical rendered output, each locked by a characterization test (sessions.js gains its first test), and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-02-SUMMARY.md` when done.
</output>
