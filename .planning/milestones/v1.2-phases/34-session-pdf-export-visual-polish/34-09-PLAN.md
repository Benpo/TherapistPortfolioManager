---
phase: 34-session-pdf-export-visual-polish
plan: 9
type: execute
wave: 4
depends_on: [34-07, 34-05, 34-04]
files_modified:
  - assets/pdf-export.js
  - assets/export-modal.js
autonomous: true
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-08: per complaint, severity renders as two stacked bars on a shared 0–10 track (track 118×8pt radius 4pt bg #eef7ea) — a red 'before' fill (pre-lightened flat hex ~#ee6a6a, NOT a GState opacity op) over a shorter green 'after' fill (#2fb37d), each captioned (reusing Before/After) and numbered."
    - "D-08: severity is fed by the structured issues[] input (not markdown text); the whole 'Severity — before & after' section is omitted when issues is empty; a severity row never splits mid-bar (ensureRoom)."
    - "D-10: each track fills from the START edge (RTL grows from the right); captions and numerals mirror and keep visual order (isInputVisual:false)."
    - "D-11: the content-stream floor tests stay green and shapeForJsPdf is NOT touched; the before bar introduces no opacity graphics-state op."
  artifacts:
    - "drawSeverityBlock(issues) in pdf-export.js, called after the body loop; buildFilteredSessionMarkdown in export-modal.js no longer emits the issues section as markdown (severity now comes from issues[] structurally) — dropped atomically with the bars so severity is never missing."
  key_links:
    - "drawSeverityBlock consumes sessionData.issues (forwarded by 34-05) and uses the heading pdf.severity.heading (34-02) + captions session.copy.scale.before/after; bar fill grows from the start edge per docDir (34-RESEARCH Pattern 2) (D-08/D-10)."
---

<objective>
Render the signature two-bar before/after severity block from structured data (D-08), and atomically stop emitting severity as markdown text so it appears exactly once — as bars. The before bar uses a pre-lightened flat hex (no opacity graphics-state op) to keep the renderer deterministic for the test suite, and fills grow from the start edge so RTL is correct by construction.

Purpose: Turn the lower-is-better severity into a length-readable, brand-consistent visual that is unit-testable and RTL-safe (D-08/D-10), without perturbing the content-stream floor tests (D-11).
Output: `drawSeverityBlock(issues)` in `assets/pdf-export.js` + removal of the issues-as-markdown emission in `assets/export-modal.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ drawSeverityBlock(issues) (pdf-export.js); ★ issues-section removed from buildFilteredSessionMarkdown (export-modal.js)
- drawHeaderBand/drawClientCard/drawFooterBand (34-06/07); deriveSessionOrdinal + contract (34-05); logo (34-01); i18n severity heading (34-02); tests (34-03/04); baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-UI-SPEC.md
@.planning/phases/34-session-pdf-export-visual-polish/design-mockups/FINAL-mockup.html
@assets/pdf-export.js
@assets/export-modal.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: drawSeverityBlock(issues) — two bars on a shared 0–10 track, flat-hex before, start-edge fills</name>
  <read_first>
    - tests/34-severity-bars.test.js (the RED contract from 34-04 — implement to GREEN)
    - 34-RESEARCH.md § Pattern 2 (fill from start edge) + § Anti-Patterns (flat hex, NOT GState; do not touch shapeForJsPdf) + § Common Pitfalls (Pitfall 5 ensureRoom per row)
    - assets/pdf-export.js lines 849-872 (drawTextLine anchor), 945-951 (ensureRoom), 699-722 (constants)
    - 34-UI-SPEC.md § Component Inventory item 5 (track 118×8pt r4, fill = value/10×track, before #ee6a6a flat / after #2fb37d, captions 9.5pt, values 10.5pt) + § Color; 34-CONTEXT.md D-08, D-10, D-12; FINAL-mockup.html
  </read_first>
  <files>assets/pdf-export.js</files>
  <action>
    Add `function drawSeverityBlock(issues)`. If issues is empty, render NOTHING (no heading, no track — D-08 empty-state). Otherwise draw the section heading (the leaf-diamond heading style from 34-07, label = pdf.severity.heading). For each issue: call ensureRoom(rowHeight) so a row never splits mid-bar (Pitfall 5); draw the complaint name (label, 11.5pt normal #2f2d38, start-anchored, takes the leading space); then on the trailing side draw two stacked barlines on a shared 0–10 track — for each (before, after): draw the full track first (setFillColor('#eef7ea'); roundedRect(trackX, y, 118, 8, 4,4,'F')), then the fill rounded-rect whose width = value/10 × 118 and whose x grows from the START edge (LTR: trackLeftX; RTL: trackRightEdge − fillW — Pattern 2). Before fill = the pre-lightened flat hex (~#ee6a6a) — do NOT use the opacity graphics-state primitive (keeps the content stream deterministic, D-08/D-11); after fill = #2fb37d. Caption each bar (reuse session.copy.scale.before/after, 9.5pt bold muted) and draw its numeral (10.5pt bold, before #ea4b4b / after #2fb37d) via the start-anchored isInputVisual:false path so digits keep visual order under RTL. Separate rows with a #eef7ea rule. Do NOT modify shapeForJsPdf (D-11).
  </action>
  <verify>
    <automated>node tests/34-severity-bars.test.js && node tests/pdf-digit-order.test.js && node tests/pdf-glyph-coverage.test.js && node tests/pdf-bidi.test.js && node tests/30-issue-delta.test.js</automated>
  </verify>
  <done>
    - `node tests/34-severity-bars.test.js` GREEN: proportional before/after fills (value/10×track), flat-hex before with NO opacity/ExtGState op, before-fill > after-fill for a higher before value (D-08).
    - Empty issues → section omitted entirely; rows never split (ensureRoom).
    - pdf-bidi, pdf-digit-order, pdf-glyph-coverage, 30-issue-delta stay green (shapeForJsPdf untouched — D-10/D-11).
  </done>
</task>

<task type="auto">
  <name>Task 2: Call drawSeverityBlock in the render loop; drop the issues-as-markdown emission</name>
  <read_first>
    - assets/pdf-export.js lines 938-953 (render entry + body loop start), 1155-1158 (end of body loop, before the footer pass — where to call drawSeverityBlock)
    - assets/export-modal.js lines 117-201 (buildFilteredSessionMarkdown — the "issues" section that emits `## issuesHeading` + before/after bullet lines, to remove), 298-303 (the markdown builder contract)
    - tests/30-export-stepper.test.js + tests/34-severity-bars.test.js + tests/34-rtl-newblocks.test.js
  </read_first>
  <files>assets/pdf-export.js, assets/export-modal.js</files>
  <action>
    In pdf-export.js, after the body block loop completes (before the footer pass), call `drawSeverityBlock(sessionData.issues || [])` so severity renders structurally from the issues[] input. In export-modal.js buildFilteredSessionMarkdown, REMOVE the issues section emission (the `## issuesHeading` heading + the before/after bullet lines, ~117-201) so severity is no longer duplicated as markdown body text — severity now lives solely in the structured block. Do these together so there is never a state where severity is missing AND never a state where it is doubled. Keep all other sections (trapped emotions, comments, insights, etc.) in the markdown body unchanged (D-07). If any existing test asserts issues-in-markdown, treat its update as a DELIBERATE expected edit aligned to the moved-out severity (document it), but do not weaken the 30-issue-delta payload contract (that pins add-session's {name,before,after}, which is unchanged).
  </action>
  <verify>
    <automated>node tests/34-severity-bars.test.js && node tests/34-rtl-newblocks.test.js && node tests/30-export-stepper.test.js && node tests/30-issue-delta.test.js</automated>
  </verify>
  <done>
    - drawSeverityBlock(sessionData.issues) is called after the body loop; buildFilteredSessionMarkdown no longer emits the issues section.
    - Severity appears exactly once (as bars), never as markdown text; trapped-emotions/other body sections unchanged.
    - 34-severity-bars + 34-rtl-newblocks GREEN; 30-export-stepper + 30-issue-delta green (D-08/D-10/D-11).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's IndexedDB issues[] → local jsPDF render → Blob download | First-party numeric/text data only; rendered locally; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-09 | Tampering | issue name/value → severity block | low | mitigate | Issue text shaped via the existing isInputVisual:false path; numeric before/after are clamped to the 0–10 track by the value/10 fill math; first-party data only. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- 34-severity-bars + 34-rtl-newblocks GREEN; pdf-bidi/pdf-digit-order/pdf-glyph-coverage/30-issue-delta/30-export-stepper green.
- Severity rendered once as bars; markdown no longer emits issues; shapeForJsPdf untouched.
</verification>

<success_criteria>
The two-bar before/after severity block renders from structured data, deterministic (flat hex, no GState) and RTL-correct, with severity removed from the markdown body — the signature visual of the redesign (D-08).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-09-SUMMARY.md` when done.
</output>
