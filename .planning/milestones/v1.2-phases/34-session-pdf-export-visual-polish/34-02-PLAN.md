---
phase: 34-session-pdf-export-visual-polish
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [PDFX-01, PDFX-03]
must_haves:
  truths:
    - "D-01: header subtitle copy is localized (EN 'A personal session summary' / HE 'סיכום אישי של המפגש') with DE/CS translations (Claude's discretion per CONTEXT § Copy)."
    - "D-08: the two-bar severity block has a localized section heading and reuses the existing localized Before/After captions."
    - "D-09: the footer brand-as-tool mark and the 'Exported on' date label are localized in all 4 locales."
    - "D-13: the non-blocking Save-before-export prompt ('Save & export' / 'Keep editing' + title/body) is localized in all 4 locales."
  artifacts:
    - "New i18n keys present and at 4-locale parity: pdf.header.subtitle, pdf.severity.heading, pdf.footer.madeWith, pdf.footer.exportedOn, export.unsaved.title, export.unsaved.body, export.unsaved.saveExport, export.unsaved.keepEditing."
  key_links:
    - "pdf.* keys consumed by pdf-export.js render fns (34-06/07/09); export.unsaved.* keys consumed by the PDFX-03 prompt (34-08); reuses session.copy.title (header title) + session.copy.scale.before/after (severity captions) (D-01/D-08/D-09)."
---

<objective>
Author the new localization strings the redesigned PDF and the save-before-export prompt need, in all four locales, at parity. The locked title and the Before/After captions already exist (`session.copy.title`, `session.copy.scale.before/after`) and are REUSED — only genuinely-new copy is added here.

Purpose: Decouple copy from the render/wiring plans so 4-locale parity is established once (enforced by `25-11-i18n-parity.test.js`) and the consuming plans (34-06/07/09 render; 34-08 prompt) reference stable keys. DE/CS translations for the new strings are Claude's discretion per CONTEXT § Copy + § Claude's Discretion.
Output: 8 new keys × 4 locales added to `assets/i18n-{en,he,de,cs}.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ i18n keys: pdf.header.subtitle, pdf.severity.heading, pdf.footer.madeWith, pdf.footer.exportedOn, export.unsaved.{title,body,saveExport,keepEditing}
- assets/branding/icon-512-base64.js (34-01); render fns drawHeaderBand/drawClientCard/drawSeverityBlock/drawFooterBand (34-06/07/09); deriveSessionOrdinal + buildSessionPDF contract (34-05); saveSessionForm() (34-08); 6 tests (34-03/04); regenerated baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-CONTEXT.md
@.planning/phases/34-session-pdf-export-visual-polish/34-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the new PDF + prompt i18n keys in all 4 locales</name>
  <read_first>
    - assets/i18n-en.js lines 136-147 (session.copy.* incl. title + scale.before/after to REUSE), 265-267 (session.type.* pill keys), 453-465 (export.* namespace)
    - assets/i18n-he.js / i18n-de.js / i18n-cs.js (matching regions; confirm session.copy.title = 'סיכום מפגש' / 'Sitzungszusammenfassung' / 'Shrnutí sezení' already present)
    - 34-CONTEXT.md § Copy (locked) + § Claude's Discretion (DE/CS translations) + 34-UI-SPEC.md § Copywriting Contract
  </read_first>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
    Add these keys to each of the 4 locale files (keep them grouped logically, e.g. a `pdf.*` cluster near session.copy.* and the `export.unsaved.*` cluster in the export.* region):
    - pdf.header.subtitle — EN "A personal session summary"; HE "סיכום אישי של המפגש"; DE + CS: faithful translations (Claude's discretion).
    - pdf.severity.heading — the locked section heading "Severity — before & after"; HE "חומרה — לפני ואחרי"; DE + CS translations.
    - pdf.footer.madeWith — EN "Made with Sessions Garden · sessionsgarden.app"; HE "נוצר עם Sessions Garden · sessionsgarden.app"; DE + CS (keep the brand token "Sessions Garden · sessionsgarden.app" verbatim, translate only the "Made with" prefix — brand-as-tool, D-09).
    - pdf.footer.exportedOn — EN "Exported on"; HE/DE/CS translations (D-09 relabel to disambiguate from the card's session date).
    - export.unsaved.title — EN e.g. "Save before exporting?"; 4 locales.
    - export.unsaved.body — EN explains a client PDF should reflect saved changes and a new session needs saving to get a correct number; 4 locales.
    - export.unsaved.saveExport — EN "Save & export"; 4 locales.
    - export.unsaved.keepEditing — EN "Keep editing"; 4 locales.
    REUSE (do NOT duplicate): session.copy.title for the header title; session.copy.scale.before / session.copy.scale.after for the severity bar captions; session.type.{clinic,online,other} for the pill (already 4-locale). Do not introduce a hardcoded pill label (D-04).
  </action>
  <verify>
    <automated>for k in pdf.header.subtitle pdf.severity.heading pdf.footer.madeWith pdf.footer.exportedOn export.unsaved.title export.unsaved.body export.unsaved.saveExport export.unsaved.keepEditing; do for f in en he de cs; do grep -q "\"$k\"" assets/i18n-$f.js || { echo "MISSING $k in $f"; exit 1; }; done; done; echo "all 8 keys present in all 4 locales"</automated>
  </verify>
  <done>
    - All 8 new keys exist verbatim in each of i18n-en/he/de/cs.js.
    - No hardcoded in-person/remote label is introduced (the pill keeps using session.type.* — D-04).
    - The brand token "Sessions Garden · sessionsgarden.app" is preserved untranslated inside pdf.footer.madeWith (D-09).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify 4-locale parity stays green</name>
  <read_first>
    - tests/25-11-i18n-parity.test.js (the parity gate — every key present in all locales)
  </read_first>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
    Run the i18n parity test and fix any key-set drift it reports (a key present in one locale but missing in another). Do not weaken the test; if it flags a missing key, add the missing translation rather than deleting the key elsewhere.
  </action>
  <verify>
    <automated>node tests/25-11-i18n-parity.test.js</automated>
  </verify>
  <done>
    - `node tests/25-11-i18n-parity.test.js` exits 0 (all 4 locales carry an identical key set including the 8 new keys).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static i18n dictionaries → app render | No external input; localization strings are first-party static literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02 | Tampering | i18n string injection into PDF/DOM | low | accept | Strings are static developer-authored literals; existing render paths already escape/shape them; no user-controlled interpolation added. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- 8 new keys present in all 4 locales; `25-11-i18n-parity.test.js` green.
- Reused keys (session.copy.title, scale.before/after, session.type.*) untouched.
</verification>

<success_criteria>
The redesigned PDF (D-01/D-08/D-09) and the save-before-export prompt (D-13) have stable, parity-checked localized copy in all four locales, ready for the render and wiring plans to reference.
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-02-SUMMARY.md` when done.
</output>
