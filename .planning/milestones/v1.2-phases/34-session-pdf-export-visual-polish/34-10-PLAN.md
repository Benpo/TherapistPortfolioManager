---
phase: 34-session-pdf-export-visual-polish
plan: 10
type: execute
wave: 5
depends_on: [34-06, 34-07, 34-09, 34-05, 34-08]
files_modified:
  - .planning/fixtures/phase-23/fixture-en.pdf.sha256
  - .planning/fixtures/phase-23/fixture-de.pdf.sha256
  - .planning/fixtures/phase-23/fixture-cs.pdf.sha256
  - .planning/fixtures/phase-23/fixture-he.pdf.sha256
  - .planning/fixtures/phase-23/fixture-he-mixed.pdf.sha256
autonomous: false
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-11: the 5-fixture pdf-latin-regression SHA-256 baselines are regenerated DELIBERATELY and VISUALLY VERIFIED against the FINAL mockup before being committed — never a silent edit."
    - "D-11: the content-stream floor tests (pdf-digit-order, pdf-glyph-coverage, pdf-bold-rendering, pdf-bidi, 30-issue-delta) stay green untouched, and the full suite (npm test) is green."
    - "D-10: a Hebrew export visually matches the mockup's RTL layout (header band, card, severity bars, footer) — no RTL regression."
  artifacts:
    - "Regenerated .planning/fixtures/phase-23/*.pdf.sha256 (5 fixtures) reflecting the locked redesign; a human-verify sign-off that each regenerated PDF + a Hebrew export match design-mockups/FINAL-mockup.html."
  key_links:
    - "regeneration via `node tests/pdf-latin-regression.test.js --regenerate` following .planning/fixtures/phase-23/README.md (Mitigation B pins CreationDate + fileId); the human checkpoint gates the commit of the new baselines (D-11)."
---

<objective>
Close the phase by regenerating the 5 SHA-256 golden baselines for the redesigned PDF — the single deliberate, expected test break (Pitfall 1) — and proving the whole suite is green. The regeneration is gated by a human visual verification that each regenerated PDF (and a live Hebrew export) matches the locked mockup, so no silent visual regression is baked into the baseline.

Purpose: Lock the post-redesign output as the new golden reference (D-11) with an explicit visual sign-off (the byte hash proves bytes changed, not that they changed CORRECTLY — only the human check does that).
Output: regenerated `.planning/fixtures/phase-23/*.pdf.sha256` + full-suite-green + human visual sign-off.

## Artifacts this phase produces (★ = owned by this plan)
- ★ regenerated .planning/fixtures/phase-23/*.pdf.sha256 (5 fixtures); ★ full-suite-green + visual sign-off
- (consumes) all render fns (34-06/07/09), data contract (34-05), PDFX-03 (34-08), logo (34-01), i18n (34-02), tests (34-03/04)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-VALIDATION.md
@.planning/fixtures/phase-23/README.md
@.planning/phases/34-session-pdf-export-visual-polish/design-mockups/FINAL-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the 5 SHA-256 baselines and prove the full suite green</name>
  <read_first>
    - .planning/fixtures/phase-23/README.md (regeneration protocol: `--regenerate` mode, Mitigation B pins /CreationDate + /ID for byte-determinism, the 5 fixtures en/de/cs/he/he-mixed)
    - tests/pdf-latin-regression.test.js lines 41-143 (--regenerate flag + the 5-fixture hashing)
    - 34-RESEARCH.md § Common Pitfalls (Pitfall 1) + 34-VALIDATION.md § Existing Tests (expected-edit vs must-stay-invariant)
  </read_first>
  <files>.planning/fixtures/phase-23/fixture-en.pdf.sha256, .planning/fixtures/phase-23/fixture-de.pdf.sha256, .planning/fixtures/phase-23/fixture-cs.pdf.sha256, .planning/fixtures/phase-23/fixture-he.pdf.sha256, .planning/fixtures/phase-23/fixture-he-mixed.pdf.sha256</files>
  <action>
    Run `node tests/pdf-latin-regression.test.js --regenerate` to rebuild all 5 baselines for the redesigned output (per the README protocol). Do NOT commit the regenerated baselines yet — Task 2 (human-verify) gates them. First confirm the content-stream floor tests are green UNTOUCHED: pdf-digit-order, pdf-glyph-coverage, pdf-bold-rendering, pdf-bidi, 30-issue-delta, and the quick-* list tests. Then run the full suite (`npm test`) and confirm green. If any floor test is RED, STOP — that signals a real RTL/order/severity regression to fix in the owning render plan, not a baseline to regenerate.
  </action>
  <verify>
    <automated>node tests/pdf-digit-order.test.js && node tests/pdf-glyph-coverage.test.js && node tests/pdf-bold-rendering.test.js && node tests/pdf-bidi.test.js && node tests/30-issue-delta.test.js && npm test</automated>
  </verify>
  <done>
    - The 5 baselines are regenerated via the README protocol (not hand-edited).
    - All content-stream floor tests stay green untouched; `npm test` is green (including the 6 new 34-*.test.js GREEN and pdf-latin-regression matching the regenerated baselines) (D-11).
    - No baseline is committed until Task 2 signs off.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of the redesigned PDF before committing baselines</name>
  <action>Pause for a blocking human visual check (see how-to-verify). The executor presents the regenerated fixtures + a live English and Hebrew export and waits for "approved" before committing the new SHA-256 baselines. On rejection, route the described mismatch back to the owning render plan (34-06/07/09).</action>
  <what-built>
    The redesigned session-export PDF (header band, embedded logo + keyline, client card with localized pill + derived Session #N, leaf-diamond section headings, free-text body, two-bar before/after severity, footer band) and 5 regenerated SHA-256 baselines awaiting visual sign-off.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/34-session-pdf-export-visual-polish/design-mockups/FINAL-mockup.html` in a browser (shows Hebrew RTL + English LTR side by side) — this is pixel-truth.
    2. Build/export a real session PDF in English AND in Hebrew (set the app language, export a saved session with ≥2 severity issues and a multi-page body). Use the regenerated fixtures (en/de/cs/he/he-mixed) as additional references.
    3. Compare against the mockup: (a) mint header band + logo under the green keyline + title/subtitle, no "Sessions Garden" letterhead; (b) cream client card, 23pt name, Date · Session #N · green localized pill; (c) leaf-diamond headings + vein rule; (d) two-bar before/after severity (red before longer than green after, flat fills); (e) footer band — made-with mark + small logo + Page X of Y + "Exported on" date.
    4. RTL check (Hebrew): every block anchors to the right, bars fill from the right, numerals read correctly, footer zones mirrored.
    5. Confirm the regenerated baselines reflect THIS verified output (no silent visual regression).
  </how-to-verify>
  <resume-signal>Type "approved" to commit the regenerated baselines, or describe the visual mismatches to fix (routed back to the owning render plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated golden baselines → CI gate | Baselines are derived from first-party fixtures with pinned CreationDate/ID; no external input; the human checkpoint prevents a wrong baseline being trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-10 | Tampering | golden SHA-256 baselines | low | mitigate | Regeneration follows the README protocol and is gated by a blocking human visual verification before commit (D-11) — prevents a silent regression baked into the baseline. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Content-stream floor tests green untouched; `npm test` green; 6 new 34-*.test.js green.
- Human visual sign-off confirms English + Hebrew exports match the FINAL mockup before baselines are committed.
</verification>

<success_criteria>
The redesigned PDF is the new golden reference: deliberately regenerated, visually verified against the mockup (EN + HE), full suite green — closing D-11 and the phase.
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-10-SUMMARY.md` when done.
</output>
