---
phase: 34-session-pdf-export-visual-polish
plan: 8
type: execute
wave: 3
depends_on: [34-05, 34-04]
files_modified:
  - assets/add-session.js
  - assets/export-modal.js
autonomous: true
requirements: [PDFX-03]
must_haves:
  truths:
    - "D-13/PDFX-03: exporting with a dirty form (window.PortfolioFormDirty()) or a never-saved new session shows a non-blocking 'Save & export' / 'Keep editing' prompt — not a hard block, not a silent stale export."
    - "D-13: 'Save & export' persists via a reusable save extracted from the inline save handler (validate → add/update → return {savedId,isNew}, NO 600ms redirect), then continues the export so a brand-new session gains an id and the export derives the correct ordinal for savedId."
    - "D-13: save-validation failure aborts the export and keeps the user editing; there is no 'export without saving' path; the save button keeps its exact existing behavior (behavior-preserving extraction guarded by the green suite)."
  artifacts:
    - "Reusable saveSessionForm() in add-session.js (returns {savedId,isNew} or null on validation failure, no redirect); the save-button submit handler delegates to it then owns the redirect; saveSessionForm exposed via the __exportModalInit ctx; the dirty-gated prompt wired into the export trigger in export-modal.js."
  key_links:
    - "export trigger (exportSessionBtn/openExportDialog) checks PortfolioFormDirty() || !getEditingSession() → prompt → ctx.saveSession() → export proceeds with savedId so deriveSessionOrdinal (34-05) locates the just-saved session (D-13)."
---

<objective>
Fence the export behind an honest save (PDFX-03 / D-13). Extract a reusable save function from the inline submit handler (`add-session.js:1066-1153`) that validates, persists, and returns `{savedId,isNew}` WITHOUT the 600ms redirect (the caller owns navigation), signalling validation failure to the caller. Wire a non-blocking "Save & export" / "Keep editing" prompt into the export trigger so a client never receives a PDF of unsaved/discarded edits, and a brand-new session gains an id and a correct derived ordinal. This is a Phase-31-style behavior-preserving extraction guarded by the green suite.

Purpose: Complete FN-1 — the ordinal is only honest for a saved session; the guard guarantees export always runs on saved data (D-13).
Output: `saveSessionForm()` in `assets/add-session.js` (exposed via ctx) + the dirty-gated prompt in `assets/export-modal.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ saveSessionForm() (add-session.js) + ctx.saveSession exposure; ★ Save-before-export prompt wiring (export-modal.js)
- deriveSessionOrdinal + buildSessionPDF contract (34-05); render fns (34-06/07/09); logo (34-01); i18n prompt keys (34-02); tests (34-03/04); baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-CONTEXT.md
@assets/add-session.js
@assets/export-modal.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract reusable saveSessionForm() from the inline submit handler (behavior-preserving)</name>
  <read_first>
    - tests/34-save-before-export.test.js (the RED PDFX-03 contract from 34-04 — implement to GREEN)
    - assets/add-session.js lines 1066-1153 (the submit handler: validation 1069-1096, sessionType/fields 1098-1106, persist add/update 1108-1148, the formSaving + 600ms redirect 1149-1152), 45-46/89-90 (formDirty/formSaving + PortfolioFormDirty), 702-715 (getIssuesPayload/validateIssues), 968-979 (__exportModalInit ctx)
    - 34-CONTEXT.md § Specifics (PDFX-03 extraction risk — validation failure must be signalled to the caller, not just toast+return) + $HOME memory reference-test-shape-coupling-extractions (Phase-31 extraction guard pattern)
  </read_first>
  <files>assets/add-session.js</files>
  <behavior>
    - saveSessionForm() validates (clientId, date, issues, heart-shield) exactly as the inline handler does; on any validation failure it shows the same toast AND returns null (signal to caller), performing no write.
    - On success it add/updates the session and returns {savedId, isNew} (isNew = no prior editingSession) — NO window.location redirect inside the function.
    - The save BUTTON path keeps its exact current behavior: after a successful saveSessionForm() it sets formSaving and does the 600ms redirect to ?sessionId=savedId.
  </behavior>
  <action>
    Extract the body of the submit handler into a reusable `async function saveSessionForm()` that: runs the existing validation (return null + the existing toast on failure — signal failure, do not just toast+return into the void), builds the same payload, calls PortfolioDB.addSession/updateSession, and returns {savedId, isNew}. Remove the 600ms `window.location.href` redirect FROM the function (caller owns navigation). Rewire the submit listener (the save button) to `const r = await saveSessionForm(); if (!r) return; formSaving = true; setTimeout(() => location.href = './add-session.html?sessionId=' + r.savedId, 600);` — preserving the exact save-button UX. Expose the function to export-modal by adding `saveSession: saveSessionForm` to the `window.__exportModalInit({...})` ctx (968-979). This is a verbatim-behavior extraction (Phase-31 pattern) — do not change validation order, toasts, payload shape, or the redirect timing of the save button.
  </action>
  <verify>
    <automated>node tests/34-save-before-export.test.js && node tests/30-export-stepper.test.js</automated>
  </verify>
  <done>
    - saveSessionForm() validates → persists → returns {savedId,isNew} (null on validation failure) with NO redirect inside it; the save button still validates/persists/redirects identically (behavior-preserving).
    - ctx now carries saveSession; `node tests/34-save-before-export.test.js` advances (extraction half GREEN) and `node tests/30-export-stepper.test.js` stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the non-blocking Save-before-export prompt into the export trigger</name>
  <read_first>
    - assets/export-modal.js lines 27-31 (ctx — now includes saveSession), 531-549 (exportCloseDialog + confirmDialog usage pattern to mirror), 551-583 (exportHandleDownloadPdf), 667-696 (openExportDialog), 778-795 (exportSessionBtn trigger)
    - assets/add-session.js lines 89-90 (PortfolioFormDirty), 762/989 (existing isDirty check patterns to mirror) + i18n keys export.unsaved.{title,body,saveExport,keepEditing} (34-02)
    - tests/34-save-before-export.test.js (the contract)
  </read_first>
  <files>assets/export-modal.js</files>
  <action>
    In the export trigger (the exportSessionBtn click → openExportDialog path), before opening the dialog, check dirty/unsaved state: `const dirty = window.PortfolioFormDirty && window.PortfolioFormDirty(); const unsaved = !ctx.getEditingSession();`. If `dirty || unsaved`, surface a non-blocking prompt using App.confirmDialog with export.unsaved.title/body and confirm = export.unsaved.saveExport / cancel = export.unsaved.keepEditing (a 2-choice prompt, NOT a hard block). On "Save & export": `const r = await ctx.saveSession(); if (!r) return;` (validation failure aborts the export and keeps the user editing — no PDF), else proceed to open/run the export using r.savedId as the session id so deriveSessionOrdinal (34-05) locates the just-saved session and a brand-new session gets a correct ordinal. On "Keep editing": dismiss and do NOT export. When clean AND saved, open the export dialog directly as today. Ensure the chosen savedId flows into the ordinal derivation in exportHandleDownloadPdf (pass it through _exportState/sessionData rather than relying on a stale editingSession for a brand-new save). There is no path that exports without saving (D-13).
  </action>
  <verify>
    <automated>node tests/34-save-before-export.test.js && node tests/30-export-stepper.test.js</automated>
  </verify>
  <done>
    - `node tests/34-save-before-export.test.js` is fully GREEN: prompt on dirty/unsaved; Save&export persists (no redirect) then exports with the correct ordinal for savedId; Keep-editing dismisses; validation failure aborts the export; clean+saved exports directly.
    - No "export without saving" path exists; `node tests/30-export-stepper.test.js` stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| add-session form → IndexedDB persist → export | First-party single-user PWA; the guard only adds a save step before a local export; no auth/network/access-control surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08 | Tampering | data integrity (stale/unsaved export) | low | mitigate | The guard prevents a client PDF reflecting unsaved/discarded edits; validation failure aborts the export (D-13) — a correctness mitigation, not a security boundary. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- 34-save-before-export GREEN; 30-export-stepper green.
- Save button behavior preserved (validate/persist/redirect); extracted save has no internal redirect.
</verification>

<success_criteria>
Export is fenced behind an honest save: a non-blocking prompt, a behavior-preserving reusable save, correct ordinal for new sessions, and no stale/unsaved export path (D-13 / PDFX-03).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-08-SUMMARY.md` when done.
</output>
