---
phase: 34-session-pdf-export-visual-polish
plan: 6
type: execute
wave: 2
depends_on: [34-01, 34-03]
files_modified:
  - assets/pdf-export.js
autonomous: true
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-01: page 1 opens with a full-bleed mint header band (#e2f3e3) carrying the offline logo under a thin green keyline, a 20pt bold deep-green title and an 11pt subtitle — and NO 'Sessions Garden' wordmark as a letterhead."
    - "D-02: below the band sits a cream rounded client card (#fdf8f0, #c8e6d4 border) with the 23pt bold client name and a meta row Date · Session #N · a green localized pill."
    - "D-04/FN-2: the pill renders the localized sessionType value verbatim (no hardcoded label)."
    - "D-05/FN-3: the logo is embedded via doc.addImage(window.IconLogoBase64,'PNG',…) — never fetched."
    - "D-10: header band, title/subtitle, client name, meta row and pill all anchor to the start edge under RTL and digits keep visual order (no regression vs Phase 23)."
    - "D-12: palette/typography/spacing match 34-UI-SPEC + FINAL mockup (icon-sampled hexes, Heebo normal+bold only, 4pt-grid spacing, 71pt content inset, full-bleed bands)."
  artifacts:
    - "drawHeaderBand() and drawClientCard() in pdf-export.js, REPLACING drawPage1Header() (878-912); the lazy-load chain loads icon-512-base64.js and the body cursor starts below the card."
  key_links:
    - "drawHeaderBand consumes window.IconLogoBase64 (34-01) via addImage; drawClientCard renders sessionData.sessionNumber (34-05) + the localized sessionType pill (D-04); both anchor via the docDir convention reusing shapeForJsPdf — never re-implementing anchoring (D-10)."
---

<objective>
Replace the plain centered page-1 header (`drawPage1Header`, pdf-export.js:878-912) with the locked branded opening: a full-bleed mint header band (embedded logo + title + subtitle, no letterhead) and a cream client card (name + Date · Session #N · localized pill). Embed the logo from the base64 module (34-01) — zero fetch. Every new text/shape anchors by `docDir` and goes through `shapeForJsPdf`, preserving the Phase 23 RTL invariant by construction.

Purpose: The first thing a client sees must read as an intentionally designed personal record (D-01/D-02), localized and RTL-correct (D-04/D-10), fully offline (D-05).
Output: `drawHeaderBand()` + `drawClientCard()` in `assets/pdf-export.js`; `drawPage1Header()` removed; the lazy-load chain loads the logo module.

## Artifacts this phase produces (★ = owned by this plan)
- ★ drawHeaderBand(), ★ drawClientCard() (pdf-export.js); ★ logo module loaded in ensureDeps
- logo asset (34-01); i18n subtitle (34-02); render tests (34-03); drawSeverityBlock/drawFooterBand (34-09/07); deriveSessionOrdinal + contract (34-05); saveSessionForm() (34-08); baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-UI-SPEC.md
@.planning/phases/34-session-pdf-export-visual-polish/34-DESIGN-DECISIONS.md
@.planning/phases/34-session-pdf-export-visual-polish/design-mockups/FINAL-mockup.html
@assets/pdf-export.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: drawHeaderBand — mint full-bleed band, embedded logo + keyline, title + subtitle</name>
  <read_first>
    - assets/pdf-export.js lines 91-114 (ensureDeps lazy-load chain — add the logo module load), 193-206 (registerFonts), 640-690 (buildSessionPDF setup, docDir at 682, pageWidth), 699-724 (layout constants), 849-872 (drawTextLine — the start-anchor convention to mirror), 878-912 (drawPage1Header being replaced)
    - 34-RESEARCH.md § Pattern 1 (RTL-safe new block) + § jsPDF primitives (rect/roundedRect/addImage)
    - 34-UI-SPEC.md § Component Inventory item 1 (Header band) + § Spacing/Typography/Color; design-mockups/FINAL-mockup.html (pixel-truth CSS for the band)
    - 34-CONTEXT.md D-01, D-12; assets/branding/icon-512-base64.js (window.IconLogoBase64)
  </read_first>
  <files>assets/pdf-export.js</files>
  <action>
    Add `'./assets/branding/icon-512-base64.js'` to the ensureDeps lazy-load chain (after the Heebo modules) so window.IconLogoBase64 is present at render time. Add `function drawHeaderBand()`: draw a full-bleed mint rect (setFillColor('#e2f3e3'); rect(0,0,595,bandHeight≈96,'F')) with content inset to MARGIN_X (71). At the START edge (left in LTR / right in RTL — branch on docDir) draw a 48pt logo tile via doc.addImage(window.IconLogoBase64,'PNG',x,y,48,48) edge-to-edge (no white frame), then a green keyline over it: setDrawColor('#3a7d5f'); setLineWidth(~1.5); roundedRect(x,y,48,48,10,10,'S'). Beside the logo (12–14pt gap, on the trailing side of the logo per docDir) draw the title (20pt bold #345e34, via session.copy.title shaped through shapeForJsPdf, start-anchored using the drawTextLine right-align/isInputVisual:false convention) and the subtitle below it (11pt normal #456b42, pdf.header.subtitle). Do NOT render a "Sessions Garden" wordmark as letterhead (D-01). Return the y where the card should begin. Use ONLY Heebo normal/bold (no 500 weight). Every doc.text passes isInputVisual:false and anchors by docDir (D-10).
  </action>
  <verify>
    <automated>node tests/34-logo-embed.test.js && node tests/pdf-glyph-coverage.test.js && node tests/pdf-digit-order.test.js</automated>
  </verify>
  <done>
    - `node tests/34-logo-embed.test.js` is GREEN (image XObject emitted, offline build succeeds — FN-3).
    - Header band is full-bleed mint with the logo under a green keyline and a localized title+subtitle; no letterhead wordmark (D-01).
    - RTL spine green: pdf-glyph-coverage + pdf-digit-order pass (D-10); all new text uses isInputVisual:false + docDir anchor + Heebo normal/bold only (D-12).
  </done>
</task>

<task type="auto">
  <name>Task 2: drawClientCard — cream card, client name, meta row (Date · Session #N · localized pill); remove drawPage1Header</name>
  <read_first>
    - assets/pdf-export.js lines 726-729 (clientName/sessionType locals), 849-872 (drawTextLine), 938-951 (render entry: `y = drawPage1Header()` + ensureRoom), 878-912 (drawPage1Header to delete)
    - 34-RESEARCH.md § Pattern 1 + § Common Pitfalls (Pitfall 5 ensureRoom for the whole card)
    - 34-UI-SPEC.md § Component Inventory item 2 (Client card + meta row + pill) + § Color (card #fdf8f0, border #c8e6d4 per D-02 — NOT the UI-SPEC FLAG-2 #bfe0b0; D-02 matches the FINAL mockup, pill #e8f5ee/#1e5c3a); 34-CONTEXT.md D-02, D-04, D-12; FINAL-mockup.html
  </read_first>
  <files>assets/pdf-export.js</files>
  <action>
    Add `function drawClientCard()`: measure the card block height first and call ensureRoom(cardHeight) so the card never splits across a page (Pitfall 5). Draw a cream rounded card: setFillColor('#fdf8f0'); setDrawColor('#c8e6d4') (D-02 card border — matches the FINAL mockup; this overrides the UI-SPEC FLAG-2 #bfe0b0; see 34-RESEARCH Open Q1 RESOLVED); roundedRect(MARGIN_X, y, USABLE_W, cardHeight, 10, 10, 'FD'). Inside (horizontal padding ~20pt, vertical ~16pt): the client name 23pt bold #345e34 start-anchored; then a meta row 12pt below carrying Date (bold #456b42 key + muted #5f5c72 value) · Session #N (key + value from sessionData.sessionNumber) · a green pill. Pill = fully-rounded roundedRect filled #e8f5ee with the localized sessionType label (sessionData.sessionType, verbatim — D-04) in 11pt bold #1e5c3a, ~12pt horizontal padding. Under RTL the row flows right→left and the pill mirrors; numerals keep visual order (isInputVisual:false). Then DELETE drawPage1Header and change the render entry (pdf-export.js:942) so the body cursor starts as: `drawHeaderBand(); var y = drawClientCard();`. Keep drawRunningHeader (pages 2+) unchanged — the band/card are page-1 only. Reuse shapeForJsPdf / the drawTextLine anchor convention; do not re-implement anchoring (D-10).
  </action>
  <verify>
    <automated>node tests/34-pill-localized.test.js && node tests/34-rtl-newblocks.test.js; node tests/pdf-digit-order.test.js && node tests/pdf-glyph-coverage.test.js</automated>
  </verify>
  <done>
    - `node tests/34-pill-localized.test.js` GREEN (localized pill verbatim across 12 type×locale cases — D-04/FN-2).
    - drawPage1Header is removed; the render entry calls drawHeaderBand() then drawClientCard(); the card uses ensureRoom so it never splits (Pitfall 5).
    - RTL spine stays green (pdf-digit-order + pdf-glyph-coverage); 34-rtl-newblocks advances toward GREEN for the card/pill blocks (D-10). Card colours/typography match 34-UI-SPEC (D-02/D-12).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's IndexedDB data → local jsPDF render → Blob download | First-party data; the only "input" is the therapist's own session text/master-data; logo is a bundled asset; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-06 | Tampering | client name → header/card text | low | mitigate | Name is shaped through the existing shapeForJsPdf path (no new injection surface); export filename still sanitized by slugify (pdf-export.js:145-155). |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- 34-logo-embed + 34-pill-localized GREEN; 34-rtl-newblocks advancing; pdf-digit-order + pdf-glyph-coverage green.
- drawPage1Header removed; header band + client card render page 1; logo embedded (no fetch).
</verification>

<success_criteria>
Page 1 opens with the locked branded header band + client card — localized pill (D-04), embedded offline logo (D-05), RTL-correct (D-10), palette/typography per spec (D-01/D-02/D-12).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-06-SUMMARY.md` when done.
</output>
