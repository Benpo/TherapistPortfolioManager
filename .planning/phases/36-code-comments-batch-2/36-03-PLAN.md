---
phase: 36-code-comments-batch-2
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - assets/crashlog.js
  - assets/report.js
  - assets/disclaimer.js
  - assets/snippets-seed.js
  - assets/crop.js
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "crashlog.js and report.js are de-phased + refined to the four-slot banner shape (D-03, D-04): their `Phase N` / `Plan N` build tags are stripped while the living-doc requirement IDs OBS-01 (crashlog) and OBS-02 (report) are PRESERVED as reworded traceability"
    - "disclaimer.js, snippets-seed.js, and crop.js carry four-slot banners (D-03); snippets-seed.js's `Phase N` title tag and crop.js's / disclaimer.js's archaeology are de-phased into plain what-it-does prose (D-04). snippets-seed.js is seed data — its banner is concise per D-05 (it registers SNIPPETS_SEED; there is little 'invariants' to invent)"
    - "All build-history archaeology is removed from the 5 files; living-doc requirement IDs (REQ-NN / OBS-NN / VER-NN) and design rationale are preserved (D-04)"
    - "The edits are provably comments-only: `npm test` stays green AND a deterministic comments-only strip-and-compare shows zero code lines changed across all 5 files (D-06)"
  prohibitions:
    - "The three deferred giants are NOT modified — `git diff --name-only` for this plan must contain none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01 scope fence)"
    - "Zero code lines change in any file — comment-only plan (D-06)"
  artifacts:
    - path: assets/crashlog.js
      provides: "De-phased + refined four-slot banner (OBS-01 crash-log capture; OBS-01 preserved)"
    - path: assets/report.js
      provides: "De-phased + refined four-slot banner (OBS-02 report controller; OBS-02 preserved)"
    - path: assets/disclaimer.js
      provides: "De-phased + refined four-slot banner (disclaimer / T&C gate logic)"
    - path: assets/snippets-seed.js
      provides: "Concise de-phased banner (seed snippet pack; registers SNIPPETS_SEED) — D-05 proportionate"
    - path: assets/crop.js
      provides: "De-phased + refined four-slot banner (shared photo crop modal; registers window.CropModule)"
  key_links:
    - from: "the 5 banners"
      to: "assets/export-modal.js lines 1-19 (convention template) + .planning/codebase/ARCHITECTURE.md + CONVENTIONS.md"
      via: "each banner copies the four-slot shape (proportionate for seed data per D-05): owns · public window.* surface · cross-window.* deps · invariants"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-03 (batch-2 — the mid group) — Extend the Phase 32 convention to `assets/crashlog.js`, `assets/report.js`, `assets/disclaimer.js`, `assets/snippets-seed.js`, and `assets/crop.js`. Two comment-only jobs per D-04: (a) each file gets a four-slot banner (D-03: owns · public `window.*` surface · cross-`window.*` deps · invariants), proportionate per D-05 for the seed-data module; (b) all build-history archaeology (`Phase N` / `Plan N` tags on crashlog/report/snippets-seed, plus any body tags in disclaimer/crop) is rewritten into plain prose. Crucially, the living-doc requirement IDs OBS-01 (crashlog) and OBS-02 (report) are PRESERVED — only their attached `(Phase N …)` parenthetical is stripped. Template = `assets/export-modal.js:1-19`. Verify comment-only per D-06.

Depends on 36-01 for sequencing only (D-02); disjoint file set → no artifact dependency.

Purpose: crashlog/report are the observability seam a maintaining agent reaches for when diagnosing a user problem — accurate banners that keep the OBS-01/OBS-02 traceability are high-value; crop and disclaimer are shared/legal modules worth orienting.
Output: 5 comment-only edited files; no new files; no new code symbols; no behavior change. The three giants (`backup.js`, `app.js`, `pdf-export.js`) are NOT touched (D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-comments-batch-2/36-CONTEXT.md
@.planning/phases/36-code-comments-batch-2/36-VALIDATION.md
@.planning/phases/32-readme-code-comments/32-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@assets/export-modal.js
</context>

## Artifacts this phase produces (this plan)

- **No new file paths.** All five targets already exist; this plan edits comments only.
- **No new code symbols** — no new functions, classes, flags, fields, `window.*` globals, or exports. The only artifacts are the banner comment blocks. Nothing here for a drift / source-grounding pass to flag.

<tasks>

<!-- The de-phase verify gate below greps for build-history literals that also appear in the <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: crashlog.js + report.js — de-phase (strip phase tags, KEEP OBS-01/OBS-02) + refine</name>
  <files>assets/crashlog.js, assets/report.js</files>
  <read_first>
    - assets/crashlog.js (JSDoc header `crashlog.js — OBS-01 local crash-log capture module (Phase 29)`; ~480 lines — read for the `window.onerror` + `unhandledrejection` capture, the dual IDB + `localStorage` mirror storage, prune-on-write, and the `CrashLog.logError/getEntries/clear` seam; note it uses `var CrashLog = (function(){…})()` and embeds its own pre-i18n string table)
    - assets/report.js (JSDoc header `report.js — OBS-02 "Report a problem" controller (Phase 29, Plan 03)`; ~418 lines — read for the redact-then-editable-preview, Copy-report, and mailto handoff; it reads `CrashLog.getEntries` + version.js integrity state)
    - assets/export-modal.js (lines 1-19 — the four-slot banner content model)
    - .planning/codebase/ARCHITECTURE.md (the `CrashLog` + `Report` component rows; the Error-Handling section: early buffer → crashlog.js persists to IDB — the deps + invariants source)
  </read_first>
  <action>
    crashlog.js — DE-PHASE + REFINE. Strip the `(Phase N)` build tag from the header title but KEEP `OBS-01` as reworded living-doc traceability (e.g. "the OBS-01 local crash-log capture module" is fine; only the phase parenthetical goes). Complete the four-slot banner: owns (uncaught-error + unhandled-rejection capture; dual IDB + localStorage-mirror storage; prune-on-write to the entry/age caps); public surface (the `CrashLog.logError/getEntries/clear` seam it registers); dependencies (raw IndexedDB via its own path — note it deliberately bypasses openDB so it survives an IDB-open failure; embeds a pre-i18n string table because it loads before i18n); invariants (never-throwing guard idiom; append() is serialized so concurrent logs don't lose updates). De-phase the body.

    report.js — DE-PHASE + REFINE. Strip the `(Phase N, Plan N)` build tag but KEEP `OBS-02`. Complete the four-slot banner: owns (the "Report a problem" controller — redacted editable preview, Copy report, mailto handoff); public surface; dependencies (`CrashLog.getEntries`, the version.js integrity state, `App` for i18n); invariants (nothing is transmitted automatically — Copy/mailto only; the preview is redacted before display). De-phase the body.

    Use the SAME dialect list + PRESERVE rules for BOTH: strip phase/plan, D-NN, T-/RFCT-, acceptance-test/code-review, gap/round/change, date-prefixed bug tickets, ISO dates; PRESERVE bare REQ-NN/OBS-NN/VER-NN + rationale. Scan EVERY comment incl. trailing comments on code lines; rewrite comment text only, never code. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/crashlog.js assets/report.js && echo DEPHASE_CLEAN</automated>
    <automated>grep -q 'OBS-01' assets/crashlog.js && grep -q 'OBS-02' assets/report.js && echo REQ_IDS_PRESERVED</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The archaeology grep over BOTH files returns empty — ZERO matches across all build-history dialects (the `(Phase N)` / `(Phase N, Plan N)` header tags are gone).
    - OBS-01 remains in crashlog.js and OBS-02 remains in report.js (grep passes) — living-doc traceability preserved, phase parenthetical stripped.
    - Each file opens with a four-slot banner (owns · public surface · window.* deps · invariants) — manual.
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>crashlog.js and report.js carry four-slot banners with phase tags de-phased and OBS-01/OBS-02 preserved as reworded traceability; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: disclaimer.js + snippets-seed.js + crop.js — de-phase + refine (seed-data banner is concise per D-05)</name>
  <files>assets/disclaimer.js, assets/snippets-seed.js, assets/crop.js</files>
  <read_first>
    - assets/disclaimer.js (opens with `//` header lines `Sessions Garden — Disclaimer / T&C gate logic`; ~357 lines — read for language detection, checkbox validation, acceptance storage, receipt generation, redirect-to-app, readonly mode)
    - assets/snippets-seed.js (JSDoc header carrying a `Phase N` title tag; ~344 lines — mostly seed data: emotion-name snippets × 4-locale meaning paragraphs; read for the `SNIPPETS_SEED` global it registers)
    - assets/crop.js (JSDoc header `CropModule — Shared photo crop module … Extracted from add-client.js`; ~289 lines — read for the canvas-based crop modal and the `window.CropModule` registration)
    - assets/export-modal.js (lines 1-19 — the four-slot banner content model)
    - .planning/codebase/ARCHITECTURE.md (the `CropModule` component row; the Gate Layer / legal-page flow for disclaimer; SNIPPETS_SEED in the resolution chain)
  </read_first>
  <action>
    disclaimer.js — DE-PHASE + REFINE. Keep or convert to the four-slot banner: owns (the disclaimer / Terms gate logic — language detection, checkbox validation, acceptance storage, receipt generation, redirect-to-app, readonly mode); public surface; dependencies (the `localStorage` terms keys, `App` i18n); invariants (gates the 5 app pages; readonly mode views terms without accepting). De-phase any body archaeology.

    snippets-seed.js — DE-PHASE + CONCISE banner (D-05: this is seed data — do not fabricate an "invariants" section where there is nothing to say). Strip the `Phase N` title tag. A concise banner stating what it is (the seed snippet pack — emotion-name snippets × 4-locale meanings) and what it registers (the `SNIPPETS_SEED` global consumed by the snippet engine) is sufficient. De-phase any body tags.

    crop.js — DE-PHASE + REFINE. Four-slot banner: owns (the shared canvas-based photo crop modal, usable on any page — extracted so add-client and add-session both use it); public surface (its `window.CropModule` registration); dependencies; invariants (pure Canvas API + Pointer Events; non-destructive cancel). De-phase the body.

    Use the SAME dialect list + PRESERVE rules for ALL THREE: strip phase/plan, D-NN, T-/RFCT-, acceptance-test/code-review, gap/round/change, date-prefixed bug tickets, ISO dates; PRESERVE REQ-NN/OBS-NN/VER-NN + rationale. Scan EVERY comment incl. trailing comments on code lines; rewrite comment text only, never code. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/disclaimer.js assets/snippets-seed.js assets/crop.js && echo DEPHASE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The archaeology grep over all THREE files returns empty — ZERO matches across all build-history dialects (snippets-seed's `Phase N` title tag is gone); the gate does not flag bare REQ-NN/OBS-NN/VER-NN.
    - disclaimer.js and crop.js open with four-slot banners; snippets-seed.js opens with a concise banner naming `SNIPPETS_SEED` (D-05 proportionate — not padded with empty sections) — manual.
    - Comments-only: covered by Task 3's strip-and-compare gate (all three files included).
  </acceptance_criteria>
  <done>disclaimer.js and crop.js carry four-slot banners and snippets-seed.js carries a concise D-05 banner; all build-history de-phased; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 5-file edit is comments-only</name>
  <files>assets/crashlog.js, assets/report.js, assets/disclaimer.js, assets/snippets-seed.js, assets/crop.js</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-02-PLAN.md (Task 3 — the authoritative strip-and-compare gate this mirrors + the soundness argument)
    - .planning/phases/36-code-comments-batch-2/36-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the five files this plan touched changed comments only. Run the strip-and-compare with the comment edits from Tasks 1–2 still UNCOMMITTED, so the baseline is git HEAD: for each file, strip block/line/trailing comments and collapse whitespace from BOTH the HEAD version (`git show HEAD:file`) and the working-tree version, then assert byte-equality. Any inequality means a code token changed → the gate FAILS; revert before committing. One-shot scratchpad Node script (not a committed test). If you committed content per-task, set `BASE` to the commit immediately before this plan's first edit and compare against that ref. Confirm the full suite is green. Confirm the scope fence: `git diff --name-only` lists none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01). Only commit once all gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';let bad=0;for(const f of ['assets/crashlog.js','assets/report.js','assets/disclaimer.js','assets/snippets-seed.js','assets/crop.js']){const a=strip(cp.execSync('git show '+base+':'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all five files (D-06), run uncommitted against git HEAD (or against the pre-plan `BASE` ref).
    - If any file prints `CODE CHANGED`, a code line was modified — revert before committing.
    - Scope fence (D-01): `git diff --name-only` excludes assets/backup.js, assets/app.js, assets/pdf-export.js.
  </acceptance_criteria>
  <done>The 5-file comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to existing browser-tier modules. No new runtime code, network calls, or data flows. crashlog's storage path and report's mailto/clipboard handoff are unchanged — banners only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03 | Tampering | comment edits silently altering code in crashlog.js / report.js / disclaimer.js / snippets-seed.js / crop.js | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green `npm test` prove no behavior change (D-06); edits isolated in their own commit(s). |

Honest framing: comment-only edits add no attack surface. No new schema push applies (schema-gate: N/A for comments). The only risk is an accidental code edit, caught mechanically by the D-06 gate.
</threat_model>

<verification>
- `npm test` green after the edits.
- Archaeology grep over all 5 files returns empty across EVERY build-history dialect — full de-phasing complete (D-04); OBS-01/OBS-02 preserved.
- Each file opens with a four-slot banner (snippets-seed concise per D-05).
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-06).
- Scope fence (D-01): the three giants are untouched.
</verification>

<success_criteria>
The 5 mid modules carry Phase-32-convention banners (proportionate for the seed-data module per D-05) with all build-history archaeology de-phased and OBS-01/OBS-02 traceability preserved; the edits are provably comments-only (strip-and-compare + green suite).
</success_criteria>

<output>
Create `.planning/phases/36-code-comments-batch-2/36-03-SUMMARY.md` when done.
</output>
