---
phase: 36-code-comments-batch-2
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - assets/landing.js
  - assets/license.js
  - assets/snippets.js
  - assets/backup-modal.js
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "landing.js's thin `/* === LANDING PAGE LOGIC === */` opener is refined into a four-slot banner (D-03): what it owns (the marketing/landing page — language switching, theme detection, smooth scroll, Lemon Squeezy checkout entry) · public surface · deps · invariants"
    - "license.js keeps its JSDoc style but is de-phased + refined to the four-slot shape (D-03, D-04): activation/validation surface, its Lemon Squeezy API dependency, and its `localStorage` key invariants — the STORE_ID/PRODUCT_ID public-constant rationale preserved"
    - "snippets.js and backup-modal.js are de-phased — the `Phase 24 Plan 04` title tag on snippets and backup-modal's heavier archaeology (Phase-number, round-number, acceptance-test, Change-number, and ISO-date tags) are rewritten into plain what-it-does prose that keeps the WHY (D-04)"
    - "All planning IDs are stripped from the 4 files per option 3 (D-07, supersedes the Phase-32 keep approach): phase/plan, D-NN, T-N-N-N, CR-/WR-, date-prefixed bug tickets, ISO dates, `.planning/` cross-refs, AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST- are converted to plain prose; only the KEEP allowlist (`AES-256`/`SHA-256`/`UTF-8`/`base64`/schema `v1–v6`/`file.js:NNN`) + design/security rationale survive (D-04, D-07)"
    - "Every banner and de-phase decision follows `36-COMMENT-STYLE-GUIDE.md` — the canonical comment standard read FIRST by the executor (D-08)"
    - "The edits are provably comments-only: `npm test` stays green AND a deterministic comments-only strip-and-compare shows zero code lines changed across all 4 files (D-06)"
  prohibitions:
    - "The three deferred giants are NOT modified — `git diff --name-only` for this plan must contain none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01 scope fence). NOTE: assets/backup-modal.js IS in scope; assets/backup.js (the giant) is NOT."
    - "Zero code lines change in any file — comment-only plan (D-06)"
  artifacts:
    - path: assets/landing.js
      provides: "Four-slot landing-page banner (refine the thin block opener) + de-phased body"
    - path: assets/license.js
      provides: "De-phased + refined JSDoc four-slot banner (activation/validation)"
    - path: assets/snippets.js
      provides: "De-phased four-slot banner (strip the Phase-number title tag)"
    - path: assets/backup-modal.js
      provides: "Heavily de-phased four-slot banner (Backup & Restore modal)"
  key_links:
    - from: "the 4 banners"
      to: "assets/export-modal.js lines 1-19 (convention template) + .planning/codebase/ARCHITECTURE.md + CONVENTIONS.md"
      via: "each banner copies the four-slot shape: owns · public window.* surface · cross-window.* deps · invariants"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-03 (batch-2 — the larger-mid group) — Extend the Phase 32 convention to `assets/landing.js`, `assets/license.js`, `assets/snippets.js`, and `assets/backup-modal.js`. Two comment-only jobs per D-04: (a) each file gets a four-slot banner (D-03: owns · public `window.*` surface · cross-`window.*` deps · invariants); landing.js has only a thin block opener to refine, license.js has a decent JSDoc header to de-phase + refine, snippets.js carries a `Phase N Plan N` title tag, and backup-modal.js carries the heaviest archaeology of this group (Phase/round/acceptance-test/Change/ISO-date tags); (b) all planning IDs are stripped per option 3 (D-07 — incl. requirement IDs REQ-/OBS-/VER-). PRIMARY template = `assets/settings.js:1-29` (four labelled slots `OWNS:`/`PUBLIC SURFACE:`/`DEPENDENCIES:`/`CONSTRAINTS:`); export-modal:1-19 is a specialized variant only; every banner follows `36-COMMENT-STYLE-GUIDE.md` (D-08). Verify comment-only per D-06.

Depends on 36-01 for sequencing only (D-02 — batch-1 establishes the rhythm); its file set is disjoint from this plan's, so there is no artifact dependency.

Purpose: these four are the mid-size UI/entry modules a maintaining agent hits often (checkout entry, license activation, snippet engine, backup modal); accurate banners orient without re-reading 500–760-line files.
Output: 4 comment-only edited files; no new files; no new code symbols; no behavior change. The three giants (`backup.js`, `app.js`, `pdf-export.js`) are NOT touched (D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-comments-batch-2/36-CONTEXT.md
@.planning/phases/36-code-comments-batch-2/36-VALIDATION.md
@.planning/phases/32-readme-code-comments/32-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@assets/settings.js
@assets/export-modal.js
</context>

## Artifacts this phase produces (this plan)

- **No new file paths.** All four targets already exist; this plan edits comments only.
- **No new code symbols** — no new functions, classes, flags, fields, `window.*` globals, or exports. The only artifacts are the banner comment blocks. Nothing here for a drift / source-grounding pass to flag.

<tasks>

<!-- The de-phase verify gate below greps for build-history literals that also appear in the <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: landing.js + license.js — refine landing banner + de-phase/refine license banner</name>
  <files>assets/landing.js, assets/license.js</files>
  <read_first>
    - assets/landing.js (opens with `/* === LANDING PAGE LOGIC === */` block comment; `var LS_CHECKOUT_URL = …` near the top; ~762 lines — read for the real surface: language switching, theme detection, smooth scroll, the Lemon Squeezy checkout link/redirect, the licensed-user auto-detect redirect)
    - assets/license.js (opens with a JSDoc header `license.js — Sessions Garden license key activation and validation`; ~583 lines — read for the activation flow, the Lemon Squeezy License API calls, the `STORE_ID`/`PRODUCT_ID` public constants, and the `localStorage` keys it writes)
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — the canonical standard: four labelled slots in order, the ≥3-of-4 reshape-vs-de-phase test, the option-3 strip-ALL-IDs split, KEEP allowlist — D-08)
    - assets/settings.js (lines 1-29 — the PRIMARY four-slot banner content model)
    - assets/export-modal.js (lines 1-19 — SPECIALIZED-VARIANT reference only; do NOT model inline style on it)
    - .planning/codebase/ARCHITECTURE.md (the License Gate Flow + "Reading .env or secrets in client code" anti-pattern — the license deps + invariants source; landing.js is a marketing page, no SW precache)
  </read_first>
  <action>
    landing.js — REFINE. Replace the thin `/* === LANDING PAGE LOGIC === */` block opener with a four-slot banner: owns (the public marketing/landing page — language switching, theme detection, smooth scroll, the Lemon Squeezy checkout entry, and the licensed-user auto-detect redirect); public surface (page module — note whether it registers any `window.*`); dependencies (the cross-`window.*` chain it reads, and the plain `LS_CHECKOUT_URL` constant); invariants (marketing page — not part of the PWA SW precache; the demo iframe is same-origin). Keep it `/* */` or `//` per the file's dominant style.

    license.js — DE-PHASE + REFINE, keeping the JSDoc `/** */` style. Rewrite the header to the four-slot shape: owns (license key activation + client-side validation); public surface; dependencies (the Lemon Squeezy License API — requires internet — and the `STORE_ID`/`PRODUCT_ID` public constants); invariants (validation is client-side only; result persisted to `localStorage` under the activation keys; STORE_ID/PRODUCT_ID are public IDs, never secrets — preserve that rationale). De-phase the whole body.

    Use the SAME strip-ALL-IDs rule (option 3, D-07) for BOTH files: strip phase/plan tags (Phase-number Plan-number, Phase N-N), decision/task IDs (D-NN, T-N-N-N), code-review IDs (CR-/WR-), acceptance-test notes, the prose-shaped tags Gap/round-N/Change-N, date-prefixed bug-ticket refs (six digits + hyphen + three alphanumerics), any ISO date (YYYY-MM-DD), `.planning/` cross-refs (`34-RESEARCH`, `32-CONTEXT`), AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST- — all to plain prose keeping the WHY. PRESERVE only the KEEP allowlist of genuine technical tokens (`AES-256`/`SHA-256`/`UTF-8`/`base64`/schema `v1–v6`/`file.js:NNN`) + the design/security rationale; KEEP live-code cross-refs (`version.js`) but STRIP `.planning/` cross-refs. Scan EVERY comment incl. trailing comments on code lines; rewrite comment text only, never code. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <!-- Option-3 de-phase gate (D-07): ID-shaped-prefix grep must be empty after excluding the KEEP allowlist. Prose tags Gap/round/Change are a MANUAL item below, not this grep. -->
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION))' assets/landing.js assets/license.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The option-3 de-phase grep over BOTH files returns empty — ZERO ID-shaped tokens remain (incl. requirement IDs REQ-/OBS-/VER-), and the KEEP allowlist is NOT flagged.
    - MANUAL (class-(b) prose tags): no `Gap`/`round-N`/`Change N` prose tags survive in either file.
    - landing.js opens with the four labelled slots (the thin `LANDING PAGE LOGIC` opener is replaced); license.js's JSDoc header is rewritten to the four-slot shape and retains the STORE_ID/PRODUCT_ID public-constant rationale as plain prose, no `VER-`/`REQ-` id (manual).
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>landing.js and license.js carry four-slot banners with all build-history de-phased (license keeps JSDoc style + STORE_ID/PRODUCT_ID rationale); suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: snippets.js + backup-modal.js — de-phase + refine (backup-modal is the heavy one)</name>
  <files>assets/snippets.js, assets/backup-modal.js</files>
  <read_first>
    - assets/snippets.js (JSDoc header whose title carries a `Phase N Plan N` tag; ~551 lines — read for the `[data-snippets="true"]` quick-paste engine surface and any `window.Snippets` registration)
    - assets/backup-modal.js (JSDoc header carrying the heaviest archaeology of this group — a Phase-number line plus round-number, acceptance-test, Change-number, and an ISO-date parenthetical; ~513 lines — read for the Backup & Restore modal surface available on every app page and its `window.BackupManager` handshake)
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — canonical standard; the class-(a) grep-enforceable IDs vs class-(b) manual prose tags (Gap/round/Change) split matters for backup-modal's heavy archaeology — D-08)
    - assets/settings.js (lines 1-29 — the PRIMARY four-slot banner content model)
    - assets/export-modal.js (lines 1-19 — SPECIALIZED-VARIANT reference only; do NOT model inline style on it)
    - .planning/codebase/ARCHITECTURE.md (the Backup Export Flow — backup-modal opens the modal, delegates to `BackupManager`; `window.Snippets` in the Shared Globals list — deps + invariants source)
  </read_first>
  <action>
    snippets.js — DE-PHASE + REFINE. Rewrite the header title (strip the `Phase N Plan N` build tag) into a plain what-it-does title, then complete the four-slot banner: owns (the snippet quick-paste engine wiring `[data-snippets="true"]` textareas); public surface (its `window.Snippets` registration, if present); dependencies (the cross-`window.*` chain — `App`, `PortfolioDB`, `SNIPPETS_SEED`); invariants (trigger validation is Unicode-aware; single recall chokepoint). De-phase the body.

    backup-modal.js — HEAVY DE-PHASE + REFINE. This header carries several build-history dialects on the opening lines (a Phase-number line, a round-number, an acceptance-test tag, a Change-number, and a dated parenthetical). Convert ALL of them into plain prose that PRESERVES the behavior/design explanation and DROPS the reference — do not delete the surrounding sentence. Complete the four-slot banner: owns (the Backup & Restore modal, available on every app page); public surface (how it is opened + its `window.BackupManager` delegation); dependencies (`BackupManager`, the ZIP/encryption pipeline it triggers); invariants (it collects options then delegates to BackupManager — it does not do the ZIP/crypto itself). De-phase the whole body incl. trailing comments on code lines.

    Use the SAME strip-ALL-IDs rule as Task 1 (option 3, D-07: strip phase/plan, D-NN, T-/CR-/WR-, acceptance-test, date-prefixed bug tickets, ISO dates, `.planning/` cross-refs, AND requirement IDs REQ-/OBS-/VER-/RFCT-/DOCS-/DEMO-/PDFX-/I18N-/TEST-; the prose-shaped Gap/round-N/Change-N tags are a MANUAL eyeball; PRESERVE only the KEEP allowlist + rationale). Touch comment text only in both files — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! ( grep -nE '(//|/\*|^[[:space:]]*\*).*(\b(REQ|OBS|VER|RFCT|DOCS|DEMO|PDFX|I18N|TEST|CR|WR)-[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|(Phase|Plan)[[:space:]]+[0-9]|Quick[-[:space:]]+[0-9]|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|\bUAT\b|code-review|[0-9]{2}-(RESEARCH|CONTEXT|VALIDATION))' assets/snippets.js assets/backup-modal.js | grep -vE 'AES-256|SHA-256|UTF-8|base64|v[0-9]|\.js:[0-9]' | grep -q . ) && echo DEPHASE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The option-3 de-phase grep over BOTH files returns empty — ZERO ID-shaped tokens remain (incl. backup-modal's Phase/ISO-date/acceptance-test tags and requirement IDs); the KEEP allowlist is NOT flagged.
    - MANUAL (class-(b) prose tags): backup-modal's `round-N` and `Change N` tags — indistinguishable from prose, not grep-covered — are eyeballed and rewritten; no such tag survives in either file.
    - snippets.js title no longer carries a phase/plan tag; both files open with the four labelled slots in order — manual.
    - backup-modal's inline historical notes are converted to behavior prose, not deleted wholesale (manual: the explanations survive, only the reference tags are gone).
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>snippets.js and backup-modal.js carry clean four-slot banners with all build-history de-phased (backup-modal's heavier archaeology converted to prose); suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 4-file edit is comments-only</name>
  <files>assets/landing.js, assets/license.js, assets/snippets.js, assets/backup-modal.js</files>
  <read_first>
    - .planning/phases/36-code-comments-batch-2/36-COMMENT-STYLE-GUIDE.md (READ FIRST — the comment standard whose comments-only discipline this gate enforces)
    - .planning/phases/32-readme-code-comments/32-02-PLAN.md (Task 3 — the authoritative strip-and-compare gate this mirrors + the soundness argument)
    - .planning/phases/36-code-comments-batch-2/36-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the four files this plan touched changed comments only. Run the strip-and-compare with the comment edits from Tasks 1–2 still UNCOMMITTED, so the baseline is git HEAD (the pre-edit committed version): for each file, strip block comments, line comments, and trailing comments and collapse whitespace from BOTH the HEAD version (`git show HEAD:file`) and the working-tree version, then assert byte-equality. Any inequality means a code token changed → the gate FAILS; revert before committing. One-shot scratchpad Node script (not a committed test). If you committed content per-task, set `BASE` to the commit immediately before this plan's first edit and compare against that ref. Confirm the full suite is green. Confirm the scope fence: `git diff --name-only` lists none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01) — note assets/backup-modal.js is in scope and is NOT the giant assets/backup.js. Only commit once all gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';let bad=0;for(const f of ['assets/landing.js','assets/license.js','assets/snippets.js','assets/backup-modal.js']){const a=strip(cp.execSync('git show '+base+':'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all four files (D-06), run uncommitted against git HEAD (or against the pre-plan `BASE` ref).
    - If any file prints `CODE CHANGED`, a code line was modified — revert before committing.
    - Scope fence (D-01): `git diff --name-only` excludes assets/backup.js, assets/app.js, assets/pdf-export.js.
  </acceptance_criteria>
  <done>The 4-file comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to existing browser-tier modules. No new runtime code, network calls, or data flows. license.js's Lemon Squeezy API call and backup-modal's crypto delegation are unchanged — banners only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02 | Tampering | comment edits silently altering code in landing.js / license.js / snippets.js / backup-modal.js | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green `npm test` prove no behavior change (D-06); edits isolated in their own commit(s). |

Honest framing: comment-only edits add no attack surface. No new schema push applies (schema-gate: N/A for comments). The only risk is an accidental code edit, caught mechanically by the D-06 gate.
</threat_model>

<verification>
- `npm test` green after the edits.
- Option-3 de-phase grep over all 4 files returns empty across EVERY ID-shaped dialect incl. requirement IDs REQ-/OBS-/VER- (D-04, D-07); the KEEP allowlist (`AES-256`/`SHA-256`/`UTF-8`/`base64`/`v[0-9]`/`file.js:NNN`) is NOT flagged; class-(b) prose tags checked manually.
- landing.js opens with a four-slot banner; license.js keeps JSDoc + STORE_ID/PRODUCT_ID rationale; snippets.js title de-tagged; backup-modal's heavy archaeology converted to prose.
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-06).
- Scope fence (D-01): the three giants are untouched (backup-modal.js is in scope; backup.js is not).
</verification>

<success_criteria>
The 4 larger-mid modules carry Phase-32-convention four-slot banners with all build-history archaeology de-phased to plain prose; the edits are provably comments-only (strip-and-compare + green suite).
</success_criteria>

<output>
Create `.planning/phases/36-code-comments-batch-2/36-02-SUMMARY.md` when done.
</output>
