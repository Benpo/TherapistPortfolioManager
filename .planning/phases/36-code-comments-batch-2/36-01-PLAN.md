---
phase: 36-code-comments-batch-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/db.js
  - assets/overview.js
  - assets/sessions.js
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "db.js opens with a brand-new file-top banner (it is header-less today, starting at `window.PortfolioDB = (() => {`) following the Phase-32 convention (D-03): what it owns (the single IndexedDB choke-point — all CRUD, v1–v6 migrations, `_dbPromise` connection pool, demo-mode DB switching) · public surface (the `window.PortfolioDB` API it registers) · dependencies (the cross-`window.*` chain it reads) · key invariants (all IDB access funnels here; demo isolation via `window.name`)"
    - "overview.js's thin `// Module-level storage` opening + its bug-ticket archaeology (the missing-birth-year client-table filter note) are rewritten into a four-slot banner + plain what-it-does prose that preserves the WHY and drops the ticket number (D-03, D-04)"
    - "sessions.js gains a brand-new banner (it is header-less today, starting at `document.addEventListener`) documenting what the sessions page owns, its `window.*` deps, and its invariants (D-03, D-04)"
    - "All build-history archaeology is removed from the 3 batch-1 files — `Phase N` / `Plan N` / `D-NN` tags and date-prefixed bug-ticket refs are converted to plain prose; living-doc requirement IDs (REQ-NN / OBS-NN / VER-NN) are preserved (D-04)"
    - "The edits are provably comments-only: `npm test` stays green AND a deterministic comments-only strip-and-compare shows zero code lines changed across all 3 files (D-06)"
    - "batch-1 (db/overview/sessions) is sequenced first to establish the convention rhythm for the Wave-2 batches (D-02)"
  prohibitions:
    - "The three deferred giants are NOT modified in this phase — `git diff --name-only` for this plan must contain none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01 scope fence)"
    - "Zero code lines change in any file — this is a comment-only plan (D-06)"
  artifacts:
    - path: assets/db.js
      provides: "Brand-new IndexedDB-choke-point banner (new banner; header-less today) + de-phased body"
    - path: assets/overview.js
      provides: "Four-slot overview-page banner (refine the thin opener) + de-phased bug-ticket archaeology"
    - path: assets/sessions.js
      provides: "Brand-new sessions-page banner (new banner; header-less today) + de-phased body"
  key_links:
    - from: "the 3 batch-1 banners"
      to: "assets/export-modal.js lines 1-19 (the convention template) + .planning/codebase/ARCHITECTURE.md + CONVENTIONS.md"
      via: "each banner copies the four-slot shape: owns · public window.* surface · cross-window.* deps · invariants, sourced from the documented IIFE + window.* resolution chain"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-03 (batch-1 — the 3 lowest-staleness modules) — Extend the Phase 32 code-comment convention to `assets/db.js`, `assets/overview.js`, and `assets/sessions.js`. Two comment-only jobs per D-04: (a) header-less files (`db.js`, `sessions.js`) get a **brand-new** four-slot banner; `overview.js` (thin `// Module-level storage` opener + bug-ticket archaeology) is **de-phased + refined** to the banner shape; (b) all `Phase N` / `Plan N` / `D-NN` / date-prefixed bug-ticket archaeology is rewritten into plain what-it-does prose. Banner shape per D-03 (owns · public `window.*` surface · cross-`window.*` deps · invariants); template = `assets/export-modal.js:1-19`. Verify comment-only per D-06 (green suite + deterministic comments-only strip-and-compare). This batch runs first (D-02) to establish the rhythm for the Wave-2 batches.

Purpose: `db.js` is the IndexedDB choke-point (highest orientation value for an AI maintainer); all three were freshly touched in the Phase 31 refactor → lowest staleness, so their banners can be written against current code with least re-reading.
Output: 3 comment-only edited files; no new files; no new code symbols; no behavior change. The three giants (`backup.js`, `app.js`, `pdf-export.js`) are NOT touched (D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-comments-batch-2/36-CONTEXT.md
@.planning/phases/36-code-comments-batch-2/36-VALIDATION.md
@.planning/phases/32-readme-code-comments/32-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@assets/export-modal.js
</context>

## Artifacts this phase produces (this plan)

- **No new file paths.** All three targets already exist; this plan edits comments only (`db.js` and `sessions.js` gain a header banner, but no new code symbols).
- **No new code symbols** — no new functions, classes, flags, fields, `window.*` globals, or exports. The only artifacts are the banner comment blocks themselves. There is nothing here for a drift / source-grounding pass to flag.

<tasks>

<!-- The de-phase verify gate below greps for build-history literals that also appear in the <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: db.js — brand-new IndexedDB-choke-point banner + de-phase body</name>
  <files>assets/db.js</files>
  <read_first>
    - assets/db.js (starts at code `window.PortfolioDB = (() => {` — NO file-top comment today; read the whole file to capture the real public `PortfolioDB` method surface, the `_dbPromise` connection pool, the `migrations[1..6]` versioned schema object, `DB_NAME`/`OLD_DB_NAME`/`DB_VERSION` constants, the demo-mode `window.name` branch, and any inline `Phase N` / `D-NN` archaeology)
    - assets/export-modal.js (lines 1-19 — the four-slot banner content model to copy)
    - .planning/codebase/ARCHITECTURE.md (the `PortfolioDB` abstraction section + "Bypassing PortfolioDB for IDB access" anti-pattern + the "IndexedDB never touched by SW" constraint — the deps + invariants source)
    - .planning/codebase/CONVENTIONS.md (the IIFE-assigned-to-window pattern; `_prefixedPrivate` + `SCREAMING_SNAKE_CASE` naming)
  </read_first>
  <action>
    Add a BRAND-NEW top-of-file banner to assets/db.js above the `window.PortfolioDB = (() => {` line (the file has no header today). Build it in the four-slot convention shape and match the file's plain `//` comment style: (1) what it owns — the single IndexedDB choke-point: all client/session/therapistSettings/snippets/crashlog CRUD, the v1–v6 versioned migrations, the `_dbPromise` connection pool, and demo-mode DB switching; (2) public surface — the `window.PortfolioDB` API it registers (name the key methods a reader/agent enters through); (3) dependencies — the cross-`window.*` chain it reads (per ARCHITECTURE.md: it is the leaf the whole app funnels into; note the raw IndexedDB API, no wrapper library); (4) key invariants — every module reaches IDB only through `PortfolioDB` (never `indexedDB` directly), IDB access is serialized through the `_dbPromise` pool, demo isolation is via the `window.name === "demo-mode"` → `demo_portfolio` DB name branch, and the SW never touches IDB. The banner itself must contain no build-history tokens.

    Then do a full de-phase of the whole body: scan EVERY comment (block header, line comments, JSDoc `*` continuations, AND trailing comments on code lines). Convert each build-history *process* reference into plain what-it-does prose — keep the real explanation and design rationale, remove ONLY the historical tag. Dialects to strip: phase/plan tags (Phase-number Plan-number, Phase N-N); decision/task IDs (D-NN, T-N-N-N, RFCT-N); acceptance-test and code-review notes; gap/round/change tags; date-prefixed bug-ticket refs (six digits + hyphen + three alphanumerics); and any ISO date (YYYY-MM-DD) inside a comment. PRESERVE living-doc traceability: bare requirement IDs (REQ-NN, OBS-NN, VER-NN) and the security/`innerHTML` rationale survive — reword only to strip an attached phase/date parenthetical. Touch comment text only — zero code lines. Do NOT commit yet (Task 3's gate compares against the pre-edit HEAD).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/db.js && echo DEPHASE_CLEAN</automated>
    <automated>head -3 assets/db.js | grep -qE '^[[:space:]]*//' && echo HAS_BANNER</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (suite stays green).
    - `assets/db.js` now opens with a `//` banner (head grep passes) — it previously started with code.
    - The archaeology grep over `assets/db.js` returns empty — ZERO matches across all build-history dialects (phase/plan, D-/T-/RFCT-, acceptance-test/code-review, gap/round/change, WR-, date-prefixed bug tickets, ISO dates, #issue). The gate is verified NOT to match bare REQ-NN / OBS-NN / VER-NN.
    - The banner covers the four slots — what it owns (IDB choke-point) · public `window.PortfolioDB` surface · cross-`window.*` deps · invariants (all IDB via PortfolioDB; `_dbPromise` serialization; demo isolation; SW never touches IDB) — manual banner-quality read.
    - Comments-only: covered by Task 3's strip-and-compare gate (this file included).
  </acceptance_criteria>
  <done>db.js carries a brand-new four-slot IndexedDB-choke-point banner and a fully de-phased body; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: overview.js + sessions.js — refine overview banner (de-phase bug-ticket) + brand-new sessions banner</name>
  <files>assets/overview.js, assets/sessions.js</files>
  <read_first>
    - assets/overview.js (opens with a thin `// Module-level storage for search filtering` line then `let _allClients` / `let _sessionsByClient`; carries the missing-birth-year client-table filter bug-ticket archaeology flagged in 36-CONTEXT — read the whole file for the real render/search surface and the `innerHTML`+i18n hardening the Phase 31 refactor added)
    - assets/sessions.js (starts at code `document.addEventListener("DOMContentLoaded", …)` — NO header today; ~198 lines; read fully for the sessions-list page surface + `App.initCommon` boot + its `innerHTML`→DOM hardening)
    - assets/export-modal.js (lines 1-19 — the four-slot banner content model)
    - .planning/codebase/ARCHITECTURE.md (the Page-Modules layer: overview/sessions read `window.App` + `window.PortfolioDB`; the "Using innerHTML for i18n strings or user data" anti-pattern — the invariants source)
  </read_first>
  <action>
    overview.js — de-phase + REFINE. Replace the thin `// Module-level storage for search filtering` opener with a four-slot banner: what it owns (the dashboard/client-overview page — client + last-session table, search filtering, Heart-Shield badge render); public surface (page module — no `window.*` export; boots on `DOMContentLoaded`); dependencies (`window.App` for i18n/nav/section-labels, `window.PortfolioDB` for client/session reads); invariants (user-provided/i18n text rendered via `textContent`/DOM nodes, never `innerHTML`). Then de-phase the body — in particular the missing-birth-year client-table filter archaeology: rewrite the bug-ticket reference into plain prose that KEEPS the reason (why the filter exists / what constraint it encodes) and DROPS the ticket number.

    sessions.js — BRAND-NEW banner (header-less today). Add a four-slot banner above `document.addEventListener`: what it owns (the sessions-list page — client filter + session rows); public surface (page module, no `window.*` export); dependencies (`window.App`, `window.PortfolioDB`); invariants (same `textContent`/DOM hardening rule). Then de-phase any body archaeology.

    Use the SAME dialect list and SAME PRESERVE rules as Task 1 (strip phase/plan, D-NN, T-/RFCT-, acceptance-test/code-review, gap/round/change, date-prefixed bug tickets, ISO dates; PRESERVE REQ-NN/OBS-NN/VER-NN + security rationale). Watch trailing comments on code lines — rewrite comment text only, never code. Touch comment text only in both files — zero code lines. Do NOT commit yet (Task 3's gate).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|[0-9]{6}-[a-z0-9]{3}|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/overview.js assets/sessions.js && echo DEPHASE_CLEAN</automated>
    <automated>head -3 assets/sessions.js | grep -qE '^[[:space:]]*//' && echo SESSIONS_HAS_BANNER</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `assets/sessions.js` now opens with a `//` banner (head grep passes) — it previously started with code.
    - The archaeology grep over BOTH files returns empty — ZERO matches across all build-history dialects; the missing-birth-year filter note survives as plain prose (the constraint explained, the ticket number gone).
    - overview.js opens with a four-slot banner (owns · public surface · window.* deps · invariants); the thin `// Module-level storage` opener is gone (manual).
    - Both banners state the `textContent`/DOM invariant (no `innerHTML` for i18n/user text) — manual.
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>overview.js carries a refined four-slot banner with the bug-ticket archaeology de-phased to prose; sessions.js has a brand-new four-slot banner; both bodies fully de-phased; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 3-file batch-1 edit is comments-only</name>
  <files>assets/db.js, assets/overview.js, assets/sessions.js</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-02-PLAN.md (Task 3 — the authoritative strip-and-compare gate this mirrors; the soundness argument: deterministic strip-and-compare is sound even with an imperfect stripper because unchanged code lines are processed identically on both sides)
    - .planning/phases/36-code-comments-batch-2/36-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the three files this plan touched changed comments only. Run the strip-and-compare with the comment edits from Tasks 1–2 still UNCOMMITTED, so the baseline is simply git HEAD (the pre-edit committed version): for each file, strip block comments, line comments, and trailing comments and collapse whitespace from BOTH the HEAD version (`git show HEAD:file`) and the working-tree version, then assert byte-equality. Any inequality means a code token changed → the gate FAILS; revert that change before committing. The check is a one-shot scratchpad Node script (not a committed test — reusable gate tooling stays deferred per 36-CONTEXT). If you already committed the content edits per-task, set `BASE` to the commit immediately before this plan's first edit and the script compares against that ref instead of HEAD. Confirm the full suite is green as the behavior backstop. Also confirm the scope fence: `git diff --name-only` for this plan lists none of assets/backup.js, assets/app.js, assets/pdf-export.js (D-01). Only commit the comment edits once all gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');const base=process.env.BASE||'HEAD';let bad=0;for(const f of ['assets/db.js','assets/overview.js','assets/sessions.js']){const a=strip(cp.execSync('git show '+base+':'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all three files (D-06), run with the edits uncommitted against git HEAD (or against the pre-plan `BASE` ref if interim-committed).
    - If any file prints `CODE CHANGED`, a code line was modified — revert that change before committing.
    - Scope fence (D-01): `git diff --name-only` for this plan excludes assets/backup.js, assets/app.js, assets/pdf-export.js.
  </acceptance_criteria>
  <done>The 3-file batch-1 comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to existing browser-tier IIFE modules. No new runtime code, network calls, or data flows. `db.js` is the IDB choke-point but its behavior is unchanged — banners only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | comment edits silently altering code in db.js / overview.js / sessions.js | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green `npm test` prove no behavior change (D-06); edits isolated in their own commit(s). |

Honest framing: comment-only edits add no attack surface. No new schema push applies (schema-gate: N/A for comments). ASVS controls are untouched; the only risk is an accidental code edit, which the D-06 gate catches mechanically.
</threat_model>

<verification>
- `npm test` green after the edits.
- Archaeology grep over all 3 files returns empty across EVERY build-history dialect (phase/plan, D-/T-/RFCT-, acceptance-test/code-review, gap/round/change, WR-, date-prefixed bug tickets, ISO dates, #issue) — full de-phasing complete (D-04); the gate does not flag bare REQ-NN/OBS-NN/VER-NN.
- `db.js` and `sessions.js` now open with a `//` banner (were header-less); `overview.js`'s thin opener is replaced by a four-slot banner.
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-06).
- Scope fence (D-01): the three giants are untouched.
</verification>

<success_criteria>
The 3 batch-1 modules carry Phase-32-convention four-slot banners (db.js + sessions.js brand-new, overview.js refined + de-phased); all build-history archaeology is de-phased to plain prose; the edits are provably comments-only (strip-and-compare + green suite); the convention rhythm is established for the Wave-2 batches.
</success_criteria>

<output>
Create `.planning/phases/36-code-comments-batch-2/36-01-SUMMARY.md` when done.
</output>