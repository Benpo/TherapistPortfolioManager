---
phase: 42-in-app-changelog-what-s-new
plan: 10
type: execute
wave: 2
depends_on: [04, 05, 07, 08]
files_modified:
  - assets/app.js
  - assets/shared-chrome.js
  - help.html
autonomous: true
requirements: [CHLG-02]
must_haves:
  truths:
    - "The '?' popover has a 'What's new' row that opens the changelog PAGE (never re-triggers the popup); hidden in demo mode"
    - "The help center links to the changelog page via a quiet 'What's new' rail/see-also link"
    - "The footer version text is a quiet link to the changelog page; the integrity marker stays an independent sibling OUTSIDE the link; the link is inert in demo mode"
  artifacts:
    - "assets/app.js (whatsNew.menuRow row in initHelpEntry + demo filter)"
    - "assets/shared-chrome.js (footer version-text link, demo-inert, warn-span independent)"
    - "help.html (What's new rail link + changelog-content-en.js/whats-new.js script tags)"
  key_links:
    - "The menu row + footer link + rail link all navigate to changelog.html (the page), not the popup (D-14) — announcements stay one-time"
    - "Demo hiding: '?' row filtered (app.js demo filter) + footer link inert (shared-chrome isDemo) — the page is unreachable from the demo iframe (D-15)"
    - "Footer link wraps ONLY the version text; .app-footer-version-warn stays outside the anchor (independence guard — no double-signalling with the integrity nudge)"
---

<objective>
Add the three changelog entry points (CHLG-02 discoverability): the "?" popover "What's new" row (D-14), a quiet help-center rail link (D-13), and the discretionary footer version link (resolved YES). Enforce demo-mode hiding for the row + footer link (D-15). Also add the popup-surface script tags to help.html (it is a chrome-mounting page). Turns the demo-gate test (T-42-V10) green.

Purpose: The changelog is reachable from the help family without competing with help topics; the popup is never re-triggerable from a menu (announcements are one-time). The footer link is discoverable-but-silent and must not entangle with the independent integrity marker.
Output: menu row + demo filter (app.js), footer link (shared-chrome.js), rail link + scripts (help.html).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@.planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md
@assets/app.js
@assets/shared-chrome.js
@help.html
</context>

## Artifacts this phase produces (shared inventory)

Modified (THIS plan): `assets/app.js` (`whatsNew.menuRow` row + demo filter), `assets/shared-chrome.js` (footer version-text link, demo-inert), `help.html` (rail link + 2 script tags). Full phase inventory: `assets/changelog-content-en.js`, `assets/whats-new.js`, `assets/changelog.js`, `changelog.html`, `assets/changelog.css`, `assets/attention-coordinator.js` (tour guard), `assets/app.css`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`, 7 tests. New i18n key used here: `whatsNew.menuRow` (plan 08).

<tasks>

<task type="auto">
  <name>Task 1: Add the "?" popover "What's new" row + demo filter in assets/app.js initHelpEntry</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js (lines 471-472 comment naming "What's new" as the third append; 518-529 addable items array; 533-535 demo filter dropping help.entry.takeTour; 504 is-active nav)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Entry points -> "?" popover row contract)
    - tests/42-demo-gate.test.js (the RED contract - plan 03)
  </read_first>
  <action>
    In initHelpEntry's addable items array (app.js:518-529), add a row `{ labelKey: 'whatsNew.menuRow', href: './changelog.html' }` - a DESTINATION link that opens the page, NOT a popup re-trigger (D-14). Place it per D-14 (exact slot discretion; e.g. after the tour row). The label renders via textContent from the i18n dict (never innerHTML). Extend the existing demo filter (app.js:533-535) - which drops help.entry.takeTour in demo mode - to ALSO drop whatsNew.menuRow (D-15). Do not add is-active wiring (the "?" need not be active on the changelog page).
  </action>
  <verify>
    <automated>node tests/42-demo-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/42-demo-gate.test.js passes the menu-row assertions: demo -> no whatsNew.menuRow row; normal -> row present with href './changelog.html'.
    - The demo filter drops BOTH help.entry.takeTour and whatsNew.menuRow.
    - node -c assets/app.js passes.
  </acceptance_criteria>
  <done>The "?" menu has a "What's new" row opening the page, hidden in demo; demo-gate menu assertions green.</done>
</task>

<task type="auto">
  <name>Task 2: Make the footer version text a quiet changelog link in assets/shared-chrome.js (demo-inert, warn-span independent)</name>
  <files>assets/shared-chrome.js</files>
  <read_first>
    - assets/shared-chrome.js (lines 108 isDemo computed; 109-111 licenseLinkHtml demo-strip pattern; 135-136 footer copy line with v{APP_VERSION} + .app-footer-version-warn span; 149-183 maybeUpgradeFooterAndNudge - the INDEPENDENT nudge, do not touch)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Entry points -> Footer version link contract + Independence guard)
    - tests/42-demo-gate.test.js (footer-link assertions - plan 03)
  </read_first>
  <action>
    In the footer copy render (shared-chrome.js:135-136), wrap ONLY the v{APP_VERSION} TEXT in a quiet anchor to ./changelog.html when NOT in demo. The sibling .app-footer-version-warn span MUST stay OUTSIDE the anchor (independence guard - never touch maybeUpgradeFooterAndNudge/buildNudge at 149-183; no double-signalling with the integrity marker). In demo (isDemo already computed at :108, same seam as licenseLinkHtml :109-111), render the version as INERT plain text (no anchor). Style the link with --color-text-muted, underline-on-hover only (never accent-colored) - add the rule to app.css if a class is needed. Reads version only from APP_VERSION (no integrity-token coupling).
  </action>
  <verify>
    <automated>node tests/42-demo-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/42-demo-gate.test.js passes the footer assertions: normal -> version text wrapped in anchor to ./changelog.html with .app-footer-version-warn OUTSIDE the anchor; demo -> inert version text (no anchor).
    - maybeUpgradeFooterAndNudge/buildNudge (149-183) are unchanged.
    - node -c assets/shared-chrome.js passes.
  </acceptance_criteria>
  <done>Footer version text links to the changelog (quiet, muted), inert in demo, with the integrity marker fully independent; demo-gate footer assertions green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the help-center "What's new" rail link + popup-surface scripts to help.html</name>
  <files>help.html</files>
  <read_first>
    - help.html (the static rail/see-also chrome, e.g. the contact-band ~99-104, for placement; lines 114-136 script chain for the insertion anchor after attention-coordinator.js)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Entry points -> Help-center link: quiet rail/see-also link, not a spine card)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (Pitfall 2 - help.html is a chrome-mounting page and needs the surface + data scripts too)
  </read_first>
  <action>
    Two edits to help.html:
    (a) Add a quiet "What's new" rail / see-also link (label via data-i18n="whatsNew.menuRow") to ./changelog.html, placed as a secondary see-also affordance (NOT a workflow-spine card competing with help topics) per D-13 / UI-SPEC.
    (b) Add the two script tags - changelog-content-en.js then whats-new.js - immediately AFTER the existing attention-coordinator.js tag (help.html is a chrome-mounting page, so the popup can fire here too; same wiring as the 7 pages in plan 09, handled here for help.html to avoid a same-wave file conflict). Order: data global before surface, both after the coordinator.
  </action>
  <verify>
    <automated>grep -q 'changelog-content-en.js' help.html &amp;&amp; grep -q 'whats-new.js' help.html &amp;&amp; grep -q 'changelog.html' help.html &amp;&amp; node -c help.html 2>/dev/null; grep -q 'changelog-content-en.js' help.html &amp;&amp; grep -q 'whats-new.js' help.html &amp;&amp; grep -q 'changelog.html' help.html &amp;&amp; echo "help.html rail link + scripts present OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms help.html contains changelog-content-en.js, whats-new.js, and a changelog.html link.
    - Source read confirms changelog-content-en.js precedes whats-new.js and both follow attention-coordinator.js (correct load order).
    - The link is a quiet see-also/rail affordance, not a primary spine card.
  </acceptance_criteria>
  <done>help.html links to the changelog page (quiet see-also) and loads the popup surface + data in the correct order (chrome-mounting page).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo iframe -> app chrome | demo mode must not expose the changelog entry points (menu row, footer link) |
| footer integrity marker vs changelog link | the two are independent signals; the version link must not entangle the integrity nudge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-03 | Information Disclosure | demo-mode entry points | low | mitigate | D-15: "?" row filtered in demo (app.js), footer link inert in demo (shared-chrome isDemo); page unreachable from demo iframe. Guarded by T-42-V10. |
| T-42-09 | Tampering (UX integrity) | footer version link vs integrity nudge | low | mitigate | Link wraps ONLY the version text; .app-footer-version-warn stays a sibling OUTSIDE the anchor; maybeUpgradeFooterAndNudge untouched - no double-signalling. |
| T-42-01 | Tampering | menu-row label render | low | mitigate | Row label via textContent from the i18n dict (never innerHTML). |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
node tests/42-demo-gate.test.js green (menu row + footer link demo behavior); help.html carries the rail link + surface/data scripts in order; integrity-nudge region unchanged.
</verification>

<success_criteria>
- CHLG-02 entry points implemented: "?" row (page link, demo-hidden), help-center rail link, footer version link (demo-inert, integrity-independent); demo-gate test green.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-10-SUMMARY.md` when done.
</output>
