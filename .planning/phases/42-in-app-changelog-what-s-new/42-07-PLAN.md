---
phase: 42-in-app-changelog-what-s-new
plan: 07
type: execute
wave: 1
depends_on: [02]
files_modified:
  - changelog.html
  - assets/changelog.js
  - assets/changelog.css
autonomous: true
requirements: [CHLG-02]
must_haves:
  truths:
    - "A persistent standalone changelog page renders reverse-chronological, version+date-grouped, benefit-led entries (New/Improved/Fixed) from the single data source"
    - "Empty categories are omitted; the v1.0 entry renders as a one-line origin marker; each version card has a stable kebab anchor for deep-links"
    - "An entry (or part) not yet translated renders in English (EN-fallback) so history is complete in every locale"
    - "The page mirrors the help.html per-page shell (shared chrome/footer/nav, tokens→app→page CSS) and is offline-ready"
  artifacts:
    - changelog.html
    - assets/changelog.js
    - assets/changelog.css
  key_links:
    - "changelog.js reads window.CHANGELOG_CONTENT_EN (plan 04) and renders via createElement+textContent; exposes window.Changelog test seam"
    - "changelog.html script chain loads changelog-content-en.js + whats-new.js (after attention-coordinator.js) then changelog.js — so the popup can also fire on this page"
    - "'See everything new' deep-links to changelog.html#{anchor}; page reads chrome strings from changelog.* i18n keys (plan 08)"
---

<objective>
Ship the standalone changelog page (CHLG-02): `changelog.html` (help-family shell), `assets/changelog.js` (reverse-chronological Variant-B renderer with EN-fallback), and `assets/changelog.css` (page + entry-card styles ported from sketch 005 Variant B). Turns the render test (T-42-V7/V8) green.

Purpose: The page carries the full release history; the popup is only an announcement. It's a standalone page in the help family (D-13), not a section inside help.html.
Output: `changelog.html`, `assets/changelog.js`, `assets/changelog.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@.planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md
@help.html
@assets/help.js
@assets/help.css
@.planning/sketches/005-changelog-register/index.html
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js` (→ `window.CHANGELOG_CONTENT_EN`, plan 04), `assets/whats-new.js` (plan 05/06), `assets/changelog.js` (THIS plan → `window.Changelog`), `changelog.html` (THIS plan, route `/changelog`), `assets/changelog.css` (THIS plan), 7 `tests/42-*.test.js`. Modified: `assets/app.css`, `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`. New i18n chrome keys the page reads: `changelog.page.title`, `changelog.page.intro`, `changelog.cat.new`, `changelog.cat.improved`, `changelog.cat.fixed` (added in plan 08).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assets/changelog.js (reverse-chron Variant-B renderer + EN-fallback)</name>
  <files>assets/changelog.js</files>
  <read_first>
    - assets/help.js (lines 22-24 XSS trust-boundary banner to copy verbatim; 349-355 render-from-global; 358-364 t() chrome; 455-463 app:language re-render; 466-478 boot; the window.Help test seam)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (## Architecture Patterns Pattern 2 + localeEntries() EN-fallback shape; ## Entry Schema)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Changelog page: render contract, entry schema, EN-fallback, anchors, focal point)
    - tests/42-changelog-render.test.js (the RED contract — plan 02)
  </read_first>
  <behavior>
    - Reads window.CHANGELOG_CONTENT_EN and renders entries reverse-chronologically into the page container(s).
    - Per entry: version+date heading, one-sentence lede, then New/Improved/Fixed blocks — each block rendered ONLY if its category array is present and non-empty (empty categories omitted, D-11).
    - v1.0 (`origin:true`) renders a single one-line marker with NO category blocks (D-01).
    - Each version card gets a stable kebab `id` equal to its `anchor` (e.g. `v1-3`) for deep-links.
    - EN-fallback (D-16): if the active-locale array is absent → all EN; if present but missing an entry version → that entry falls back to EN.
    - Re-renders on `app:language`; exposes window.Changelog seam.
  </behavior>
  <action>
    Mirror `assets/help.js`. Copy the XSS trust-boundary banner (help.js:22-24) verbatim: ALL dynamic text via `createElement`+`textContent`; only compile-time-literal SVG may use innerHTML. Author an IIFE (`"use strict"`, Phase-31 four-slot banner). Implement `localeEntries()` per RESEARCH Pattern 2: read `window['CHANGELOG_CONTENT_'+lang.toUpperCase()]` merged over `window.CHANGELOG_CONTENT_EN` per-entry (fallback to EN when a locale/entry is missing) — use the app's existing portfolio-language accessor (the same one help.js/i18n uses) for `lang`. Implement `render()`: iterate `localeEntries()` (already newest-first), build a per-version card with `id=entry.anchor`, an `<h2>` from `entry.version`+`entry.date`, the `entry.lede`, then for each of new/improved/fixed present+non-empty build a small uppercase colored category heading (label text from i18n keys `changelog.cat.new`/`.improved`/`.fixed` via `t()`) + a bullet list; skip absent/empty categories; for `origin:true` render only the one-line lede. Set the page title/intro from `t('changelog.page.title')`/`t('changelog.page.intro')` (help.js:358-364 idiom). Wire `App.initCommon()` + DOMContentLoaded boot and the `app:language` re-render listener (help.js:455-478). Expose `window.Changelog = { render, ... }`.
  </action>
  <verify>
    <automated>node tests/42-changelog-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-changelog-render.test.js` PASSES (was RED in plan 02): reverse-chron cards, empty-category omission, v1.0 one-line, kebab anchors, EN-fallback.
    - `grep -nE 'innerHTML' assets/changelog.js` shows innerHTML only for compile-time-literal SVG (if any) — never for entry/lede/highlight/bullet text.
    - Exposes `window.Changelog`; re-renders on `app:language`.
    - `node -c assets/changelog.js` passes.
  </acceptance_criteria>
  <done>changelog.js renders the full history (Variant B) from the single source, XSS-safe, with EN-fallback and stable anchors; render test green.</done>
</task>

<task type="auto">
  <name>Task 2: Create changelog.html (help-family shell + script chain)</name>
  <files>changelog.html</files>
  <read_first>
    - help.html (full file: lines 4-15 head guard scripts to copy UNCHANGED; 22-28 CSS chain; 28 body data-nav; 73/93 empty content containers; 114-136 script chain incl the content-corpus comment; 137-139 SW registration footer)
    - .planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md (changelog.html section — the copy-and-adapt checklist)
    - assets/changelog.js (the container id(s) it fills — keep consistent)
  </read_first>
  <action>
    Copy `help.html` and adapt. Keep the 4 head guard scripts (help.html:4-15) UNCHANGED. CSS chain: `tokens.css` → `app.css` → `changelog.css` (do NOT drop app.css — the popup + `.btn-primary` live there). Use `data-nav="changelog"` (or omit — the "?" need not be active here; initHelpEntry only marks is-active for nav==='help', app.js:504). Provide the empty content container(s) `changelog.js` fills (mirror help.html's empty `#helpCards` idiom). Script chain mirrors help.html:114-136 (i18n → db → version → shared-chrome → attention-coordinator → app.js core), and — mirroring the help corpus comment — load `changelog-content-en.js` then `whats-new.js` AFTER `attention-coordinator.js`, then `changelog.js` AFTER `changelog-content-en.js`. This page is a chrome-mounting page, so the popup can also fire here — that is why it loads the popup surface + data. Keep the SW-registration footer script (help.html:137-139) verbatim. Page `<h1>` and intro carry `data-i18n` for `changelog.page.title`/`changelog.page.intro`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('changelog.html','utf8'); const need=['assets/tokens.css','assets/app.css','assets/changelog.css','assets/attention-coordinator.js','assets/changelog-content-en.js','assets/whats-new.js','assets/changelog.js']; const miss=need.filter(x=>!s.includes(x)); if(miss.length){console.error('MISSING',miss);process.exit(1)} const ac=s.indexOf('attention-coordinator.js'),cc=s.indexOf('changelog-content-en.js'),cj=s.indexOf('changelog.js'); if(!(ac<cc&&cc<cj)){console.error('ORDER wrong');process.exit(1)} console.log('changelog.html shell + order OK')"</automated>
  </verify>
  <acceptance_criteria>
    - changelog.html loads tokens.css, app.css, changelog.css and the script chain includes attention-coordinator.js, then changelog-content-en.js + whats-new.js, then changelog.js — in that order (verify command passes).
    - Head guard scripts (crashlog/terms/license/dark-theme) copied unchanged from help.html.
    - Empty content container(s) present for changelog.js to fill; SW-registration footer script present.
  </acceptance_criteria>
  <done>changelog.html is a valid help-family shell that loads the page renderer, the data source, and the popup surface in the correct order; offline-registration script intact.</done>
</task>

<task type="auto">
  <name>Task 3: Create assets/changelog.css (Variant B entry/category styles, token-based)</name>
  <files>assets/changelog.css</files>
  <read_first>
    - assets/help.css (the page-scoping idiom + how a page stylesheet is structured)
    - .planning/sketches/005-changelog-register/index.html (Variant B `.entry`/`.cat-sec`/`.entry-lede` rules — port token-based rules ONLY)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Color category tokens, Spacing, Typography; Accepted Exceptions for radii)
    - assets/tokens.css (semantic color tokens: green-600/500/400/200, orange-500, surface, text, borders)
  </read_first>
  <action>
    Author `assets/changelog.css` porting sketch 005 Variant B: entry cards (`--color-surface`, radius `14px`, padding `1.5rem 1.75rem` — approved sketch values), page head + intro, one-sentence lede, and the New/Improved/Fixed category headings (small, `text-transform:uppercase`, `letter-spacing:0.08em`) using the UI-SPEC category tokens — New `--color-green-600`(light)/`--color-green-400`(dark), Improved `--color-green-500`/`--color-green-200`, Fixed `--color-orange-500` (both themes) — each with a `[data-theme="dark"]` override. Use CSS LOGICAL PROPERTIES only (RTL-safe). Port ONLY token-based rules from the sketch — do NOT port any literal-hex color (the sketch's Variant-A chip hex and any literal button color are excluded; category colors come from the tokens above). NO literal hex anywhere in this file.
  </action>
  <verify>
    <automated>grep -nE '#[0-9a-fA-F]{3,8}\b' assets/changelog.css &amp;&amp; echo "HEX-FOUND-FAIL" || echo "no literal hex in changelog.css OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}' assets/changelog.css` finds NO literal hex (all colors via semantic tokens).
    - Category headings use the UI-SPEC tokens (green-600/500 + orange-500 light; green-400/200 + orange-500 dark) with `[data-theme="dark"]` overrides.
    - Uses logical properties only (no physical `left`/`right`/`margin-left`/`margin-right`).
    - `node tests/42-changelog-render.test.js` still passes.
  </acceptance_criteria>
  <done>changelog.css styles the Variant-B page/entry cards/category headings token-only, RTL/dark-safe, zero literal hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array → page DOM | changelog entry strings render into the page |
| demo iframe → app chrome | the page must be unreachable from demo (entry points gated in plan 10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | changelog.js render path | medium | mitigate | All entry text via createElement+textContent (help.js:22-24 banner copied verbatim); innerHTML only for compile-time-literal SVG. Guarded by T-42-V7 + the innerHTML grep. |
| T-42-03 | Information Disclosure | /changelog reachability in demo | low | mitigate | The page itself carries no demo guard; reachability is gated at the entry points (plan 10: "?" row filtered, footer link inert) + coordinator disabled in demo. No secret data on the page regardless. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-changelog-render.test.js` green; changelog.html shell + script order verified; no literal hex in changelog.css; innerHTML grep clean in changelog.js. Real offline page load is field-verified in the phase-gate UAT.
</verification>

<success_criteria>
- CHLG-02 page implemented and render-test-green: reverse-chron Variant-B history, empty-category omission, v1.0 one-line, EN-fallback, stable anchors, help-family shell, token-only RTL/dark styles.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-07-SUMMARY.md` when done.
</output>
