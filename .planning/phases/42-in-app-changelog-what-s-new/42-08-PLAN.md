---
phase: 42-in-app-changelog-what-s-new
plan: 08
type: execute
wave: 1
depends_on: [03]
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [CHLG-02]
must_haves:
  truths:
    - "Every UI-chrome string for the changelog page and What's-New popup exists as a data-i18n key in all four locales (en/he/de/cs), non-empty, no emoji"
    - "The EN canonical values match the UI-SPEC copywriting contract; HE/DE/CS follow each locale's existing register conventions"
  artifacts:
    - "assets/i18n-en.js (+ he/de/cs) — 10 new changelog.*/whatsNew.* keys"
  key_links:
    - "changelog.js reads changelog.page.title/intro + changelog.cat.* ; whats-new.js reads whatsNew.title/sub/seeAll/close ; app.js reads whatsNew.menuRow"
    - "T-42-V11 (plan 03) enforces presence + non-empty + parity + no-emoji across all four locales"
---

<objective>
Add the 10 UI-chrome i18n keys for the changelog page and What's-New popup to all four locale dictionaries (D-17 standing rule: chrome strings ship in all 4 locales THIS phase; entry BODY translation is Phase 42.1). Turns the i18n-parity test (T-42-V11) green.

Purpose: The page title/intro/category labels and popup headline/subline/buttons/menu row are all `data-i18n`-driven; they must exist in every locale so the page and popup are fully localized chrome even before entry-body translation lands.
Output: 10 new keys × 4 locale files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@.planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md
@assets/i18n-en.js
@assets/i18n-he.js
</context>

## Artifacts this phase produces (shared inventory)

New i18n keys (THIS plan, all 4 locales): `changelog.page.title`, `changelog.page.intro`, `changelog.cat.new`, `changelog.cat.improved`, `changelog.cat.fixed`, `whatsNew.title`, `whatsNew.sub`, `whatsNew.seeAll`, `whatsNew.close`, `whatsNew.menuRow`. Other phase artifacts: `assets/changelog-content-en.js`, `assets/whats-new.js`, `assets/changelog.js`, `changelog.html`, `assets/changelog.css`, `assets/attention-coordinator.js` (guard), `assets/app.css`, `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, 7 `tests/42-*.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Add 10 changelog.*/whatsNew.* chrome keys to all four locale dictionaries</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (lines 596-598, 650 — the help.entry.* block; add the new keys in the same family, position flexible)
    - assets/i18n-he.js (the parallel help.entry.* block + the file's HE register conventions — infinitive/house style)
    - assets/i18n-de.js and assets/i18n-cs.js (their help.entry.* blocks + register: DE Sie, CS formal)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Copywriting Contract table — EN canonical strings)
    - tests/42-i18n-parity.test.js (the RED contract — plan 03)
  </read_first>
  <action>
    Add these 10 keys to EACH of the four locale objects, near the existing `help.entry.*` keys. EN canonical values from the UI-SPEC Copywriting Contract: `changelog.page.title`="What's new in Sessions Garden"; `changelog.page.intro`="Every release, in plain language — newest first. Everything always stays on your device."; `changelog.cat.new`="New"; `changelog.cat.improved`="Improved"; `changelog.cat.fixed`="Fixed"; `whatsNew.title`="What's new in version {X.Y}" (keep the `{X.Y}` placeholder token the popup interpolates — do NOT hardcode a version); `whatsNew.sub`="A quick look at what's waiting for you."; `whatsNew.seeAll`="See everything new"; `whatsNew.close`="Close"; `whatsNew.menuRow`="What's new".
    For HE/DE/CS, translate each grounded in that file's existing register conventions (HE infinitive/plural house style, DE Sie, CS formal), preserving the `{X.Y}` placeholder verbatim in the `whatsNew.title` value. Category labels are NOUNS (New/Improved/Fixed), not imperatives, so the translations read as natural noun forms. No emojis (D-10). Keep JSON/object syntax valid in every file.
  </action>
  <verify>
    <automated>node tests/42-i18n-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-i18n-parity.test.js` PASSES (was RED in plan 03): all 10 keys present + non-empty + parity + no-emoji across en/he/de/cs.
    - `whatsNew.title` retains the `{X.Y}` interpolation placeholder in ALL four locales (no hardcoded version number).
    - EN values match the UI-SPEC Copywriting Contract verbatim.
    - `node -c assets/i18n-en.js && node -c assets/i18n-he.js && node -c assets/i18n-de.js && node -c assets/i18n-cs.js` all pass.
  </acceptance_criteria>
  <done>All 10 chrome keys exist in 4 locales, parity-test green, {X.Y} placeholder preserved, register-appropriate translations, no emoji.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dictionary → rendered chrome | localized label strings render into page/popup DOM (via data-i18n / t(), textContent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Tampering | i18n chrome strings | low | mitigate | Parity test enforces no-emoji + non-empty; all labels render via textContent/data-i18n (no innerHTML), so a label string cannot inject markup. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-i18n-parity.test.js` green; `{X.Y}` placeholder preserved in all locales; all four dicts valid JS.
</verification>

<success_criteria>
- All changelog.*/whatsNew.* chrome keys ship in 4 locales (D-17), parity-test green, no emoji, version placeholder intact.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-08-SUMMARY.md` when done.
</output>
