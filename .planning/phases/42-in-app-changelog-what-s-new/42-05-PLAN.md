---
phase: 42-in-app-changelog-what-s-new
plan: 05
type: execute
wave: 1
depends_on: [01]
files_modified:
  - assets/whats-new.js
  - assets/app.css
autonomous: true
requirements: [CHLG-01]
must_haves:
  truths:
    - "After a version change, opening the app shows the What's-New popup exactly once; dismissing records the version so it never reappears until the next change"
    - "First-ever launch suppresses the popup (welcome owns first-run via its subsume-write)"
    - "A version with no entry silently skips the popup AND advances the last-seen version quietly (D-07)"
    - "Only Close, Esc, and 'See everything new' dismiss — backdrop clicks do NOTHING (D-09) — and each records the version"
    - "The popup is a modest centered modal (not full-screen) rendering the latest entry's 2-4 highlights, with dialog a11y"
  artifacts:
    - assets/whats-new.js
    - "assets/app.css (.whats-new-* popup CSS block — the chosen always-loaded popup CSS home)"
  key_links:
    - "whats-new.js self-registers via AttentionCoordinator.register({id:'whats-new',eligible,show}) at eval time, into pre-reserved PRECEDENCE[1], before bootAttentionSurfaces() calls run()"
    - "eligible() reads window.CHANGELOG_CONTENT_EN (plan 04) + window.AppVersion.APP_VERSION; dismiss() writes sg.whatsNewLastSeenVersion on all 3 paths"
    - "Popup CTA 'See everything new' navigates to changelog.html#{latest anchor} (e.g. #v1-3)"
---

<objective>
Ship `assets/whats-new.js` — the self-registering coordinator surface implementing CHLG-01 — plus its popup CSS block in the always-loaded `assets/app.css`. Turns the gating/dismiss tests (T-42-V1..V5) green.

Purpose: This is the once-per-version announcement. Phase 40 pre-reserved the `'whats-new'` slot and pre-wrote the storage key, so integration is one `register()` call plus the surface DOM/behavior. The hardest parts (arbitration, first-run suppression, dismiss a11y) are inherited from the welcome overlay idiom.
Output: `assets/whats-new.js` + `.whats-new-*` rules in `app.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@.planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md
@assets/attention-coordinator.js
@assets/version.js
@.planning/sketches/005-changelog-register/index.html
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js` (→ `window.CHANGELOG_CONTENT_EN`, plan 04), `assets/whats-new.js` (THIS plan; surface `'whats-new'`), `assets/changelog.js`, `changelog.html`, `assets/changelog.css`, 7 `tests/42-*.test.js`. Modified: `assets/app.css` (THIS plan adds `.whats-new-*` popup CSS), `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`. Storage: reuses existing `sg.whatsNewLastSeenVersion` (attention-coordinator.js:51) — no new keys.

## Popup CSS home DECISION (resolves RESEARCH Open Q1)

Popup styles live in `assets/app.css` (NOT `assets/changelog.css`). Rationale: `app.css` is loaded on every chrome-mounting page and is already in `PRECACHE_URLS` (sw.js:39) — the popup can fire on any launch page and must be styled offline everywhere; `changelog.css` only loads on the page. This matches the existing coordinator-surface precedent (the `.welcome-overlay` block already lives in `app.css:4741-4900`). Consequence: NO new per-page `<link>`, NO new precache entry for popup CSS, and T-42-V6 (plan 03) correctly does NOT expect a `whats-new.css`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assets/whats-new.js (coordinator surface: gating, dismiss, reconcile, popup DOM)</name>
  <files>assets/whats-new.js</files>
  <read_first>
    - assets/attention-coordinator.js (lines 47 PRECEDENCE incl 'whats-new', 51 WHATS_NEW_LAST_SEEN='sg.whatsNewLastSeenVersion', 59-62 lsGet/lsSet try/catch, 78-105 register/run, 126-254 welcome overlay mount/dismiss idiom, 201-218 keydown focus-trap + Escape, 220-232 dismiss writes keys, 247-253 mount a11y, 256 welcome register, 502 _getSurface)
    - assets/version.js (line 27 APP_VERSION, 332-351 export)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Interaction Contract → What's-New popup: content, focal point, dismiss D-09, a11y, deep-link)
    - .planning/sketches/005-changelog-register/index.html (popup .overlay/.popup DOM — structure only; copy is placeholder)
    - tests/42-whats-new-gating.test.js + tests/42-whats-new-dismiss.test.js (the RED contract to satisfy)
  </read_first>
  <behavior>
    - eligible(): true iff `APP_VERSION !== lsGet('sg.whatsNewLastSeenVersion')` AND an entry exists for APP_VERSION in `window.CHANGELOG_CONTENT_EN`; false otherwise (T-42-V1).
    - First-run: with fresh state, run() shows welcome; after welcome subsume-write, whats-new eligible() is false (T-42-V2).
    - Silent-skip reconcile (D-07): at eval, if `APP_VERSION !== lastSeen && !entryFor(APP_VERSION)` → `lsSet('sg.whatsNewLastSeenVersion', APP_VERSION)` (T-42-V3).
    - show(): builds a modest centered modal (role=dialog, aria-modal=true, aria-labelledby) rendering the latest entry's 2-4 highlights via textContent; moves focus into the dialog; scroll lock (T-42-V5).
    - dismiss(): a single helper called by Close, Esc, and "See everything new" — each removes the overlay, restores focus to opener, and writes lastSeen=APP_VERSION. Backdrop/outside click is a NO-OP (T-42-V4). "See everything new" writes lastSeen BEFORE navigating to `changelog.html#{latest anchor}`.
  </behavior>
  <action>
    Author a self-registering IIFE (`"use strict"`, Phase-31 four-slot responsibility banner, no new globals) mirroring the welcome surface. Copy the `lsGet`/`lsSet` try/catch helpers and reuse the EXACT key string `sg.whatsNewLastSeenVersion` (attention-coordinator.js:51) — do not invent a key. Read the version ONLY from `window.AppVersion.APP_VERSION` (version.js:27) — never `INTEGRITY_TOKEN` or a second constant. Read entries from `window.CHANGELOG_CONTENT_EN`; `entryFor(v)` returns the entry whose `version===v`.
    Register at eval time: `window.AttentionCoordinator.register({ id:'whats-new', eligible, show })` (guard on the coordinator existing), so registration precedes `bootAttentionSurfaces()`'s `run()` (app.js:1464-1473).
    Build `show()` as a teaser-plus-link announcement (D-05: headline + top 2-4 highlights + one primary "See everything new" + a quiet Close; the popup is an announcement, not a reader) by mirroring the welcome overlay mount (attention-coordinator.js:126-254): remember `opener=document.activeElement`, `App.lockBodyScroll()`, create `.whats-new-overlay > .whats-new-popup[role="dialog"][aria-modal="true"][aria-labelledby=<headlineId>]`, headline text from i18n `whatsNew.title` interpolating major-minor of APP_VERSION, subline `whatsNew.sub`, a list of the latest entry's `highlights` each appended via `createElement`+`textContent` (XSS boundary — NO innerHTML with content; only compile-time-literal SVG may use innerHTML), a primary `.btn-primary` button labelled `whatsNew.seeAll` (D-05), and a quiet `.btn-quiet` button labelled `whatsNew.close` (D-05). Move focus to the primary CTA on mount.
    Implement the focus-trap `onKeydown` (Tab cycle between the two buttons; Escape → dismiss) verbatim from attention-coordinator.js:201-218, adjusting the two focus endpoints. Wire Close→dismiss, "See everything new"→dismiss then `location.href='changelog.html#'+latest.anchor`, Esc→dismiss. Attach NO overlay/backdrop click handler (D-09; the welcome has none either).
    Add the D-07 `reconcileSilentSkip` IIFE that runs at eval.
    Note: this module does NOT need the coordinator file edited — registration is self-contained here (attention-coordinator.js already reserves the slot).
  </action>
  <verify>
    <automated>node tests/42-whats-new-gating.test.js &amp;&amp; node tests/42-whats-new-dismiss.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-whats-new-gating.test.js` and `node tests/42-whats-new-dismiss.test.js` both PASS (were RED in plan 01).
    - Surface registered under id `'whats-new'` reachable via `AttentionCoordinator._getSurface('whats-new')`.
    - Uses the exact key `sg.whatsNewLastSeenVersion` and reads version only from `window.AppVersion.APP_VERSION` — `grep -n 'INTEGRITY_TOKEN' assets/whats-new.js` returns nothing.
    - `grep -nE 'innerHTML' assets/whats-new.js` shows innerHTML used ONLY for compile-time-literal SVG (if at all), never for entry/highlight text.
    - No overlay/backdrop click→dismiss handler present (backdrop is a no-op).
    - `node -c assets/whats-new.js` passes.
  </acceptance_criteria>
  <done>whats-new.js registers, gates (V1/V2/V3), renders the modest modal with a11y (V5), dismisses deliberately on all 3 paths with backdrop no-op (V4), and reconciles silent-skip — all guarded tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add .whats-new-* popup CSS to assets/app.css (token-based, no literal hex)</name>
  <files>assets/app.css</files>
  <read_first>
    - assets/app.css (lines 1701-1704 .modal-overlay + --color-modal-overlay-bg; 4741-4900 .welcome-overlay/.welcome-panel block — the adjacent placement + logical-property idiom to mirror; existing .btn-primary/.btn-quiet definitions)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Color, Spacing, Typography, Interaction Contract → popup weight/focal point)
    - .planning/sketches/005-changelog-register/index.html (Variant B popup .overlay/.popup rules — port token-based rules ONLY)
    - assets/tokens.css (line 114 --color-text-inverse and the semantic color tokens)
  </read_first>
  <action>
    Add a `.whats-new-overlay` / `.whats-new-popup` CSS block to `app.css`, placed adjacent to the `.welcome-overlay` block (~4741) with a section banner. Port the sketch 005 popup layout as a MODEST CENTERED modal (`max-inline-size: 420px`, `border-radius: 16px` — the approved sketch values, D-06/UI-SPEC), backdrop uses `--color-modal-overlay-bg` (matching `.modal-overlay`), surface uses `--color-surface`, text uses `--color-text`/`--color-text-muted`. Use CSS LOGICAL PROPERTIES only (`padding-inline`, `margin-block`, `inset-inline-*`) — RTL-safe. Add the `[data-theme="dark"]` overrides the sketch shows.
    Do NOT port the sketch's literal-hex button color — the popup buttons reuse the app's existing `.btn-primary` (filled, holds the single 10% accent) and `.btn-quiet` (neutral Close); if a text-on-primary color is needed use `var(--color-text-inverse)` (tokens.css:114). NO literal hex anywhere in the added block. Focus-visible outline `2px solid var(--color-primary)` `outline-offset:2px`; buttons meet the 44x44 tap-target minimum (inherited app rule).
  </action>
  <verify>
    <automated>awk '/whats-new-overlay|whats-new-popup/{f=1} f' assets/app.css | grep -nE '#[0-9a-fA-F]{3,8}\b' &amp;&amp; echo "HEX-FOUND-FAIL" || echo "no literal hex in whats-new block OK"</automated>
  </verify>
  <acceptance_criteria>
    - The `.whats-new-overlay`/`.whats-new-popup` block exists in `app.css` and contains NO literal hex color (the awk-scoped grep finds none).
    - Block uses logical properties only (no physical `left`/`right`/`margin-left`/`margin-right` in the added rules) and includes a `[data-theme="dark"]` variant.
    - Buttons are the app's `.btn-primary`/`.btn-quiet` (no bespoke button color); backdrop uses `--color-modal-overlay-bg`.
    - `node tests/42-whats-new-dismiss.test.js` still passes (CSS change does not break the jsdom behavior test).
  </acceptance_criteria>
  <done>Popup is styled from app.css (always-loaded, precached) as a modest centered modal, token-based, RTL/dark-safe, zero literal hex, reusing app button classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array → popup DOM | latest entry highlights render into the modal |
| localStorage `sg.whatsNewLastSeenVersion` → app | governs whether the popup shows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | whats-new.js show() render | medium | mitigate | All highlight text via createElement+textContent (help.js:22-24 contract); only compile-time-literal SVG may use innerHTML. Guarded by T-42-V5. |
| T-42-02 | Tampering/Spoofing | localStorage sg.whatsNewLastSeenVersion | low | accept | Non-sensitive UX flag; worst case one popup re-shows or is suppressed. Reads/writes wrapped in try/catch (lsGet/lsSet). No security impact. |
| T-42-06 | Spoofing | version source | low | mitigate | Version read ONLY from AppVersion.APP_VERSION; never the SW/integrity token (grep-verified) — no double-signalling with the integrity nudge. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-whats-new-gating.test.js` + `node tests/42-whats-new-dismiss.test.js` green; no INTEGRITY_TOKEN read; no backdrop-close handler; no literal hex in the popup CSS block. Real offline popup behavior is field-verified in the phase-gate UAT (jsdom cannot exercise the SW).
</verification>

<success_criteria>
- CHLG-01 popup behavior fully implemented and test-green: once-per-version gating, first-run suppression, D-07 silent-skip, deliberate-dismiss (backdrop no-op), latest-highlights content with dialog a11y, deep-link CTA.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-05-SUMMARY.md` when done.
</output>
