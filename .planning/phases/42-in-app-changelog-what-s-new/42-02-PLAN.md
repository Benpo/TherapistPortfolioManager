---
phase: 42-in-app-changelog-what-s-new
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/42-changelog-render.test.js
  - tests/42-changelog-integrity.test.js
autonomous: true
requirements: [CHLG-02, CHLG-03, CHLG-04]
must_haves:
  truths:
    - "Page-render behavior (reverse-chron cards, empty categories omitted, v1.0 one-line, kebab anchors, EN-fallback) is pinned RED before changelog.js exists"
    - "Data-source structural integrity (single source, v1.3 first, highlights 2-4, no emoji, v1.0 origin-only) is pinned RED before changelog-content-en.js exists"
  artifacts:
    - tests/42-changelog-render.test.js
    - tests/42-changelog-integrity.test.js
  key_links:
    - "Render test seeds a window.CHANGELOG_CONTENT_EN fixture + evals changelog.js and reaches it via window.Changelog (mirrors window.Help)"
    - "Integrity test loads the REAL assets/changelog-content-en.js + assets/i18n-en.js in a vm sandbox (mirrors tests/39-help-integrity.test.js)"
---

<objective>
Author the two RED test files that pin the changelog PAGE render (CHLG-02) and the single DATA SOURCE integrity + self-hosting v1.3 entry (CHLG-03/04) BEFORE implementation.

Purpose: These guard the two hardest correctness claims — that both surfaces read ONE source, and that the data shape is Phase-43-checkable (highlights field, no emoji, v1.0 origin-only). Behavior/integrity tests precede implementation (project rule).
Output: `tests/42-changelog-render.test.js` (T-42-V7/V8) and `tests/42-changelog-integrity.test.js` (T-42-V9), auto-discovered by `tests/run-all.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@tests/39-help-render.test.js
@tests/39-help-integrity.test.js
@assets/help.js
@assets/help-content-en.js
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js` (→ `window.CHANGELOG_CONTENT_EN`), `assets/whats-new.js` (surface `'whats-new'`), `assets/changelog.js` (→ `window.Changelog`), `changelog.html` (route `/changelog`), `assets/changelog.css`, 7 `tests/42-*.test.js`. Modified: `assets/app.css` (`.whats-new-*` popup CSS), `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`. Entry schema fields: `version`, `anchor`, `date`, `lede`, `highlights[2-4]`, `categories{new?,improved?,fixed?}`, `origin?`. Storage: reuses `sg.whatsNewLastSeenVersion`. New i18n keys: `changelog.*` (5) + `whatsNew.*` (5).

<tasks>

<task type="auto">
  <name>Task 1: RED page-render test — tests/42-changelog-render.test.js (T-42-V7/V8)</name>
  <files>tests/42-changelog-render.test.js</files>
  <read_first>
    - tests/39-help-render.test.js (jsdom render harness to mirror: build window, inject content-global fixture, eval the page module, assert DOM built from the global)
    - assets/help.js (lines 22-24 XSS banner, 349-464 render-from-global, 455-463 app:language re-render, window.Help seam)
    - .planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md (T-42-V7/V8 rows)
  </read_first>
  <action>
    Mirror `tests/39-help-render.test.js`. In jsdom, provide the empty container(s) `changelog.html` will ship, seed a `window.CHANGELOG_CONTENT_EN` fixture (a v1.3 content entry with `categories.new` + `categories.improved`, a v1.2 entry with all three categories, and a v1.0 `{ origin:true }` entry with no categories/highlights), then eval `assets/changelog.js` and drive its render via `window.Changelog`.
    Assert:
    - T-42-V7: entries render REVERSE-CHRONOLOGICAL (v1.3 card before v1.2 before v1.0); an entry with an empty/absent category renders NO block for it (empty categories omitted, D-11); the v1.0 `origin:true` entry renders as a single one-line marker with NO New/Improved/Fixed blocks (D-01); each version card carries a stable kebab `id` matching its `anchor` (e.g. `v1-3`).
    - T-42-V8 (EN-fallback, D-16): when `window.CHANGELOG_CONTENT_HE` is absent, the EN entries render; when a locale array is present but missing an entry version, that entry falls back to its EN counterpart (history complete in every locale).
    References `assets/changelog.js` (absent) → RED.
  </action>
  <verify>
    <automated>node tests/42-changelog-render.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-changelog-render.test.js` exits NON-zero now (RED, missing changelog.js), not a parse error (`node -c` passes).
    - Test asserts reverse-chron order, empty-category omission, v1.0 one-line origin render, kebab `id` anchors, and EN-fallback — mapped to T-42-V7/V8.
    - Test drives render through `window.Changelog` (the test seam mirroring `window.Help`) and injects its own `window.CHANGELOG_CONTENT_EN` fixture (independent of plan 04's real file).
  </acceptance_criteria>
  <done>Render test exists, valid JS, fails RED for missing changelog.js, pins V7/V8.</done>
</task>

<task type="auto">
  <name>Task 2: RED data-integrity test — tests/42-changelog-integrity.test.js (T-42-V9)</name>
  <files>tests/42-changelog-integrity.test.js</files>
  <read_first>
    - tests/39-help-integrity.test.js (lines 29-52 vm-sandbox load of i18n-en.js + content module, structure assertions to mirror)
    - assets/help-content-en.js (lines 1-46 header-comment schema contract precedent)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (## Entry Schema — the exact field contract)
    - .planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md (T-42-V9 row)
  </read_first>
  <action>
    Mirror `tests/39-help-integrity.test.js`. In a `vm` sandbox, load the REAL `assets/i18n-en.js` then `assets/changelog-content-en.js` and read `window.CHANGELOG_CONTENT_EN`. Assert the schema contract (RESEARCH ## Entry Schema):
    - `version` is a unique semver string per entry; array is strictly REVERSE-CHRONOLOGICAL by version.
    - Each CONTENT entry (non-origin) has a non-empty `lede`, a `highlights` array of length 2-4 (D-08), and a `categories` object whose keys ⊆ {`new`,`improved`,`fixed`} with each present category a non-empty string array.
    - The v1.0 entry has `origin:true` and NO `highlights`/`categories` (D-01 one-line marker).
    - The FIRST array element is the latest and equals `window.AppVersion.APP_VERSION` semantics — `version === '1.3.0'` — and has non-empty `highlights` (self-hosting proof, CHLG-04; popup source, D-08).
    - NO emoji code point appears in any string field (D-10) — assert against the surrogate-pair / emoji-range regex used by the i18n no-emoji tests.
    References `assets/changelog-content-en.js` (absent) → RED.
  </action>
  <verify>
    <automated>node tests/42-changelog-integrity.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-changelog-integrity.test.js` exits NON-zero now (RED, missing changelog-content-en.js), not a parse error (`node -c` passes).
    - Test asserts: unique semver + reverse-chron; content-entry `lede` non-empty; `highlights` length 2-4; `categories` keys ⊆ {new,improved,fixed} non-empty; v1.0 `origin:true` with no highlights/categories; first element `version==='1.3.0'` with highlights; zero emoji.
    - Test loads the REAL data module via vm (not a fixture) so it genuinely validates plan 04's output (mirrors 39-help-integrity vm pattern).
  </acceptance_criteria>
  <done>Integrity test exists, valid JS, fails RED for missing data module, pins V9 including the CHLG-04 v1.3-first assertion and the D-10 no-emoji gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array → rendered DOM | changelog entry strings render into the page DOM |
| localStorage `sg.whatsNewLastSeenVersion` → app | untrusted stored flag governs the popup (out of scope for this render/data plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | changelog.js render path | medium | mitigate | The render test asserts entry strings render as `textContent`; the actual mitigation (createElement+textContent only; no innerHTML with content) lands in plan 06 per help.js:22-24. |
| T-42-04 | Tampering | changelog-content-en.js emoji/register hygiene | low | mitigate | The integrity test enforces the D-10 no-emoji contract on all data strings, blocking accidental control/emoji injection at author time. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A); tests use the installed jsdom/vm/assert only. |
</threat_model>

<verification>
Both files fail RED for the right reason (missing `changelog.js` / `changelog-content-en.js`), are valid JS, and are auto-discovered by `tests/run-all.js`. Plans 04 and 06 turn them green.
</verification>

<success_criteria>
- `tests/42-changelog-render.test.js` and `tests/42-changelog-integrity.test.js` exist, are valid JS, exit non-zero (RED).
- Render pins V7/V8; integrity pins V9 including v1.3-first (CHLG-04) and D-10 no-emoji.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-02-SUMMARY.md` when done.
</output>
