---
phase: 42-in-app-changelog-what-s-new
plan: 09
type: execute
wave: 2
depends_on: [04, 05, 07]
files_modified:
  - add-client.html
  - add-session.html
  - index.html
  - report.html
  - reporting.html
  - sessions.html
  - settings.html
  - sw.js
autonomous: true
requirements: [CHLG-01]
must_haves:
  truths:
    - "The What's-New popup can fire on whichever page the session starts — because changelog-content-en.js + whats-new.js load on EVERY chrome-mounting page"
    - "The changelog page, its renderer, styles, data, and the popup surface are all available fully offline via the correct precache arrays"
  artifacts:
    - "7 chrome-mounting HTML pages (add-client, add-session, index, report, reporting, sessions, settings) — each with changelog-content-en.js + whats-new.js in the script chain"
    - "sw.js — /assets/changelog-content-en.js, /assets/whats-new.js, /assets/changelog.js, /assets/changelog.css in PRECACHE_URLS; /changelog in PRECACHE_HTML"
  key_links:
    - "AttentionCoordinator.run() (called in app.js initCommon on every page) needs the surface registered + the data global loaded, or the popup slot is empty on that launch page (Pitfall 2)"
    - "The PAGE goes in PRECACHE_HTML as extensionless /changelog (mirrors /help), NOT changelog.html in PRECACHE_URLS (correction #1; Pitfall 1)"
---

<objective>
Wire the popup surface + data source onto EVERY chrome-mounting page and add the two-array offline precache to `sw.js`. This is the real integration cost that makes CHLG-01's "works fully offline" and "fires on whichever page the session starts" true. Turns the precache test (T-42-V6) green.

Purpose: `AttentionCoordinator.run()` fires once on the launch page; if `whats-new.js` isn't registered and `window.CHANGELOG_CONTENT_EN` isn't loaded there, the popup never fires (Pitfall 2). And the offline guarantee requires the correct two-array precache split (Pitfall 1 / correction #1).
Output: 2 new script tags on 7 pages; 4 assets in PRECACHE_URLS + `/changelog` in PRECACHE_HTML.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@help.html
@sw.js
</context>

## Artifacts this phase produces (shared inventory)

Modified (THIS plan): 7 chrome-mounting HTML pages (add-client, add-session, index, report, reporting, sessions, settings) get 2 new `<script>` tags each; `sw.js` gets the two-array precache entries. NOTE: `help.html` gets its 2 scripts + rail link in plan 10 (avoids a same-wave file conflict); `changelog.html` is born with its scripts in plan 07. Full phase inventory: `assets/changelog-content-en.js`, `assets/whats-new.js`, `assets/changelog.js`, `changelog.html`, `assets/changelog.css`, `assets/attention-coordinator.js` (tour guard), `assets/app.css`, `assets/app.js`, `assets/shared-chrome.js`, `assets/i18n-{en,he,de,cs}.js`, 7 tests. Storage: reuses `sg.whatsNewLastSeenVersion`.

<tasks>

<task type="auto">
  <name>Task 1: Add changelog-content-en.js + whats-new.js to the 7 chrome-mounting pages' script chains</name>
  <files>add-client.html, add-session.html, index.html, report.html, reporting.html, sessions.html, settings.html</files>
  <read_first>
    - help.html (lines 114-136 script chain — the reference order: version.js + attention-coordinator.js load before app.js; corpus-comment placement)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (Pitfall 2 + load-order note: after version.js + attention-coordinator.js + changelog-content-en.js, before app.js runs initCommon)
    - index.html (locate its existing attention-coordinator.js script tag as the insertion anchor)
  </read_first>
  <action>
    On EACH of the 7 pages, insert two script tags — first `assets/changelog-content-en.js`, then `assets/whats-new.js` — immediately AFTER the existing `attention-coordinator.js` script tag (which itself follows `version.js`), and before `app.js`'s initCommon runs at DOMContentLoaded. Order matters: data global before the surface; both after the coordinator (whats-new registers into it) and after version.js (reads APP_VERSION). Match each page's existing indentation/quoting style (relative `./assets/...` src like the sibling tags). Do NOT touch help.html (plan 10) or changelog.html (plan 07). Make the identical edit on all 7 pages so the popup can fire regardless of which page starts the session.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const ps=['add-client','add-session','index','report','reporting','sessions','settings'];let bad=[];for(const p of ps){const s=fs.readFileSync(p+'.html','utf8');const ac=s.indexOf('attention-coordinator.js'),cc=s.indexOf('changelog-content-en.js'),wn=s.indexOf('whats-new.js');if(cc<0||wn<0){bad.push(p+':missing')}else if(!(ac<cc&&cc<wn)){bad.push(p+':order')}}if(bad.length){console.error('FAIL',bad);process.exit(1)}console.log('all 7 pages wired in order OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 pages contain `changelog-content-en.js` then `whats-new.js`, both AFTER `attention-coordinator.js` (verify command passes).
    - Neither help.html nor changelog.html is modified by this task.
    - `node -e` HTML integrity check passes for each page (no broken script tags).
  </acceptance_criteria>
  <done>All 7 chrome-mounting pages load the data source + popup surface in the correct order, so the popup fires on any launch page.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two-array offline precache entries to sw.js</name>
  <files>sw.js</files>
  <read_first>
    - sw.js (lines 33 PRECACHE_URLS array, 120-143 PRECACHE_HTML array incl the /help precedent at ~142, 175 fetch(url,{cache:'reload'}) anti-stale guard)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (## Offline Wiring — exact entries; Pitfall 1 anti-pattern)
    - tests/42-precache.test.js (the RED contract to satisfy — plan 03)
  </read_first>
  <action>
    In `PRECACHE_URLS` (sw.js:33) add the four sub-resource assets: `/assets/changelog-content-en.js`, `/assets/whats-new.js`, `/assets/changelog.js`, `/assets/changelog.css`. In `PRECACHE_HTML` (sw.js:120-143) add the extensionless page route `/changelog` (mirror the `/help` entry at ~142). Do NOT put the page (changelog.html / any HTML) in PRECACHE_URLS — that reproduces the redirect failure the array split prevents (Pitfall 1). Do NOT add a popup-CSS entry — the popup CSS lives in `app.css` (already precached). Do NOT manually bump CACHE_NAME — it auto-rolls from the deploy-stamped INTEGRITY_TOKEN. Leave the anti-stale `fetch(url,{cache:'reload'})` guard (sw.js:175) intact.
  </action>
  <verify>
    <automated>node tests/42-precache.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-precache.test.js` PASSES (was RED in plan 03): four assets in PRECACHE_URLS, `/changelog` in PRECACHE_HTML, page NOT in PRECACHE_URLS, `fetch(url,{cache:'reload'})` intact.
    - No `whats-new.css` entry (popup CSS lives in app.css).
    - No manual CACHE_NAME edit.
    - `node -c sw.js` passes.
  </acceptance_criteria>
  <done>The page (as /changelog) + its four assets are precached via the correct two arrays; anti-stale guard intact; precache test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW precache → offline delivery | correct two-array precache is required for the offline guarantee (CHLG-01) |
| localStorage `sg.whatsNewLastSeenVersion` → app | governs the popup on whichever page loads it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-05 | Denial of Service (offline availability) | sw.js precache shape | low | mitigate | Two-array split (assets→PRECACHE_URLS, page→PRECACHE_HTML) + anti-stale fetch guarantees offline availability without stale-cache poisoning; guarded by T-42-V6 + real-device offline UAT. |
| T-42-01 | Tampering | popup on non-changelog pages | medium | mitigate | The popup surface loaded on every page still renders via textContent only (mitigation owned by plan 05); this plan only wires the script load order. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-precache.test.js` green; all 7 pages wired in correct order; no page in PRECACHE_URLS; anti-stale guard intact. Real offline cold-launch popup + /changelog navigation is field-verified in the phase-gate UAT (jsdom cannot exercise the SW).
</verification>

<success_criteria>
- CHLG-01 offline + every-page firing wired: 7 pages load the surface/data in order; sw.js precaches the four assets + /changelog via the correct arrays; precache test green.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-09-SUMMARY.md` when done.
</output>
