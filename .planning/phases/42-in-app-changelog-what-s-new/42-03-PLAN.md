---
phase: 42-in-app-changelog-what-s-new
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/42-precache.test.js
  - tests/42-demo-gate.test.js
  - tests/42-i18n-parity.test.js
autonomous: true
requirements: [CHLG-01, CHLG-02]
must_haves:
  truths:
    - "The two-array offline precache contract (assets → PRECACHE_URLS, page → PRECACHE_HTML, anti-stale fetch intact) is pinned RED before sw.js is edited"
    - "Demo-mode hiding of the What's-New entry points is pinned RED before app.js/shared-chrome.js are edited"
    - "4-locale parity + non-empty + no-emoji for all changelog.*/whatsNew.* chrome keys is pinned RED before i18n files are edited"
  artifacts:
    - tests/42-precache.test.js
    - tests/42-demo-gate.test.js
    - tests/42-i18n-parity.test.js
  key_links:
    - "Precache test isolates the PRECACHE_URLS block AND the PRECACHE_HTML block separately (mirrors tests/39-help-precache.test.js) — asserting the page is NOT in PRECACHE_URLS"
    - "Demo test drives initHelpEntry with window.name==='demo-mode' and asserts no whatsNew.menuRow row + inert footer version link"
---

<objective>
Author the three RED test files pinning the offline precache SHAPE (CHLG-01/02, correction #1), demo-mode hiding (D-15), and 4-locale i18n parity (D-17) BEFORE the wiring/i18n plans run.

Purpose: These lock the three mechanical-correctness constraints the research flagged — especially the two-array precache split that prevents the redirect failure, and the every-page demo suppression. Shape/parity tests precede implementation.
Output: `tests/42-precache.test.js` (T-42-V6), `tests/42-demo-gate.test.js` (T-42-V10), `tests/42-i18n-parity.test.js` (T-42-V11).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@tests/39-help-precache.test.js
@tests/41-demo-gate.test.js
@tests/40-i18n-parity.test.js
@assets/app.js
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js`, `assets/whats-new.js`, `assets/changelog.js`, `changelog.html` (route `/changelog`), `assets/changelog.css`, 7 `tests/42-*.test.js`. Modified: `assets/app.css` (`.whats-new-*` popup CSS), `assets/app.js` (`whatsNew.menuRow` row + demo filter), `assets/shared-chrome.js` (footer link, demo-inert), `help.html`, `sw.js` (two-array precache), 7 chrome pages (2 scripts each), `assets/i18n-{en,he,de,cs}.js`. New i18n keys: `changelog.page.title`, `changelog.page.intro`, `changelog.cat.new`, `changelog.cat.improved`, `changelog.cat.fixed`, `whatsNew.title`, `whatsNew.sub`, `whatsNew.seeAll`, `whatsNew.close`, `whatsNew.menuRow`.

<tasks>

<task type="auto">
  <name>Task 1: RED precache-shape test — tests/42-precache.test.js (T-42-V6)</name>
  <files>tests/42-precache.test.js</files>
  <read_first>
    - tests/39-help-precache.test.js (lines 44-71: isolate PRECACHE_URLS regex + PRECACHE_HTML regex separately; assert anti-stale fetch(url,{cache:'reload'}); the /help precedent)
    - sw.js (lines 33 PRECACHE_URLS, 120 PRECACHE_HTML, 175 fetch(url,{cache:'reload'}))
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (## Offline Wiring — the exact array assignments; Pitfall 1)
  </read_first>
  <action>
    Mirror `tests/39-help-precache.test.js`. fs-read `sw.js`, isolate the `PRECACHE_URLS` array text and the `PRECACHE_HTML` array text as SEPARATE regions (do not scan the whole file — mirror how 39-help isolates each block). Assert:
    - `PRECACHE_URLS` contains `/assets/changelog-content-en.js`, `/assets/whats-new.js`, `/assets/changelog.js`, `/assets/changelog.css` (the four sub-resource assets).
    - `PRECACHE_HTML` contains `/changelog` (the extensionless page route) — and `PRECACHE_URLS` does NOT contain any `changelog.html` / `/changelog` HTML entry (the Pitfall-1 anti-pattern).
    - The anti-stale guard `fetch(url,{cache:'reload'})` is still present in the install handler (region-scoped grep, tolerant of whitespace) and there is no bare `cache.add(url)` for these entries.
    Note the popup CSS is intentionally NOT a precache entry (it lives in `app.css`, already precached) — do NOT assert a `whats-new.css` entry.
    References the not-yet-edited `sw.js` entries → RED.
  </action>
  <verify>
    <automated>node tests/42-precache.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-precache.test.js` exits NON-zero now (RED, sw.js not yet edited), not a parse error (`node -c` passes).
    - Test isolates PRECACHE_URLS and PRECACHE_HTML as distinct regions and asserts the four assets in URLS + `/changelog` in HTML + page NOT in URLS.
    - Test re-asserts `fetch(url,{cache:'reload'})` intact.
    - Test does NOT expect a `whats-new.css` entry (popup CSS lives in app.css).
  </acceptance_criteria>
  <done>Precache test exists, valid JS, fails RED for un-edited sw.js, pins V6 with the two-array split.</done>
</task>

<task type="auto">
  <name>Task 2: RED demo-gate test — tests/42-demo-gate.test.js (T-42-V10)</name>
  <files>tests/42-demo-gate.test.js</files>
  <read_first>
    - tests/41-demo-gate.test.js (the demo-mode harness to mirror: set window.name='demo-mode', run initHelpEntry, assert a row is absent)
    - assets/app.js (lines 471-472 comment, 518-529 items array, 533-535 demo filter)
    - assets/shared-chrome.js (lines 108 isDemo, 135-136 footer version line)
  </read_first>
  <action>
    Mirror `tests/41-demo-gate.test.js`. Two assertions:
    - Menu row (D-15): with `window.name==='demo-mode'`, running `initHelpEntry` renders NO row whose label resolves from `whatsNew.menuRow`; in normal mode the row IS present and its `href` is `./changelog.html`.
    - Footer link (D-15 / discretion guard): with `window.name==='demo-mode'`, the `SharedChrome` footer renders the `v{APP_VERSION}` text INERT (no `<a href="./changelog.html">` wrapping the version text); in normal mode the version text IS wrapped in an anchor to `./changelog.html` while the `.app-footer-version-warn` span remains a SIBLING OUTSIDE the anchor.
    References the not-yet-added row/link → RED.
  </action>
  <verify>
    <automated>node tests/42-demo-gate.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-demo-gate.test.js` exits NON-zero now (RED), not a parse error (`node -c` passes).
    - Test asserts: demo → no `whatsNew.menuRow` row; normal → row present with `href='./changelog.html'`.
    - Test asserts: demo → footer version text inert; normal → version text wrapped in anchor to `./changelog.html` with `.app-footer-version-warn` OUTSIDE the anchor.
  </acceptance_criteria>
  <done>Demo-gate test exists, valid JS, fails RED, pins V10 (menu row + footer link independence guard).</done>
</task>

<task type="auto">
  <name>Task 3: RED i18n-parity test — tests/42-i18n-parity.test.js (T-42-V11)</name>
  <files>tests/42-i18n-parity.test.js</files>
  <read_first>
    - tests/40-i18n-parity.test.js (the parity harness to mirror: vm-load all 4 locale dicts, assert presence + non-empty + parity + no-emoji per key)
    - assets/i18n-en.js (lines 596-598, 650 help.entry.* precedent for where chrome keys live)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (the 10 chrome keys to add)
  </read_first>
  <action>
    Mirror `tests/40-i18n-parity.test.js`. In a vm sandbox load `assets/i18n-en.js`, `i18n-he.js`, `i18n-de.js`, `i18n-cs.js`. For EACH of the 10 keys — `changelog.page.title`, `changelog.page.intro`, `changelog.cat.new`, `changelog.cat.improved`, `changelog.cat.fixed`, `whatsNew.title`, `whatsNew.sub`, `whatsNew.seeAll`, `whatsNew.close`, `whatsNew.menuRow` — assert: present in all 4 locales, non-empty string in each, key set is identical across locales (parity), and no emoji code point in any value (D-10).
    References the not-yet-added keys → RED.
  </action>
  <verify>
    <automated>node tests/42-i18n-parity.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-i18n-parity.test.js` exits NON-zero now (RED, keys not yet added), not a parse error (`node -c` passes).
    - Test enumerates all 10 keys and asserts presence + non-empty + cross-locale parity + no-emoji across en/he/de/cs.
  </acceptance_criteria>
  <done>i18n-parity test exists, valid JS, fails RED, pins V11 for all 10 chrome keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo iframe → app chrome | demo mode must not expose changelog entry points (menu row, footer link, page route) |
| SW precache → offline delivery | correct two-array precache is required for the offline guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-03 | Information Disclosure | demo-mode entry points | low | mitigate | The demo-gate test pins D-15 suppression (menu row filtered, footer link inert; coordinator already disabled in demo). Actual gating lands in plan 09. |
| T-42-05 | Denial of Service (offline availability) | sw.js precache shape | low | mitigate | The precache test pins the two-array split + anti-stale fetch so the page/assets are genuinely available offline (correction #1); actual sw.js edit lands in plan 08. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A); tests use installed jsdom/vm/assert/fs only. |
</threat_model>

<verification>
All three files fail RED for the right reason (un-edited sw.js / app.js / shared-chrome.js / i18n files), are valid JS, and are auto-discovered by `tests/run-all.js`. Plans 07/08/09 turn them green.
</verification>

<success_criteria>
- `tests/42-precache.test.js`, `tests/42-demo-gate.test.js`, `tests/42-i18n-parity.test.js` exist, valid JS, exit non-zero (RED).
- Precache pins the two-array split (page NOT in PRECACHE_URLS); demo pins V10; i18n pins V11 for all 10 keys.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-03-SUMMARY.md` when done.
</output>
