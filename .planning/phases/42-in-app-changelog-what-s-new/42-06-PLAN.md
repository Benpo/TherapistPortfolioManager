---
phase: 42-in-app-changelog-what-s-new
plan: 06
type: execute
wave: 1
depends_on: [01]
files_modified:
  - assets/attention-coordinator.js
autonomous: true
requirements: [CHLG-01]
must_haves:
  truths:
    - "While the onboarding tour is active, the attention coordinator's run() shows NO governed surface — protecting the What's-New popup AND every other coordinator-managed surface (welcome, security-note, install-nudge, mobile-hint, tour-reminder) during the tour"
    - "The guard is ONE check inside the coordinator, not a per-popup check — every current and future surface inherits it automatically"
    - "Suppression during the tour does NOT burn the one-per-session marker, so once the tour ends a later run() can still show an eligible surface"
  artifacts:
    - "assets/attention-coordinator.js (run() tour-active early-return guard)"
  key_links:
    - "run() reads window.Tour.isActive() (tour.js:657) — typeof-guarded, mirroring the defense-in-depth window.Tour guard at attention-coordinator.js:242"
    - "The guard sits alongside the existing isDemo() (line 93) and session-marker (line 94) non-claiming early returns in run() (line 92)"
---

<objective>
Add ONE coordinator-level guard to `assets/attention-coordinator.js`'s `run()`: while the onboarding tour is active (`window.Tour.isActive()`), `run()` returns early and shows nothing. This protects the What's-New popup and every other governed surface during the tour — implemented once at the coordinator, not as per-surface checks. Turns the tour-guard test (T-42-V12, plan 01) green.

Purpose: Ben-locked split (recorded in STATE.md completed-task 260709-o77, line ~453): the backup **schedule prompt** was fixed narrowly in `checkBackupSchedule()` (o77, "option 1"; the backup surface is NOT coordinator-governed per Phase 40 D-04), and the **coordinator-level tour guard was explicitly assigned to Phase 42** ("sentence handed to the 42 planner"; routing backup through the coordinator was the rejected "option 2"). This plan is that coordinator half — it COMPLEMENTS o77 (disjoint surfaces: o77 covers the backup modal, this covers the six governed coordinator surfaces) and catches re-invoked `run()` calls (e.g. the install-nudge re-arming `run()` when `beforeinstallprompt` fires mid-tour) at the coordinator level, so the new What's-New popup and every other governed surface inherit tour-suppression from one check.
Output: a tour-active early-return in `run()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@assets/attention-coordinator.js
@assets/tour.js
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js`, `assets/whats-new.js`, `assets/changelog.js`, `changelog.html`, `assets/changelog.css`, 7 `tests/42-*.test.js`. Modified: `assets/attention-coordinator.js` (THIS plan — run() tour guard), `assets/app.css`, `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`. This plan touches ONLY `attention-coordinator.js` and does not conflict with the whats-new.js surface (plan 05) which self-registers without editing the coordinator.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tour-active early-return guard to run() in assets/attention-coordinator.js</name>
  <files>assets/attention-coordinator.js</files>
  <read_first>
    - assets/attention-coordinator.js (lines 87-105 run(): the doc-comment contract that the session marker is claimed ONLY when a surface shows; the two existing non-claiming early returns — isDemo() at 93 and ssGet(SESSION_MARKER) at 94; the typeof-guarded window.Tour usage at 242 as the defense-in-depth idiom to mirror)
    - assets/tour.js (lines 37, 657, 673, 702 — window.Tour.isActive() returns whether a tour run is currently mounted)
    - tests/42-coordinator-tour-guard.test.js (the RED contract to satisfy — plan 01)
  </read_first>
  <behavior>
    - When `window.Tour` exists and `window.Tour.isActive()` returns true, `run()` returns immediately and invokes NO surface's show().
    - When `window.Tour` is absent OR `isActive()` returns false, `run()` behaves exactly as before (shows the top-eligible surface).
    - The guard is a NON-claiming early return: it does not set `sg.surfaceShownThisSession`, so a subsequent `run()` after the tour ends can still show an eligible surface.
  </behavior>
  <action>
    In `run()` (attention-coordinator.js:92), add a third early-return guard alongside the existing `if (isDemo()) return;` (line 93) and `if (ssGet(SESSION_MARKER) === '1') return;` (line 94). Add: return early when the tour is active — `if (window.Tour && typeof window.Tour.isActive === 'function' && window.Tour.isActive()) return;` — typeof-guarded exactly like the defense-in-depth `window.Tour` usage at line 242. Place it so it returns BEFORE the PRECEDENCE loop and BEFORE any session-marker claim (it must not burn the one-per-session slot). Add a one-line comment noting this is the coordinator-wide tour-suppression guard (protects all governed surfaces during a tour; fixes the backup-prompt-over-tour class of bug). Update the run() header doc-comment (lines 87-92) to note the tour-active guard alongside demo-off (D-09) and one-per-session (D-02). Do NOT add any per-surface tour checks anywhere else.
  </action>
  <verify>
    <automated>node tests/42-coordinator-tour-guard.test.js &amp;&amp; node tests/40-coordinator.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-coordinator-tour-guard.test.js` PASSES (was RED in plan 01).
    - `node tests/40-coordinator.test.js` still PASSES (no regression to existing precedence/one-per-session/demo behavior).
    - `grep -n 'Tour' assets/attention-coordinator.js` shows the guard uses `window.Tour.isActive()` (typeof-guarded) inside `run()`; the guard is the ONLY new tour check (no per-surface tour flags added).
    - The guard returns before the PRECEDENCE loop and does not call the session-marker setter.
    - `node -c assets/attention-coordinator.js` passes.
  </acceptance_criteria>
  <done>run() suppresses every governed surface while the tour is active via one typeof-guarded check, without burning the session marker; tour-guard test green, no coordinator regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tour runtime → coordinator arbitration | an active tour must be able to suppress competing governed surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Denial of Service (UX integrity) | coordinator run() over an active tour | low | mitigate | One typeof-guarded `window.Tour.isActive()` early-return suppresses all governed surfaces during a tour; non-claiming so it cannot permanently starve surfaces (marker not burned). Guarded by T-42-V12. |
| T-42-08 | Tampering | window.Tour stub/absence | low | accept | The guard is typeof-guarded — a missing/partial window.Tour degrades to prior behavior (no crash); worst case a surface shows during a tour (the pre-existing behavior), no security impact. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-coordinator-tour-guard.test.js` green; `node tests/40-coordinator.test.js` still green (no regression). Guard is one typeof-guarded check in run(), non-claiming.
</verification>

<success_criteria>
- One coordinator-level guard suppresses ALL governed surfaces (incl. the What's-New popup) while the tour is active, without burning the session marker; test-green, no per-popup checks, no coordinator regressions.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-06-SUMMARY.md` when done.
</output>
