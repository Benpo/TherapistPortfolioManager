---
phase: 42-in-app-changelog-what-s-new
plan: 11
type: execute
wave: 3
depends_on: [04, 07, 09]
files_modified:
  - assets/changelog-content-en.js
autonomous: false
requirements: [CHLG-02, CHLG-03, CHLG-04]
user_setup: []
must_haves:
  truths:
    - "Every changelog entry's BODY copy (v1.0 origin line, v1.1, consolidated v1.2, self-hosted v1.3) has passed the D-03 wording pipeline AND Ben's explicit approval before ship (D-04)"
    - "The v1.3 entry reads as the first, self-hosting changelog entry — proof the pipeline works on its own release notes (CHLG-04)"
    - "Copy is benefit-led, register-filtered (only user-feelable changes), calm no-emoji garden voice; the v1.0 line is a warm one-line origin marker"
  artifacts:
    - "assets/changelog-content-en.js (final approved BODY copy replacing the plan-04 placeholders)"
  key_links:
    - "Copy is drafted from PROJECT.md Validated ledger + the v1.2.4 users draft + milestone archives (D-03) - git only for dates; the v1.2.4 draft is UNAPPROVED source material"
    - "Ben approves ALL copy (backfill v1.1->v1.3 AND the v1.3 entry) rendered on the real /changelog page + popup before it is considered final"
---

<objective>
Draft the final changelog entry BODY copy through the D-03 wording pipeline, get Ben's explicit approval (D-04), and land the approved copy into `assets/changelog-content-en.js` (replacing plan 04's structurally-valid placeholders). This closes CHLG-04's self-hosting proof with real, approved prose.

Purpose: The v1.2.4 draft and PROJECT.md ledger are SOURCE material, never approved copy. Like the Phase 40 welcome-copy and Phase 41 tour-copy gates, all entry copy passes factual -> native-speaker -> DNA/voice gates AND Ben before ship. v1.3 ships (push) only after Phase 43, so this approval is against the roadmap-scoped v1.3 entry; GATE-04 re-checks it at the ship moment.
Output: approved EN entry copy in `changelog-content-en.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-CONTEXT.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/SessionsGarden-DNA-EN.md
@.claude/context/2026-07-06_changelog-v1.2.4-users.md
@assets/changelog-content-en.js
</context>

## Artifacts this phase produces (shared inventory)

Finalized (THIS plan): the BODY copy inside `assets/changelog-content-en.js` (schema + placeholders from plan 04; versions/dates/anchors already final). Copy scope: v1.3 lede+highlights+categories (self-hosting), v1.2 consolidated lede+highlights+categories (D-02), v1.1 lede+highlights+categories, v1.0 one-line origin lede (D-01). No structural change to the file's schema.

<tasks>

<task type="auto">
  <name>Task 1: Draft all entry copy through the D-03 wording pipeline (factual -> native-speaker -> DNA/voice)</name>
  <files>assets/changelog-content-en.js</files>
  <read_first>
    - .planning/PROJECT.md (Requirements/Validated ledger - per-version shipped-feature lists: v1.0/v1.1/v1.2)
    - .claude/context/2026-07-06_changelog-v1.2.4-users.md (Ben's UNAPPROVED v1.2.4 users draft - register/content seed only)
    - .planning/SessionsGarden-DNA-EN.md (voice brief for all entry copy)
    - .planning/phases/42-in-app-changelog-what-s-new/42-CONTEXT.md (D-01 backfill shape, D-02 v1.2 consolidation, D-03 sourcing+filters, D-10 no-emoji, specifics: v1.0 tone, per-version lede, privacy-reassurance register)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md ("Verified backfill facts" - the per-version content sources)
  </read_first>
  <action>
    Rewrite the BODY strings in each entry of `window.CHANGELOG_CONTENT_EN` (leave version/anchor/date structure untouched). Apply the D-03 pipeline: (1) FACTUAL - ground every bullet in PROJECT.md's Validated ledger + the v1.2.4 draft + milestone archives; apply the REGISTER FILTER so only user-feelable changes survive (v1.2 internal test/refactor work becomes at most a reliability-grade bullet like install reliability on Safari; no bugfix-log energy). (2) NATIVE-SPEAKER - natural EN prose, category items as nouns. (3) DNA/VOICE - calm, warm, non-technical garden register; benefit-led one-sentence lede per version; privacy-reassurance energy where fitting. Per version: v1.3 drafted NOW from the locked roadmap/REQUIREMENTS scope (D-12; GATE-04 re-checks at the ship moment) - help center, welcome, guided tour, this changelog, mobile hint - with 2-4 hand-picked highlights; v1.2 ONE consolidated story (date engine/Personalize, custom Session Format, filters/sorting, Safari install reliability, DB self-recovery); v1.1 (PDF export, snippets, encrypted backups, backup reminders); v1.0 a single warm origin line (no bullets). NO emojis (D-10). Keep highlights length 2-4 and categories subset {new,improved,fixed}. Mark the header comment: copy DRAFTED via D-03 pipeline, PENDING Ben's D-04 approval (Task 2).
  </action>
  <verify>
    <automated>node tests/42-changelog-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/42-changelog-integrity.test.js still PASSES (structure intact: highlights 2-4, categories subset, v1.0 origin-only, first==1.3.0, no emoji).
    - grep for emoji code points in assets/changelog-content-en.js returns nothing.
    - Every content entry's lede + bullets are grounded in the read_first sources (no invented features); v1.2 is a single consolidated story (no per-patch log).
    - Header comment flags copy as pending D-04 approval.
  </acceptance_criteria>
  <done>All entry copy is drafted through the D-03 pipeline, register-filtered, no emoji, structure-test green, and clearly marked pending Ben's approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ben approves ALL entry copy (backfill v1.1->v1.3 AND the v1.3 entry) - D-04 gate</name>
  <files>none (human approval gate — no files changed in this task)</files>
  <action>Halt for Ben's D-04 approval of the drafted changelog copy. Present the entries as rendered on the real /changelog page + What's-New popup, then either lock the copy (on "approved") or collect specific per-entry wording edits for Task 3. This is a blocking human checkpoint — do not proceed to Task 3 without Ben's response.</action>
  <what-built>
    The full changelog EN copy is drafted and rendering on the real surfaces: the /changelog page (all four version entries, reverse-chronological, Variant B) and the What's-New popup (v1.3 highlights). This is the D-04 copy-approval gate - identical in spirit to the Phase 40 welcome-copy and Phase 41 tour-copy approvals. The v1.2.4 draft and PROJECT.md ledger were SOURCE material only; nothing here was previously approved by Ben.
  </what-built>
  <how-to-verify>
    1. Open the changelog page locally (or read the rendered entries) and review each version's lede + New/Improved/Fixed bullets: v1.3 (help center, welcome, guided tour, in-app changelog, mobile hint), v1.2 (the single consolidated story), v1.1 (PDF export, snippets, encrypted backups, backup reminders), v1.0 (the one-line origin marker).
    2. Trigger/preview the What's-New popup for v1.3 and confirm the 2-4 highlights read as the right teaser.
    3. Check the register: calm, benefit-led, no dev jargon, no emojis, privacy-reassurance tone; v1.2 reads as one story (not a patch log); v1.0 is a warm origin line.
    4. Approve as-is, or list specific wording changes per entry.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the copy, or provide the specific per-entry wording edits to apply in Task 3.</resume-signal>
  <verify><human-check>Ben has reviewed all four entries + the popup teaser and either approved or supplied per-entry edits.</human-check></verify>
  <done>Ben has explicitly approved the copy (or provided exact edits) — the D-04 gate is cleared and Task 3 may proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Ben's approved wording and lock the copy</name>
  <files>assets/changelog-content-en.js</files>
  <read_first>
    - assets/changelog-content-en.js (the drafted entries from Task 1)
    - Task 2 resume-signal (Ben's approval or specific edits)
  </read_first>
  <action>
    Apply Ben's Task-2 feedback verbatim: if "approved" as-is, update the header comment to record D-04 APPROVED (date + "approved by Ben") and remove the "pending approval" marker. If edits were given, apply them exactly per entry (implement Ben's literal wording - do not substitute a cleverer phrasing), then mark APPROVED. Do not alter version/anchor/date structure. Re-run the integrity test.
  </action>
  <verify>
    <automated>node tests/42-changelog-integrity.test.js &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Header comment records D-04 APPROVED (no lingering "pending approval").
    - node tests/42-changelog-integrity.test.js passes; npm test (full suite) passes.
    - grep for emoji code points returns nothing.
    - Ben's literal edits (if any) are applied exactly.
  </acceptance_criteria>
  <done>The changelog copy is Ben-approved (D-04) and locked; integrity + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array -> rendered DOM | the finalized entry strings render into the page + popup (textContent-only, mitigation owned by plans 05/07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Tampering | changelog-content-en.js copy hygiene | low | mitigate | Integrity test re-enforces no-emoji + structure after the copy swap; consumers render every string via textContent so no markup executes. |
| T-42-10 | Repudiation (content accuracy) | unapproved copy shipping | low | mitigate | The D-04 blocking human checkpoint ensures no entry copy ships without Ben's explicit approval; GATE-04 (Phase 43) re-checks the v1.3 entry at the ship moment. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (RESEARCH audit N/A). |
</threat_model>

<verification>
Integrity test green after both the draft and the approved swap; full suite green at lock; emoji grep clean; header records D-04 approval. This plan is autonomous:false - Task 2 blocks on Ben.
</verification>

<success_criteria>
- All changelog entry copy passes the D-03 pipeline AND Ben's D-04 approval; v1.3 self-hosts as the first entry; copy locked with integrity + full suite green.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-11-SUMMARY.md` when done.
</output>
