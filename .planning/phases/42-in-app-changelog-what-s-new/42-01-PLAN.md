---
phase: 42-in-app-changelog-what-s-new
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/42-whats-new-gating.test.js
  - tests/42-whats-new-dismiss.test.js
  - tests/42-coordinator-tour-guard.test.js
autonomous: true
requirements: [CHLG-01]
must_haves:
  truths:
    - "Behavior tests for the What's-New popup exist and FAIL RED (no whats-new.js yet) — proving they exercise real behavior, not vacuous truths"
    - "Gating, first-run suppression, silent-skip reconcile, deliberate-dismiss, and popup content/a11y are each pinned by an executable assertion before any implementation runs"
    - "The coordinator's tour-active suppression guard is pinned RED: while window.Tour.isActive() is true, run() shows NO governed surface and does NOT burn the one-per-session marker"
  artifacts:
    - tests/42-whats-new-gating.test.js
    - tests/42-whats-new-dismiss.test.js
    - tests/42-coordinator-tour-guard.test.js
  key_links:
    - "Tests reach the surface via AttentionCoordinator._getSurface('whats-new') (attention-coordinator.js:502) after eval'ing whats-new.js in the jsdom builder"
    - "Fixtures seed window.AppVersion.APP_VERSION and window.CHANGELOG_CONTENT_EN so tests are independent of the real data file (plan 04)"
    - "The tour-guard test stubs window.Tour.isActive() and asserts run() (attention-coordinator.js:92) shows nothing — the guard is coordinator-level, protecting ALL six PRECEDENCE surfaces, not a per-popup check"
---

<objective>
Author the three RED behavior-test files that pin (a) the What's-New popup contract (CHLG-01) and (b) the coordinator-level tour-active suppression guard, BEFORE implementation. These are Wave 0 scaffolds: they MUST fail now (no `assets/whats-new.js`; no tour guard in the coordinator yet) and turn green when plan 05 ships the surface and plan 06 adds the guard.

Purpose: Behavior-tests-before-implementation is a project hard rule (memory `feedback-behavior-verification.md`). Grep gates verify shape, not behavior — these jsdom tests verify the coordinator surface actually gates/dismisses/renders correctly AND that `run()` suppresses every governed surface while the onboarding tour is active (fixes the captured "backup prompt fires over active tour" class of bug at the coordinator level).
Output: `tests/42-whats-new-gating.test.js` (T-42-V1/V2/V3), `tests/42-whats-new-dismiss.test.js` (T-42-V4/V5), and `tests/42-coordinator-tour-guard.test.js` (T-42-V12), auto-discovered by `tests/run-all.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@tests/40-coordinator.test.js
@tests/40-welcome-overlay.test.js
@assets/attention-coordinator.js
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js` (→ `window.CHANGELOG_CONTENT_EN`), `assets/whats-new.js` (coordinator surface id `'whats-new'`), `assets/changelog.js` (→ test seam `window.Changelog`), `changelog.html` (route `/changelog`), `assets/changelog.css`, and 7 `tests/42-*.test.js`.
Modified: `assets/app.css` (`.whats-new-*` popup CSS — the chosen always-loaded popup CSS home), `assets/app.js` (`whatsNew.menuRow` row + demo filter), `assets/shared-chrome.js` (footer version-text link), `help.html` (rail link + 2 scripts), `sw.js` (two-array precache), 7 chrome pages (2 scripts each), `assets/i18n-{en,he,de,cs}.js` (10 keys).
Globals: `window.CHANGELOG_CONTENT_EN`, `window.Changelog`. Coordinator surface id: `'whats-new'` (PRECEDENCE[1], pre-reserved).
New i18n keys: `changelog.page.title`, `changelog.page.intro`, `changelog.cat.new`, `changelog.cat.improved`, `changelog.cat.fixed`, `whatsNew.title`, `whatsNew.sub`, `whatsNew.seeAll`, `whatsNew.close`, `whatsNew.menuRow`.
Entry schema fields: `version`, `anchor`, `date`, `lede`, `highlights[2-4]`, `categories{new?,improved?,fixed?}`, `origin?`.
Storage: reuses existing `sg.whatsNewLastSeenVersion` (no new keys); written on every popup dismiss path + D-07 silent-skip reconcile.

<tasks>

<task type="auto">
  <name>Task 1: RED gating test — tests/42-whats-new-gating.test.js (T-42-V1/V2/V3)</name>
  <files>tests/42-whats-new-gating.test.js</files>
  <read_first>
    - tests/40-coordinator.test.js (the jsdom builder to reuse: sets up window, evals attention-coordinator.js, seeds globals; reach surfaces via `AttentionCoordinator._getSurface(id)`)
    - assets/attention-coordinator.js (lines 47 PRECEDENCE, 51 WHATS_NEW_LAST_SEEN const, 78-105 register/run, 227-231 welcome subsume-write, 502 _getSurface)
    - .planning/phases/42-in-app-changelog-what-s-new/42-VALIDATION.md (T-42-V1/V2/V3 rows)
  </read_first>
  <action>
    Mirror `tests/40-coordinator.test.js`'s jsdom harness. Build a fresh jsdom window per case, eval `assets/attention-coordinator.js` then `assets/whats-new.js`, seed `window.AppVersion = { APP_VERSION: '1.3.0' }` and `window.CHANGELOG_CONTENT_EN` fixtures, obtain the surface via `AttentionCoordinator._getSurface('whats-new')`.
    Assert three behaviors keyed on the exact storage key string `sg.whatsNewLastSeenVersion` (do NOT invent a new key):
    - T-42-V1 (once-per-version gating): with a fixture entry for `1.3.0` present and `sg.whatsNewLastSeenVersion` unset (or != `1.3.0`), `surface.eligible()` returns true; when `sg.whatsNewLastSeenVersion === '1.3.0'`, `eligible()` returns false.
    - T-42-V2 (first-ever-launch suppression): fresh state (no `sg.welcomeSeen`, no `sg.whatsNewLastSeenVersion`) → `AttentionCoordinator.run()` shows `welcome` (PRECEDENCE[0]) not `whats-new`; after simulating welcome dismiss (localStorage `sg.whatsNewLastSeenVersion` set to APP_VERSION per attention-coordinator.js:227-231), whats-new `eligible()` is false.
    - T-42-V3 (silent-skip reconcile, D-07): APP_VERSION differs from lastSeen AND no fixture entry exists for APP_VERSION → `eligible()` false, `show` is NEVER invoked by `run()`, AND after eval `sg.whatsNewLastSeenVersion` has been advanced to APP_VERSION by the reconcile step.
    These reference `assets/whats-new.js`, which does not exist yet — the file MUST throw/fail on load or the assertions MUST fail. That is the intended RED state.
  </action>
  <verify>
    <automated>node tests/42-whats-new-gating.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-whats-new-gating.test.js` exits NON-zero now (RED — no whats-new.js), and the failure is an assertion/load failure, not a syntax error in the test.
    - Test source contains three distinct assertions mapped to T-42-V1, T-42-V2, T-42-V3 (grep the file for the three test labels).
    - Test uses `AttentionCoordinator._getSurface('whats-new')` and the literal storage key `sg.whatsNewLastSeenVersion` (no fabricated key).
    - `node -c tests/42-whats-new-gating.test.js` passes (valid JS; RED is behavioral, not a parse error).
  </acceptance_criteria>
  <done>The gating test exists, is syntactically valid, fails RED for the right reason (missing implementation), and pins V1/V2/V3.</done>
</task>

<task type="auto">
  <name>Task 2: RED dismiss/content test — tests/42-whats-new-dismiss.test.js (T-42-V4/V5)</name>
  <files>tests/42-whats-new-dismiss.test.js</files>
  <read_first>
    - tests/40-welcome-overlay.test.js (dismiss + a11y assertion patterns to mirror: role/aria-modal, focus, keydown)
    - assets/attention-coordinator.js (lines 126-254 welcome overlay mount/dismiss idiom, 201-218 keydown focus-trap + Escape, 220-232 dismiss writes keys)
    - .planning/phases/42-in-app-changelog-what-s-new/42-UI-SPEC.md (Interaction Contract → What's-New popup: dismiss D-09, a11y, content 2-4 highlights)
  </read_first>
  <action>
    Mirror `tests/40-welcome-overlay.test.js`. Eval `attention-coordinator.js` + `whats-new.js` in jsdom, seed `window.AppVersion.APP_VERSION='1.3.0'` and a `window.CHANGELOG_CONTENT_EN` fixture whose latest entry has 3 `highlights`. Call the surface's `show()` and assert:
    - T-42-V5 (content + a11y): the mounted overlay contains a `[role="dialog"]` with `aria-modal="true"` and `aria-labelledby` pointing at the headline; the headline text includes the APP_VERSION (`1.3.0` / major-minor form); the body renders each of the 2-4 fixture `highlights` as `textContent` (assert exact strings appear); focus is moved into the dialog on mount.
    - T-42-V4 (deliberate-dismiss, D-09): a simulated backdrop/outside click leaves the popup mounted AND `sg.whatsNewLastSeenVersion` unchanged; each of Close click, Escape keydown, and the "See everything new" CTA activation removes the overlay AND writes `sg.whatsNewLastSeenVersion === APP_VERSION`. Assert NO overlay-click dismiss handler exists (backdrop is a no-op).
    References `assets/whats-new.js` (absent) → RED.
  </action>
  <verify>
    <automated>node tests/42-whats-new-dismiss.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before impl)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-whats-new-dismiss.test.js` exits NON-zero now (RED for missing implementation), not a parse error.
    - Test asserts backdrop-click is a no-op (popup stays, lastSeen unchanged) AND all three deliberate paths (Close, Esc, "See everything new") remove the overlay and write `sg.whatsNewLastSeenVersion=APP_VERSION`.
    - Test asserts `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, focus-into-dialog, and that 2-4 highlight strings render as text.
    - `node -c tests/42-whats-new-dismiss.test.js` passes.
  </acceptance_criteria>
  <done>The dismiss/content test exists, is valid JS, fails RED for the right reason, and pins V4/V5 including the backdrop-no-op guard.</done>
</task>

<task type="auto">
  <name>Task 3: RED coordinator tour-guard test — tests/42-coordinator-tour-guard.test.js (T-42-V12)</name>
  <files>tests/42-coordinator-tour-guard.test.js</files>
  <read_first>
    - tests/40-coordinator.test.js (the jsdom builder to reuse: eval attention-coordinator.js, register surfaces, call run())
    - assets/attention-coordinator.js (lines 82-105 isDemo + run(): the two existing non-claiming early returns at 93-94 and the "session marker claimed ONLY when a surface shows" contract at 88-92; PRECEDENCE 47)
    - assets/tour.js (lines 37, 657, 673, 702 — window.Tour.isActive() is the active-run signal)
  </read_first>
  <action>
    Mirror the `tests/40-coordinator.test.js` harness. Eval `attention-coordinator.js` in jsdom, register at least one always-eligible governed surface (or seed state so `welcome`/`whats-new` would be eligible), and drive `AttentionCoordinator.run()` under two conditions using a stub `window.Tour = { isActive: function(){ return <bool>; } }`:
    - Tour ACTIVE (`isActive()` returns true): `run()` shows NO surface (no eligible surface's `show` is invoked) — assert a spy on `show` is never called and no overlay is mounted.
    - Tour INACTIVE (`isActive()` returns false, or `window.Tour` absent): `run()` shows the top-eligible surface as normal (control case, proves the guard is specific to the active-tour condition).
    - No-marker-burn: after a suppressed `run()` (tour active), the one-per-session marker `sg.surfaceShownThisSession` is NOT set, so a later `run()` once the tour is inactive still shows the eligible surface.
    This references the not-yet-added guard in `attention-coordinator.js run()` → RED (today run() ignores window.Tour and would show a surface even with isActive()===true).
  </action>
  <verify>
    <automated>node tests/42-coordinator-tour-guard.test.js; test $? -ne 0 &amp;&amp; echo "RED-OK (expected failure before guard)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-coordinator-tour-guard.test.js` exits NON-zero now (RED — coordinator has no tour guard yet), not a parse error (`node -c` passes).
    - Test asserts: tour active → run() invokes no surface show; tour inactive → run() shows the eligible surface; suppressed run() does NOT set `sg.surfaceShownThisSession` (marker not burned).
    - Test stubs `window.Tour.isActive()` (the real signal, tour.js:657) rather than any per-surface flag — the guard is coordinator-level.
  </acceptance_criteria>
  <done>Tour-guard test exists, valid JS, fails RED for the missing coordinator guard, pins the run()-suppression-during-tour behavior (T-42-V12) including the marker-not-burned invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array → rendered DOM | changelog/popup entry strings render into the DOM — the only injection surface this phase adds |
| localStorage `sg.whatsNewLastSeenVersion` → app | untrusted stored flag governs popup show/suppress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | popup render path (whats-new.js) | medium | mitigate | These RED tests assert 2-4 highlight strings render as `textContent`; the actual mitigation lands in plan 05 (createElement+textContent only, help.js:22-24 contract). No `innerHTML` with content. |
| T-42-02 | Tampering/Spoofing | localStorage sg.whatsNewLastSeenVersion | low | accept | Non-sensitive UX flag; worst case re-show/suppress one popup. Reads/writes wrapped in try/catch (attention-coordinator lsGet/lsSet). No security impact. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced this phase (RESEARCH Package Legitimacy Audit: N/A; test files use the already-installed jsdom devDependency). No install tasks. |
</threat_model>

<verification>
All three files fail RED now for the right reason (missing `assets/whats-new.js`; missing coordinator tour guard), are valid JS, and are auto-discovered by `tests/run-all.js`. Plan 05 turns the popup tests green; plan 06 turns the tour-guard test green.
</verification>

<success_criteria>
- `tests/42-whats-new-gating.test.js`, `tests/42-whats-new-dismiss.test.js`, and `tests/42-coordinator-tour-guard.test.js` exist, are valid JS, and exit non-zero (RED).
- Popup assertions map to T-42-V1..V5 with the exact `sg.whatsNewLastSeenVersion` key and the `_getSurface('whats-new')` seam.
- The tour-guard assertion (T-42-V12) stubs `window.Tour.isActive()` and pins run()-suppression + the marker-not-burned invariant.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-01-SUMMARY.md` when done.
</output>
