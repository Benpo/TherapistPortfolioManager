---
phase: 42-in-app-changelog-what-s-new
plan: 04
type: execute
wave: 1
depends_on: [02]
files_modified:
  - assets/changelog-content-en.js
autonomous: true
requirements: [CHLG-03, CHLG-04]
must_haves:
  truths:
    - "A single window.CHANGELOG_CONTENT_EN array exists that both the popup (latest highlights) and the page (full history) will read — no second version constant, no git scraping"
    - "The array is reverse-chronological with the v1.3 entry first (self-hosting), a consolidated v1.2 entry, a v1.1 entry, and a v1.0 origin-only marker"
    - "The data-integrity test T-42-V9 passes on structure (highlights 2-4, categories subset, v1.0 origin-only, latest=1.3.0, zero emoji)"
  artifacts:
    - assets/changelog-content-en.js
  key_links:
    - "window.CHANGELOG_CONTENT_EN is the ONE source both assets/whats-new.js (plan 05) and assets/changelog.js (plan 06) consume"
    - "Entry versions match window.AppVersion.APP_VERSION semantics (version.js:27 = '1.3.0'); dates sourced from git/PROJECT.md only (D-03)"
---

<objective>
Create the single structured data source `assets/changelog-content-en.js` (`window.CHANGELOG_CONTENT_EN`) — schema + header-comment contract + STRUCTURALLY-VALID PLACEHOLDER copy for all four entries (v1.3, v1.2, v1.1, v1.0 origin). This satisfies CHLG-03 (one source) and the CHLG-04 self-hosting SHAPE. Final approved BODY copy lands in plan 10 (Ben's D-04 approval).

Purpose: Both surfaces read this one array; the popup reads the latest `highlights`, the page reads the full history. Locking the schema now unblocks plans 05 and 06 and turns the integrity test (T-42-V9) green on structure.
Output: `assets/changelog-content-en.js` with a documented schema and four entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md
@.planning/phases/42-in-app-changelog-what-s-new/42-PATTERNS.md
@.planning/phases/42-in-app-changelog-what-s-new/42-CONTEXT.md
@assets/help-content-en.js
@assets/version.js
</context>

## Artifacts this phase produces (shared inventory)

New files: `assets/changelog-content-en.js` (→ `window.CHANGELOG_CONTENT_EN`), `assets/whats-new.js`, `assets/changelog.js` (→ `window.Changelog`), `changelog.html` (route `/changelog`), `assets/changelog.css`, 7 `tests/42-*.test.js`. Modified: `assets/app.css` (`.whats-new-*` popup CSS), `assets/app.js`, `assets/shared-chrome.js`, `help.html`, `sw.js`, 7 chrome pages, `assets/i18n-{en,he,de,cs}.js`.
Entry schema fields (THIS plan defines them): `version` (unique semver), `anchor` (kebab, e.g. `v1-3`), `date` (human string, git-sourced), `lede` (one benefit-led sentence), `highlights` (2-4 hand-picked strings — the popup source + Phase-43 hook), `categories` ({`new`?,`improved`?,`fixed`?} — empty categories omitted), `origin` (optional boolean for v1.0). Array is reverse-chronological. Storage: reuses `sg.whatsNewLastSeenVersion` (defined elsewhere).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assets/changelog-content-en.js (schema + 4 placeholder entries)</name>
  <files>assets/changelog-content-en.js</files>
  <read_first>
    - assets/help-content-en.js (lines 1-46 header-comment schema-doc style + terminology/no-emoji note; lines 47-50 IIFE + window.HELP_CONTENT_EN registration idiom to mirror)
    - .planning/phases/42-in-app-changelog-what-s-new/42-RESEARCH.md (## Entry Schema — exact field contract; "Verified backfill facts" for dates/versions)
    - .planning/phases/42-in-app-changelog-what-s-new/42-CONTEXT.md (D-01 backfill shape, D-02 v1.2 consolidation, D-08 highlights, D-10 no-emoji, D-11 Variant B)
    - tests/42-changelog-integrity.test.js (the RED contract this file must satisfy — plan 02)
  </read_first>
  <behavior>
    - `window.CHANGELOG_CONTENT_EN` is a reverse-chronological array: [v1.3, v1.2, v1.1, v1.0].
    - First element `version==='1.3.0'`, `anchor==='v1-3'`, non-empty `lede`, `highlights` length 2-4, `categories` with `new`/`improved` (and `fixed` if applicable) as non-empty string arrays.
    - v1.2 entry consolidates v1.2.1-v1.2.4 into ONE story (D-02), dated `'July 2026'` (final release date, PROJECT.md 2026-07-07), with `new`/`improved`/`fixed` categories.
    - v1.1 entry dated `'June 2026'` (PROJECT.md 2026-06-22).
    - v1.0 entry is `{ version:'1.0.0', anchor:'v1-0', date:'May 2026', lede:<one warm origin line>, origin:true }` with NO highlights/categories.
    - No emoji code point anywhere.
  </behavior>
  <action>
    Mirror `assets/help-content-en.js`: an IIFE with `"use strict"`, a documented header comment block ("── Schema (enforced by tests/42-changelog-integrity.test.js) ──" listing every field), then assign `window.CHANGELOG_CONTENT_EN` a reverse-chronological array of the four entries per RESEARCH ## Entry Schema.
    Populate STRUCTURALLY-VALID PLACEHOLDER copy: short, factual, register-filtered seed strings sourced from PROJECT.md's Validated ledger (v1.1: PDF export, snippets, encrypted backups, backup reminders; v1.2: date engine/Personalize tab, Session Format custom, filters/sorting, Safari install reliability, DB self-recovery; v1.3: help center, welcome, guided tour, this changelog, mobile hint) and the v1.2.4 users draft — enough to satisfy every T-42-V9 assertion (non-empty lede, highlights len 2-4, categories subset non-empty, no emoji). Mark the file with a header comment line: copy is DRAFT/PLACEHOLDER pending the D-03 wording pipeline + D-04 Ben approval (finalized in plan 10). Version numbers/dates/anchors are FINAL now (not placeholder). Do NOT read the version from anything but the semver in each entry / `AppVersion.APP_VERSION` semantics — never `INTEGRITY_TOKEN` or a second constant.
    Do NOT add any emoji. Keep entry/category register as nouns (New/Improved/Fixed rendered by the page from i18n keys, not baked into data).
  </action>
  <verify>
    <automated>node tests/42-changelog-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42-changelog-integrity.test.js` PASSES (was RED in plan 02).
    - `window.CHANGELOG_CONTENT_EN` is reverse-chronological; first element `version==='1.3.0'` with `anchor==='v1-3'` and `highlights` length 2-4.
    - v1.2 is a single consolidated entry dated `'July 2026'`; v1.1 dated `'June 2026'`; v1.0 is `origin:true` with no highlights/categories.
    - `grep -nE '[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]' assets/changelog-content-en.js` returns nothing (no emoji, D-10).
    - Header comment documents the schema (mirrors help-content-en.js:1-46) AND flags copy as DRAFT pending D-04.
    - `node -c assets/changelog-content-en.js` passes.
  </acceptance_criteria>
  <done>The single data source exists, is schema-valid, integrity test green, versions/dates/anchors final, body copy clearly marked placeholder for plan 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client data array → rendered DOM | this file IS the data array; its strings render into popup + page DOM downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Tampering | changelog-content-en.js content hygiene | low | mitigate | Integrity test (T-42-V9) enforces the D-10 no-emoji contract + structure at author time; consumers (plans 05/06) render every string via textContent, so no markup can execute even if a string contained HTML. |
| T-42-06 | Spoofing | version source | low | mitigate | Entry versions are plain semver strings matching AppVersion.APP_VERSION; the file never reads the SW/integrity token layer (anti-pattern) — single source of truth preserved (CHLG-03). |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages introduced (static JS data module; RESEARCH audit N/A). |
</threat_model>

<verification>
`node tests/42-changelog-integrity.test.js` green; emoji grep clean; file is valid JS and self-registers `window.CHANGELOG_CONTENT_EN`.
</verification>

<success_criteria>
- One structured source exists (CHLG-03); v1.3 is the first entry (CHLG-04 shape); integrity test passes; body copy marked draft pending plan 10.
</success_criteria>

<output>
Create `.planning/phases/42-in-app-changelog-what-s-new/42-04-SUMMARY.md` when done.
</output>
