---
phase: 32-readme-code-comments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/settings.js
  - assets/add-session.js
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "settings.js carries a rewritten orientation banner reflecting its post-refactor SLIMMED shape — it explicitly notes that the Snippets section and the Photos tab were extracted OUT into their own modules — following the responsibility-banner convention: owns · public surface · window.* deps · invariants (DOCS-02, D-09, D-10, D-10b)"
    - "add-session.js gains a BRAND-NEW top-of-file header banner (it currently has none) covering: what it owns, its public surface, that it boots the export-modal handshake at load, its window.* dependencies, and its invariants (DOCS-02, D-09, D-10b)"
    - "All build-history archaeology is removed from both files — header titles and inline reference tags are rewritten into plain prose that preserves the explanation and drops the historical reference (D-10a)"
    - "The edits are provably comments-only: the existing suite stays green (npm test) AND a deterministic comments-only strip-and-compare shows zero code lines changed across both files (D-11)"
    - "Together with plan 02 these complete the comment pilot establishing the reusable convention + comments-only-diff process for a later batch-2 phase (D-12)"
  artifacts:
    - path: assets/settings.js
      provides: "Slimmed-parent orientation banner (de-phase + rewrite to post-extraction shape; keep the JSDoc style + security/innerHTML invariant)"
    - path: assets/add-session.js
      provides: "Brand-new header banner from scratch + body de-phase"
  key_links:
    - from: "add-session.js new banner"
      to: "assets/export-modal.js banner (the convention template) + the window.__exportModalInit handshake add-session boots"
      via: "the banner documents that add-session calls the export-modal init handshake once at boot"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-02 (part B — the 2 slimmed parents) — Apply the comment pilot to `assets/settings.js` and `assets/add-session.js`: remove build-history archaeology (D-10a) and give each a responsibility/structure banner (D-10b), scoped to exactly these files (D-09). settings.js gets its existing JSDoc banner rewritten to reflect its post-refactor slimmed shape (Snippets + Photos extracted out); add-session.js — currently header-less — gets a brand-new banner. Verify comment-only with a green suite plus a deterministic comments-only strip-and-compare (D-11). With plan 02, this completes the pilot that makes a batch-2 phase templated (D-12).

Purpose: the two parents are where the prior refactor's extraction is least self-evident from the code; an accurate slimmed-shape banner (and a first-ever banner for add-session) is the highest-orientation-value part of the pilot for a future agent.
Output: 2 comment-only edited files; no new files; no new code symbols; no behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-code-comments/32-CONTEXT.md
@.planning/phases/32-readme-code-comments/32-RESEARCH.md
@.planning/phases/32-readme-code-comments/32-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- No new file paths. Both targets already exist; this plan edits comments only (add-session.js gains a header banner but no new code symbols).
- No new `window.*` globals and no new exports — comment/header text only.

<tasks>

<!-- The broadened de-phase verify gate below greps for these build-history literals; they also appear in the de-phase <action> dialect lists as tokens to REMOVE. Exempt them from the comment-text-discipline gate (#429): -->
<!-- planner-discipline-allow: UAT -->
<!-- planner-discipline-allow: g7p -->
<!-- planner-discipline-allow: code-review -->

<task type="auto">
  <name>Task 1: settings.js — de-phase + rewrite the banner to the slimmed shape</name>
  <files>assets/settings.js</files>
  <read_first>
    - assets/settings.js (lines 1-13 are the current JSDoc header; keep the JSDoc /** */ style and the security/innerHTML invariant block ~lines 9-12)
    - assets/export-modal.js (lines 1-19 — the four-slot banner content model)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"DOCS-02 before-state" row for settings.js — ~24 refs; §"The comments-only-diff gate")
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the settings.js slimmed-parent guidance: note Snippets + Photos were extracted out; stay in JSDoc style)
    - .planning/codebase/ARCHITECTURE.md (the cross-window.* chain settings.js reads — for the "dependencies" slot)
  </read_first>
  <action>
    In assets/settings.js, rewrite the JSDoc header banner (keep the /** */ JSDoc style — do not switch to // lines) AND do a FULL CLEAN de-phase of the whole file. Scan EVERY comment and header line (the JSDoc header, line comments, JSDoc `*` continuations, AND trailing comments on code lines). Convert ANY build-history *process* reference into plain what-it-does prose — keep the real explanation and any design rationale, remove ONLY the historical tag. Do NOT rely on a pre-catalogued list (the RESEARCH before-state count is a floor; this file carries ~38 refs across many dialects — controller-title number prefix, T-/D- task IDs, Gap tags, UAT-<x> notes, a WR-<n>/code-review note, an #issue ref, and dated SPEC/amendment notes).

    Build-history *process* dialects to strip (the gate regex below was tuned against the 5 live files this revision): phase/plan tags (Phase <n> Plan <n>, Phase <n>-<n>); decision/task IDs (D-<n>, T-<n>-<n>-<n>, RFCT-<n>); acceptance-test tags (UAT and its UAT-<x> / post-UAT variants); gap tags (Gap <n>, Gap <letter>); round/change tags (Round-<n>, Change <x>); code-review / work-ref tags (WR-<n>, dated code-review notes, #<issue>); codenames (the g7p test-hook, Quick-<code>); and any ISO date (YYYY-MM-DD) inside a comment.

    PRESERVE (these are NOT build-history — living-doc traceability + design rationale; the gate does NOT flag them): the SECURITY/innerHTML rationale stays verbatim in meaning (the "user-controlled text → never innerHTML" explanation survives, only its mitigation tag is dropped); bare requirement IDs (REQ-<n>, OBS-<n>) and UI-SPEC references stay (e.g. keep "OBS-01 surfacing" and "disable-only per SPEC REQ-2" — strip only the attached `(Phase X Plan Y)` / dated-amendment parenthetical).

    Update the banner to reflect the file's post-refactor SLIMMED shape: state explicitly that the Snippets section now lives in settings-snippets.js and the Photos tab now lives in settings-photos.js (extracted out), and describe what settings.js still owns (the section rows + save flow + the peer-broadcast on save). Cover the four slots — owns · public surface · window.* deps · invariants. Touch comment text only — zero code lines.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|\bg7p\b|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/settings.js && echo DEPHASE_CLEAN</automated>
    <automated>grep -qi 'settings-snippets' assets/settings.js && grep -qi 'settings-photos' assets/settings.js && echo EXTRACTION_NOTED</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The broadened archaeology grep over `assets/settings.js` returns empty — ZERO matches across all build-history dialects (phase/plan, D-/T-/RFCT-, UAT, Gap, Round/Change, WR-, g7p, Quick, code-review, ISO dates, #issue). The regex was tuned against the live files this revision and verified NOT to match bare REQ-<n> / UI-SPEC / OBS-<n>.
    - The banner names both `settings-snippets` and `settings-photos` as the extracted-out modules (grep passes) — proving the slimmed-shape rewrite (D-10b).
    - The banner stays in JSDoc /** */ style and retains the security/innerHTML invariant (manual).
    - Reviewer confirms no dated or coded build-process reference (UAT-Dx, Gap N, WR-NN, Round-N, Change X, g7p, Quick-NNN, dated fix-notes) remains in any comment; living-doc references (bare REQ-NN, UI-SPEC, OBS-NN) may remain as reworded traceability (manual).
    - Comments-only: covered by Task 3's strip-and-compare gate (this file included).
  </acceptance_criteria>
  <done>settings.js carries a JSDoc orientation banner reflecting its slimmed post-extraction shape with ALL build-history process references removed across every dialect (the security rationale + living-doc REQ/OBS references preserved); suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: add-session.js — add a brand-new header banner + de-phase the body</name>
  <files>assets/add-session.js</files>
  <read_first>
    - assets/add-session.js (currently starts at code `let clientCache = [];` — NO header at all; the first comment is an inline block ~line 8; this file has ~29 build-history refs in the body including a trailing comment on a code line ~line 104)
    - assets/export-modal.js (lines 1-19 — build the new banner in this four-slot shape; note add-session boots window.__exportModalInit(ctx) at load)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"DOCS-02 before-state" row for add-session.js; Pitfall 1 — trailing-comment de-phasing on code lines; §"The comments-only-diff gate")
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the add-session.js new-banner guidance + the trailing-comment caution)
    - .planning/codebase/ARCHITECTURE.md (the cross-window.* chain add-session reads — for the "dependencies" slot)
  </read_first>
  <action>
    In assets/add-session.js, add a BRAND-NEW top-of-file header banner (the file has none today). Build it in the four-slot convention shape: what add-session owns (the add/edit-session form, client cache, inline photo capture, the session reading/edit modes); its public surface; that it boots the export-modal handshake once at load (it calls window.__exportModalInit(ctx), passing live accessor closures for its mutable session state plus the shared DOM elements — this pairs with the export-modal banner); its window.* dependencies (the App.*, PortfolioDB.*, Snippets.*/SNIPPETS_SEED, I18N chain it reads); and its key invariants. Match the file's plain // comment style. The new banner must itself be free of build-history tokens.

    Then do a FULL CLEAN de-phase of the body using the SAME build-history dialect list and the SAME PRESERVE rules spelled out in Task 1 (scan EVERY comment + JSDoc `*` + trailing comment; convert each build-history process reference to plain what-it-does prose keeping the explanation and any design rationale; do NOT rely on a pre-catalogued list — the RESEARCH before-state ~30-ref count is a floor). This file's refs span phase/plan tags (incl. Phase <n>-<n> and "GAP fix"), D-<n>, T-<n>-<n>-<n>, the g7p test-hook, Quick-<code> codenames, and Gap-<letter> tags. PRESERVE the living-doc references: keep bare REQ-<n> / OBS-<n> and UI-SPEC (e.g. keep "REQ-3, REQ-5" and "Section visibility" — strip only the attached dated `amended YYYY-MM-DD` parenthetical).

    Pay special attention to TRAILING comments on code lines (Pitfall 1) — e.g. the snapshot comment on the lastSavedSnapshot declaration and the inline `// D-04` re-translate note — rewrite only the comment text, never the code. The strip-and-compare gate (Task 3) is what proves comments-only, not a line-prefix check. Touch comment text only — zero code lines.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|/\*|^[[:space:]]*\*).*((Phase|Plan)[[:space:]]+[0-9]|\bD-[0-9]|\bT-[0-9]+-[0-9]|RFCT-[0-9]|Quick[-[:space:]]+[0-9]|UAT|\bGap[[:space:]]+([0-9N]|[A-Z]\b)|\bWR-[0-9]|[Rr]ound-[0-9]|Change[[:space:]]+[A-Z0-9]\b|\bg7p\b|[0-9]{4}-[0-9]{2}-[0-9]{2}|code-review|#[0-9])' assets/add-session.js && echo DEPHASE_CLEAN</automated>
    <automated>head -3 assets/add-session.js | grep -qE '^\s*//' && echo HAS_BANNER</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `assets/add-session.js` now opens with a // comment banner (head grep passes) — it previously started with code.
    - The broadened archaeology grep over `assets/add-session.js` returns empty — ZERO matches across all build-history dialects (phase/plan, D-/T-/RFCT-, UAT, Gap, Round/Change, WR-, g7p, Quick, code-review, ISO dates, #issue), including the trailing-comment cases (Pitfall 1). The regex was tuned against the live files this revision and verified NOT to match bare REQ-<n> / UI-SPEC / OBS-<n>.
    - The new banner covers the four slots and documents that add-session boots the export-modal handshake at load (manual).
    - Reviewer confirms no dated or coded build-process reference (UAT-Dx, Gap N, WR-NN, Round-N, Change X, g7p, Quick-NNN, dated fix-notes) remains in any comment; living-doc references (bare REQ-NN, UI-SPEC, OBS-NN) may remain as reworded traceability (manual).
    - Comments-only: covered by Task 3's strip-and-compare gate (this file included).
  </acceptance_criteria>
  <done>add-session.js has a brand-new four-slot header banner and a fully de-phased body with ALL build-history process references removed across every dialect (explanations + living-doc REQ/OBS references preserved); suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 2-file edit is comments-only</name>
  <files>assets/settings.js, assets/add-session.js</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"The comments-only-diff gate" reference implementation + §"Validation Architecture" soundness argument)
    - .planning/phases/32-readme-code-comments/32-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the two files this plan touched changed comments only. Run the authoritative strip-and-compare with the comment edits still UNCOMMITTED, so the baseline is git HEAD (the pre-edit committed version): for each file, strip block comments, line comments, and trailing comments and collapse whitespace from BOTH the HEAD version (git show HEAD:file) and the working-tree version, then assert byte-equality. Any inequality means a code token changed and the gate FAILS. The check is a one-shot scratchpad Node script (not a committed test). Also confirm the full suite is green. Only commit the comment edits once both gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');let bad=0;for(const f of ['assets/settings.js','assets/add-session.js']){const a=strip(cp.execSync('git show HEAD:'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across both files (D-11), run with the edits uncommitted against git HEAD.
    - If any file prints `CODE CHANGED`, a code line was modified — revert that change before committing.
  </acceptance_criteria>
  <done>The 2-file comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to two existing browser-tier IIFE/controller modules. No new runtime code, network calls, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Tampering | comment edits silently altering code in settings.js / add-session.js | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green npm test prove no behavior change (D-11); edits isolated in their own commit(s). add-session.js is the largest target (the new banner is additive comment text only). |

Honest framing: comment-only edits add no attack surface. ASVS V5/V6/V7 controls are untouched; the only risk is an accidental code edit, caught mechanically by the D-11 gate.
</threat_model>

<verification>
- `npm test` green after the edits.
- Broadened archaeology grep over both files returns empty across EVERY build-history dialect (phase/plan, D-/T-/RFCT-, UAT, Gap, Round/Change, WR-, g7p, Quick, code-review, ISO dates, #issue) — full de-phasing complete (D-10a); the gate was tuned against the live files this revision and does not flag bare REQ-<n>/UI-SPEC/OBS-<n>.
- settings.js banner names both extracted modules (slimmed-shape rewrite); add-session.js now opens with a banner.
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-11).
</verification>

<success_criteria>
settings.js carries an accurate slimmed-shape orientation banner and add-session.js gains a brand-new four-slot banner; both are fully de-phased; the edits are provably comments-only; the pilot convention is complete for the batch-2 phase.
</success_criteria>

<output>
Create `.planning/phases/32-readme-code-comments/32-03-SUMMARY.md` when done.
</output>
