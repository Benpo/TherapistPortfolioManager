---
phase: 32-readme-code-comments
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md
  - .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "A comment-coverage map artifact exists at .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md flagging EVERY production JS module as done or remaining, with the 5 pilot files marked done and db.js / overview.js / sessions.js flagged as batch-1 for the follow-up comments phase (D-14)"
    - "A help-content inventory artifact exists at .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md — an EN-only topic/workflow tree where each leaf is a title + one-line intent mapped to a real feature/page and tagged {persona source, P26 status, suggested format, priority}, INVENTORY ONLY (not help copy) (D-13)"
    - "The help inventory is grounded in the CURRENT live app (the real pages + refreshed codebase maps + REQUIREMENTS) and the P26 7-step workflow spine, and EXCLUDES demo.html / demo-hints.js as a topic source because the demo is stale (D-13)"
    - "Both artifacts live in .planning/ and seed future phases (coverage map → comments batch-2; help inventory → the future help/onboarding phase); neither is shipped"
  artifacts:
    - path: .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md
      provides: "Per-module done/remaining comment-coverage table with batch-1 flagged"
      contains: "batch-1"
    - path: .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md
      provides: "EN-only help topic/workflow tree (inventory only) with per-leaf tags"
  key_links:
    - from: "32-COMMENT-COVERAGE-MAP.md"
      to: "32-RESEARCH.md §Comment-Coverage Map (D-14) table"
      via: "the per-module table is pre-built in RESEARCH — lift it and mark statuses"
    - from: "32-HELP-CONTENT-INVENTORY.md auditor lens"
      to: "the live app pages + the P26 7-step workflow spine"
      via: "every real feature/page gets a topic; demo excluded as a source"
---

<objective>
Produce the two lightweight by-product planning artifacts that fall out of the doc work, both in `.planning/`: the comment-coverage map (D-14) that seeds a future comments batch-2 phase, and the help-content inventory (D-13) that extends the P26 workflow spine and seeds a future help/onboarding phase. Both are INVENTORY/MAP artifacts, not shipped product and not help copy.

Purpose: make the next two phases plannable. The coverage map turns "which files still need comments?" into a templated batch-2 plan; the help inventory turns "what does the user need help with?" into a grounded topic tree a future help phase can write copy against.
Output: two new markdown files under the phase dir. No code; no shipped artifacts; no new code symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-code-comments/32-CONTEXT.md
@.planning/phases/32-readme-code-comments/32-RESEARCH.md
@.planning/phases/32-readme-code-comments/32-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- NEW file: `.planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md`
- NEW file: `.planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md`
- Both are `.planning/` planning artifacts — never shipped (the deploy pipeline's "Verify no sensitive files" step blocks `.planning/`). No code symbols.

<tasks>

<task type="auto">
  <name>Task 1: Comment-coverage map (D-14)</name>
  <files>.planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"Comment-Coverage Map (D-14)" — the per-module table is ALREADY built; lift it)
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the coverage-map analog: copy the table, mark statuses, follow STRUCTURE.md table-with-status style)
    - .planning/codebase/STRUCTURE.md (the table-with-status presentation style)
  </read_first>
  <action>
    Create .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md. Lift the per-module table from RESEARCH §"Comment-Coverage Map (D-14)" (Module · Lines · DOCS-02 status, ~27 modules, excluding the *.min.js vendor files and the i18n dictionaries). Mark the 5 pilot files (export-modal.js, settings-snippets.js, settings-photos.js, settings.js, add-session.js) as done (this phase). Flag db.js, overview.js, and sessions.js as batch-1 for the follow-up comments phase (freshly touched in the prior refactor → lowest staleness); mark everything else remaining. Note which remaining modules currently lack any file header (a new banner will be needed) per the RESEARCH header scan. Add a one-line intro stating the map seeds the comments batch-2 phase and that the batch-2 convention is the pilot convention from this phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && grep -qi 'batch-1' .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && grep -q 'db.js' .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && grep -q 'overview.js' .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && grep -q 'sessions.js' .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && grep -qi 'done' .planning/phases/32-readme-code-comments/32-COMMENT-COVERAGE-MAP.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at the path above.
    - It contains a per-module table covering the production JS modules with a done/remaining status column.
    - The 5 pilot files are marked done.
    - db.js, overview.js, sessions.js are present and flagged as batch-1 (greps pass).
    - Modules lacking a current header are noted (manual).
  </acceptance_criteria>
  <done>32-COMMENT-COVERAGE-MAP.md lists every production JS module with done/remaining status, the 5 pilot files done, and db.js/overview.js/sessions.js as batch-1 — a ready seed for the comments batch-2 phase.</done>
</task>

<task type="auto">
  <name>Task 2: Help-content inventory (D-13)</name>
  <files>.planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"Help-Content Inventory (D-13) — grounded method": ~6 agents = 4 persona lenses + 1 feature-coverage auditor + 1 synthesizer; the live page list; the output schema; the demo exclusion; Pitfall 5)
    - .planning/phases/32-readme-code-comments/32-CONTEXT.md (D-13 full decision: persona attribution, inventory-only, EN-only, demo excluded)
    - .planning/milestones/v1.1-phases/26-in-app-onboarding-overview-help-system/26-UI-SPEC.md (the P26 standalone Help page + the ~7-step workflow spine the inventory extends + the domain-correct copywriting contract)
    - .planning/REQUIREMENTS.md (the feature set the auditor maps topics against)
    - .planning/codebase/STRUCTURE.md (the live pages + module responsibilities — the auditor's grounding; also the table/tree presentation style)
  </read_first>
  <action>
    Create .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md as an EN-only topic/workflow tree — INVENTORY ONLY, not help copy.

    Method (RESEARCH-grounded; exact agent count/prompts are Claude's Discretion — ~6 total): run four persona lenses (struggling novice / "stupid user", trainer-onboarder, power user, domain expert Sapir-like) — each proposes the topics/questions its persona needs; one grounded feature-coverage auditor that walks the live app pages (index, sessions, add-client, add-session, settings, reporting, report, landing — confirm the set with `ls *.html`) and ensures every real feature has a topic; and one synthesizer that dedupes persona + auditor output into a single tree. You MAY run the lenses as Task sub-agents or inline as structured reasoning.

    Ground ONLY in the current live app (the real pages + the refreshed .planning/codebase/*.md + REQUIREMENTS) and the P26 7-step workflow spine; organize the tree along that workflow spine. You MAY harvest live guidance signals (empty-state strings, greeting quotes). EXCLUDE demo.html and demo-hints.js as a topic source — the demo is stale (D-13, Pitfall 5); its rendering pattern may inform a future tour build but never the topic list.

    Each leaf carries: title + one-line intent + mapped feature/page + tags {persona source, P26 status, suggested format (tour step | FAQ | page section), priority}. Do NOT write help copy — that is the future help phase's job. Add a one-line intro stating the artifact extends the P26 spine and seeds the future help/onboarding phase, EN-only, demo excluded.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md && grep -qiE 'persona' .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md && grep -qiE 'priority' .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md && grep -qiE 'intent' .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md && ! grep -qi 'demo-hints' .planning/phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at the path above.
    - It is a topic/workflow tree organized along the P26 7-step spine, EN-only.
    - Each leaf has a title, a one-line intent, a mapped feature/page, and the four tags {persona source, P26 status, suggested format, priority} (greps for persona/priority/intent pass; full tag set verified manually).
    - It is inventory only — no help copy paragraphs (manual).
    - It does NOT cite demo.html / demo-hints.js as a topic source (negative grep on `demo-hints` passes; demo is excluded — D-13).
    - Every live app page's real features are represented (manual cross-check against `ls *.html`).
  </acceptance_criteria>
  <done>32-HELP-CONTENT-INVENTORY.md is a grounded EN-only topic tree (inventory only) covering the live app along the P26 spine, with per-leaf tags and the demo excluded — a ready seed for the future help phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Two `.planning/` markdown planning artifacts. Never shipped (the deploy "Verify no sensitive files" step blocks `.planning/`). No app-tier impact, no runtime code, no network calls, no data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-40 | (correctness, not security) | help inventory grounded in the stale demo | low | mitigate | Exclude demo.html / demo-hints.js as a source (D-13, Pitfall 5); ground only in the live pages + P26 spine. This is an accuracy guardrail, not an attack-surface mitigation. |

Honest framing: these are internal planning documents with no security surface. No ASVS controls apply; nothing reaches production. The only listed item is a correctness guardrail (don't seed from stale UI).
</threat_model>

<verification>
- Both artifacts exist at their `.planning/` paths.
- Coverage map: per-module done/remaining table, 5 pilot files done, db.js/overview.js/sessions.js as batch-1 (greps pass).
- Help inventory: EN-only topic tree along the P26 spine, per-leaf tags present, demo excluded (greps pass + manual tag/coverage review).
</verification>

<success_criteria>
The comment-coverage map and the help-content inventory both exist under the phase dir, are grounded as specified (D-14 / D-13), and are ready to seed the comments batch-2 phase and the future help/onboarding phase respectively.
</success_criteria>

<output>
Create `.planning/phases/32-readme-code-comments/32-04-SUMMARY.md` when done.
</output>
