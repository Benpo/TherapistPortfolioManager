---
phase: 32-readme-code-comments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/export-modal.js
  - assets/settings-snippets.js
  - assets/settings-photos.js
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "Each of the 3 extracted modules (export-modal.js, settings-snippets.js, settings-photos.js) carries a plain what-it-does header following the responsibility-banner convention — what it owns · its public surface (the window.* it registers + any handshake) · its dependencies (the cross-window.* chain it reads) · its key invariants (DOCS-02, D-09, D-10b)"
    - "All build-history archaeology is removed from these 3 files — header titles and inline reference tags are rewritten into plain prose that preserves the explanation and drops the historical reference (D-10a)"
    - "The edits are provably comments-only: the existing suite stays green (npm test) AND a deterministic comments-only strip-and-compare shows zero code lines changed across the 3 files (D-11)"
    - "These edits, together with plan 03, are the comment pilot establishing the reusable comment convention + comments-only-diff process for a later batch-2 phase (D-12)"
  artifacts:
    - path: assets/export-modal.js
      provides: "Extracted export-modal module banner (light de-phase only; header is the convention template)"
    - path: assets/settings-snippets.js
      provides: "Snippet-settings module banner (de-phase title + refs; keep the cross-IIFE chain block + security note)"
    - path: assets/settings-photos.js
      provides: "Photos-settings module banner (heaviest inline de-phase)"
  key_links:
    - from: "the 3 banners"
      to: "assets/export-modal.js lines 1-19 (the convention template)"
      via: "all banners copy the four-slot shape: owns · public surface · window.* deps · invariants"
    - from: "the comments-only guarantee"
      to: "the pre-edit committed version of each file (git HEAD before the comment edits)"
      via: "deterministic strip-and-compare (comments+whitespace stripped, assert byte-equal) + green npm test"
---

<objective>
DOCS-02 (part A — the 3 extracted modules) — Apply the comment pilot to `assets/export-modal.js`, `assets/settings-snippets.js`, and `assets/settings-photos.js`: remove build-history archaeology from their comments (D-10a) and ensure each opens with a responsibility/structure banner following the convention (D-10b), scoped to exactly these files (D-09). Verify the work is comment-only with a green suite plus a deterministic comments-only strip-and-compare (D-11). This plus plan 03 is the pilot that makes a later batch-2 phase templated and repeatable (D-12).

Purpose: end the comment-cleanup drag deliberately with a tight, reviewable pilot whose convention transfers — not a risky all-files sweep. The export-modal header is already the gold-standard template; the other two have rich headers that mainly need de-phasing.
Output: 3 comment-only edited files; no new files; no new code symbols; no behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-readme-code-comments/32-CONTEXT.md
@.planning/phases/32-readme-code-comments/32-RESEARCH.md
@.planning/phases/32-readme-code-comments/32-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- No new file paths. All three targets already exist; this plan edits comments only.
- No new code symbols, no new `window.*` globals, no new exports — comment/header text only.

<tasks>

<task type="auto">
  <name>Task 1: export-modal.js — light de-phase (the convention template)</name>
  <files>assets/export-modal.js</files>
  <read_first>
    - assets/export-modal.js (lines 1-19 are the gold-standard banner; the only job is removing the two refactor reference tags)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"DOCS-02 before-state" row for export-modal.js — 2 refs to remove; §"The comments-only-diff gate")
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the export-modal banner template excerpt + the de-phasing pattern)
  </read_first>
  <action>
    In assets/export-modal.js, the header is already the clean convention template — do NOT rewrite it. Make only the light de-phase: remove the two build-history reference tags (the refactor identifiers catalogued for this file in the RESEARCH before-state table), rewriting the surrounding sentence into plain prose that keeps the meaning (the behavior-preserving closure-extraction explanation and the ctx-handshake description stay). Touch comment text only — no code lines. Do not commit yet if you want the comments-only gate (Task 3) to compare against the pre-edit HEAD; otherwise capture the pre-edit ref first.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|^[[:space:]]*\*).*((Phase|Plan) [0-9]|D-[0-9]|T-[0-9]+-[0-9]|RFCT-[0-9]|Quick [0-9])' assets/export-modal.js && echo DEPHASE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (suite stays green).
    - The archaeology grep over `assets/export-modal.js` returns empty (no phase/plan numbers and no decision/task/refactor/quick-task reference tags remain in comments).
    - The header still describes: what the module owns, its public surface (the single private window handshake it adds), its window.* dependencies, and its invariants (manual: banner quality check).
    - Comments-only: covered by Task 3's strip-and-compare gate (this file included).
  </acceptance_criteria>
  <done>export-modal.js carries the same gold-standard banner with all build-history reference tags removed; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: settings-snippets.js + settings-photos.js — de-phase headers and inline archaeology</name>
  <files>assets/settings-snippets.js, assets/settings-photos.js</files>
  <read_first>
    - assets/settings-snippets.js (its header lines 1-22 are already the right shape; the cross-IIFE chain block ~lines 9-18 and the security note must be PRESERVED — they are the deps + invariants slots)
    - assets/settings-photos.js (its header lines 1-24 are rich and the right shape; this file has the heaviest inline archaeology of the five — the inline decision tags describe real behavior and must be converted to prose, not deleted)
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"DOCS-02 before-state" rows for both files with the ref counts; §"The comments-only-diff gate")
    - .planning/phases/32-readme-code-comments/32-PATTERNS.md (the per-file de-phase guidance + the cross-IIFE chain block to keep)
  </read_first>
  <action>
    In assets/settings-snippets.js: rewrite the build-history prefix in the header title into a plain what-it-does title; remove the stray document/source-line reference tags called out for this file in the RESEARCH before-state table; KEEP the cross-IIFE identifier-resolution chain block (the model for the "dependencies" slot in all banners) and KEEP the security note (the "invariants" slot). Result: a clean four-slot banner — owns · public surface · window.* deps · invariants — with no build-history references.

    In assets/settings-photos.js: rewrite the build-history prefix in the header title into plain prose; convert ALL inline historical reference tags (the bracketed decision identifiers and the mid-body post-UAT-fix note catalogued for this file in the RESEARCH before-state table) into plain prose that PRESERVES the behavior explanation and DROPS the reference — do not just delete the surrounding sentence. Ensure the resulting header covers the four slots. This is the heaviest de-phase of the pilot; watch for trailing/mid-body comments on code lines (they will not start with a comment marker — the strip-and-compare gate in Task 3 is what proves comments-only, not a line-prefix check).

    Touch comment text only in both files — zero code lines.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>! grep -nE '(//|^[[:space:]]*\*).*((Phase|Plan) [0-9]|D-[0-9]|T-[0-9]+-[0-9]|RFCT-[0-9]|Quick [0-9])' assets/settings-snippets.js assets/settings-photos.js && echo DEPHASE_CLEAN</automated>
    <automated>grep -q 'Cross-IIFE' assets/settings-snippets.js && echo CHAIN_PRESERVED</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The archaeology grep over both files returns empty (no phase/plan numbers, no decision/task/refactor/quick-task reference tags in comments).
    - settings-snippets.js still contains the cross-IIFE resolution-chain block (grep `Cross-IIFE` passes) and the security note (manual).
    - settings-photos.js inline historical notes are converted to behavior prose, not deleted wholesale (manual: the explanations survive, only the reference tags are gone).
    - Each file opens with a four-slot banner: owns · public surface · window.* deps · invariants (manual).
    - Comments-only: covered by Task 3's strip-and-compare gate (both files included).
  </acceptance_criteria>
  <done>Both files carry clean four-slot banners with no build-history references; the snippets cross-IIFE chain + security note are preserved; the photos inline notes are converted to prose; suite green; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the 3-file edit is comments-only</name>
  <files>assets/export-modal.js, assets/settings-snippets.js, assets/settings-photos.js</files>
  <read_first>
    - .planning/phases/32-readme-code-comments/32-RESEARCH.md (§"The comments-only-diff gate" reference implementation + §"Validation Architecture" soundness argument — deterministic strip-and-compare is sound even with an imperfect stripper)
    - .planning/phases/32-readme-code-comments/32-VALIDATION.md (Per-Task Verification Map: comments-only strip-and-compare = zero code lines changed)
    - tests/25-08-single-source-audit.test.js (the fs.readFileSync + git-show source-audit style this check mirrors)
  </read_first>
  <action>
    Prove the three files this plan touched changed comments only. Run the authoritative strip-and-compare with the comment edits still UNCOMMITTED, so the baseline is simply git HEAD (the pre-edit committed version): for each file, strip block comments, line comments, and trailing comments and collapse whitespace from BOTH the HEAD version (git show HEAD:file) and the working-tree version, then assert byte-equality. Any inequality means a code token changed and the gate FAILS. The check is a one-shot scratchpad Node script (not a committed test — reusable gate tooling is a batch-2 concern), modeled on the project source-audit test style. Also confirm the full suite is green as the behavior backstop. Only commit the comment edits once both gates pass.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'),fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'').replace(/\s+/g,'');let bad=0;for(const f of ['assets/export-modal.js','assets/settings-snippets.js','assets/settings-photos.js']){const a=strip(cp.execSync('git show HEAD:'+f).toString());const b=strip(fs.readFileSync(f,'utf8'));if(a!==b){console.error('CODE CHANGED: '+f);bad=1;}}if(!bad)console.log('COMMENTS_ONLY_OK');process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - The strip-and-compare prints `COMMENTS_ONLY_OK` and exits 0 — zero code lines changed across all three files (D-11), run with the edits uncommitted against git HEAD.
    - If any file prints `CODE CHANGED`, a code line was modified — revert that change before committing.
  </acceptance_criteria>
  <done>The 3-file comment edit is provably comments-only (strip-and-compare green + suite green); edits committed in their own isolated commit(s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edits to existing browser-tier IIFE modules. No new runtime code, network calls, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Tampering | comment edits silently altering code in the 3 IIFE modules | low | mitigate | Comments-only strip-and-compare gate (zero code lines changed) + green npm test prove no behavior change (D-11); edits isolated in their own commit(s). |

Honest framing: comment-only edits add no attack surface. ASVS V5/V6/V7 controls are untouched; the only risk is an accidental code edit, which the D-11 gate catches mechanically.
</threat_model>

<verification>
- `npm test` green after the edits.
- Archaeology grep over all 3 files returns empty (de-phasing complete — D-10a).
- settings-snippets.js retains the cross-IIFE chain block + security note.
- Comments-only strip-and-compare prints `COMMENTS_ONLY_OK` (zero code lines changed — D-11).
</verification>

<success_criteria>
The 3 extracted modules carry clean four-slot responsibility banners with no build-history archaeology; the edits are provably comments-only (strip-and-compare + green suite); the comment convention is demonstrated for the batch-2 phase.
</success_criteria>

<output>
Create `.planning/phases/32-readme-code-comments/32-02-SUMMARY.md` when done.
</output>
