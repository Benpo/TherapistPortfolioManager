---
phase: 24-pre-launch-final-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 03    # Plan 03 owns add-session.js, add-session.html, i18n-*.js in Wave 2
  - 06    # Plan 06 owns add-session.js, add-session.html, i18n-*.js in Wave 3
files_modified:
  - assets/db.js
  - assets/app.js
  - assets/add-session.js
  - add-session.html
  - assets/backup.js
  - sw.js
  - assets/i18n-en.js
  - assets/i18n-de.js
  - assets/i18n-he.js
  - assets/i18n-cs.js
  - assets/snippets-seed.js
  - assets/snippets.js
autonomous: true
requirements_addressed:
  - D-08    # IDB v5 additive migration: new snippets store
  - D-09    # Seed-pack source as JS module assets/snippets-seed.js
  - D-10    # Deterministic seed IDs (ec.<cell>.<slug>); idempotent + deletedSeedIds tracking
  - D-11    # Caret-mirror autocomplete popover (Notion-style)
  - D-13    # Trigger-time locale fallback active → en → he → de → cs
  - D-18    # App.getSnippets() cache + BroadcastChannel sync extension
  - D-19    # Backup/restore extended to include snippets store
  - D-20    # sw.js PRECACHE_URLS extended (pre-commit hook auto-bumps CACHE_NAME)
  - D-21    # Demo-mode parity — same seed in demo_portfolio and sessions_garden
  - D-33    # i18n strategy 4 locales
  - D-34    # SW precache + cache version handling
  - D-35    # Backup compat — pre-v1.1 backups restore cleanly
  # SPEC.md requirements (Item 3, REQ-1 through REQ-9):
  - REQ-1   # Snippet trigger engine
  - REQ-2   # User-configurable prefix (1–2 chars) — schema field; UI is in Plan 05
  - REQ-3   # 4-locale data shape with fallback chain
  - REQ-4   # Auto-installed seed pack of 60 snippets
  - REQ-7   # Inline autocomplete popover
  - REQ-9   # data-snippets="true" on 7 textareas
user_setup: []

must_haves:
  truths:
    - "assets/db.js DB_VERSION constant equals 5 (was 4 in Phase 22)"
    - "After openDB(), db.objectStoreNames contains 'snippets' (new store added in v5 migration)"
    - "The new snippets store has keyPath 'id' and a unique case-insensitive index on 'trigger'"
    - "On first run after v5 migration, the snippets store contains exactly 60 records sourced from assets/snippets-seed.js, each with origin='seed' and a deterministic id of the form 'ec.<col><row>.<slug>'"
    - "Re-running the migration (e.g., relaunching the app) does NOT duplicate the 60 seeds"
    - "A user-deleted seed snippet stays deleted across relaunches via a deletedSeedIds set persisted in therapistSettings (sectionKey 'snippetsDeletedSeeds')"
    - "PortfolioDB exposes getAllSnippets(), getSnippet(id), addSnippet(snippet), updateSnippet(snippet), deleteSnippet(id), resetSeedSnippet(id)"
    - "App.getSnippets() returns a cached array eagerly loaded in App.initCommon() (mirrors App.getSectionLabel pattern from Phase 22 D-09/D-10)"
    - "BroadcastChannel 'sessions-garden-settings' carries a new event type 'snippets' that triggers a refetch of App._snippetCache in peer tabs"
    - "assets/snippets.js exposes window.Snippets with public API: init(), bindTextarea(textareaEl), unbindTextarea(textareaEl), expand(textareaEl, snippet) — and is auto-bound to all textareas matching [data-snippets=\"true\"]"
    - "The snippet engine detects <prefix><keyword> + word-boundary (whitespace, Enter, .,;:!?) and replaces the matched substring with the active-locale expansion (with fallback chain active → en → he → de → cs per D-13)"
    - "Typing <prefix><partial> (≥1 char after prefix) shows a caret-anchored popover with ≤8 matches sorted by trigger; Arrow Up/Down navigate, Enter inserts with trailing space, Esc dismisses"
    - "Popover positions via caret-mirror technique: a hidden mirror div clones textarea typography + content-up-to-caret + a final span, and span.getBoundingClientRect() yields caret coords; popover uses inset-inline-start/inset-inline-end for RTL safety"
    - "All 7 session-form textareas in add-session.html (trappedEmotions, heartShieldEmotions, sessionInsights, limitingBeliefs, additionalTech, sessionComments, customerSummary) have data-snippets=\"true\" attribute"
    - "Triggers do NOT fire in client-form notes, edit-client modal notes, settings inputs, export editor (REQ-9 acceptance)"
    - "Imported / loaded snippet JSON is validated before insertion into IDB: each record must have id (string), trigger (string matching /^[a-z0-9-]{2,32}$/), expansions (object with at most 4 keys: he/en/cs/de all string), tags (array of strings only), origin ('seed'|'user'), createdAt + updatedAt (string)"
    - "Trigger detection regex is anchored, non-greedy, and ReDoS-safe: no nested quantifiers, no unbounded alternations, max trigger length capped at 32"
    - "assets/backup.js extends exportBackup manifest with snippets array; importBackup writes snippets back into IDB; pre-v1.1 backups (missing snippets key) restore without errors and the v5 migration repopulates the seed pack"
    - "sw.js PRECACHE_URLS includes /assets/snippets.js and /assets/snippets-seed.js (pre-commit hook auto-bumps CACHE_NAME on commit)"
    - "Phase 24 plans MUST NOT manually edit sw.js CACHE_NAME (pre-commit hook owns it)"
    - "i18n keys for autocomplete popover added to 4 locales: snippets.autocomplete.empty (no matches indicator) — kept minimal in this plan; the editor/settings strings are owned by Plan 05"
  artifacts:
    - path: "assets/db.js"
      provides: "DB_VERSION=5 + migration 5 creates snippets store + idempotent seed-pack populate + deletedSeedIds tracking via therapistSettings"
      contains: "const DB_VERSION = 5"
      contains: "snippets"
    - path: "assets/snippets-seed.js"
      provides: "window.SNIPPETS_SEED — array of 60 records from Sapir's PDF, 4-locale expansions, ec.<cell> tags"
      contains: "SNIPPETS_SEED"
    - path: "assets/snippets.js"
      provides: "window.Snippets — trigger engine, caret-mirror popover, locale fallback, data-snippets binding"
      contains: "window.Snippets"
    - path: "assets/app.js"
      provides: "App.getSnippets(), App._snippetCache, eager-load in initCommon, BroadcastChannel 'snippets' event handling"
      contains: "App.getSnippets"
    - path: "add-session.html"
      provides: "7 session textareas marked data-snippets=\"true\""
      contains: "data-snippets=\"true\""
    - path: "assets/backup.js"
      provides: "Extended manifest with snippets array; importBackup handles snippets restore + pre-v1.1 backward compat"
      contains: "snippets"
    - path: "sw.js"
      provides: "PRECACHE_URLS extended with /assets/snippets.js + /assets/snippets-seed.js (CACHE_NAME left for pre-commit hook)"
      contains: "/assets/snippets.js"
  key_links:
    - from: "assets/db.js openDB onupgradeneeded MIGRATIONS[5]"
      to: "assets/snippets-seed.js SNIPPETS_SEED"
      via: "synchronous read of window.SNIPPETS_SEED inside the upgrade transaction (snippets-seed.js loaded before db.js by HTML script order)"
      pattern: "SNIPPETS_SEED"
    - from: "[data-snippets=\"true\"] textarea"
      to: "window.Snippets.bindTextarea"
      via: "MutationObserver or initial-load querySelectorAll in Snippets.init()"
      pattern: "data-snippets"
    - from: "assets/backup.js exportBackup"
      to: "PortfolioDB.getAllSnippets()"
      via: "await call inside exportBackup"
      pattern: "getAllSnippets"
---

<objective>
Build the snippet quick-paste **engine + storage + seed pack + textarea wiring** (Item 3a). The Settings UI for managing snippets (list view, modal editor, tag filter, import/export) is split into Plan 05 to keep this plan within budget.

**What ships here:**
1. **IDB v5 additive migration** — new `snippets` object store + idempotent seed populate from Sapir's PDF.
2. **assets/snippets-seed.js** — JS module exporting `SNIPPETS_SEED` array of 60 records (60 emotions × 4-locale expansions × ec.<col><row> tags).
3. **assets/snippets.js** — trigger engine: input-event listener on `[data-snippets="true"]` textareas, word-boundary expansion, caret-mirror autocomplete popover with keyboard nav.
4. **App.getSnippets() cache** + BroadcastChannel snippets event type (App.initCommon eager-load).
5. **7 session textareas wired** with `data-snippets="true"` in add-session.html.
6. **assets/backup.js extended** to round-trip the snippets store; pre-v1.1 backups restore cleanly.
7. **sw.js PRECACHE_URLS extended** with the new asset files (CACHE_NAME owned by pre-commit hook).

**What does NOT ship in this plan (Plan 05's scope):**
- Settings UI list view, search input, tag filter chips, modal editor with single-lang default + "Edit translations" reveal.
- Snippet-only JSON import/export buttons + collision modal.
- Settings i18n keys for editor / list / import flow.
- The user-configurable `snippetPrefix` UI (the SCHEMA field is added in this plan with default `;`; the UI input lives in Plan 05).

Purpose: Deliver the engine + storage so Plan 05 builds on a stable IDB store and a working trigger/autocomplete engine. By splitting at this seam, each plan stays within ~50% context budget per the GSD planner rules.

Output: 60 seed snippets installed at first launch; typing `;<keyword>` + word-boundary in any of the 7 session textareas expands; caret-anchored autocomplete works in LTR + RTL; backups round-trip the data; pre-v1.1 backups restore cleanly with seed-pack repopulation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md
@.planning/phases/24-pre-launch-final-cleanup/24-SPEC.md
@.planning/phases/24-pre-launch-final-cleanup/24-03-SUMMARY.md
@.planning/phases/24-pre-launch-final-cleanup/24-06-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/22-session-workflow-loop-pre-session-context-card-editable-sess/22-02-db-migration-app-cache-i18n-PLAN.md
@.planning/todos/pending/2026-05-07_emotions-quick-paste.md
@assets/db.js
@assets/app.js
@assets/add-session.js
@add-session.html
@assets/backup.js
@sw.js

# Seed-pack content source (binary, executor must read pages for emotion names + 4-lang text):
@.planning/todos/pending/2026-05-07_emotions-quick-paste - Sapir's feedback - Text to include.pdf

<interfaces>
<!-- Existing identifiers Phase 24 Plan 04 extends. Read before editing. -->

From assets/db.js (current state — Phase 22 v4):
- `const DB_VERSION = 4` (line 4) — to be bumped to 5
- `const MIGRATIONS = { 1: ..., 2: ..., 3: ..., 4: therapistSettingsStore }` — append `5: snippetsStore`
- The migration function signature: `function migrationFn(db, transaction) { /* createObjectStore etc */ }`
- Phase 22 migration v4 example (line 247-253):
  ```
  4: function therapistSettingsStore(db) {
    if (!db.objectStoreNames.contains("therapistSettings")) {
      db.createObjectStore("therapistSettings", { keyPath: "sectionKey" });
    }
  }
  ```
- Public API for therapistSettings (line 472-498) — mirror this shape for snippets CRUD:
  ```
  setTherapistSetting(record), getAllTherapistSettings(), clearTherapistSettings()
  ```

From assets/app.js (line 38-54, 366-405):
- `let _sectionLabelCache = new Map()` — module scope cache
- `function getSectionLabel(sectionKey, defaultI18nKey)` — lookup with fallback
- `async function initCommon()` (line 366) — eager-loads `_sectionLabelCache`; opens BroadcastChannel `sessions-garden-settings`; dispatches `app:settings-changed` DOM event
- BroadcastChannel handler (line 391-401) — accepts `{ type: 'settings-changed' }` messages, refetches therapist settings
- Public API exposed at module bottom (line 985+) — append `getSnippets` to the returned object

From add-session.html (the 7 target textareas — line numbers approximate):
- `<textarea id="heartShieldEmotions" ...>` (line ~224) — inside data-section-key="heartShieldEmotions"
- `<textarea id="trappedEmotions" ...>` (line ~242)
- `<textarea id="sessionInsights" ...>` (line ~255)
- `<textarea id="limitingBeliefs" ...>` (line ~268)
- `<textarea id="additionalTech" ...>` (line ~281)
- `<textarea id="sessionComments" ...>` (line ~310)
- `<textarea id="customerSummary" ...>` (line ~323)
- All currently have `class="textarea session-textarea"` and `data-i18n-placeholder="..."` — preserve these; ADD `data-snippets="true"` as an additional attribute.

From assets/backup.js (manifest structure, line 556-571):
```
var manifest = {
  version: 2,
  exportedAt: ...,
  appVersion: "1.0",
  clients: clientsClean,
  sessions: allSessions,
  therapistSettings: allTherapistSettings,
  settings: { language: ..., theme: ... }
};
```
- importBackup normalizeManifest at line 472-498 — extend to handle missing `snippets` key gracefully
- The restore loop at line 793 (therapistSettings) — mirror this pattern for snippets

From sw.js (line 12, 19-65):
- `const CACHE_NAME = 'sessions-garden-vNNN'` — PRE-COMMIT HOOK OWNS THIS, DO NOT EDIT
- `const PRECACHE_URLS = [...]` — ADD entries here only

From .planning/phases/22-.../22-02-db-migration-app-cache-i18n-PLAN.md:
- The closest precedent for this plan's structure. Read it for the migration + cache + BroadcastChannel + i18n approach.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/snippets-seed.js with 60 records sourced from Sapir's Emotion Code PDF (D-09, D-10, REQ-4)</name>
  <read_first>
    - .planning/todos/pending/2026-05-07_emotions-quick-paste - Sapir's feedback - Text to include.pdf (read all pages — 15 pages of EC chart content)
    - .planning/phases/24-pre-launch-final-cleanup/24-SPEC.md (REQ-4 acceptance criteria + seed-pack structure)
    - .planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md (D-09, D-10 — file location, identity rule, deletedSeedIds)
  </read_first>
  <action>
    Create a new file `assets/snippets-seed.js` exporting `window.SNIPPETS_SEED` as a frozen array of exactly 60 snippet records. Each record's structure:

    ```
    {
      id: "ec.<col><row>.<english-slug>",   // deterministic; e.g. "ec.a1.betrayal"
      trigger: "<english-slug>",             // lowercase a-z0-9- only, 2-32 chars
      expansions: {
        he: "<Hebrew meaning paragraph>",    // may be empty string if PDF lacks Hebrew for this cell
        en: "<English meaning paragraph>",
        cs: "<Czech meaning paragraph>",
        de: "<German meaning paragraph>"
      },
      tags: ["ec.<col><row>"],                // e.g. ["ec.a1"]; one tag in v1 seed
      origin: "seed",
      createdAt: "2026-05-14T00:00:00.000Z", // identical across all seeds for deterministic backup diff
      updatedAt: "2026-05-14T00:00:00.000Z"
    }
    ```

    **Extraction rules from the PDF:**

    1. The PDF is structured as the Classic Emotion Code chart: Column A (rows 1–6) and Column B (rows 1–6), each cell containing 5 emotions. 12 cells × 5 emotions = 60 entries.

    2. For each emotion, extract:
       - Canonical English name (e.g., `Betrayal`) → derive `trigger` as lowercase, hyphen-joined slug (e.g., `betrayal`, `unreceived-love`, `heart-shock`). Validate against `/^[a-z0-9-]{2,32}$/`. If a translation lacks an English name, use the Hebrew slug transliterated.
       - 4-locale meaning paragraph. If a specific locale is missing in the PDF, set `expansions[locale]` to empty string — the engine's fallback chain (active → en → he → de → cs) handles missing locales at trigger time per REQ-3.
       - The cell coordinate (e.g., `a1` for Column A row 1) → tag `ec.a1`.

    3. The cell-to-organ mapping (informational only; the seed pack uses `ec.<col><row>` tags, NOT organ names):
       - ec.a1 = Heart / Small Intestine
       - ec.a2 = Spleen / Stomach
       - ec.a3 = Lung / Colon
       - ec.a4 = Liver / Gallbladder
       - ec.a5 = Kidneys / Bladder
       - ec.a6 = (per PDF; verify)
       - ec.b1 through ec.b6 = remaining cells per PDF.

    4. Construct the IDs deterministically: `id = "ec." + cellCoord + "." + trigger` (e.g., `"ec.a1.betrayal"`). This is the key the migration uses for idempotent insert (D-10).

    5. The 4 timestamps MUST be identical static strings — `"2026-05-14T00:00:00.000Z"` — so the seed pack is byte-stable across regenerations. The migration sets snippet.createdAt = snippet.updatedAt from these values, so user "modified seed" detection in the export flow (REQ-8) can compare `updatedAt > createdAt` reliably.

    6. The file structure:

       ```
       /**
        * snippets-seed.js — Sessions Garden seed snippet pack (Phase 24).
        * Source: Sapir's Emotion Code chart PDF (2026-05-07).
        * 60 emotions × 4-locale meaning paragraphs × 12 chart cells.
        * Loaded synchronously before assets/db.js by HTML script order
        * so the v5 migration can read SNIPPETS_SEED inside onupgradeneeded.
        */
       (function () {
         "use strict";
         window.SNIPPETS_SEED = Object.freeze([
           // ec.a1 — Heart / Small Intestine (5 emotions)
           { id: "ec.a1.betrayal", trigger: "betrayal", expansions: { he: "...", en: "...", cs: "...", de: "..." }, tags: ["ec.a1"], origin: "seed", createdAt: "2026-05-14T00:00:00.000Z", updatedAt: "2026-05-14T00:00:00.000Z" },
           // ... 59 more entries
         ]);
       })();
       ```

    7. Encode Hebrew strings as plain UTF-8 inside JS string literals. The file is served as UTF-8 (already configured via HTML meta charset).

    8. For PDF pages where translation is missing in a specific locale: set that locale's value to the empty string `""` — do NOT invent text. The engine's locale fallback chain (D-13, REQ-3) handles this at trigger time.

    9. Final file size target: 30–50KB. Each meaning paragraph is roughly 100–300 chars × 4 locales × 60 entries = ~60–80KB raw, ~30–50KB after gzip (CF Pages serves gzipped). If the file exceeds 60KB raw, consider tighter wording — but DO NOT truncate or paraphrase: copy Sapir's wording verbatim.

    10. Validate via script (executor runs this Node check before committing):

        ```
        node -e "
        const fs = require('fs'); const vm = require('vm');
        const ctx = { window: {} }; vm.createContext(ctx);
        vm.runInContext(fs.readFileSync('assets/snippets-seed.js','utf8'), ctx);
        const arr = ctx.window.SNIPPETS_SEED;
        if (!Array.isArray(arr) || arr.length !== 60) throw new Error('expected 60 entries, got ' + (arr && arr.length));
        const slugRe = /^[a-z0-9-]{2,32}$/;
        const cellRe = /^ec\.[ab][1-6]$/;
        const seen = new Set();
        for (const s of arr) {
          if (!slugRe.test(s.trigger)) throw new Error('bad trigger: ' + s.trigger);
          if (seen.has(s.trigger.toLowerCase())) throw new Error('duplicate trigger: ' + s.trigger);
          seen.add(s.trigger.toLowerCase());
          if (!s.id.startsWith('ec.')) throw new Error('bad id: ' + s.id);
          if (!s.tags.every(t => cellRe.test(t))) throw new Error('bad tag in ' + s.id);
          if (s.origin !== 'seed') throw new Error('bad origin: ' + s.origin);
          if (!s.expansions || typeof s.expansions !== 'object') throw new Error('bad expansions: ' + s.id);
          for (const loc of ['he','en','cs','de']) {
            if (typeof s.expansions[loc] !== 'string') throw new Error('non-string expansion ' + loc + ' in ' + s.id);
          }
          if (!s.expansions.en) console.warn('warn: empty en for ' + s.id); // fallback chain handles, but flag
        }
        console.log('OK 60 entries, unique triggers');
        "
        ```
  </action>
  <verify>
    <automated>
test -f assets/snippets-seed.js && \
node -e "
const fs = require('fs'); const vm = require('vm');
const ctx = { window: {} }; vm.createContext(ctx);
vm.runInContext(fs.readFileSync('assets/snippets-seed.js','utf8'), ctx);
const arr = ctx.window.SNIPPETS_SEED;
if (!Array.isArray(arr) || arr.length !== 60) { console.error('expected 60, got', arr && arr.length); process.exit(1); }
const slugRe = /^[a-z0-9-]{2,32}$/;
const cellRe = /^ec\.[ab][1-6]$/;
const seen = new Set();
for (const s of arr) {
  if (!slugRe.test(s.trigger)) { console.error('bad trigger:', s.trigger); process.exit(1); }
  if (seen.has(s.trigger.toLowerCase())) { console.error('dup trigger:', s.trigger); process.exit(1); }
  seen.add(s.trigger.toLowerCase());
  if (!s.id.startsWith('ec.')) { console.error('bad id:', s.id); process.exit(1); }
  if (!Array.isArray(s.tags) || !s.tags.every(t => cellRe.test(t))) { console.error('bad tags in', s.id); process.exit(1); }
  if (s.origin !== 'seed') { console.error('bad origin:', s.id); process.exit(1); }
  if (typeof s.expansions !== 'object') { console.error('bad expansions:', s.id); process.exit(1); }
  for (const loc of ['he','en','cs','de']) if (typeof s.expansions[loc] !== 'string') { console.error('non-string', loc, 'in', s.id); process.exit(1); }
}
console.log('OK');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `assets/snippets-seed.js` exists and exports `window.SNIPPETS_SEED` as a frozen Array of length exactly 60.
    - Every entry has `id` matching `/^ec\.[ab][1-6]\.[a-z0-9-]+$/`.
    - Every `trigger` matches `/^[a-z0-9-]{2,32}$/`.
    - All triggers are unique (case-insensitive).
    - Every entry has `expansions` with exactly 4 string keys: `he`, `en`, `cs`, `de` (empty strings are acceptable; non-string values are not).
    - Every entry has `origin === "seed"`.
    - Every entry has `createdAt === updatedAt === "2026-05-14T00:00:00.000Z"`.
    - Every entry's `tags` is an array of `ec.<col><row>` strings.
    - The file does NOT contain any `<script>`, `eval`, `Function(`, or `innerHTML` — it is pure data.
    - File size between 20KB and 80KB (compressed gzip likely ~10–30KB).
    - The Node validation script above prints `OK` and exits 0.
  </acceptance_criteria>
  <done>
    `assets/snippets-seed.js` is a validated 60-entry seed pack, ready for migration consumption.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump DB_VERSION to 5, add snippets store + idempotent seed migration + deletedSeedIds tracking + CRUD API in assets/db.js (D-08, D-10, D-21)</name>
  <read_first>
    - assets/db.js (entire file — pay attention to lines 1–260 for DB_VERSION + MIGRATIONS structure and lines 460–500 for therapistSettings CRUD as the precedent)
    - assets/snippets-seed.js (Task 1 output)
    - .planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md (D-08, D-10, D-21)
    - .planning/phases/22-session-workflow-loop-pre-session-context-card-editable-sess/22-02-db-migration-app-cache-i18n-PLAN.md (precedent shape)
  </read_first>
  <action>
    Modify `assets/db.js`:

    1. Bump `const DB_VERSION = 4;` to `const DB_VERSION = 5;`.

    2. Append a new migration to MIGRATIONS:

       ```
       5: function snippetsStore(db, transaction) {
         // Phase 24 D-08: additive migration — creates the snippets object store.
         // Schema: { id, trigger, expansions: {he,en,cs,de}, tags: string[], origin, createdAt, updatedAt }
         // Indexed on trigger (unique, case-insensitive via stored lowercase).
         if (!db.objectStoreNames.contains("snippets")) {
           const store = db.createObjectStore("snippets", { keyPath: "id" });
           store.createIndex("trigger", "trigger", { unique: true });
           store.createIndex("origin", "origin", { unique: false });
         }
         // Seed-pack populate happens AFTER the upgrade transaction completes (see seedSnippetsIfNeeded).
         // We don't seed inside onupgradeneeded because therapistSettings (which holds deletedSeedIds)
         // may not be readable mid-upgrade depending on the IDB implementation; defer to a post-open hook.
       }
       ```

    3. Add a new function `seedSnippetsIfNeeded(db)` that runs AFTER `openDB()` resolves on every page load. Logic:

       ```
       async function seedSnippetsIfNeeded(db) {
         // Read deletedSeedIds from therapistSettings (Phase 22 store).
         const deletedSeedIds = await readDeletedSeedIds();
         // Read existing snippet IDs (only origin === "seed") from the snippets store.
         const existingSeedIds = new Set();
         await new Promise((resolve, reject) => {
           const tx = db.transaction("snippets", "readonly");
           const store = tx.objectStore("snippets");
           const idx = store.index("origin");
           const req = idx.openCursor(IDBKeyRange.only("seed"));
           req.onsuccess = (e) => {
             const cursor = e.target.result;
             if (cursor) { existingSeedIds.add(cursor.value.id); cursor.continue(); }
             else resolve();
           };
           req.onerror = () => reject(req.error);
         });
         // For each entry in SNIPPETS_SEED, insert if it's not in deletedSeedIds AND not in existingSeedIds.
         const toInsert = window.SNIPPETS_SEED.filter(s => !deletedSeedIds.has(s.id) && !existingSeedIds.has(s.id));
         if (toInsert.length === 0) return;
         const tx = db.transaction("snippets", "readwrite");
         const store = tx.objectStore("snippets");
         for (const s of toInsert) {
           // trigger is stored lowercase for case-insensitive uniqueness via the index
           store.add({ ...s, trigger: s.trigger.toLowerCase() });
         }
         await new Promise((res, rej) => { tx.oncomplete = res; tx.onerror = () => rej(tx.error); });
       }

       async function readDeletedSeedIds() {
         try {
           const tx = (await openDB()).transaction("therapistSettings", "readonly");
           const store = tx.objectStore("therapistSettings");
           const req = store.get("snippetsDeletedSeeds");
           const rec = await new Promise((res, rej) => { req.onsuccess = () => res(req.result); req.onerror = () => rej(req.error); });
           if (!rec || !Array.isArray(rec.deletedIds)) return new Set();
           return new Set(rec.deletedIds);
         } catch (e) {
           console.warn("snippets: failed to read deletedSeedIds", e);
           return new Set();
         }
       }

       async function markSeedSnippetDeleted(seedId) {
         const tx = (await openDB()).transaction("therapistSettings", "readwrite");
         const store = tx.objectStore("therapistSettings");
         const existing = await new Promise((res, rej) => {
           const r = store.get("snippetsDeletedSeeds");
           r.onsuccess = () => res(r.result || { sectionKey: "snippetsDeletedSeeds", deletedIds: [] });
           r.onerror = () => rej(r.error);
         });
         const set = new Set(existing.deletedIds || []);
         set.add(seedId);
         existing.deletedIds = Array.from(set);
         store.put(existing);
       }

       async function unmarkSeedSnippetDeleted(seedId) {
         // Used when user "Reset to default" restores a previously-deleted seed.
         const tx = (await openDB()).transaction("therapistSettings", "readwrite");
         const store = tx.objectStore("therapistSettings");
         const existing = await new Promise((res, rej) => {
           const r = store.get("snippetsDeletedSeeds");
           r.onsuccess = () => res(r.result || { sectionKey: "snippetsDeletedSeeds", deletedIds: [] });
           r.onerror = () => rej(r.error);
         });
         existing.deletedIds = (existing.deletedIds || []).filter(id => id !== seedId);
         store.put(existing);
       }
       ```

    4. Modify `openDB()` (line ~259) so that after the migration completes and the DB is open, it calls `await seedSnippetsIfNeeded(db)` once. Use a module-scope `let _seedingDone = false;` flag to avoid re-running on every transaction.

    5. Add public API functions:

       ```
       async function getAllSnippets() {
         return withStore("snippets", "readonly", function (store) {
           return new Promise((res, rej) => {
             const req = store.getAll();
             req.onsuccess = () => res(req.result || []);
             req.onerror = () => rej(req.error);
           });
         });
       }

       async function getSnippet(id) {
         return withStore("snippets", "readonly", function (store) {
           return new Promise((res, rej) => {
             const req = store.get(id);
             req.onsuccess = () => res(req.result || null);
             req.onerror = () => rej(req.error);
           });
         });
       }

       async function addSnippet(snippet) {
         validateSnippetShape(snippet); // throw on invalid
         const now = new Date().toISOString();
         const record = { ...snippet, trigger: snippet.trigger.toLowerCase(), origin: snippet.origin || "user", createdAt: snippet.createdAt || now, updatedAt: now };
         return withStore("snippets", "readwrite", function (store) {
           return new Promise((res, rej) => {
             const req = store.add(record);
             req.onsuccess = () => res(req.result);
             req.onerror = () => rej(req.error);
           });
         });
       }

       async function updateSnippet(snippet) {
         validateSnippetShape(snippet);
         const existing = await getSnippet(snippet.id);
         if (!existing) throw new Error("snippet not found: " + snippet.id);
         const record = { ...existing, ...snippet, trigger: snippet.trigger.toLowerCase(), updatedAt: new Date().toISOString() };
         return withStore("snippets", "readwrite", function (store) {
           return new Promise((res, rej) => {
             const req = store.put(record);
             req.onsuccess = () => res(req.result);
             req.onerror = () => rej(req.error);
           });
         });
       }

       async function deleteSnippet(id) {
         const existing = await getSnippet(id);
         if (existing && existing.origin === "seed") {
           await markSeedSnippetDeleted(id);
         }
         return withStore("snippets", "readwrite", function (store) {
           return new Promise((res, rej) => {
             const req = store.delete(id);
             req.onsuccess = () => res(req.result);
             req.onerror = () => rej(req.error);
           });
         });
       }

       async function resetSeedSnippet(id) {
         // Restore a seed snippet from SNIPPETS_SEED, clearing the deletedSeedIds entry if any.
         const seed = (window.SNIPPETS_SEED || []).find(s => s.id === id);
         if (!seed) throw new Error("not a seed snippet: " + id);
         await unmarkSeedSnippetDeleted(id);
         return withStore("snippets", "readwrite", function (store) {
           return new Promise((res, rej) => {
             const req = store.put({ ...seed, trigger: seed.trigger.toLowerCase() });
             req.onsuccess = () => res(req.result);
             req.onerror = () => rej(req.error);
           });
         });
       }

       function validateSnippetShape(s) {
         // Throws Error on invalid. Used by addSnippet/updateSnippet AND by backup import path.
         if (!s || typeof s !== "object") throw new Error("snippet must be object");
         if (typeof s.id !== "string" || !s.id) throw new Error("snippet.id required string");
         if (typeof s.trigger !== "string" || !/^[a-z0-9-]{2,32}$/.test(s.trigger.toLowerCase())) throw new Error("snippet.trigger must be 2-32 chars of a-z0-9-");
         if (!s.expansions || typeof s.expansions !== "object") throw new Error("snippet.expansions required object");
         for (const loc of ["he", "en", "cs", "de"]) {
           if (loc in s.expansions && typeof s.expansions[loc] !== "string") throw new Error("snippet.expansions." + loc + " must be string");
         }
         if (s.tags != null) {
           if (!Array.isArray(s.tags) || !s.tags.every(t => typeof t === "string")) throw new Error("snippet.tags must be array of strings");
         }
         if (s.origin != null && s.origin !== "seed" && s.origin !== "user") throw new Error("snippet.origin must be 'seed' or 'user'");
       }
       ```

    6. Append the new functions to the public API object returned at module bottom: `{ ..., getAllSnippets, getSnippet, addSnippet, updateSnippet, deleteSnippet, resetSeedSnippet, validateSnippetShape }`.

    7. Update `clearAll()` (line ~456) to also `await clearStore("snippets")` so a full wipe clears the snippet store too — but this is destructive intent only (used by debug tooling); the migration re-seeds on next open.

    8. **Demo-mode parity (D-21):** the migration runs identically in demo_portfolio and sessions_garden DBs because DB_NAME selection happens BEFORE openDB and the migration code doesn't check DB_NAME. Verify by manual UAT — open demo, confirm 60 seeds appear.

    9. Add HTML script tag for `snippets-seed.js` BEFORE `db.js` on EVERY page that loads db.js. The migration reads `window.SNIPPETS_SEED`, so the seed file must be parsed before db.js opens the DB. Pages: index.html, add-client.html, add-session.html, sessions.html, reporting.html, settings.html, license.html, demo.html. Add `<script src="./assets/snippets-seed.js"></script>` right before `<script src="./assets/db.js"></script>`.

    10. The script-tag-add list is large. The executor MAY use a sed-batch helper but MUST grep verify after that EVERY page that loads db.js also loads snippets-seed.js immediately before.
  </action>
  <verify>
    <automated>
grep -q "const DB_VERSION = 5" assets/db.js && \
grep -q "snippetsStore" assets/db.js && \
grep -q "seedSnippetsIfNeeded" assets/db.js && \
grep -q "markSeedSnippetDeleted" assets/db.js && \
grep -q "function validateSnippetShape" assets/db.js && \
grep -q "getAllSnippets" assets/db.js && \
grep -q "resetSeedSnippet" assets/db.js && \
for f in index.html add-client.html add-session.html sessions.html reporting.html settings.html license.html demo.html; do
  if grep -q "src=\".*db.js\"" "$f"; then
    grep -q "snippets-seed.js" "$f" || { echo "MISSING snippets-seed.js in $f"; exit 1; }
  fi
done
    </automated>
  </verify>
  <acceptance_criteria>
    - `assets/db.js` contains exactly one `const DB_VERSION = 5` declaration.
    - `assets/db.js` MIGRATIONS object has key `5` whose function creates the `snippets` object store with keyPath `id` and unique index on `trigger`.
    - `assets/db.js` defines `seedSnippetsIfNeeded`, `readDeletedSeedIds`, `markSeedSnippetDeleted`, `unmarkSeedSnippetDeleted`, `validateSnippetShape`, `getAllSnippets`, `getSnippet`, `addSnippet`, `updateSnippet`, `deleteSnippet`, `resetSeedSnippet`.
    - `validateSnippetShape` throws on: missing id, trigger not matching `/^[a-z0-9-]{2,32}$/`, non-object expansions, non-string locale expansion, non-array tags, tag elements that are not strings, origin not in {'seed','user'}.
    - `openDB()` calls `seedSnippetsIfNeeded` exactly once per page load (idempotent via a module-scope flag).
    - Every HTML page that loads `assets/db.js` also loads `assets/snippets-seed.js` in the immediately-prior script tag (grep gate enforces).
    - Manual UAT: clear IDB via DevTools → reload app → 60 records appear in the snippets store with origin='seed'.
    - Manual UAT: delete one seed via `PortfolioDB.deleteSnippet('ec.a1.betrayal')` in DevTools console → reload → that seed does NOT come back (deletedSeedIds tracks it).
    - Manual UAT: call `PortfolioDB.resetSeedSnippet('ec.a1.betrayal')` in DevTools console → seed reappears AND deletedSeedIds is cleared.
    - Manual UAT (demo parity): visit demo mode → 60 records appear in demo_portfolio IDB.
    - No regression: openDB() still upgrades cleanly from v3 / v4 DBs; clients / sessions / therapistSettings stores intact.
  </acceptance_criteria>
  <done>
    DB v5 migration is additive, idempotent, and respects deletedSeedIds. CRUD API + validation function are exposed. Every HTML page loads snippets-seed.js before db.js.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create assets/snippets.js trigger engine + caret-mirror autocomplete popover (REQ-1, REQ-7, D-11, D-13)</name>
  <read_first>
    - assets/snippets-seed.js + assets/db.js (Tasks 1 + 2 outputs)
    - .planning/phases/24-pre-launch-final-cleanup/24-SPEC.md (REQ-1, REQ-7 acceptance criteria)
    - .planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md (D-11, D-13)
    - add-session.html (the 7 target textareas)
    - assets/app.css (existing popover / modal styles for visual consistency)
  </read_first>
  <action>
    Create `assets/snippets.js` that exposes `window.Snippets` with the inline trigger engine + caret-mirror autocomplete popover. IIFE pattern, no module imports, vanilla JS.

    **Public API:**

    ```
    window.Snippets = {
      init,                   // call from each page's DOMContentLoaded after App.initCommon
      bindTextarea(el),       // bind a single textarea (used by init + future dynamic attachment)
      unbindTextarea(el),     // teardown
      getPrefix,              // return current snippet prefix (default ';')
      setPrefix(newPrefix),   // validate + persist via PortfolioDB.setTherapistSetting (Plan 05 calls this from Settings UI)
    };
    ```

    **Implementation requirements:**

    1. **Snippet cache load.** On `init()`, call `await App.getSnippets()` (the cache wired in Task 4). Listen for `document.addEventListener("app:snippets-changed", refreshCache)` so Plan 05 edits propagate.

    2. **Prefix storage.** `getPrefix()` reads from a module-scope cache loaded eagerly on init: `await PortfolioDB.getTherapistSetting?.("snippetPrefix")` falling back to `";"`. `setPrefix(newPrefix)` validates:
       - Length 1 or 2 (REQ-2).
       - Each character must NOT match `/[\s\w'"\`<>]/` (whitespace, letter, digit, quotes, angle brackets per CONSTRAINTS in SPEC).
       - If invalid, throw `Error` with a key like `snippets.error.invalidPrefix` (executor: validation here returns an error code; Plan 05 owns the UI message).
       - Persist via `PortfolioDB.setTherapistSetting({ sectionKey: "snippetPrefix", value: newPrefix })`.
       - Update module-scope cache and broadcast via BroadcastChannel `sessions-garden-settings` with `{ type: "snippets", subtype: "prefix-changed" }`.

    3. **Textarea binding.** On `init()`, run `document.querySelectorAll("[data-snippets=\"true\"]")` and call `bindTextarea` on each. Each bind attaches:
       - `input` event listener — runs `onInput(e)` (handles expansion + autocomplete trigger detection).
       - `keydown` event listener — runs `onKeyDown(e)` (handles Arrow Up/Down/Enter/Esc when popover is open).
       - `blur` event listener — closes popover.

    4. **Trigger detection (`onInput`)** — REQ-1 acceptance:
       - Read textarea.value and selectionStart (caret position).
       - Look backward from caret to find a possible trigger: `value.slice(0, caret)` matches `/(?:^|[\s\.,;:!?\(\)\[\]\{\}])(<prefix>[a-z0-9-]+)$/i` (anchored by start-of-string OR word-boundary char, captures `<prefix><keyword>`). Use `String.prototype.lastIndexOf(prefix)` instead of a regex if simpler — but the boundary check before the prefix is mandatory (prevents `email::shortcut` from triggering on `::shortcut`).
       - If user just typed a word-boundary character (the LAST char typed is one of `[\s\n\.,;:!?]`), check the substring IMMEDIATELY before the word-boundary: extract `<prefix><keyword>` (NO partial — must end before the boundary). If `<keyword>` exactly matches a snippet's `trigger` (case-insensitive), perform expansion (see step 6). Otherwise, do not expand.
       - If user is mid-typing (last char is `a-z0-9-` and at least 1 char after prefix), open / update the autocomplete popover. Use a debounce of 0 (input event is already throttled).

    5. **Locale fallback (REQ-3, D-13):** when expanding, pick `expansions[App.getCurrentLang()]`. If empty, walk the chain `active → en → he → de → cs` and use the first non-empty value. If ALL are empty (shouldn't happen given validation), do not expand (no destructive empty replacement).

    6. **Expansion mechanic (REQ-1):**
       - Compute the substring to remove: caret position - (prefix length + keyword length) → caret position - 1 (the word-boundary char is preserved AFTER the expansion per acceptance criteria 1).
       - Wait — re-read REQ-1: "the matched <prefix><keyword> substring is replaced with the snippet text followed by the trailing space". So the word-boundary char is preserved AFTER the inserted text.
       - Use `textarea.setRangeText(replacement, start, end, "end")` to replace the matched text with the expansion, then place the caret AFTER the inserted text + the boundary char.
       - Trigger `input` event manually with `bubbles: true` so any external listener (e.g., dirty-state tracker in add-session.js) registers the change.

    7. **Caret-mirror popover positioning (D-11):**
       - Create a hidden `<div class="snippets-caret-mirror">` once at init, append to body. Copy the textarea's computed styles (font, padding, line-height, width, white-space, word-wrap, direction) onto the mirror. Set `mirror.style.visibility = "hidden"; position: absolute; top: 0; left: -9999px;` so it's offscreen but renders.
       - On each input where the popover should be shown, set `mirror.textContent = textarea.value.slice(0, caretIndex)` and append a final `<span id="snippets-caret-marker"></span>` with zero-width content.
       - Read `mirrorSpan.getBoundingClientRect()` to get caret coords relative to the mirror. Translate to textarea-absolute coords via the textarea's bounding rect + scroll offset.
       - Position the popover element with `position: fixed; top: caretY + lineHeight; inset-inline-start: caretX;` (logical property mandatory for RTL).
       - If popover would overflow viewport bottom, position above caret instead: `top: caretY - popoverHeight;`.

    8. **Popover DOM:**
       ```
       <div class="snippets-popover" id="snippetsPopover" role="listbox" aria-label="...">
         <ul class="snippets-popover-list">
           <li class="snippets-popover-item" role="option" tabindex="-1" data-snippet-id="...">
             <span class="snippets-popover-trigger">prefix+trigger</span>
             <span class="snippets-popover-preview">first 60 chars of EN expansion</span>
           </li>
           ...
         </ul>
       </div>
       ```

    9. **Keyboard nav (REQ-7):**
       - `Arrow Down` → next item, wrap to first after last.
       - `Arrow Up` → previous, wrap to last after first.
       - `Enter` → insert selected, then trailing space; close popover. preventDefault to avoid form submit.
       - `Esc` → close popover. preventDefault.
       - Tab → close popover (let Tab move focus naturally).
       - Click on item → same as Enter on that item.

    10. **Match selection (REQ-7):** filter snippets whose lowercase `trigger` starts with the typed partial keyword (case-insensitive). Sort by trigger ascending. Limit to first 8 results. Empty match → close popover.

    11. **RTL safety (REQ-7 acceptance):**
        - Popover positions via `inset-inline-start` (computed from caret bounding rect + textarea direction).
        - In a Hebrew textarea (`dir="rtl"`), caret-mirror copies `direction: rtl` and reads the span rect — the rect's `right` edge corresponds to the visual caret position in RTL, so the popover anchors to `right` instead of `left` via the logical property.

    12. **Safety constraints (security, per `<phase_specific_constraints>`):**
        - Expansion content is inserted via textarea value mutation (setRangeText) — NEVER via innerHTML. Textareas don't render HTML, so this is naturally XSS-safe.
        - The popover preview renders `expansion.slice(0, 60)` via `textContent` (NEVER innerHTML).
        - The popover trigger label renders `<prefix><trigger>` via `textContent` (NEVER innerHTML).
        - All regexes are anchored, non-greedy, with bounded quantifiers (`{2,32}`, no `.*` in trigger patterns) — ReDoS-safe.
        - Snippet data loaded from `App.getSnippets()` is already validated at IDB-write time (Task 2's `validateSnippetShape`).

    13. **CSS for the popover** — add to `assets/app.css` (a small block, ~30 LOC):
        ```
        .snippets-popover {
          position: fixed;
          z-index: var(--z-popover, 350);
          background: var(--color-surface);
          border: 1px solid var(--color-border);
          border-radius: var(--radius-sm);
          box-shadow: var(--shadow-md);
          max-block-size: 240px;
          overflow-y: auto;
          min-inline-size: 240px;
          max-inline-size: 320px;
        }
        .snippets-popover.is-hidden { display: none; }
        .snippets-popover-list { list-style: none; margin: 0; padding: 0; }
        .snippets-popover-item {
          display: flex;
          flex-direction: column;
          gap: var(--space-1);
          padding: var(--space-2) var(--space-3);
          cursor: pointer;
        }
        .snippets-popover-item.is-active,
        .snippets-popover-item:hover {
          background: var(--color-bg-hover);
        }
        .snippets-popover-trigger {
          font-family: var(--font-mono, monospace);
          font-weight: 600;
          color: var(--color-text);
        }
        .snippets-popover-preview {
          font-size: var(--font-size-xs);
          color: var(--color-text-muted);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        ```
        Use whatever tokens exist in tokens.css (substitute if names differ — the existing tokens.css is the source of truth).

    14. **File header documenting threat model:**
        ```
        /**
         * snippets.js — inline text-snippet expansion engine (Phase 24).
         *
         * Trigger: <prefix><slug> + word-boundary → replaced with expansion.
         * Popover: caret-mirror anchored, RTL-safe, ≤8 results, keyboard nav.
         *
         * Security:
         *  - Expansion content inserted into textarea.value via setRangeText (no HTML parse → XSS-safe).
         *  - Popover preview uses textContent, never innerHTML.
         *  - Trigger detection regex anchored, max trigger length 32 chars → ReDoS-safe.
         *  - Snippet data validated at IDB write (db.js validateSnippetShape).
         *  - No eval / new Function / dynamic regex from user input.
         */
        ```
  </action>
  <verify>
    <automated>
test -f assets/snippets.js && \
grep -q "window.Snippets" assets/snippets.js && \
grep -q "bindTextarea" assets/snippets.js && \
grep -q "snippets-caret-mirror" assets/snippets.js && \
grep -q "data-snippets" assets/snippets.js && \
grep -q "inset-inline-start" assets/snippets.js && \
grep -q "setRangeText" assets/snippets.js && \
! grep -q "innerHTML\s*=" assets/snippets.js && \
! grep -qE "eval\s*\(|new Function\(" assets/snippets.js && \
grep -q "snippets-popover" assets/app.css
    </automated>
  </verify>
  <acceptance_criteria>
    - `assets/snippets.js` exists and defines `window.Snippets` with public methods `init`, `bindTextarea`, `unbindTextarea`, `getPrefix`, `setPrefix`.
    - `assets/snippets.js` uses `textarea.setRangeText` (or equivalent value-only mutation) for expansion — NEVER `innerHTML` or `outerHTML`.
    - `assets/snippets.js` does NOT contain `eval(`, `new Function(`, or dynamically-constructed RegExp from user input.
    - The caret-mirror element is created at init and reused across calls (not recreated per input).
    - Popover positioning uses `inset-inline-start` / `inset-inline-end` (logical) and is verified in Hebrew RTL textareas.
    - CSS in `assets/app.css` includes `.snippets-popover` block using design tokens.
    - Manual UAT (LTR): in any of the 7 marked textareas, type `;<known-seed-trigger>` then space → text expands.
    - Manual UAT (LTR): type `;<partial>` → popover appears with up to 8 matches sorted alphabetically.
    - Manual UAT (RTL Hebrew): type `;<known-seed-trigger>` then space in a marked textarea while app is in Hebrew → expansion uses the Hebrew text from `expansions.he` (or falls back to en if he is empty).
    - Manual UAT (RTL Hebrew): popover anchors to the caret in the correct visual position (does NOT appear off-screen on the left).
    - Manual UAT (keyboard): popover open → Down navigates next, Up navigates previous, Enter inserts + trailing space + closes popover, Esc closes.
    - Manual UAT (NO trigger): typing `;notarealtrigger ` does NOT replace anything (typed text remains).
    - Manual UAT (NO boundary): typing `;betrayal` (no space) does NOT expand; popover may show if `betrayal` is a partial of other triggers, otherwise hidden.
    - Manual UAT (prefix change): in DevTools, call `Snippets.setPrefix("::")` → typing `::betrayal ` now expands, and `;betrayal ` no longer expands. (Settings UI for this lives in Plan 05.)
  </acceptance_criteria>
  <done>
    Snippet trigger engine + caret-mirror autocomplete are functional in all 7 marked textareas, in LTR + RTL, with keyboard nav and locale fallback.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire 7 textareas with data-snippets="true" + add App.getSnippets cache + BroadcastChannel snippets event + script tags + backup round-trip + sw.js precache (D-18, D-19, D-20, D-33, D-35, REQ-9)</name>
  <read_first>
    - add-session.html (7 target textareas line numbers from interfaces section)
    - assets/app.js (lines 38–54 for cache pattern, 366–405 for initCommon + BroadcastChannel)
    - assets/backup.js (lines 472–500 manifest normalize, 511–580 export, 673–800 import)
    - sw.js (lines 19–65 PRECACHE_URLS)
    - All HTML pages that include db.js
  </read_first>
  <action>
    Four sub-changes:

    **A. add-session.html — add `data-snippets="true"` to exactly 7 textareas (REQ-9):**

    Mark these 7 IDs with the attribute (add `data-snippets="true"` to each `<textarea>` tag — preserve all existing attributes):
    - `#trappedEmotions`
    - `#heartShieldEmotions`
    - `#sessionInsights`
    - `#limitingBeliefs`
    - `#additionalTech`
    - `#sessionComments`
    - `#customerSummary`

    Do NOT mark client-form notes, inline edit-client modal notes, settings inputs, or the export editor / preview textarea. Verify with grep that no other textarea in any HTML file carries the attribute.

    **B. assets/app.js — extend with App.getSnippets() cache + BroadcastChannel snippets event:**

    1. Add module-scope cache near `_sectionLabelCache`:
       ```
       let _snippetCache = []; // array of all snippets, refreshed eagerly + on BroadcastChannel
       ```

    2. Add public function:
       ```
       function getSnippets() {
         // Synchronous getter; returns a copy of the current cache.
         return _snippetCache.slice();
       }
       async function refreshSnippetCache() {
         try {
           _snippetCache = await PortfolioDB.getAllSnippets();
         } catch (e) {
           console.warn("App.refreshSnippetCache failed:", e);
           _snippetCache = [];
         }
         document.dispatchEvent(new CustomEvent("app:snippets-changed", { detail: { count: _snippetCache.length } }));
       }
       ```

    3. Modify `initCommon()` (line ~366) to eagerly load the cache BEFORE setLanguage runs, mirroring the therapistSettings load:
       ```
       try {
         _snippetCache = await PortfolioDB.getAllSnippets();
       } catch (err) {
         console.warn("Snippets unavailable on initCommon:", err);
         _snippetCache = [];
       }
       ```

    4. Modify the BroadcastChannel handler (line ~391-401) to handle a new event type:
       ```
       channel.onmessage = (e) => {
         try {
           if (e.data && e.data.type === "settings-changed") {
             const rows = await PortfolioDB.getAllTherapistSettings();
             _sectionLabelCache = new Map(rows.map(r => [r.sectionKey, r]));
             document.dispatchEvent(new CustomEvent("app:settings-changed"));
           } else if (e.data && e.data.type === "snippets") {
             await refreshSnippetCache();
           }
         } catch (err) {
           console.warn("BroadcastChannel refresh failed:", err);
         }
       };
       ```

    5. Export `getSnippets`, `refreshSnippetCache` in the public API object at the module bottom.

    **C. assets/backup.js — extend manifest and restore loop to include snippets (D-19, D-35):**

    1. In `exportBackup()` (line ~511), after the therapistSettings read:
       ```
       var allSnippets = [];
       try {
         if (typeof db.getAllSnippets === "function") {
           allSnippets = await db.getAllSnippets();
         }
       } catch (e) {
         console.warn("Backup: snippets read failed; exporting empty:", e);
         allSnippets = [];
       }
       ```

    2. Add `snippets: allSnippets,` to the manifest object at line ~556. Bump manifest `version: 2` to `version: 3` so future code can detect the format. Update `appVersion` to `"1.1"`.

    3. In `normalizeManifest` (line ~472), add backward-compat default:
       ```
       if (!Array.isArray(manifest.snippets)) {
         manifest.snippets = [];
       }
       ```

    4. In `importBackup()` restore loop (line ~793 area, after therapistSettings restore), add:
       ```
       // Phase 24 D-19 / D-35: restore snippets. Validate each row; skip malformed.
       for (var i = 0; i < manifest.snippets.length; i++) {
         var snip = manifest.snippets[i];
         try {
           window.PortfolioDB.validateSnippetShape(snip);
           // upsert: addSnippet if not exists, else updateSnippet
           var existing = await window.PortfolioDB.getSnippet(snip.id);
           if (existing) {
             await window.PortfolioDB.updateSnippet(snip);
           } else {
             await window.PortfolioDB.addSnippet(snip);
           }
         } catch (e) {
           console.warn("Backup restore: skipping malformed snippet row", snip, e);
         }
       }
       ```

    5. **Pre-v1.1 backup compatibility (D-35):** if manifest.snippets is empty/missing AND the import target DB is fresh (no snippets present), the v5 migration's seed pack will repopulate automatically on next openDB. No special-case needed — the empty-array normalize at step 3 is sufficient.

    **D. sw.js PRECACHE_URLS — add new asset paths (D-20, D-34):**

    1. Append to `PRECACHE_URLS` array (line ~19):
       ```
       '/assets/snippets-seed.js',
       '/assets/snippets.js',
       ```

    2. **DO NOT manually edit `CACHE_NAME`.** Pre-commit hook auto-bumps it on every asset commit. If the executor attempts to manually edit CACHE_NAME, the pre-commit hook may double-bump or conflict — fail the plan.

    3. **DO NOT bypass commits with `--no-verify`.** All commits must run through the pre-commit hook so CACHE_NAME bumps correctly.

    **E. Script tags on all pages that load snippets.js or use Snippets API:**

    1. Pages that need the snippet engine to BE LOADED:
       - add-session.html (the 7 textareas live here)
       - settings.html (Plan 05 will need Snippets.setPrefix from the Settings UI; load now is fine — it's lazy-init)

    2. Add `<script src="./assets/snippets.js"></script>` AFTER `<script src="./assets/db.js"></script>` and AFTER `<script src="./assets/app.js"></script>` (must come after both) in: add-session.html, settings.html.

    3. The snippets-seed.js script tag was already added in Task 2 step 9 to every page that loads db.js.

    4. Init invocation: in add-session.html's existing inline DOMContentLoaded script (or the relevant page module), add `await window.Snippets.init();` AFTER `await App.initCommon();`. For settings.html, init may be deferred to first interaction (Plan 05 decides).
  </action>
  <verify>
    <automated>
test "$(grep -c 'data-snippets="true"' add-session.html)" = "7" && \
! grep -l "data-snippets=\"true\"" add-client.html index.html settings.html sessions.html reporting.html 2>/dev/null && \
grep -q "App.getSnippets\|getSnippets" assets/app.js && \
grep -q "_snippetCache" assets/app.js && \
grep -q "refreshSnippetCache" assets/app.js && \
grep -q "app:snippets-changed" assets/app.js && \
grep -q "snippets" assets/backup.js && \
grep -q "manifest.snippets" assets/backup.js && \
grep -q "validateSnippetShape" assets/backup.js && \
grep -q "/assets/snippets.js" sw.js && \
grep -q "/assets/snippets-seed.js" sw.js && \
grep -q "src=\".*/snippets.js\"" add-session.html && \
grep -q "src=\".*/snippets.js\"" settings.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `add-session.html` has exactly 7 occurrences of `data-snippets="true"` (one per target textarea).
    - No other HTML file has `data-snippets="true"`.
    - `assets/app.js` defines `getSnippets`, `refreshSnippetCache`, `_snippetCache`, dispatches `app:snippets-changed` event, eagerly loads cache in `initCommon`.
    - The BroadcastChannel handler in app.js recognizes message type `"snippets"` and refreshes the cache.
    - `assets/backup.js` exports manifest with a `snippets` array; importBackup validates each row via `PortfolioDB.validateSnippetShape` and skips malformed; pre-v1.1 backups (manifest.snippets missing) restore without errors.
    - Manifest version bumped from 2 to 3; appVersion bumped to "1.1".
    - `sw.js` PRECACHE_URLS contains both `/assets/snippets.js` and `/assets/snippets-seed.js`.
    - `sw.js` CACHE_NAME was NOT manually edited (verified by `git log -p sw.js` showing only PRECACHE_URLS diff after the executor's commits — pre-commit hook's CACHE_NAME bump happens automatically on commit).
    - `add-session.html` and `settings.html` both load `assets/snippets.js` via `<script>` tag AFTER db.js and app.js.
    - Manual UAT (cross-tab): open two tabs, edit a snippet in tab A via DevTools (`PortfolioDB.updateSnippet(...)` + broadcast post), tab B's `App.getSnippets()` reflects the change within 100ms (verified by listening for `app:snippets-changed` in DevTools console).
    - Manual UAT (backup round-trip): export a backup with custom snippets → reset IDB → import → all snippets restored.
    - Manual UAT (pre-v1.1 backup): export from a v4 (pre-snippets) build → import into v5 → no error; v5 migration repopulates seed pack.
  </acceptance_criteria>
  <done>
    7 textareas wired. App.getSnippets cache + BroadcastChannel sync working. Backup includes snippets and tolerates pre-v1.1 manifests. SW precaches new assets without manual CACHE_NAME edit. All script tags ordered correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → snippet trigger detection | user types `<prefix><keyword>` — trigger detection regex must not be ReDoS-exploitable. |
| seed pack file → IDB | assets/snippets-seed.js is read by db.js migration; treated as trusted source but still passes through validateSnippetShape at migration boundary. |
| backup import JSON → IDB | manifest.snippets parsed from user-supplied file. MUST be validated. |
| IDB snippet → textarea expansion | snippet.expansions[locale] inserted into textarea.value via setRangeText. Textarea does not render HTML → naturally XSS-safe at this boundary. |
| IDB snippet preview → popover DOM | snippet.expansions[locale].slice(0, 60) rendered into popover via textContent. |
| cross-tab BroadcastChannel | other tabs can post messages; channel is same-origin scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | XSS | popover preview DOM | mitigate | Preview rendered via `previewSpan.textContent = expansion.slice(0,60)`. NEVER innerHTML. Acceptance test enforces no `innerHTML\s*=` in assets/snippets.js. |
| T-24-04-02 | XSS | textarea expansion | mitigate | `setRangeText` mutates textarea.value (plain text). Textarea elements never render HTML in their value. No injection vector. |
| T-24-04-03 | Tampering / validation bypass | backup import malformed snippet | mitigate | importBackup calls `PortfolioDB.validateSnippetShape(snip)` for each row; throws on invalid; surrounding try/catch skips the malformed row and continues. Schema check enforces id/trigger/expansions/tags/origin shapes. |
| T-24-04-04 | ReDoS | trigger detection regex | mitigate | All regexes are anchored at start or end, use bounded quantifiers `{2,32}`, no nested quantifiers, no unbounded alternations. Worst-case linear in input length. Max trigger length 32 chars enforced by validateSnippetShape. |
| T-24-04-05 | Information Disclosure | snippets store visible to demo mode | accept | Per D-21, demo mode has the same seed pack — intentional parity for demo discovery. No PII in seed pack (Sapir's PDF is clinical reference content). |
| T-24-04-06 | Tampering | BroadcastChannel attacker | accept | Same-origin scope; no cross-origin attacker. A malicious script in a same-origin tab could post fake `{type:"snippets"}` messages → recipient just refetches IDB. No harm — IDB is the source of truth. |
| T-24-04-07 | Tampering | seed pack identity collision | mitigate | Deterministic id format `ec.<col><row>.<slug>` prevents accidental ID reuse. Unique-trigger index on the store rejects duplicates at IDB level. |
| T-24-04-08 | Repudiation / data loss | user deletes seed by accident | mitigate | `resetSeedSnippet(id)` restores via Plan 05's "Reset to default" affordance + clears deletedSeedIds entry. Bulk reset will exist in Plan 05. |
| T-24-04-09 | DoS | backup import with 10000 snippets | accept | Each import is user-initiated, runs sequentially. Worst case ~10s for 10K — acceptable for a one-time admin action. UI can spinner-overlay in Plan 05 if needed. |
| T-24-04-10 | Cache divergence | sw.js precache stale after asset update | mitigate | Pre-commit hook auto-bumps CACHE_NAME on every assets/* commit → new SW version → cache busts. Plan explicitly forbids manual CACHE_NAME edit (would double-bump or skew). |
</threat_model>

<verification>
- Validate snippets-seed.js with the Node script in Task 1.
- IDB inspection: open DevTools → Application → IndexedDB → sessions_garden → snippets — confirm 60 records with origin=seed.
- Manual UAT for trigger expansion + autocomplete + RTL + locale fallback across all 4 languages.
- Manual UAT for cross-tab sync via two-tab BroadcastChannel test.
- Manual UAT for backup export → reset → import round-trip.
- Manual UAT for pre-v1.1 (v4) backup import → no errors, seed pack repopulates.
- Verify pre-commit hook bumps CACHE_NAME on commit (look at `git diff sw.js` post-commit).
</verification>

<success_criteria>
- IDB v5 migration runs cleanly on fresh installs AND upgrades from v3 / v4.
- 60 seed snippets present after first launch; idempotent on re-launch.
- Deleted seeds stay deleted via deletedSeedIds.
- Trigger expansion + caret-mirror popover work in all 7 marked textareas, LTR + RTL.
- App.getSnippets cache + BroadcastChannel sync.
- backup.js round-trips snippets; pre-v1.1 backups restore cleanly.
- sw.js precaches new assets; CACHE_NAME left to pre-commit hook.
</success_criteria>

<output>
After completion, create `.planning/phases/24-pre-launch-final-cleanup/24-04-SUMMARY.md` per the standard SUMMARY template, noting:
- Confirmation that 60 entries are in the seed pack with the Node validation output pasted.
- Migration v5 verified on a v4 → v5 upgrade and a fresh v5 install.
- Manual UAT screenshot or text confirmation for trigger expansion in LTR + RTL.
- Backup round-trip confirmation including pre-v1.1 import.
- Confirmation that CACHE_NAME diff post-commit was bumped by the pre-commit hook (not by the executor).
- Hand-off note for Plan 05: which API functions are available, where the Settings UI should hook in.
</output>
