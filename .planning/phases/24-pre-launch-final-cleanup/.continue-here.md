# Phase 24 — Resume Context

**Last session:** 2026-05-14 (session 2). Plans 01, 02, 03, 04, 06, 07, 08 all shipped + UAT confirmed. **Plan 05 is the only remaining Phase 24 plan.** SW cache v124.

## Last-session commit log (session 2 — Plan 04, in order)

| Commit | What |
|--------|------|
| `fe6093b` | docs(24-04) — refresh plan against post-Plan-06 codebase + test-coverage table |
| `fdac9b8` | Task 1 — 60-record seed pack + 6-scenario shape test |
| `80f8450` | Task 2 — IDB v5 migration + CRUD + validateSnippetShape + 15 test scenarios |
| `01a6f05` | Task 3 — assets/snippets.js trigger engine + caret popover + 11-scenario regex test + 12-scenario UAT |
| `d783922` | Task 4 — wire 7 textareas + App.getSnippets cache + BroadcastChannel + backup v3 + sw.js precache + 5-scenario cache test |
| `f96a73b` | chore — bump sw.js CACHE_NAME v122 → v123 (hook skipped on prior commit) |
| `95ec375` | docs(24-04) — initial SUMMARY |
| `9c161c3` | fix(24-04) — prepend `<Name> — ` to every seed expansion (240 paragraphs) + regression-guard scenario G; SW v123 → v124 |

## Critical Anti-Patterns (carry-forward from 24-01, still binding)

| Pattern | Severity | Why it matters |
|---------|----------|---------------|
| Marking a plan task `completed` based on grep gates alone for runtime-behavior code | **blocking** | 24-01 incident: structural grep passed while behavior was broken because an unrelated TypeError aborted the handler. Behavior verification BEFORE marking done is the only mitigation. See `[[feedback-behavior-verification]]` memory. |
| Skipping identifier-resolution trace for code that crosses inside/outside DOMContentLoaded or IIFE boundaries | **blocking** | 24-01 root cause was a top-level function referencing a `const` declared inside DOMContentLoaded. Bare reference resolved to `window.X` via legacy named-element access. For any function-scope-crossing edit, walk the chain explicitly: local → closure → module → window-named-element → undefined. |

Plan 04 followed this protocol successfully (5 test files, 38 scenarios). Plan 05 must continue it.

## Phase 24 status

**Shipped + UAT-confirmed (7 of 8 plans):**
- 24-01 (overview cleanups), 24-02, 24-03, 24-04 (snippet engine — this session), 24-06 (spotlight session-info), 24-07 (photo-crop modal stacking), 24-08 (nav-guard parity).

**Remaining (1 of 8):**
- **24-05** (Snippet Settings UI) — depends on 24-04's storage + engine + cache. Last plan before launch-shape for v1.1.

## Plan 24-05 — what it ships

Per `24-05-PLAN.md` (already written by gsd-planner in a prior session; may need refresh):
- **Prefix input** — calls `Snippets.setPrefix(newPrefix)` (already exposed in 24-04).
- **List view** — reads `App.getSnippets()` (sync, returns cached array; cache refreshes via `App.refreshSnippetCache()` and BroadcastChannel).
- **Search box + tag-filter chips** — client-side filter over the list.
- **Modal editor** — single-lang default + "Edit translations" reveal for he/en/cs/de. Validates via `PortfolioDB.validateSnippetShape` before save. On save, calls `App.refreshSnippetCache()` AND posts `{type: 'snippets-changed'}` on the `sessions-garden-settings` BroadcastChannel.
- **Snippet-only JSON import/export** — buttons + collision modal. Reuses the manifest-v3 shape from `backup.js`.
- **Reset-to-default action** — calls `PortfolioDB.resetSeedSnippet(id)` (already exposed in 24-04).
- **i18n keys** — Settings UI strings in 4 locales.
- **snippets.js script tag** on `settings.html` (deferred from 24-04 Task 4; sw.js PRECACHE_URLS already has it).

## What 24-04 ships that 24-05 builds on

| API | Location | Used by Plan 05 for |
|-----|----------|---------------------|
| `App.getSnippets()` — sync, returns copy of cache | assets/app.js | List view render |
| `App.refreshSnippetCache()` — async, reloads from IDB + dispatches `app:snippets-changed` | assets/app.js | After Settings UI mutates a snippet |
| `PortfolioDB.validateSnippetShape(snippet)` — pure, throws on invalid | assets/db.js | Modal editor save guard |
| `PortfolioDB.{addSnippet, updateSnippet, deleteSnippet, getSnippet, resetSeedSnippet}` | assets/db.js | All CRUD operations |
| `Snippets.setPrefix(newPrefix)` — accepts 1–2 char prefix | assets/snippets.js | Prefix input save |
| `BroadcastChannel "sessions-garden-settings"` with event `{type: 'snippets-changed'}` | assets/app.js | Cross-tab sync after mutation |

## How to start the next session

Open a **new conversation** with the handoff prompt below. The prompt is self-contained and includes the anti-pattern protocol, the Plan 04 commit reference, and Plan 05's exact scope.

```
[paste the handoff prompt — see Ben's saved copy]
```

## Conventions worth carrying

- **Pre-commit hook auto-bumps `sw.js CACHE_NAME`** on every asset commit — EXCEPT when sw.js itself is in the diff. Any commit that touches sw.js for non-CACHE_NAME reasons (e.g., PRECACHE_URLS) needs a manual `chore: bump CACHE_NAME` follow-up. See memory `reference-pre-commit-sw-bump.md`.
- **Hebrew copy: verbatim from source** — Plan 04 preserved Sapir's wording without applying the noun/infinitive normalization. Same default for Plan 05's i18n strings (Settings UI), but apply noun/infinitive convention to Sessions Garden's own labels per `feedback` memory.
- **Working test template** — `tests/24-04-seed-pack.test.js`, `24-04-shape-validator.test.js`, `24-04-trigger-regex.test.js` are pure-function templates. `24-04-idb-migration.test.js` is the handwritten IDB shim template. `24-04-app-cache.test.js` shows how to load `app.js` in a vm (note: requires `App.installNavGuard` rewrite to `window.App.installNavGuard` because vm context lacks legacy named-element auto-aliasing).
- **Same-date session-sort tiebreaker** (`date desc → createdAt desc → id desc`) — applied at 3 sites in Plan 06. Plan 05 doesn't sort sessions, but if it sorts snippets by creation time, use the same shape (`createdAt desc → id desc`).
- **`origin/main` is the live-app branch.** Local main is now ~33 commits ahead. DO NOT push without Ben's explicit OK.
