# Phase 24 — Resume Context

**Last session:** 2026-05-14. Plans 01, 02, 03 shipped (commits `573da4a`, `a33c2c9`, `230d58f`, `7934eec`, `28bf1e6`). Plan 01 needed two follow-up fixes after Ben's UAT exposed grep-gate verification missing a latent bug. v110 of sw.js.

**Remaining:** Plans 04 (snippet engine — big), 05 (snippet Settings UI), 06 (pre-session context card). All dependencies satisfied.

## Critical Anti-Patterns

| Pattern | Severity | Why it matters |
|---------|----------|---------------|
| Marking a plan task `completed` based on grep gates alone for runtime-behavior code | **blocking** | Phase 24-01 incident: structural verification (function exists, comments present, call sites migrated) passed; behavior was still broken because a pre-existing `TypeError` in `resetInlineClientForm` blocked the dropdown change handler from reaching `populateSpotlight`. Two failed fix attempts followed, each based on a wrong mental model of the bug. Behavior verification BEFORE marking done is the only mitigation. See `[[feedback-behavior-verification]]` memory. |
| Skipping identifier-resolution trace for code that crosses inside/outside DOMContentLoaded or IIFE boundaries | **blocking** | The Phase 24-01 root cause was a top-level `function resetInlineClientForm()` referencing `inlineBirthDatePicker` which was declared `const` inside DOMContentLoaded. Bare reference resolved to `window.inlineBirthDatePicker` via legacy named-element access (the `<div id="...">` in HTML), which has no `.clear()` method → TypeError. For any function-scope-crossing edit, walk the identifier lookup chain explicitly: local → closure → module → window-named-element → undefined. |

## Required Protocol for Remaining Plans (04, 05, 06)

Before implementing any task in the remaining plans:

1. **Read the PLAN.md task in full.** Identify which surfaces are runtime-behavior (IDB ops, event handlers, DOM mutation, scope-sensitive code) vs pure-structural (i18n key additions, file creation, comment additions).
2. **For each runtime-behavior task, write a falsifiable behavior test FIRST.** Test file lives at `assets/__tests__/24-XX-{topic}.test.cjs` or appropriate runner (jsdom + `fake-indexeddb` for IDB, `node -e` for pure functions, documented browser-console snippet with numbered steps for visual UI). Show Ben the test BEFORE implementing.
3. **Plan 04 specifically — high-leverage test surfaces:**
   - IDB v5 migration: testable with `fake-indexeddb` — assert objectStoreNames, version, idempotent re-run, `deletedSeedIds` tracking
   - 60-snippet seed pack: testable by `require()` — assert array length, shape of each record (`id`, `trigger`, `expansions: {he,en,cs,de}`, `tags`, `origin: 'seed'`), 4-locale coverage spot-check on 3 random records
   - Trigger detection regex: pure-function, full unit-test coverage of the prefix-detection + word-boundary expansion logic — assert NO ReDoS via timing tests on adversarial inputs
   - Snippet shape validator: pure-function, test happy paths + every rejection condition
   - Caret-mirror popover positioning: hardest to test; document a browser UAT script with screenshot expectations instead
4. **Run tests after implementation. Show actual output.** Not "I believe it works." If tests pass → commit. If browser UAT is required → mark task `in_progress`, NOT `completed`, until Ben confirms.
5. **Commit cadence:** one commit per task. Pre-commit hook auto-bumps `sw.js CACHE_NAME` — do NOT pre-bump, do NOT bypass with `--no-verify`.

## How to start the next session

```
/gsd-execute-phase 24 --interactive
```

The orchestrator will read this file and force me to acknowledge the two blocking anti-patterns BEFORE proceeding to plan execution. Do not skip that step.

## Outstanding UAT for shipped plans

| Plan | Item Ben needs to verify in browser |
|------|------------------------------------|
| 24-02 | Export preview pane: `## heading\nbody` renders as proper h2 + p. Severity reversal: spot-check 3 historical sessions on overview clock-icon for before→after order (was Case-C, code already correct since 2026-03-09 — TODO was stale). |
| 24-03 | (1) Cancel button on edit-session: silent return on clean, confirm dialog on dirty. (2) Hebrew strings match D-05 lockdown: `ביטול`, `ביטול השינויים`, `הצגה`, `כן, לבטל`, `המשך עריכה`. (3) Overview clock-icon expansion button reads "View" / `הצגה` / `Anzeigen` / `Zobrazit`. |

These DO NOT block starting Plan 04. They block phase completion / final UAT.
