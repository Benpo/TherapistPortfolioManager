---
phase: 05-legal-and-production-packaging
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - assets/license.js
  - license.html
  - sw.js
  - manifest.json
  - assets/icons/icon-192.png
  - assets/icons/icon-512.png
  - _headers
  - index.html
  - sessions.html
  - add-client.html
  - add-session.html
  - reporting.html
  - disclaimer.html
autonomous: true
requirements: [DIST-03, DIST-04]

must_haves:
  truths:
    - "After accepting terms, user without a license key is redirected to license.html"
    - "User can enter a license key and activate it via Lemon Squeezy API (requires internet for first activation)"
    - "After activation, daily app use requires NO internet — license check is localStorage-only"
    - "Invalid or cross-product keys show a clear error message with link to purchase"
    - "The app works fully offline after first load — service worker caches all static assets"
    - "The app is installable as a PWA via Add to Home Screen on Android and manually on iOS"
    - "Service worker updates do NOT affect IndexedDB data — only HTTP-layer assets are cached"
  artifacts:
    - path: "license.html"
      provides: "License key entry page with i18n, activation form, error display, purchase link"
      min_lines: 50
    - path: "assets/license.js"
      provides: "License activation via Lemon Squeezy API, localStorage storage, offline validation, i18n"
      min_lines: 60
    - path: "sw.js"
      provides: "Cache-first service worker precaching all static assets"
      min_lines: 20
    - path: "manifest.json"
      provides: "PWA manifest with Sessions Garden name, icons, standalone display"
      min_lines: 10
    - path: "_headers"
      provides: "Cloudflare Pages cache control headers"
      min_lines: 4
  key_links:
    - from: "index.html (and 4 other app pages)"
      to: "license.html"
      via: "Inline synchronous gate script (after terms gate)"
      pattern: "portfolioLicenseActivated"
    - from: "license.js"
      to: "https://api.lemonsqueezy.com/v1/licenses/activate"
      via: "fetch POST on key submission"
      pattern: "lemonsqueezy\\.com/v1/licenses/activate"
    - from: "license.js"
      to: "localStorage"
      via: "portfolioLicenseActivated + portfolioLicenseKey + portfolioLicenseInstance"
      pattern: "localStorage\\.setItem\\('portfolioLicense"
    - from: "sw.js"
      to: "caches API"
      via: "Cache-first fetch handler with precache on install"
      pattern: "caches\\.open"
    - from: "all HTML pages"
      to: "sw.js"
      via: "navigator.serviceWorker.register"
      pattern: "serviceWorker\\.register"
---

<objective>
Implement license key gating via Lemon Squeezy License API and full PWA setup (service worker + manifest) for offline capability and installability.

Purpose: Access control ensures only paying customers use the app. PWA setup ensures the app works offline after first load and is installable, which is the core value proposition ("works without internet, data never leaves your device").
Output: license.html gate page, license.js activation logic, sw.js service worker, manifest.json, placeholder PWA icons, cache headers, and gate/SW registration scripts in all app pages.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legal-and-production-packaging/05-CONTEXT.md
@.planning/phases/05-legal-and-production-packaging/05-RESEARCH.md
@.planning/phases/05-legal-and-production-packaging/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: gate script pattern already in all 5 app pages -->
<!-- The license gate script goes AFTER the terms gate script, BEFORE theme script -->

Gate order in each app page <head>:
1. Terms gate (from Plan 01): checks portfolioTermsAccepted
2. License gate (this plan): checks portfolioLicenseActivated
3. Theme detection (existing): sets data-theme

New localStorage keys this plan introduces:
- portfolioLicenseKey (the license key string)
- portfolioLicenseInstance (Lemon Squeezy instance_id)
- portfolioLicenseActivated ("1" when activated)

Existing localStorage keys from Plan 01:
- portfolioTermsAccepted (ISO timestamp)
- portfolioTermsLang (language code)

Lemon Squeezy License API (no API key needed):
- POST https://api.lemonsqueezy.com/v1/licenses/activate
  Body: license_key={key}&instance_name={name}
  Returns: { activated, instance: { id }, meta: { store_id, product_id } }
- POST https://api.lemonsqueezy.com/v1/licenses/validate
  Body: license_key={key}&instance_id={id}
  Returns: { valid, meta: { store_id, product_id } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create license key gate page and activation logic</name>
  <files>license.html, assets/license.js</files>
  <action>
Create 2 new files:

**license.html** — License key entry page. Structure:
- Same CSS (tokens.css + app.css) for visual consistency
- Theme detection script in head (same inline pattern as app pages)
- Terms gate script in head (redirect to disclaimer.html if terms not accepted — prevent bypassing terms by going directly to license.html)
- NO license gate script (this IS the license page)
- Centered card layout with Sessions Garden branding
- Language support: read portfolioTermsLang or portfolioLang from localStorage, load i18n-disclaimer.js for shared strings or embed license-specific strings in license.js
- Input field for license key (text input, monospace font for readability)
- "Activate" button
- Error display area (hidden by default)
- "Don't have a license key?" link — points to landing page (./landing.html) or placeholder URL. Text: "Purchase Sessions Garden" with link
- Loading state while activation request is in flight (disable button, show spinner or "Activating..." text)
- On successful activation: redirect to ?next parameter or ./index.html
- Support re-activation: if portfolioLicenseKey exists but portfolioLicenseActivated is missing, pre-fill the key field and show message "Your license needs to be re-activated. Please connect to the internet and click Activate."

**assets/license.js** — License activation logic:
- Constants at top of file: STORE_ID and PRODUCT_ID as placeholders (e.g. 0 or empty string) with TODO comments for Sapir to fill in after creating Lemon Squeezy product. Add clear comment: "// TODO: Replace with actual values from Lemon Squeezy dashboard after product creation"
- activateLicenseKey(key) async function:
  1. Generate instance_name: 'sessions-garden-' + Date.now()
  2. POST to https://api.lemonsqueezy.com/v1/licenses/activate with license_key and instance_name (Content-Type: application/x-www-form-urlencoded)
  3. Check response: if !data.activated, throw error with data.error message
  4. Verify meta.store_id and meta.product_id match hardcoded constants (skip verification if constants are 0/placeholder — for development)
  5. Store in localStorage: portfolioLicenseKey, portfolioLicenseInstance (instance.id), portfolioLicenseActivated = "1"
  6. Return data
- isLicensed() function: return portfolioLicenseActivated === "1" && !!portfolioLicenseInstance
- Error handling: network errors show "Activation requires an internet connection. Please connect and try again." Invalid key shows "Invalid license key. Please check your key and try again." Cross-product key shows "This license key is not valid for Sessions Garden."
- Device limit exceeded (Lemon Squeezy returns error when activation_limit reached): show "You have reached the device limit for this license. Contact [support email placeholder] to deactivate an existing device."
- i18n for license page: embed translations for license-specific strings (key input label, activate button, error messages, purchase link text) in all 4 languages inside license.js or load from i18n-disclaimer.js if extended there
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['license.html','assets/license.js']; const missing=files.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('Missing:',missing);process.exit(1)} const js=fs.readFileSync('assets/license.js','utf8'); ['lemonsqueezy.com/v1/licenses/activate','portfolioLicenseActivated','portfolioLicenseKey','portfolioLicenseInstance','STORE_ID','PRODUCT_ID'].forEach(k=>{if(!js.includes(k)){console.error('Missing in license.js:',k);process.exit(1)}}); console.log('License files present with key content')"</automated>
  </verify>
  <done>license.html shows key entry form, activates via Lemon Squeezy API, stores credentials in localStorage, handles errors gracefully, supports re-activation, redirects to app on success</done>
</task>

<task type="auto">
  <name>Task 2: Create PWA infrastructure and inject gates into app pages</name>
  <files>sw.js, manifest.json, assets/icons/icon-192.png, assets/icons/icon-512.png, _headers, index.html, sessions.html, add-client.html, add-session.html, reporting.html, disclaimer.html</files>
  <action>
Create PWA files and modify existing pages:

**manifest.json** — At project root. Use exact values from RESEARCH.md:
- name: "Sessions Garden", short_name: "Sessions Garden"
- description: "Therapeutic session tracking for energy healing practitioners"
- start_url: "/index.html", scope: "/"
- display: "standalone"
- background_color: "#faf7f2" (matches --color-bg light mode)
- theme_color: "#2d6a4f" (matches --color-primary)
- icons: 192x192 and 512x512 PNG placeholders
- lang: "en"

**assets/icons/icon-192.png and icon-512.png** — Generate simple placeholder icons. Use a canvas-based Node script or create minimal valid PNG files. The icon should be a simple green circle (#2d6a4f) on cream background (#faf7f2) with "SG" text — or just solid green squares. These are PLACEHOLDERS — proper icons are deferred. Must be valid PNG files that pass PWA audit.

**sw.js** — At project root. Cache-first service worker per RESEARCH.md Pattern 3:
- CACHE_NAME = 'sessions-garden-v1'
- PRECACHE_URLS: all HTML files (index, sessions, add-client, add-session, reporting, disclaimer, license, landing), all CSS (tokens.css, app.css), all JS (app.js, db.js, i18n.js, i18n-en/he/de/cs.js, overview.js, sessions.js, add-client.js, add-session.js, reporting.js, disclaimer.js, license.js, landing.js, i18n-disclaimer.js), fonts (assets/fonts/Rubik-Variable.woff2), manifest.json
- Install event: precache all URLs
- Activate event: delete old caches, call self.clients.claim()
- Fetch event: cache-first for GET requests only (return cached, fallback to network)
- Do NOT call self.skipWaiting() — per RESEARCH.md, avoid half-old-half-new state with multiple tabs
- CRITICAL: service worker only handles HTTP cache layer. It CANNOT and DOES NOT touch IndexedDB. Add a comment at the top of sw.js explicitly stating this.

**_headers** — At project root. Cloudflare Pages cache headers per RESEARCH.md:
```
/*.html
  Cache-Control: no-cache
/*.js
  Cache-Control: public, max-age=86400
/*.css
  Cache-Control: public, max-age=86400
```

**Modify all 5 app pages** (index.html, sessions.html, add-client.html, add-session.html, reporting.html):
1. Add license gate script in <head> — AFTER the terms gate (from Plan 01), BEFORE theme script:
```html
<script>
  (function(){try{if(localStorage.getItem('portfolioTermsAccepted')&&!localStorage.getItem('portfolioLicenseActivated')){window.location.replace('./license.html');}}catch(e){})();
</script>
```
2. Add manifest link in <head>: `<link rel="manifest" href="./manifest.json">`
3. Add SW registration before closing </body> tag (before existing script tags or after — after is fine):
```html
<script>
  if('serviceWorker' in navigator){window.addEventListener('load',function(){navigator.serviceWorker.register('/sw.js').catch(function(e){console.warn('SW registration failed:',e)})});}
</script>
```
4. Add iOS install prompt: a dismissible banner shown when on iOS + not in standalone mode. Can be done in app.js or as an inline script. Show once per session (use sessionStorage to track dismissal). Text: "Install Sessions Garden: tap the Share button, then 'Add to Home Screen'". Detect iOS: /iP(hone|ad|od)/.test(navigator.userAgent) && !window.matchMedia('(display-mode: standalone)').matches

**Modify disclaimer.html:**
- Add manifest link in head
- Add SW registration script
- Do NOT add license gate (disclaimer must be accessible without license)

The gate order in each app page <head> is now:
1. Terms gate → disclaimer.html
2. License gate → license.html
3. Theme detection → data-theme attribute
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['sw.js','manifest.json','_headers','assets/icons/icon-192.png','assets/icons/icon-512.png']; const missing=files.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('Missing:',missing);process.exit(1)} const pages=['index.html','sessions.html','add-client.html','add-session.html','reporting.html']; pages.forEach(p=>{const h=fs.readFileSync(p,'utf8'); if(!h.includes('portfolioLicenseActivated'))console.error(p+': missing license gate'); if(!h.includes('manifest.json'))console.error(p+': missing manifest link'); if(!h.includes('serviceWorker'))console.error(p+': missing SW registration')}); const sw=fs.readFileSync('sw.js','utf8'); if(!sw.includes('sessions-garden-v'))console.error('sw.js: missing cache name'); if(sw.includes('skipWaiting'))console.error('sw.js: should NOT use skipWaiting'); console.log('PWA infrastructure verified')"</automated>
  </verify>
  <done>Service worker caches all static assets for offline use, manifest.json makes app installable, license gate redirects unlicensed users, all 5 app pages have terms gate + license gate + manifest + SW registration, _headers file controls Cloudflare caching, placeholder icons exist</done>
</task>

</tasks>

<verification>
1. Open index.html with terms accepted but no license — redirects to license.html
2. On license.html: enter invalid key — shows clear error message
3. Enter valid Lemon Squeezy key (if available) — activates, stores in localStorage, redirects to app
4. After activation, app loads without license check (offline-capable)
5. DevTools > Application > Manifest shows valid manifest with icons
6. DevTools > Application > Service Workers shows sw.js registered and activated
7. Go offline in DevTools > Network — reload app — it loads from cache
8. On iOS Safari (or simulated): install guidance banner appears
9. Clear portfolioLicenseActivated from localStorage — app redirects to license.html, key is pre-filled for re-activation
</verification>

<success_criteria>
- License key gate blocks app access for unlicensed users (after terms acceptance)
- Lemon Squeezy API activation works with proper error handling
- Daily use requires no internet after initial activation
- Service worker precaches all static assets
- App loads fully offline after first visit
- manifest.json present with correct metadata
- PWA is installable on Android and via manual steps on iOS
- Service worker does NOT touch IndexedDB
</success_criteria>

<output>
After completion, create `.planning/phases/05-legal-and-production-packaging/05-02-SUMMARY.md`
</output>
