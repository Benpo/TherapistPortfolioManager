---
phase: 14-i18n-bugs-legal-footer-cleanup-and-contact-email-update
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/landing.js
  - assets/license.js
  - assets/landing.css
  - disclaimer.html
  - impressum.html
autonomous: true
requirements: [D-01, D-02, D-08]
gap_closure: true

must_haves:
  truths:
    - "Clicking 'I have a license' from Hebrew landing opens license page in Hebrew"
    - "Disclaimer brand text clicks through to landing page"
    - "Impressum heading shows Hebrew text when lang=he"
    - "License CTA button in header is visually enlarged and prominent"
  artifacts:
    - path: "assets/landing.js"
      provides: "?lang= param appended to hero-enter-link and pricing-license-link"
      contains: "setHref.*license.*lang"
    - path: "assets/license.js"
      provides: "URL param reader in getLicenseLang()"
      contains: "URLSearchParams"
    - path: "disclaimer.html"
      provides: "Clickable brand link"
      contains: "a.*disclaimer-brand"
    - path: "impressum.html"
      provides: "Hebrew title for Impressum"
      contains: "he.*אודות"
  key_links:
    - from: "assets/landing.js"
      to: "license.html"
      via: "setHref with ?lang= param"
      pattern: "setHref.*license\\.html.*lang"
    - from: "assets/license.js"
      to: "URL params"
      via: "URLSearchParams in getLicenseLang()"
      pattern: "URLSearchParams.*lang"
---

<objective>
Fix four quick UAT gaps: (1) license page links from landing miss ?lang= param so Hebrew users land on English, (2) disclaimer brand is a non-clickable div, (3) Impressum Hebrew heading says "Impressum" instead of Hebrew, (4) license CTA button needs more visual prominence.

Purpose: Close UAT test failures 2, 6, 9, A1, A2 with minimal targeted fixes.
Output: Four single-point fixes across 5 files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-01-SUMMARY.md
@.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-02-SUMMARY.md
@.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-03-SUMMARY.md

<interfaces>
<!-- From assets/landing.js (lines 410-471): -->
setHref('hero-enter-link', './license.html');
setHref('pricing-license-link', './license.html');

<!-- From assets/license.js (lines 96-104): -->
function getLicenseLang() {
  try {
    var stored = localStorage.getItem('portfolioTermsLang') || localStorage.getItem('portfolioLang');
    if (stored && LICENSE_I18N[stored]) return stored;
    var nav = (navigator.language || '').toLowerCase().slice(0, 2);
    if (LICENSE_I18N[nav]) return nav;
  } catch (e) { /* ignore */ }
  return 'en';
}

<!-- From disclaimer.html (line 252): -->
<div class="disclaimer-brand" role="banner">

<!-- From impressum.html (line 47): -->
var TITLES = { en: 'Impressum', he: 'Impressum', de: 'Impressum', cs: 'Impressum' };

<!-- From assets/landing.css (lines 118-129): -->
.landing-enter-link {
  font-size: 1rem;
  font-weight: 500;
  padding: 0.5rem 1.25rem;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: License lang passthrough and disclaimer brand link</name>
  <files>assets/landing.js, assets/license.js, disclaimer.html</files>
  <action>
    1. In assets/landing.js, update the two setHref calls for license links to append ?lang= parameter:
       - Find `setHref('hero-enter-link', './license.html');` (line ~412) and change to `setHref('hero-enter-link', './license.html?lang=' + lang);`
       - Find `setHref('pricing-license-link', './license.html');` (line ~470) and change to `setHref('pricing-license-link', './license.html?lang=' + lang);`
       - The `lang` variable is already available in scope of the `applyLang` function where these calls live.

    2. In assets/license.js, add URL param reader to getLicenseLang() BEFORE the localStorage check:
       - At the top of the function body, add:
         ```
         var params = new URLSearchParams(window.location.search);
         var urlLang = params.get('lang');
         if (urlLang && LICENSE_I18N[urlLang]) return urlLang;
         ```
       - Keep the existing localStorage fallback chain after this new block.

    3. In disclaimer.html, change the brand container from `<div>` to `<a>`:
       - Find `<div class="disclaimer-brand" role="banner">` (line ~252)
       - Change to `<a class="disclaimer-brand" href="./landing.html" role="banner">`
       - Find the corresponding closing `</div>` and change to `</a>`
       - The CSS already has `text-decoration: none; color: inherit;` on `.disclaimer-brand` so no style changes needed.
  </action>
  <verify>
    <automated>grep -n "license.html?lang" assets/landing.js | head -5 && grep -n "URLSearchParams" assets/license.js | head -3 && grep -n "disclaimer-brand.*href" disclaimer.html | head -3</automated>
  </verify>
  <done>License links from landing include ?lang= param; license.js reads URL param first; disclaimer brand is a clickable link to landing page.</done>
</task>

<task type="auto">
  <name>Task 2: Impressum Hebrew heading and license CTA enlargement</name>
  <files>impressum.html, assets/landing.css</files>
  <action>
    1. In impressum.html, update the TITLES object in the inline script (line ~47):
       - Change `he: 'Impressum'` to `he: 'אודות / Impressum'`
       - This provides a Hebrew label while retaining the German legal term for reference.

    2. In assets/landing.css, enlarge the `.landing-enter-link` (the header license CTA button):
       - Change `font-size: 1rem` to `font-size: 1.1875rem` (19px — matches hero CTA)
       - Change `padding: 0.5rem 1.25rem` to `padding: 0.6875rem 2rem` (more generous horizontal padding)
       - Add `min-inline-size: 220px` and `text-align: center` so the button has visual weight
       - Change `font-weight: 500` to `font-weight: 600` to match the primary CTA weight
       - This addresses the "not enlarged enough" cosmetic feedback while keeping the existing color scheme.
  </action>
  <verify>
    <automated>grep -n "אודות" impressum.html | head -3 && grep -A5 "landing-enter-link {" assets/landing.css | head -8</automated>
  </verify>
  <done>Impressum shows "אודות / Impressum" for Hebrew users; license CTA button is visually prominent with larger font, more padding, and min-width.</done>
</task>

</tasks>

<verification>
- Open landing.html in Hebrew, click "I have a license" link — license.html should open with ?lang=he in URL and display in Hebrew
- Open disclaimer.html — click "Sessions Garden" brand text — should navigate to landing.html
- Open impressum.html?lang=he — heading should show "אודות / Impressum"
- Landing page header license button should be noticeably larger and more prominent than before
</verification>

<success_criteria>
- License page receives and displays correct language from landing page links
- Disclaimer brand is a clickable anchor element
- Impressum has Hebrew heading text
- License CTA button has increased font-size, padding, and min-width
</success_criteria>

<output>
After completion, create `.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-04-SUMMARY.md`
</output>
