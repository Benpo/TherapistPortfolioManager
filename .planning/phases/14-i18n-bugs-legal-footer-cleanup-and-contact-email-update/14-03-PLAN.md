---
phase: 14-i18n-bugs-legal-footer-cleanup-and-contact-email-update
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - impressum.html
  - datenschutz.html
  - landing.html
  - assets/landing.js
  - assets/landing.css
  - sw.js
autonomous: true
requirements:
  - D-05
  - D-06

must_haves:
  truths:
    - "Footer Impressum link opens a dedicated impressum.html page, not an inline accordion"
    - "Footer Privacy link opens a dedicated datenschutz.html page, not an inline accordion"
    - "Both new pages detect language from portfolioLang localStorage and display content accordingly"
    - "Footer link labels match the active language (Hebrew shows Hebrew labels, not German)"
    - "Both new pages are cached by the service worker for offline access"
  artifacts:
    - path: "impressum.html"
      provides: "Dedicated Impressum page with language detection"
      contains: "Angaben"
    - path: "datenschutz.html"
      provides: "Dedicated Privacy Policy page with language detection"
      contains: "Datenschutz"
    - path: "sw.js"
      provides: "PRECACHE_URLS includes new pages"
      contains: "impressum.html"
    - path: "landing.html"
      provides: "Footer links point to new pages; accordion HTML removed"
  key_links:
    - from: "landing.html"
      to: "impressum.html"
      via: "footer link href"
      pattern: "impressum.html"
    - from: "landing.html"
      to: "datenschutz.html"
      via: "footer link href"
      pattern: "datenschutz.html"
    - from: "sw.js"
      to: "impressum.html"
      via: "PRECACHE_URLS entry"
      pattern: "impressum.html"
---

<objective>
Extract Impressum and Datenschutz content from inline accordions in landing.html into dedicated standalone pages, update footer to link to them, and register new pages in service worker.

Purpose: Legal content gets proper dedicated pages instead of collapsible accordions, with language matching the active language selection.
Output: Two new HTML pages (impressum.html, datenschutz.html), cleaned landing.html footer, updated sw.js.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated impressum.html and datenschutz.html pages</name>
  <files>impressum.html, datenschutz.html</files>
  <read_first>
    - disclaimer.html (lines 1-30 — page structure pattern to follow for standalone legal pages)
    - landing.html (lines 366-533 — full legal accordion HTML to extract content from)
    - assets/landing.js (lines 96-116, 202-212, 308-318, 414-424 — i18n keys for impressum/datenschutz titles)
  </read_first>
  <action>
**D-05: Create dedicated legal pages**

Create `impressum.html` following the `disclaimer.html` page pattern (standalone HTML with inline styles, theme detection script, language detection):

```html
<!doctype html>
<html lang="en" dir="ltr">
<head>
  <script>
    (function(){try{var t=localStorage.getItem('portfolioTheme');if(t==='dark')document.documentElement.setAttribute('data-theme','dark');}catch(e){}})();
  </script>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Impressum — Sessions Garden</title>
  <link rel="stylesheet" href="./assets/tokens.css" />
  <link rel="stylesheet" href="./assets/app.css" />
  <link rel="manifest" href="./manifest.json" />
  <style>
    .legal-page { min-height: 100vh; display: flex; flex-direction: column; background: var(--color-bg); color: var(--color-text); }
    .legal-topbar { display: flex; align-items: center; justify-content: space-between; padding: 0.75rem 1.25rem; border-bottom: 1px solid var(--color-border); }
    .legal-topbar-brand { font-weight: 600; font-size: 1.125rem; color: var(--color-primary); text-decoration: none; }
    .legal-back-link { font-size: 0.875rem; color: var(--color-primary); text-decoration: none; }
    .legal-back-link:hover { text-decoration: underline; }
    .legal-content { flex: 1; max-width: 720px; margin: 2rem auto; padding: 0 1.5rem; line-height: 1.7; }
    .legal-content h2 { margin-top: 0; color: var(--color-primary); }
    .legal-content h3 { margin-top: 1.5rem; }
    .legal-content pre { font-family: inherit; white-space: pre-wrap; margin: 0; }
  </style>
</head>
<body>
  <div class="legal-page">
    <div class="legal-topbar">
      <a class="legal-topbar-brand" href="./landing.html">Sessions Garden</a>
      <a id="back-link" class="legal-back-link" href="./landing.html">Back to home</a>
    </div>
    <div class="legal-content">
      <!-- Copy the FULL Impressum content from landing.html lines 376-386 here -->
      <h2 id="impressum-heading">Impressum</h2>
      <h3 id="impressum-angaben">Angaben gemass DDG ss 5</h3>
      <pre id="impressum-placeholder" style="font-family:inherit;margin:0;white-space:pre-wrap;">[YOUR_BUSINESS_NAME]
[YOUR_STREET_ADDRESS]
[YOUR_CITY_POSTAL_CODE]

Kontakt:
E-Mail: [YOUR_BUSINESS_EMAIL]
Telefon: [YOUR_PHONE_NUMBER]

Steuernummer: [YOUR_TAX_ID]</pre>
    </div>
  </div>
  <script>
    (function() {
      var TITLES = { en: 'Impressum', he: 'Impressum', de: 'Impressum', cs: 'Impressum' };
      var BACK = { en: 'Back to home', he: 'חזרה לדף הבית', de: 'Zuruck zur Startseite', cs: 'Zpet na hlavni stranku' };
      var ANGABEN = { en: 'Information pursuant to DDG ss 5', he: 'פרטים בהתאם ל-DDG סעיף 5', de: 'Angaben gemass DDG ss 5', cs: 'Udaje podle DDG ss 5' };
      function detectLang() {
        var params = new URLSearchParams(window.location.search);
        var p = params.get('lang');
        if (p && TITLES[p]) return p;
        try { var s = localStorage.getItem('portfolioLang'); if (s && TITLES[s]) return s; } catch(e) {}
        var nav = (navigator.language || '').split('-')[0].toLowerCase();
        return TITLES[nav] ? nav : 'en';
      }
      var lang = detectLang();
      document.documentElement.setAttribute('lang', lang);
      document.documentElement.setAttribute('dir', lang === 'he' ? 'rtl' : 'ltr');
      var h = document.getElementById('impressum-heading');
      if (h) h.textContent = TITLES[lang];
      var a = document.getElementById('impressum-angaben');
      if (a) a.textContent = ANGABEN[lang];
      var b = document.getElementById('back-link');
      if (b) b.textContent = BACK[lang];
    })();
  </script>
</body>
</html>
```

Create `datenschutz.html` with the same page pattern. Copy ALL the Datenschutz content from `landing.html` lines 396-530 (both German and English versions) into the `.legal-content` div. Keep all the existing element IDs so they remain addressable. Add a language detection script identical in pattern to impressum.html but with:

```js
var TITLES = { en: 'Privacy Policy', he: 'מדיניות פרטיות', de: 'Datenschutzerklarung', cs: 'Zasady ochrany osobnich udaju' };
var BACK = { en: 'Back to home', he: 'חזרה לדף הבית', de: 'Zuruck zur Startseite', cs: 'Zpet na hlavni stranku' };
```

Set the page title and heading based on detected language. Both German and English content sections remain visible (they are the full bilingual privacy policy).

IMPORTANT: Use actual unicode characters (umlauts, accents, Hebrew) in the HTML files, not escaped sequences. Copy the exact content from landing.html — do not paraphrase or summarize the legal text.
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && test -f impressum.html && test -f datenschutz.html && grep 'detectLang' impressum.html && grep 'detectLang' datenschutz.html && grep 'YOUR_BUSINESS_NAME' impressum.html && grep 'Datenschutz' datenschutz.html && echo "Legal pages created: OK"</automated>
  </verify>
  <acceptance_criteria>
    - impressum.html exists at project root with theme detection, language detection, back link
    - impressum.html contains the `[YOUR_BUSINESS_NAME]` placeholder content from landing.html
    - impressum.html script has `detectLang()` checking URL param, localStorage, browser fallback
    - datenschutz.html exists at project root with same page structure pattern
    - datenschutz.html contains ALL German and English privacy policy sections from landing.html (sections 1-10)
    - datenschutz.html has language detection that sets heading text and page direction
    - Both pages link back to `./landing.html`
    - Both pages load `tokens.css` and `app.css` for consistent theming
  </acceptance_criteria>
  <done>Two standalone legal pages exist with full content, language detection, and consistent styling</done>
</task>

<task type="auto">
  <name>Task 2: Update footer links, remove accordion, register in service worker</name>
  <files>landing.html, assets/landing.js, sw.js</files>
  <read_first>
    - landing.html (lines 366-551 — legal accordion section and footer)
    - assets/landing.js (lines 558-583 — applyLang impressum/datenschutz/footer section)
    - sw.js (lines 19-58 — PRECACHE_URLS array)
  </read_first>
  <action>
**D-05: Remove accordion, update footer links**

In `landing.html`, delete the entire legal accordion section (lines 366-534, from `<!-- LEGAL ACCORDION -->` through the closing `</div>` of `.legal-accordion`). This removes approximately 168 lines of inline Impressum and Datenschutz HTML.

In `landing.html`, update the footer links (currently around line 546-547):

Change from:
```html
<a id="footer-impressum-link" href="#footer-impressum-toggle">Impressum</a>
<a id="footer-privacy-link" href="#footer-privacy-toggle">Privacy Policy</a>
```
to:
```html
<a id="footer-impressum-link" href="./impressum.html">Impressum</a>
<a id="footer-privacy-link" href="./datenschutz.html">Privacy Policy</a>
```

**D-06: Footer link text already i18n'd**

The existing `applyLang` in `landing.js` already sets footer link text via `setText2('footer-impressum-link', t.footerImpressum)` and `setText2('footer-privacy-link', t.footerPrivacy)`. The `footerImpressum` translations are already defined for all 4 languages. However, check the Hebrew value — currently `footerImpressum` in `he` is `'Impressum'` (German word). This should stay as `'Impressum'` because it's a proper legal term used internationally (like "FAQ"). The key issue was the accordion toggle showing German — now that it's a simple link, the translated label from `footerImpressum` will display correctly.

In `assets/landing.js`, in the `applyLang` function, remove the lines that set impressum/datenschutz accordion content (lines 558-576 approximately):
```js
// Remove these lines:
setText('impressum-title', t.impressumTitle);
setText('impressum-angaben', t.impressumAngaben);
setText('impressum-note', t.impressumNote);
setPreText('impressum-placeholder', t.impressumPlaceholder);
setText('datenschutz-title', t.datenschutzTitle);
setText('datenschutz-art13-title', t.datenschutzArt13Title);
setText('datenschutz-art13', t.datenschutzArt13);
setText('datenschutz-hosting-title', t.datenschutzHostingTitle);
setText('datenschutz-hosting', t.datenschutzHosting);
setText('datenschutz-payment-title', t.datenschutzPaymentTitle);
setText('datenschutz-payment', t.datenschutzPayment);
setText('datenschutz-storage-title', t.datenschutzStorageTitle);
setText('datenschutz-storage', t.datenschutzStorage);
setText('datenschutz-rights-title', t.datenschutzRightsTitle);
setText('datenschutz-rights', t.datenschutzRights);
setText('datenschutz-note', t.datenschutzNote);
```

These elements no longer exist in landing.html. The `setText` calls would harmlessly no-op (they check for element existence), but removing them keeps code clean.

Also remove the corresponding i18n keys from the `LANDING_I18N` object for all 4 languages: `impressumTitle`, `impressumAngaben`, `impressumNote`, `impressumPlaceholder`, `datenschutzTitle`, `datenschutzArt13Title`, `datenschutzArt13`, `datenschutzHostingTitle`, `datenschutzHosting`, `datenschutzPaymentTitle`, `datenschutzPayment`, `datenschutzStorageTitle`, `datenschutzStorage`, `datenschutzRightsTitle`, `datenschutzRights`, `datenschutzNote`. Keep `footerImpressum` and `footerPrivacy` — those are still used for footer link text.

Also add `?lang=` parameter to footer legal links. In `applyLang`, after the footer text updates, add:
```js
var impLink = document.getElementById('footer-impressum-link');
if (impLink) impLink.href = './impressum.html?lang=' + lang;
var privLink = document.getElementById('footer-privacy-link');
if (privLink) privLink.href = './datenschutz.html?lang=' + lang;
```

**Service worker update**

In `sw.js`, add to the `PRECACHE_URLS` array:
```js
'/impressum.html',
'/datenschutz.html',
```

Also bump `CACHE_NAME` from `'sessions-garden-v14'` to `'sessions-garden-v15'` to trigger cache refresh.

Also remove the `.legal-accordion` and `.legal-details` CSS rules from `assets/landing.css` if they exist (search for `legal-accordion`, `legal-details`, `legal-summary`, `legal-details-content`).
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && ! grep 'legal-accordion' landing.html && ! grep 'footer-impressum-toggle' landing.html && grep 'impressum.html' landing.html && grep 'datenschutz.html' landing.html && grep 'impressum.html' sw.js && grep 'datenschutz.html' sw.js && grep 'sessions-garden-v15' sw.js && echo "Footer + SW update: OK"</automated>
  </verify>
  <acceptance_criteria>
    - landing.html does NOT contain `legal-accordion` div or any `<details>` legal sections
    - landing.html does NOT contain `#footer-impressum-toggle` or `#footer-privacy-toggle` anchor hrefs
    - landing.html footer links point to `./impressum.html` and `./datenschutz.html`
    - assets/landing.js `applyLang` does NOT contain `setText('impressum-title'` or any impressum/datenschutz content setters
    - assets/landing.js `applyLang` DOES contain href updates for `footer-impressum-link` and `footer-privacy-link` with `?lang=` parameter
    - assets/landing.js `LANDING_I18N` objects no longer contain `impressumTitle`, `datenschutzTitle`, etc. keys
    - assets/landing.js `LANDING_I18N` objects STILL contain `footerImpressum` and `footerPrivacy` keys
    - sw.js `PRECACHE_URLS` includes `/impressum.html` and `/datenschutz.html`
    - sw.js `CACHE_NAME` is `'sessions-garden-v15'`
  </acceptance_criteria>
  <done>Legal accordion removed from landing.html (~168 lines deleted); footer links point to dedicated pages with lang param; service worker caches new pages; i18n keys cleaned up</done>
</task>

</tasks>

<verification>
- Footer "Impressum" link opens impressum.html (not an accordion)
- Footer "Privacy Policy" link opens datenschutz.html (not an accordion)
- Both new pages detect language from localStorage and display correct heading/direction
- In Hebrew mode, footer links show Hebrew labels and new pages render RTL
- Service worker precaches both new pages (check PRECACHE_URLS)
- landing.html is ~168 lines shorter (accordion content removed)
</verification>

<success_criteria>
- Impressum and Datenschutz are standalone pages, not inline accordions
- Footer links navigate to dedicated pages with language parameter
- Both new pages follow the disclaimer.html pattern (standalone, themed, language-aware)
- Service worker updated to cache new pages with bumped cache version
- No orphaned i18n keys in LANDING_I18N for removed accordion content
</success_criteria>

<output>
After completion, create `.planning/phases/14-i18n-bugs-legal-footer-cleanup-and-contact-email-update/14-03-SUMMARY.md`
</output>
