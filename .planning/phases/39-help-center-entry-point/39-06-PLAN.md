---
phase: 39-help-center-entry-point
plan: 06
type: execute
wave: 3
depends_on: [39-01, 39-03, 39-04]
files_modified:
  - sw.js
  - assets/version.js
  - tests/39-help-precache.test.js
autonomous: false
requirements: [HELP-07, HELP-04]
user_setup:
  - service: installed-pwa
    why: "HELP-07 requires verifying Help opens fully offline on a REAL installed PWA (Add to Dock / installed Chrome), which cannot be exercised by the zero-dep jsdom/vm harness (Pitfall 2 — WebKit-only stale-SW failures are invisible to Chromium)."
must_haves:
  truths:
    - "sw.js precaches the new help assets: /help added to PRECACHE_HTML (extensionless, CF pretty-URL convention) and /assets/help.js + /assets/help-content-en.js + /assets/help.css added to PRECACHE_URLS."
    - "The static precache still fetches with reload cache-mode (the stale-proofing guarded by tests/sw-precache-cache-reload.test.js is preserved, not regressed)."
    - "A CACHE_NAME/INTEGRITY_TOKEN refresh ships so installed PWAs re-precache the new files (the pre-commit hook skips the auto-bump when sw.js is in the diff — a manual follow-up chore is required)."
    - "A static test asserts the three help assets + the /help HTML entry are present in the precache lists."
    - "Help opens fully offline on a real installed PWA (network off, relaunch) — the '?' → Help center path and deep-links work with no connection."
    - "The rendered help.html passes Sapir's content review (D-19 step 5) and the native-speaker + App-DNA review gates (D-19 gates B/C, Sonnet tier per D-20); help surfaces render in real WebKit with correct RTL mirroring, dark mode, and soft-type Rubik (not system fallback)."
  artifacts:
    - sw.js
    - assets/version.js
    - tests/39-help-precache.test.js
  key_links:
    - "sw.js precache references the exact new paths produced by Plan 04 (help.html→/help, assets/help.js, assets/help.css) and Plan 01 (assets/help-content-en.js)."
    - "CACHE_NAME derives from AppVersion.INTEGRITY_TOKEN (assets/version.js) — the version bump is the mechanism that rolls the cache for installed apps."
  prohibitions:
    - statement: "The static precache must NOT revert to the stale-prone bare add form — the reload-mode fetch stays (guarded by tests/sw-precache-cache-reload.test.js)."
      status: resolved
      verification: "tests/sw-precache-cache-reload.test.js still exits 0 after the sw.js edit."
    - statement: "landing.html / demo are not added to the help precache scope, and no new fetch origins are introduced."
      status: resolved
      verification: "Diff adds only the three help assets + /help; no other precache/origin changes."
---

<objective>
Ship Help offline and close the phase's human-verification gates: add the help page + its assets to the service-worker precache (with the required CACHE_NAME refresh chore), guard the precache with a static test, then run the three checkpoints that only a human/real device can clear — real-offline navigation on an installed PWA (HELP-07), and the D-19 content review (Sapir + native/DNA gates) plus the real-WebKit RTL/dark/soft-type visual verification of the rendered help surfaces (HELP-04, Pitfalls 2/5/6).

Purpose: HELP-07 (offline) is the highest-risk delivery step in this phase (SW precache + stale-SW on installed Safari are documented repo failure modes), and HELP-04's content only counts once Sapir has reviewed exactly what practitioners see.

Output: sw.js precache + version chore + precache test, then signed-off offline + content + visual checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-RESEARCH.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
@.planning/SessionsGarden-DNA-EN.md
</context>

## Artifacts this phase produces (this plan)

New entries in sw.js `PRECACHE_URLS` (assets/help.js, assets/help-content-en.js, assets/help.css) and `PRECACHE_HTML` (/help); a bumped `APP_VERSION` in assets/version.js (release-moment cache refresh). New file: `tests/39-help-precache.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Add help assets to SW precache + CACHE_NAME refresh chore + precache test</name>
  <files>sw.js, assets/version.js, tests/39-help-precache.test.js</files>
  <read_first>
    - sw.js lines 26–128 (CACHE_NAME derivation from AppVersion.INTEGRITY_TOKEN; PRECACHE_URLS array; PRECACHE_HTML array with the extensionless CF pretty-URL convention; precacheHtml redirect-safe pattern)
    - sw.js lines 149–172 (install: the reload-mode static fetch loop + htmlPromises — the stale-proofing to preserve)
    - assets/version.js lines 22–31 (APP_VERSION hand-set semver = '1.2.5' currently; INTEGRITY_TOKEN deploy-stamped git short-hash that CACHE_NAME derives from)
    - tests/sw-precache-cache-reload.test.js (the reload-mode regression guard to keep green)
    - .planning/phases/39-help-center-entry-point/39-RESEARCH.md Pitfall 1 + memory reference-pre-commit-sw-bump.md (why PRECACHE_URLS edits need a manual version/CACHE_NAME chore)
  </read_first>
  <action>
Edit sw.js: add `/assets/help.js`, `/assets/help-content-en.js`, `/assets/help.css` to `PRECACHE_URLS` (the Rubik faces + watering-can.png are already precached), and add `/help` to `PRECACHE_HTML` (extensionless, matching the /sessions, /settings entries). Do NOT change the install-time static fetch mode — it must keep using the reload cache-mode that bypasses the HTTP cache (the anti-stale guard). Introduce no new fetch origins; add nothing besides the three help assets + /help.

Then perform the CACHE_NAME refresh chore: bump `APP_VERSION` in assets/version.js to the next release value for v1.3's first shipped phase (exact semver is Ben's call at the phase-close checkpoint — propose `1.3.0`), and ensure the change ships as a follow-up such that a fresh INTEGRITY_TOKEN/CACHE_NAME rolls for installed PWAs (per reference-pre-commit-sw-bump.md the pre-commit hook skips the auto-bump when sw.js is in the diff, so the version bump is the deliberate refresh signal). Note the two-step nature (precache edit + version chore) in the summary.

Create `tests/39-help-precache.test.js` (fs source-scan, model: tests/sw-precache-cache-reload.test.js): read sw.js and assert `/assets/help.js`, `/assets/help-content-en.js`, `/assets/help.css` appear in PRECACHE_URLS and `/help` appears in PRECACHE_HTML, AND re-assert the reload-mode static fetch is still present (so this test also fails if the anti-stale form is reverted). Exit 0/1 per run-all.
  </action>
  <verify>
    <automated>node tests/39-help-precache.test.js && node tests/sw-precache-cache-reload.test.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js PRECACHE_URLS contains the three help assets; PRECACHE_HTML contains /help.
    - The reload-mode static precache fetch is unchanged (tests/sw-precache-cache-reload.test.js exits 0).
    - assets/version.js APP_VERSION is bumped (proposed 1.3.0) as the cache-refresh signal.
    - `node tests/39-help-precache.test.js` exits 0; `npm test` runs the whole suite green.
  </acceptance_criteria>
  <done>Help assets + page are in the SW precache, the anti-stale guard is intact, the version bump refreshes the cache, and a static precache test locks it in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real offline navigation on an installed PWA (HELP-07)</name>
  <files>(verification only — no files modified)</files>
  <action>Blocking human-verify checkpoint — no code changes. A human runs the real-device offline verification steps below on an installed PWA (Safari included) because the zero-dep jsdom/vm harness cannot exercise a real service worker + offline navigation (Pitfall 2).</action>
  <what-built>help.html + assets/help.js/help.css/help-content-en.js are now in the SW precache (with a rolled CACHE_NAME); the "?" entry (Plan 03) opens the Help center.</what-built>
  <how-to-verify>
    1. Deploy (or serve locally over the SW) so the new CACHE_NAME installs; confirm the app updates (footer version shows the bumped APP_VERSION).
    2. On an INSTALLED PWA (macOS Safari "Add to Dock" AND, if available, installed Chrome/Edge), relaunch so the new SW activates.
    3. Turn the network OFF (airplane mode / disable Wi-Fi).
    4. Open the app, click the "?" → "Help center". Confirm help.html loads fully offline: rail + cards render, search works, a deep-link (e.g. ./help.html#adding-a-client from the Overview empty state) opens the right card, and no asset 404s in the console.
    5. Specifically test Safari (WebKit) — Chromium passing does NOT prove Safari (Pitfall 2). If Help is stale/blank, that's the stale-SW failure mode: confirm caches.keys() shows the new CACHE_NAME.
  </how-to-verify>
  <resume-signal>Type "approved" once Help opens fully offline on the installed PWA (Safari included), or describe what failed (404s, stale content, blank page).</resume-signal>
  <verify><human-check>Help opens fully offline (rail/cards/search/deep-link work, no 404s) on an installed PWA including real Safari/WebKit.</human-check></verify>
  <done>HELP-07 field-verified: Help is fully usable offline on an installed PWA, Safari included.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-19 content review (Sapir + native/DNA gates) + real-WebKit RTL/dark/soft-type check (HELP-04)</name>
  <files>(verification only — no files modified; any wording/visual fixes land as follow-up edits in the owning plan)</files>
  <action>Blocking human-verify checkpoint — no code changes here. Run the D-19 content-review pipeline against the RENDERED help.html (Gate A factual, Gate B native-speaker + Gate C App-DNA at Sonnet tier per D-20, then Sapir sign-off) and the real-WebKit visual verification below. Language-facing gate agents run at Sonnet level (D-20).</action>
  <what-built>The rendered help.html with full EN content (Plan 01), soft-type help surfaces, and the "?" entry — the exact surface practitioners see.</what-built>
  <how-to-verify>
    1. Content — D-19 pipeline sign-off on the RENDERED page (not the raw JS): confirm Gate A (every P1/P2 step matches the live app; quoted labels are correct via {ui:key}), Gate B (native-quality EN, calm garden voice — Sonnet-tier review per D-20), Gate C (terminology is Session Format / Heart-Wall / client / session everywhere — never patient/treatment; same word for the same thing across topics). Then Sapir reads help.html locally in a browser and approves or lists fixes.
    2. Visual (real WebKit, not jsdom) — open help.html in Safari: verify the rail, chevrons, and botanical accent mirror correctly in Hebrew RTL (switch language via the globe); dark mode renders (botanical invert+screen, tokens resolve, no dark blob); soft-type headings render from the real Rubik faces (400/600/700), NOT a system fallback (Pitfall 5 — compare heading metric to app body).
    3. Confirm the search no-match state shows the calm "write to us" fallback and the contact band renders the mailto + report-a-problem hint.
  </how-to-verify>
  <resume-signal>Type "approved" once Sapir + the native/DNA review pass and the WebKit RTL/dark/soft-type checks are clean, or list the wording/visual fixes to land first.</resume-signal>
  <verify><human-check>Sapir + native/DNA content gates pass on the rendered page; help surfaces render correctly in real Safari for RTL, dark mode, and soft-type Rubik (not system fallback).</human-check></verify>
  <done>HELP-04 content is signed off on the rendered help.html and the WebKit RTL/dark/soft-type visual checks are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker cache → app | The SW serves precached help assets to the installed app offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-10 | Tampering | stale/poisoned SW cache serving old help code | medium | mitigate | Preserve reload-mode static precache (bypasses HTTP cache) + roll CACHE_NAME via the APP_VERSION bump so installed PWAs re-precache; guarded by tests/sw-precache-cache-reload.test.js + tests/39-help-precache.test.js and field-verified offline on real Safari (Task 2). |
| T-39-11 | Denial of Service | help unavailable offline (missing precache entry) | low | mitigate | Static precache test asserts all four help entries present; real-offline checkpoint confirms end-to-end. |

No package-manager installs occur in this plan (zero new deps); T-39-SC not applicable.
</threat_model>

<verification>
- `node tests/39-help-precache.test.js` + `node tests/sw-precache-cache-reload.test.js` exit 0; full `npm test` green.
- Task 2 checkpoint: Help opens fully offline on an installed PWA (Safari included).
- Task 3 checkpoint: Sapir + native/DNA content review pass; WebKit RTL/dark/soft-type clean.
</verification>

<success_criteria>
- HELP-07 delivered and field-verified offline; HELP-04 content signed off on the rendered page; the SW precache + anti-stale guard are locked by tests.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-06-SUMMARY.md` when done.
</output>
