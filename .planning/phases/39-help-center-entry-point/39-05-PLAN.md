---
phase: 39-help-center-entry-point
plan: 05
type: execute
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - assets/overview.js
  - assets/sessions.js
  - assets/reporting.js
  - reporting.html
  - tests/39-empty-state-coaching.test.js
autonomous: true
requirements: [HELP-05]
must_haves:
  truths:
    - "The first-run trio each shows a soft, non-accent 'Show me how' secondary button below the existing calm empty sentence, deep-linking into the matching help topic: Overview no-clients → ./help.html#adding-a-client, Sessions no-sessions-at-all → ./help.html#starting-a-session, Reporting no-data → ./help.html#overview."
    - "Sessions distinguishes TRUE-empty (zero sessions in the DB) from FILTER-empty (sessions exist but the current filter matches none): coaching + button appear ONLY on true-empty; the existing filter-empty message (sessions.empty) is unchanged and shows no coaching button."
    - "Reporting shows a true no-data coaching state (zero sessions / zero clients) with the button; when data exists the stat grid renders normally with no coaching."
    - "Empty-state buttons + coaching sentences are built via createElement + textContent using help.deeplink.cta / help.deeplink.startSession / help.deeplink.readDashboard (Plan 02); Overview reuses its existing overview.clients.empty sentence."
    - "The three deep-link anchors match window.HELP_DEEPLINKS (Plan 01): adding-a-client, starting-a-session, overview."
  artifacts:
    - assets/overview.js
    - assets/sessions.js
    - assets/reporting.js
    - reporting.html
    - tests/39-empty-state-coaching.test.js
  key_links:
    - "Deep-link hrefs (./help.html#<section-id>) resolve to real help.html cards (Plan 04) and section ids validated by the Plan 01 integrity test via HELP_DEEPLINKS."
    - "Coaching labels come from the i18n keys added in Plan 02."
  prohibitions:
    - statement: "The 'Show me how' button uses a soft/non-accent secondary style — it never competes with the app's accent (D-22); no --color-primary CTA."
      status: resolved
      verification: "Button uses a neutral/surface button class (e.g. button ghost), not the primary accent; code review."
    - statement: "Coaching does NOT appear on filter-empty Sessions (sessions exist but hidden by a filter) — only on true zero-sessions (Pitfall 3)."
      status: resolved
      verification: "jsdom test: with sessions present but filtered out, the coaching button is absent; with zero sessions it is present."
    - statement: "Buttons/sentences built via createElement + textContent, never innerHTML with interpolated strings."
      status: resolved
      verification: "Code review + test asserts DOM nodes (not innerHTML string) for the coaching block."
---

<objective>
Wire the first-run empty-state coaching deep-links (HELP-05, D-21/D-22): a soft "Show me how" secondary button under the calm empty sentence on the three first-run journey states — Overview (no clients), Sessions (no sessions at all), Reporting (no data) — each deep-linking into the matching help topic with its card auto-expanded.

Purpose: coach a stuck first-run practitioner into the right help topic exactly where intent is unambiguous. The one non-trivial bit is distinguishing Sessions' TRUE-empty from its existing FILTER-empty (the current `sessions.empty` string is a filter-empty).

Output: coaching buttons on overview.js / sessions.js / reporting.js (+ reporting.html markup), guarded by a jsdom behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-RESEARCH.md
@.planning/phases/39-help-center-entry-point/39-PATTERNS.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

New symbols: a coaching-block builder (button + optional sentence) in each of overview.js / sessions.js / reporting.js; a Sessions true-empty vs filter-empty branch; a Reporting no-data coaching container in reporting.html (e.g. `#reportingEmpty`). New file: `tests/39-empty-state-coaching.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overview + Sessions coaching (with true-empty split)</name>
  <files>assets/overview.js, assets/sessions.js</files>
  <read_first>
    - assets/overview.js lines 638–648 (renderClientRows — the genuine no-clients branch that shows #emptyState when !clients.length; where the CTA button attaches)
    - assets/sessions.js lines 130–172 (renderSessions — sessions = getAllSessions(); the filter chain; the current !filtered.length branch that shows the filter-empty #sessionsEmpty)
    - index.html line ~164 (#emptyState container markup) and sessions.html line ~84 (#sessionsEmpty container markup)
    - assets/i18n-en.js (overview.clients.empty exists; help.deeplink.cta + help.deeplink.startSession added in Plan 02)
    - assets/app.js (App.t; overview.js/lang re-render convention on app:language)
  </read_first>
  <behavior>
    - Overview: when clients.length === 0, #emptyState shows the existing overview.clients.empty sentence AND a "Show me how" (help.deeplink.cta) secondary button linking ./help.html#adding-a-client. When clients exist, no coaching.
    - Sessions TRUE-empty: when the unfiltered total session count === 0, show a coaching block = help.deeplink.startSession sentence + a "Show me how" button → ./help.html#starting-a-session.
    - Sessions FILTER-empty: when total sessions > 0 but filtered.length === 0, show the existing sessions.empty message ONLY — no coaching button.
    - The buttons are soft/non-accent (button ghost style) and built via createElement + textContent.
  </behavior>
  <action>
Overview (overview.js, renderClientRows no-clients branch): when `!clients.length`, in addition to showing `#emptyState` (existing overview.clients.empty), append a soft secondary button (createElement, class `button ghost` or the repo's non-accent secondary class) with textContent `App.t('help.deeplink.cta')` and href/navigation to `./help.html#adding-a-client`. Guard against double-append on re-render (check for an existing coaching button before adding, or clear the container). Ensure it re-localizes on app:language along the existing re-render path.

Sessions (sessions.js, renderSessions): compute a true-empty signal from the UNFILTERED source — the `sessions` array length from `getAllSessions()` BEFORE the filter (`const totalSessions = sessions.length;`). Branch:
- `totalSessions === 0` (true-empty): show a coaching block containing `App.t('help.deeplink.startSession')` + a soft "Show me how" button → `./help.html#starting-a-session`.
- `totalSessions > 0 && !filtered.length` (filter-empty): keep the existing `#sessionsEmpty` (sessions.empty) message exactly as-is — no coaching button.
Implement without altering the filter-empty copy or its trigger. Detection approach (a dedicated true-empty container vs reusing #sessionsEmpty with a conditional child) is your discretion; keep the filter-empty message untouched. Build the coaching block via createElement + textContent.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const o=fs.readFileSync('assets/overview.js','utf8');const s=fs.readFileSync('assets/sessions.js','utf8');if(!/help\.html#adding-a-client/.test(o))throw new Error('overview deep-link missing');if(!/help\.deeplink\.cta/.test(o))throw new Error('overview cta key missing');if(!/help\.html#starting-a-session/.test(s))throw new Error('sessions deep-link missing');if(!/help\.deeplink\.startSession/.test(s))throw new Error('sessions true-empty key missing');console.log('OK overview+sessions wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - overview.js appends a soft "Show me how" (help.deeplink.cta) button → ./help.html#adding-a-client in the no-clients branch, without double-appending on re-render.
    - sessions.js derives total session count pre-filter and shows coaching (help.deeplink.startSession + button → ./help.html#starting-a-session) ONLY on true-empty; the filter-empty sessions.empty message is unchanged.
    - Both buttons are non-accent secondary and built via createElement + textContent.
  </acceptance_criteria>
  <done>Overview and Sessions coach the first-run user into the right topic, with Sessions correctly distinguishing true-empty from filter-empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reporting no-data coaching</name>
  <files>assets/reporting.js, reporting.html</files>
  <read_first>
    - assets/reporting.js (renderReporting: computes totalClients/totalSessions; renders "-" placeholders when empty; the app:language re-render listener)
    - reporting.html lines ~50–78 (the Reporting card + stat-grid; where a no-data coaching container mounts)
    - assets/i18n-en.js (help.deeplink.readDashboard + help.deeplink.cta from Plan 02)
    - assets/overview.js empty-state pattern (createElement + textContent secondary button, for consistency)
  </read_first>
  <behavior>
    - When Reporting has no data (totalSessions === 0, i.e. nothing to aggregate), a coaching block shows help.deeplink.readDashboard + a "Show me how" button → ./help.html#overview, and the empty stat grid is de-emphasized/hidden.
    - When sessions exist, the stat grid renders as today and no coaching appears.
  </behavior>
  <action>
Add a `#reportingEmpty` container to reporting.html (hidden by default, near the stat-grid, following the helper-text/empty-state markup convention). In reporting.js's renderReporting, after computing `totalSessions`, branch: when `totalSessions === 0`, show `#reportingEmpty` with `App.t('help.deeplink.readDashboard')` + a soft "Show me how" button → `./help.html#overview` (createElement + textContent), and hide (or leave zeroed) the stat grid; otherwise hide `#reportingEmpty` and render stats as today. Re-localize on the existing app:language path. Choose "no data" as `totalSessions === 0` (the dashboard aggregates sessions); document the choice in a comment.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('assets/reporting.js','utf8');const h=fs.readFileSync('reporting.html','utf8');if(!/help\.html#overview/.test(r))throw new Error('reporting deep-link missing');if(!/help\.deeplink\.readDashboard/.test(r))throw new Error('reporting key missing');if(!/reportingEmpty/.test(h)||!/reportingEmpty/.test(r))throw new Error('reportingEmpty container not wired');console.log('OK reporting wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - reporting.html has a `#reportingEmpty` container; reporting.js shows it (with help.deeplink.readDashboard + button → ./help.html#overview) only when totalSessions === 0.
    - When data exists, `#reportingEmpty` is hidden and the stat grid renders as before.
    - Button is non-accent secondary, built via createElement + textContent.
  </acceptance_criteria>
  <done>Reporting coaches the no-data first-run user to the dashboard help topic.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom empty-state coaching behavior test</name>
  <files>tests/39-empty-state-coaching.test.js</files>
  <read_first>
    - tests/30-04-settings-characterization.test.js (jsdom bootstrap that executes a real assets/*.js with a stubbed PortfolioDB + built DOM)
    - assets/sessions.js + assets/overview.js + assets/reporting.js (the branches under test)
    - assets/i18n-en.js (seed window.I18N.en so App.t resolves the coaching keys)
  </read_first>
  <action>
Create `tests/39-empty-state-coaching.test.js` using the jsdom harness with a stubbed `PortfolioDB`. Cover:
1. Overview no-clients: getAllClients → [] → renderClientRows shows #emptyState and a "Show me how" button whose href/target is ./help.html#adding-a-client.
2. Sessions TRUE-empty: getAllSessions → [] → renderSessions shows coaching (help.deeplink.startSession text present) + button → ./help.html#starting-a-session.
3. Sessions FILTER-empty (the critical Pitfall-3 guard): getAllSessions → [one session] but a filter set so filtered.length === 0 → the coaching button is ABSENT and the existing filter-empty message is shown. (Set the client/date/format filter to exclude the one session.)
4. Reporting no-data: getAllSessions → [] → #reportingEmpty shown with button → ./help.html#overview; with one session present → #reportingEmpty hidden.
Exit 0/1 per run-all. Assert on DOM nodes (querySelector on the built elements), not source strings.
  </action>
  <verify>
    <automated>node tests/39-empty-state-coaching.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/39-empty-state-coaching.test.js` exits 0 with PASS lines for the four cases including the filter-empty negative.
    - Making the Sessions branch fire coaching on filter-empty makes case 3 fail (falsifiable — verify, then revert).
    - `npm test` runs it green.
  </acceptance_criteria>
  <done>The trio's coaching + the true-empty/filter-empty distinction are covered by an automated behavior test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render path → DOM | Coaching sentence + button injected into empty-state containers; labels from i18n dict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-09 | Tampering | empty-state coaching DOM | low | mitigate | Sentence + button built with createElement + textContent from i18n dict values; no user input, no innerHTML. |

No package-manager installs occur in this plan; T-39-SC not applicable.
</threat_model>

<verification>
- Both wiring smoke-checks pass; `node tests/39-empty-state-coaching.test.js` exits 0; `npm test` green.
</verification>

<success_criteria>
- HELP-05 delivered: the first-run trio coaches into the matching help topic; Sessions correctly fires only on true-empty; deep-link anchors match HELP_DEEPLINKS.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-05-SUMMARY.md` when done.
</output>
