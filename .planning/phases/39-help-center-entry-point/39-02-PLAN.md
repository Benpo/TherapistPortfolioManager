---
phase: 39-help-center-entry-point
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [HELP-01, HELP-02, HELP-05]
must_haves:
  truths:
    - "New UI-chrome i18n keys exist with EN canonical values and full 4-locale parity (en/he/de/cs): nav.help, help.entry.label, help.entry.center, help.entry.contact, help.page.title, help.search.placeholder, help.search.noMatch, help.search.writeToUs, help.deeplink.cta, help.deeplink.startSession, help.deeplink.readDashboard."
    - "Every EN key added also exists in he.js, de.js, and cs.js (no fallback, no missing key) — verified by the existing tests/25-11-i18n-parity.test.js parity gate."
    - "Hebrew values use noun/infinitive forms (Phase 24 D-05), not masculine imperatives."
  artifacts:
    - assets/i18n-en.js
    - assets/i18n-he.js
    - assets/i18n-de.js
    - assets/i18n-cs.js
  key_links:
    - "help.entry.* + nav.help consumed by the '?' header entry (Plan 03)."
    - "help.page.title + help.search.* consumed by help.html/help.js (Plan 04)."
    - "help.deeplink.* consumed by the empty-state coaching trio (Plan 05)."
  prohibitions:
    - statement: "No emoji in any new UI-chrome string (project-wide copy rule)."
      status: resolved
      verification: "Manual review of added values; no pictographic code points."
    - statement: "Help BODY content is NOT added to the i18n files — only UI-chrome strings (help body is EN-only in help-content-en.js, D-18)."
      status: resolved
      verification: "Only the 11 chrome keys above are added; no topic/body copy keys."
---

<objective>
Add the small set of new UI-chrome i18n keys (menu labels, page title, search chrome, empty-state coaching) with full 4-locale parity, so Plans 03/04/05 can consume them without touching the i18n files (this isolates the shared-file bottleneck into Wave 1).

Purpose: `assets/i18n-{en,he,de,cs}.js` is edited by three downstream surfaces (header, page, empty-state). Front-loading every new key here lets those three run in parallel in Wave 2 as read-only consumers.

Output: 11 new keys × 4 locales, passing the standing i18n parity gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

New i18n keys (chrome only): `nav.help`, `help.entry.label`, `help.entry.center`, `help.entry.contact`, `help.page.title`, `help.search.placeholder`, `help.search.noMatch`, `help.search.writeToUs`, `help.deeplink.cta`, `help.deeplink.startSession`, `help.deeplink.readDashboard` — added to all four locale dictionaries.

<tasks>

<task type="auto">
  <name>Task 1: Add the new UI-chrome keys to EN with canonical values</name>
  <files>assets/i18n-en.js</files>
  <read_first>
    - assets/i18n-en.js (existing key blocks + placement conventions: nav.* block near top, section grouping style)
    - .planning/phases/39-help-center-entry-point/39-UI-SPEC.md (Copywriting Contract table — the exact EN canonical draft strings and their key homes)
  </read_first>
  <action>
Add these keys to `window.I18N.en` in assets/i18n-en.js (place `nav.help` in the nav block; group the `help.*` keys together in a new labeled block):
- `nav.help` = "Help"
- `help.entry.label` = "Help"  (aria-label / tooltip for the "?" affordance)
- `help.entry.center` = "Help center"  ("?" popover primary item; opens help.html)
- `help.entry.contact` = "Contact us"  ("?" popover second item; mailto)
- `help.page.title` = "Help center"  (help.html Display heading)
- `help.search.placeholder` = "Search help…"
- `help.search.noMatch` = "Nothing here matches your search yet. We’re happy to help directly —"  (the "write to us" link is appended by help.js using the next key)
- `help.search.writeToUs` = "write to us"
- `help.deeplink.cta` = "Show me how"  (empty-state secondary button, all three states)
- `help.deeplink.startSession` = "No sessions yet — start your first session to begin."  (Sessions TRUE-empty coaching; distinct from the existing filter-empty sessions.empty)
- `help.deeplink.readDashboard` = "No data yet. Once you record sessions, your dashboard fills in here."  (Reporting no-data coaching)

Overview reuses its existing `overview.clients.empty` sentence — do NOT add a help.deeplink.addClient sentence key (documented decision: overview keeps its current empty string; Plan 05 only adds the CTA button under it).
  </action>
  <verify>
    <automated>node -e "global.window={};require('./assets/i18n-en.js');const en=window.I18N.en;['nav.help','help.entry.label','help.entry.center','help.entry.contact','help.page.title','help.search.placeholder','help.search.noMatch','help.search.writeToUs','help.deeplink.cta','help.deeplink.startSession','help.deeplink.readDashboard'].forEach(k=>{if(!en[k])throw new Error('missing EN key '+k)});console.log('OK EN keys')"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 keys exist in `window.I18N.en` with the exact canonical values above.
    - `nav.help` sits in the nav block; the `help.*` keys are grouped.
    - No emoji; no help topic/body copy keys added.
  </acceptance_criteria>
  <done>EN canonical chrome keys are present and grouped.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the keys into HE, DE, CS with parity</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (the 11 keys + values just added — the source of truth for parity)
    - assets/i18n-he.js (Hebrew noun/infinitive convention, RTL; how existing help-adjacent chrome is phrased)
    - assets/i18n-de.js (existing chrome tone; formal register)
    - assets/i18n-cs.js (existing chrome tone)
    - tests/25-11-i18n-parity.test.js (the bidirectional parity gate this must satisfy)
  </read_first>
  <action>
Add the same 11 keys to `window.I18N.he`, `window.I18N.de`, and `window.I18N.cs` with natural translations. Hebrew MUST use noun/infinitive forms (Phase 24 D-05): e.g. help.entry.center → "מרכז העזרה", help.entry.contact → "צרו קשר", help.deeplink.cta → a soft "הראו לי איך"-style phrase in the app's voice. DE/CS follow their existing formal register (e.g. help.entry.center DE "Hilfe-Center", CS "Centrum nápovědy"). Keep parity EXACT — every EN key present in all three, no extra keys. These are chrome strings; quality is Sapir/native review's job downstream, but the key STRUCTURE must be complete now so the parity gate passes.
  </action>
  <verify>
    <automated>node -e "global.window={};['en','he','de','cs'].forEach(l=>require('./assets/i18n-'+l+'.js'));const I=window.I18N;const ks=['nav.help','help.entry.label','help.entry.center','help.entry.contact','help.page.title','help.search.placeholder','help.search.noMatch','help.search.writeToUs','help.deeplink.cta','help.deeplink.startSession','help.deeplink.readDashboard'];['he','de','cs'].forEach(l=>ks.forEach(k=>{if(!I[l][k])throw new Error('missing '+l+' '+k)}));console.log('OK 4-locale parity')"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 keys exist in he/de/cs with non-empty values.
    - `node tests/25-11-i18n-parity.test.js` exits 0 (bidirectional parity preserved).
    - Hebrew values are noun/infinitive, not masculine imperatives.
  </acceptance_criteria>
  <done>4-locale parity holds; the parity gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → DOM | Chrome strings are rendered via data-i18n/textContent by existing plumbing; no interpolation of user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-03 | Tampering | i18n string values | low | accept | Static authored strings rendered through the existing data-i18n/textContent seam; no dynamic/user content, no markup injection surface. |

No package-manager installs occur in this plan; T-39-SC not applicable.
</threat_model>

<verification>
- Both node smoke-checks pass.
- `node tests/25-11-i18n-parity.test.js` exits 0.
</verification>

<success_criteria>
- 11 new chrome keys present with 4-locale parity; downstream plans consume them read-only.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-02-SUMMARY.md` when done.
</output>
