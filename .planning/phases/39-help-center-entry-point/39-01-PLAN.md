---
phase: 39-help-center-entry-point
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/help-content-en.js
  - tests/39-help-integrity.test.js
autonomous: true
requirements: [HELP-03, HELP-04, HELP-05, HELP-06]
must_haves:
  truths:
    - "assets/help-content-en.js exports one global (window.HELP_CONTENT_EN) — an ordered array of section objects covering every leaf of the Phase 32 topic tree plus the post-inventory delta (session formats incl. custom, date-format personalization, filters/sorting, next-session date, report-a-problem, updates)."
    - "The content is organized along the 7-step workflow spine, with the featured personalization section (featured:true) first and a clearly-separated technical-track group ('technical')."
    - "Every quoted on-screen label in a P1/P2 step is a {ui:key} token (not literal prose), and every {ui:key} resolves to a real key in window.I18N.en."
    - "window.HELP_DEEPLINKS maps addClient/startSession/readDashboard to real section ids (adding-a-client / starting-a-session / overview) — the empty-state trio anchor contract."
    - "Install content is computer-only (Chrome/Edge + macOS Safari Add to Dock) plus one expectation-setting mobile note — never iOS/Android install legs (D-15/D-16)."
    - "node tests/39-help-integrity.test.js exits 0 — validating {ui:key} resolution, unique non-empty ids, deep-link registry resolution, section/group coverage, per-topic covers metadata, computer-only install, and terminology."
  artifacts:
    - assets/help-content-en.js
    - tests/39-help-integrity.test.js
  key_links:
    - "help-content-en.js topic/section ids ARE the deep-link anchors consumed by empty-state buttons (Plan 05) and the rail/cards (Plan 04)."
    - "{ui:key} tokens resolve against window.I18N.en (assets/i18n-en.js) — the live-label seam (D-23)."
  prohibitions:
    - statement: "No help topic is sourced from demo.html / demo-seed.js / demo-hints.js (stale — NOT a content source)."
      status: resolved
      verification: "Content grounded only in live app pages + assets/i18n-en.js + the Phase 32 inventory; reviewer/Gate-A grounding check."
    - statement: "No emoji appears in any topic title or body text (project-wide copy rule)."
      status: resolved
      verification: "Integrity test scans titles/body for emoji code points → 0."
    - statement: "The word 'patient' never appears in body copy (terminology must be client/session, Session Format, Heart-Wall — never patient/treatment)."
      status: resolved
      verification: "Integrity test negative-matches /\\bpatient\\b/i across all body text → 0 hits."
    - statement: "No universal / single 'Install' instruction and no iOS/Android install topic (computer-only per D-15)."
      status: resolved
      verification: "Integrity test asserts the install section topic ids are chrome + safari(Add to Dock) + mobile-note and exclude ios/android ids."
---

<objective>
Author the anti-rot help-content substrate: a single structured content module (`assets/help-content-en.js`) carrying the full EN help corpus organized on the 7-step workflow spine, with live-label `{ui:key}` interpolation tokens (D-23), per-topic `covers` metadata (D-24), a deep-link registry, and a co-located static integrity test (D-25) that joins `npm test`.

Purpose: This module is the single source of truth for every help topic id, anchor, and covered file. Plans 04 (page render), 05 (empty-state deep-links), and the Phase 43 docs gate all key off the ids and `covers` metadata defined here. Getting the schema + ids right first is what lets the rest of the phase run in parallel.

Output: `assets/help-content-en.js` (new) + `tests/39-help-integrity.test.js` (new, GREEN).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-RESEARCH.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
@.planning/milestones/v1.2-phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md
@.planning/SessionsGarden-DNA-EN.md
@.planning/sketches/002-help-page-ia/hybrid.html
</context>

## Artifacts this phase produces (this plan)

New symbols created here (exclude from drift/source-grounding checks):
- `window.HELP_CONTENT_EN` — ordered array of section objects.
- `window.HELP_DEEPLINKS` — `{ addClient, startSession, readDashboard }` → section id map.
- Content schema fields: section `{ id, title, group, featured, topics[] }`; topic `{ id, title, priority, body[], covers[] }`; body node types `p` / `note` / `steps` / `glyph`.
- Section ids (anchors): `make-it-yours`, `adding-a-client`, `starting-a-session`, `capturing-emotions`, `heart-wall`, `severity`, `review-export`, `overview`, `backups`, `installing`, `license`, `troubleshooting` (plus any the inventory requires).
- Topic ids (anchors): `topic-*` (e.g. `topic-first-client`, `topic-install-chrome`, `topic-install-safari`, `topic-install-mobile-note`).
- New file: `assets/help-content-en.js`, `tests/39-help-integrity.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Define the content schema and author the session-loop + featured personalization sections</name>
  <files>assets/help-content-en.js</files>
  <read_first>
    - .planning/sketches/002-help-page-ia/hybrid.html (the approved section/topic set + anchor ids: make-it-yours, adding-a-client, starting-a-session, capturing-emotions, heart-wall, severity, review-export, overview, and every topic-* id)
    - .planning/milestones/v1.2-phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md (the ~40-leaf checklist + per-leaf priority P1/P2/P3 + feature/file mapping that seeds `covers`)
    - .planning/SessionsGarden-DNA-EN.md (voice: calm, warm, garden-branded, native-quality EN for a global audience including US natives — D-17)
    - assets/i18n-en.js (the ONLY source for {ui:key} tokens and exact on-screen label spelling; e.g. overview.export, nav.*, settings tab labels, backup.* — quote by key, never invent a label)
    - assets/snippets-seed.js (window.* dict-export convention to mirror)
  </read_first>
  <action>
Create `assets/help-content-en.js` exporting `window.HELP_CONTENT_EN` as an ordered array of section objects. Schema (fixed here; the integrity test enforces it):
- section: `id` (kebab anchor, unique), `title` (noun/gerund per copywriting contract), `group` = one of `session-loop` | `technical`, `featured` (boolean, exactly one section true = `make-it-yours`), `topics` (array).
- topic: `id` (unique `topic-*` anchor), `title` (noun form), `priority` = 1 | 2 | 3, `covers` (non-empty array of repo file/page paths seeded from the inventory's feature-mapping column, e.g. `['assets/settings.js','settings.html']`), `body` (array of block nodes).
- body block node types: `{type:'p', text}` (prose), `{type:'note', text}` (muted aside), `{type:'steps', items:[...] }` (numbered steps — P1 topics MUST use full stupid-proof numbered steps per D-13), `{type:'glyph', name}` (install SVG glyph reference resolved by help.js in Plan 04; used only in the install section).

Author, in this task, the FEATURED personalization section (`make-it-yours`, group `session-loop`, featured true) and the session-loop spine sections in order: `adding-a-client`, `starting-a-session`, `capturing-emotions`, `heart-wall`, `severity`, `review-export`, `overview`. Use the mockup's topic ids/titles as the baseline and the inventory as the coverage checklist. Include the post-inventory delta where it belongs: custom session formats + date-format personalization under `make-it-yours`; filters/sorting + next-session date under `overview`.

Quote every on-screen label via `{ui:settings.tab.backups}`-style tokens keyed to EXISTING keys in assets/i18n-en.js (D-23) — the label text must never be typed as literal prose. P1 topics (per the inventory priority) get numbered `steps`; P2 lighter; P3 minimal (D-13). Terminology: client/session, Session Format, Heart-Wall — never patient/treatment. No emojis in any text. Do NOT source anything from demo.html/demo-seed.js.
  </action>
  <verify>
    <automated>node -e "global.window={};require('./assets/help-content-en.js');const s=window.HELP_CONTENT_EN;if(!Array.isArray(s))throw new Error('not array');if(s.filter(x=>x.featured).length!==1)throw new Error('need exactly one featured');const loop=s.filter(x=>x.group==='session-loop').map(x=>x.id);['make-it-yours','adding-a-client','starting-a-session','capturing-emotions','heart-wall','severity','review-export','overview'].forEach(id=>{if(!loop.includes(id))throw new Error('missing session-loop section '+id)});console.log('OK session-loop sections',loop.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/help-content-en.js` sets `window.HELP_CONTENT_EN` to an array; exactly one section has `featured:true` (`make-it-yours`).
    - All eight session-loop section ids above are present in order, each with ≥1 topic.
    - Every topic has a unique `topic-*` id, a `priority` of 1/2/3, and a non-empty `covers` array.
    - Every quoted UI label in body text is a `{ui:key}` token (grep shows `{ui:` tokens present; no bare quoted button names as prose).
    - No occurrence of `patient` or any emoji in the authored text.
  </acceptance_criteria>
  <done>The featured personalization section + the seven session-loop spine sections are authored with grounded, {ui:key}-interpolated, priority-scaled content; the shape smoke-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the technical track (backups, install computer-only, license, troubleshooting) + deep-link registry</name>
  <files>assets/help-content-en.js</files>
  <read_first>
    - assets/help-content-en.js (the schema + sections written in Task 1)
    - .planning/milestones/v1.2-phases/32-readme-code-comments/32-HELP-CONTENT-INVENTORY.md (Technical-tips track, License & trial, Troubleshooting rows + priorities)
    - .planning/phases/39-help-center-entry-point/39-UI-SPEC.md (Copywriting Contract rows: PWA-install computer-only wording, expectation-setting mobile topic, technical-track privacy anchor, contact band tone)
    - assets/i18n-en.js (labels for backup/export/report-a-problem/license flows to quote via {ui:key})
  </read_first>
  <action>
Append the `technical`-group sections in order: `backups`, `installing`, `license`, `troubleshooting`. Include the post-inventory delta topics: report-a-problem (under `troubleshooting`), updates/working-offline + "data never leaves your browser" privacy anchor (under `backups`).

`installing` section is COMPUTER-ONLY per D-15/D-16 — author exactly these topics and no others for install: `topic-install-chrome` (Chrome/Edge: install icon at the end of the address bar → Install), `topic-install-safari` (macOS Safari: File menu or Share menu → Add to Dock), and `topic-install-mobile-note` (D-16 expectation-setting: built for your computer; opens in a phone browser but data lives per-device with no sync). Each install step topic uses a `{type:'glyph', name}` node (`name` = `install-chrome` / `install-safari`) that Plan 04 renders as inline SVG chrome art. Do NOT author `topic-install-ios` or `topic-install-android` and do NOT write a single universal Install button — this supersedes the mockup's iOS/Android legs.

Then define and export `window.HELP_DEEPLINKS = { addClient:'adding-a-client', startSession:'starting-a-session', readDashboard:'overview' }` — the empty-state trio anchor contract consumed by Plan 05 and validated by the integrity test. Close with the "Still need help?" contact intent captured as a topic or trailing section note referencing `contact@sessionsgarden.app` and the Report-a-problem-in-Settings hint ("nothing is ever sent automatically") — the actual band is rendered in Plan 04, but the copy lives here.
  </action>
  <verify>
    <automated>node -e "global.window={};require('./assets/help-content-en.js');const s=window.HELP_CONTENT_EN;const tech=s.filter(x=>x.group==='technical').map(x=>x.id);['backups','installing','license','troubleshooting'].forEach(id=>{if(!tech.includes(id))throw new Error('missing technical section '+id)});const inst=s.find(x=>x.id==='installing').topics.map(t=>t.id);['topic-install-chrome','topic-install-safari','topic-install-mobile-note'].forEach(id=>{if(!inst.includes(id))throw new Error('missing install topic '+id)});if(inst.some(id=>/ios|android/.test(id)))throw new Error('ios/android install topic present — D-15 violation');const dl=window.HELP_DEEPLINKS;const ids=new Set(s.map(x=>x.id));['adding-a-client','starting-a-session','overview'].forEach(v=>{if(!ids.has(v))throw new Error('deeplink target missing '+v)});console.log('OK technical+deeplinks')"</automated>
  </verify>
  <acceptance_criteria>
    - The four `technical`-group sections are present in order, each with ≥1 topic.
    - `installing` topics are exactly the chrome + safari(Add-to-Dock) + mobile-note set; no ios/android topic id exists.
    - `window.HELP_DEEPLINKS` values (`adding-a-client`, `starting-a-session`, `overview`) are all real section ids.
    - report-a-problem, working-offline, and the privacy/"data never leaves your browser" topics exist under troubleshooting/backups.
  </acceptance_criteria>
  <done>The technical track + license + troubleshooting are authored computer-only-install-correct, and the deep-link registry resolves to real section ids.</done>
</task>

<task type="auto">
  <name>Task 3: Author the D-25 static integrity test</name>
  <files>tests/39-help-integrity.test.js</files>
  <read_first>
    - tests/33-i18n-de-cs-completion.test.js (the fs/vm sandbox + test(name,fn) + exit-0/1 harness to copy verbatim)
    - tests/run-all.js (auto-discovery + exit contract this file joins)
    - assets/help-content-en.js (the module under test — its schema, ids, HELP_DEEPLINKS, {ui:key} tokens)
    - assets/i18n-en.js (window.I18N.en — the {ui:key} resolution target)
  </read_first>
  <action>
Create `tests/39-help-integrity.test.js` following the 33-i18n fs/vm sandbox pattern: build a sandbox with `window={I18N:{},QUOTES:{}}`, `vm.createContext`, then `vm.runInContext` for `assets/i18n-en.js` then `assets/help-content-en.js`. Read `window.I18N.en`, `window.HELP_CONTENT_EN`, `window.HELP_DEEPLINKS`. Assert (each a named check, exit 1 on any failure per the run-all.js contract):
1. Every `{ui:key}` token found in every topic body text field resolves to an existing key in `window.I18N.en` (scan `p`/`note`/`steps` text via regex `/\{ui:([^}]+)\}/g`).
2. All section ids and all topic ids are unique and non-empty.
3. Every `HELP_DEEPLINKS` value is a real section id.
4. Coverage: exactly one section has `featured:true`; both groups `session-loop` and `technical` are present; the required section ids from Tasks 1–2 are all present.
5. Every topic has `priority` in {1,2,3} and a non-empty `covers` array.
6. Computer-only install: the `installing` section topic ids include chrome + safari + mobile-note and match none of `/ios|android/`.
7. Terminology + copy hygiene: no body text matches `/\bpatient\b/i`; no body text contains an emoji code point (scan for surrogate-pair / pictographic ranges).
Wire nothing new into run-all.js — it auto-discovers `tests/*.test.js`.
  </action>
  <verify>
    <automated>node tests/39-help-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/39-help-integrity.test.js` exits 0 and prints a PASS line for each of the seven checks.
    - Temporarily introducing an unresolved `{ui:zzz.missing}` token OR a `patient` word into help-content-en.js makes the test exit 1 (falsifiable — verify manually, then revert).
    - `npm test` (node tests/run-all.js) discovers and runs the new file green.
  </acceptance_criteria>
  <done>The integrity test is live in `npm test`, guards {ui:key} resolution + deep-link anchors + coverage + computer-only install + terminology, and is proven falsifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored data → render (Plan 04) | Help body strings become DOM in help.js; `{ui:key}` tokens resolve to i18n dict values only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `{ui:key}` interpolation tokens | low | mitigate | Tokens resolve ONLY against `window.I18N.en` keys (no user input); integrity test fails on any unresolved token so a typo can never render a raw `{ui:...}` string. |
| T-39-02 | Information Disclosure | help body copy | low | accept | Content is public product documentation; contains no secrets, no client data, no license internals. |

No package-manager installs occur in this plan (zero new dependencies — verified in RESEARCH Package Legitimacy Audit); T-39-SC is not applicable.
</threat_model>

<verification>
- `node tests/39-help-integrity.test.js` exits 0.
- `npm test` green (new file auto-discovered).
- Manual falsifiability spot-check on the integrity test (unresolved token + terminology), then revert.
</verification>

<success_criteria>
- `assets/help-content-en.js` covers the full inventory + post-inventory delta, spine-organized, featured-first, computer-only install, `{ui:key}`-interpolated, with `covers` on every topic.
- `window.HELP_DEEPLINKS` gives Plan 05 stable anchor targets.
- The integrity test is a standing `npm test` gate.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-01-SUMMARY.md` when done.
</output>
