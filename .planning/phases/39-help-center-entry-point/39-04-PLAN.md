---
phase: 39-help-center-entry-point
plan: 04
type: execute
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - help.html
  - assets/help.js
  - assets/help.css
  - tests/39-help-render.test.js
autonomous: true
requirements: [HELP-02, HELP-03, HELP-06]
must_haves:
  truths:
    - "help.html is a standalone app page (per-page pattern: SW register, #headerActions, #nav-placeholder, SharedChrome footer, body data-nav='help') that renders its rail + cards from window.HELP_CONTENT_EN — never hardcoded topic copy (D-18)."
    - "The page IA is the approved hybrid A+C (D-01): a sticky unnumbered rail grouped 'The session loop' / 'The technical bits' (divider) (D-03), collapsed cards with the featured personalization card open at top (D-04), multiple cards stay open (D-02), scroll-spy highlights the active section, clicking a rail item scrolls to AND expands its card."
    - "Every {ui:key} token in help body renders the current live i18n label via t() (D-23); a Hebrew UI shows Hebrew button names under EN help text."
    - "Client-side substring search (D-06) live-filters cards + rail items; empty rail groups hide their label + divider; a no-match state shows the calm 'write to us' contact fallback (never a dead end); the searched term is echoed via textContent."
    - "Deep-link arrival (help.html#<id>) auto-expands the owning card and scrolls to it; every topic is anchor-addressable; the page otherwise always opens at the top (no per-page context landing) (D-11)."
    - "Install instructions are computer-only (Chrome/Edge address-bar icon + macOS Safari Add to Dock) with inline SVG chrome glyphs (D-14) and one expectation-setting mobile note (D-16) — never a universal Install button, never iOS/Android legs (D-15)."
    - "Help surfaces use soft type (D-05: Rubik 400 headings in --color-primary-deeper, 600 sub-topic h3, 700 labels) loaded from the real assets/fonts/Rubik-*.woff2 files, semantic tokens only, logical CSS properties, [data-theme=dark]-aware, no literal hex."
  artifacts:
    - help.html
    - assets/help.js
    - assets/help.css
    - tests/39-help-render.test.js
  key_links:
    - "help.js iterates window.HELP_CONTENT_EN (Plan 01) and interpolates {ui:key} via App.t() (app.js); footer via SharedChrome.renderFooter()."
    - "help.html script order loads i18n → app.js → shared-chrome.js → help-content-en.js → help.js so both the content array and t() are available at render."
    - "Page title + search chrome from help.page.title / help.search.* (Plan 02)."
  prohibitions:
    - statement: "Topic copy is NOT hardcoded in help.html — cards/rail are built by help.js from the content array (bypassing the array would defeat the integrity test + covers metadata, D-18)."
      status: resolved
      verification: "help.html has empty rail/content containers; grep shows no topic prose in help.html; render test asserts cards built from HELP_CONTENT_EN length."
    - statement: "Dynamic text (card/rail/search results, no-match term echo) is built via createElement + textContent, never innerHTML with interpolated strings; inline SVG glyphs are the only compile-time-literal innerHTML."
      status: resolved
      verification: "render test asserts no-match term echo uses textContent (an injected <img onerror> term does not execute)."
    - statement: "No universal Install button and no iOS/Android install content on the page (computer-only per D-15)."
      status: resolved
      verification: "Install section renders only chrome + safari + mobile-note topics from the content model (guarded upstream by the integrity test)."
    - statement: "No literal hex and no physical CSS properties (right:/left:/margin-left) in help.css — semantic tokens + logical properties only (UI-SPEC)."
      status: resolved
      verification: "grep of help.css: no #hex, no bare right:/left:/margin-left."
---

<objective>
Build the standalone offline help center: `help.html` (page shell) + `assets/help.js` (renders the rail + cards from `window.HELP_CONTENT_EN` with `{ui:key}` interpolation, search, deep-link auto-expand, scroll-spy) + `assets/help.css` (soft-type help surfaces). Port the approved mockup's interaction JS near-verbatim, changing only the content source to the data array.

Purpose: HELP-02 (browsable help center + anchor deep-links), HELP-03 (workflow-spine IA + featured personalization + technical track), HELP-06 (computer-only per-browser install with SVG glyphs). This is the surface a stressed practitioner lands on — calmer than the rest of the app by design.

Output: `help.html`, `assets/help.js`, `assets/help.css`, and a jsdom render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-RESEARCH.md
@.planning/phases/39-help-center-entry-point/39-PATTERNS.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
@.planning/sketches/002-help-page-ia/hybrid.html
</context>

## Artifacts this phase produces (this plan)

New files: `help.html`, `assets/help.js`, `assets/help.css`, `tests/39-help-render.test.js`. New symbols in help.js: `interpolateUiLabels()`, `setOpen()`, `applySearch()`, `openForHash()`, the render entry point, and the install-glyph SVG lookup. New CSS classes on help surfaces (soft-type): reuse the mockup's `.help-card`, `.card-head`, `.card-body`, `.rail*`, `.help-search`, `.search-empty`, `.contact-band`, `.tech-band`, `.page-head`, `.soft-type` etc.

<tasks>

<task type="auto">
  <name>Task 1: help.html page shell + help.css soft-type surfaces</name>
  <files>help.html, assets/help.css</files>
  <read_first>
    - sessions.html (the per-page head/body scaffold to copy: crashlog/terms/license/theme bootstrap scripts, favicon/manifest links, #nav-placeholder + #headerActions header, toast, SW registration, script order)
    - .planning/sketches/002-help-page-ia/hybrid.html (the full CSS for rail/cards/search/contact-band/tech-band/page-head/soft-type + mobile jump-strip; the @font-face Rubik faces; the layout grid) — this is the source of the CSS to port into help.css
    - .planning/phases/39-help-center-entry-point/39-UI-SPEC.md (Typography soft-type weight set 400/600/700 + accepted 3-weight exception; Color reserved-accent list; Spacing scale; Interaction Contract rows)
    - assets/tokens.css (the semantic --color-*/--space-*/--shadow-*/--radius-* vars the CSS must reference)
  </read_first>
  <action>
Create `help.html` by copying sessions.html's head + body scaffold (keep the crashlog early-buffer, terms-gate, license-gate, theme bootstrap scripts, favicon/manifest/apple-touch links, tokens.css + app.css links) and ADD `<link rel="stylesheet" href="./assets/help.css">`. Body carries `data-nav="help"`. Inside the container: the standard header (brand link + `#nav-placeholder` + `#headerActions` — chrome mounts via initCommon), then the help layout as EMPTY containers help.js will populate: a `.page-head` (with the `assets/illustrations/watering-can.png` botanical accent + an h1 bound to help.page.title + the contract intro line), a two-column `.layout` with an empty sticky `.rail` (id rail) containing the desktop search box, and an empty `.content` main with the mobile jump-strip (D-08: search + collapsible "Jump to a section" details pinned at top, rail hidden on small screens), a `#searchEmpty` no-match block, and the `.contact-band` ("Still need help?" closing band per D-07 — real-person reassurance + contact@sessionsgarden.app mailto + the Report-a-problem-in-Settings "nothing is ever sent automatically" hint). Do NOT hardcode topic cards/rail items — help.js builds them. Footer via SharedChrome (matching sessions.html). Script order at the bottom: i18n-en/he/de/cs → i18n.js → db.js → version.js → crashlog.js → shared-chrome.js → date-format.js → app.js → jszip/backup/backup-modal (chrome trio, for the cloud icon) → `help-content-en.js` → `help.js`; then the SW registration snippet.

Create `assets/help.css` by porting the mockup's help-surface CSS (rail, cards, search, contact-band, tech-band, page-head, soft-type headings, mobile jump-strip, botanical dark-mode invert+screen). Reference ONLY semantic tokens from tokens.css and logical properties; drop the mockup's `.sketch-tools` chrome. Ensure the Rubik faces load from the REAL `assets/fonts/Rubik-{Regular,SemiBold,Bold}.woff2` (Pitfall 5 — verify against app.css's existing @font-face rather than re-declaring conflicting ones; if app.css already declares Rubik, do not redeclare — just use `font-weight` 400/600/700). Soft type is always on for help surfaces (no toggle): Rubik 400 headings in `--color-primary-deeper`, 600 sub-topic h3, 700 interactive labels.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('help.html','utf8');if(!/data-nav=\"help\"/.test(h))throw new Error('body data-nav help missing');if(!/help\.css/.test(h))throw new Error('help.css link missing');if(!/help-content-en\.js/.test(h)||!/assets\/help\.js/.test(h))throw new Error('help scripts missing');const c=fs.readFileSync('assets/help.css','utf8');if(/#[0-9a-fA-F]{3,6}\b/.test(c))throw new Error('literal hex in help.css');if(/(^|[^-])(margin-left|margin-right)\s*:/.test(c)||/[^-](\bright|\bleft)\s*:/.test(c))throw new Error('physical property in help.css');console.log('OK shell+css')"</automated>
  </verify>
  <acceptance_criteria>
    - `help.html` is a valid app page: body[data-nav=help], links tokens.css + app.css + help.css, header has #headerActions + #nav-placeholder, script order loads help-content-en.js before help.js, includes SW registration.
    - Rail/content containers are EMPTY of topic copy (help.js populates them); page-head binds help.page.title + intro; contact band + no-match block present.
    - `assets/help.css` uses only semantic tokens (no `#hex`) and logical properties (no bare right:/left:/margin-left); soft-type 400/600/700 from real Rubik faces.
  </acceptance_criteria>
  <done>The page shell + soft-type CSS exist, token/logical-property clean, ready for help.js to populate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: help.js renderer — cards/rail from content, {ui:key} interpolation, search, deep-link, scroll-spy, install glyphs</name>
  <files>assets/help.js</files>
  <read_first>
    - .planning/sketches/002-help-page-ia/hybrid.html lines 698–833 (the JS to port: setOpen, rail-click scroll+expand, in-rail chevron toggle, openForHash deep-link, applySearch substring filter + group-label/divider/tech-band hiding + no-match echo, IntersectionObserver scroll-spy rootMargin '-10% 0px -70% 0px')
    - assets/help-content-en.js (window.HELP_CONTENT_EN schema — section {id,title,group,featured,topics[]}; topic {id,title,priority,body[],covers[]}; body node types p/note/steps/glyph)
    - assets/app.js lines 14–22 (t() — the i18n resolver reused by {ui:key} interpolation) and lines 672–679 (overview.js-style createElement+textContent DOM-build convention)
    - assets/overview.js lines 667–735 (createElement + textContent DOM-build reference — the repo convention for dynamic text, never innerHTML)
  </read_first>
  <behavior>
    - render() builds one rail item per section (grouped session-loop/technical with a divider) and one .help-card per section (h2 = section.title; each topic → h3 anchor id + body blocks); the featured section renders first, raised, is-open, with a "Start here" tag.
    - The number of .help-card elements equals window.HELP_CONTENT_EN.length; each card id === section.id; each topic h3 id === topic.id.
    - A body {type:'p'|'note'} renders a <p> via textContent; {type:'steps'} renders an <ol> of <li> via textContent; a {ui:key} token inside any text renders t(key) (interpolateUiLabels), NOT the literal token.
    - {type:'glyph', name} in the install section renders a compile-time-literal inline SVG for that browser chrome (install-chrome / install-safari); no screenshots.
    - Typing a non-matching term shows #searchEmpty with the term echoed via textContent and a mailto 'write to us' link; matching filters cards + rail items and hides empty rail groups + divider.
    - location.hash on load auto-expands the owning card and scrolls to the anchor; with no hash the page opens at top (featured open).
  </behavior>
  <action>
Create `assets/help.js` as a self-booting module (DOMContentLoaded → App.initCommon() then render()). Implement:
- `render()`: iterate `window.HELP_CONTENT_EN`; build the rail (group by `section.group`, featured first, unnumbered, each item a rail-link + a chevron rail-toggle that expands the section's topic sub-list in-rail; sub-links point at `#<topic.id>`) and the content column (one `.help-card` per section; featured section gets `.featured .is-open` + a "Start here" tag; technical-group cards wrapped in the `.tech-band`). For each topic render an `<h3 id="topic.id">` + its body blocks. Set the page-head h1 to `App.t('help.page.title')` and bind the search placeholder to `App.t('help.search.placeholder')`.
- `interpolateUiLabels(text)`: replace `/\{ui:([^}]+)\}/g` with `App.t(key.trim())` — reuse for every text node. Build all dynamic text with createElement + textContent (never innerHTML); render body-block `glyph` nodes and card/rail chevrons as compile-time-literal inline SVG only.
- Port `setOpen`, rail-click (scroll + setOpen(card,true)), chevron in-rail toggle, `applySearch` (substring over card textContent; hide non-matching cards + rail items; hide a rail group label when all its items are hidden; hide the divider + tech-band when empty; show `#searchEmpty` with the term set via textContent + the `help.search.writeToUs` mailto link on zero hits), and the IntersectionObserver scroll-spy — all from the mockup, sourcing content from the array.
- `openForHash(location.hash)` on load (auto-expand owning card, then scrollIntoView) for empty-state deep-link arrival; also wire rail-sub + jump-strip anchors through openForHash.
- Re-render/re-translate on the `app:language` event so {ui:key} labels and RTL flip live (call App.applyTranslations + re-run interpolation).
Compose the search no-match copy from `help.search.noMatch` + a `help.search.writeToUs` mailto link. Render the contact band from help-content-en's closing copy (mailto:contact@sessionsgarden.app + report-a-problem hint).
  </action>
  <verify>
    <automated>node tests/39-help-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - help.js builds `window.HELP_CONTENT_EN.length` cards, each id === section.id, featured card first + is-open.
    - A `{ui:overview.export}`-style token in a topic body renders as the resolved label text (t()), not the literal `{ui:...}`.
    - Search no-match echoes the term via textContent (an `<img onerror=...>` term string does NOT execute) and offers the mailto fallback.
    - Install section renders only the chrome/safari/mobile-note topics with inline SVG glyphs; no universal Install button.
    - No literal hex / physical properties introduced in JS-set styles (styling stays in help.css).
  </acceptance_criteria>
  <done>help.js renders the full hybrid A+C IA from the content array with live-label interpolation, search + no-match fallback, deep-link auto-expand, scroll-spy, and computer-only install glyphs.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom render + interpolation + XSS-echo test</name>
  <files>tests/39-help-render.test.js</files>
  <read_first>
    - tests/30-04-settings-characterization.test.js (jsdom bootstrap that executes a real assets/*.js against a built DOM + window.I18N stub + boot-handler capture)
    - assets/help.js (the renderer under test)
    - assets/help-content-en.js + assets/i18n-en.js (content + i18n to load into the jsdom window)
    - help.html (the DOM skeleton the test recreates: #rail, .content, #searchEmpty, .page-head)
  </read_first>
  <action>
Create `tests/39-help-render.test.js` using the repo jsdom harness. Build the help.html skeleton DOM (page-head, #rail with the search input, .content, #searchEmpty with a term span, .contact-band), load window.HELP_CONTENT_EN (help-content-en.js) + window.I18N.en (i18n-en.js), stub App with a real t() that reads window.I18N.en (or execute app.js), then execute help.js's render. Assert: (1) `.help-card` count === HELP_CONTENT_EN.length and the featured card is first + has is-open; (2) a topic body containing a `{ui:<key>}` token renders the resolved i18n label text (pick a real key present in the authored content); (3) drive applySearch with a term that matches nothing including an HTML-ish payload (e.g. `<img src=x onerror=alert(1)>`) and assert #searchEmpty is shown, the echoed term is present as text (querying textContent) and NO `<img>` element was created in #searchEmpty (textContent, not innerHTML); (4) openForHash('#<a real topic id>') expands the owning card. Exit 0/1 per run-all.
  </action>
  <verify>
    <automated>node tests/39-help-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/39-help-render.test.js` exits 0 with PASS lines for card-count, featured-first, {ui:key} interpolation, no-match textContent echo (no injected element), and deep-link auto-expand.
    - Switching the no-match echo to innerHTML in help.js makes the XSS assertion fail (falsifiable — verify, then revert).
    - `npm test` runs it green.
  </acceptance_criteria>
  <done>The page renderer, live-label interpolation, and the search-echo XSS guard are covered by an automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search input → DOM | The user-typed search term is echoed into the no-match state |
| content array → DOM | Help body strings + {ui:key} resolutions become rendered DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Tampering | search no-match term echo | medium | mitigate | Term set via textContent (never innerHTML) — render test asserts an `<img onerror>` term does not create an element or execute. |
| T-39-07 | Tampering | help body / {ui:key} render | low | mitigate | All dynamic text built with createElement+textContent; {ui:key} resolves only to i18n dict values (no user input); inline SVG glyphs are compile-time literals. |
| T-39-08 | Information Disclosure | offline-served help page | low | accept | Static public documentation; no client data, no secrets rendered. |

No package-manager installs occur in this plan (zero new deps); T-39-SC not applicable.
</threat_model>

<verification>
- `node tests/39-help-render.test.js` exits 0; `npm test` green (integrity test from Plan 01 also passes since content ids match rendered anchors).
- help.html shell + help.css token/logical-property smoke-checks pass.
- Visual RTL/dark/soft-type + real-Safari checks are performed in Plan 06 (WebKit checkpoint).
</verification>

<success_criteria>
- HELP-02/03/06 delivered: a browsable, anchor-deep-linkable, workflow-spine help center with featured personalization, technical track, search + no-match fallback, and computer-only install — rendered from the content array with live labels.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-04-SUMMARY.md` when done.
</output>
