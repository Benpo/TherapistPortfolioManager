---
phase: 39-help-center-entry-point
plan: 03
type: execute
wave: 2
depends_on: [39-02]
files_modified:
  - assets/app.js
  - tests/39-help-entry.test.js
autonomous: true
requirements: [HELP-01, HELP-02]
must_haves:
  truths:
    - "A '?' help affordance mounts into #headerActions on every app page via a new initHelpEntry() called from initCommon(), beside the cloud + gear controls."
    - "Clicking '?' toggles a popover (D-09 globe-pattern: outside-click + Esc dismiss, aria-expanded synced) whose day-one items are 'Help center' (→ ./help.html) and 'Contact us' (→ mailto:contact@sessionsgarden.app) (D-10), built as an addable list so Phases 40–42 can append entries."
    - "The '?' button carries .is-active when body[data-nav]='help', mirroring .settings-gear-btn (D-12: 36×36 visual / 44×44 tap, inline SVG, RTL auto-flip), and re-localizes its aria-label + item labels on the app:language event (install-once listener)."
    - "renderNav() includes a Help entry (<a href='./help.html' data-nav='help' data-i18n='nav.help'>) so help.html gets active nav marking for free."
    - "initHelpEntry() is idempotent — a repeated initCommon()/double-mount does not create a second .help-entry-btn."
  artifacts:
    - assets/app.js
    - tests/39-help-entry.test.js
  key_links:
    - "initHelpEntry() reads t('help.entry.*') and nav.help from the i18n keys added in Plan 02."
    - "The popover 'Help center' item navigates to ./help.html (built in Plan 04); 'Contact us' opens the same mailto used by the help-page contact band."
  prohibitions:
    - statement: "No physical CSS or literal hex is introduced for the '?' control; it reuses the existing .header-control-btn class and logical-property layout (RTL auto-flips with no JS)."
      status: resolved
      verification: "Diff shows reuse of header-control-btn / is-active classes; no new right:/left:/#hex in app.js."
    - statement: "Popover item labels and the no-match search echo are set via textContent, never innerHTML with interpolated strings (inline SVG glyph is the only compile-time-literal innerHTML)."
      status: resolved
      verification: "jsdom test asserts item labels set via textContent; code review of the mount."
---

<objective>
Add the persistent "?" help entry to the shared header chrome: a new `initHelpEntry()` in `assets/app.js` (mirroring the settings-gear mount + reusing the globe popover pattern) plus a Help entry in `renderNav()`, wired into `initCommon()` so it appears on every SW-registered app page.

Purpose: HELP-01 (reach Help from any page) + HELP-02's nav entry. This is pure app-chrome composition of two already-tested patterns; the popover is an addable list so Phases 40–42 append "Replay welcome / Take tour / What's new" without a rewrite (D-09).

Output: `initHelpEntry()` + Help nav entry in `assets/app.js`, guarded by a jsdom mount test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-help-center-entry-point/39-CONTEXT.md
@.planning/phases/39-help-center-entry-point/39-RESEARCH.md
@.planning/phases/39-help-center-entry-point/39-PATTERNS.md
@.planning/phases/39-help-center-entry-point/39-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

New symbols: `initHelpEntry()` (module-private in app.js's IIFE), its `_listenerInstalled` static flag, the `.help-entry-btn` header button + its help popover, and a new `nav.help` anchor in `renderNav()`. New file: `tests/39-help-entry.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add initHelpEntry() + Help nav entry and wire into initCommon()</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js lines 202–274 (initLanguagePopover — the exact popover open/close/outside-click/aria-expanded pattern to reuse for D-09)
    - assets/app.js lines 391–455 (initSettingsLink — the icon-button mount shape: idempotent guard, aria-label via t(), inline-SVG literal, .is-active on body[data-nav], install-once app:language re-translate listener)
    - assets/app.js lines 137–156 (renderNav — where the Help anchor is appended; data-nav active marking loop)
    - assets/app.js lines 648–655 (initCommon chrome-wiring call order — insert initHelpEntry() after initSettingsLink())
    - .planning/phases/39-help-center-entry-point/39-PATTERNS.md (§ initHelpEntry pattern assignments — mount, popover, addable-list, re-translate)
  </read_first>
  <behavior>
    - Calling initCommon() on a page with #headerActions mounts exactly one .help-entry-btn.
    - Calling initHelpEntry() twice mounts only one .help-entry-btn (idempotent guard).
    - The help popover contains two items whose textContent is t('help.entry.center') and t('help.entry.contact'); the first navigates to ./help.html, the second is a mailto:contact@sessionsgarden.app link/anchor.
    - Clicking the button toggles the popover and flips aria-expanded true/false; an outside click closes it.
    - On body[data-nav]='help', the button has class is-active.
    - renderNav() output contains an anchor with href ./help.html, data-nav='help', data-i18n='nav.help'.
  </behavior>
  <action>
Add a module-private `initHelpEntry()` in app.js modeled on `initSettingsLink` (mount) + `initLanguagePopover` (popover). Mount an inline-SVG "?" glyph button (`class="header-control-btn help-entry-btn"`, 20×20 in a 24×24 viewBox, `stroke="currentColor"`, `stroke-width="1.8"`, `aria-hidden` on the svg) into `#headerActions` with the idempotent guard `if (actions.querySelector('.help-entry-btn')) return;`. Set aria-label + title from `t('help.entry.label')` with a plain "Help" fallback. Build a popover element (reuse the globe popover's hidden/aria-expanded/outside-click behavior) whose items come from an array so future phases can append: day-one items `{ labelKey:'help.entry.center', href:'./help.html' }` and `{ labelKey:'help.entry.contact', href:'mailto:contact@sessionsgarden.app' }`. Each item is an anchor whose textContent is `t(labelKey)` (never innerHTML for the label). Add `is-active` when `document.body.dataset.nav === 'help'`. Install an app:language re-translate listener exactly once via an `initHelpEntry._listenerInstalled` static flag (mirroring initSettingsLink) that re-sets the aria-label + item labels. Call `initHelpEntry()` inside `initCommon()` right after `initSettingsLink()`. Insertion order within #headerActions is your discretion (after the gear is fine). In `renderNav()`, append a Help anchor (`href="./help.html" data-nav="help" data-i18n="nav.help"`) as the last nav item — active marking is automatic via the existing loop + applyTranslations(placeholder).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('assets/app.js','utf8');if(!/function initHelpEntry\(/.test(s))throw new Error('initHelpEntry missing');if(!/help-entry-btn/.test(s))throw new Error('help-entry-btn class missing');if(!/data-nav=\"help\"/.test(s)&&!/data-nav='help'/.test(s))throw new Error('nav help entry missing');if(!/initHelpEntry\(\)/.test(s.split('function initCommon')[1]||''))throw new Error('initHelpEntry not called in initCommon');console.log('OK app.js wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/app.js` defines `initHelpEntry(` and calls it inside `initCommon`.
    - The Help nav anchor (`href="./help.html"`, `data-nav="help"`, `data-i18n="nav.help"`) is present in renderNav's template.
    - The popover items are built from an array (addable) with labels set via textContent and the two day-one hrefs (./help.html, mailto:contact@sessionsgarden.app).
    - No new literal hex or physical right:/left: added.
  </acceptance_criteria>
  <done>The "?" entry mounts on every app page via initCommon, opens an addable popover, mirrors the gear's is-active + re-translate, and the Help nav entry is present.</done>
</task>

<task type="auto">
  <name>Task 2: jsdom mount + idempotency + popover behavior test</name>
  <files>tests/39-help-entry.test.js</files>
  <read_first>
    - tests/30-04-settings-characterization.test.js OR any tests/*.test.js that jsdom-executes real app.js (the jsdom bootstrap + window.I18N stub + DOMContentLoaded/handler-capture convention)
    - tests/run-all.js (exit-0/1 contract + auto-discovery)
    - assets/app.js (the initHelpEntry + renderNav code under test)
    - assets/i18n-en.js (to seed window.I18N.en for t() during the test)
  </read_first>
  <action>
Create `tests/39-help-entry.test.js` using the repo's jsdom harness. Build a minimal document with `#nav-placeholder` and `#headerActions`, seed `window.I18N.en` from assets/i18n-en.js (so t() resolves help.entry.*/nav.help), execute app.js in the jsdom window, then drive the public surface: call `App.renderNav()` and assert the Help anchor exists (href ./help.html, data-nav help). Call `App.initCommon()` (await it) with body[data-nav]='help' and assert exactly one `.help-entry-btn` mounts, it has `is-active`, its aria-label equals t('help.entry.label'), and the popover has two items whose textContent equals t('help.entry.center') / t('help.entry.contact') with hrefs ./help.html and mailto:contact@sessionsgarden.app. Call initCommon()/initHelpEntry a second time and assert still exactly one `.help-entry-btn` (idempotency). Simulate a click on the button and assert aria-expanded flips to "true"; dispatch an outside document click and assert it returns to "false". Exit 0/1 per the run-all contract.
  </action>
  <verify>
    <automated>node tests/39-help-entry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/39-help-entry.test.js` exits 0 with PASS lines for: nav entry present, single mount, is-active parity, aria-label from i18n, two popover items with correct textContent + hrefs, idempotency, toggle + outside-click dismiss.
    - Removing the idempotency guard in app.js makes the "single mount" assertion fail (falsifiable — verify, then revert).
    - `npm test` discovers and runs it green.
  </acceptance_criteria>
  <done>The header entry's mount, idempotency, is-active parity, i18n labels, and popover dismiss are guarded by an automated jsdom test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome render → DOM | initHelpEntry injects a button + popover; labels from i18n dict, SVG is a compile-time literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04 | Tampering | inline "?" SVG glyph | low | mitigate | SVG is a hard-coded compile-time literal (same convention as gear/globe); no user-interpolated markup. |
| T-39-05 | Tampering | popover item labels | low | mitigate | Labels set via textContent from the i18n dict, not innerHTML — no injection surface. |

No package-manager installs occur in this plan; T-39-SC not applicable.
</threat_model>

<verification>
- app.js wiring smoke-check passes.
- `node tests/39-help-entry.test.js` exits 0; `npm test` green.
- Manual: on any app page the "?" appears beside cloud/gear, opens a 2-item popover, dismisses on outside click (final visual check folded into Plan 06).
</verification>

<success_criteria>
- HELP-01 header affordance present on every app page; HELP-02 nav entry present; popover addable for later phases.
</success_criteria>

<output>
Create `.planning/phases/39-help-center-entry-point/39-03-SUMMARY.md` when done.
</output>
