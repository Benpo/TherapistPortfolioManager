---
phase: 45-rich-text-rendering-export-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/md-render.js
  - tests/45-mdrender-lists.test.js
  - tests/45-mdrender-escape.test.js
  - tests/45-mdrender-strip.test.js
autonomous: true
requirements: [RTXT-06, RTXT-07, RTXT-10]
must_haves:
  truths:
    - "MdRender renders ordered lists (1. 2. 3.) as <ol><li> and bullet lists as <ul><li>"
    - "MdRender renders nested (indented) bullet AND numbered lists as nested <ul>/<ol>"
    - "Legacy accidental asterisks (2 * 3 * 4) stay literal — never italicized (D-08)"
    - "A <script> tag typed in a note renders inert (escape-first, XSS-safe)"
    - "MdRender.strip(md) returns plain text with inline AND block markers removed (D-06)"
  artifacts:
    - "assets/md-render.js extended in place (no fork, no new module)"
    - "window.MdRender.strip function added"
    - "tests/45-mdrender-lists.test.js, tests/45-mdrender-escape.test.js, tests/45-mdrender-strip.test.js"
  key_links:
    - "MdRender inline hardening (D-08) must match pdf-export.js hardening exactly (Plan 02) — pinned example table is the shared contract"
    - "The single-newline-<br> / blank-line-paragraph contract (md-render.js header L25-31) stays byte-identical"
---

<objective>
Extend the shipped escape-first renderer `window.MdRender` (`assets/md-render.js`, 75 lines) in place so markdown-at-rest session notes render correctly wherever they are READ: add ordered-list (`1.`) support, nested bullet+numbered lists, CommonMark-style inline hardening (D-08), and a shared markdown→plain-text `strip()` helper for the D-06 compact surfaces.

Purpose: MdRender is the ONLY sanctioned `innerHTML` path in the app (escape-first is the whole security model). Every downstream read surface (read-mode overlay in Plan 04, export-modal preview already wired, compact-surface strip in Plan 04) depends on this renderer being correct. This plan makes the renderer complete before any surface consumes it.

Output: An extended `MdRender` (render + new strip) with ordered/nested lists and hardened inline rules, plus three falsifiable behavior tests. No new module, no fork, no dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-PATTERNS.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md
@assets/md-render.js
@tests/31-sessions-render-hardening.test.js
</context>

## Artifacts this phase produces (Plan 01 scope)

New/changed symbols introduced by this plan (for source-grounding / drift exclusion):

- `window.MdRender.strip(markdown)` — NEW public function: returns plain text with inline (`**`, `*`) AND block (`#`/`##`/`###`, `-`/`*`, `1.`) markers stripped; agrees with the D-08 hardened inline rules; used by the D-06 compact surfaces (Plan 04).
- Ordered-list rendering in `renderBlock` — emits `<ol><li>…</li></ol>` for `1.`/`2.`/`3.` lines (behavior, no new exported symbol).
- Nested-list rendering — indentation-depth tracking emits nested `<ul>`/`<ol>` (behavior).
- Hardened `applyInline` emphasis regexes (D-08) — behavior change, same function name.
- Test files: `tests/45-mdrender-lists.test.js`, `tests/45-mdrender-escape.test.js`, `tests/45-mdrender-strip.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ordered-list + nested-list support to MdRender.renderBlock</name>
  <files>assets/md-render.js, tests/45-mdrender-lists.test.js</files>
  <read_first>
    - assets/md-render.js (the full file — you extend `renderBlock` L32-59 and `render` L61-70; the list branch is L45-55)
    - tests/31-sessions-render-hardening.test.js (the repo's render-test shape to copy: load real asset into an isolated jsdom window, assert OBSERVABLE rendered HTML, end with a count guard — no async DOMContentLoaded vacuous-green trap)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pattern 3 "Nested lists", Pitfall 3 "every() breaks on indented sub-items")
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md (Spacing: each nested level = one 24px logical indent step; ul/ol first level padding-inline-start:24px)
  </read_first>
  <behavior>
    Write tests/45-mdrender-lists.test.js FIRST, GREEN against unchanged source only for the already-supported bullet case, then RED for the new cases before implementing:
    - `- a\n- b` → `<ul><li>a</li><li>b</li></ul>` (existing — regression-lock, output byte-identical)
    - `1. a\n2. b` → `<ol><li>a</li><li>b</li></ol>` (NEW ordered)
    - `3. a\n4. b` → `<ol><li>a</li><li>b</li></ol>` (ordinals need not be re-derived in HTML — the browser numbers `<ol>`; do NOT hardcode start=1 assumptions that break; preserve source order)
    - `- a\n  - b\n- c` → nested: `<ul><li>a<ul><li>b</li></ul></li><li>c</li></ul>` (NEW nested bullet; two-space indent = one level)
    - `1. a\n   1. b` → nested ordered inside ordered (NEW nested numbered)
    - `- a\n  1. b` → bullet with nested ordered child (mixed nesting)
    - A single flat list with no indentation produces byte-identical output to today (regression guard — assert exact string)
    - End with a count guard asserting every case ran (e.g. an assertion counter === number of cases).
  </behavior>
  <action>
    Rework the list branch of `renderBlock` (md-render.js L45-55) to (1) accept `1.`/`2.`/`3.` ordered markers and emit `<ol>` (D-04: numbered lists must render everywhere), (2) track leading-whitespace depth per line and recurse into nested `<ul>`/`<ol>` (D-05: nested lists land in Phase 45). A legacy hand-typed `- item` line rendering as a real bullet is the accepted meaning-preserving upgrade (D-09) — not a violation of "unchanged in meaning". The current `lines.every(isListItem)` check (L47) fails the moment ANY line is indented (Pitfall 3) — replace the flat `every()` gate with detection that treats an indented continuation as part of the list block and builds a tree by leading-whitespace depth (recommended: 2 spaces per level, but accept any consistent indent step). Bullet markers `-`/`*` → `<ul>`; numbered `1.` etc. → `<ol>`. Each `<li>` content still goes through `applyInline`. Preserve the existing bullet-marker strip (`replace(/^[-*]\s+/, "")`) and add the ordered strip (`replace(/^\s*\d+\.\s+/, "")`). Do NOT change `render` block-splitting on blank lines, and do NOT touch the LOCKED single-newline-`<br>` / blank-line-paragraph contract (header comment L25-31) — nested lists are still inside one blank-line-delimited block. Regression requirement: flat (non-nested, non-ordered) input must produce byte-identical output to the current implementation.
  </action>
  <verify>
    <automated>node tests/45-mdrender-lists.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-mdrender-lists.test.js` exits 0
    - Test asserts `1. a\n2. b` renders `<ol>` with two `<li>` (source assertion on returned HTML string)
    - Test asserts `- a\n  - b\n- c` renders a nested `<ul>` inside the first `<li>`
    - Test asserts a flat `- a\n- b` list output is byte-identical to the pre-change output (regression lock)
    - Test ends with a count guard proving all cases executed (no vacuous green)
    - `assets/md-render.js` still contains the LOCKED contract comment block (the `<br>` / paragraph rule text) unchanged
  </acceptance_criteria>
  <done>Ordered and nested (bullet + numbered) lists render as correct nested HTML; flat-list output is unchanged; the locked paragraph contract is intact; the test is falsifiable and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden inline emphasis rules (D-08) + escape-first regression test</name>
  <files>assets/md-render.js, tests/45-mdrender-escape.test.js</files>
  <read_first>
    - assets/md-render.js (`escapeHtml` L8-15, `applyInline` L17-23 — the two emphasis regexes you tighten)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (D-08; Pitfall 2 "MdRender/PDF inline rules disagree"; Security Domain "escape-first")
    - .planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md (D-08 wording: markers must hug non-whitespace so `2 * 3 * 4` stays literal)
  </read_first>
  <behavior>
    Write tests/45-mdrender-escape.test.js FIRST covering the D-08 shared contract and escape-first safety. Pin these exact input→outcome expectations (this table is the canonical D-08 contract shared verbatim with Plan 02's pdf-export.js hardening):

    | Input | Renders emphasis? | Expected |
    | `**bold**` | yes | `<strong>bold</strong>` |
    | `*italic*` | yes | `<em>italic</em>` |
    | `** bold **` | no | literal `** bold **` (space adjacent inside markers) |
    | `2 * 3 * 4` | no | literal (legacy multiplication — no `<em>`) |
    | `a *b* c` | yes | `<em>b</em>` (inner markers hug `b`) |
    | `word * more` | no | literal (dangling single star) |

    Plus escape-first cases:
    - `<script>alert(1)</script>` in a note → output contains `&lt;script&gt;` and NO live `<script>` element (assert via innerHTML into a detached node then querySelector('script') === null)
    - `<img src=x onerror=1>` → escaped, no `<img>` element created
    - End with a count guard.
  </behavior>
  <action>
    Tighten the two emphasis regexes in `applyInline` (md-render.js L20-21) so `*`/`**` markers must hug non-whitespace: the character immediately after an opening marker and immediately before a closing marker must be non-whitespace. Keep the existing bold-first-then-italic order and the italic rule's `**`-avoidance (`(^|[^*])` prefix and `(?!\*)` suffix). CRITICAL Safari-compat constraint: do NOT use lookbehind (`(?<!\s)`) — Safari before 16.4 does not support it and this is a therapist-facing PWA; achieve the "closing marker preceded by non-whitespace" requirement with capture-group / character-class technique (same style as the existing `(^|[^*])` prefix), matching a non-whitespace char at the content boundaries. The content-body character class stays `[^*\n]`. escapeHtml runs on the ENTIRE input before any rule (unchanged) — the escape-first design must be preserved. The resulting behavior must match the pinned example table exactly, because Plan 02 hardens the PDF pipeline against the SAME table and Plan 05 asserts cross-pipeline agreement.
  </action>
  <verify>
    <automated>node tests/45-mdrender-escape.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-mdrender-escape.test.js` exits 0
    - `2 * 3 * 4` produces no `<em>` in the rendered output (source/behavior assertion)
    - `** bold **` renders literally (no `<strong>`)
    - `**bold**` still renders `<strong>bold</strong>` (positive case preserved)
    - `<script>` in note text produces no live script element when the output is assigned via innerHTML
    - `assets/md-render.js` `applyInline` contains no lookbehind token `(?<`
    - Test ends with a count guard
  </acceptance_criteria>
  <done>Inline emphasis hugs non-whitespace (legacy asterisks stay literal), escape-first is intact and proven XSS-safe, no lookbehind used, and behavior matches the shared D-08 contract table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MdRender.strip() markdown→plain-text helper (D-06)</name>
  <files>assets/md-render.js, tests/45-mdrender-strip.test.js</files>
  <read_first>
    - assets/md-render.js (the module return object L72-74 — you add `strip` alongside `render`; reuse the hardened emphasis logic from Task 2)
    - assets/pdf-export.js (`stripInlineMarkdown` L423-436 — the existing inline-only strip whose inline behavior your helper must AGREE with; your helper additionally strips block markers)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pattern 4 "Shared markdown→plain-text strip helper")
  </read_first>
  <behavior>
    Write tests/45-mdrender-strip.test.js FIRST:
    - `**bold**` → `bold`
    - `*italic*` → `italic`
    - `- item` → `item`
    - `1. item` → `item`
    - `# Heading` → `Heading`, `## H2` → `H2`, `### H3` → `H3`
    - `2 * 3 * 4` → `2 * 3 * 4` (legacy literal preserved — same D-08 hugging rule as render)
    - A multi-line note `## Progress\n- **big** step` → `Progress\nbig step` (block markers gone, inline markers gone, line structure reasonable for a compact display)
    - `strip("")` → `""` and `strip(null)` → `""`
    - End with a count guard.
  </behavior>
  <action>
    Add a `strip(markdown)` function to the `MdRender` module and expose it on the returned object (L72-74) next to `render`. It returns plain text (NOT HTML — callers assign via `textContent`, never innerHTML) with: inline emphasis markers removed using the SAME hardened rule as Task 2 (so `2 * 3 * 4` stays literal), and leading block markers removed per line — heading `#`/`##`/`###`, bullet `-`/`*`, ordered `N.`. Do NOT HTML-escape in strip (output is plain text for textContent). Preserve line breaks between note lines (a single `\n` join is fine for compact one-line/`textContent` consumers — the surface truncates as needed). This helper is consumed by the three compact surfaces in Plan 04 (`sessions.js:262`, `overview.js:848`, `add-session.js` spotlight ~1606) via `textContent`.
  </action>
  <verify>
    <automated>node tests/45-mdrender-strip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-mdrender-strip.test.js` exits 0
    - `assets/md-render.js` return object exposes `strip` (source assertion: `strip:` present in the returned object)
    - `MdRender.strip("**bold**")` === `"bold"`
    - `MdRender.strip("# Heading")` === `"Heading"`
    - `MdRender.strip("2 * 3 * 4")` === `"2 * 3 * 4"` (legacy literal)
    - `MdRender.strip(null)` === `""`
    - Test ends with a count guard
  </acceptance_criteria>
  <done>`MdRender.strip` returns marker-free plain text agreeing with the D-08 inline rule, ready for textContent consumption by compact surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note text → MdRender output | User-entered session note strings (untrusted) are transformed to HTML that Plan 04 will assign via innerHTML. This is the app's single sanctioned innerHTML path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering/Elevation | MdRender.render → innerHTML (stored XSS via note content) | high | mitigate | Escape-first design preserved: `escapeHtml` runs on the ENTIRE input before any markdown rule (Task 2). Test asserts `<script>`/`<img onerror>` render inert. No second escaper introduced. |
| T-45-02 | Tampering | MdRender.strip output | low | mitigate | strip returns plain text consumed only via textContent (Plan 04) — never innerHTML; no escaping needed but no HTML emitted either. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (research Package Legitimacy Audit: N/A). No new dependency; nothing to slopsquat. |
</threat_model>

<verification>
- `node tests/45-mdrender-lists.test.js && node tests/45-mdrender-escape.test.js && node tests/45-mdrender-strip.test.js` all exit 0
- `node tests/run-all.js` full suite stays green (no regression in existing md-render / export-preview consumers)
- The LOCKED `<br>`/paragraph contract comment in md-render.js is unchanged
</verification>

<success_criteria>
- MdRender renders ordered and nested (bullet + numbered) lists as correct nested HTML
- Legacy accidental asterisks stay literal; escape-first XSS safety proven falsifiable
- `MdRender.strip` exists and returns marker-free plain text
- Flat-list output byte-identical to pre-change (no regression)
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-01-SUMMARY.md` when done
</output>
