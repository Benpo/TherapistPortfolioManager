---
phase: 45-rich-text-rendering-export-foundation
plan: 04
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - assets/add-session.js
  - assets/app.css
  - assets/sessions.js
  - assets/overview.js
  - sessions.html
  - index.html
  - tests/45-read-mode-render.test.js
  - tests/45-compact-strip.test.js
  - tests/31-overview-render-hardening.test.js
  - tests/31-sessions-render-hardening.test.js
autonomous: true
requirements: [RTXT-06]
must_haves:
  truths:
    - "In read mode, saved note fields show rendered formatting (bold, lists, headings) — never literal ** or - tokens"
    - "Read-mode rendered HTML is written ONLY via MdRender.render (escape-first); a fallback path uses textContent, never raw innerHTML"
    - "Note-content headings in read mode use the subordinate register (weight+spacing led, no rule/diamond) scoped to the note container only"
    - "The three compact surfaces (sessions table cell, overview comments line, spotlight quote) show markdown STRIPPED to plain text via textContent"
    - "md-render.js is LOADED (a script tag, before the consumer script) on ALL THREE pages the D-06 surfaces live on — add-session.html (already), sessions.html, index.html — so MdRender.strip actually runs in production, not just in the jsdom test that loads it itself (BLOCKER 1)"
    - "The read-mode edit-toggle, copy-all, and export buttons still work; editing toggles back to raw textareas"
  artifacts:
    - "assets/add-session.js: .note-rendered overlay wired into setReadMode; spotlight quote uses MdRender.strip"
    - "assets/app.css: .note-rendered note-heading register (RTL-safe logical properties) + a .export-preview ol indent rule for MdRender's new ordered-list output in the Step-2 preview"
    - "assets/sessions.js + assets/overview.js: compact surfaces route through MdRender.strip"
    - "sessions.html + index.html: a `<script src=\"./assets/md-render.js\"></script>` tag added before the sessions.js / overview.js consumer script (BLOCKER 1 — md-render.js was previously loaded ONLY by add-session.html)"
    - "tests/45-read-mode-render.test.js, tests/45-compact-strip.test.js; extended 31-*-render-hardening locks"
  key_links:
    - "The read-mode innerHTML is the app's ONE sanctioned exception — route exclusively through MdRender; extend (never weaken) the 31-* render-hardening locks"
    - "Textarea stays the single source of truth: render on read-mode entry, tear down on edit-mode entry (Pitfall 5 — don't run autoGrow on a hidden textarea)"
    - ".note-rendered heading styles must NOT bleed onto .export-preview h1/h2/h3 (UI-SPEC §B selector-scoping)"
    - "LOAD ORDER: md-render.js must be loaded BEFORE sessions.js (sessions.html) and overview.js (index.html) so window.MdRender is defined when the consumer runs; without the script tag the strip guard falls back to raw markdown on two of three surfaces FOREVER in prod while the jsdom test (which loads md-render.js itself) false-passes — the repo's documented false-GREEN pattern (BLOCKER 1)"
---

<objective>
Stand up the primary NEW read surface: a rendered-HTML overlay in the add-session read mode so saved formatted notes display as styled text instead of literal `**`/`-` tokens (RTXT-06). Wire the three compact surfaces (sessions table cell, overview comments line, add-session spotlight quote) to strip markdown to plain text (D-06) — AND load md-render.js on the two pages those surfaces live on (sessions.html, index.html), which currently do not load it so the strip silently no-ops there (BLOCKER 1) — add the `.note-rendered` CSS register (UI-SPEC §B), and extend the render-hardening test locks to cover the new, narrow innerHTML exception.

Purpose: Reading a session is the everyday surface. Today `setReadMode(true)` only sets `textarea.readOnly` — a saved `**bold**` shows literally. This plan makes notes look right on read, using MdRender (Plan 01) as the single escape-first innerHTML path, while keeping every other surface textContent-only.

Output: Read-mode formatted rendering + stripped compact surfaces + scoped CSS + extended hardening locks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-PATTERNS.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md
@assets/export-modal.js
</context>

## Artifacts this phase produces (Plan 04 scope)

New/changed symbols introduced by this plan (for source-grounding / drift exclusion):

- `.note-rendered` — NEW CSS class + its scoped `h1/h2/h3/p/ul/ol/li` selectors (subordinate note-heading register, UI-SPEC §B). RTL-safe logical properties.
- Read-mode overlay wiring in `setReadMode` (add-session.js) — renders/tears down a `.note-rendered` sibling per note textarea; new helper(s) (e.g. `renderReadModeNotes` / `clearReadModeNotes`, executor's naming).
- Compact-surface strip calls: `MdRender.strip(...)` at `sessions.js:262`, `overview.js:848`, and the add-session spotlight quote (~L1606) — all via `textContent`.
- `<script src="./assets/md-render.js"></script>` tag added to `sessions.html` (before `sessions.js` at L136) and `index.html` (before `overview.js` at L374) — BLOCKER 1: md-render.js was loaded ONLY by add-session.html, so `MdRender` was `undefined` on the sessions table and overview pages and the strip guard fell back to raw markdown there permanently. (No SW change: md-render.js is already in PRECACHE_URLS at sw.js:82. No new docs friction: see the docs-gate note below.)
- Test files: `tests/45-read-mode-render.test.js`, `tests/45-compact-strip.test.js`; extensions to `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js`.

### Docs-gate note for the two new HTML files (BLOCKER 1, verified against scripts/docs-gate.js + scripts/lib/role-table.js)

`sessions.html` and `index.html` are root-level `*.html` pages, so per role-table.js they are watched **TRIGGER** files (raising a changelog demand + a per-file help demand). BOTH are already listed in a help topic's `covers[]` in `assets/help-content-en.js` (`sessions.html` → topic-new-session L168 + topic-past-sessions L182; `index.html` → topic-first-client L126, topic-dashboard L345, topic-filters L354, topic-next-session L364, topic-missing-clients L527). Per docs-gate.js L378-393, a covered TRIGGER file's help demand is SATISFIED whenever ANY EN help edit accompanies the push (`helpEdited` is a push-global boolean — WHICH topic is trusted, not verified). Plan 05 Task 2 already edits `assets/help-content-en.js` (and `assets/changelog-content-en.js`), so both HTML files **ride the existing Plan 05 help/changelog edits** — NO new `Help-Unaffected:` trailer and NO extra help topic is needed for them. (Contrast md-render.js, which has NO covering topic and therefore still needs its own `Help-Unaffected:` trailer per Plan 05.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-mode .note-rendered overlay via MdRender + scoped CSS register</name>
  <files>assets/add-session.js, assets/app.css, tests/45-read-mode-render.test.js</files>
  <read_first>
    - assets/add-session.js `setReadMode` (L306-338 — sets textarea.readOnly today), `resizeReadModeTextareas`/`clearReadModeTextareas` (L267-278), `autoGrow` (L66-73), `readModeTextareas` collection (L168 = `document.querySelectorAll(".session-textarea")`)
    - add-session.html note fields (the 7 `.session-textarea` note fields and their ids: `heartShieldEmotions`, `trappedEmotions`, `sessionInsights`, `limitingBeliefs`, `additionalTech`, `sessionComments`, `customerSummary`)
    - assets/export-modal.js `exportUpdatePreview` (L533-543 — the proven MdRender→innerHTML wiring with textContent fallback to copy)
    - assets/app.css `.export-preview` rendered-markdown block (L3555-3579 — the baseline heading/list CSS to adapt, using logical properties)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md (§ A screen note body, § B note-heading subordinate register 17/16/15px @600, selector-scoping requirement)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pattern 1 read-mode overlay; Pitfall 5 overlay/autogrow desync)
  </read_first>
  <behavior>
    Write tests/45-read-mode-render.test.js FIRST (copy the 31-*-render-hardening shape: load real add-session.js into an isolated jsdom window with the note textareas present, inject a key-returning App.t stub, call setReadMode directly — no async DOMContentLoaded vacuous-green):
    - Enter read mode with a note field value `**bold**\n- a\n- b`: assert a `.note-rendered` sibling exists, is visible, and its innerHTML contains `<strong>bold</strong>` and a `<ul>` (rendered, not literal `**`).
    - Assert the source textarea is hidden while the overlay shows.
    - Enter read mode with `<script>alert(1)</script>`: assert the overlay produces no live `<script>` element (escape-first via MdRender).
    - Toggle back to edit mode: assert the `.note-rendered` overlay is torn down and the textarea is visible/editable again with its original value intact (single source of truth).
    - Assert the fallback: if `window.MdRender` is undefined, the overlay uses textContent (literal markdown), never raw innerHTML.
    - End with a count guard.
  </behavior>
  <action>
    Extend `setReadMode` (add-session.js L306-338) so that for each of the 7 note `.session-textarea` fields, entering read mode renders `window.MdRender.render(textarea.value)` into a sibling `.note-rendered` element (create it if absent), hides the textarea, and shows the rendered node; entering edit mode reverses it. ALL sessions — pre- and post-v1.4 — render through this one uniform MdRender path (D-07: no per-session format flag, no new field); hand-typed `#`/`##`/`###` note lines render as headings here too (D-01), at the subordinate register. (tear down / hide the overlay, show the textarea) — mirror the `clearReadModeTextareas` lifecycle (L274). Route the rendered HTML EXCLUSIVELY through MdRender (escape-first, XSS-safe); when `window.MdRender` is unavailable, fall back to `textContent` (literal), NEVER raw innerHTML — copy the exact pattern from `exportUpdatePreview` (export-modal.js L537-542). Keep the textarea as the single source of truth; do NOT run `autoGrow`/`resizeReadModeTextareas` on a hidden textarea (Pitfall 5). Preserve the existing edit-toggle, copy-all (copySessionBtn), and export (exportSessionBtn) flow. In app.css, add a `.note-rendered` block adapting the `.export-preview` conventions (logical properties: `padding-inline-start:24px` for ul/ol, `margin-block` for headings) and the UI-SPEC §B subordinate note-heading register — Note H1/H2/H3 at 17/16/15px, weight 600, NO border-bottom / NO rule / NO diamond, differentiated from body by weight+spacing not size jump. ALSO add a `.export-preview ol { padding-inline-start: 24px; }` rule: app.css currently styles only `.export-preview ul` (:3577), and MdRender's NEW `<ol>` output (Plan 01) now appears in the already-wired Step-2 export preview — without this rule ordered lists there render at browser-default indent. Add it alongside the `.note-rendered ol` rule (NOTE 5). SCOPE these heading styles to `.note-rendered` ONLY so they never bleed onto `.export-preview h1/h2/h3` (UI-SPEC §B selector-scoping requirement). Must be WCAG-AA legible in dark mode and RTL using existing `--color-text`/`--color-surface` tokens (no new colors).
  </action>
  <verify>
    <automated>node tests/45-read-mode-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-read-mode-render.test.js` exits 0
    - Entering read mode on `**bold**\n- a\n- b` produces a visible `.note-rendered` node whose innerHTML contains `<strong>bold</strong>` and `<ul>` (behavior assertion)
    - The source textarea is hidden while the overlay is shown
    - A `<script>` note value produces no live script element in the overlay
    - Toggling to edit mode tears down the overlay and restores the editable textarea with its original value
    - `assets/add-session.js` overlay code writes innerHTML only from `MdRender.render(...)` (source assertion) with a `textContent` fallback branch
    - `assets/app.css` contains a `.note-rendered` selector; note-heading rules are scoped under `.note-rendered` (not bare `h1/h2/h3`)
    - `assets/app.css` adds a `.export-preview ol` rule (matching the existing `.export-preview ul` indent) so MdRender ordered lists in the Step-2 preview indent correctly (NOTE 5)
    - Test ends with a count guard
  </acceptance_criteria>
  <done>Read mode shows rendered formatting through the escape-first MdRender path, tears down cleanly on edit, preserves the toggle/copy/export flow, and the note-heading CSS is subordinate and scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip markdown to plain text on the three compact surfaces (D-06) + load md-render.js on sessions.html / index.html (BLOCKER 1)</name>
  <files>assets/sessions.js, assets/overview.js, assets/add-session.js, sessions.html, index.html, tests/45-compact-strip.test.js</files>
  <read_first>
    - assets/sessions.js L262 (`trappedCell.textContent = session.trappedEmotions || "-"`)
    - assets/overview.js L848 (`commentsLine.textContent = ` ...comments... `)
    - assets/add-session.js spotlight quote (~L1606-1614 — `refs.summaryQuote.textContent = summaryText`)
    - assets/md-render.js (`MdRender.strip` added in Plan 01 Task 3 — the helper these surfaces call)
    - sessions.html (script block L106-136 — `md-render.js` is NOT present; `sessions.js` loads at L136; add-session.html:612 shows the canonical `<script src="./assets/md-render.js"></script>` tag to copy)
    - index.html (script block L348-374 — `md-render.js` is NOT present; `overview.js` loads at L374)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pattern 4 strip helper; D-06)
  </read_first>
  <behavior>
    Write tests/45-compact-strip.test.js FIRST:
    - sessions.js trapped-emotions cell: a session with `**bold** trapped\n- x` renders `bold trapped x` (or equivalent stripped one-line text) via textContent — assert NO `<strong>`, NO literal `**`.
    - overview.js comments line: a comment `## Note\n**strong**` displays stripped (`Note strong`) via textContent.
    - add-session spotlight quote: a customerSummary with `**bold**` shows `bold` via textContent.
    - Assert the values are set with textContent (no innerHTML) — the render-hardening convention stays intact for these surfaces.
    - SOURCE ASSERTION (BLOCKER 1 false-GREEN guard): read the raw `sessions.html` and `index.html` files and assert each contains a `md-render.js` `<script>` tag AND that the tag appears BEFORE the consumer script (`md-render.js` index < `sessions.js` index in sessions.html; `md-render.js` index < `overview.js` index in index.html). Without this, the jsdom compact-strip test loads md-render.js itself and false-passes while production shows literal `**` on both pages forever.
    - End with a count guard.
  </behavior>
  <action>
    (1) Add `<script src="./assets/md-render.js"></script>` to `sessions.html` (immediately before the `<script src="./assets/sessions.js"></script>` line at L136) and to `index.html` (immediately before `<script src="./assets/overview.js"></script>` at L374). This is BLOCKER 1: md-render.js was loaded ONLY by add-session.html:612, so on the sessions table and overview pages `window.MdRender` was `undefined` and the strip guard below fell back to raw markdown PERMANENTLY in production. Load order matters — md-render.js is a synchronous IIFE defining `window.MdRender`, so its tag MUST precede the consumer script. No SW change needed (md-render.js is already in PRECACHE_URLS at sw.js:82). No new docs-gate trailer needed for these two HTML files — see the "Docs-gate note" above: both are already in a help topic's `covers[]`, and Plan 05's existing help edit satisfies their demand (docs-gate.js L382).
    (2) Route the three compact surfaces through `MdRender.strip(...)` before assigning to `textContent`: `sessions.js:262` (trapped-emotions cell), `overview.js:848` (comments line — strip `commentsText` before interpolating into the label), and the add-session spotlight quote (~L1606, `summaryText`). These stay `textContent` assignments — NOT the innerHTML exception. Keep the `MdRender` availability guard (fall back to the raw string via textContent if absent — still safe, just shows literal markers) as defence in depth, but with step (1) done the primary strip path now actually runs on all three surfaces. Do not change the surrounding DOM structure or i18n label wiring.
  </action>
  <verify>
    <automated>node tests/45-compact-strip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-compact-strip.test.js` exits 0
    - The sessions trapped-emotions cell shows stripped plain text (no `**`, no `<strong>`) via textContent
    - The overview comments line shows stripped plain text
    - The spotlight quote shows stripped plain text
    - All three assign via textContent (source assertion — no innerHTML on these sites)
    - `sessions.html` contains a `md-render.js` script tag positioned before `sessions.js`; `index.html` contains one positioned before `overview.js` (source assertion on the raw HTML — BLOCKER 1 false-GREEN guard)
    - Test ends with a count guard
  </acceptance_criteria>
  <done>The three compact surfaces display markdown stripped to plain text via textContent on ALL their pages (md-render.js now loaded on sessions.html + index.html); no styled HTML, no layout risk, hardening convention intact.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the 31-* render-hardening locks to cover the new read-mode innerHTML exception</name>
  <files>tests/31-overview-render-hardening.test.js, tests/31-sessions-render-hardening.test.js</files>
  <read_first>
    - tests/31-overview-render-hardening.test.js (existing textContent-only lock for overview.js interpolated-i18n sites)
    - tests/31-sessions-render-hardening.test.js (existing textContent-only lock for the sessions.js view-button site)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Security Domain "Regression that weakens the textContent-only hardening"; PATTERNS "Extend (never weaken)")
  </read_first>
  <action>
    EXTEND (never weaken) the two 31-* render-hardening locks so they continue to prove the compact/interpolated sites in sessions.js and overview.js remain textContent-only AFTER this phase's changes (the strip helper output still goes through textContent). The read-mode `innerHTML` in add-session.js is a DELIBERATE, narrow exception — add an assertion (in whichever 31-* file is the natural home, or a brief note referencing tests/45-read-mode-render.test.js) documenting that the ONLY sanctioned innerHTML note path is MdRender-routed and lives in the read-mode overlay, so the textContent-only convention elsewhere is unchanged. Do not delete or relax any existing assertion; the existing characterization baselines must stay green.
  </action>
  <verify>
    <automated>node tests/31-overview-render-hardening.test.js && node tests/31-sessions-render-hardening.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/31-overview-render-hardening.test.js && node tests/31-sessions-render-hardening.test.js` exit 0
    - The two files still assert textContent-only for the sessions.js/overview.js sites (no existing assertion removed or weakened)
    - The strip-helper compact-surface changes did not break the existing characterization baselines
  </acceptance_criteria>
  <done>The render-hardening locks still enforce textContent-only on the compact surfaces; the read-mode innerHTML exception is explicitly documented as MdRender-routed and narrow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved note text → read-mode innerHTML | The read-mode overlay is the app's single sanctioned innerHTML write of user content — it must route exclusively through MdRender (escape-first). |
| saved note text → compact surface textContent | Compact surfaces stay textContent-only; strip removes markers but emits no HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering/Elevation | Read-mode overlay innerHTML (stored XSS) | high | mitigate | innerHTML written ONLY from MdRender.render output (escape-first); textContent fallback when MdRender absent; test asserts `<script>` renders inert. |
| T-45-07 | Tampering | Regression weakening textContent-only hardening on other surfaces | medium | mitigate | Task 3 extends (never weakens) the 31-* locks; compact surfaces stay textContent; the innerHTML exception is single, narrow, test-locked. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase; no new dependency. |
</threat_model>

<verification>
- `node tests/45-read-mode-render.test.js && node tests/45-compact-strip.test.js` exit 0
- `node tests/31-overview-render-hardening.test.js && node tests/31-sessions-render-hardening.test.js` exit 0 (no weakened assertions)
- `node tests/run-all.js` full suite green
- Real installed-Safari read-mode + dark-mode + RTL visual check is deferred to the Plan 05 phase-gate checkpoint (jsdom cannot see rendering/RTL)
</verification>

<success_criteria>
- Read mode shows formatted notes (bold/lists/headings), never literal tokens, via the escape-first MdRender path
- Compact surfaces show stripped plain text via textContent
- Edit-toggle/copy/export flow preserved; overlay tears down cleanly on edit
- Note-heading CSS subordinate + scoped; RTL/dark-mode legible with existing tokens
- Render-hardening locks extended, not weakened; full suite green
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-04-SUMMARY.md` when done
</output>
