---
phase: 45-rich-text-rendering-export-foundation
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [RTXT-06, RTXT-07, RTXT-08]
files_modified:
  - assets/md-render.js
  - assets/pdf-export.js
  - tests/45-pipeline-agreement.test.js
  - tests/45-mdrender-lists.test.js
  - tests/45-pdf-nested-lists.test.js
  - assets/changelog-content-en.js

must_haves:
  truths:
    - "Reading a saved session where a text line is immediately followed by a `#`/`##`/`###` line renders a REAL heading in read mode (never a literal `## X` with <br>), matching what the PDF already produces — closes GAP-45-01, defends RTXT-06."
    - "A `#`/`##`/`###` line that appears after text INSIDE a heading's body remainder (e.g. `## H\\ntext\\n### Sub`) also renders as a real heading — the heading-remainder recursion re-enters the same split."
    - "A marker-only line — bare `-`/`*`/`N.` with OPTIONAL trailing whitespace — renders as an EMPTY list item consistently, so `1.` and `1. ` are identical and nothing typed disappears, in BOTH read mode (MdRender) and PDF (parseMarkdown) — closes GAP-45-02 per Ben's 2026-07-13 lock, defends RTXT-06 + RTXT-07."
    - "`1.5 mg` (and any `\\d+\\.` immediately followed by a non-whitespace character) stays LITERAL paragraph text — never a list — in both pipelines (the 1.5-guard is preserved)."
    - "The two emphasis regexes in md-render.js applyInline stay CHARACTER-IDENTICAL to pdf-export.js stripInlineMarkdown's; the D-08 corpus, the randomized strip-vs-strip pass, and the parseInlineBold join-invariant all still hold (D-08 untouched)."
    - "Verbatim markdown copy/share (RTXT-08/D-10) and the encrypted `.sgbackup` round-trip (RTXT-10) are unaffected — these are renderer-only changes; no export-modal, storage, or backup source is touched."
  artifacts:
    - "assets/md-render.js — renderBlock() gains a text-then-heading split (mirrors the WARNING-3 text-then-list split); isListItem()/listType()/stripListMarker() and the strip() per-line block regex adopt the marker-only rule."
    - "assets/pdf-export.js — parseMarkdown() list-detection, per-item ordered-ness, ordinal capture, marker-strip, and paragraph-terminator regexes adopt the same marker-only rule."
    - "tests/45-pipeline-agreement.test.js — new cross-pipeline cases: text-then-heading, heading-remainder-then-heading, marker-only bare + trailing-space for BOTH marker types, and the 1.5 literal guard; count guard bumped to the new total."
    - "tests/45-mdrender-lists.test.js + tests/45-pdf-nested-lists.test.js — RED-first per-pipeline cases for both gaps; count guards bumped."
    - "assets/changelog-content-en.js — the staged v1.4.0 EN entry amended to reflect the rendering-robustness fixes (satisfies the docs-gate changelog demand)."
  key_links:
    - "In renderBlock, the heading-scan split MUST run BEFORE the list branch so a compound `text\\n## H\\n- item` block recurses to <p> + <h2> + <ul> correctly (heading at index 0 is already handled by the block-start heading branch above; the scan only fires for a heading at index > 0)."
    - "In pdf-export.js parseMarkdown, the paragraph-terminator regexes MUST be updated in lockstep with the list-detection regexes to the SAME marker rule, or the paragraph collector and the list grouper disagree on marker-only lines."
    - "Detection uses a LOOKAHEAD `(?=\\s|$)` after the marker; stripping/capture consume `(?:\\s+|$)`. Both forms accept marker-only lines AND preserve the 1.5-guard (a `\\d+\\.` followed by a digit fails the lookahead)."
    - "The emphasis regexes (bold + italic) in applyInline / stripInlineMarkdown / parseInlineBold MUST NOT be touched — the character-identity gate in 45-pipeline-agreement.test.js reads them from source."
---

<objective>
Close the two diagnosed UAT gaps from Ben's 2026-07-13 real-device round (45-UAT.md ## Gaps), both bounded renderer defects, without touching the escape-first security path, the D-08 emphasis regexes, or any storage/export surface.

- **GAP-45-01 (major):** a `#`/`##`/`###` line typed directly after a text line renders literally in read mode (`<p>text<br>## X</p>`) while the PDF already renders it as a heading — a cross-pipeline divergence of exactly the class the 45-05 agreement corpus exists to prevent. Fix in md-render.js only; PDF needs NO change.
- **GAP-45-02 (minor):** marker-only lines are trailing-space-sensitive (`1. ` parses as an empty list item, `1.` stays a paragraph), so consecutive lines merge and typed lines visually disappear. Ben's LOCKED decision: marker-only lines (bare `-`/`*`/`N.` + optional trailing whitespace) parse as EMPTY list items consistently in BOTH pipelines (`1.` ≡ `1. `, CommonMark-aligned); the `1.5 mg` literal guard is preserved. Touches md-render.js AND pdf-export.js.

Both fixes are locked into tests/45-pipeline-agreement.test.js so MdRender ⟺ PDF can never silently diverge on these classes again.

Purpose: read mode and PDF agree on headings-after-text and on marker-only lines; no invisible-whitespace surprises; the therapist sees what they typed.
Output: extended md-render.js + pdf-export.js renderers, extended agreement + per-pipeline test corpora, an amended v1.4.0 changelog entry, and the docs-gate trailers that keep the phase's Definition-of-Done satisfiable at push time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@CLAUDE.md
</context>

<coverage_audit>
Gap-closure coverage (source = 45-UAT.md ## Gaps):

| Gap | Severity | Task | Requirement defended |
|-----|----------|------|----------------------|
| GAP-45-01 text-then-heading renders literal in read mode | major | Task 1 | RTXT-06 |
| GAP-45-02 marker-only lines trailing-space-sensitive | minor | Task 2 | RTXT-06, RTXT-07 |
| Cross-pipeline agreement lock (both classes) | — | Task 1 + Task 2 (agreement corpus) | RTXT-06/07 |
| Verbatim pass-through invariant unbroken (RTXT-08/D-10) | — | Task 1 + Task 2 (renderer-only, full-suite green) | RTXT-08 |
| Docs hard-gate (user-facing renderer change) | — | Task 3 | — |

No UAT item is left unplanned. Out of scope (correctly excluded): the second-backup-import bug (UAT test 7 — filed as a separate v1.4 ship-blocker, backup.js untouched this phase); the 45-06 real-device checkpoint (re-run after this plan lands).
</coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-45-01 — split text-then-heading inside a block in md-render.js renderBlock</name>
  <files>assets/md-render.js, tests/45-mdrender-lists.test.js, tests/45-pipeline-agreement.test.js</files>

  <read_first>
    - assets/md-render.js — renderBlock() (current lines 92-140): the block-start heading branch (its remainder recursion is the pattern to mirror), the `var lines = block.split(...)` computation (line 108), and the WARNING-3 text-then-list split (lines 118-122) which this fix copies.
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md — GAP-45-01 diagnosis + agreed fix direction.
    - tests/45-mdrender-lists.test.js — the render() harness, the existing text-then-list case (lines 94-99), and the EXPECTED_COUNT guard (line 113) to bump.
    - tests/45-pipeline-agreement.test.js — the TEXT-THEN-LIST agreement test (lines 254-271) is the shape to copy for text-then-heading; the EXPECTED count guard (line 274).
    - assets/pdf-export.js — parseMarkdown() paragraph collector (lines 703-715): confirm the PDF ALREADY terminates a paragraph at a heading line (`!/^#{1,3}\s+/.test(...)`), so the PDF side needs NO change and the read-mode fix converges toward existing PDF behavior.
  </read_first>

  <behavior>
    RED-first. Write these as failing assertions against the UNCHANGED renderer, confirm they fail, then implement.
    - MdRender.render('Emotions:\n## Summary') === '<p>Emotions:</p><h2>Summary</h2>' (currently returns a literal '<p>Emotions:<br>## Summary</p>' — RED).
    - MdRender.render('note text\n# Title') renders '<h1>Title</h1>' after '<p>note text</p>'; the string contains NO literal '# Title'.
    - Heading-remainder-then-heading: MdRender.render('## H\ntext\n### Sub') === '<h2>H</h2><p>text</p><h3>Sub</h3>' (currently the remainder 'text\n### Sub' renders '<p>text<br>### Sub</p>' — RED).
    - Compound: MdRender.render('text\n## H\n- item') === '<p>text</p><h2>H</h2><ul><li>item</li></ul>'.
    - Regression-lock (must stay GREEN): the LOCKED single-newline contract render('line1\nline2') === '<p>line1<br>line2</p>' is unchanged (a plain paragraph with no heading line is untouched); render('Emotions:\n- anger') === '<p>Emotions:</p><ul><li>anger</li></ul>' (text-then-list unchanged); a `####` (4-hash) line is NOT a heading and stays literal paragraph text.
    - In tests/45-pipeline-agreement.test.js: add a text-then-heading agreement case and a heading-remainder-then-heading agreement case proving BOTH pipelines agree — MdRender emits real <h2>/<h3> (no literal `##`/`###`) AND pdf-export parseMarkdown('Emotions:\n## Summary') yields a para block {type:'para', text:'Emotions:'} followed by a heading block {type:'heading', level:2, text:'Summary'} (and for the remainder case, heading L2 → para 'text' → heading L3).
  </behavior>

  <action>
    In renderBlock(), immediately AFTER the `var lines = block.split(/\r?\n/).filter(...)` computation and BEFORE the `firstListIdx` list scan, add a text-then-heading split that mirrors the existing WARNING-3 text-then-list split:

    - Scan `lines` for the FIRST line matching the heading test `/^#{1,3}\s+/` (same 1-3-hash-then-whitespace shape as the block-start heading branch and the pdf-export paragraph terminator). Record its index as firstHeadingIdx.
    - If firstHeadingIdx is greater than 0, return renderBlock(lines.slice(0, firstHeadingIdx).join("\n")) concatenated with renderBlock(lines.slice(firstHeadingIdx).join("\n")) — render the leading text lines as their own sub-block, then recurse on the remainder STARTING at the heading line. The remainder recursion re-enters renderBlock, whose block-start heading branch then consumes the heading (and any `body`/further-heading remainder), which is why one split also fixes the `### Sub` after text inside a heading's body remainder (Ben's image 2).
    - A heading at index 0 never reaches this scan: a block that starts with a heading is fully handled by the block-start heading branch above (which always matches `/^(#{1,3})\s+.../`), so the scan only fires for firstHeadingIdx > 0.

    Do NOT change the block-start heading branch, the WARNING-3 list split, the list-run/tail recursion, the paragraph `<br>` path, or applyInline. The PDF pipeline is NOT touched by this task — parseMarkdown already terminates paragraphs at heading lines.

    Explain in a code comment (concept-level, no fenced examples) that this closes GAP-45-01 and converges read mode toward pdf-export's already-correct paragraph-terminates-at-heading behavior.

    Then extend the two test files with the behavior cases above and bump each EXPECTED / EXPECTED_COUNT guard so passed+failed equals the new actual total.
  </action>

  <verify>
    <automated>node tests/45-mdrender-lists.test.js && node tests/45-pipeline-agreement.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/45-mdrender-lists.test.js` exits 0; its count guard equals the new number of cases (RED cases now GREEN, LOCKED single-newline + text-then-list cases still GREEN).
    - `node tests/45-pipeline-agreement.test.js` exits 0; the new text-then-heading and heading-remainder-then-heading agreement cases pass and the EXPECTED count guard equals the new total.
    - `assets/pdf-export.js` is byte-unchanged by this task (verify with `git diff --stat assets/pdf-export.js` showing no change from this task's commits).
    - MdRender.render('Emotions:\n## Summary') produces the substring '<h2>Summary</h2>' and does NOT contain the substring '## Summary'.
  </acceptance_criteria>

  <done>
    A `#`/`##`/`###` line typed directly under a text line (or under a heading's body remainder) renders as a real heading in read mode, character-for-character consistent with the PDF; the LOCKED single-newline and text-then-list contracts are intact; pdf-export.js is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-45-02 — marker-only lines parse as empty list items consistently in BOTH pipelines</name>
  <files>assets/md-render.js, assets/pdf-export.js, tests/45-mdrender-lists.test.js, tests/45-pdf-nested-lists.test.js, tests/45-pipeline-agreement.test.js</files>

  <read_first>
    - assets/md-render.js — the list helpers isListItem() (line 37), listDepth() (line 43), listType() (line 49), stripListMarker() (line 52), buildList/buildListLevel (lines 57-83), and the strip() per-line block-marker regex (line 170). listDepth() is NOT changed (marker-only lines still carry depth from leading spaces).
    - assets/pdf-export.js — parseMarkdown() (lines 619-727): the list-block entry detect (line 641), the first-line ordered-ness `listOrdered` (line 659), the while-loop continue condition (line 661), per-item `itemOrdered` (line 679), the ordinal capture `ordMatch` (line 681), the two marker-strip replaces (lines 684, 691), the per-item depth calc (lines 677-678, unchanged), and the paragraph-terminator regexes (lines 708-711). Do NOT touch stripInlineMarkdown (lines 442-445) or parseInlineBold (lines 496-579) — those are the pinned emphasis functions.
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md — GAP-45-02 diagnosis + Ben's LOCKED 2026-07-13 decision (marker-only = empty list item in BOTH pipelines; `1.` ≡ `1. `; 1.5-guard preserved; lone `-` separator → empty bullet is an accepted side effect).
    - tests/45-mdrender-lists.test.js and tests/45-pdf-nested-lists.test.js — the render()/parse harnesses and their count guards (line 113 / line 191).
    - tests/45-pipeline-agreement.test.js — the nested-list agreement helpers pdfListTags/mdListTags (lines 211-232) and the EXPECTED count guard (line 274).
    - assets/sessions.js (line ~266-270) — the compact-surface call site `window.MdRender.strip(trappedRaw)` then `|| "-"`: confirm the `|| "-"` fallback absorbs an empty strip() result, so the strip() rule change is safe here.
  </read_first>

  <behavior>
    RED-first. Confirm each fails/behaves-old on the UNCHANGED source, then implement.
    MdRender (tests/45-mdrender-lists.test.js):
    - MdRender.render('1.') === '<ol><li></li></ol>' (currently '<p>1.</p>' — RED).
    - MdRender.render('1.') === MdRender.render('1. ')  (the `1.` ≡ `1. ` lock — currently they differ: '1. ' already renders '<ol><li></li></ol>').
    - MdRender.render('-') === '<ul><li></li></ul>' and MdRender.render('- ') === '<ul><li></li></ul>' (bare and trailing-space bullet).
    - MdRender.render('*') === '<ul><li></li></ul>' (bare `*` bullet, per Ben's explicit `-`/`*`/`N.`).
    - Regression-lock (stays GREEN): MdRender.render('1.5 mg') === '<p>1.5 mg</p>' (1.5-guard — never a list); render('- a\n- b') === '<ul><li>a</li><li>b</li></ul>' and render('1. a\n2. b') === '<ol><li>a</li><li>b</li></ol>' (content-bearing lists unchanged); the nested cases '- a\n  - b\n- c', '1. a\n   1. b', '- a\n  1. b' render byte-identically to today.
    PDF (tests/45-pdf-nested-lists.test.js):
    - parseMarkdown('1.')[0] is a {type:'list'} block whose items[0] === {text:'', ordinal:1, depth:0, ordered:true} (currently a para block — RED).
    - parseMarkdown('1. ') yields the SAME list/item shape as parseMarkdown('1.') (the `1.` ≡ `1. ` lock on the PDF side).
    - parseMarkdown('-')[0] is a {type:'list'} block whose items[0] === {text:'', depth:0, ordered:false}; same for '- ' and '*'.
    - Regression-lock (stays GREEN): parseMarkdown('1.5 mg')[0].type === 'para' (1.5-guard); the existing nested-list assertions in this file are unchanged.
    Cross-pipeline (tests/45-pipeline-agreement.test.js):
    - Add a marker-only agreement case proving, for each of `-`, `- `, `*`, `1.`, `1. `: MdRender renders an empty list item of the correct list type AND pdf-export parseMarkdown produces a list block with an empty-text item of the matching ordered-ness — i.e. neither pipeline drops the line and both agree on ol-vs-ul.
    - Add a 1.5-guard agreement case: `1.5 mg` is a paragraph (not a list) in BOTH pipelines.
  </behavior>

  <action>
    Adopt one marker rule across both renderers. DETECTION uses a lookahead after the marker: the marker (`[-*]` bullet, or `\d+\.` ordinal) must be followed by whitespace OR end-of-line — expressed as `(?=\s|$)`. STRIPPING / ordinal-capture consume the trailing whitespace-or-end — expressed as `(?:\s+|$)`. Both forms accept a marker-only line AND keep the 1.5-guard, because in `1.5` the `\d+\.` is followed by `5` (neither whitespace nor end), so the ordinal branch fails and the line is not a list.

    assets/md-render.js:
    - isListItem(line): change the required-trailing-whitespace form to the lookahead form so the marker may be followed by whitespace OR end-of-line (bullet OR `\d+\.` ordinal), preserving the leading-`\s*` allowance.
    - listType(line): the ordered test becomes `\d+\.` followed by whitespace-or-end (lookahead), else "ul" — so a bare `1.` is recognised as "ol", not "ul".
    - stripListMarker(line): the strip regex consumes the marker then `(?:\s+|$)`, so a bare marker strips to an empty string (`-` → "", `1.` → "").
    - strip() per-line block regex (line 170): apply the SAME marker rule (marker then `(?:\s+|$)`) so the compact-surface plain-text helper is internally consistent with the render path — `1.` ≡ `1. ` there too. This is safe: every compact call site guards the result with `|| "-"`, which absorbs an empty string. The 1.5-guard still holds (`1.5 mg` matches neither branch).
    - listDepth() is UNCHANGED — marker-only lines still carry nesting depth from their leading spaces (floor(spaces/2)); an empty item at depth N is valid.
    - Do NOT touch applyInline's two emphasis regexes.

    assets/pdf-export.js parseMarkdown() — apply the same rule to EVERY marker regex so the list grouper and the paragraph collector stay in agreement (the paragraph terminator MUST change in lockstep with the list detector):
    - list-block entry detect (bullet + ordered) → lookahead form.
    - `listOrdered` first-line ordered-ness → `\d+\.` lookahead form.
    - while-loop continue condition (bullet + ordered) → lookahead form.
    - per-item `itemOrdered` → `\d+\.` lookahead form.
    - `ordMatch` ordinal capture → `/^\s*(\d+)\.(?:\s+|$)/` so a bare `5.` still captures ordinal 5 (typed-ordinal contract preserved — do NOT position-derive).
    - the two marker-strip `.replace(...)` calls → consume `(?:\s+|$)` so a bare marker strips to "".
    - BOTH paragraph-terminator regexes (bullet + ordered) → lookahead form, matching the detector.
    Do NOT touch stripInlineMarkdown or parseInlineBold (the pinned emphasis functions).

    Comment each change (concept-level, no fenced examples) citing GAP-45-02 and Ben's 2026-07-13 lock, and note the 1.5-guard is preserved by the lookahead.

    Then add the behavior cases above to all three test files and bump each count guard to the new actual total.
  </action>

  <verify>
    <automated>node tests/45-mdrender-lists.test.js && node tests/45-pdf-nested-lists.test.js && node tests/45-pipeline-agreement.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/45-mdrender-lists.test.js`, `node tests/45-pdf-nested-lists.test.js`, and `node tests/45-pipeline-agreement.test.js` each exit 0 with count guards equal to their new totals.
    - The character-identity + no-lookbehind + D-08 corpus + randomized join-invariant tests in 45-pipeline-agreement.test.js still pass (the emphasis regexes were not touched).
    - MdRender.render('1.') and MdRender.render('1. ') return the identical string; parseMarkdown('1.') and parseMarkdown('1. ') return list blocks with identical item shape.
    - MdRender.render('1.5 mg') contains '<p>' and no list tag; parseMarkdown('1.5 mg')[0].type === 'para'.
  </acceptance_criteria>

  <done>
    Marker-only lines (`-`/`*`/`N.`, bare or with trailing whitespace) render as empty list items identically in read mode and PDF, nothing typed disappears, `1.` ≡ `1. `, and `1.5 mg` remains literal text in both pipelines; the D-08 emphasis functions and the character-identity gate are intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: satisfy the docs hard-gate for the user-facing renderer fixes</name>
  <files>assets/changelog-content-en.js</files>

  <read_first>
    - CLAUDE.md — the "Definition of Done — docs must not rot" contract: watched `assets/*` code raises a changelog demand (satisfied by an EN `changelog-content-en.js` edit) and per-file help demand; the three trailer keys and their casing; `Help-Unaffected:` is honored from any commit in the range, one trailer may name multiple comma-separated files with a mandatory em-dash reason.
    - .planning/phases/45-rich-text-rendering-export-foundation/45-05-SUMMARY.md (lines 74-90) — the staged v1.4.0 EN changelog entry (anchor `v1-4`), the `changelog-integrity-locale.test.js` parity gate (skeleton + highlight-count parity across en/he/de/cs), and the exact `Help-Unaffected: assets/md-render.js — ...` trailer form already used for md-render.js.
    - HELP-MAP.md (lines 20-21) — `assets/pdf-export.js` is owned by `review-export/topic-single-export`; `assets/md-render.js` has NO covering help topic (docs-gate trigger requiring a Help-Unaffected trailer).
    - assets/changelog-content-en.js — the v1.4.0 entry to amend (schema: 2-4 highlights, `{new, improved}` categories, no emojis, client/session terminology).
  </read_first>

  <action>
    Amend the STAGED v1.4.0 EN changelog entry (anchor `v1-4`) in assets/changelog-content-en.js to reflect the two rendering-robustness fixes — e.g. refine an existing `improved` highlight so it states that headings and lists render reliably regardless of blank lines or trailing spaces. Keep the highlight COUNT and the category-key skeleton IDENTICAL to what ships now so the `changelog-integrity-locale.test.js` parity gate stays green with EN-only edits; if any structural change is unavoidable, mirror the SAME skeleton into changelog-content-{he,de,cs}.js and add those files to the commit. Do NOT bump APP_VERSION (stays 1.3.0 mid-milestone; the v1.4 semver footer flips at the v1.4 release boundary per the version-bump convention). This EN edit satisfies the global changelog demand for both watched files touched in Tasks 1-2.

    Because pdf-export.js's owning help topic (`review-export/topic-single-export`) already states the PDF preserves bold/italics/lists/nesting/headings, and md-render.js has no covering topic, the user-visible behavior needs no new help copy — waive the per-file help demand with a trailer on a commit in the pushed range (exact casing):

    `Help-Unaffected: assets/md-render.js, assets/pdf-export.js — rendering-robustness gap fixes (text-then-heading + marker-only lines); behavior already documented under review-export + starting-a-session help topics and the v1.4 changelog entry`

    Record this trailer in the plan's SUMMARY so the push carries it. Do NOT run a docs-only deploy for this change — let it ride with the next code push per the avoid-docs-only-deploys convention.
  </action>

  <verify>
    <automated>node tests/run-all.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/run-all.js` exits 0 — the FULL suite is green (renderer behavior, 45-pipeline-agreement character-identity, 31-* render-hardening locks, changelog-integrity + changelog-integrity-locale parity), proving no regression from Tasks 1-2 or the changelog edit.
    - `git diff assets/changelog-content-en.js` shows an edit to the v1.4.0 entry (the changelog demand is satisfied by any EN edit).
    - The SUMMARY records the exact `Help-Unaffected:` trailer text (case-sensitive) to be placed on a commit in the pushed range so `scripts/docs-gate.js` passes at push time.
  </acceptance_criteria>

  <done>
    The full test suite is green, the staged v1.4.0 changelog entry reflects the fixes, and the SUMMARY carries the exact Help-Unaffected trailer so the docs hard-gate passes when Ben next pushes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored note markdown → read-mode innerHTML | user-typed note text is rendered to HTML in the add-session read-mode overlay |
| stored note markdown → PDF text | the same note text is parsed and drawn into the exported PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-07-01 | Tampering / Info-disclosure (XSS) | md-render.js renderBlock rendering to innerHTML | low | accept | No new XSS surface: render() escapes HTML (escapeHtml) BEFORE any structural transform, and this plan changes only block-structure detection (heading-split, marker rule) operating on already-escaped text — the escape-first path and the sole-innerHTML-writer invariant are untouched. |
| T-45-07-02 | Tampering (silent cross-pipeline divergence) | md-render.js ⟺ pdf-export.js | medium | mitigate | Both gap fixes land as cross-pipeline agreement cases in tests/45-pipeline-agreement.test.js (text-then-heading, heading-remainder, marker-only both types, 1.5-guard); the character-identity + join-invariant gates keep the D-08 emphasis regexes pinned so preview and export cannot disagree on formatting. |
| T-45-07-03 | Tampering (regex over-broadening breaks the 1.5-guard) | marker-detection regexes | medium | mitigate | Detection uses `(?=\s|$)` and stripping `(?:\s+|$)`, both of which fail on `\d+\.` followed by a digit; a regression-lock test (`1.5 mg` stays a paragraph in both pipelines) fails loudly if the guard is ever weakened. |
</threat_model>

<artifacts>
## Artifacts this phase produces

**Changed symbols — assets/md-render.js:**
- `renderBlock()` — new text-then-heading split branch (before the list scan).
- `isListItem()` — marker-only lookahead rule.
- `listType()` — bare-ordinal recognised as "ol".
- `stripListMarker()` — strips a marker-only line to "".
- `strip()` — per-line block-marker regex adopts the marker-only rule (compact-surface consistency).
- (unchanged, asserted intact: `applyInline`, `listDepth`, `buildList`, `buildListLevel`, the two emphasis regexes.)

**Changed symbols — assets/pdf-export.js:**
- `parseMarkdown()` — list-detect, first-line + per-item ordered-ness, `ordMatch` ordinal capture, both marker-strip replaces, and both paragraph-terminator regexes adopt the marker-only rule.
- (unchanged, asserted intact: `stripInlineMarkdown`, `parseInlineBold`, `clipSegmentsToRange`, the section-count/heading render paths.)

**New test cases (extending existing files; count guards bumped):**
- tests/45-mdrender-lists.test.js — text-then-heading, heading-remainder-then-heading, compound text→heading→list; marker-only (`1.`, `1. `, `-`, `- `, `*`); 1.5-guard regression-lock; `1.` ≡ `1. ` equivalence.
- tests/45-pdf-nested-lists.test.js — marker-only list blocks (empty-text items, correct ordered-ness), `1.` ≡ `1. `, 1.5-guard regression-lock.
- tests/45-pipeline-agreement.test.js — text-then-heading + heading-remainder-then-heading cross-pipeline agreement; marker-only cross-pipeline agreement (both marker types, bare + trailing-space); 1.5-guard cross-pipeline agreement; EXPECTED count guard bumped.

**Docs:**
- assets/changelog-content-en.js — amended v1.4.0 entry.
- Commit trailer: `Help-Unaffected: assets/md-render.js, assets/pdf-export.js — …` (recorded in SUMMARY).
</artifacts>

<verification>
- `node tests/45-mdrender-lists.test.js` — exit 0, count guard matches.
- `node tests/45-pdf-nested-lists.test.js` — exit 0, count guard matches.
- `node tests/45-pipeline-agreement.test.js` — exit 0, new agreement cases + character-identity + join-invariant all pass.
- `node tests/run-all.js` — full suite green (no regression in 31-* render-hardening, changelog-integrity, changelog-integrity-locale).
- `git diff --stat` shows only assets/md-render.js, assets/pdf-export.js, the three named test files, and assets/changelog-content-en.js changed (no export-modal/backup/storage/add-session source touched).
</verification>

<success_criteria>
- Read mode renders a heading typed directly under text (and under a heading's body remainder) as a real heading, matching the PDF (GAP-45-01 closed).
- Marker-only lines render as empty list items consistently in both pipelines; `1.` ≡ `1. `; nothing typed disappears; `1.5 mg` stays literal (GAP-45-02 closed per Ben's lock).
- The D-08 emphasis regexes stay character-identical; verbatim copy/share and backup round-trip are unaffected.
- Both fix classes are locked into the cross-pipeline agreement corpus; the full suite is green; the docs hard-gate is satisfiable at push time.
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-07-SUMMARY.md` when done. Record the exact `Help-Unaffected:` trailer text so the next push clears `scripts/docs-gate.js`.
</output>
