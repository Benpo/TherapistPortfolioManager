---
phase: 45-rich-text-rendering-export-foundation
plan: 06
type: execute
wave: 4
depends_on: [45-05]
files_modified: []
autonomous: false
requirements: [RTXT-06, RTXT-07, RTXT-10]
must_haves:
  truths:
    - "A REAL opened PDF (not jsdom) shows Hebrew formatted notes: bold preserved, bullet+numbered+nested lists correct-direction, note headings subordinate & chrome-free"
    - "A REAL encrypted .sgbackup restore on a real device carries formatted notes with zero format changes"
    - "On an installed Safari PWA, read mode shows formatted notes (dark mode + RTL legible)"
  artifacts:
    - "45-06-SUMMARY.md recording the real-device verification outcome (pass, or defects to fix)"
  key_links:
    - "jsdom cannot see caret/PDF/RTL-visual behavior — this repo has shipped false-GREEN jsdom PDF tests before; the milestone lock requires real-device verification"
    - "Use the Phase 44 pre-prod environment (second CF Pages project) for the on-device check"
---

<objective>
The milestone-locked phase gate: verify the rendering foundation on a REAL opened PDF and a REAL device — never jsdom-only. This repo has shipped false-GREEN jsdom PDF tests (memory reference-pdf-jsdom-inert-gates), and the v1.4 milestone made real-PDF + real-device verification an explicit success criterion. All automated tests are green by now (Plans 01-05); this plan closes the two things jsdom structurally cannot certify: PDF visual fidelity (Hebrew RTL, note-heading subordination, nested-list direction) and an encrypted-backup restore on a real device.

Purpose: Formatting that passes jsdom can still be visually wrong in the real PDF or on a real device. This is the human confirmation step.

Output: A recorded verification outcome. If defects are found, they route to a gap-closure plan; if clean, the phase's success criteria 2 and 4 are satisfied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 06 scope)

- No source or symbol changes. Produces `45-06-SUMMARY.md` recording the real-device verification result. (Any defects found are handed to `/gsd-plan-phase --gaps`.)

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-device / real-PDF phase gate (Hebrew PDF, installed Safari read mode, real .sgbackup restore)</name>
  <files>none (verification-only — produces 45-06-SUMMARY.md)</files>
  <action>
    Deploy the current branch to the Phase 44 pre-prod Cloudflare Pages environment (the second CF Pages project, e.g. the sg-prpr-*.pages.dev URL — NOT the production deploy branch the docs-gate anchors to), then present the three real surfaces below to Ben for confirmation. This is a human-only verification: jsdom cannot see PDF visual fidelity, Hebrew RTL direction, caret/render behavior, or a real-device backup restore, and this repo has shipped false-GREEN jsdom PDF tests before. Do not mark the phase complete until Ben confirms all three surfaces or reports defects to route to gap closure.
  </action>
  <verify>
    <human-check>Ben confirms read mode, real Hebrew PDF, and real device .sgbackup restore all pass (see how-to-verify steps).</human-check>
  </verify>
  <done>Ben types "approved" after all three real surfaces pass, or defects are captured in 45-06-SUMMARY.md and routed to /gsd-plan-phase --gaps.</done>
  <what-built>
    Phases 45-01 through 45-05 delivered, all automated tests green: MdRender ordered/nested lists + D-08 hardening + strip; PDF inline hardening + nested lists + subordinate chrome-free note headings (D-02/D-03); read-mode `.note-rendered` overlay; compact-surface strip; verbatim copy/share; encrypted-backup round-trip; cross-pipeline agreement; changelog + help updated. jsdom cannot see PDF visual fidelity, Hebrew RTL direction, or a real device restore — this checkpoint verifies exactly those.
  </what-built>
  <how-to-verify>
    Deploy the branch to the Phase 44 pre-prod CF Pages environment (the second project, e.g. the sg-prpr-*.pages.dev URL) and verify on real surfaces:

    1. READ MODE (installed Safari PWA, and once in dark mode):
       - Open a saved session whose notes contain: `**bold**`, `*italic*`, a `- ` bullet list, a `1.` numbered list, a two-level nested list, and a `## heading` and `### heading`.
       - Confirm the notes show as STYLED text (bold is bold, lists have markers/indent, headings read as subordinate to the field label — weight-led, no rule/diamond) — never raw `**`/`-`/`#` tokens.
       - Confirm dark mode and (switch to Hebrew) RTL both stay legible; nested lists indent toward the correct side in RTL.

    2. PDF EXPORT — open the REAL generated PDF (not a jsdom assertion), with the app language set to HEBREW:
       - Export a session containing Hebrew text plus bold, a numbered list, a bullet list, a two-level nested list, and a `## note heading`.
       - Confirm: bold is preserved; lists render with correct RTL direction and nested items indent toward the RIGHT; the note `## heading` renders SUBORDINATE and chrome-free (NO leaf-diamond, NO green rule) and is clearly NOT mistakable for a branded document section heading; the document section headings still carry their branded chrome; the "Severity — before & after" block lands in the correct section slot (not shifted by the note heading).
       - Confirm italic renders at regular weight in the PDF (documented accepted limitation — not a bug).

    3. ENCRYPTED BACKUP round-trip on a REAL device:
       - Export an encrypted `.sgbackup` of a portfolio containing the formatted session, then restore it into a fresh portfolio on a real device.
       - Confirm the formatted notes come back with zero format changes (bold/lists/nesting/heading intact).

    Report any surface that fails (screenshots welcome).
  </how-to-verify>
  <resume-signal>Type "approved" if all three surfaces pass, or describe the defects (which surface, what looked wrong) so they can be routed to a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated-green → real-device truth | jsdom green does not prove PDF/RTL/device correctness; this gate is the boundary where visual/real-device truth is confirmed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-09 | Repudiation (false-GREEN) | jsdom PDF tests passing while visually broken | high | mitigate | Mandatory real-opened-PDF + real-device human checkpoint (this plan); documented false-GREEN history is the reason it is blocking. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase; verification-only. |
</threat_model>

<verification>
- Human confirms all three real surfaces (read mode, real Hebrew PDF, real device restore) pass
- Any defect is captured in 45-06-SUMMARY.md and routed to `/gsd-plan-phase --gaps`
</verification>

<success_criteria>
- Real opened Hebrew PDF: bold + lists (incl. nesting) preserved, RTL correct, note headings subordinate & chrome-free, severity block placed correctly (ROADMAP success criterion 2)
- Real device encrypted-backup restore carries formatted notes with zero format changes (ROADMAP success criterion 4)
- Read mode on installed Safari shows formatted notes, dark-mode + RTL legible (ROADMAP success criterion 1)
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-06-SUMMARY.md` when done
</output>
