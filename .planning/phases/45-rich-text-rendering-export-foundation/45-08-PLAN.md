---
phase: 45-rich-text-rendering-export-foundation
plan: 08
type: execute
wave: 1
depends_on: ["45-07"]
gap_closure: true
autonomous: true
requirements: [RTXT-06, RTXT-07]
files_modified:
  - assets/md-render.js
  - tests/45-mdrender-lists.test.js
  - tests/45-pipeline-agreement.test.js
  - assets/changelog-content-en.js

must_haves:
  truths:
    - "A same-depth marker-TYPE change starts a NEW list element in read mode (CommonMark): `-\\n1. x` renders `<ul><li></li></ul><ol><li value=\"1\">x</li></ol>`, matching the PDF's per-item markers — closes GAP-45-03, defends RTXT-06 + RTXT-07 (D-04/D-05 ordered+mixed lists render everywhere)."
    - "Both flip directions split (ul→ol AND ol→ul), including empty marker-only items and mid-run flips at ANY nesting depth (a child run that flips type splits too): `1. x\\n- y` → `<ol><li value=\"1\">x</li></ol><ul><li>y</li></ul>`; `- a\\n  - b\\n  1. c` → `<ul><li>a<ul><li>b</li></ul><ol><li value=\"1\">c</li></ol></li></ul>`."
    - "EDITOR-1:1 ordinals: every ordered `<li>` carries `value=\"{typed ordinal}\"` so the screen shows the digits the user typed — `11. jj` displays 11.; `1.\\n1.\\n1.` displays 1,1,1; block-separated `1. X` / blank / `2. Y` displays 1 then 2 — closes GAP-45-04, defends RTXT-06 (D-04). The `value` attribute is emitted ONLY on ordered `<li>`; unordered `<li>` never carries it."
    - "The `value` attribute is a static integer from `parseInt` of the `\\d+` ordinal capture group — never raw user text — so no user-controlled string lands in the `.note-rendered` innerHTML attribute unescaped; the escape-first path and sole-innerHTML-writer invariant are untouched."
    - "pdf-export.js is BYTE-UNCHANGED this round (already editor-1:1 via item.ordinal, already keys per-item ordered-ness): both fixes live in md-render.js buildList/buildListLevel ONLY; `git diff --stat assets/pdf-export.js` shows no change (D-07 uniform path preserved on the read side)."
    - "Round-1 behaviors stay GREEN: text-then-heading split, marker-only empty items (`1.` ≡ `1. `), the `1.5 mg`/`11.fj`/`1.lkjklj` literal guard via the `(?=\\s|$)` lookahead, trailing-space ≡ bare. D-08 emphasis regexes untouched; escape-first + single-newline `<br>` contract; shared nesting floor(spaces/2); verbatim pass-through (RTXT-08/D-10) and `.sgbackup` round-trip (RTXT-10) unaffected — renderer-only changes."
  artifacts:
    - "assets/md-render.js — buildList/buildListLevel restructured so a same-depth type flip closes the current list and opens a sibling list (GAP-45-03); ordered `<li>` emitted with a `value` attribute from a new numeric ordinal capture (GAP-45-04). isListItem/listType/listDepth/stripListMarker DETECTION regexes UNCHANGED."
    - "tests/45-mdrender-lists.test.js — new RED-first cases: same-depth flips both directions (empty + non-empty), mid-run child-depth flip, ordinal-continuity across a flip, typed non-sequential ordinals, repeated `1.`, block-separated numbered runs; existing ordered-list expected strings updated to carry `value=\"N\"`; count guard bumped."
    - "tests/45-pipeline-agreement.test.js — new cross-pipeline cases: same-depth type-flip agreement (both directions, empty items) and typed-ordinal/block-separated agreement; mdListTags nested-tag regex tolerant of `<li value=\"N\">`; marker-only agreement expected updated for value-bearing ordered rows; count guard bumped."
    - "assets/changelog-content-en.js — the staged v1.4.0 EN entry amended to reflect numbered-list numbering + mixed-marker robustness (satisfies the docs-gate changelog demand; EN-only, highlight count + category skeleton unchanged so the locale-parity gate stays green)."
  key_links:
    - "In buildListLevel, the type-flip break condition MUST test the ITEM's own marker type (`items[i].type`) against the OPENED list's type at the SAME depth — a deeper child at a differing type is nested, not split; only a same-depth sibling flip starts a new list. Children are recursed through the SAME sibling-splitting path so a child-run flip also splits (GAP-45-03 applies at every depth)."
    - "The `value` attribute keys on the ITEM's own ordered-ness (`items[i].type === 'ol'`), mirroring pdf-export.js's per-item `item.ordered`/`item.ordinal` contract — so after a split each ordered run's first `<li>` carries its typed ordinal and browser renumber-to-1 can never override display continuity (`1. a\\n- b\\n2. c` shows 1 / bullet / 2)."
    - "The ordinal capture regex is `/^\\s*(\\d+)\\.(?:\\s+|$)/` — CHARACTER-MATCHED to pdf-export.js parseMarkdown's `ordMatch` (bare `5.` → ordinal 5, `1.5` fails the branch → paragraph). It ADDS a capture; it does NOT alter isListItem/listType/stripListMarker, so the round-1 1.5-guard and marker-only rules are untouched."
    - "No CSS change: `.note-rendered ol` and `.export-preview ol` (app.css) use the native list marker (no `list-style: none`, no CSS counters), so the browser honors `<li value=\"N\">` and the on-screen number matches the typed ordinal. The emphasis regexes in applyInline MUST NOT be touched — the character-identity gate in 45-pipeline-agreement.test.js reads them from source."
---

<objective>
Close the two round-2 UAT gaps from Ben's 2026-07-13 stress-test on build 207036c (45-UAT.md ## Gaps GAP-45-03 + GAP-45-04). Both are bounded read-mode renderer defects in `assets/md-render.js` `buildList`/`buildListLevel` ONLY; `assets/pdf-export.js` is already correct (per-item markers + typed ordinals) and stays byte-unchanged.

- **GAP-45-03 (minor):** a same-depth marker-TYPE change folds into the open list — `-` (ul) followed in the same run by `1. ` (ol) renders the ordered item as a BULLET in read mode, while the PDF keys each item's own marker and prints `1.`. Ben's LOCKED direction (CommonMark): a marker-type change at the same level STARTS A NEW LIST (`<ul>…</ul><ol>…</ol>`), both directions, incl. empty marker-only items, at every nesting depth. Fix in md-render.js only; PDF needs NO change.
- **GAP-45-04 (minor):** read mode renumbers ordered items positionally (browser 1..N) while the PDF preserves typed ordinals — `11. jj` shows `1. jj` on screen but `11. jj` in the PDF; `1. X` / blank / `2. Y` shows 1/1 on screen but 1/2 in PDF. Ben's LOCKED direction: EDITOR-1:1 — read mode honors typed ordinals via `<li value="N">` so screen ≡ PDF ≡ what the user typed. Fix in md-render.js only; PDF already editor-1:1.

Both fixes are locked into tests/45-pipeline-agreement.test.js so read mode ⟺ PDF can never silently diverge on same-depth mixed-type siblings or typed ordinals again. Round-1 behaviors (text-then-heading, marker-only empty items, the 1.5-guard, D-08 emphasis identity) stay green.

Purpose: the therapist sees on screen exactly the list markers and numbers they typed, identical to the exported PDF — no positional renumbering, no marker-type folding.
Output: restructured md-render.js list builder, extended md-render + agreement corpora with count guards bumped, an amended v1.4.0 changelog entry, and the fresh docs-gate trailer that keeps the phase Definition-of-Done satisfiable at push time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-07-SUMMARY.md
@CLAUDE.md
</context>

<coverage_audit>
Gap-closure coverage (source = 45-UAT.md ## Gaps, round 2):

| Gap | Severity | Task | Requirement / Decision defended |
|-----|----------|------|---------------------------------|
| GAP-45-03 same-depth marker-type flip folds into the open list | minor | Task 2 | RTXT-06, RTXT-07 (D-04 ordered, D-05 mixed/nested) |
| GAP-45-04 read mode renumbers ordered items positionally | minor | Task 1 | RTXT-06 (D-04 ordered lists render as typed) |
| Cross-pipeline agreement lock (both classes) | — | Task 1 + Task 2 (agreement corpus) | RTXT-06/07 |
| Round-1 invariants unbroken (text-then-heading, marker-only, 1.5-guard, D-08) | — | Task 1 + Task 2 (regression-lock cases, full-suite green) | RTXT-06/07/08, D-08 |
| Verbatim pass-through + backup round-trip unaffected (RTXT-08/D-10, RTXT-10) | — | Task 1 + Task 2 (renderer-only, byte-diff scope) | RTXT-08, RTXT-10 |
| Docs hard-gate (user-facing renderer change) | — | Task 3 | — |

No round-2 UAT item is left unplanned. GAP-45-01 + GAP-45-02 are already resolved by 45-07 (verified on-device). Out of scope (correctly excluded): the second-backup-import bug (UAT test 7 — separate v1.4 ship-blocker, backup.js untouched); the 45-06 real-device checkpoint (re-run after this plan lands).
</coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-45-04 — ordered `<li>` carries the typed ordinal via `value="N"` (EDITOR-1:1)</name>
  <files>assets/md-render.js, tests/45-mdrender-lists.test.js, tests/45-pipeline-agreement.test.js</files>

  <read_first>
    - assets/md-render.js — the list helpers `stripListMarker()` (lines 59-63), `buildListLevel()` (lines 66-86), `buildList()` (lines 87-92). Note buildList maps each line to `{ depth, type, content }`; this task adds an `ordinal`. Note buildListLevel emits `"<li>" + content + …` in TWO places (the nested branch line 77 and the flat branch line 80) — both must carry the value attribute for ordered items.
    - assets/pdf-export.js — parseMarkdown()'s ordinal capture (lines 690-694): `var ordMatch = /^\s*(\d+)\.(?:\s+|$)/.exec(rawLine); var typedOrdinal = ordMatch ? parseInt(ordMatch[1], 10) : (items.length + 1);` — MIRROR this exact capture shape in md-render.js so the two pipelines agree on the typed ordinal (incl. the round-1 bare `5.` → ordinal 5 form). pdf-export.js is NOT edited — it already carries item.ordinal.
    - assets/app.css — `.export-preview ol` (line 3584) and `.note-rendered ol`/`.note-rendered li` (lines 3612-3619): confirm NO `list-style: none` and NO CSS counters, so the native ordered-list marker honors `<li value="N">` and the on-screen number equals the typed ordinal. No CSS change is needed; do not add one.
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md — GAP-45-04 diagnosis + Ben's LOCKED 2026-07-13 direction (editor-1:1 via `<li value="N">`; `1./1./1.` displays 1,1,1 — accepted).
    - tests/45-mdrender-lists.test.js — the render() harness; the EXISTING ordered-list cases whose expected strings CHANGE this task (lines 62-64, 66-69, 79-84, 86-91, 139-146); the count guard (line 163).
    - tests/45-pipeline-agreement.test.js — the `mdListTags` nested-tag regex (line 229, `/<li>[^<]*<(ul|ol)>/`) which must tolerate `<li value="N">`; the marker-only agreement test (lines 309-330) whose ordered-row expectation changes; the EXPECTED count guard (line 341); the parseMarkdown seam (line 63) for reading pdf item.ordinal.
  </read_first>

  <behavior>
    RED-first. Add/adjust these as assertions, confirm the NEW ones fail against the unchanged renderer, then implement.

    NEW cases (currently RED — read mode renumbers positionally):
    - `MdRender.render('11. jj') === '<ol><li value="11">jj</li></ol>'` (currently `<ol><li>jj</li></ol>` — RED; browser would show 1.).
    - `MdRender.render('1.\n1.\n1.') === '<ol><li value="1"></li><li value="1"></li><li value="1"></li></ol>'` (three typed-1 empty items — accepted per Ben's lock).
    - Block-separated (blank line splits blocks; render joins blocks with a single "\n"): `MdRender.render('1. X\n\n2. Y') === '<ol><li value="1">X</li></ol>\n<ol><li value="2">Y</li></ol>'`.

    UPDATED existing cases (ordered `<li>` now carries `value`):
    - `MdRender.render('1. a\n2. b') === '<ol><li value="1">a</li><li value="2">b</li></ol>'`.
    - `MdRender.render('3. a\n4. b') === '<ol><li value="3">a</li><li value="4">b</li></ol>'` (this now DIRECTLY proves "no force start=1" on screen).
    - `MdRender.render('1. a\n   1. b') === '<ol><li value="1">a<ol><li value="1">b</li></ol></li></ol>'`.
    - `MdRender.render('- a\n  1. b') === '<ul><li>a<ol><li value="1">b</li></ol></li></ul>'` (the parent ul `<li>` has NO value; the nested ol `<li>` does; the `/<li>a<ol>/` ok-assert still holds).
    - `MdRender.render('1.') === '<ol><li value="1"></li></ol>'` and `MdRender.render('1. ') === '<ol><li value="1"></li></ol>'` (the `1.` ≡ `1. ` equivalence assert stays green).

    Regression-lock (stays GREEN — bullets never carry value; guard preserved):
    - `MdRender.render('- a\n- b') === '<ul><li>a</li><li>b</li></ul>'`; `MdRender.render('- a\n  - b\n- c')` unchanged; `MdRender.render('1.5 mg') === '<p>1.5 mg</p>'`.

    Cross-pipeline (tests/45-pipeline-agreement.test.js):
    - NEW typed-ordinal agreement: for `11. jj`, MdRender's HTML contains `value="11"` AND `parseMarkdown('11. jj')[0].items[0].ordinal === 11`; for block-separated `1. X\n\n2. Y`, MdRender emits two `<ol>` with `value="1"` then `value="2"` AND parseMarkdown yields two list blocks (filtering blank) whose items[0].ordinal are 1 then 2 — both pipelines display 1 then 2.
    - Update `mdListTags` nested regex to `/<li(?:\s[^>]*)?>[^<]*<(ul|ol)>/` so the existing nested `1. a\n   1. b` agreement case still finds the nested `<ol>` under `<li value="1">a`.
    - Update the marker-only agreement expectation (lines 309-330) so ordered rows (`1.`, `1. `) expect `<ol><li value="1"></li></ol>` while unordered rows are unchanged (e.g. build the expected `<li>` open tag as `'<li' + (row.ordered ? ' value="1"' : '') + '>'`).
  </behavior>

  <action>
    In `assets/md-render.js`:
    - Add a `listOrdinal(line)` helper that captures the typed ordinal with `/^\s*(\d+)\.(?:\s+|$)/` and returns `parseInt(match[1], 10)` (or null for a non-ordered line) — CHARACTER-MATCH pdf-export.js parseMarkdown's `ordMatch` regex so the pipelines agree, and note in a concept-level comment (no fenced examples) that this only ADDS a numeric capture and does NOT change the round-1 detection (`isListItem`/`listType`) or the 1.5-guard.
    - In `buildList()`, extend the per-line map object to include `ordinal: listOrdinal(l)` alongside `depth/type/content`.
    - In `buildListLevel()`, compute a value attribute for each item keyed on the ITEM's own type: for `items[i].type === 'ol'`, emit `<li value="{ordinal}">`; otherwise emit a bare `<li>`. Apply it in BOTH `<li>` emission sites (the nested-child branch AND the flat branch). Build the attribute string by concatenating the integer ordinal only — because the ordinal is `parseInt` of a `\d+` capture it is a finite integer whose string form is digits-only, so NO user-controlled text reaches the attribute (state this security rationale in a concept-level comment; the ordinal MUST come from the numeric capture, never `content`/raw text).
    - Do NOT touch `isListItem`, `listType`, `listDepth`, `stripListMarker`, `applyInline` (the two emphasis regexes), `renderBlock`, `strip`, or pdf-export.js. This task changes ONLY ordinal capture + the `<li>` emission.

    Then extend/adjust the two test files with the behavior cases above and bump each count guard so `passed + failed` equals the new actual total.
  </action>

  <verify>
    <automated>node tests/45-mdrender-lists.test.js && node tests/45-pipeline-agreement.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/45-mdrender-lists.test.js` exits 0; its count guard equals the new number of cases; the updated ordered-list cases and the 3 new GAP-45-04 cases pass, and every unordered/1.5-guard case stays GREEN.
    - `node tests/45-pipeline-agreement.test.js` exits 0; the new typed-ordinal agreement case passes, the nested `1. a\n   1. b` case still finds its nested `<ol>` (regex tolerant of `value`), the character-identity + no-lookbehind + D-08 corpus + join-invariant cases still pass, and the count guard equals the new total.
    - `MdRender.render('11. jj')` contains the substring `value="11"`; `MdRender.render('- a\n- b')` contains NO `value=` (bullets never carry it).
    - `git diff --stat assets/pdf-export.js` shows NO change from this task's commits.
  </acceptance_criteria>

  <done>
    Every ordered `<li>` in read mode carries `value="{typed ordinal}"` so the on-screen number matches what the user typed and the PDF; unordered `<li>` is unchanged; the ordinal is a safe integer from the numeric capture; pdf-export.js and the D-08 emphasis path are untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-45-03 — a same-depth marker-type flip starts a NEW list (CommonMark)</name>
  <files>assets/md-render.js, tests/45-mdrender-lists.test.js, tests/45-pipeline-agreement.test.js</files>

  <read_first>
    - assets/md-render.js — `buildListLevel()` (lines 66-86) as it now stands AFTER Task 1 (ordered items emit `<li value>`). Today it opens ONE list of `items[start].type` and appends EVERY same-depth item as a plain `<li>` inside it regardless of that item's own type — that is the GAP-45-03 defect. `buildList()` (lines 87-92) calls `buildListLevel(items, 0, items[0].depth)` once and does not loop over sibling lists.
    - assets/pdf-export.js — parseMarkdown() list grouper (lines 650-712): it keeps a same-depth mixed-type run in ONE list block but records per-item `ordered`/`ordinal`, so the PDF renderer already draws a bullet then a `1.` for `-\n1. x`. This is the target read-mode markers must match. pdf-export.js is NOT edited.
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UAT.md — GAP-45-03 diagnosis + Ben's LOCKED direction (marker-type change at the same level starts a new list, both directions, incl. empty items, every nesting depth).
    - tests/45-mdrender-lists.test.js — the render() harness + count guard (updated in Task 1).
    - tests/45-pipeline-agreement.test.js — the `pdfListTags`/`mdListTags` helpers (lines 211-232), the parseMarkdown seam, the EXPECTED count guard.
  </read_first>

  <behavior>
    RED-first. Confirm each new case fails on the post-Task-1 renderer (a flipped sibling still folds into the open list), then implement.

    NEW cases (tests/45-mdrender-lists.test.js) — LOCKED expected strings:
    - `MdRender.render('-\n1. x') === '<ul><li></li></ul><ol><li value="1">x</li></ol>'` (ul→ol flip, matches the gap contract exactly).
    - `MdRender.render('1. x\n- y') === '<ol><li value="1">x</li></ol><ul><li>y</li></ul>'` (ol→ul flip).
    - `MdRender.render('- a\n1. b') === '<ul><li>a</li></ul><ol><li value="1">b</li></ol>'` (non-empty, ul→ol).
    - `MdRender.render('-\n1.') === '<ul><li></li></ul><ol><li value="1"></li></ol>'` (BOTH empty marker-only, flip).
    - Ordinal-continuity across a flip: `MdRender.render('1. a\n- b\n2. c') === '<ol><li value="1">a</li></ol><ul><li>b</li></ul><ol><li value="2">c</li></ol>'` (the second `<ol>`'s first `<li value="2">` proves the flip cannot reset the on-screen number to 1).
    - Child-depth flip: `MdRender.render('- a\n  - b\n  1. c') === '<ul><li>a<ul><li>b</li></ul><ol><li value="1">c</li></ol></li></ul>'` (a flip inside a nested child run splits too).

    Regression-lock (stays GREEN — homogeneous runs do NOT split):
    - `MdRender.render('1. a\n2. b') === '<ol><li value="1">a</li><li value="2">b</li></ol>'` (pure ordered run — one `<ol>`).
    - `MdRender.render('- a\n  - b\n- c') === '<ul><li>a<ul><li>b</li></ul></li><li>c</li></ul>'` (nested bullets, unchanged).
    - The round-1 text-then-heading, marker-only, and 1.5-guard cases stay green.

    Cross-pipeline (tests/45-pipeline-agreement.test.js):
    - NEW same-depth type-flip agreement: for `-\n1. x`, `1. x\n- y`, and empty `-\n1.` — assert MdRender's HTML equals the LOCKED split string above AND `parseMarkdown(src)` (filtering blank) produces ONE list block whose `items` carry the matching per-item `ordered` sequence (e.g. `-\n1. x` → items[0].ordered === false, items[1].ordered === true, items[1].ordinal === 1). This proves both pipelines mark the SAME items as bullet-vs-numbered even though the PDF keeps one block and read mode splits into two lists.
  </behavior>

  <action>
    In `assets/md-render.js`, restructure the list builder so a same-depth marker-TYPE flip closes the current list and opens a sibling list (Ben's LOCKED CommonMark direction), while a DEEPER child of a differing type continues to nest:

    - Split the single-list emission into (a) a "build ONE list" routine that opens `<type>`, consumes a maximal run of SAME-type items at the current depth (recursing on any deeper children), and STOPS at the first same-depth item whose own `type` differs from the opened type — returning both the HTML and the index where it stopped; and (b) a "build sibling lists at a depth" routine that loops the one-list routine while items remain at `>=` the current depth, concatenating each sibling list. Have `buildList()` call the sibling-list routine at the top level, and have the nested-child recursion inside a `<li>` call the SAME sibling-list routine (so a child-run flip also splits — GAP-45-03 applies at every depth). Preserve the existing `depth > depth` safety `continue` branch.
    - The break condition compares the ITEM's own `type` (its own marker) to the OPENED list's `type` at the SAME depth only; a deeper item is nested via the child recursion, never treated as a flip. Keep the value-attribute emission from Task 1 (each ordered run's first `<li>` carries its typed ordinal, so display continuity holds across a flip — `1. a\n- b\n2. c` shows 1 / bullet / 2).
    - Do NOT touch the marker DETECTION helpers (`isListItem`/`listType`/`listDepth`/`stripListMarker`/`listOrdinal`), `renderBlock`, `applyInline`, `strip`, or pdf-export.js. `renderBlock` still hands a contiguous list-line run to `buildList`; the new splitting happens entirely inside the builder.

    Comment each change concept-level (no fenced examples) citing GAP-45-03 and Ben's 2026-07-13 CommonMark lock. Then add the behavior cases above to both test files and bump each count guard to the new actual total.
  </action>

  <verify>
    <automated>node tests/45-mdrender-lists.test.js && node tests/45-pipeline-agreement.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/45-mdrender-lists.test.js` exits 0; the 6 new GAP-45-03 cases pass with the LOCKED expected strings; the pure-ordered-run, nested-bullet, text-then-heading, marker-only, and 1.5-guard regression cases stay GREEN; count guard equals the new total.
    - `node tests/45-pipeline-agreement.test.js` exits 0; the same-depth type-flip agreement case passes (MdRender split string + pdf per-item ordered-ness sequence agree) and every prior case (character-identity, join-invariant, nested, text-then-list, text-then-heading, marker-only, typed-ordinal) still passes; count guard equals the new total.
    - `MdRender.render('-\n1. x') === '<ul><li></li></ul><ol><li value="1">x</li></ol>'` exactly; `MdRender.render('1. a\n2. b')` remains a single `<ol>` (homogeneous runs are NOT split).
    - `git diff --stat assets/pdf-export.js` shows NO change from this task's commits.
  </acceptance_criteria>

  <done>
    A same-depth marker-type flip renders as separate sibling lists in read mode (both directions, empty and non-empty, at every depth), matching the PDF's per-item markers; homogeneous runs and nesting are unchanged; ordinal continuity holds across a flip; pdf-export.js and the D-08 path are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: satisfy the docs hard-gate for the read-mode list-rendering fixes</name>
  <files>assets/changelog-content-en.js</files>

  <read_first>
    - CLAUDE.md — the "Definition of Done — docs must not rot" contract: `assets/md-render.js` is a watched code file NOT in the changelog-only tier, so this push raises a changelog demand (satisfied by an EN `changelog-content-en.js` edit) AND a per-file help demand for md-render.js (waived by a `Help-Unaffected:` trailer). Note md-render.js has NO covering help topic; `Help-Unaffected:` is honored from ANY commit in the pushed range; the reason after the em-dash is mandatory. pdf-export.js is byte-unchanged this round so it need not be named (and 45-07's trailer already covered it in-range).
    - .planning/phases/45-rich-text-rendering-export-foundation/45-07-SUMMARY.md (Task 3 + the "Docs-gate trailer" section) — the staged v1.4.0 EN changelog entry (anchor `v1-4`), the exact `Help-Unaffected: assets/md-render.js …` trailer form already used, and the note that a fresh trailer on THIS round's docs commit keeps the gate clean regardless of when 45-07 deploys.
    - assets/changelog-content-en.js (lines 72-92) — the v1.4.0 entry: 3 highlights, `new` (1 item) + `improved` (2 items) categories, no emojis, client/session terminology. The locale-parity gate `changelog-integrity-locale.test.js` requires the highlight COUNT and category-key skeleton to match across en/he/de/cs, so an EN-only refinement of existing copy keeps it green.
  </read_first>

  <action>
    Amend the STAGED v1.4.0 EN changelog entry (anchor `v1-4`) in assets/changelog-content-en.js to reflect the two read-mode robustness fixes — refine the existing `new` reading highlight (and/or the `improved` list) so it conveys that numbered lists show the numbers you typed and that switching between bullets and numbers renders correctly. Keep the highlight COUNT (3) and the category-key skeleton (`new` 1, `improved` 2) IDENTICAL so the locale-parity gate stays green with an EN-only edit; if any structural change is unavoidable, mirror the SAME skeleton into changelog-content-{he,de,cs}.js and add those files to the commit. Do NOT bump APP_VERSION (stays 1.3.0 mid-milestone; the v1.4 semver footer flips at the v1.4 release boundary per the version-bump convention). This EN edit satisfies the global changelog demand for the watched md-render.js change in Tasks 1-2, independent of 45-07's deploy timing.

    Record — in this plan's SUMMARY, to be placed on a commit in the pushed range (exact casing, case-sensitive) — the fresh Help-Unaffected trailer waiving md-render.js's per-file help demand:

    `Help-Unaffected: assets/md-render.js — read-mode list-rendering gap fixes (same-depth marker-type flip starts a new list; ordered items display the typed ordinal); behavior documented under review-export + starting-a-session help topics and the v1.4 changelog entry`

    Do NOT run a docs-only deploy for this change — let it ride with the next code push per the avoid-docs-only-deploys convention.
  </action>

  <verify>
    <automated>node tests/run-all.js</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/run-all.js` exits 0 — the FULL suite is green (renderer behavior, 45-pipeline-agreement character-identity + join-invariant, 45-read-mode-render source/CSS locks, 31-* render-hardening, changelog-integrity + changelog-integrity-locale parity), proving no regression from Tasks 1-2 or the changelog edit.
    - `git diff assets/changelog-content-en.js` shows an edit to the v1.4.0 entry (the changelog demand is satisfied by any EN edit).
    - The SUMMARY records the exact `Help-Unaffected:` trailer text (case-sensitive) to be placed on a commit in the pushed range so `scripts/docs-gate.js` passes at push time.
  </acceptance_criteria>

  <done>
    The full test suite is green, the staged v1.4.0 changelog entry reflects the numbered-list + mixed-marker robustness fixes, and the SUMMARY carries the exact fresh Help-Unaffected trailer so the docs hard-gate passes when Ben next pushes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored note markdown → read-mode innerHTML | user-typed note text rendered to HTML in the add-session `.note-rendered` overlay (the app's ONE sanctioned innerHTML note surface) |
| stored note markdown → PDF text | the same note text parsed and drawn into the exported PDF (unchanged this round) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-08-01 | Tampering / Info-disclosure (attribute injection into innerHTML) | md-render.js buildListLevel `<li value="N">` emission | low | mitigate | The `value` is `parseInt` of a `\d+` numeric capture group — a finite integer whose string form is digits-only; no user-controlled text (never `content`/raw line) is concatenated into the attribute, so the escape-first path and the sole-innerHTML-writer invariant are untouched. A regression case asserts bullets carry NO `value` and ordered values are digit-only. |
| T-45-08-02 | Tampering (silent cross-pipeline divergence) | md-render.js ⟺ pdf-export.js | medium | mitigate | Both fixes land as cross-pipeline agreement cases in tests/45-pipeline-agreement.test.js (same-depth type-flip both directions incl. empty; typed non-sequential ordinals; block-separated numbered runs); the character-identity + join-invariant gates keep the D-08 emphasis regexes pinned so preview and export cannot disagree on formatting. |
| T-45-08-03 | Tampering (regex over-broadening breaks the round-1 1.5-guard) | new `listOrdinal` capture regex | medium | mitigate | `listOrdinal` uses `/^\s*(\d+)\.(?:\s+|$)/` (character-matched to pdf's `ordMatch`) and only ADDS a capture — it does not alter `isListItem`/`listType`/`stripListMarker`; the round-1 `1.5 mg` → paragraph regression cases stay green in both the md-render and agreement suites. |
</threat_model>

<artifacts>
## Artifacts this phase produces

**Changed symbols — assets/md-render.js:**
- `listOrdinal()` — NEW helper capturing the typed ordinal `/^\s*(\d+)\.(?:\s+|$)/` → `parseInt` (GAP-45-04).
- `buildList()` — per-line map object gains `ordinal`.
- `buildListLevel()` (restructured) — same-depth type-flip closes the current list and opens a sibling list (GAP-45-03); ordered `<li>` emits `value="{ordinal}"` in both emission sites (GAP-45-04); nested-child recursion routes through the sibling-splitting path.
- (unchanged, asserted intact: `isListItem`, `listType`, `listDepth`, `stripListMarker`, `renderBlock`, `applyInline`'s two emphasis regexes, `strip`.)

**assets/pdf-export.js:** BYTE-UNCHANGED (already per-item markers + typed `item.ordinal`).

**New/updated test cases (existing files; count guards bumped):**
- tests/45-mdrender-lists.test.js — GAP-45-04: `11. jj`, repeated `1.`, block-separated `1. X`/`2. Y`, updated existing ordered-list expected strings to carry `value`. GAP-45-03: same-depth flips (both directions, empty + non-empty), child-depth flip, ordinal-continuity across a flip; homogeneous-run + 1.5-guard regression-locks.
- tests/45-pipeline-agreement.test.js — typed-ordinal agreement; same-depth type-flip agreement (MdRender split ⟺ pdf per-item ordered-ness); `mdListTags` regex tolerant of `<li value>`; marker-only agreement expected updated for ordered rows.

**Docs:**
- assets/changelog-content-en.js — amended v1.4.0 entry.
- Commit trailer (recorded in SUMMARY): `Help-Unaffected: assets/md-render.js — read-mode list-rendering gap fixes …`
</artifacts>

<verification>
- `node tests/45-mdrender-lists.test.js` — exit 0, count guard matches (GAP-45-03 + GAP-45-04 cases green, regression-locks green).
- `node tests/45-pipeline-agreement.test.js` — exit 0, new type-flip + typed-ordinal agreement cases pass, character-identity + join-invariant intact, count guard matches.
- `node tests/run-all.js` — full suite green (no regression in 45-read-mode-render, 31-* render-hardening, changelog-integrity, changelog-integrity-locale).
- `git diff --stat assets/pdf-export.js` — NO change (byte-unchanged this round).
- `git diff --stat` overall shows only assets/md-render.js, tests/45-mdrender-lists.test.js, tests/45-pipeline-agreement.test.js, and assets/changelog-content-en.js changed (no export-modal / backup / storage / add-session / CSS source touched).
</verification>

<success_criteria>
- Read mode renders a same-depth marker-type flip as separate sibling lists, matching the PDF's per-item markers (GAP-45-03 closed per Ben's CommonMark lock).
- Every ordered `<li>` displays the typed ordinal via `value="N"`, so screen ≡ PDF ≡ what the user typed, including repeated and block-separated numbering (GAP-45-04 closed).
- pdf-export.js is byte-unchanged; the D-08 emphasis regexes stay character-identical; the round-1 text-then-heading / marker-only / 1.5-guard behaviors stay green; verbatim copy/share and backup round-trip are unaffected.
- Both fix classes are locked into the cross-pipeline agreement corpus; the full suite is green; the docs hard-gate is satisfiable at push time via the recorded Help-Unaffected trailer.
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-08-SUMMARY.md` when done. Record the exact `Help-Unaffected:` trailer text (case-sensitive) so the next push clears `scripts/docs-gate.js`.
</output>
