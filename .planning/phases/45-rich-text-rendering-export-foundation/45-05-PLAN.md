---
phase: 45-rich-text-rendering-export-foundation
plan: 05
type: execute
wave: 3
depends_on: [45-01, 45-02, 45-03, 45-04]
files_modified:
  - tests/45-backup-roundtrip.test.js
  - tests/45-pipeline-agreement.test.js
  - assets/changelog-content-en.js
  - assets/help-content-en.js
autonomous: true
requirements: [RTXT-06, RTXT-07, RTXT-08, RTXT-10]
must_haves:
  truths:
    - "An encrypted .sgbackup encrypt→restore round-trip preserves formatted note strings byte-for-byte (RTXT-10)"
    - "MdRender and the PDF pipeline agree on inline formatting (D-08) AND on nested-list structure across a BROADENED corpus (pinned rows + adjacency/boundary/multi-marker cases + mixed-type nesting + a randomized property pass), with the emphasis regexes proven CHARACTER-IDENTICAL at source level"
    - "An EN changelog entry documents the formatted-notes feature; the owning EN help topics describe reading/exporting formatted notes"
  artifacts:
    - "tests/45-backup-roundtrip.test.js (falsifiable encrypt/decrypt)"
    - "tests/45-pipeline-agreement.test.js (cross-renderer D-08 + nested agreement; char-identical source assertion)"
    - "assets/changelog-content-en.js entry + assets/help-content-en.js topic updates (docs hard-gate DoD)"
  key_links:
    - "backup.js is UNCHANGED — session objects round-trip wholesale via JSON; the test protects a property, it does not add serialization"
    - "The changelog + help EN edits satisfy the Phase 43 docs hard-gate; md-render.js is a genuine docs-gate TRIGGER with no help topic (docs-gate.js:379-392) — a `Help-Unaffected: assets/md-render.js — …` trailer MUST land on a commit in the pushed range (executor obligation, NOT just a SUMMARY note)"
---

<objective>
Close the phase's automated verification and satisfy the docs hard-gate. Add the two integration guards that no single feature plan owns: the RTXT-10 encrypted-backup round-trip (formatted notes survive `.sgbackup` byte-for-byte) and the cross-pipeline D-08 agreement (MdRender and the PDF renderer never disagree on what formats — the divergence risk when Plans 01 and 02 harden in parallel). Then write the EN changelog entry and update the owning EN help topics so the user-facing feature ships past the Phase 43 docs gate.

Purpose: These are cross-cutting concerns that depend on all four feature plans. `backup.js` needs no source change (notes are opaque strings in a whole-object JSON serialization) — the round-trip is a property to protect. The docs are a Definition-of-Done contract for this user-facing change.

Output: Two integration tests + changelog + help updates. The real-device/real-PDF verification is the separate Plan 06 gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-PATTERNS.md
@HELP-MAP.md
</context>

## Artifacts this phase produces (Plan 05 scope)

New/changed symbols introduced by this plan (for source-grounding / drift exclusion):

- Test files: `tests/45-backup-roundtrip.test.js`, `tests/45-pipeline-agreement.test.js`.
- A new entry/highlight in `window.CHANGELOG_CONTENT_EN` (assets/changelog-content-en.js) for the rich-text rendering feature.
- Updated topic body strings in `window.HELP_CONTENT_EN` (assets/help-content-en.js) for the owning topics (review-export, starting-a-session).
- No production source symbols — backup.js is unchanged.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RTXT-10 encrypted-backup round-trip + cross-pipeline D-08 agreement tests</name>
  <files>tests/45-backup-roundtrip.test.js, tests/45-pipeline-agreement.test.js</files>
  <read_first>
    - assets/backup.js `window.BackupManager` (L15+): `_encryptBlob` (L56), `_decryptBlob` (L74), `_assembleBackupZip` (L559), `JSON.stringify(manifest,...)` (L626), `importBackup` (L1023), `testBackupPassword` (L957)
    - tests/snippet-prefix-backup-roundtrip.test.js (existing backup round-trip test shape to copy — jsdom + WebCrypto)
    - assets/md-render.js (`MdRender.strip`, hardened `applyInline` from Plan 01) and assets/pdf-export.js (`stripInlineMarkdown`, `parseInlineBold` from Plan 02)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Legacy & Round-Trip Safety table; "The canonical RTXT-10 test"; Pitfall 2 pipeline disagreement)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-01-PLAN.md (the PINNED D-08 example table — the shared corpus)
  </read_first>
  <behavior>
    tests/45-backup-roundtrip.test.js (falsifiable, real WebCrypto — NOT a stub that vacuously passes):
    - Build a session object whose note fields contain `**bold**`, a `1.`/`2.` numbered list, a `- ` bullet list, a nested item, and a `## heading`.
    - Encrypt to a `.sgbackup` blob and decrypt-restore back (via the BackupManager encrypt/decrypt path).
    - Assert each restored note string is BYTE-IDENTICAL to the original (===). Assert the assertion actually ran on non-empty formatted content (guard against a vacuous pass).
    tests/45-pipeline-agreement.test.js (load BOTH renderers into jsdom):
    - BROADENED D-08 corpus — the pinned rows (`**bold**`, `*italic*`, `** bold **`, `2 * 3 * 4`, `a *b* c`, `word * more`, plus a Hebrew bold example) PLUS adjacency/boundary/multi-marker cases that two independently-hardened regexes could disagree on: `***x***`, `a**b**c`, `un*believable*ly`, `**a *b* c**`. Assert `MdRender.strip(x)` inline behavior and `pdf-export.stripInlineMarkdown(x)` agree on which substrings are markers (both strip the same emphasis; both leave `2 * 3 * 4` literal) for EVERY case.
    - RANDOMIZED property pass: generate a batch of short `*`-laced strings (mix of `*`, `**`, spaces, letters) and assert `MdRender.strip(x)` and `pdf-export.stripInlineMarkdown(x)` agree on all of them (property-style guard against unseen divergences).
    - CHARACTER-IDENTICAL source assertion: read both source files and assert the emphasis regexes in md-render.js `applyInline` are textually identical (char-for-char pattern source) to pdf-export.js `stripInlineMarkdown`'s — this is the structural guard the WARNING requires (behavior agreement + identical source). Also assert NEITHER contains a lookbehind `(?<` (Safari constraint preserved).
    - Assert MdRender's render output emphasizes exactly the substrings the PDF's parseInlineBold marks bold (both pipelines format the same tokens).
    - NESTED-LIST agreement (extend beyond inline-only): over the shared nesting corpus from Plan 01 Task 1 (`- a\n  - b\n- c`, `1. a\n   1. b`, and the mixed-type `- a\n  1. b`), assert MdRender's rendered nesting structure (a `<ol>`/`<ul>` nested inside the correct `<li>`, child run typed by its own marker) corresponds to the PDF `parseMarkdown` per-item depth + per-item ordered-ness (both pipelines agree on depth and on which nested run is ordered vs bulleted).
    - End each file with a count guard.
  </behavior>
  <action>
    Author two test-only files; add NO production source change. (1) `tests/45-backup-roundtrip.test.js` protects RTXT-10: a session with formatted notes survives an encrypted `.sgbackup` encrypt→decrypt round-trip byte-for-byte (backup.js already serializes session objects wholesale via JSON — this locks that notes are never individually transformed). Copy the WebCrypto/jsdom setup from `tests/snippet-prefix-backup-roundtrip.test.js`. (2) `tests/45-pipeline-agreement.test.js` protects the key link between Plan 01 (MdRender) and Plan 02 (pdf-export): over a BROADENED D-08 corpus (pinned rows + `***x***`, `a**b**c`, `un*believable*ly`, `**a *b* c**`, plus a small randomized `*`-laced property pass) MdRender and the PDF pipeline must agree on what counts as inline formatting, AND the two emphasis regexes must be CHARACTER-IDENTICAL at the source level (read both files and assert textual identity — the structural guard, since two independently-hardened regexes can pass the ~6 pinned rows yet diverge on adjacency/multi-marker inputs). Extend agreement to NESTED lists too: over the shared nesting corpus (incl. mixed-type `- a\n  1. b`), MdRender's rendered nesting and the PDF `parseMarkdown` per-item depth + per-item ordered-ness must correspond. This is the cross-cutting guard that catches any divergence introduced by the two parallel Wave-1 hardening edits. Both tests must be falsifiable (confirm each fails if you deliberately feed a wrong expectation) and end with a count guard.
  </action>
  <verify>
    <automated>node tests/45-backup-roundtrip.test.js && node tests/45-pipeline-agreement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-backup-roundtrip.test.js && node tests/45-pipeline-agreement.test.js` exit 0
    - The backup test asserts byte-identical (`===`) restored note strings for content containing bold + ordered + bullet + nested + heading
    - The agreement test asserts MdRender and pdf-export.stripInlineMarkdown agree on the BROADENED corpus (pinned rows + `***x***`, `a**b**c`, `un*believable*ly`, `**a *b* c**`) plus a randomized `*`-laced property pass, including `2 * 3 * 4` staying literal in both
    - The agreement test asserts the emphasis regexes in md-render.js `applyInline` are CHARACTER-IDENTICAL to pdf-export.js `stripInlineMarkdown`'s (source-level read + compare), and neither uses lookbehind `(?<`
    - The agreement test asserts MdRender and PDF `parseMarkdown` agree on nested-list depth + per-item ordered-ness over the shared nesting corpus (incl. mixed-type `- a\n  1. b`)
    - Both tests end with a count guard (no vacuous green)
    - `assets/backup.js` is unchanged by this plan (test-only)
  </acceptance_criteria>
  <done>The encrypted-backup round-trip and cross-pipeline D-08 agreement are locked with falsifiable tests; no backup source change.</done>
</task>

<task type="auto">
  <name>Task 2: Docs hard-gate — changelog entry + owning help topics (EN)</name>
  <files>assets/changelog-content-en.js, assets/help-content-en.js</files>
  <read_first>
    - assets/changelog-content-en.js (schema in the header comment: version/anchor/date/lede/highlights/categories{new,improved,fixed}; reverse-chronological; no emojis; client/session terminology)
    - tests/changelog-integrity.test.js (the schema gate the entry must satisfy)
    - HELP-MAP.md (topic index — review-export → topic-single-export covers pdf-export.js/export-modal.js; review-export → topic-export-formats; starting-a-session → topic-past-sessions covers sessions.js; starting-a-session → topic-new-session covers add-session.js)
    - assets/help-content-en.js (the EN help corpus of record; find the review-export and starting-a-session topics by id)
    - project CLAUDE.md "Definition of Done" (changelog + affected help topics or explicit unaffected) and .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pitfall 6 docs hard-gate; Open Question 1 — default to updating the existing session/export topic, not a new topic)
  </read_first>
  <action>
    Satisfy the Phase 43 docs hard-gate for this user-facing change (EN is the corpus of record). (1) In `assets/changelog-content-en.js`, add a changelog entry/highlight for the rich-text rendering feature: formatted session notes (bold, italic, bullet + numbered + nested lists, headings) now render as styled text when reading a session, export to PDF with Hebrew RTL preserved, and copy/share as markdown. Keep it benefit-led, no emojis, client/session terminology, schema-valid per tests/changelog-integrity.test.js. Follow the project version-bump convention (memory project-version-bump-convention) — append to the current in-progress v1.4 entry if one exists, otherwise create the entry; note the version decision in the SUMMARY for Ben. (2) In `assets/help-content-en.js`, update the owning EN topics to describe the new behavior: review-export/topic-single-export (and topic-export-formats) — the PDF now preserves bold, lists (incl. nesting), and note headings with Hebrew RTL; and starting-a-session/topic-past-sessions — reading a saved session now shows formatted notes. Do NOT create a new help topic (Phase 46 adds the editing UI — the topic grows then). (3) EXECUTOR OBLIGATION (NOTE 8, not just a SUMMARY note): `md-render.js` is a genuine docs-gate TRIGGER — it is NOT changelog-only and has no `covers[]` help topic, so `scripts/docs-gate.js` (L379-392) BLOCKS the push unless its help demand is waived. The executor MUST place the trailer `Help-Unaffected: assets/md-render.js — rendering engine; user-visible change documented under review-export + starting-a-session topics and the changelog` on a commit in the pushed range (per project CLAUDE.md, `Help-Unaffected:` is honored from ANY commit in the range — it may sit on Plan 01's md-render.js commit or the pushed tip; the reason after the em-dash is mandatory). Verify the wording matches the exact key casing `Help-Unaffected:` and names the file. Also record the trailer and the version-bump decision in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/changelog-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/changelog-integrity.test.js` exits 0 (the new/updated entry is schema-valid)
    - `assets/changelog-content-en.js` contains a highlight/entry mentioning formatted session notes / rich-text rendering (source assertion)
    - `assets/help-content-en.js` review-export and starting-a-session topics reference formatted-note rendering / PDF formatting (source assertion)
    - No emojis added; client/session terminology used
    - A `Help-Unaffected: assets/md-render.js — …` trailer (exact key casing, file named, mandatory reason) is placed on a commit in the pushed range — executor obligation, since md-render.js is a docs-gate TRIGGER with no help topic (NOTE 8), NOT merely a SUMMARY note
    - SUMMARY records the version-bump decision and the md-render.js Help-Unaffected trailer
  </acceptance_criteria>
  <done>An EN changelog entry and the owning EN help topics document reading/exporting formatted notes; the docs hard-gate is satisfiable for this push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note content → encrypted .sgbackup → restore | Formatted note strings cross the encrypt/decrypt boundary. The risk is data integrity (format drift), not confidentiality (AES-256-GCM unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-08 | Tampering (data integrity) | Backup round-trip silently altering note formatting | medium | mitigate | Falsifiable byte-identical round-trip test (Task 1); backup.js unchanged (whole-object JSON). |
| T-45-03 | Tampering | MdRender / PDF pipeline divergence after parallel D-08 hardening | medium | mitigate | Cross-pipeline agreement test (Task 1) over the pinned corpus. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase; no new dependency. |
</threat_model>

<verification>
- `node tests/45-backup-roundtrip.test.js && node tests/45-pipeline-agreement.test.js && node tests/changelog-integrity.test.js` exit 0
- `node tests/run-all.js` FULL suite green (this is the per-phase-gate sampling: 172+ files)
- Changelog + help EN edits present (docs-gate satisfiable)
</verification>

<success_criteria>
- Encrypted-backup round-trip preserves formatted notes byte-for-byte (RTXT-10, automated)
- MdRender and PDF agree on inline formatting across the D-08 corpus
- EN changelog entry + owning help topics updated; schema-valid; docs gate satisfiable
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-05-SUMMARY.md` when done
</output>
