---
phase: 45-rich-text-rendering-export-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/pdf-export.js
  - tests/45-inline-hardening.test.js
  - tests/45-pdf-nested-lists.test.js
autonomous: true
requirements: [RTXT-07, RTXT-10]
must_haves:
  truths:
    - "PDF inline emphasis obeys the same D-08 hug-non-whitespace rule as MdRender (2 * 3 * 4 stays literal); the emphasis regexes are CHARACTER-IDENTICAL to md-render.js applyInline's"
    - "The PDF invariant parseInlineBold(x).map(s=>s.text).join('') === stripInlineMarkdown(x) still holds after hardening"
    - "Nested (indented) bullet AND numbered lists render with increasing physical indent per level, correct in Hebrew RTL"
    - "Mixed-type nested lists (a numbered run nested under a bullet, or vice-versa) render each nested run with its OWN marker type — not the parent's block-level flag (- a / 1. b → bullet then a numbered child, NOT '- 1. b')"
  artifacts:
    - "assets/pdf-export.js parseMarkdown + parseInlineBold + stripInlineMarkdown extended in place (per-item depth AND per-run ordered-ness, not one block-level flag)"
    - "tests/45-inline-hardening.test.js, tests/45-pdf-nested-lists.test.js"
  key_links:
    - "D-08 hardening in stripInlineMarkdown AND parseInlineBold must be identical, or drawSegmentedLine misaligns bold runs"
    - "PDF inline rule must match MdRender's (Plan 01 pinned example table) — the emphasis regexes stay CHARACTER-IDENTICAL to applyInline's; Plan 05 asserts cross-pipeline agreement at source level"
    - "The nested-list indent-step→depth convention (2 spaces = 1 level, incl. mixed-type nesting) is the shared contract pinned in Plan 01 Task 1 — keep this pipeline consistent with it"
    - "Nested-list indent is a PHYSICAL offset keyed off docDir (not logical CSS) — RTL must indent rightward"
---

<objective>
Extend the Phase 23 Hebrew-bidi PDF pipeline (`assets/pdf-export.js`) in place so formatted notes survive PDF export: apply the D-08 inline-emphasis hardening to `stripInlineMarkdown` and `parseInlineBold` (preserving the segment invariant that `drawSegmentedLine` relies on), and add nested-list support (bullet + numbered) with correct physical indentation in both LTR and Hebrew RTL.

Purpose: The PDF is one of the three read surfaces. Bold and ordered lists already render (quick tasks iwr/c8x/cx5). The NEW work here is (1) making the PDF's inline rule agree with MdRender's hardened rule so preview and PDF never disagree, and (2) list nesting. This plan deliberately excludes the note-heading D-02/D-03 work (that is Plan 03, which also touches this file) to keep this change bounded and to sequence pdf-export.js edits one hand at a time.

Output: A hardened, nesting-aware PDF parse/render pipeline with falsifiable tests, verified against the shared D-08 contract table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-CONTEXT.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-PATTERNS.md
@.planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md
@.planning/milestones/v1.1-phases/23-pdf-hebrew-rtl-rewrite
</context>

## Artifacts this phase produces (Plan 02 scope)

New/changed symbols introduced by this plan (for source-grounding / drift exclusion):

- Hardened `stripInlineMarkdown` + `parseInlineBold` emphasis matching (D-08) — behavior change, same function names.
- Nested-list rendering in `parseMarkdown` + the list-draw loop — per-item indent depth field (e.g. `item.indent` / `block.items[n].depth`, executor's choice) and a physical per-level indent offset keyed off `docDir` (behavior + one new item field).
- Test files: `tests/45-inline-hardening.test.js`, `tests/45-pdf-nested-lists.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden PDF inline rules (D-08) preserving the segment invariant</name>
  <files>assets/pdf-export.js, tests/45-inline-hardening.test.js</files>
  <read_first>
    - assets/pdf-export.js `stripInlineMarkdown` (L423-436) and `parseInlineBold` (L438-478+) — the two functions that must harden identically
    - assets/pdf-export.js L504 — the inner-italic strip INSIDE a bold span (`inner = inner.replace(/(^|[^*])\*([^*\n]+?)\*(?!\*)/g, '$1$2')`); this is a THIRD emphasis site inside parseInlineBold that must harden in lockstep or `**2 * 3 * 4**` breaks the invariant (WARNING 4)
    - assets/pdf-export.js L448-453 — the documented INVARIANT `parseInlineBold(input).map(s=>s.text).join('') === stripInlineMarkdown(input)` that `drawSegmentedLine` relies on
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pitfall 2; Code Examples "PDF bidi-segment invariant")
    - .planning/phases/45-rich-text-rendering-export-foundation/45-01-PLAN.md (Task 2 — the PINNED D-08 example table this pipeline must match verbatim)
  </read_first>
  <behavior>
    Write tests/45-inline-hardening.test.js FIRST. Assert on the EXACT SAME D-08 example table pinned in Plan 01 Task 2, evaluated against pdf-export's own functions:
    - `stripInlineMarkdown('2 * 3 * 4')` === `'2 * 3 * 4'` (legacy literal — no strip)
    - `stripInlineMarkdown('**bold**')` === `'bold'`
    - `stripInlineMarkdown('** bold **')` === `'** bold **'` (space-adjacent → literal)
    - `stripInlineMarkdown('a *b* c')` === `'a b c'`
    - `stripInlineMarkdown('**2 * 3 * 4**')` === `'2 * 3 * 4'` (outer `**` bold stripped, but the INNER `* 3 *` is space-adjacent so it stays LITERAL — the space-adjacent multiplication survives inside a bold span; WARNING 4)
    - For EACH of those inputs (plus a Hebrew example `הסיכום **מודגש** כאן`): assert the invariant `parseInlineBold(x).map(s=>s.text).join('') === stripInlineMarkdown(x)` holds (this is the falsifiable guard against the two functions diverging). The `**2 * 3 * 4**` row is the specific guard for the inner-italic strip at pdf-export.js:504 — if that strip is NOT hardened in lockstep it turns `2 * 3 * 4` into `2  3  4` (or `2 3 * 4`) inside the bold segment while stripInlineMarkdown leaves `2 * 3 * 4` literal, breaking the invariant and misaligning `drawSegmentedLine` (WARNING 4).
    - `parseInlineBold('**bold**')` yields a segment with `bold:true` for `bold` (positive case preserved)
    - End with a count guard.
    Load pdf-export.js into an isolated jsdom window with the vendored jsPDF stub as the existing PDF tests do; confirm the test goes RED on the pre-hardening source for `2 * 3 * 4` before implementing.
  </behavior>
  <action>
    Tighten the emphasis regexes in BOTH `stripInlineMarkdown` (L434-435) and `parseInlineBold` so `*`/`**` markers must hug non-whitespace — identical rule to md-render.js `applyInline` (Plan 01 Task 2), matching the pinned D-08 example table byte-for-byte. Same Safari-compat constraint: NO lookbehind — use the capture-group / character-class technique already present (`(^|[^*])` prefix, `(?!\*)` suffix). CHARACTER-IDENTICAL requirement: the hardened emphasis regexes in `stripInlineMarkdown` MUST end up textually identical (same pattern source, char-for-char) to md-render.js `applyInline`'s (Plan 01 Task 2) — not merely equivalent; two independently-hardened regexes can both pass the ~6-row table yet diverge on adjacency/multi-marker cases (`***x***`, `a**b**c`, `un*believable*ly`, `**a *b* c**`). Plan 05 Task 1 asserts this character-identity at the source level. The two functions here MUST also change in lockstep: any input's stripped output must equal the concatenation of its parseInlineBold segment texts, or `drawSegmentedLine` will misplace bold runs after bidi reorder. CRITICAL (WARNING 4): `parseInlineBold` hardens in THREE places, not one — its own bold match, its italic single-`*` match, AND the inner-italic strip INSIDE a bold span at pdf-export.js:504 (`inner = inner.replace(/(^|[^*])\*([^*\n]+?)\*(?!\*)/g, '$1$2')`). That :504 inner strip must take the SAME hug-non-whitespace hardening, or `**2 * 3 * 4**` yields a bold segment `2  3  4` while `stripInlineMarkdown` leaves `2 * 3 * 4` — the invariant breaks. Harden the :504 inner strip identically to stripInlineMarkdown's emphasis rule. Do not alter the `{text, bold}` segment shape, the bidi handling, or the ordered-list typed-ordinal contract. This is inline-emphasis hardening only — do NOT touch heading rendering or list nesting here (Task 2 / Plan 03).
  </action>
  <verify>
    <automated>node tests/45-inline-hardening.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-inline-hardening.test.js` exits 0
    - `stripInlineMarkdown('2 * 3 * 4')` returns the string unchanged (behavior assertion)
    - `stripInlineMarkdown('**2 * 3 * 4**')` === `'2 * 3 * 4'` and the invariant `parseInlineBold('**2 * 3 * 4**').map(s=>s.text).join('') === stripInlineMarkdown('**2 * 3 * 4**')` holds — proving the inner-italic strip at pdf-export.js:504 was hardened in lockstep (WARNING 4)
    - The invariant `parseInlineBold(x).map(s=>s.text).join('') === stripInlineMarkdown(x)` is asserted true for every table input including a Hebrew case and the `**2 * 3 * 4**` space-adjacent-italic-inside-bold case
    - `assets/pdf-export.js` inline emphasis code contains no lookbehind token `(?<`
    - The `stripInlineMarkdown` emphasis regexes are CHARACTER-IDENTICAL to md-render.js `applyInline`'s (shared-contract requirement; the cross-file source assertion lives in Plan 05 Task 1)
    - Test was confirmed RED on pre-change source for the `2 * 3 * 4` case (note this in the SUMMARY)
    - Test ends with a count guard
  </acceptance_criteria>
  <done>PDF inline emphasis hugs non-whitespace, matches MdRender's D-08 rule, and preserves the parseInlineBold/stripInlineMarkdown invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nested-list rendering to the PDF pipeline (bullet + numbered, RTL-safe)</name>
  <files>assets/pdf-export.js, tests/45-pdf-nested-lists.test.js</files>
  <read_first>
    - assets/pdf-export.js `parseMarkdown` list branch (L596-635 — where list items are collected; ordered-vs-unordered is decided ONCE from the first line at L615 as a BLOCK-level `ordered` flag pushed at L634; the unordered branch strip `replace(/^\s*[-*]\s+/, "")` at L630 does NOT strip `1.`; typed-ordinal contract L622-628)
    - assets/pdf-export.js list-draw loop (~L1425-1520 — the renderer reads the BLOCK-level `block.ordered` (~L1425) for the whole run; `listPrefix`, `drawSegmentedLine`, the docDir-keyed anchoring at L1484-1499 that already handles RTL ordered-list prefixes)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-01-PLAN.md (Task 1 SHARED NESTING CONVENTION table — 2 spaces = 1 level, incl. the mixed-type `- a\n  1. b` case; this pipeline must AGREE with it)
    - .planning/phases/45-rich-text-rendering-export-foundation/45-RESEARCH.md (Pattern 3 nesting; Pitfall 4 "RTL nesting indents the wrong direction"; Don't Hand-Roll "Deriving list ordinals from position")
    - .planning/phases/45-rich-text-rendering-export-foundation/45-UI-SPEC.md (§ Spacing: +one indent step per nested level; § C PDF)
    - memory reference-rtl-logical-props-physical-coords (physical coords, not logical, for PDF)
  </read_first>
  <behavior>
    Write tests/45-pdf-nested-lists.test.js FIRST as a falsifiable jsPDF spy (guard against the repo's false-GREEN jsdom-PDF history — memory reference-pdf-jsdom-inert-gates):
    - Parse `- a\n  - b\n- c`: `parseMarkdown` produces list items carrying an indent/depth field (depth 0 for a/c, depth 1 for b) — assert on the parsed block structure, NOT just that a function was called.
    - Parse `1. a\n   1. b`: nested ordered item carries depth 1 AND preserves its typed ordinal (do not position-derive).
    - Parse `- a\n  1. b` (MIXED type): the depth-0 item `a` is unordered (bullet); the depth-1 item `b` is ORDERED and carries its typed ordinal — assert the nested run's ordered-ness is decided by its OWN marker, NOT the block's first-line flag. Render it and assert the nested `b` draws with a numbered prefix (e.g. `1.`), NOT a bullet and NOT the literal `1.` left un-stripped inside a bullet item (the pre-change block-level flag would produce `- 1. b`).
    - Render an LTR doc: spy on `drawSegmentedLine` (or the doc.text anchor) and assert the depth-1 item's start offset is greater (further indented) than the depth-0 item's by the per-level step.
    - Render an RTL (Hebrew) doc: assert the depth-1 item indents toward the RIGHT margin (physical offset decreases the rightX anchor by the per-level step), NOT leftward.
    - Render an RTL (Hebrew) doc with a MIXED-type nested ordered child whose content is LTR (e.g. `- שלום\n  1. hello`): assert the nested ordered child `1. hello` takes the split-row ordered-prefix path (two `drawSegmentedLine` calls: a right-anchored prefix + content just inside it) — i.e. the split-row branch keyed off the ITEM's ordered-ness, not `block.ordered` (whose block is unordered here). This is the NOTE 5 falsifiable guard against the c8x Bug B regression.
    - Confirm the spy test goes RED on pre-change source (flat lists ignore depth; the mixed case renders `- 1. b`; the RTL mixed ordered child skips the split-row path because `block.ordered` is false) before implementing.
    - End with a count guard.
  </behavior>
  <action>
    Extend `parseMarkdown`'s list branch (L596-635) to record, PER ITEM: (a) its nesting depth from leading whitespace (per the SHARED NESTING CONVENTION in Plan 01 Task 1 — 2 spaces = 1 level; keep consistent), and (b) its OWN ordered-ness from its OWN marker (`\d+\.` → ordered, `[-*]` → unordered). Do NOT keep relying on the single block-level `ordered` flag decided from the first line (L615) — that flag cannot represent a mixed-type nested run and causes the unordered strip (L630) to leave a nested `1.` un-stripped, rendering `- 1. b` (D-05: nested lists land in Phase 45; D-04: numbered lists render everywhere). Apply the marker-appropriate strip per item: ordered items strip `^\s*\d+\.\s+` and keep `{text, ordinal}` (typed ordinal, NOT position-derived); unordered items strip `^\s*[-*]\s+`. Store depth as a per-item field alongside. (You may keep the block-level `ordered` as the depth-0 run's type for back-compat, but each item's render prefix MUST follow the item's own type.) Then in the list-draw loop (~L1425-1520) choose each item's prefix from ITS OWN ordered-ness and offset its anchor by `depth * PER_LEVEL_INDENT` as a PHYSICAL offset keyed off `docDir`: in LTR add to the left/start anchor; in RTL subtract from the `rightX` anchor (mirror the existing docDir prefix-anchoring at L1484-1499 — reason in physical coordinates, never logical CSS, per Pitfall 4). NOTE 5 — the RTL split-row branch condition at pdf-export.js:1484 currently keys on the BLOCK-level `block.ordered` (`if (docDir === 'rtl' && wi === 0 && block.ordered && firstStrongDir(listStripped) === 'ltr')`): convert that `block.ordered` term to the ITEM's own ordered-ness (the per-item ordered flag you added), else a nested ORDERED child with LTR content in a Hebrew doc — whose parent block is unordered, so `block.ordered` is false — skips the split-row path and re-triggers the c8x Bug B class (misplaced ordered prefix after bidi reorder). Preserve the existing bidi segment rendering, the typed-ordinal contract, and the LTR/RTL split-row logic for ordered prefixes. Flat (depth-0-only, single-type) lists must render byte-identically to today.
  </action>
  <verify>
    <automated>node tests/45-pdf-nested-lists.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/45-pdf-nested-lists.test.js` exits 0
    - `parseMarkdown('- a\n  - b\n- c')` items expose a depth/indent field: depth 0, 1, 0 respectively (source/behavior assertion)
    - `parseMarkdown('- a\n  1. b')` classifies item `a` unordered and nested item `b` ORDERED (per-item marker, not block-level flag); `b` renders a numbered prefix, NOT `- 1. b` (mixed-type nesting agrees with MdRender / Plan 01)
    - Nested ordered item retains its typed ordinal (not position-derived)
    - LTR render: depth-1 item is anchored further from the start margin than depth-0 (spy assertion on the draw anchor)
    - RTL render: depth-1 item indents toward the right margin (physical offset off docDir)
    - Test confirmed RED on pre-change source before implementation (noted in SUMMARY)
    - Test ends with a count guard
  </acceptance_criteria>
  <done>Nested bullet, numbered, AND mixed-type lists render with correct per-item marker and per-level physical indent in both LTR and Hebrew RTL; flat single-type lists unchanged; ordinals preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note markdown → jsPDF output | User note strings flow into the vendored jsPDF renderer. No innerHTML/DOM sink here — jsPDF draws text/vectors; XSS is not the surface. The risk is correctness (bold misalignment, RTL mis-indent), not injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-03 | Tampering (data integrity) | parseInlineBold / stripInlineMarkdown divergence after D-08 | medium | mitigate | Invariant asserted in Task 1 test on every table input; both functions hardened in lockstep. |
| T-45-04 | Information disclosure (garbled export) | RTL nested-list indent direction | low | mitigate | Physical-offset-by-docDir (Task 2); verified in Hebrew on a real opened PDF at the phase gate (Plan 05 checkpoint). |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase; jsPDF/bidi.min.js pre-vendored and unchanged. |
</threat_model>

<verification>
- `node tests/45-inline-hardening.test.js && node tests/45-pdf-nested-lists.test.js` exit 0
- `node tests/run-all.js` full suite stays green — existing PDF tests (Phase 23/34, iwr/c8x/cx5) must not regress
- Both new PDF tests were confirmed RED on pre-change source (false-GREEN guard)
</verification>

<success_criteria>
- PDF inline emphasis matches the shared D-08 contract (regexes character-identical to MdRender's); invariant preserved
- Nested bullet + numbered + MIXED-type lists render with correct per-item marker and correct LTR and RTL physical indent
- Typed-ordinal contract and existing bidi rendering intact
- No regression in the existing PDF test suite
</success_criteria>

<output>
Create `.planning/phases/45-rich-text-rendering-export-foundation/45-02-SUMMARY.md` when done
</output>
