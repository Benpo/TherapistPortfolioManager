---
phase: 26-in-app-onboarding-overview-help-system
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/index.html
  - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/help.html
  - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.css
  - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.js
  - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/README.md
autonomous: true
requirements: [D-02, D-09, D-10, D-11]
must_haves:
  truths:
    - "Opening the mockup shows the full-screen welcome, the Help page, and the interactive tour as the UI-SPEC specifies (D-09: full-screen branded welcome with two CTAs; D-02: persistent globally-reachable '?' entry point beside cloud+gear)"
    - "Deleting/hiding any tour step's data-tour anchor degrades that step to a centered modal with text + a working 'take me there' link — never a silent skip (D-11: hybrid tour + graceful degradation demonstrated per step)"
    - "The mockup renders without layout breakage in [dir=rtl] and [data-theme=dark]"
    - "The mockup visually reads as the same product (garden tokens + botanical identity), not a bolted-on skin (D-10: extend the garden design system, dark-mode aware, RTL-safe)"
    - "The mockup is unreachable from the production build / service-worker precache and contains no real client data, license keys, or secrets"
  artifacts:
    - path: ".planning/phases/26-in-app-onboarding-overview-help-system/mockup/help.html"
      provides: "Clickable Help page mockup (workflow spine + technical track + '?' entry point)"
      min_lines: 40
    - path: ".planning/phases/26-in-app-onboarding-overview-help-system/mockup/index.html"
      provides: "Mockup entry: full-screen welcome + a second page to demonstrate the cross-page tour hop"
      min_lines: 40
    - path: ".planning/phases/26-in-app-onboarding-overview-help-system/mockup/README.md"
      provides: "Throwaway-mockup notice + the RTL/dark/anchor-fallback/cross-page validation checklist"
      contains: "not production"
  key_links:
    - from: "mockup tour steps"
      to: "26-UI-SPEC.md Graceful Degradation contract"
      via: "delete-anchor → centered modal + take-me-there demonstration"
      pattern: "data-tour"
    - from: "mockup copy"
      to: "26-CONTENT-OUTLINE-EN.md help.* strings"
      via: "spine + technical-track + welcome + tour copy transcribed from the outline"
      pattern: "help\\."
---

<objective>
Build a clickable HTML mockup of the full-screen welcome + the Help page + the
interactive tour, extending the garden/botanical design system, dark-mode aware
and RTL-safe, that visually validates the 26-01 UI-SPEC against the 26-02 EN
content — with NO production wiring (D-08). It lives at a clearly non-production
path and must be unreachable from the build / service worker.

Purpose: Prove the design works (especially the D-11 graceful-degradation and
the cross-page tour hop) before the follow-up phase writes production code, so
the implementation phase needs no re-design.

Output: a self-contained mockup under
`.planning/phases/26-in-app-onboarding-overview-help-system/mockup/` plus a
README stating it is a throwaway design artifact and listing the validation
checklist. NO changes to `assets/*`, `*.html` production pages, `sw.js`, or i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-in-app-onboarding-overview-help-system/26-CONTEXT.md
@.planning/phases/26-in-app-onboarding-overview-help-system/26-UI-SPEC.md
@.planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md
@.planning/phases/26-in-app-onboarding-overview-help-system/26-RESEARCH.md

<interfaces>
<!-- Design references the mockup must visually cohere with. Read to replicate identity/selectors; the mockup COPIES tokens into its own throwaway stylesheet — it must NOT import or modify production assets (D-08/D-13). -->

26-UI-SPEC.md (plan 26-01 output) — the binding contract: the chosen "?" entry-point placement,
  the full-screen welcome structure + `sg.welcomeSeen` behavior, the tour-step schema, and the
  "Graceful Degradation" per-step resolution + verification checklist. The mockup MUST render
  what this spec says.

26-CONTENT-OUTLINE-EN.md (plan 26-02 output) — the EN copy: transcribe `help.welcome.*`,
  `help.spine.*`, `help.tour.*`, `help.tech.*` strings verbatim into the mockup so the design
  is validated against real content (not lorem ipsum).

Garden identity to replicate (reference only — do NOT import or edit):
  - `assets/tokens.css` — copy the needed `--color-*` / `--shadow-*` custom properties and the
    `[data-theme="dark"]` overrides into the throwaway `mockup.css`; Rubik font.
  - `assets/demo-hints.js` — pulsing-dot + tooltip rendering pattern, RTL logical properties
    (`inset-inline-end`, `margin-inline-start`), dark `[data-theme="dark"]` selector, off-screen
    clamp — replicate the *pattern* in `mockup.js`, do not copy the iframe gate.
  - Phase 25 header (`25-UI-SPEC.md` "Header entry point") — `#headerActions` with cloud + gear
    at 44×44, RTL auto-flip; the "?" affordance sits beside them per the 26-01 chosen placement.
  - Botanical illustrations under `assets/illustrations/` / `assets/landing/` — reference the
    visual identity (a static placeholder or copied asset is fine for a throwaway mockup;
    do NOT modify the originals).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the welcome + Help page mockup in the garden design system (dark + RTL aware)</name>
  <read_first>
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-UI-SPEC.md (the "?" placement, welcome structure, Component Inventory — render exactly this)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md (transcribe help.welcome.* + help.spine.* + help.tech.* copy verbatim)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-RESEARCH.md ("Design Validation" table, "Welcome-Overlay UX")
    - assets/tokens.css (copy the needed custom properties + [data-theme="dark"] block into mockup.css — do NOT import or modify the original)
    - .planning/phases/25-backup-architectural-rework/25-UI-SPEC.md ("Header entry point" — the cloud+gear header the "?" sits beside)
  </read_first>
  <action>
    Create the mockup directory
    `.planning/phases/26-in-app-onboarding-overview-help-system/mockup/` containing:

    - `mockup.css` — a SELF-CONTAINED throwaway stylesheet that copies (not @imports) the
      required `tokens.css` custom properties and the `[data-theme="dark"]` overrides, uses
      logical properties only (`margin-inline-*`, `inset-inline-*`, `padding-inline-*`), and
      reproduces the garden look. No ad-hoc hex outside the copied token set.
    - `index.html` — the mockup entry page: the **full-screen branded welcome** per the
      26-01 spec (warm greeting + benefit sentences + two CTAs "Take the guided tour" /
      "I'll explore myself", botanical illustration, garden tokens), plus a minimal app-shell
      header containing the Phase-25 `#headerActions` cluster (cloud + gear placeholders) and
      the **"?" entry point** at the 26-01-chosen placement, 44×44, beside them.
    - `help.html` — the **Help page** mockup: the workflow-spine sections in D-03 order and
      the parallel technical-tips track, with the flagship "make it yours" customizable-
      sections section surfaced early — copy transcribed verbatim from 26-CONTENT-OUTLINE-EN.md.
      Include the shared header + "?" affordance and a nav slot matching the 26-01 `renderNav`
      decision.
    - A top-of-`index.html` toggle pair (plain mockup JS, not production wiring) to flip
      `[data-theme="dark"]` and `[dir="rtl"]` so the validator can exercise both. Include a
      `lang`/RTL toggle that swaps to a short Hebrew-representative string on at least the
      welcome + one Help section to exercise RTL + length variance.
    - `README.md` — states this is a THROWAWAY design mockup, NOT production, MUST NOT be
      added to `sw.js` PRECACHE_URLS or the deploy whitelist, contains no real client data /
      license keys / secrets, and lists the validation checklist (RTL render, dark mode,
      anchor-fallback walkthrough, cross-page continuity) from RESEARCH "Design Validation".

    Fidelity is Claude's discretion (static clickable vs. lightly interactive) — enough to
    validate the design per RESEARCH. NO production logic: the CTAs and "?" navigate
    statically between the mockup pages only; no `localStorage` gating logic, no real data.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-in-app-onboarding-overview-help-system/mockup/index.html && test -f .planning/phases/26-in-app-onboarding-overview-help-system/mockup/help.html && test -f .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.css && test -f .planning/phases/26-in-app-onboarding-overview-help-system/mockup/README.md && grep -Eqi "not production|throwaway|do not.*precache|not.*sw\.js" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/README.md && grep -Eq "data-theme|\\[dir" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/index.html && grep -Eqi "inset-inline|margin-inline|padding-inline" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.css && ! grep -RqsE "PRECACHE_URLS|importScripts" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/</automated>
  </verify>
  <acceptance_criteria>
    - The mockup directory exists with index.html, help.html, mockup.css, README.md.
    - index.html renders the full-screen branded welcome per 26-01 (greeting + benefit sentences + two CTAs + botanical/garden treatment) and a header with the Phase-25 cloud+gear cluster plus the "?" entry point at the 26-01-chosen placement, 44×44.
    - help.html renders the workflow spine in D-03 order + the parallel technical track, with the flagship customizable-sections section surfaced early; copy is transcribed from 26-CONTENT-OUTLINE-EN.md (no lorem ipsum).
    - mockup.css copies the needed tokens.css custom properties + a [data-theme="dark"] block and uses logical properties only (no physical right/left/text-align:right, no ad-hoc hex outside the token set).
    - Working `[data-theme="dark"]` and `[dir="rtl"]` (+ a Hebrew-representative string) toggles exist; manual checklist enumerated in README confirms no layout breakage in either mode.
    - README states the mockup is throwaway/non-production, must not enter sw.js PRECACHE_URLS or the deploy whitelist, and contains no real client data/keys/secrets.
    - No file under `assets/`, no production `*.html`, no `sw.js`, no i18n file is modified; the mockup directory contains no service-worker registration or PRECACHE reference.
  </acceptance_criteria>
  <done>A garden-coherent, dark+RTL-aware welcome and Help page mockup exists at the non-production path with content from the EN outline and a throwaway-notice README; zero production code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the interactive tour mockup and demonstrate the D-11 degradation + cross-page hop</name>
  <read_first>
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-UI-SPEC.md ("Graceful Degradation" section — the per-step resolution + verification checklist this task must visibly demonstrate)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md (help.tour.* copy — transcribe verbatim into the tour steps)
    - .planning/phases/26-in-app-onboarding-overview-help-system/mockup/index.html and help.html (the files Task 1 created — add the tour layer + data-tour anchors here)
    - assets/demo-hints.js (replicate the pulsing-dot/tooltip + RTL/dark/off-screen-clamp PATTERN in mockup.js; do NOT copy the iframe gate, do NOT modify this file)
  </read_first>
  <action>
    Create `mockup.js` (throwaway mockup script — NOT production) and add `data-tour="…"`
    anchor attributes to the relevant elements across the mockup `index.html` + `help.html`
    matching the concrete anchor list in 26-01's tour-step contract, in D-03 spine order.

    `mockup.js` must implement, as a demonstration only (no production persistence/wiring):

    - The replayable tour over the mockup, modeled on the `demo-hints.js` rendering pattern
      (pulsing dot + tooltip, logical-property positioning, `[data-theme="dark"]` aware),
      launched from the welcome "Take the guided tour" CTA and re-launchable from the "?".
    - **The D-11 graceful-degradation demonstration**: provide a visible control (e.g. a
      "hide this step's anchor" toggle) so a reviewer can, per step, remove the `data-tour`
      anchor and SEE the step degrade to a centered modal showing the step's i18n text + a
      working "Take me there" link — never a silently-skipped step. This must work for every
      tour step.
    - **The cross-page hop demonstration**: at least one step whose target lives on the other
      mockup page; clicking "Next" visibly navigates to that page and the tour visibly
      resumes there (static demonstration is acceptable — carry `{stepIndex}` via a URL
      param or sessionStorage for the mockup only; this proves the design, it is not
      production logic).
    - A language/RTL flip during the tour that re-renders the active tooltip in the new
      direction (demonstrating the `app:language` re-render rule) without stranding it.

    Update `README.md`'s validation checklist into an explicit, step-by-step manual script a
    reviewer follows: (1) RTL render of welcome+Help+every tour step+every fallback modal;
    (2) dark-mode toggle of all surfaces; (3) per-step delete-anchor → modal+take-me-there;
    (4) cross-page hop resumes; (5) language flip mid-tour does not strand a tooltip; (6) the
    "?" sits beside cloud+gear without disrupting the Phase-25 header. Each item has an
    objective pass criterion (no subjective "looks good").

    NO production wiring: no real `localStorage.sg.welcomeSeen` logic, no real data, no
    service-worker, the mockup stays entirely under the non-production path.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.js && grep -q "data-tour" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/help.html && grep -Eqi "take me there|takeMeThere" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.js && grep -Eqi "modal|fallback|degrade" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.js && grep -q "help.tour" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/mockup.js && grep -Eqi "delete.*anchor|hide.*anchor|anchor.*fallback|cross-page|resume" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/README.md && ! grep -RqsE "sw\.js|serviceWorker|PRECACHE_URLS" .planning/phases/26-in-app-onboarding-overview-help-system/mockup/</automated>
  </verify>
  <acceptance_criteria>
    - mockup.js exists and renders a replayable tour over the mockup using the demo-hints.js rendering pattern (pulsing dot + tooltip, logical-property positioning, dark-aware), launched from the welcome CTA and re-launchable from the "?".
    - `data-tour` anchors are present across the mockup pages matching the 26-01 tour-step list in D-03 order; tour copy is transcribed from `help.tour.*` in 26-CONTENT-OUTLINE-EN.md.
    - A reviewer can, per step, remove the step's `data-tour` anchor and see it degrade to a centered modal with the step's text + a working "Take me there" link — demonstrated for every step, never a silent skip (D-11).
    - At least one tour step crosses to the other mockup page and the tour visibly resumes there (static demonstration acceptable).
    - A mid-tour language/RTL flip re-renders the active tooltip without stranding it.
    - README contains an explicit step-by-step validation script with objective pass criteria for RTL, dark mode, per-step anchor-fallback, cross-page resume, mid-tour language flip, and header non-regression.
    - No production source / `*.html` / `sw.js` / i18n file modified; the mockup directory contains no service-worker registration or PRECACHE reference.
  </acceptance_criteria>
  <done>The interactive tour mockup demonstrates the D-11 graceful-degradation per step and the cross-page hop, with a reviewer-followable validation script; zero production code touched and the mockup is unreachable from the production build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mockup files → production build / service worker | A throwaway mockup must never enter the deploy whitelist or `sw.js` PRECACHE_URLS, or it ships to customers |
| mockup content → real user data | A design mockup must contain only fake placeholder content, never real client records, license keys, or secrets |
| mockup tour design → PII/session exposure | The `data-tour` anchors + tour rendering must, by design, not read or log session content or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Information Disclosure | mockup file contents | mitigate | Acceptance criteria forbid any real client data, license key, or secret in the mockup; only fabricated placeholder copy and the generic EN outline strings are used |
| T-26-10 | Elevation of Privilege | mockup reachable from production | mitigate | Mockup lives ONLY under `.planning/phases/26-…/mockup/`; README + acceptance criteria forbid adding it to `sw.js` PRECACHE_URLS or the deploy whitelist; verify gate asserts no service-worker/PRECACHE reference exists in the mockup dir |
| T-26-11 | Tampering | accidental edit of production assets while replicating identity | mitigate | The mockup copies tokens into a self-contained `mockup.css` rather than importing production CSS; acceptance criteria + verification assert no `assets/*`, `*.html`, `sw.js`, or i18n file is modified |
| T-26-12 | Information Disclosure | tour-anchor design exposing PII | accept | Residual risk LOW — `data-tour` is an inert marker attribute; the mockup uses fake content and the tour reads no real data; the design contract (26-01) already forbids the engine reading/logging PII |
| T-26-13 | Repudiation | n/a (static throwaway artifact, no server/auth/audit) | accept | Residual risk LOW — no multi-user/server/audit surface |

Residual risk is LOW: the deliverable is a non-wired static mockup at a
non-production path. The only real exposure (shipping the mockup, or leaking real
data into it) is closed by T-26-09/T-26-10/T-26-11, which the acceptance criteria
and verify gates enforce mechanically.
</threat_model>

<verification>
- The mockup exists and runs by opening `index.html` in a browser — no build, no server.
- It renders the welcome, Help page, and interactive tour per 26-01, using 26-02 copy.
- `[dir="rtl"]` and `[data-theme="dark"]` toggles work with no enumerated layout breakage.
- Every tour step's anchor can be removed to demonstrate the D-11 modal + take-me-there fallback; the cross-page hop resumes; a mid-tour language flip does not strand a tooltip.
- README marks the mockup throwaway/non-production with a reviewer validation script.
- `git status --porcelain` shows changes ONLY under `.planning/phases/26-…/mockup/`; no `assets/*`, `*.html`, `sw.js`, or i18n file touched; no PRECACHE/service-worker reference in the mockup dir.
</verification>

<success_criteria>
The clickable mockup visually validates the 26-01 UI-SPEC against the 26-02 EN
content — including a per-step demonstration of the D-11 graceful degradation and
the cross-page tour hop — in the garden design system, dark-mode and RTL safe,
with zero production wiring and zero reachability from the production build.
</success_criteria>

<output>
After completion, create `.planning/phases/26-in-app-onboarding-overview-help-system/26-03-SUMMARY.md`
</output>
