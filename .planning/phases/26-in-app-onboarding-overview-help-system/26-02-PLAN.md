---
phase: 26-in-app-onboarding-overview-help-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-12, D-13, D-14]
must_haves:
  truths:
    - "A new trial therapist reading the EN content outline understands how to run a session end-to-end and discovers every major feature in the place it is used (D-03: workflow-first content model / spine order)"
    - "The flagship customizable-session-sections personalization story and per-session export lead the narrative, not buried as options (D-04: comprehensive coverage, flagship customizable sections + per-session export)"
    - "The technical-tips track comprehensively covers backups/restore, PDF export, per-browser PWA install, the offline/local-only explainer, the 2-device activation limit, deactivate/transfer, and troubleshooting (D-05: technical track = backups & restore + PDF export; D-06: PWA-install/user-manual TODO folded fully)"
    - "Every piece of copy is string-key-ready (data-i18n style, help.* namespace, EN filled) and authored so it translates to natural Hebrew noun/infinitive forms (D-13: independent of the marketing demo, no shared content contract; D-14: calm/wellness + practice-mgmt tone benchmark)"
    - "Every feature claim matches actual shipped app behavior (the basis for Sapir's clinical-accuracy review) (D-12: Claude drafts EN canonical content from app behavior, Sapir reviews for clinical accuracy + tone)"
  artifacts:
    - path: ".planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md"
      provides: "The canonical EN workflow-spine + technical-track content outline, i18n-key-mapped"
      contains: "help.tour"
  key_links:
    - from: "content outline workflow spine"
      to: "D-03 spine order (add client → session → emotions → Heart Shield → severity → finish/export → overview)"
      via: "ordered sections each mapped to help.* i18n keys"
      pattern: "help\\."
    - from: "technical-tips track"
      to: "D-06 folded PWA/user-manual scope"
      via: "per-browser install + offline explainer + 2-device activation + deactivate/transfer + troubleshooting subsections"
      pattern: "Add to Home Screen|offline|activation"
---

<objective>
Author the canonical English content outline for the in-app help system,
organized around the therapeutic workflow spine (D-03), leading with the
flagship personalization story (D-04), with a parallel technical-tips track
covering the fully-folded PWA/user-manual scope (D-05/D-06). Claude drafts EN
from actual app behavior; Sapir reviews later for clinical accuracy and tone
(D-12). HE/DE/CS are deferred.

Purpose: Give the follow-up build phase ready-to-key EN copy so it implements
the Help page / welcome / tour without re-deriving the content architecture,
and give Sapir a single accuracy-reviewable artifact.

Output: `.planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md`.
NO production code, NO i18n-file edits — copy is presented as a key→EN-string
table/outline that the build phase will transcribe into `assets/i18n-en.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-in-app-onboarding-overview-help-system/26-CONTEXT.md
@.planning/phases/26-in-app-onboarding-overview-help-system/26-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Behavior sources the EN copy must accurately describe, and the i18n-key pattern it must follow. Read for accuracy; DO NOT modify any source file this phase. -->

i18n key pattern — `assets/i18n-en.js` + `assets/i18n.js`: dot-notation keys, EN object filled first.
  Author all help copy under `help.*` namespaces: `help.welcome.*`, `help.spine.*` (per spine step),
  `help.tour.*` (one per tour step — must align 1:1 with the 26-01 tour-step list), `help.tech.*`
  (technical-tips track subsections). Present as a key → EN-string outline, NOT an i18n.js edit.

Actual app behavior the copy must match (read these modules to verify claims — do NOT modify):
  - `assets/add-client.js` / `add-client.html` — inline-create vs full add; client types
    (Adult/Child/Animal/Other per Phase 19 i18n keys); photo (energetic-connection framing).
  - `assets/add-session.js` / `add-session.html` — start-session entry points; pre-session
    context card (Phase 22); emotions quick-paste; the customizable/editable session sections
    on/off + editable section titles (Phase 22 — the D-04 flagship); multi-issue list.
  - Heart Shield model — session-level field, removal tracking, status computed from session
    scan (Phase 9 model; STATE.md "Heart Shield tracking moved to session-level").
  - Severity — before/after 0–10 per issue, multi-issue, reversal, read mode.
  - Per-session export / PDF export — Phase 22 per-session document export
    (`pdf-export.js` wired Phase 22-09).
  - Backups & restore — Phase 25 final shape (Export/Import/encrypted backup, cloud-icon
    recency signal); restore = replace-all.
  - PWA / offline / activation — `sw.js` precache; "data never leaves the device" core value
    (PROJECT.md); 2-device activation limit + deactivate/transfer (Phase 18/19 license model);
    folded TODO `.planning/todos/pending/2026-03-24-pwa-install-guidance-and-user-manual.md`.

Hebrew-aware authoring constraint — Phase 24 D-05 (memory-locked, project-wide): Hebrew uses
  noun/infinitive forms, NO imperatives. Author EN headings/labels in noun/gerund form
  ("Adding a client", "Capturing emotions") so the later HE translation phase is not boxed
  into rule-violating imperatives (RESEARCH Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the workflow-spine content + flagship personalization story (EN, i18n-key-mapped)</name>
  <read_first>
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md (if it exists — extend, do not clobber)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTEXT.md (D-03 spine order, D-04 flagship + named must-haves, D-07 new-trial-user lens, D-12 authorship, D-13 demo independence)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-RESEARCH.md ("Content Architecture", "Six Help Patterns" tone synthesis, "i18n + RTL Constraints for Help/Tour Copy", Pitfall 5)
    - .planning/PROJECT.md (core value, audience: solo Emotion Code/Body Code therapists, mostly non-technical, Hebrew-primary)
    - assets/add-session.js and add-session.html (verify the editable/on-off session sections + emotions quick-paste + multi-issue behavior the copy describes)
  </read_first>
  <action>
    Create `.planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md`
    with: a short provenance header (D-12: Claude-drafted EN from app behavior, Sapir reviews
    for clinical accuracy + tone; D-13: independent of the marketing demo) and a "How to use
    this outline" note (every entry is `i18n-key → EN string`, EN-first, HE/DE/CS deferred).

    Author the **workflow spine** (D-03) as ordered sections, EACH mapped to `help.spine.*`
    i18n keys, in exactly this order, teaching each feature where it is used:
    1. Adding a client — inline-create vs. full add; client types (Adult/Child/Animal/Other);
       photo (energetic-connection framing for remote work).
    2. Starting a session — entry points (client card "New session" vs. Add Session dropdown);
       the pre-session context card.
    3. Capturing emotions — quick-paste (lead with the speed story — D-04 named must-have).
    4. The Heart Shield workflow — session-level field, removal tracking, status-from-scan.
    5. Severity tracking — before/after 0–10 per issue, multi-issue, reversal (call out the
       non-obvious one), read mode.
    6. Finishing: review & export — per-session export framed as "send to your client" /
       "file in your own records" (D-04 named must-have); read mode.
    7. Back to overview — KPIs, search/filters, what the dashboard tells you.

    Author the **flagship personalization story** as a prominent, EARLY section (NOT a footnote):
    **Customizable session sections** — turning text boxes/sections on/off and editable section
    titles to match each therapist's own workflow, framed as "make Sessions Garden yours"
    (D-04). Mark it explicitly as the lead differentiator in the Help page IA.

    Voice (D-14): warm/calm (Calm-like) + clinician-domain-fit (SimplePractice-like);
    explicitly NOT generic SaaS product-tour energy; non-technical (Sapir is the reference
    user). Hebrew-aware: noun/gerund headings ("Adding a client", "Capturing emotions"),
    no imperative-only constructions (Pitfall 5) — add a one-line note flagging this for the
    later HE phase.

    Every feature claim must match actual shipped behavior (verified against the modules in
    `<read_first>`). Also author the `help.welcome.*` keys (warm greeting + 1–2 benefit
    sentences + the two CTA labels) and the `help.tour.*` keys — ONE per tour step, aligned
    1:1 with the workflow-spine steps so plan 26-01's tour-step list maps cleanly. NO scope
    reduction: deliver full copy for every spine step and the flagship section.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -q "help.spine" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -q "help.welcome" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -q "help.tour" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "customiz(e|able) session section|make .*yours" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "per-session export|send to your client|file in your own records" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "heart shield" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "reversal" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md</automated>
  </verify>
  <acceptance_criteria>
    - 26-CONTENT-OUTLINE-EN.md exists with a D-12 provenance header and an `i18n-key → EN string` format note.
    - The workflow spine appears as ordered sections in the exact D-03 order (add client → start session → capture emotions/quick-paste → Heart Shield → severity incl. reversal → finish/per-session export → overview), each mapped to `help.spine.*` keys.
    - A prominent early "Customizable session sections / make it yours" section exists, explicitly marked as the lead personalization differentiator (D-04).
    - Per-session export is framed as "send to your client" / "file in your own records" (D-04).
    - `help.welcome.*` keys (greeting + benefit sentences + two CTA labels) and `help.tour.*` keys (one per spine step) are present and align 1:1 with the spine.
    - Headings use noun/gerund form with an explicit note flagging the Hebrew noun/infinitive constraint for the later translation phase (Pitfall 5).
    - Every feature claim is consistent with the shipped behavior of the modules read; no claimed feature that does not exist.
    - No production source / i18n / HTML file is created or modified.
  </acceptance_criteria>
  <done>The workflow-spine + flagship-personalization EN copy is authored, i18n-key-mapped, behavior-accurate, Hebrew-aware, and aligned to the 26-01 tour-step list.</done>
</task>

<task type="auto">
  <name>Task 2: Author the technical-tips track including the fully-folded PWA/user-manual content (D-05/D-06)</name>
  <read_first>
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md (the file Task 1 created — extend it)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTEXT.md (D-05, D-06, Folded Todos section)
    - .planning/phases/26-in-app-onboarding-overview-help-system/26-RESEARCH.md ("PWA-Install / Offline / Activation Guidance (D-06)", Pitfall 4, the per-browser install facts incl. iOS Safari no-beforeinstallprompt)
    - .planning/todos/pending/2026-03-24-pwa-install-guidance-and-user-manual.md (the full folded scope)
    - .planning/PROJECT.md ("data never leaves the device" core value — the emotional anchor of the technical track)
  </read_first>
  <action>
    Append a **Technical-Tips Track** to `26-CONTENT-OUTLINE-EN.md`, mapped to `help.tech.*`
    i18n keys, as a clearly-separated parallel track to the workflow spine (SimplePractice
    separates "use the product" from "set up / admin"). It MUST comprehensively cover, each
    as its own subsection with full EN copy:

    - **Backups & restore** — why it matters (local-only ⇒ the user is the only backup),
      Export / Import / encrypted-backup flow (Phase 25 final shape), the cloud-icon recency
      signal, restore = replace-all. (Also satisfies the Phase 25 D-26 deferred help concern.)
    - **PDF / per-session export (technical view)** — the Phase 22 per-session document
      export, when/why to use it (cross-link the spine "finish" section, do not duplicate).
    - **PWA install — per browser** (the hard one): separate illustrated step copy for
      Chrome/Edge desktop (address-bar install icon), **Safari iOS (Share sheet → Add to
      Home Screen — there is NO programmatic prompt; teach the manual flow visually)**,
      Android Chrome/Firefox/Samsung. NO single universal "Install" CTA copy (Pitfall 4).
    - **"Data never leaves your browser" — the offline explainer** — frame as the emotional
      anchor for this privacy-first audience (core value, not a footnote); pair directly with
      "that's why backups matter".
    - **The 2-device activation limit + deactivate / transfer** — plain-language: what
      "activation" means, the 2-device limit, deactivate-before-transfer, how to move to a
      new computer/browser. Use correct browser/endpoint terminology (the folded TODO + the
      `device-browser-terminology-fix` pending todo flag this exact wording sensitivity). The
      copy must NOT instruct users in a way that weakens the 2-device activation or
      local-only security model (e.g., no "share your key" / no "bypass activation" framing).
    - **Troubleshooting** — a decision-tree-style section: cleared cache → data still in
      IndexedDB vs. truly lost; re-activation; "I don't see my clients" path; lost-passphrase
      = unrecoverable (Phase 19 encrypted-backup reality).

    Same voice + Hebrew-aware authoring rules as Task 1. Every claim must match shipped
    behavior. Fold the PWA/user-manual scope FULLY (D-06) — do not stub or partially cover
    any of the seven topics above; "fold it fully" supersedes any earlier triage.
  </action>
  <verify>
    <automated>grep -q "help.tech" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "Add to Home Screen|Share sheet" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "Safari|iOS" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "2.device|two.device|activation" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "deactivat|transfer" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "never leaves|local.only|offline" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "troubleshoot|cleared cache" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md && grep -Eqi "backup|restore" .planning/phases/26-in-app-onboarding-overview-help-system/26-CONTENT-OUTLINE-EN.md</automated>
  </verify>
  <acceptance_criteria>
    - 26-CONTENT-OUTLINE-EN.md contains a Technical-Tips Track mapped to `help.tech.*`, separated from the spine.
    - It covers ALL of: backups & restore; PDF/per-session export (technical view); per-browser PWA install (Chrome/Edge desktop, Safari iOS Share→Add to Home Screen, Android) with NO single universal install CTA; the "data never leaves your browser" offline explainer as the emotional anchor; the 2-device activation limit; deactivate/transfer; and a troubleshooting decision tree.
    - The activation/transfer copy does not instruct users in a way that weakens the 2-device or local-only security model (no key-sharing / bypass framing); lost-passphrase = unrecoverable is stated.
    - iOS Safari is described with the manual Share→Add-to-Home-Screen flow and no promise of a one-tap install (Pitfall 4).
    - Voice and Hebrew-aware constraints from Task 1 hold; claims match shipped behavior.
    - No production source / i18n / HTML file is created or modified.
  </acceptance_criteria>
  <done>The technical-tips track fully folds the PWA/user-manual scope (all seven topics) as i18n-key-mapped EN copy that does not weaken the security model; zero production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help content → user's security behavior | What the activation/transfer/offline copy tells a non-technical therapist directly shapes whether they preserve the 2-device + local-only security model |
| content outline → follow-up build phase | The EN copy is transcribed verbatim into shipped i18n; an unsafe instruction becomes shipped guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Information Disclosure | the EN content outline itself | mitigate | Contains NO real client data, NO license keys, NO secrets — only generic instructional copy; an explicit acceptance criterion forbids embedding any real identifier or key |
| T-26-06 | Elevation of Privilege | activation / deactivate-transfer help copy (D-06) | mitigate | Copy is authored to reinforce the 2-device + local-only model (plain-language explanation of the limit and deactivate-before-transfer) and is explicitly forbidden from suggesting key-sharing or activation-bypass workarounds |
| T-26-07 | Tampering | inaccurate copy misleading users into data loss | mitigate | Every claim verified against shipped behavior; troubleshooting states lost-passphrase = unrecoverable and restore = replace-all so users are not misled into destructive actions |
| T-26-08 | Repudiation | n/a (local-only single-user app, no audit/server surface) | accept | Residual risk LOW — no multi-user/server/audit surface exists |

Residual risk for this plan is LOW: it produces a Markdown content outline only. The
security-relevant surface is the *correctness and safety of the guidance copy*
(T-26-06/T-26-07), which the acceptance criteria bind explicitly.
</threat_model>

<verification>
- `26-CONTENT-OUTLINE-EN.md` exists with the D-12 provenance header.
- The workflow spine follows D-03 order exactly; the flagship personalization story leads; per-session export is framed per D-04.
- The technical-tips track fully folds all seven D-05/D-06 topics; no universal install CTA; security model not weakened.
- All copy is presented as `help.*` i18n-key → EN-string; `help.tour.*` aligns 1:1 with the 26-01 tour-step list.
- No file under `assets/`, no `*.html`, no `sw.js`, no i18n file created or modified.
- No scope-reduction language present.
</verification>

<success_criteria>
A new trial therapist reading the EN content outline understands how to run a
session end-to-end and discovers every major feature where it is used; the
flagship personalization story and per-session export lead; the technical track
comprehensively and safely covers the fully-folded PWA/user-manual scope; all
copy is i18n-key-ready and Hebrew-aware; every claim matches shipped behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/26-in-app-onboarding-overview-help-system/26-02-SUMMARY.md`
</output>
