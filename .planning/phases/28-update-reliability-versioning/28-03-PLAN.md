---
phase: 28-update-reliability-versioning
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - index.html
  - add-session.html
  - add-client.html
  - reporting.html
  - sessions.html
  - settings.html
  - license.html
  - demo.html
  - disclaimer.html
  - disclaimer-en.html
  - disclaimer-he.html
  - disclaimer-cs.html
  - impressum.html
  - impressum-en.html
  - impressum-he.html
  - impressum-cs.html
  - datenschutz.html
  - datenschutz-en.html
  - datenschutz-he.html
  - datenschutz-cs.html
  - landing.html
autonomous: true
requirements: [VER-02, VER-04]
must_haves:
  truths:
    - "The per-page <meta http-equiv=\"Content-Security-Policy\"> tag is deleted from all 21 HTML pages (CSP now comes only from the _headers HTTP header verified in Plan 02 — one source, no drift)"
    - "The 20 SW-registered app pages load <script src=\"./assets/version.js\"></script> ABOVE shared-chrome.js so the footer and the integrity check can read the version constant"
  artifacts:
    - path: "index.html"
      provides: "version.js script tag wired, CSP meta removed"
      contains: "assets/version.js"
  key_links:
    - from: "the 20 app pages"
      to: "assets/version.js"
      via: "<script src=\"./assets/version.js\"> placed before shared-chrome.js"
      pattern: "assets/version\\.js"
    - from: "all 21 pages"
      to: "_headers CSP"
      via: "per-page <meta> CSP deleted; the single HTTP header (Plan 02) is now the only CSP source"
      pattern: "http-equiv=\"Content-Security-Policy\""
  prohibitions:
    - "Do NOT delete the CSP <meta> until Plan 02's _headers CSP is verified equivalent (this plan depends_on 28-02 for exactly that gate)"
    - "Do NOT add version.js to landing.html's footer/integrity path — landing has no SW registration and renders its own footer; the 20 app pages are the version.js consumers"
    - "Do NOT edit the duplicated inline SW-registration <script> in the 20 app pages — the launch-update logic (D-06) lives in app.js, not in the inline registration snippet"
---

<objective>
Converge all 21 HTML pages onto the single CSP source and wire the version constant into the app
pages. (1) Delete the per-page `<meta http-equiv="Content-Security-Policy">` tag from ALL 21 pages
now that the `_headers` HTTP CSP header is verified equivalent (Plan 02) — one CSP source, no drift
(VER-04 deletion side). (2) Inject `<script src="./assets/version.js"></script>` ABOVE `shared-chrome.js`
on the 20 SW-registered app pages so the footer label (Plan 04) and the integrity check (Plan 04) can
read the source-of-truth constant (VER-02 wiring side).

Purpose: This is the single-owner HTML-pages plan — both the CSP-meta deletion and the version.js
script injection touch the same set of files, so they are done together to avoid concurrent edits.
It is gated on Plan 01 (version.js must exist) and Plan 02 (CSP header must be verified equivalent
before the meta can be safely deleted).
Output: 21 HTML pages with the CSP meta removed; 20 app pages with version.js wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-update-reliability-versioning/28-CONTEXT.md
@.planning/phases/28-update-reliability-versioning/28-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols created in THIS plan (downstream drift checks must EXCLUDE these):

- A `<script src="./assets/version.js"></script>` tag on each of the 20 app pages (NEW per-page wiring)

This plan creates no new JS symbols; it consumes `assets/version.js` (Plan 01) and the verified `_headers` CSP (Plan 02). The footer marker, integrity check, and nudge that actually USE version.js are built in Plan 04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the per-page CSP <meta> from all 21 HTML pages</name>
  <files>index.html, add-session.html, add-client.html, reporting.html, sessions.html, settings.html, license.html, demo.html, disclaimer.html, disclaimer-en.html, disclaimer-he.html, disclaimer-cs.html, impressum.html, impressum-en.html, impressum-he.html, impressum-cs.html, datenschutz.html, datenschutz-en.html, datenschutz-he.html, datenschutz-cs.html, landing.html</files>
  <read_first>
    - index.html (line 23 — the exact CSP meta tag to delete)
    - .planning/phases/28-update-reliability-versioning/28-02-SUMMARY.md (Plan 02's "SAFE TO DELETE" sign-off — confirm the _headers CSP is verified equivalent before deleting)
    - _headers (confirm the Content-Security-Policy header is present, per Plan 02)
    - landing.html (line 12 — the CSP meta, identical content)
    - .planning/phases/28-update-reliability-versioning/28-PATTERNS.md (the "21 *.html pages (MODIFIED)" section)
  </read_first>
  <action>
    First confirm Plan 02's `_headers` CSP header exists and was signed off as byte-equivalent (read the 28-02-SUMMARY). Then delete the single line `<meta http-equiv="Content-Security-Policy" content="...">` from EACH of the 21 HTML pages (the 20 app pages + landing.html). The content is verified byte-identical across all pages, so the deletion is mechanical. Remove the whole `<meta>` line including its leading indentation; do not leave a blank artifact line if the surrounding markup convention is no blank line there (match each file's existing head spacing). Do not touch any other `<meta>` tag (viewport, charset, theme-color, etc.). The 21 pages are: index, add-session, add-client, reporting, sessions, settings, license, demo, disclaimer(+en/he/cs), impressum(+en/he/cs), datenschutz(+en/he/cs), landing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && CNT=$(grep -l 'http-equiv="Content-Security-Policy"' *.html 2>/dev/null | wc -l | tr -d ' '); if [ "$CNT" != "0" ]; then echo "FAIL: $CNT pages still carry a CSP meta:"; grep -l 'http-equiv="Content-Security-Policy"' *.html; exit 1; fi; HCNT=$(node -e "const fs=require('fs');console.log(/Content-Security-Policy:/.test(fs.readFileSync('_headers','utf8'))?'1':'0')"); if [ "$HCNT" != "1" ]; then echo "FAIL: _headers CSP missing — meta must not be deleted before header exists"; exit 1; fi; echo "OK: 0 pages carry CSP meta; _headers CSP present"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `grep -l 'http-equiv="Content-Security-Policy"' *.html | wc -l` returns 0 (no page carries the CSP meta)
    - Source: `_headers` still contains the `Content-Security-Policy:` header (the deletion did not orphan the CSP — Plan 02 gate honored)
    - Source: other `<meta>` tags (charset, viewport, theme-color) are untouched on a spot-checked page (index.html)
    - All 21 pages edited (20 app + landing)
  </acceptance_criteria>
  <done>The CSP <meta> is gone from all 21 pages; the only CSP source is the verified _headers HTTP header (VER-04 fully closed).</done>
</task>

<task type="auto">
  <name>Task 2: Inject version.js <script> above shared-chrome.js on the 20 app pages</name>
  <files>index.html, add-session.html, add-client.html, reporting.html, sessions.html, settings.html, license.html, demo.html, disclaimer.html, disclaimer-en.html, disclaimer-he.html, disclaimer-cs.html, impressum.html, impressum-en.html, impressum-he.html, impressum-cs.html, datenschutz.html, datenschutz-en.html, datenschutz-he.html, datenschutz-cs.html</files>
  <read_first>
    - index.html (lines 329-345 — the load-order-sensitive script block; shared-chrome.js is at ~line 338; the inline SW-registration <script> follows)
    - assets/version.js (Plan 01 — confirm the global it exposes; it must load before shared-chrome.js reads it)
    - .planning/phases/28-update-reliability-versioning/28-PATTERNS.md (the "assets/version.js (NEW)" — "Where it loads in the page" — and the "21 *.html pages" sections)
  </read_first>
  <action>
    On each of the 20 SW-registered app pages (NOT landing.html — it has no SW registration and renders its own footer via landing.js), add `<script src="./assets/version.js"></script>` on its own line IMMEDIATELY BEFORE the existing `<script src="./assets/shared-chrome.js"></script>` tag. Load order is load-bearing: version.js must define its global before shared-chrome.js (footer) and before the integrity check (Plan 04) read it. Match the existing per-page indentation of the script block. Do NOT modify the duplicated inline SW-registration `<script>` snippet — the D-06 launch/visibilitychange `registration.update()` lives in app.js (already loaded everywhere), so no inline-registration edit is needed across 20 pages. The 20 app pages are every *.html EXCEPT landing.html. Confirm via grep that exactly the SW-registered pages get the tag.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && FAIL=0; for f in $(grep -l "serviceWorker.register" *.html); do node -e "const fs=require('fs');const h=fs.readFileSync('$f','utf8');const vi=h.indexOf('assets/version.js');const si=h.indexOf('assets/shared-chrome.js');if(vi===-1){console.log('FAIL '+'$f'+': no version.js script');process.exit(1)}if(si!==-1&&vi>si){console.log('FAIL '+'$f'+': version.js loads AFTER shared-chrome.js');process.exit(1)}" || FAIL=1; done; if [ "$FAIL" != "0" ]; then exit 1; fi; if grep -q "assets/version.js" landing.html; then echo "FAIL: landing.html should NOT load version.js"; exit 1; fi; echo "OK: 20 app pages load version.js before shared-chrome.js; landing untouched"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: every page containing `serviceWorker.register` also contains `assets/version.js`, and the version.js `<script>` appears BEFORE the shared-chrome.js `<script>` (load order verified by the automated check)
    - Source: `landing.html` does NOT contain a `version.js` script tag (it has no SW and renders its own footer)
    - CLI: `grep -l "assets/version.js" *.html | wc -l` returns 20
    - Source: the inline SW-registration snippet is unchanged on a spot-checked page (index.html)
  </acceptance_criteria>
  <done>All 20 app pages load version.js before shared-chrome.js; landing.html is correctly excluded; no inline SW-registration snippet was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML head → browser CSP | After this plan, CSP comes ONLY from the HTTP header; a missing/incorrect header would leave pages with NO CSP |
| script load order | version.js must define its global before consumers (footer/integrity) read it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Elevation (XSS) | CSP meta deletion | mitigate | Deletion is gated on Plan 02's verified-equivalent header (depends_on 28-02 + the SUMMARY sign-off + the automated check that re-confirms the header exists before/while deleting); CSP is never absent — it just moves source |
| T-28-09 | Tampering | script load order | mitigate | Automated check asserts version.js loads before shared-chrome.js on every app page; wrong order would only make the footer read undefined (handled defensively in Plan 04), not a security issue |
| T-28-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — zero-build project; nothing to verify |

No high-severity threat is left unmitigated.
</threat_model>

<verification>
- 0 HTML pages carry the CSP `<meta>` (Task 1 CLI check), and `_headers` still has the header.
- 20 app pages load version.js before shared-chrome.js; landing.html excluded (Task 2 automated check).
- This plan must run AFTER Plan 02 (CSP header verified) and Plan 01 (version.js exists) — enforced by depends_on.
</verification>

<success_criteria>
- Single CSP source: the HTTP header only; all 21 `<meta>` CSP tags removed (VER-04 closed).
- version.js wired into the 20 app pages in correct load order (VER-02 wiring — footer + integrity can now read it).
- landing.html correctly excluded from version.js wiring.
</success_criteria>

<output>
Create `.planning/phases/28-update-reliability-versioning/28-03-SUMMARY.md` when done. Record: confirmation of 0 CSP metas remaining, the 20-page version.js wiring list, and confirmation landing.html was excluded.
</output>
