---
phase: 46.1-preview-edit-experience-redesign
plan: 07
subsystem: verification
tags: [device-gate, uat, human-verify, webkit-probe, rtl, pdf]
requires:
  - "46.1-01 (RED test scaffold + WebKit probe set E)"
  - "46.1-02 (current-state Edit/Preview switcher)"
  - "46.1-03 (in-place swap + Frame, MdRender sole sink)"
  - "46.1-04 (Frame treatment + PREVIEW chip + switcher CSS)"
  - "46.1-05 (four-locale copy + add-session defaults)"
  - "46.1-06 (EN changelog + help topics — docs hard-gate)"
provides:
  - "Ben's real-device ratification of the preview/edit redesign (MacBook installed-Safari PWA + iPhone Safari, LTR + Hebrew RTL) — the locked-process step-3 device gate, closed 'approved' 2026-07-20 on pre-prod build 3d5cbaf"
  - "Re-verification of the carried Phase-46 export items (46-16 items 2, 11-layout, 12, 13) under the new design"
  - "RTXT-01/RTXT-04/RTXT-05 confirmed on real hardware (nested lists render in preview AND real PDF, Hebrew RTL intact)"
affects: []
tech-stack:
  added: []
  patterns:
    - "Device gate held open across four UAT fix rounds; every fix re-verified live on the pre-prod origin (sg-prpr) before final approval"
    - "Behavior-contract-first rebuild for editor semantics: ratify an explicit clause list with Ben, then RED tests, then implementation (bold/italic toggle)"
key-files:
  created: []
  modified: []
decisions:
  - "Formatting bar is INERT while previewing (Ben decision B — 'dimmed means dead'); UI-SPEC Amendment appended, supersedes the mockup's return-and-apply behavior"
  - "Bold/italic toggle semantics ratified as an 11-clause behavior contract (46.1-BOLD-SEMANTICS-RESEARCH.md): D1 swap-on-collision / D2 empty-pair caret / D3 mixed→apply — Ben ratified 2026-07-19"
  - "Two set-C precondition REDs at 1440×820 ACCEPTED as harness vacuity (content-driven card fits the probe's 64-line document, so the edit area never overflows); documented in the probe header, Ben signed off — not defects"
  - "Strict markdown kept deliberately: hand-typed leading spaces before '## x' render literal in BOTH pipelines (accepted manual-edit sharp edge)"
  - "Hand-typed *** pipeline divergence filed as pending todo 2026-07-19-triple-star-breaks-pipeline-join-invariant.md (pre-existing, out of phase scope)"
metrics:
  duration: ~2 days (checkpoint gate open 2026-07-18 → 2026-07-20, four UAT fix rounds)
  completed: 2026-07-20
status: complete
---

# Phase 46.1 Plan 07: Real-Device Ratification Gate Summary

Ben ratified the redesigned preview/edit experience on real hardware — MacBook installed-Safari PWA + iPhone Safari, LTR + Hebrew RTL, real PDF with nested lists — closing the locked-process step-3 device gate with "approved" on 2026-07-20 against pre-prod build `3d5cbaf`, after four live-verified UAT fix rounds held inside the open checkpoint.

## Task 1 — Machine gate

- `node tests/run-all.js`: green throughout the gate — 191 tests at phase-46 close, **205 passed / 0 failed** at the final approved build (`3d5cbaf`), re-confirmed at SUMMARY time.
- `node tests/webkit/46-export-step2-layout.mjs`: extended **set E 12/12 GREEN** (in-place-swap preview visible on open at 1440×820 without scroll, pinned bar, editor hidden while previewing, RTL pass).
- **Accepted set-C condition:** the two set-C PRECONDITION checks report RED at 1440×820 — the content-driven card grows to fit the probe's 64-line test document, so the edit area never overflows and the non-vacuity guard cannot arm. Documented as a KNOWN-VACUOUS CONDITION in the probe header (commit `e1bd8b8`); Ben signed off. Harness limitation, not a product defect; set C stays armed at 1000×700.

## Task 2 — Device gate (checkpoint outcome)

- **2026-07-19:** conditional approval — all checklist items passed on device, but Ben demanded a docs content pass (help/changelog critique) before final sign-off. That pass landed as UAT round 4's docs commits.
- **2026-07-20:** final **"approved"** on pre-prod build `3d5cbaf` (sg-prpr origin), after the full how-to-verify checklist (note fields 1–4, export 5–9, both surfaces in Hebrew RTL, dark mode, real PDF with nested lists) plus re-verification of the carried Phase-46 items (46-16 items 2, 11-layout, 12, 13) under the new design.

## UAT fix rounds 1–3 (2026-07-18/19, commits `40a6a81..7009f2c`)

Defects Ben found live during the open gate, fixed and re-verified on pre-prod:

- PREVIEW chip gets the eye glyph on both surfaces (`40a6a81`); chip straddles the Frame border + export preview scrolls its body, not the Frame (`a12ad0b`).
- Indent/outdent dim + aria-disable while the caret is on a heading line (`8c4df8a`).
- Preview renders leading indentation on paragraph lines — faithful to PDF + copy (`1cc0dd3`).
- Discard-guard rebuilt (`d8b9e5a`): Back is silent + lossless, Continue with an unchanged selection is lossless, the discard dialog moves to the section-toggle moment; new `export.discard.bodySections` copy in all 4 locales.
- Export maximize button reverses to a restore glyph while maximized (`ea15296`).
- Undo/redo buttons dim when their history has nothing to step to (`842f170`); undo/redo scroll the changed text back into view in the export editor (`0381902`).
- Heading dropdown marks the active register with a checkmark + tint, not green text (`1e2f127`).
- Formatting bar made INERT while previewing (`db8aceb`) — Ben decision B, "dimmed means dead"; UI-SPEC Amendment appended, superseding the mockup's return-and-apply.
- Applying a heading register strips the line's leading indentation (`7009f2c`).

## UAT round 4 (2026-07-19/20, commits `c4982a8..3d5cbaf`)

- **Docs restructure per Ben's critique** (`c4982a8`, `c7487ef`, `6749ff7`, `3d5cbaf`): help corpus reorganized — section reframed as "Writing session notes", a dedicated searchable "Formatting session notes" topic, new `list` block type in the help renderer, he/de/cs parity stubs; the 1.4.0 changelog entry reworked concise.
- **Four fixed bugs:** stale Preview switcher on field change — every preview close path resets the bar UI (`e144ebf`); pure deletions ride `execCommand('delete')` instead of an empty insertText, fixing the inescapable list-exit — WebKit-probe-proven (`e3a1460`, probe `tests/webkit/46.1-list-exit-deletion.mjs`); leave-page app dialog extended to all 7 same-tab nav triggers on add-session (`3374dfa`); note-heading registers get a real on-screen size hierarchy 22/18/16px (`73c01f9`).
- **Bold/italic toggle rebuilt to a ratified behavior contract** (`042afc5..eaea002`, +3 test files): the naive wrapper replaced by a state-based toggle operating on real grammar spans, per the 11-clause contract in `46.1-BOLD-SEMANTICS-RESEARCH.md` — decisions D1 (swap on collision), D2 (empty-pair caret), D3 (mixed→apply) ratified by Ben 2026-07-19. RED contract suite first (`042afc5`), implementation (`40d8fa8`), no-op outcomes skip editInsert but honor the caret move (`989ae8c`), sweep + fuzz-involution hardening (`eaea002`).

## Accepted limitations (carried, signed off)

- **Set-C harness vacuity at 1440×820** — see Task 1; probe-header documented, not a defect.
- **Manual-edit sharp edge:** hand-typed leading spaces before `## x` render literal in BOTH pipelines (preview and PDF agree); strict markdown kept deliberately.
- **Hand-typed `***` divergence** between pipelines: pre-existing, out of phase scope; filed as pending todo `2026-07-19-triple-star-breaks-pipeline-join-invariant.md`.

## Deviations from Plan

- The plan routed any device-gate defects to a formal gap-closure round (`/gsd-plan-phase --gaps`). Instead, defects were fixed inline across four UAT rounds while the checkpoint stayed open, each round re-deployed to pre-prod and re-verified live by Ben before the final "approved". Same rigor (RED-first tests where behavior changed, WebKit probe for geometry claims), tighter loop; no separate gap round was needed.
- The gate's fix rounds modified product source (this plan itself was scoped verification-only); those changes carry their own conventional commits listed above and were absorbed into the gate's re-verification. This SUMMARY's own commits are docs-only.

## Comment Hygiene

No planning IDs in shipped code from this plan (verification-only; the UAT fix commits were swept per CONVENTIONS.md §Comments).

## Known Stubs

None introduced by this plan. The he/de/cs help parity stubs from round 4 are deliberate (EN is the corpus of record; locale passes follow later, per the docs-gate contract).

## Verification

- `node tests/run-all.js` — 205 passed, 0 failed (re-run at SUMMARY time on `3d5cbaf`).
- WebKit probe set E — 12/12 GREEN on the approved build (set-C 1440×820 vacuity accepted as above).
- Ben's "approved" — 2026-07-20, pre-prod build `3d5cbaf`.

## Self-Check: PASSED

- tests/webkit/46-export-step2-layout.mjs — FOUND (vacuity note in header)
- .planning/phases/46.1-preview-edit-experience-redesign/46.1-BOLD-SEMANTICS-RESEARCH.md — FOUND
- .planning/todos/pending/2026-07-19-triple-star-breaks-pipeline-join-invariant.md — FOUND
- Commits `40a6a81`, `7009f2c`, `c4982a8`, `db8aceb`, `e3a1460`, `eaea002`, `3d5cbaf` — FOUND in `git log`
- Suite 205/205 — re-executed, green
