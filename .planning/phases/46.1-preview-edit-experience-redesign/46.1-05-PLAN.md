---
phase: 46.1-preview-edit-experience-redesign
plan: 05
type: execute
wave: 2
depends_on: ["46.1-01"]
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - add-session.html
autonomous: true
requirements: [RTXT-01, RTXT-04]

must_haves:
  truths:
    - "Every new/changed string ships in all four locales (EN authored as corpus of record + HE/DE/CS parity, full RTL for Hebrew): the PREVIEW chip, the reconciled empty-state BODY (title already correct), the reconciled discard-dialog copy, the reconciled export ephemeral note + Step-2 helper, and the reconciled steps-1->2 'Continue' CTA."
    - "The steps-1->2 primary CTA reads 'Continue' in all four locales (`export.next1` AND `export.next2` reconciled from 'Next: Edit document' / 'Next: Get document'); the step-3 CTA (`export.done`) stays 'Done' per Ben's ratified decision."
    - "The Edit/Preview switcher segment labels resolve to the shipped `toolbar.backToEdit` (\"Edit\") and `toolbar.preview` (\"Preview\") keys; the new `toolbar.previewChip` key is \"PREVIEW\"."
    - "The discard-dialog copy matches the UI-SPEC (EN source of truth): title \"Discard your export edits?\", body \"These changes apply to this export only and won't be saved.\", confirm \"Discard\", cancel \"Keep editing\" — under the SAME shipped keys the confirm dialog already reads (export.discard.title/body/yes/no)."
    - "The export ephemeral-edit note reads the UI-SPEC copy (\"Edits here apply to this export only. Restructure freely — nothing is saved back to the session.\") and the Step-2 helper references the Edit / Preview switch, not a single \"Preview button\"; the HTML default text and the i18n value agree."
    - "No four-key parity gap: every key added/changed in EN exists in HE, DE, and CS; orphaned keys from the removed target-state toggle are cleaned in all four locales."
  artifacts:
    - assets/i18n-en.js
    - assets/i18n-he.js
    - assets/i18n-de.js
    - assets/i18n-cs.js
    - add-session.html
  key_links:
    - "EN is the corpus of record for the docs gate; a locale-only edit does NOT satisfy a docs demand. i18n-*.js and add-session.html copy changes are covered by the docs gate (plan 06) via changelog + topic-quick-paste/review-export help."
    - "The JS switcher (plan 02) and chip (plan 03) read these keys via t() with fallbacks, so a missing locale value degrades to the fallback rather than breaking."
  prohibitions:
    - statement: "A new/changed string ships EN-only (or a locale-only edit) breaking four-locale parity or the RTL contract."
      status: resolved
      verification: "For every EN key touched, the same key exists with a translated value in i18n-he.js, i18n-de.js, i18n-cs.js (parity grep); Hebrew values are RTL-correct."
---

<objective>
Author the copy contract for the redesigned surfaces in all four locales and reconcile the shipped
strings the redesign changes: the PREVIEW chip, the switcher segment labels, the empty-state copy, the
discard-dialog copy (shipped keys carry new UI-SPEC values), and the export ephemeral note + Step-2 helper.
EN is authored as the corpus of record; HE/DE/CS ship in-phase for parity (the locale pass is in scope,
not deferred), with full RTL correctness for Hebrew.

Purpose: the words. File-disjoint from the JS (02/03) and CSS (04) plans — runs in parallel (wave 2).
i18n dictionaries are CHANGELOG-ONLY plumbing; add-session.html copy is a TRIGGER file whose help
coverage lands in plan 06.
Output: reconciled + new keys in `assets/i18n-en.js` (+ he/de/cs) and updated default copy in
`add-session.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md
@assets/i18n-en.js
@add-session.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN corpus — new + reconciled keys; add-session.html default copy</name>
  <files>assets/i18n-en.js, add-session.html</files>
  <read_first>
    - assets/i18n-en.js (the export + toolbar block ~488-545: `export.next1` ~506 "Next: Edit document", `export.next2` ~507 "Next: Get document", `export.done` ~508 "Done"; `export.discard.title/body/yes/no` ~516-519 currently "Discard your edits?"/"Your changes to the document will be lost."/"Yes, discard"/"Keep editing"; `export.ephemeralNote` ~502; `export.step2.helper` ~501; `toolbar.preview` ~537, `toolbar.backToEdit` ~539, `toolbar.hidePreview` ~538, `toolbar.previewEmptyTitle` ~540 ALREADY "Nothing to preview yet", `toolbar.previewEmptyBody` ~541)
    - add-session.html (export Step-2 markup ~456-467: `export.step2.helper` default text ~461, `export.ephemeralNote` default text ~462)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Copywriting Contract — the EN copy table, source of truth)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Open Question 3 — discard copy reconcile; Assumptions Log)
  </read_first>
  <action>
    Update assets/i18n-en.js (corpus of record) to the UI-SPEC §Copywriting copy:
    - ADD `toolbar.previewChip`: "PREVIEW" (the floating Frame chip, uppercase — read by the plan-03 chip).
    - RECONCILE the empty-state BODY only: `toolbar.previewEmptyTitle` is ALREADY "Nothing to preview yet"
      (i18n-en.js:540) — leave it (do NOT re-set it; it is a no-op). Change `toolbar.previewEmptyBody` from
      "Start typing to see the formatted result." to "Start typing above." The shipped title+body split is a
      CONSCIOUS adaptation of the UI-SPEC's one-line "Nothing to preview yet — start typing above." (title
      "Nothing to preview yet" + body "Start typing above." reads as the spec line) — keep the split, note it
      here as an accepted adaptation.
    - RECONCILE the steps-1->2 CTA to "Continue" (Ben's decision): set BOTH `export.next1` AND `export.next2`
      to "Continue" (they are the step-1->2 and step-2->3 primary-CTA labels; export-modal.js reads them via
      App.t, so the button shows "Continue" with no JS change). LEAVE `export.done` = "Done" — the step-3 CTA
      stays "Done" per Ben's ratified decision (the format cards do the exporting; the footer button closes).
    - Switcher segment labels REUSE existing keys (no new keys): `toolbar.backToEdit` stays "Edit" (the edit
      segment), `toolbar.preview` stays "Preview" (the preview segment). Verify both read exactly those words.
    - REMOVE the now-orphaned `toolbar.hidePreview` key (the removed target-state toggle owned it).
    - RECONCILE the discard-dialog copy under the shipped keys the confirm dialog already reads:
      `export.discard.title` -> "Discard your export edits?"; `export.discard.body` -> "These changes apply
      to this export only and won't be saved."; `export.discard.yes` -> "Discard"; `export.discard.no` ->
      "Keep editing".
    - RECONCILE `export.ephemeralNote` -> "Edits here apply to this export only. Restructure freely —
      nothing is saved back to the session." and update its DEFAULT text in add-session.html (line ~462) to
      match.
    - RECONCILE `export.step2.helper` so it references the Edit / Preview switch rather than "the Preview
      button in the toolbar" (e.g. "Step 2 of 3 — Edit your document below; switch to Preview any time to
      see how it will look."), and update the add-session.html default text (line ~461) to match.
    Keep all other export/toolbar keys unchanged. Do not add planning-ID comments.
  </action>
  <verify>
    <automated>grep -q 'toolbar.previewChip' assets/i18n-en.js && grep -q 'Discard your export edits' assets/i18n-en.js && grep -q '"export.next1": "Continue"' assets/i18n-en.js && grep -q '"export.next2": "Continue"' assets/i18n-en.js && grep -q 'Restructure freely' add-session.html && node --check assets/i18n-en.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/i18n-en.js contains `toolbar.previewChip` = "PREVIEW"; `toolbar.hidePreview` is removed; `toolbar.backToEdit` = "Edit" and `toolbar.preview` = "Preview".
    - `export.next1` = "Continue" AND `export.next2` = "Continue"; `export.done` is UNCHANGED = "Done" (step-3 CTA stays "Done" per Ben's decision).
    - `export.discard.title` = "Discard your export edits?", `export.discard.body` = "These changes apply to this export only and won't be saved.", `export.discard.yes` = "Discard", `export.discard.no` = "Keep editing".
    - `export.ephemeralNote` contains "Restructure freely" and add-session.html's ephemeral-note default text matches it; `export.step2.helper` references switching to Preview (not "the Preview button").
    - `toolbar.previewEmptyTitle` is left = "Nothing to preview yet" (no-op); `toolbar.previewEmptyBody` = "Start typing above.".
    - Run-all wave-timing: do NOT gate on `node tests/run-all.js` exit-0 here — the four `46.1-*` scaffold tests stay RED until their owning plans land (02 → switcher/format; 03 → inplace-swap + full green; 04 → frame-no-surface-alt), and this plan delivers NONE of them. The i18n four-locale parity gates go green after Task 2 (locale parity), verified there. This EN-only task's machine gate is the greps above + a parse check.
  </acceptance_criteria>
  <done>The EN corpus carries the new chip key, reconciled empty-state + discard + ephemeral-note + Step-2 copy, and reused switcher labels; the add-session.html defaults match the EN values.</done>
</task>

<task type="auto">
  <name>Task 2: HE / DE / CS parity (RTL-correct Hebrew)</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (the final EN values from Task 1 — the source of record to translate)
    - assets/i18n-he.js (the export + toolbar block — mirror the EN keys; Hebrew is RTL; find `export.next1`, `export.next2`, `export.done`, `export.discard.*`, `export.ephemeralNote`, `export.step2.helper`, `toolbar.preview`, `toolbar.backToEdit`, `toolbar.hidePreview`, `toolbar.previewEmpty*`)
    - tests/25-11-i18n-parity.test.js, tests/40-i18n-parity.test.js, tests/42-i18n-parity.test.js (the four-locale key-parity gates this task must keep/turn green — every EN key touched must exist in HE/DE/CS and vice versa)
    - assets/i18n-de.js (same keys, German)
    - assets/i18n-cs.js (same keys, Czech)
  </read_first>
  <action>
    Mirror every key added/changed in Task 1 into assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js
    (four-locale parity is in-phase scope, not deferred):
    - ADD `toolbar.previewChip` in each locale (a short uppercase-style "preview" label appropriate to the
      language; Hebrew RTL).
    - RECONCILE the steps-1->2 CTA: set BOTH `export.next1` AND `export.next2` to the native "Continue" word
      in each language; LEAVE `export.done` as the native "Done" word (step-3 CTA stays "Done" per Ben's
      decision — do not change it).
    - RECONCILE `toolbar.previewEmptyTitle`/`Body` to the "Nothing to preview yet / Start typing above."
      meaning in each language (the title+body split mirrors EN's accepted adaptation of the one-line spec).
    - RECONCILE `export.discard.title/body/yes/no` to the UI-SPEC meaning ("Discard your export edits?" /
      "these changes apply to this export only and won't be saved" / "Discard" / "Keep editing") in each
      language.
    - RECONCILE `export.ephemeralNote` and `export.step2.helper` to the new EN meaning in each language.
    - REMOVE the orphaned `toolbar.hidePreview` key in each locale (parity with EN).
    - Ensure `toolbar.preview` and `toolbar.backToEdit` read the correct native words for the two switcher
      segments in each locale.
    Verify Hebrew renders RTL-correctly (no LTR-only punctuation artifacts); keep native, natural phrasing
    (not machine-literal). No planning-ID comments.
  </action>
  <verify>
    <automated>for f in he de cs; do grep -q 'toolbar.previewChip' assets/i18n-$f.js || exit 1; grep -q 'toolbar.hidePreview' assets/i18n-$f.js && exit 1; done; node tests/25-11-i18n-parity.test.js && node tests/40-i18n-parity.test.js && node tests/42-i18n-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `toolbar.previewChip` exists in i18n-he.js, i18n-de.js, i18n-cs.js.
    - `toolbar.hidePreview` is absent from all four locale files (parity with the EN removal).
    - `export.next1` and `export.next2` carry the native "Continue" word in HE/DE/CS; `export.done` is unchanged in each.
    - The reconciled `export.discard.*`, `export.ephemeralNote`, `export.step2.helper`, and `toolbar.previewEmpty*` keys exist with translated values in HE/DE/CS.
    - The four-locale parity gates pass: `node tests/25-11-i18n-parity.test.js`, `node tests/40-i18n-parity.test.js`, and `node tests/42-i18n-parity.test.js` all exit 0 (every EN key touched exists in HE/DE/CS and vice versa). Full `node tests/run-all.js` exit-0 is NOT gated here — the four `46.1-*` scaffold tests stay red until their owning plans (02/03/04) land; this plan delivers none of them.
  </acceptance_criteria>
  <done>All new/changed strings ship in HE/DE/CS with RTL-correct Hebrew; the orphaned key is removed in every locale; four-locale parity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static string dictionaries + HTML default copy; no user input crosses a boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | (inherited) preview render path | high | mitigate | Copy-only; the render path is untouched. Strings are inserted via textContent / data-i18n, never as innerHTML. |
| T-46.1-SC | Tampering | npm installs | low | accept | Zero package installs. |
</threat_model>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification):
- **i18n keys:** `toolbar.previewChip` (all four locales). Reconciled values on existing keys: `toolbar.previewEmptyBody` (title left unchanged), `export.next1` + `export.next2` (→ "Continue"; `export.done` unchanged = "Done"), `export.discard.title`/`body`/`yes`/`no`, `export.ephemeralNote`, `export.step2.helper`. Removed key: `toolbar.hidePreview` (all four locales).
- **HTML:** updated default copy for `export.ephemeralNote` and `export.step2.helper` in add-session.html.

<verification>
- grep assets/i18n-en.js — `toolbar.previewChip`, `export.next1`/`export.next2` = "Continue" (`export.done` still "Done"), reconciled discard/ephemeral/empty-state-body values present; `toolbar.hidePreview` absent.
- grep i18n-he/de/cs.js — `toolbar.previewChip` present, `export.next1`/`export.next2` reconciled, `toolbar.hidePreview` absent (parity).
- grep add-session.html — the ephemeral-note + Step-2 default text match the EN values.
- `node tests/25-11-i18n-parity.test.js`, `node tests/40-i18n-parity.test.js`, `node tests/42-i18n-parity.test.js` — exit 0 (four-locale parity). Full `run-all` is NOT gated here (46.1 scaffold tests await plans 02/03/04).
</verification>

<success_criteria>
The redesign's copy ships complete in all four locales (EN corpus + HE/DE/CS parity, RTL-correct Hebrew):
the PREVIEW chip, reconciled empty-state, discard-dialog, ephemeral note, and Step-2 helper; the switcher
labels reuse existing keys; the orphaned toggle key is gone everywhere; and the add-session.html defaults
agree with EN. Help/changelog coverage for the push lands in plan 06.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-05-SUMMARY.md` when done.
</output>