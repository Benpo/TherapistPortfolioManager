---
phase: 46.1-preview-edit-experience-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/46.1-inplace-swap.test.js
  - tests/46.1-switcher-current-state.test.js
  - tests/46.1-format-in-preview.test.js
  - tests/46.1-frame-no-surface-alt.test.js
  - tests/webkit/46-export-step2-layout.mjs
autonomous: true
requirements: [RTXT-01, RTXT-04, RTXT-05]

must_haves:
  truths:
    - "Four jsdom/source-audit test files exist and FAIL RED against the current shipped source (the behavior they assert is not built yet)."
    - "Each jsdom test dispatches real `mousedown` (never `.click()`) and asserts OUTCOMES only (element visibility, `.is-active` / `is-hidden` class, activeElement, rendered-text presence, aria-pressed) — never module internals like `_previewOpen`."
    - "The WebKit probe (tests/webkit/46-export-step2-layout.mjs) drives preview through the retargeted switcher segment (`[data-mode=\"preview\"]`, old `data-action=\"preview\"` selector gone) and carries an extended assertion set E for the export in-place-swap: preview visible on open at 1440x820 without scroll, bar pinned, editor hidden while previewing, RTL pass — consistent with the content-driven card sizing."
  artifacts:
    - tests/46.1-inplace-swap.test.js
    - tests/46.1-switcher-current-state.test.js
    - tests/46.1-format-in-preview.test.js
    - tests/46.1-frame-no-surface-alt.test.js
    - tests/webkit/46-export-step2-layout.mjs
  key_links:
    - "tests/run-all.js globs top-level tests/*.test.js — the four new files are auto-discovered by naming alone (no config edit); the webkit probe is manual-only and NEVER part of npm test."
    - "Harness cloned verbatim from tests/46-persistent-bar-dispatch.test.js (fresh JSDOM per case; win.eval assets/md-render.js then text-edit.js then rich-toolbar.js in index.html order — md-render.js is required so MdRender.render is the real escape-first renderer, not the textContent fallback; execCommand stubbed to force TextEdit's splice fallback; pass-through App.t)."
  prohibitions:
    - statement: "A jsdom test asserts an internal flag (e.g. _previewOpen / _previewField / _focused) instead of an observable outcome."
      status: resolved
      verification: "Grep the four new test files — no reference to _previewOpen/_previewField/_focused; assertions read DOM state (classList, hidden, activeElement, textContent, getAttribute)."
    - statement: "A jsdom test triggers a control with .click() instead of a real mousedown MouseEvent."
      status: resolved
      verification: "Grep the four new test files — control activation uses dispatchEvent(new win.MouseEvent('mousedown', ...)); no `.click()` on toolbar controls."
---

<objective>
Write the Wave-0 RED-first test scaffold that pins the new preview/edit behavior BEFORE any
implementation. Behavior-before-implementation is project law (MEMORY feedback-behavior-verification;
this repo has shipped false-GREEN jsdom PDF tests before). Every implementation plan in this phase is
verified against these files.

Purpose: encode the Gap-14 (current-state switcher), Gap-15 (Frame, never surface-alt), in-place-swap,
and format-in-preview contracts as falsifiable outcome assertions that fail RED today and go GREEN only
when the surface is correctly rewired.
Output: four `tests/46.1-*.test.js` files (auto-discovered by tests/run-all.js) + an extended assertion
set E on the manual WebKit probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-VALIDATION.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-PATTERNS.md
@tests/46-persistent-bar-dispatch.test.js
@assets/rich-toolbar.js
@assets/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests — in-place swap + Ctrl/Cmd+E toggle, and current-state switcher</name>
  <files>tests/46.1-inplace-swap.test.js, tests/46.1-switcher-current-state.test.js</files>
  <read_first>
    - tests/46-persistent-bar-dispatch.test.js (the CANONICAL harness for this subsystem — clone its makeEnv/mousedown helpers verbatim: fresh JSDOM per case, win.eval text-edit.js then rich-toolbar.js in index.html order, execCommand stubbed to false, pass-through win.App.t, persistentBar(taP) = taP.previousElementSibling)
    - assets/md-render.js (registers `window.MdRender` with `render(md)`; index.html + add-session.html load it BEFORE rich-toolbar.js — the env MUST eval it or renderPreview falls to the textContent fallback and a `<strong>` assertion is RED forever)
    - assets/rich-toolbar.js (mount, buildToolbar, openPreview/closePreview/togglePreview, _dispatch, onFieldKeyDown, onFieldFocusOut ~322-335 — the surface these tests will drive; the deferred hide at :329-334 is the focusout-cascade the swap must be made preview-aware against)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (Interaction Contract §The mode model — the LOCKED behavior being asserted)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Validation Architecture → Test Map rows for RTXT-04)
  </read_first>
  <action>
    Create `tests/46.1-inplace-swap.test.js` and `tests/46.1-switcher-current-state.test.js`, cloning the
    makeEnv() env-setup and the mousedown(win, el) helper from tests/46-persistent-bar-dispatch.test.js
    (mount a shared note-field textarea AND a persistent export-editor analogue; the persistent bar is
    the field's previousElementSibling). CRITICAL harness addition: in makeEnv(), `win.eval(asset('assets/md-render.js'))`
    BEFORE `win.eval(asset('assets/rich-toolbar.js'))` (index.html order) so `window.MdRender.render` is the
    REAL escape-first renderer — otherwise renderPreview takes the textContent fallback (rich-toolbar.js:584-586)
    and the `<strong>` assertion below can never go green. Assert OUTCOMES only — never
    `_previewOpen`/`_previewField`/`_focused`.

    `46.1-inplace-swap.test.js` (RTXT-04, in-place swap + Ctrl/Cmd+E) — assert:
    - Enter preview (mousedown the Preview mode segment `[data-mode="preview"]`): the textarea carries the
      hidden marker class `is-hidden` (textarea removed from the box) AND a preview frame element
      (`.rich-toolbar-preview`) is present and NOT `.is-hidden`, AND the frame's render body
      (`.rich-toolbar-preview-body`) contains the real MdRender output for the field value (value `**hi**`
      renders a `<strong>`; assert `frame.querySelector('.rich-toolbar-preview-body strong')` is present and
      its textContent includes "hi").
    - No-soft-keyboard on the PERSISTENT bar (replaces retargeted Case B's coverage): enter preview via the
      persistent export-editor analogue's Preview segment with NO prior focus, and assert
      `win.document.activeElement !== taP` (the swap resolves the field WITHOUT focusing it — the shipped
      iOS-keyboard-suppression invariant).
    - Leave preview (mousedown the Edit mode segment `[data-mode="edit"]`): the textarea no longer carries
      `is-hidden` (visible again) and is the document.activeElement (refocused), and the frame is `.is-hidden`.
    - `Ctrl/Cmd+E` — TWO distinct paths, because jsdom applies no CSS (an `is-hidden` textarea is not actually
      blurred), so each path must be exercised explicitly:
      (a) enter-preview direction (textarea still focused): focus the field, dispatch a real keydown on the
      FIELD with `ctrlKey:true, key:'e'` (and a second case with `metaKey:true`) and assert it flips
      edit→preview (textarea gains `is-hidden`);
      (b) return direction via the DOCUMENT-level fallback: while previewing, explicitly `taField.blur()` (jsdom
      will not blur a display:none-less hidden element on its own) and dispatch the `Ctrl/Cmd+E` keydown on
      `win.document` (not the field), then assert it returns to edit (textarea loses `is-hidden`) — this is
      the ONLY assertion that exercises the document-level listener plan 03 adds; a field-only keydown would
      pass even if that listener were missing.
    - Focusout-suppression falsifier (BLOCKER-3 guard, NOTE field): focus the shared note field, enter preview
      (its textarea gains `is-hidden`), then dispatch a real `focusout` on the field and FLUSH the 0ms deferred
      timer (the makeEnv harness runs real timers; advance/await a macrotask so onFieldFocusOut's `setTimeout(...,0)`
      fires). Assert the frame is STILL open (not `is-hidden`) AND the shared bar is NOT `is-hidden` — proving the
      swap-initiated blur does not cascade the bar+preview closed the instant preview opens.
    - One thing on screen at a time: while previewing, the textarea is `is-hidden` (never both visible in one
      scroll container).

    `46.1-switcher-current-state.test.js` (RTXT-04 Gap-14 falsifier) — assert:
    - The bar renders a two-segment mode switcher: exactly two mode controls exist, resolvable by
      `[data-mode="edit"]` and `[data-mode="preview"]`.
    - CURRENT-STATE, not target-state: while editing, the `[data-mode="edit"]` segment carries the active
      class `is-active` and `[data-mode="preview"]` does NOT; after entering preview, `[data-mode="preview"]`
      carries `is-active` and `[data-mode="edit"]` does NOT. (The rejected old design turned a single
      target-labelled toggle green while in the opposite mode — this test fails that shape.)
    - The active segment's label matches the mode it represents (edit segment label resolves to the Edit
      copy, preview segment to the Preview copy) — never a green segment whose label names the other mode.

    All controls activate via real `mousedown` MouseEvents (the toolbar binds mousedown, not click). No
    planning-ID strings in the test bodies beyond ordinary descriptive names.
  </action>
  <verify>
    <automated>node tests/46.1-inplace-swap.test.js; s1=$?; node tests/46.1-switcher-current-state.test.js; s2=$?; test $s1 -ne 0 -a $s2 -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/46.1-inplace-swap.test.js exists and `node tests/46.1-inplace-swap.test.js` exits NON-ZERO (RED) against current shipped source.
    - tests/46.1-switcher-current-state.test.js exists and exits NON-ZERO (RED) against current shipped source.
    - Both files contain `new win.MouseEvent('mousedown'` and contain NO `_previewOpen`, `_previewField`, or `_focused` reference (grep-clean).
    - Both files reference `data-mode="edit"` and `data-mode="preview"` (the switcher segments) and the `is-hidden` textarea marker.
    - tests/46.1-inplace-swap.test.js evals `assets/md-render.js` in its env, asserts a `.rich-toolbar-preview-body strong` from a `**hi**` value, dispatches a `Ctrl/Cmd+E` keydown on `win.document` (the document-level return path) after blurring the field, and contains a focusout-suppression case (dispatch `focusout` + flush the 0ms timer → frame still open, bar not `is-hidden`), plus a persistent-bar `activeElement !== field` assertion.
  </acceptance_criteria>
  <done>Two RED jsdom test files pin the in-place-swap + Ctrl/Cmd+E and current-state-switcher contracts; both fail against current source; both assert outcomes via real mousedown.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests — format-in-preview return-and-apply + pinned-outside-strip; Frame source-audit (no surface-alt, sole innerHTML sink)</name>
  <files>tests/46.1-format-in-preview.test.js, tests/46.1-frame-no-surface-alt.test.js</files>
  <read_first>
    - tests/46-persistent-bar-dispatch.test.js (harness clone source)
    - assets/rich-toolbar.js (buildToolbar structure + renderPreview: the SOLE innerHTML sink is `pane.innerHTML = window.MdRender.render(val)`; the empty-state uses textContent)
    - assets/app.css (the `.rich-toolbar-preview` rule at ~3663-3669 — currently `background: var(--color-surface-alt)`, the Gap-15 offender the source-audit will fail against)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Color LOAD-BEARING COLOR RULE; §Interaction Contract "formatting-in-preview returns to Edit and applies")
  </read_first>
  <action>
    <!-- planner-discipline-allow: --color-surface-alt -->
    Create `tests/46.1-format-in-preview.test.js` (jsdom behavior) and `tests/46.1-frame-no-surface-alt.test.js`
    (source-audit over file text).

    `46.1-format-in-preview.test.js` (RTXT-01) — clone the harness; assert OUTCOMES:
    - Return-and-apply: enter preview (mousedown the preview segment), then mousedown the Bold formatting
      button. Assert the mode returns to edit (the edit segment is `is-active`, the textarea is visible /
      not `is-hidden`) AND the bold markers were applied to the field value (value gained `**`). Proves the
      one-gesture "click formatting while previewing returns to Edit and applies".
    - Pinned OUTSIDE the scroll strip (structural): the mode switcher controls (`[data-mode]`) are NOT
      descendants of the horizontally-scrollable strip element `.rich-toolbar-scroll` (query the strip,
      assert it does not contain any `[data-mode]`), while the formatting buttons ARE inside the strip.
      This structural assertion is why Preview stays visible while formatting scrolls on phone widths.

    `46.1-frame-no-surface-alt.test.js` (RTXT-04 Gap-15 + V5 security) — read the files as TEXT and assert:
    - REGION-SCOPED negative audit: extract the `.rich-toolbar-preview { ... }` rule block from
      assets/app.css (from the selector to its closing brace) and assert that block does NOT contain the
      token `--color-surface-alt`. Region-scope is mandatory — that token legitimately appears in OTHER
      app.css rules, so a file-wide count would false-fail. (RED now: the current preview rule uses it.)
    - Positive Frame check: the same rule block contains `var(--color-surface)` as its background and a
      `var(--color-green-border-ghost)` border token.
    - SOLE innerHTML sink (V5 / threat T-46.1-01 regression guard): in assets/rich-toolbar.js, every
      `.innerHTML =` assignment in the preview path is fed by `window.MdRender.render(` — assert there is
      no `.innerHTML =` in rich-toolbar.js that is not the MdRender-fed one (count innerHTML assignments;
      assert the only one reads MdRender.render). This is a regression guard: it is GREEN on shipped
      source today and MUST stay green through the rewire.

    Use a resilient rule-extraction (match the selector, then read to the next `}`); do not hard-code line
    numbers. No planning-ID strings in comments.
  </action>
  <verify>
    <automated>node tests/46.1-format-in-preview.test.js; s1=$?; node tests/46.1-frame-no-surface-alt.test.js; s2=$?; test $s1 -ne 0 -a $s2 -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/46.1-format-in-preview.test.js exists and exits NON-ZERO (RED) against current source (no switcher / no `.rich-toolbar-scroll` strip yet).
    - tests/46.1-frame-no-surface-alt.test.js exists and exits NON-ZERO (RED) against current source (the shipped `.rich-toolbar-preview` rule still contains the section-header background token).
    - The frame audit extracts the `.rich-toolbar-preview` rule region and checks it in isolation (grep the test for a rule-block extraction, not a whole-file token count).
    - The sole-innerHTML-sink assertion references `MdRender.render` and asserts a single sink in rich-toolbar.js.
  </acceptance_criteria>
  <done>Two RED test files pin the return-and-apply + pinned-outside-strip behavior and the Gap-15 Frame source-audit plus the sole-innerHTML-sink security guard; both fail against current source.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the WebKit real-geometry probe — set E for export in-place-swap</name>
  <files>tests/webkit/46-export-step2-layout.mjs</files>
  <read_first>
    - tests/webkit/46-export-step2-layout.mjs (the probe header documents assertion sets A-E; set E already covers "preview reveal without prior focus" — E1 opened+visible+aria, E2 pane clears sticky bar, E3 content rendered; extend for in-place swap. CRITICAL: `SEL.previewBtn` at :165-167 targets the now-DELETED `[data-action="preview"]` control and E1 asserts its `aria-pressed` at :394-399 — both must be RETARGETED to the new switcher segment or set E stays RED at the machine gate. The C-precondition fallback at :306-311 uses `btn.click()`; the switcher binds `mousedown`, so that fallback must dispatch mousedown on the segment.)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Export surface — persistent always-visible toolbar, in-place swap within the CONTENT-DRIVEN edit area, preview reveal; §Sizing — height auto + max-height cap ~94%, maximize ~92%, editor min-height 280px grows the card, NEVER hard 50dvh)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Common Pitfalls 2 + 5 — prove the frame is visible-on-open WITHOUT scroll before deleting reveal math; WebKit-only geometry)
  </read_first>
  <action>
    <!-- planner-discipline-allow: data-action="preview" -->
    Extend `tests/webkit/46-export-step2-layout.mjs` for the in-place-swap export surface (this probe is
    MANUAL — run by a human at the device gate, never part of npm test; keep that header contract).
    FIRST, retarget the deleted control (WARNING 9/10):
    - Change `SEL.previewBtn` from `...[data-action="preview"]` to the switcher segment
      `...> .rich-toolbar .rich-toolbar-swap-btn[data-mode="preview"]`, and add `SEL.editSeg` for
      `...[data-mode="edit"]`. E1's pressed check keeps reading `aria-pressed` — now on the segment (plan 02
      puts `aria-pressed` on each segment mirroring `is-active`).
    - In the C-precondition fallback (:306-311), replace `btn.click()` with a real `mousedown` MouseEvent on
      `SEL.previewBtn` (the switcher binds mousedown, not click).
    THEN add the in-place-swap assertions, driven with real WebKit geometry at viewport 1440x820, driving the
    Preview segment via a real `mousedown`:
    - Open export Step 2, do NOT focus the editor first, mousedown the Preview segment: the preview frame is
      visible in the viewport on open (its top is at or below the pinned bar's bottom and its box is within
      the edit-area visible rect) WITHOUT any manual scroll — the in-place swap puts the frame where the
      editor was, so no below-fold reveal is needed.
    - The persistent toolbar stays pinned (its rect top equals the edit-area top) after entering preview and
      while the frame is shown.
    - Editor-hidden assertion: the `#exportEditor` is hidden (its client rect collapses / `display:none`)
      while previewing and returns visible after mousedown on the Edit segment — proving one box at a time
      (no stacked scroll container). Name this assertion clearly (e.g. an `editorHidden` measurement) so the
      verify grep below can find it.
    - RTL pass: repeat with dir="rtl" (Hebrew) — the switcher pins at the bar's logical end, the chip pins
      at inset-inline-start, and the frame is still visible-on-open.
    SIZING CONSISTENCY (WARNING 14): plan 04 changes the export card to the CONTENT-DRIVEN model (height auto
    + max-height ~94dvh; maximize ~92dvh; editor 280px floor). Verify the existing A/B/C stay valid — the
    setup loads a 64-line document, so the card grows to its max-height cap and the edit area still overflows
    (C non-vacuous) with a usable editor (B) and unclipped pinned toolbar (A). The D maximize guard
    (`h >= 0.85 * viewportHeight`) still holds because that long doc fills to the ~92dvh cap; if the
    content-driven auto height makes D marginal, keep D's long-document precondition explicit rather than
    weakening the threshold. Do NOT re-hard-size the card in the probe.
    Keep the existing sets A-D intact (only the selector + click→mousedown fixes above); document the new
    set E in-place-swap cases in the probe header.
  </action>
  <verify>
    <automated>node --check tests/webkit/46-export-step2-layout.mjs && node -e "const s=require('fs').readFileSync('tests/webkit/46-export-step2-layout.mjs','utf8'); process.exit(/data-mode=\"preview\"/.test(s) && /editorHidden|editor.*hidden/i.test(s) && /rtl/i.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `SEL.previewBtn` is retargeted to the `.rich-toolbar-swap-btn[data-mode="preview"]` segment and `SEL.editSeg` (data-mode="edit") is added; the old `data-action="preview"` selector no longer appears anywhere in the file; the C-fallback drives the segment via mousedown (no `btn.click()` on it).
    - The probe contains new set-E in-place-swap assertions referencing 1440x820, a Preview open-without-prior-focus case, a pinned-bar check, an editor-hidden-while-previewing measurement, and an `rtl` case; E1 reads the segment's `aria-pressed`.
    - The probe header still states it is manual-only and NOT part of npm test; A/B/C/D remain, consistent with the content-driven card sizing (long-document setup keeps C non-vacuous and D >= 0.85*viewport).
    - The file parses (node --check passes): `node --check tests/webkit/46-export-step2-layout.mjs` exits 0.
  </acceptance_criteria>
  <done>The manual WebKit probe drives preview through the new switcher segment (old preview-action selector gone), carries an extended set E for the export in-place-swap (visible-on-open + pinned-bar + editor-hidden + RTL), and its A-D sizing assertions remain valid under the content-driven card model — ready to run at the device gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note text → preview render | Untrusted note text is rendered to HTML in the preview frame (the one place user text becomes DOM in this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | rich-toolbar.js preview render (innerHTML) | high | mitigate | This plan ENCODES the mitigation as a guard: `46.1-frame-no-surface-alt.test.js` asserts the sole innerHTML sink is `MdRender.render` output. Implementation plans must keep it green. |
| T-46.1-SC | Tampering | npm installs | low | accept | This phase installs ZERO packages (vanilla PWA, no build step). No install task exists; jsdom is already present. |
</threat_model>

## Artifacts this phase produces

New symbols this phase creates (so downstream drift verification excludes them):
- **Test files:** `tests/46.1-inplace-swap.test.js`, `tests/46.1-switcher-current-state.test.js`, `tests/46.1-format-in-preview.test.js`, `tests/46.1-frame-no-surface-alt.test.js` (new); extended `tests/webkit/46-export-step2-layout.mjs`.
- **DOM markers asserted (created by later plans):** the switcher segments `[data-mode="edit"]` / `[data-mode="preview"]`, the active-segment class `is-active` on `.rich-toolbar-swap-btn`, the scroll strip `.rich-toolbar-scroll`, the `is-hidden` textarea marker in preview, the Frame chip `.rich-toolbar-preview-chip`, the render body `.rich-toolbar-preview-body`.

<verification>
- `node tests/46.1-inplace-swap.test.js` — exits NON-ZERO (RED) against current source.
- `node tests/46.1-switcher-current-state.test.js` — exits NON-ZERO (RED).
- `node tests/46.1-format-in-preview.test.js` — exits NON-ZERO (RED).
- `node tests/46.1-frame-no-surface-alt.test.js` — exits NON-ZERO (RED).
- `node --check tests/webkit/46-export-step2-layout.mjs` — exits 0 (parses).
- The four new files use real `mousedown` and assert outcomes only (grep-clean of internal-flag names).
</verification>

<success_criteria>
Four RED jsdom/source-audit tests exist and fail against current shipped source, pinning the
current-state switcher, in-place swap, Ctrl/Cmd+E, return-and-apply, pinned-outside-strip, Gap-15 Frame,
and sole-innerHTML-sink contracts; the manual WebKit probe carries an extended set E for the export
in-place-swap. Implementation plans 02-05 turn these GREEN.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-01-SUMMARY.md` when done.
</output>
