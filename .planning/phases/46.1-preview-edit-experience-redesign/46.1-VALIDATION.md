---
phase: 46.1
slug: preview-edit-experience-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-18
---

# Phase 46.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 46.1-RESEARCH.md `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Zero-npm custom runner + Node `assert` + jsdom (`^29.1.1`) — no Jest/Vitest/Mocha |
| **Config file** | none — `tests/run-all.js` globs top-level `tests/*.test.js`, each in its own `node` child process, exit-0/1 contract |
| **Quick run command** | `node tests/<task-specific-file>.test.js` (single file) |
| **Full suite command** | `node tests/run-all.js` (191/191 green at Phase 46 close) |
| **Real-geometry gate (manual)** | `node tests/webkit/46-export-step2-layout.mjs` (Playwright WebKit; RED/GREEN falsifiable) |
| **Estimated runtime** | ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run the task's single test file (`node tests/<file>.test.js`)
- **After every plan wave:** Run `node tests/run-all.js`
- **Before `/gsd-verify-work`:** Full suite green + WebKit probe green + Ben's real-device gate (MacBook default + maximized + iPhone Safari, LTR + Hebrew RTL)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Task IDs assigned at planning; seeded from the requirement→test map in RESEARCH.md.
> Behavior-BEFORE-implementation is project law: Wave-0 jsdom tests are written RED,
> assert OUTCOMES (visibility, `.on` class, `activeElement`, rendered text, aria) —
> never internals — and dispatch real `mousedown` (not `.click()`).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | 01/03 | 0 | RTXT-04 | — | Preview renders via MdRender (escape-first; env evals md-render.js); swap + Ctrl/Cmd+E (incl. DOCUMENT-level return path); focusout-suppression falsifier (frame stays open after focusout+timer-flush); persistent-bar `activeElement !== field` | unit (jsdom) | `node tests/46.1-inplace-swap.test.js` | ❌ W0 | ⬜ pending |
| TBD | 02 | 0 | RTXT-04 | — | N/A (Gap-14 current-state falsifier; segment aria-pressed mirrors is-active) | unit (jsdom) | `node tests/46.1-switcher-current-state.test.js` | ❌ W0 | ⬜ pending |
| TBD | 02 | 0 | RTXT-01 | — | N/A (format-in-preview return-and-apply; pinned-outside-strip structure) | unit (jsdom) | `node tests/46.1-format-in-preview.test.js` | ❌ W0 | ⬜ pending |
| TBD | 04 | 0 | RTXT-04 | — | N/A (Gap-15 source-audit: no `--color-surface-alt` in preview rules; sole-innerHTML-sink) | source-audit | `node tests/46.1-frame-no-surface-alt.test.js` | ❌ W0 | ⬜ pending |
| TBD | 03 | 3 | RTXT-01 | — | Export lifecycle: close-while-previewing → reopen shows Edit + fresh render; Back→Continue dirty guard (discard-confirm before regenerate) | unit (jsdom) | `node tests/46.1-export-lifecycle.test.js` | ❌ (plan 03 creates) | ⬜ pending |
| TBD | TBD | — | RTXT-05 | — | N/A (indent/outdent + renumber regression) | unit (jsdom) | existing text-edit transform tests | ✅ | ⬜ pending |
| TBD | TBD | — | RTXT-04 | — | N/A (export preview visible on open; pinned bar; RTL-safe) | WebKit probe (manual) | extend `tests/webkit/46-export-step2-layout.mjs` | ✅ (extend) | ⬜ pending |
| TBD | TBD | — | all | — | N/A | suite | `node tests/run-all.js` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/46.1-inplace-swap.test.js` — RTXT-04 swap + Ctrl/Cmd+E toggle (RED first). Harness MUST eval `assets/md-render.js` (real renderer). Includes: the DOCUMENT-level Ctrl/Cmd+E return path (blur field + dispatch keydown on document), the focusout-suppression falsifier (dispatch focusout + flush the 0ms timer → frame still open, bar not is-hidden), and a persistent-bar `activeElement !== field` assertion.
- [ ] `tests/46.1-switcher-current-state.test.js` — RTXT-04 Gap-14 current-state falsifier (RED first); asserts segment `aria-pressed` mirrors `is-active`
- [ ] `tests/46.1-format-in-preview.test.js` — RTXT-01 return-and-apply + pinned-outside-strip structure (RED first)
- [ ] `tests/46.1-frame-no-surface-alt.test.js` — RTXT-04 Gap-15 source-audit + sole-innerHTML-sink (RED first)
- [ ] Extend `tests/webkit/46-export-step2-layout.mjs` — retarget `SEL.previewBtn` to `[data-mode="preview"]`; export in-place-swap visible-on-open + pinned bar + editor-hidden + RTL pass
- [ ] Framework install: none needed (jsdom already installed)

**Retargeted / co-located tests (NOT Wave 0 — land with their owning plan):**
- Plan 02 retargets `tests/46-persistent-bar-dispatch.test.js` (Case B) and `tests/30-export-stepper.test.js` (case F) to drive preview via the new `[data-mode="preview"]` segment (they hard-assert the deleted control otherwise → suite can never go green).
- Plan 03 creates `tests/46.1-export-lifecycle.test.js` (close-reset + Back→Continue dirty-guard cases) alongside the export-modal.js fix.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Export preview visible on open at 1440×820, bar pinned, RTL-safe | RTXT-04 | Real WebKit geometry — jsdom has no layout | `node tests/webkit/46-export-step2-layout.mjs` (extended set) |
| Real-device gate | RTXT-01/04/05 | Ben's ratification gate per locked process | MacBook default + maximized + iPhone Safari; LTR + Hebrew RTL |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
