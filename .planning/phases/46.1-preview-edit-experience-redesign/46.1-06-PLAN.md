---
phase: 46.1-preview-edit-experience-redesign
plan: 06
type: execute
wave: 4
depends_on: ["46.1-02", "46.1-03", "46.1-04", "46.1-05"]
files_modified:
  - assets/help-content-en.js
  - assets/changelog-content-en.js
autonomous: true
requirements: [RTXT-01, RTXT-04, RTXT-05]

must_haves:
  truths:
    - "The push carries an EN changelog entry describing the redesigned preview/edit experience (in-place Edit/Preview swap + unified Frame preview language on both note fields and export), satisfying the push-global changelog demand."
    - "The affected EN help topics are updated to describe the new UX: `topic-quick-paste` (capturing-emotions — the note-field toolbar + Edit/Preview switch) AND `topic-single-export` + `topic-export-formats` (review-export — the export Step-2 Edit/Preview surface), satisfying the per-file help demand for the TRIGGER files ACTUALLY modified this phase: rich-toolbar.js + add-session.html (→ topic-quick-paste) and export-modal.js (→ topic-single-export + topic-export-formats). text-edit.js is NOT modified this phase, so it raises no demand; the topic set is unchanged from the pre-revision plan."
    - "EN is the corpus of record — the changelog and help edits are EN (a locale-only edit would not satisfy a demand)."
  artifacts:
    - assets/help-content-en.js
    - assets/changelog-content-en.js
  key_links:
    - "HELP-MAP.md ownership (read cold, recomputed from the FINAL files_modified union): rich-toolbar.js → topic-quick-paste; export-modal.js → topic-single-export + topic-export-formats; add-session.html → many incl. topic-quick-paste. text-edit.js is not modified this phase (no demand). app.css + the four i18n dicts are CHANGELOG-ONLY (no per-file help demand, no Help-Unaffected trailer needed); tests/* are unwatched. So one EN help edit covering topic-quick-paste + topic-single-export + topic-export-formats + one EN changelog edit satisfies the whole push."
    - "The docs gate (scripts/docs-gate.js, git hook + CI) is authoritative; this plan makes the phase's push pass the Definition of Done."
  prohibitions:
    - statement: "The push ships user-facing surface changes without an EN changelog entry AND updated affected help topics (or an explicit Help-Unaffected trailer)."
      status: resolved
      verification: "assets/changelog-content-en.js has a new v1.4-line entry for the redesign; assets/help-content-en.js edits the capturing-emotions/topic-quick-paste and review-export/topic-single-export + topic-export-formats topics."
---

<objective>
Satisfy the project Definition of Done / docs hard-gate for this phase's push: an EN changelog entry plus
updated EN help topics describing the redesigned preview/edit experience. The docs gate
(scripts/docs-gate.js, enforced by the git hook and authoritatively by CI) blocks a push of the TRIGGER
files ACTUALLY modified this phase — rich-toolbar.js (plans 02/03), export-modal.js (plan 03), and
add-session.html (plan 05) — unless a changelog entry AND the affected help topics are updated. (text-edit.js
is NOT modified this phase, so it raises no demand; app.css + the four i18n dicts are CHANGELOG-ONLY;
tests/* are unwatched by the gate.) EN is the corpus of record.

Purpose: keep the docs from rotting — the redesigned surfaces must be documented before ship. Runs after
the implementation plans so it describes the FINAL behavior (wave 4).
Output: a new entry in `assets/changelog-content-en.js` + topic edits in `assets/help-content-en.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md
@HELP-MAP.md
@assets/help-content-en.js
@assets/changelog-content-en.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN changelog entry for the preview/edit redesign</name>
  <files>assets/changelog-content-en.js</files>
  <read_first>
    - assets/changelog-content-en.js (the latest entry pattern; the 1.4.0 entry — 1.4.0 is PREPROD-ONLY, no real user has seen its what's-new yet, so this redesign extends the existing 1.4.0 entry rather than bumping the version — the extended entry ships to users together with the redesign)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Copywriting + §Interaction — the user-facing summary of what changed)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Docs Gate Implications — the changelog demand is push-global; one EN edit satisfies it)
  </read_first>
  <action>
    Add/extend the EN changelog content in assets/changelog-content-en.js to describe the preview/edit
    redesign in user-facing terms: the Edit/Preview switch now shows which mode you are in (current-state),
    Preview replaces the editor in place (one view at a time) with a clearly-framed "PREVIEW" look on both
    the session note fields and the export document, formatting dims while previewing and a click returns
    you to editing, and Ctrl/Cmd+E toggles preview. Fold this into the existing 1.4.0 changelog entry and do
    NOT bump the version (Ben's decision): 1.4.0 is preprod-only, so no real user has seen its what's-new —
    extending the 1.4.0 entry re-announces nothing to nobody, and the extended entry ships to users together
    with the redesign. Keep it concise and practitioner-facing (no engineering jargon, no gap/plan IDs).
  </action>
  <verify>
    <automated>grep -qiE 'preview' assets/changelog-content-en.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/changelog-content-en.js contains a user-facing description of the Edit/Preview redesign within the existing 1.4.0 entry (no new version number introduced — 1.4.0 is preprod-only per Ben's decision).
    - The entry text names both surfaces (note fields + export) and the Edit/Preview switch behavior.
    - No planning IDs (D-NN, "Gap 14", task refs) in the changelog copy.
    - `node tests/run-all.js` exits 0 (wave 4 — full suite green; changelog-integrity tests stay green).
  </acceptance_criteria>
  <done>The existing 1.4.0 EN changelog entry (not version-bumped) describes the preview/edit redesign, satisfying the push-global changelog demand.</done>
</task>

<task type="auto">
  <name>Task 2: EN help topics — capturing-emotions/topic-quick-paste + review-export topics</name>
  <files>assets/help-content-en.js</files>
  <read_first>
    - HELP-MAP.md (cold — the topic index: rich-toolbar.js → topic-quick-paste (capturing-emotions); export-modal.js → topic-single-export + topic-export-formats (review-export); add-session.html → many incl. topic-quick-paste. NOTE: text-edit.js also maps to topic-quick-paste but is NOT modified this phase, so it raises no demand — the topic set is unchanged regardless.)
    - assets/help-content-en.js (the `topic-quick-paste`, `topic-single-export`, and `topic-export-formats` topic bodies — the EN corpus to update)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Interaction Contract — the behavior to describe: current-state switcher, in-place swap, Frame preview, Ctrl/Cmd+E, export persistent bar)
  </read_first>
  <action>
    Update the EN help topics (corpus of record) so they describe the new preview/edit UX and thereby
    satisfy the per-file help demand for the TRIGGER files in the push:
    - `topic-quick-paste` (capturing-emotions): describe the note-field formatting bar's Edit/Preview switch
      — how to preview a note's formatted result (the framed PREVIEW view replaces the field in place),
      how to return to editing (click Edit, click any formatting button, or Ctrl/Cmd+E), and that the
      familiar snippets + autogrow still work.
    - `topic-single-export` and `topic-export-formats` (review-export): describe the export Step-2 editor's
      always-visible toolbar and the Edit/Preview switch (preview replaces the editor in place; edits here
      apply to this export only), so a practitioner knows how to review before exporting.
    Keep the writing practitioner-facing and consistent with the shipped help voice; do not invent features
    outside the UI-SPEC. EN only here (HE/DE/CS help translation is a later locale pass, per the docs gate —
    EN satisfies the demand). No planning IDs.
  </action>
  <verify>
    <automated>grep -q 'topic-quick-paste' assets/help-content-en.js && grep -q 'topic-single-export' assets/help-content-en.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/help-content-en.js `topic-quick-paste` body describes the note-field Edit/Preview switch (in-place framed preview + how to return to editing incl. Ctrl/Cmd+E).
    - assets/help-content-en.js `topic-single-export` and `topic-export-formats` bodies describe the export Step-2 always-visible toolbar + Edit/Preview switch.
    - Edits are EN (assets/help-content-en.js), the corpus of record.
    - No planning IDs in the help copy.
    - `node tests/run-all.js` exits 0.
  </acceptance_criteria>
  <done>The affected EN help topics describe the new preview/edit UX, satisfying the per-file help demand for the push's TRIGGER files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Documentation content only; no user input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | (inherited) preview render path | high | mitigate | Docs-only; render path untouched. The security control (MdRender sole sink) is verified in plans 03/04. |
| T-46.1-SC | Tampering | npm installs | low | accept | Zero package installs. |
</threat_model>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification):
- **Content:** a revised unreleased-v1.4 entry in `assets/changelog-content-en.js`; updated topic bodies in `assets/help-content-en.js` (topic-quick-paste, topic-single-export, topic-export-formats). No new topic IDs, no new covers[] entries (this phase adds no new asset module).

<verification>
- grep assets/changelog-content-en.js — a preview/edit redesign description within the unreleased v1.4 entry.
- grep assets/help-content-en.js — topic-quick-paste + review-export topics updated.
- `node tests/run-all.js` — exits 0.
- The docs gate (git hook / CI) passes for the phase push (changelog + affected help topics present; app.css + i18n dicts are CHANGELOG-ONLY and need no Help-Unaffected trailer).
</verification>

<success_criteria>
The phase's push satisfies the Definition of Done: an EN changelog entry describes the preview/edit
redesign and the affected EN help topics (capturing-emotions + review-export) are updated — so the docs
gate passes without any Help-Unaffected/emergency-skip trailer.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-06-SUMMARY.md` when done.
</output>