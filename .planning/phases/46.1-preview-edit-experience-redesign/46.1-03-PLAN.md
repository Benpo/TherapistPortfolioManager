---
phase: 46.1-preview-edit-experience-redesign
plan: 03
type: execute
wave: 3
depends_on: ["46.1-02"]
files_modified:
  - assets/rich-toolbar.js
  - assets/export-modal.js
  - tests/46.1-export-lifecycle.test.js
autonomous: true
requirements: [RTXT-01, RTXT-04, RTXT-05]

must_haves:
  truths:
    - "Entering preview HIDES the textarea (`is-hidden`) and shows the preview Frame in the SAME box (note fields: frame min-height = the textarea's offsetHeight; export: frame flex-fills the edit area); leaving preview shows the textarea again and refocuses it. One thing on screen at a time."
    - "The preview Frame is a positioned wrapper `.rich-toolbar-preview` containing a floating chip `.rich-toolbar-preview-chip` (\"PREVIEW\") and a `.note-rendered .rich-toolbar-preview-body` render target; MdRender.render output is assigned to the BODY only (the sole innerHTML sink), so the chip survives re-renders."
    - "`Ctrl/Cmd+E` toggles Edit<->Preview on both surfaces and keeps working while the textarea is hidden (bound where it still reaches — document/bar level, guarded to the active/bar field)."
    - "The empty-state block (no text) renders inside the Frame body via textContent (never innerHTML), with the reconciled copy keys."
    - "The export below-fold reveal is reconciled to the in-place swap: the Frame occupies the editor's box so it is visible on open; the revealPreviewPane scroll math is retargeted to the Frame and left as a harmless no-op safety (not deleted blindly — proven by the WebKit probe)."
    - "The shared note-field bar's hide-on-focusout is preview-aware: the swap-initiated blur (hiding the focused textarea) does NOT cascade the bar + preview closed the instant preview opens; a genuine outside pointerdown still closes preview and undocks the bar."
    - "Closing the export modal resets preview: `RichToolbar.resetPreview()` restores the previewing field to Edit (textarea shown, Frame hidden, `.is-preview` cleared, edit segment active) WITHOUT forcing focus, and export-modal.js calls it on close — so reopening Step 2 always starts in Edit with a fresh render, never a stale 'mode lies' Frame."
    - "Back->Continue is guarded (Ben's decision 3): if step-2 edits are dirty (`hasEditedPreview`), pressing Continue from step 1 shows the discard-confirm (export.discard.* keys) BEFORE onNext regenerates and overwrites editor.value; confirm regenerates + advances to step 2, cancel stays on step 1 with the edits intact."
  artifacts:
    - assets/rich-toolbar.js
    - assets/export-modal.js
    - tests/46.1-export-lifecycle.test.js
  key_links:
    - "openPreview/closePreview are the shipped open/close that plan 02's setMode already calls — this plan upgrades their internals to the in-place swap; setMode's segment/`.is-preview` toggling is untouched."
    - "MdRender.render remains the SOLE innerHTML sink (V5 / T-46.1-01); assigned to the Frame BODY, not the wrapper."
  prohibitions:
    - statement: "A stacked editor+preview in one scroll container (both visible, scroll escapes preview) — the rejected Gap-14 concept."
      status: resolved
      verification: "node tests/46.1-inplace-swap.test.js exits 0 — while previewing the textarea carries `is-hidden`; the frame and textarea are never both visible."
    - statement: "A second, non-MdRender innerHTML assignment introduced into the preview path."
      status: resolved
      verification: "node tests/46.1-frame-no-surface-alt.test.js exits 0 — the only innerHTML sink in rich-toolbar.js is MdRender.render output (assigned to the Frame body)."
    - statement: "Planning IDs in shipped code comments."
      status: resolved
      verification: "git diff assets/rich-toolbar.js — no 'Gap N', 'D-N', '46.1', task-ID tokens in added comment lines."
---

<objective>
Upgrade the preview open/close from the shipped below-editor sibling pane to an IN-PLACE SWAP (the
preview Frame replaces the editor in the same box; one thing on screen at a time) with the unified Frame
treatment (floating PREVIEW chip), and add the `Ctrl/Cmd+E` mode toggle that keeps reaching while the
textarea is hidden. This structurally eliminates the mixed-state scroll problem (Gap-14a) and the
below-fold pane (Gap-13) because there is no second box to scroll to.

Purpose: the mechanism half of the redesign — where the preview is shown and how it swaps back to edit.
Runs after plan 02 (same file) which already wired the current-state switcher to call openPreview/closePreview.
Output: rewired `buildPreviewPane` / `renderPreview` / `openPreview` / `closePreview` / `revealPreviewPane`
+ a preview-aware focusout guard + a public `resetPreview()` + a `Ctrl/Cmd+E` binding in
`assets/rich-toolbar.js`, plus the export-modal lifecycle fixes (reset-on-close + Back->Continue dirty
guard) in `assets/export-modal.js` and a new `tests/46.1-export-lifecycle.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-PATTERNS.md
@.planning/sketches/009-note-field-preview/index.html
@.planning/sketches/008-export-preview-edit-model/index.html
@assets/rich-toolbar.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-place swap — Frame wrapper + chip + body render target; openPreview/closePreview swap; reveal reconcile</name>
  <files>assets/rich-toolbar.js</files>
  <read_first>
    - assets/rich-toolbar.js (buildPreviewPane ~544-554 — currently sets class "note-rendered rich-toolbar-preview" and inserts afterend; renderPreview ~560-587 — the SOLE innerHTML sink `pane.innerHTML = MdRender.render(val)` + the textContent empty-state block ~564-578; openPreview ~626-634; closePreview ~666-674; revealPreviewPane ~646-663 scoped to .export-edit-area; dockTo closePreview-on-field-change ~289; onFieldFocusOut ~322-335 — the persistent early-return at :324 (why the export bar is exempt) and the DEFERRED hide block at :329-334 (the `setTimeout(...,0)` that reaches `hideToolbar()` when activeElement is body — the focusout-cascade the swap must be made preview-aware against); the heading-menu outside-dismiss idiom `onDocDown` ~519-533 (analog for the genuine-outside-click listener); the public return object ~909-913 — where `resetPreview` gets exposed)
    - .planning/sketches/009-note-field-preview/index.html (setMode ~613-635: `ta.classList.add('is-hidden')`, `pv.classList.add('is-on')`, `pv.querySelector('.preview-frame').style.minHeight = ta.offsetHeight + 'px'`; on edit reverse + `ta.focus()`)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Pattern 1 In-place swap; Pitfall 2 — prove reveal moot before deleting; Security Domain V5 — MdRender sole sink)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Note-field surface + §Export surface — in-place swap, empty state, autogrow height match)
  </read_first>
  <behavior>
    - Enter preview: the field's textarea gains `is-hidden`; a `.rich-toolbar-preview` Frame is shown (not
      `is-hidden`) with min-height equal to the textarea's pre-hide offsetHeight (note fields); its
      `.rich-toolbar-preview-body` contains MdRender output for the value; a `.rich-toolbar-preview-chip`
      is present.
    - Leave preview: the textarea loses `is-hidden` and is document.activeElement; the Frame is `is-hidden`.
    - Empty field in preview: the Frame body shows the empty-state title/body via textContent (no innerHTML).
    - A focusout on the previewing note field (the swap-initiated blur) does NOT close the preview or hide the
      bar; a mousedown outside the field's bar + Frame DOES close and undock.
    - `RichToolbar.resetPreview()` returns any open preview to Edit without focusing the field.
  </behavior>
  <action>
    Rewire the preview element + open/close in assets/rich-toolbar.js to the in-place swap (shipped-namespace
    names, decided in PATTERNS; restyle-in-place decision: keep the class `.rich-toolbar-preview` as the
    Frame wrapper rather than renaming to `.preview-frame`):
    - `buildPreviewPane(ta)`: build a Frame WRAPPER `div.rich-toolbar-preview` (NOT carrying `.note-rendered`
      anymore) with `aria-live="polite"`; inside it append (a) a chip `span.rich-toolbar-preview-chip` whose
      textContent is `t("toolbar.previewChip", "PREVIEW")`, and (b) a render body
      `div.note-rendered.rich-toolbar-preview-body`. Insert the wrapper `afterend` the textarea and store it
      on `ta._notePreview` as today (keep the deliberate separate-property comment intent, no planning IDs).
      Cache/resolve the body via `frame.querySelector('.rich-toolbar-preview-body')`.
    - `renderPreview(ta)`: target the BODY, not the wrapper. Empty/whitespace value -> add `is-empty` to the
      wrapper and build the empty-state block (title `t("toolbar.previewEmptyTitle", ...)`, body
      `t("toolbar.previewEmptyBody", ...)`) into the BODY via textContent (never innerHTML). Non-empty ->
      remove `is-empty`; assign `body.innerHTML = window.MdRender.render(val)` (the SOLE innerHTML sink) with
      the shipped textContent fallback when MdRender is absent. Do NOT introduce any other innerHTML write.
    - `openPreview(ta)`: build/show the Frame (remove `is-hidden` from the wrapper), set `_previewOpen`/
      `_previewField`, render, AND perform the swap — capture the textarea's current offsetHeight, set the
      Frame's `style.minHeight` to that height (so the Frame occupies the same box on the autogrowing note
      fields), then add `is-hidden` to the textarea. Then call the reconciled reveal (below).
    - `closePreview()`: remove `is-hidden` from the textarea (show it), clear the min-height override on the
      Frame, add `is-hidden` to the Frame, reset `_previewOpen`/`_previewField`, and refocus the textarea
      (`ta.focus()`) — matching the sketch. Keep the debounce clear.
    - `revealPreviewPane` reconcile (Pitfall 2 — do NOT delete blindly): the export edit area is still
      `overflow-y:auto`, but with the swap the Frame occupies the (now-hidden) editor's box, so it is visible
      on open. Retarget the function at the Frame wrapper and keep its guarded, vertical-only, physical-rect
      scroll math as a harmless no-op safety (delta ~0 when the Frame sits where the editor was). The WebKit
      probe (set E) proves visible-on-open; leave the safety in rather than removing it this phase.
    - Make the shared bar's hide-on-focusout PREVIEW-AWARE (the in-place swap hides the FOCUSED textarea →
      display:none blurs it → in a real browser focus falls to body → onFieldFocusOut's deferred check
      (:329-334) sees a non-registered activeElement and calls `hideToolbar()` → `closePreview()`, so the
      note-field preview would self-close the instant it opens; jsdom is blind to this because it applies no
      CSS). Inside the deferred `setTimeout(...,0)` block, BEFORE calling `hideToolbar()`, return early when
      the preview is open for the blurring field (`_previewOpen && _previewField === ev.currentTarget`) — the
      blur is swap-initiated, not a real focus-leave, so keep the bar + the open preview. (The persistent
      export bar is already exempt via the :324 early return.) For genuine dismissal, bind a one-shot
      outside-`mousedown` listener when a NOTE-field preview opens (mirror the heading-menu `onDocDown`
      idiom, deferred bind so the opening click does not fire it): a mousedown that lands outside the field's
      own bar AND its preview Frame calls `closePreview()` + `hideToolbar()`; tear the listener down in
      closePreview. Plan 01's focusout-suppression falsifier (dispatch focusout + flush the 0ms timer → frame
      still open, bar not is-hidden) pins this.
    - Expose a public `resetPreview()` on the module's return object (alongside mount/unmount/refreshButtonState):
      if a preview is open, restore that field to Edit — remove `is-hidden` from its textarea (show it),
      clear the Frame's min-height override, add `is-hidden` to the Frame, reset `_previewOpen`/`_previewField`,
      remove `.is-preview` from the bar, and set the edit segment `is-active`/`aria-pressed` — WITHOUT calling
      `ta.focus()` (a modal is closing; do not force focus onto a hidden editor). export-modal.js (Task 3)
      calls this on modal close so a reopen starts in Edit with a fresh render.
    - Keep the dockTo "close preview on field change" behavior and the Gap-16 guard intact. No planning IDs
      in added comments; describe mechanisms, not tickets.
  </action>
  <verify>
    <automated>node tests/46.1-inplace-swap.test.js && node tests/46.1-frame-no-surface-alt.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/rich-toolbar.js emits `rich-toolbar-preview-chip` and `rich-toolbar-preview-body` class strings; the wrapper `.rich-toolbar-preview` no longer also carries `note-rendered` (the body does).
    - `body.innerHTML = window.MdRender.render(` is the ONLY `.innerHTML =` assignment in assets/rich-toolbar.js (grep: exactly one innerHTML assignment, MdRender-fed).
    - `node tests/46.1-inplace-swap.test.js` exits 0 (textarea hidden on preview + frame body has MdRender output + refocus on edit + the focusout-suppression falsifier: frame stays open, bar stays docked after a focusout+timer-flush).
    - `node tests/46.1-frame-no-surface-alt.test.js` exits 0 (sole-innerHTML-sink guard still green).
    - `RichToolbar.resetPreview` is exposed on the public return object (grep: `resetPreview` in the returned object literal) and returns an open preview to Edit without focusing the field.
    - `node tests/run-all.js` exits 0 (no regression; wave 3 runs after all wave 2, so the app.css Frame fix and locale copy have landed).
    - Added comment lines contain no planning IDs.
  </acceptance_criteria>
  <done>Preview is an in-place swap: the textarea hides and the Frame (chip + note-rendered body) shows in the same box; leaving refocuses the field; MdRender stays the sole sink; the swap + sole-sink tests and the suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ctrl/Cmd+E mode toggle reaching while the textarea is hidden</name>
  <files>assets/rich-toolbar.js</files>
  <read_first>
    - assets/rich-toolbar.js (onFieldKeyDown ~747-813 — bound to the textarea's keydown; the Ctrl/Cmd+Z/Y block ~791-807; the desktop Ctrl/Cmd+B/I gate ~808-812; togglePreview ~681-686 and setMode from plan 02)
    - .planning/sketches/008-export-preview-edit-model/index.html (handleCtrlE ~1046-1050 and the document-level Ctrl+E listener ~1051-1054 that fires when the active element is NOT a textarea — the pattern for reaching while the editor is hidden)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md (Pitfall 3 — a hidden textarea receives no keys; bind Ctrl/Cmd+E where it still reaches; Assumptions Log A1 — Ctrl/Cmd+E is a NET-NEW binding, no conflict)
  </read_first>
  <behavior>
    - With a field focused and editing, `Ctrl/Cmd+E` enters preview (textarea gains `is-hidden`). While
      previewing (textarea hidden), `Ctrl/Cmd+E` again returns to edit (textarea visible, refocused). Both
      directions work on both surfaces.
  </behavior>
  <action>
    Add a `Ctrl/Cmd+E` toggle to assets/rich-toolbar.js that survives the hidden textarea (RESEARCH A1: net-new,
    no existing `=== "e"` binding):
    - In `onFieldKeyDown`, handle `Ctrl/Cmd+E` (key === "e", ctrl or meta, no alt) BEFORE the coarse-pointer
      return: preventDefault and call `setMode(_previewOpen ? 'edit' : 'preview', ta)` (toggle for the field
      that currently owns the keydown). This covers the enter-preview direction (textarea still focused).
    - Add a DOCUMENT-level keydown listener (bound once, like the existing selectionchange bind in mount)
      that fires `Ctrl/Cmd+E` ONLY when the active element is not a registered textarea (i.e. while
      previewing, when the textarea is hidden/blurred): guard to a resolvable active field — the
      `_previewField` (the field currently previewing) — and call `setMode('edit', _previewField)` to exit.
      Ignore when no preview is open and no registered field context exists (no global hijack). Remove the
      document listener in unmount alongside the selectionchange teardown.
    - Do not alter the Ctrl/Cmd+Z/Y or Ctrl/Cmd+B/I handling. No planning IDs in comments.
  </action>
  <verify>
    <automated>node tests/46.1-inplace-swap.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/rich-toolbar.js binds `Ctrl/Cmd+E` in onFieldKeyDown (grep: a `key`/`=== "e"` branch calling setMode) AND a document-level keydown that toggles back to edit while previewing (fires only when the active element is not a registered textarea; guarded to `_previewField`).
    - `node tests/46.1-inplace-swap.test.js` exits 0 including the Ctrl+E and Cmd+E both-direction cases — specifically the RETURN case where plan 01 blurs the field and dispatches the keydown on `document`, which exercises THIS document-level listener (a field-only keydown would pass even if it were missing).
    - `node tests/run-all.js` exits 0.
    - unmount removes the added document keydown listener (grep: removeEventListener for the keydown handler).
    - No planning IDs in added comments.
  </acceptance_criteria>
  <done>Ctrl/Cmd+E toggles Edit<->Preview on both surfaces and keeps working while the textarea is hidden (document-level fallback); the swap test and full suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export modal lifecycle — reset preview on close + Back->Continue dirty guard</name>
  <files>assets/export-modal.js, tests/46.1-export-lifecycle.test.js</files>
  <read_first>
    - assets/export-modal.js (exportCloseDialog ~642-660 — the discard-check then `modal.classList.add("is-hidden")` at :654 + `_exportState.cleanup()`; the cleanup teardown ~995-1005; onNext ~925-950 — the step-1 branch ~926-944 that on Continue rebuilds `editor.value = md` at :937, `undoReset`, and sets `hasEditedPreview = false` at :943; onBack ~922-924; the discard-confirm shape `App.confirmDialog({ titleKey:"export.discard.title", messageKey:"export.discard.body", confirmKey:"export.discard.yes", cancelKey:"export.discard.no" })` ~646-651)
    - assets/rich-toolbar.js (the `resetPreview()` exposed in Task 1)
    - tests/30-export-stepper.test.js (buildEnv ~114+ and loadRealPdf ~70-104 — the REAL-page modal jsdom harness to CLONE for the new lifecycle test: loads add-session.html body + assets, App stub, mock PortfolioDB; drives the REAL DOMContentLoaded + #exportSessionBtn; asserts observable state. The app-stub `App.confirmDialog` is the discard-dialog seam — make it return a controllable value per case)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Copywriting — discard dialog copy; §Interaction — edits are ephemeral to this export)
  </read_first>
  <behavior>
    - Close-while-previewing -> reopen: enter export Step 2, open Preview (mousedown `[data-mode="preview"]`),
      close the modal, reopen and advance to Step 2 again — the editor is VISIBLE, the Frame is hidden, the
      Edit segment is active (no stale "mode lies" Frame); a fresh render occurs.
    - Back->Continue dirty guard: on Step 2 edit the document (hasEditedPreview true), press Back to Step 1,
      press Continue — a discard-confirm appears. Confirm -> the editor regenerates and advances to Step 2.
      Cancel -> stays on Step 1 and the prior edits remain in editor.value (not regenerated, not advanced).
  </behavior>
  <action>
    Wire the two export-modal lifecycle fixes in assets/export-modal.js and pin them with a new
    tests/46.1-export-lifecycle.test.js (cloning the 30-export-stepper buildEnv harness; run-all auto-discovers
    it by name):
    - Reset preview on close (BLOCKER 6): in `exportCloseDialog`, after the discard-check passes and BEFORE
      `modal.classList.add("is-hidden")` (~:654), call `window.RichToolbar && window.RichToolbar.resetPreview
      && window.RichToolbar.resetPreview()` so preview state never survives the close — a reopen of Step 2
      always starts in Edit with a fresh render. (resetPreview does not force focus, safe on a closing modal.)
    - Back->Continue dirty guard (Ben's decision 3): make `onNext` async and, in the step-1 branch, BEFORE it
      overwrites `editor.value = md` (:937), guard the regenerate: if `_exportState.hasEditedPreview` is true,
      `const ok = await App.confirmDialog({ titleKey:"export.discard.title", messageKey:"export.discard.body",
      confirmKey:"export.discard.yes", cancelKey:"export.discard.no" });` and if `!ok` RETURN immediately
      (stay on Step 1, editor.value + generatedMarkdown + hasEditedPreview untouched → the user's step-2 edits
      survive). If ok (or not dirty), proceed with the existing collect->build->`editor.value = md`->undoReset->
      `hasEditedPreview = false`->`exportSetActiveStep(2)` sequence unchanged. Reuse the SAME discard keys/dialog
      the close-path already uses (export.discard.*, reconciled in plan 05) — no new copy.
    - Create tests/46.1-export-lifecycle.test.js: clone the 30-export-stepper buildEnv (App stub with a
      controllable `confirmDialog`, mock DB, real DOMContentLoaded, open via #exportSessionBtn). Two cases,
      asserting OBSERVABLE state only: (1) close-while-previewing -> reopen -> `#exportEditor` visible + the
      Frame hidden + the Edit segment `is-active`; (2) dirty step-2 edit -> Back -> Continue with confirmDialog
      stubbed to resolve false -> stays on Step 1 (`.export-step.is-active` data-step === "1") and editor.value
      unchanged; then with confirmDialog true -> advances to Step 2 and editor.value is the regenerated markdown.
    - No planning IDs in added comments.
  </action>
  <verify>
    <automated>node tests/46.1-export-lifecycle.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/export-modal.js calls `RichToolbar.resetPreview()` in exportCloseDialog before hiding the modal (grep: `resetPreview` in export-modal.js).
    - `onNext` is async and awaits `App.confirmDialog` with the `export.discard.*` keys before the step-1 regenerate when `hasEditedPreview` is true; a cancelled confirm returns without overwriting editor.value.
    - tests/46.1-export-lifecycle.test.js exists, covers the close-reset case AND the dirty-guard (confirm + cancel) case, and exits 0.
    - `node tests/run-all.js` exits 0 (wave 3 — full suite green).
    - No planning IDs in added comments.
  </acceptance_criteria>
  <done>Closing the export modal resets preview so a reopen starts in Edit with a fresh render; Back->Continue prompts the discard-confirm before overwriting dirty step-2 edits (confirm regenerates, cancel preserves edits); the lifecycle test pins both and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note text → preview Frame body render | The note value is rendered to HTML in the Frame body — the one place user text becomes DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | rich-toolbar.js Frame body render (innerHTML) | high | mitigate | MdRender.render (escape-first) is the SOLE innerHTML sink, assigned to the Frame BODY only; chip + empty-state use textContent. Verified by `46.1-frame-no-surface-alt.test.js` sole-sink assertion (run in the suite). No second innerHTML write introduced by the swap. |
| T-46.1-SC | Tampering | npm installs | low | accept | Zero package installs. |
</threat_model>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification):
- **DOM/CSS classes emitted:** `.rich-toolbar-preview-chip` (floating chip), `.rich-toolbar-preview-body` (the `.note-rendered` render target). The `.rich-toolbar-preview` wrapper is reused (restyled by plan 04) and no longer carries `.note-rendered`.
- **JS behavior:** in-place swap in openPreview/closePreview (textarea `is-hidden` + Frame min-height), retargeted `renderPreview`/`buildPreviewPane`, a preview-aware focusout guard + one-shot outside-mousedown dismissal, `Ctrl/Cmd+E` toggle (field-level + document-level fallback), and a new public `resetPreview()`.
- **export-modal.js:** `resetPreview()` call on modal close; async `onNext` with the Back->Continue discard-confirm guard. **New test:** `tests/46.1-export-lifecycle.test.js` (close-reset + dirty-guard cases).

<verification>
- `node tests/46.1-inplace-swap.test.js` — exits 0 (swap both directions + Ctrl/Cmd+E).
- `node tests/46.1-frame-no-surface-alt.test.js` — exits 0 (sole-innerHTML-sink guard green).
- `node tests/run-all.js` — exits 0 (RTXT-05 indent/outdent/renumber regression + all shipped mechanisms green).
- Grep assets/rich-toolbar.js: exactly one MdRender-fed innerHTML assignment; no planning-ID comments.
</verification>

<success_criteria>
Preview replaces the editor in place (one thing on screen at a time) with the unified Frame + PREVIEW
chip; leaving refocuses the field; `Ctrl/Cmd+E` toggles both surfaces even while the textarea is hidden;
MdRender stays the sole innerHTML sink; the export below-fold reveal is reconciled to a harmless safety;
and all shipped mechanisms (undo, lists, snippets, autogrow) remain green.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-03-SUMMARY.md` when done.
</output>
