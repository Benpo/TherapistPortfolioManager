---
phase: 46.1-preview-edit-experience-redesign
plan: 07
type: execute
wave: 5
depends_on: ["46.1-02", "46.1-03", "46.1-04", "46.1-05", "46.1-06"]
files_modified: []
autonomous: false
requirements: [RTXT-01, RTXT-04, RTXT-05]

must_haves:
  truths:
    - "The full jsdom suite is green (node tests/run-all.js) and the manual WebKit probe (extended set E) is green before Ben's eyes: export in-place-swap preview visible-on-open at 1440x820 without scroll, bar pinned, editor hidden while previewing, RTL pass."
    - "On a real MacBook, at the DEFAULT (non-maximized) export Step-2 size, the persistent toolbar and a multi-line editor are usable (Gap-10 does not recur); the toolbar is always visible and never scrolls away on a long document (Gap-11)."
    - "The Edit/Preview switcher reads its CURRENT mode correctly (no green-label-contradicts-mode), the preview Frame reads as a preview (not the orange section-header), and manual scrolling never leaves the mode in a mixed state (Gap-14/15 fixed) — on both the 7 note fields and export."
    - "Every persistent-bar control works on FIRST click without prior focus (Gap-12), preview opens in view (Gap-13), snippets quick-paste + autogrow still work, nested lists (indent/outdent) render in preview and PDF (RTXT-05), and the carried Phase-46 items (2, 11-layout, 12, 13) pass under the new design."
    - "Verified LTR and Hebrew RTL, on real installed-Safari PWA + real iPhone (jsdom cannot see caret/scroll/RTL/PDF)."
  artifacts: []
  key_links:
    - "This is the phase's own real-device ratification gate (Ben-ratified locked process step 3). It supersedes the deferred Phase-46 46-16 preview/export items now redesigned under 46.1."
    - "WebKit probe run: node tests/webkit/46-export-step2-layout.mjs (Playwright resolved from TPM_Docs/video-pipeline/node_modules; npx playwright install webkit if stale)."
  prohibitions:
    - statement: "Declaring the phase done on jsdom-green alone (this repo has shipped false-GREEN jsdom PDF tests before; jsdom cannot see caret/scroll/RTL/PDF)."
      status: resolved
      verification: "Ben's real-device sign-off on the checklist below is required to resume; jsdom green is necessary but not sufficient."
---

<objective>
The phase's closing real-device ratification gate. Everything before this is machine-verified (jsdom
suite + WebKit probe); this gate is Ben's eyes on real hardware, because jsdom cannot see caret, scroll,
RTL-visual, or PDF behavior — and this repo has shipped false-GREEN jsdom tests before. It also re-verifies
the Phase-46 items carried into 46.1 (export Step-2 layout/flow: 46-16 items 2, 11-layout, 12, 13) under
the new design.

Purpose: prove the redesigned preview/edit experience is bulletproof on both surfaces, LTR + Hebrew RTL,
on a real MacBook + real iPhone Safari — the locked-process step-3 device gate.
Output: Ben's ratification (or a gap list routing to a gap-closure round).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-VALIDATION.md
@.planning/phases/46-rich-text-toolbar-editor/46-UAT.md
@tests/webkit/46-export-step2-layout.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Machine gate — full suite + WebKit probe green</name>
  <files>none (runs the suite + manual WebKit probe; no source files modified)</files>
  <read_first>
    - tests/webkit/46-export-step2-layout.mjs (the extended set E — the manual real-geometry probe; header documents how to run it)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-VALIDATION.md (Sampling Rate — phase gate = full suite green + WebKit probe green + Ben's device gate)
  </read_first>
  <action>
    Run the full automated gate and the manual real-geometry probe, and confirm both green BEFORE handing to
    Ben:
    - `node tests/run-all.js` — the full jsdom suite (all 46.1-* tests GREEN plus no regression; was 191/191
      at Phase 46 close, now higher with the new files).
    - `node tests/webkit/46-export-step2-layout.mjs` — the WebKit probe with the extended set E (export
      in-place-swap visible-on-open at 1440x820 without scroll, pinned bar, editor hidden while previewing,
      RTL pass). Resolve Playwright from the pinned install (TPM_Docs/video-pipeline/node_modules); if the
      WebKit binary is stale, `npx playwright install webkit` first. This probe is manual — not part of
      npm test — by design.
    Capture the probe output (RED/GREEN per assertion set) for the checkpoint summary.
  </action>
  <verify>
    <automated>node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/run-all.js` exits 0 (all 46.1-inplace-swap / switcher-current-state / format-in-preview / frame-no-surface-alt tests GREEN; no regression).
    - `node tests/webkit/46-export-step2-layout.mjs` reports GREEN for set E (recorded in the checkpoint summary); a RED set-E assertion blocks the human gate.
  </acceptance_criteria>
  <done>The full jsdom suite and the WebKit set-E probe are green; results captured for Ben's device gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-device / real-PDF ratification of the preview/edit redesign (MacBook + iPhone, LTR + Hebrew RTL)</name>
  <files>none (verification-only — produces 46.1-07-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (the LOCKED interaction contract being ratified)
    - .planning/phases/46-rich-text-toolbar-editor/46-UAT.md (gaps 14/15 fixed here; carried items 2, 11-layout, 12, 13 re-verified under the new design)
  </read_first>
  <action>
    Deploy the branch to the Phase 44 pre-prod Cloudflare Pages environment (the second CF Pages project,
    e.g. the sg-prpr-*.pages.dev URL — NOT the production deploy branch the docs-gate anchors to), then
    present the surfaces below to Ben for confirmation on real hardware. This is human-only verification:
    jsdom cannot see caret, scroll, RTL-visual, or PDF behavior, and this repo has shipped false-GREEN jsdom
    tests before. No code changes here — do not close the phase until Ben confirms every item or defects are
    captured and routed to a gap-closure round (/gsd-plan-phase --gaps).
  </action>
  <verify>
    <human-check>Ben confirms the redesigned preview/edit experience passes on real MacBook (installed Safari PWA) + real iPhone Safari, LTR + Hebrew RTL, per the how-to-verify checklist; and the WebKit set-E probe is green.</human-check>
  </verify>
  <done>Ben types "approved" after every checklist item passes, or defects are captured in 46.1-07-SUMMARY.md and routed to /gsd-plan-phase --gaps.</done>
  <what-built>
    The redesigned preview/edit experience on BOTH rich-text surfaces: the 7 session note fields and the
    export Step-2 flow. A two-segment CURRENT-STATE Edit/Preview switcher pinned at the end of the toolbar
    (outside the scrollable formatting strip); an in-place swap (Preview replaces the editor in the same box,
    one view at a time); the unified Frame preview treatment with a floating "PREVIEW" chip (never the orange
    section-header background); formatting dims while previewing and a click returns you to editing; Ctrl/Cmd+E
    toggles preview. The shipped mechanisms survive: the persistent always-visible export toolbar, first-click
    dispatch without prior focus, autogrow, snippets, undo/redo, nested lists, and the 46-15 sizing floor.
  </what-built>
  <how-to-verify>
    Verify on a REAL installed-Safari PWA (MacBook) AND a REAL iPhone Safari, in BOTH English (LTR) and
    Hebrew (RTL). jsdom cannot see any of this — your eyes are the gate.

    NOTE FIELDS (add/edit session, all 7 fields):
    1. Focus a note field with formatted text — the toolbar docks above it. Click Preview: the field is
       REPLACED in place by a framed rendered view with a "PREVIEW" chip; it does NOT read like the orange
       "Limiting Beliefs" section header (Gap 15). Click Edit (or any formatting button, or Ctrl/Cmd+E): you
       return to the textarea, refocused.
    2. The switcher shows where you ARE — the green segment matches the current mode (never green + a label
       naming the other mode) (Gap 14). Scrolling never leaves a mixed edit/preview state.
    3. On a phone width, the formatting buttons scroll but the Edit/Preview switch stays visible.
    4. Snippets quick-paste and autogrow still work; accepting a snippet with Enter inside a list does not
       add a spurious bullet.

    EXPORT (Step 2):
    5. At the DEFAULT (non-maximized) size on the MacBook, the toolbar and a multi-line editor are usable —
       NOT a clipped sliver (Gap 10). The toolbar is ALWAYS visible and never scrolls away on a long
       document (Gap 11).
    6. Every toolbar control works on the FIRST click before you focus the editor (Gap 12). Click Preview:
       the framed preview appears IN VIEW (not below the fold) (Gap 13), replacing the editor in place.
    7. Maximize works; phone shows the full-screen takeover. Edits apply to this export only; the discard
       dialog reads "Discard your export edits?" / "Discard" / "Keep editing".

    BOTH SURFACES: repeat the key steps in Hebrew (RTL) — the switcher pins at the correct end, the chip
    sits on the correct side, directional icons (indent/outdent/undo/redo) mirror, semantic icons (bold,
    italic, eye, pencil) do not. Export a session with nested bullet lists to a real PDF and confirm the
    nesting renders (RTXT-05) with Hebrew RTL intact.
  </how-to-verify>
  <resume-signal>Type "approved" if every item passes, or list the failing items (surface, LTR/RTL, device) to route a gap-closure round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note text → preview render | Verification confirms the escape-first render behaves on real devices; no new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | preview render path | high | mitigate | Verified upstream (MdRender sole sink, source-audit green in plans 03/04). This gate confirms no visible raw-HTML/token leakage in the rendered preview on real devices. |
| T-46.1-SC | Tampering | npm installs | low | accept | Zero package installs across the phase. |
</threat_model>

## Artifacts this phase produces

New symbols created by THIS plan: none (verification-only). Consumes the artifacts of plans 01-06.

<verification>
- `node tests/run-all.js` — exits 0.
- `node tests/webkit/46-export-step2-layout.mjs` — set E green.
- Ben's real-device sign-off ("approved") on the checklist above — required to resume.
</verification>

<success_criteria>
The redesigned preview/edit experience is ratified on real hardware (MacBook installed-Safari PWA + iPhone
Safari, LTR + Hebrew RTL): current-state switcher, in-place swap, unified Frame preview, always-visible
export toolbar, first-click dispatch, below-fold-free preview, snippets/autogrow/undo/nested-lists intact,
and the carried Phase-46 export items pass under the new design. Ben's "approved" closes the phase; any
failing item routes a gap-closure round.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-07-SUMMARY.md` when done.
</output>
