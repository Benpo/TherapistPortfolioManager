---
phase: 46.1-preview-edit-experience-redesign
plan: 02
type: execute
wave: 2
depends_on: ["46.1-01"]
files_modified:
  - assets/rich-toolbar.js
  - tests/46-persistent-bar-dispatch.test.js
  - tests/30-export-stepper.test.js
autonomous: true
requirements: [RTXT-01, RTXT-04]

must_haves:
  truths:
    - "The bar renders a two-segment current-state Edit/Preview switcher (`[data-mode=\"edit\"]` / `[data-mode=\"preview\"]`) pinned OUTSIDE the horizontally-scrollable strip `.rich-toolbar-scroll`; the active segment carries `is-active` AND `aria-pressed=\"true\"` (the inactive segment `aria-pressed=\"false\"`), inside a `role=\"group\"` container with an accessible label — the state reflects where the user IS (Gap-14 fix)."
    - "The old single target-state preview toggle is GONE: the `preview` entry is removed from the scrollable GROUPS, the labeled-preview branch of makeButton is removed, and `updatePreviewButton` (the rejected target-state eye/pencil swap) is deleted along with ALL FOUR of its call sites (dockTo x2, openPreview, closePreview)."
    - "The two shipped tests that drove preview through the now-deleted `[data-action=\"preview\"]` control are retargeted to the new switcher: tests/46-persistent-bar-dispatch.test.js Case B and tests/30-export-stepper.test.js case F drive preview via the `[data-mode=\"preview\"]` segment (real mousedown) and assert the segment's state signal (`aria-pressed`/`is-active`) instead of the deleted button; Case B's 'preview did NOT focus the field' assertion is preserved under the new driver. The full suite can go green again."
    - "Clicking a mode segment routes through `setMode(mode)`, which toggles the active segment, sets `.is-preview` on the bar, and opens/closes the preview for the bar's own field (resolved bar->field, works without prior focus on the persistent export bar)."
    - "Clicking a formatting button while previewing returns to Edit and applies the action in a single gesture."
    - "Every shipped mechanism survives byte-intact: the Gap-16 container mousedown guard, the `_barField` bar->field dispatch, the MdRender sole innerHTML sink, undo/redo routing, list/heading transforms, snippets Enter-yield, autogrow."
  artifacts:
    - assets/rich-toolbar.js
  key_links:
    - "buildToolbar() serves BOTH the shared note-field bar AND each persistent export bar, so adding the switcher there gives both surfaces it for free (RESEARCH Open Question 2)."
    - "The mode segments dispatch through the existing `_dispatch` bar->field resolution (`_barField.get(ownBar)`), so the persistent export switcher acts on the export editor even before it is focused."
  prohibitions:
    - statement: "A single target-labelled toggle whose active (green) styling contradicts its label (the rejected old design: green + 'Edit' label while actually previewing)."
      status: resolved
      verification: "node tests/46.1-switcher-current-state.test.js exits 0 — the active segment always matches the current mode; two distinct segments, not one target-state toggle."
    - statement: "Planning IDs (D-NN, 'Gap 14', task/phase refs) written into shipped code comments."
      status: resolved
      verification: "git diff assets/rich-toolbar.js contains no 'Gap 1', 'D-', '46.1', '46-15', or task-ID tokens in added comment lines (CONVENTIONS.md §Comments, FIXED)."
---

<objective>
Replace the rejected single target-state preview toggle with a two-segment CURRENT-STATE Edit/Preview
switcher pinned outside a horizontally-scrollable formatting strip, on BOTH rich-text surfaces (the 7
note fields AND the persistent export bar), by editing the ONE `buildToolbar()` they share. This is the
Gap-14 fix: the switcher shows where you ARE (active segment = current mode), never a green label that
contradicts the mode.

Purpose: the interaction-model half of the redesign — which buttons to show and how the mode reads at a
glance. The in-place-swap mechanics land in plan 03 (same file, next wave); this plan wires a working
switcher over the existing preview open/close so an intermediate build already reads correctly.
Output: rewired `buildToolbar` / `makeButton` / `_dispatch` in `assets/rich-toolbar.js` with a
`setMode(mode)` current-state controller.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-RESEARCH.md
@.planning/phases/46.1-preview-edit-experience-redesign/46.1-PATTERNS.md
@.planning/sketches/008-export-preview-edit-model/index.html
@.planning/sketches/009-note-field-preview/index.html
@assets/rich-toolbar.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bar restructure — scroll strip + pinned current-state switcher; remove the old target-state toggle; retarget the two shipped preview tests</name>
  <files>assets/rich-toolbar.js, tests/46-persistent-bar-dispatch.test.js, tests/30-export-stepper.test.js</files>
  <read_first>
    - assets/rich-toolbar.js (buildToolbar lines ~217-247; GROUPS ~125-146 — the `preview` entry is GROUPS[4]; makeButton ~161-184 with its `spec.action === "preview"` labeled branch; updatePreviewButton ~603-624 — the rejected target-state eye/pencil swap. `updatePreviewButton` has EXACTLY FOUR call sites: dockTo :297 (persistent branch) and :305 (shared branch), openPreview :632, closePreview :673. NOTE: hideToolbar (~308-313) does NOT call it directly — it reaches it only via `closePreview()` at :311. All four call sites must go when the function is deleted.)
    - tests/46-persistent-bar-dispatch.test.js (Case B, ~119-136 — resolves `.rich-toolbar-btn[data-action="preview"]`, mousedowns it, asserts `aria-pressed="true"` + pane visible + rendered value + the load-bearing `activeElement !== taP` no-soft-keyboard assertion; this is the coverage to preserve under the new driver)
    - tests/30-export-stepper.test.js (case F, ~374-423 — asserts `[data-action="preview"]` exists on the persistent export bar and drives preview through it; keep its MdRender-stub idiom ~384-386 and its `.rich-toolbar-preview` pane assertions, only retarget the driver)
    - .planning/sketches/008-export-preview-edit-model/index.html (canonical: TB_HTML wrapped in `<span class="tb-scroll">`, SWAP_HTML appended AFTER it as a sibling; `.swap` two-segment control with `data-mode="edit"`/`data-mode="preview"`, active segment `.on`)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-PATTERNS.md (naming-drift table — this plan's shipped-namespace names below)
  </read_first>
  <behavior>
    - After buildToolbar, all formatting groups (bold/italic/lists/heading dropdown/indent/outdent/undo/redo)
      are inside a single element `.rich-toolbar-scroll`; the switcher `.rich-toolbar-swap` is a SIBLING
      after it (never inside the strip). `tests/46.1-format-in-preview.test.js` pinned-outside-strip assertion passes.
    - Exactly two mode segments exist: `.rich-toolbar-swap-btn[data-mode="edit"]` and `[data-mode="preview"]`.
      While editing, the edit segment has `is-active` + `aria-pressed="true"`, the preview segment has neither
      (`aria-pressed="false"`); the container is `role="group"` with an accessible label.
    - No `[data-action="preview"]` control remains anywhere in the bar.
    - The two shipped preview tests are GREEN under the new driver: tests/46-persistent-bar-dispatch.test.js
      Case B and tests/30-export-stepper.test.js case F both open preview via `[data-mode="preview"]` and read
      the segment's `aria-pressed`/`is-active` state; Case B still asserts the field is NOT focused.
  </behavior>
  <action>
    <!-- planner-discipline-allow: data-action="preview" -->
    Rewire `buildToolbar()` in assets/rich-toolbar.js to the shipped-namespace structure (naming decided
    here per PATTERNS naming-drift; state these names in code as ordinary class strings, no planning IDs):
    - Wrap the formatting controls in a NEW strip element `div.rich-toolbar-scroll` — move the
      `GROUPS.forEach(...)` group/separator/heading-dropdown appends to append into this strip instead of
      directly onto `bar`. The strip is the horizontal-scroll container going forward.
    - Append a NEW switcher element `span.rich-toolbar-swap` (role="group", aria-label from
      `t("toolbar.aria", ...)` or a mode aria string) as a sibling AFTER the strip, so it is pinned at the
      bar's logical end and never scrolls. It contains two `button.rich-toolbar-swap-btn`:
      one `data-mode="edit"` (pencil icon + label `t("toolbar.backToEdit", "Edit")`) and one
      `data-mode="preview"` (eye icon + label `t("toolbar.preview", "Preview")`). Reuse the existing
      `ICONS.pencil` and `ICONS.preview` (eye) SVGs. Give each segment the per-control focus-preservation
      binding (bindPreserveFocus) so a click never blurs the field, and wire it to dispatch its mode
      (see Task 2 `_dispatch` mode branch). Set ARIA state on each segment: `aria-pressed` mirrors
      `is-active` — at build the edit segment gets `aria-pressed="true"`, the preview segment
      `aria-pressed="false"`; `setMode` (Task 2) flips both `is-active` and `aria-pressed` together so the
      pressed state always matches the current mode (this is what plan 01's switcher test and the retargeted
      WebKit probe read).
    - Remove the `preview` entry from GROUPS (it is GROUPS[4], the last group). Remove the
      `spec.action === "preview"` labeled-button branch from makeButton (the `.rich-toolbar-btn--labeled`
      preview button and its label span). Delete `updatePreviewButton` entirely (it encodes the rejected
      target-state eye<->pencil / "Preview"<->"Edit" swap) and remove ALL FOUR of its call sites: the two in
      dockTo (:297 persistent branch, :305 shared branch), and the two one-liner `updatePreviewButton()`
      calls inside openPreview (:632) and closePreview (:673). Deleting those two one-liners is a MECHANICAL
      deletion permitted here so the file has no dangling reference mid-wave (a literal executor would
      otherwise ship a ReferenceError on the first preview open); the REST of openPreview/closePreview
      internals (the in-place swap) stay untouched in this plan — plan 03 owns that rewire. The switcher's
      active-state is driven by `setMode` instead.
    - Retarget the two shipped preview tests (same commit that deletes `data-action="preview"`, so the suite
      stays green): in tests/46-persistent-bar-dispatch.test.js Case B, resolve the control via
      `bar.querySelector('.rich-toolbar-swap-btn[data-mode="preview"]')` (NOT `[data-action="preview"]`),
      drive it with the existing real-`mousedown` helper, and assert the segment reports `aria-pressed="true"`
      + `is-active` after activation — KEEP the pane-visible, rendered-value, and `activeElement !== taP`
      (no-soft-keyboard) assertions verbatim. In tests/30-export-stepper.test.js case F, resolve the driver
      the same way (`.rich-toolbar-swap-btn[data-mode="preview"]`) via mousedown; keep the `.rich-toolbar-preview`
      pane + injected-MdRender-output assertions (the pane still docks `afterend` the editor). Do NOT alter
      any other case in either file.
    - Keep VERBATIM the container-level `bar.addEventListener("mousedown", ev.preventDefault)` focus guard at
      buildToolbar (~232); `ensurePersistentBar` / `_barField` wiring; `makeSeparator`; the heading dropdown
      injection after the list group.
    - RTL glyph marking (NET-NEW — the shipped toolbar has NO mirroring today): add an extra `icon-flip`
      class in makeButton to the DIRECTIONAL buttons only (action `indent`, `outdent`, `undo`, `redo`) so
      their glyphs can mirror under RTL; the CSS rule lands in plan 04 using the app's `html[dir="rtl"]`
      convention. Semantic glyphs (bold, italic, and the eye/pencil switcher icons) must NOT get icon-flip.
    Set the initial active segment to edit (edit segment gets `is-active` at build). Do NOT add any
    `data-tour-*` attribute to the new elements (the tour anchors none of these — RESEARCH §Guided Tour).
  </action>
  <verify>
    <automated>grep -q 'rich-toolbar-scroll' assets/rich-toolbar.js && grep -q 'rich-toolbar-swap-btn' assets/rich-toolbar.js && grep -q 'aria-pressed' assets/rich-toolbar.js && node --check assets/rich-toolbar.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/rich-toolbar.js emits `rich-toolbar-scroll` and `rich-toolbar-swap` and `rich-toolbar-swap-btn` class strings; `document.querySelector('.rich-toolbar-scroll [data-mode]')` returns null in a built bar (switcher is outside the strip); each segment carries `aria-pressed` and the container is `role="group"`.
    - The old preview-action control, `updatePreviewButton`, and the labeled-preview class no longer occur in assets/rich-toolbar.js (grep-clean), and no dangling call to the deleted function remains (node --check parses; all four call sites gone).
    - Behavioral greens (switcher flip, retargeted shipped tests) are gated in Task 2 — this task's machine gate is structural + parse-clean because the segment→mode wiring lands with `setMode` in Task 2.
    - No added comment line in the diff contains a planning ID (`grep -nE '(Gap [0-9]|D-[0-9]|46\.1|46-1[0-9]|Task [0-9])'` over added comment lines returns nothing).
  </acceptance_criteria>
  <done>The bar renders a pinned two-segment current-state switcher (aria-pressed on each segment, role=group container) outside a scroll strip; the old target-state toggle and updatePreviewButton (all four call sites) are gone; the two shipped preview tests are retargeted to the switcher; the file parses with no dangling reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: setMode current-state controller + mode dispatch + formatting-click-in-preview returns-and-applies</name>
  <files>assets/rich-toolbar.js</files>
  <read_first>
    - assets/rich-toolbar.js (_dispatch ~689-739 — the `preview` action at :737; the persistent bar->field branch at :697-702 where the view-only 'preview' path resolves the field WITHOUT focusing it (:698-701); the FOCUS-ANCHOR block at :711-717 (`if (!_everFocused.has(barField)...` → `barField.focus(); dockTo(barField);`) which DOES focus the field — the new mode branch must sit BEFORE this block; openPreview/closePreview/togglePreview ~626-686; the no-soft-keyboard resolve-without-focus note ~677-680; refreshButtonState ~447-465)
    - .planning/sketches/008-export-preview-edit-model/index.html (setModeB ~1003-1015: toggles `.on` on segments, `.is-muted` on the bar, hides/shows editor, renders on preview, refocus on edit; formatting-click-in-preview returns to edit first at ~982)
    - .planning/sketches/009-note-field-preview/index.html (setMode ~613-635 canonical; formatting click returns to edit then applies ~579)
    - .planning/phases/46.1-preview-edit-experience-redesign/46.1-UI-SPEC.md (§Interaction Contract §The mode model — LOCKED)
  </read_first>
  <behavior>
    - `setMode('preview')` marks the preview segment `is-active` + `aria-pressed="true"` (edit segment
      inactive + `aria-pressed="false"`), sets `.is-preview` on the bar, and opens the preview for the
      resolved field. `setMode('edit')` reverses both attributes and refocuses the field.
    - Clicking the Preview mode segment on the persistent export bar works with NO prior focus AND does NOT
      focus the field (resolves bar->field WITHOUT focusing — the no-soft-keyboard invariant): after a
      persistent-bar Preview click, `document.activeElement !== field`. This replaces the coverage the
      retargeted Case B used to provide.
    - Clicking a formatting button while previewing first returns to Edit, then applies — the field value
      changes and the mode is edit afterward (`tests/46.1-format-in-preview.test.js` return-and-apply passes).
    - The retargeted shipped tests are GREEN: tests/46-persistent-bar-dispatch.test.js and
      tests/30-export-stepper.test.js both open preview via `[data-mode="preview"]` and read `aria-pressed`.
  </behavior>
  <action>
    Add a `setMode(mode, field)` current-state controller to assets/rich-toolbar.js and route the mode
    segments and formatting-in-preview through it:
    - `setMode(mode, field)`: resolve the target field (passed `field`, else the bar's `_barField`, else
      `_focused` / `_previewField`); on THAT field's bar (barFor(field)) toggle `is-active` AND `aria-pressed`
      TOGETHER on the two `[data-mode]` segments (preview segment active+pressed for 'preview', edit segment
      active+pressed for 'edit'); toggle `.is-preview` on that bar (the CSS hook plan 04 uses to dim the
      formatting strip while previewing); then call the existing `openPreview(field)` for 'preview' or
      `closePreview()` for 'edit'. This reuses the shipped preview open/close (the in-place-swap upgrade to
      those functions lands in plan 03 — do NOT change their internals here).
    - In `_dispatch`, add a mode branch AT THE TOP of the decision logic — placed BEFORE the focus-anchor
      block at :711-717 (the `if (!_everFocused.has(barField)...` selection-anchor + `barField.focus()` +
      `dockTo(barField)`), i.e. right after `ownBar`/`barField` are resolved (:695-696) and alongside/replacing
      the existing view-only `if (action === "preview")` branch at :698-701. If the clicked element resolves
      to a mode segment (`el.closest('[data-mode]')`), call `setMode(seg.dataset.mode, barField || _focused)`
      and RETURN — never falling through to the focus-anchor block. Placing it before that block is what keeps
      a Preview click from focusing the export editor (preserving the no-soft-keyboard invariant the shipped
      view-only path guarantees at :698-701 / :677-680); plan 01's persistent-bar switcher test asserts
      `activeElement !== field` after a Preview click.
    - Formatting-click-in-preview returns-and-applies: in the formatting/undo/redo/heading arm of `_dispatch`,
      if the preview is currently open for the resolved field, call `setMode('edit', field)` FIRST (which
      closes preview + refocuses), THEN proceed with the transform — so a single click on Bold while
      previewing exits preview and applies bold. Keep the transform path (doEmphasis / insertListMarker /
      applyHeading / indent / outdent / undo / redo) otherwise unchanged.
    - Replace the old `case "preview": togglePreview();` handling with the setMode mode branch above (the old
      `preview` action string no longer exists as a control; togglePreview stays as the open/close helper
      that setMode and Ctrl+E, added in plan 03, both call).
    - Keep refreshButtonState driven as today; the switcher active-state is owned by setMode, not
      refreshButtonState. No planning IDs in added comments.
  </action>
  <verify>
    <automated>node tests/46.1-switcher-current-state.test.js && node tests/46.1-format-in-preview.test.js && node tests/46-persistent-bar-dispatch.test.js && node tests/30-export-stepper.test.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/rich-toolbar.js defines `setMode` and calls it from the `_dispatch` mode branch (grep: `function setMode` and `el.closest('[data-mode]')` or equivalent present); the branch sits before the :711 focus-anchor block.
    - `node tests/46.1-switcher-current-state.test.js` exits 0 and `node tests/46.1-format-in-preview.test.js` exits 0 (return-and-apply + pinned-outside-strip both pass).
    - `node tests/46-persistent-bar-dispatch.test.js` exits 0 (retargeted Case B: preview via `[data-mode="preview"]`, aria-pressed asserted, field NOT focused) and `node tests/30-export-stepper.test.js` exits 0 (retargeted case F).
    - Run-all wave-timing (this is a WAVE-2 plan): do NOT gate on `node tests/run-all.js` exit-0 here — by design `tests/46.1-inplace-swap.test.js` stays RED until plan 03 (wave 3, in-place swap + Ctrl/Cmd+E) and `tests/46.1-frame-no-surface-alt.test.js` stays RED until plan 04 (wave 2, app.css Frame restyle). Running run-all is expected to show EXACTLY those two 46.1 files red and NO other new failures; every previously-green test (list/undo/heading/snippets + the two retargeted shipped tests above) must stay green. Full run-all exit-0 is gated in plan 03.
    - The persistent export switcher opens preview on FIRST click with no prior focus AND does not focus the field (covered by the retargeted Case B + the format-in-preview harness mounting a persistent field).
  </acceptance_criteria>
  <done>setMode drives the current-state switcher (is-active + aria-pressed together) on both surfaces via the shared bar->field dispatch, placed before the focus-anchor block so Preview never focuses the field; formatting-in-preview returns to Edit and applies in one gesture; the switcher/return-and-apply tests and the two retargeted shipped tests are green; run-all shows only inplace-swap + frame-no-surface-alt still red (owned by plans 03/04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note text → preview render | The switcher opens the preview; the render path (innerHTML) is unchanged by this plan (still the shipped MdRender sole sink). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46.1-01 | Tampering / Elevation | rich-toolbar.js preview render | high | mitigate | Render path untouched here; the sole-innerHTML-sink guard (`46.1-frame-no-surface-alt.test.js`, run in the suite) stays green. No new innerHTML assignment introduced. |
| T-46.1-SC | Tampering | npm installs | low | accept | Zero package installs in this plan. |
</threat_model>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification):
- **JS:** `setMode(mode, field)` function; mode-segment dispatch branch in `_dispatch` (placed before the focus-anchor block).
- **DOM/CSS classes + ARIA emitted:** `.rich-toolbar-scroll` (scroll strip), `.rich-toolbar-swap` (`role="group"` switcher), `.rich-toolbar-swap-btn` + `.rich-toolbar-swap-btn.is-active` + `aria-pressed` on each segment, `.rich-toolbar.is-preview` (bar mode flag), `[data-mode="edit"]` / `[data-mode="preview"]` markers, `.icon-flip` (on the directional indent/outdent/undo/redo buttons, for RTL mirroring).
- **Removed:** the `preview` GROUPS entry, `updatePreviewButton` + all four call sites, the `.rich-toolbar-btn--labeled` preview button, the old preview-action attribute.
- **Retargeted tests (this plan owns them exclusively):** tests/46-persistent-bar-dispatch.test.js, tests/30-export-stepper.test.js — driven via the new `[data-mode="preview"]` segment.

<verification>
- `node tests/46.1-switcher-current-state.test.js` — exits 0.
- `node tests/46.1-format-in-preview.test.js` — exits 0.
- `node tests/46-persistent-bar-dispatch.test.js` — exits 0 (retargeted). `node tests/30-export-stepper.test.js` — exits 0 (retargeted).
- `node tests/run-all.js` — NOT gated to exit-0 in this wave-2 plan: `46.1-inplace-swap.test.js` (awaits plan 03) and `46.1-frame-no-surface-alt.test.js` (awaits plan 04) are the only permitted-red 46.1 files; no other new failure. Full run-all green is gated in plan 03.
- Grep assets/rich-toolbar.js: no `data-action="preview"`, no `updatePreviewButton`, no planning-ID comments.
</verification>

<success_criteria>
Both rich-text surfaces render a pinned, two-segment, current-state Edit/Preview switcher outside a
scrollable formatting strip; the rejected target-state toggle is gone; formatting-in-preview returns and
applies; all shipped mechanisms (Gap-16 guard, bar->field dispatch, undo/list/heading transforms,
snippets Enter-yield) remain green. Plan 03 upgrades the preview open/close to the in-place swap.
</success_criteria>

<output>
Create `.planning/phases/46.1-preview-edit-experience-redesign/46.1-02-SUMMARY.md` when done.
</output>
