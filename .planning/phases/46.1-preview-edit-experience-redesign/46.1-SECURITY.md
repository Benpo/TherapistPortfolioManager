---
phase: 46.1
slug: preview-edit-experience-redesign
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: 2026-07-22
---

# Phase 46.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| note text → `MdRender.render` → Frame BODY innerHTML | The redesigned preview Frame assigns innerHTML to the Frame BODY only, exclusively from the escape-first `MdRender.render` output — the single sanctioned innerHTML sink | Session note text (sensitive therapy content) |
| Frame chip / empty-state → textContent | The Frame chip and empty-state are set via `textContent` / `data-i18n`, never innerHTML — no HTML emitted from these surfaces | Session note text / static i18n strings |
| preview styling (`--color-surface-alt`) → CSS rule | The Gap-15 styling change is CSS-only; the render path (`rich-toolbar.js`) is untouched — no new DOM sink introduced by the visual redesign | none (presentation only) |
| automated-green (jsdom) → real-device truth | jsdom cannot see raw-HTML/token leakage in the rendered preview; the 46.1-07 gate confirms no visible leakage on real devices | Verification evidence |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-46.1-01 | Tampering / Elevation | `rich-toolbar.js` Frame body preview render (innerHTML) | high | mitigate | `MdRender.render` (escape-first) is the SOLE innerHTML sink, assigned to the Frame BODY only; chip + empty-state use `textContent`. Encoded as a guard: `tests/46.1-frame-no-surface-alt.test.js` asserts the sole-sink invariant and (Gap-15) that no `--color-surface-alt` appears in the preview rule; run in the suite and kept green across the CSS-only / copy-only / docs-only plans, which leave the render path untouched | closed |
| T-46.1-SC | Tampering | Supply chain: npm installs | low | accept | Zero package installs across the phase (vanilla PWA, no build step); jsdom already present. No install task exists. Package Legitimacy Audit verdict: N/A (AR-46.1-01) | closed |

*Status: open · closed — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-46.1-01 | T-46.1-SC | Zero package installs across all 7 plans (vanilla PWA, no build step; jsdom already present) — no supply-chain exposure introduced | plan-time threat model (all 7 plans) | 2026-07-22 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-07-22 | 2 | 2 | 0 | /gsd-secure-phase orchestrator (ASVS L1 grep-depth; short-circuit — register authored at plan time; sole-innerHTML-sink control verified: `escapeHtml` runs first in `md-render.js`, guard test `tests/46.1-frame-no-surface-alt.test.js` present) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-07-22
