---
phase: 46.1-preview-edit-experience-redesign
verified: 2026-07-19T22:43:37Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 46.1: Preview & Edit Experience Redesign Verification Report

**Phase Goal:** A finalized, Ben-ratified preview/edit experience across BOTH rich-text surfaces (the 7 session note fields AND the export Step-2 flow) — designed first (sketch round → UI-SPEC), implemented second (RED-first), closed by the phase's own real-device gate. Fixes phase-46 gaps 14 (mode model read inverted) and 15 (preview read as a section header), and re-verifies the carried Phase-46 export items 2 / 11-layout / 12 / 13 under the new design.
**Contract:** `46.1-UI-SPEC.md` **including its 2026-07-19 Amendment** (formatting bar is INERT during preview — "dimmed means dead", Ben decision B; supersedes the originally ratified return-and-apply mockup behavior).
**Verified:** 2026-07-19T22:43:37Z (local 2026-07-20)
**Status:** passed
**Re-verification:** No — initial verification

**Build identity:** the working tree's shipped source is byte-identical to the device-gate-approved pre-prod build `3d5cbaf` — `git diff --stat 3d5cbaf..HEAD` touches only `.planning/` (ROADMAP, STATE, 46.1-07-SUMMARY). Everything verified below IS the approved build.

## Goal Achievement

### Observable Truths (merged from ROADMAP goal + UI-SPEC/Amendment + plan `must_haves`)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **Current-state Edit/Preview switcher (Gap-14 fix):** two segments `[data-mode="edit"]`/`[data-mode="preview"]` pinned OUTSIDE the scrollable strip; active segment = CURRENT mode, green fill + `is-active` + `aria-pressed` moving together; the rejected target-state toggle is GONE | ✓ VERIFIED | `rich-toolbar.js:224-248` (segments built with `aria-pressed`, `role`d group with `toolbar.modeAria` label), `:894-906` (`setMode` flips both signals together); `app.css:5492-5517` (`.rich-toolbar-swap` `flex:0 0 auto`, `.is-active` = `--color-primary` + `--color-text-inverse`). Old selector dead: `grep -rn 'data-action="preview"' assets/ tests/` → zero matches anywhere. Executed: `tests/46.1-switcher-current-state.test.js` + retargeted `tests/46-persistent-bar-dispatch.test.js` (drives `[data-mode="preview"]`, line 124) + `tests/30-export-stepper.test.js` (line 408) all pass in the 205/205 suite; WebKit probe E1/E7 pass in a real engine. |
| 2 | **In-place swap on both surfaces + Ctrl/Cmd+E:** preview REPLACES the editor in the same box (textarea gets `is-hidden`, Frame sized to `offsetHeight` BEFORE the hide so the box never collapses); leaving preview restores + refocuses (setMode's edit branch); `Ctrl/Cmd+E` toggles both directions and still reaches while the textarea is hidden | ✓ VERIFIED | `rich-toolbar.js:746-762` (openPreview sizes then hides), `:824-836` (restoreEditSurface), `:1141-1146` (field-level enter direction w/ stopPropagation), `:1155-1167` (document-level return fallback, guarded to `_previewOpen`). Behavioral: `tests/46.1-inplace-swap.test.js` passes; WebKit probe E4 (frame visible on open, no manual scroll), E6 (`#exportEditor` collapsed while previewing), E7 (editor returns on Edit-segment mousedown) — all PASS in real WebKit, LTR AND RTL (E8 pass). |
| 3 | **Frame preview treatment (Gap-15 fix):** `background: var(--color-surface)` + `border: 1.5px solid var(--color-green-border-ghost)` (dark: `--color-border`) + floating `.rich-toolbar-preview-chip` "PREVIEW" pill with eye glyph; NEVER `--color-surface-alt`; `MdRender.render` is the SOLE innerHTML sink, assigned to the frame BODY only; empty state built via `textContent` | ✓ VERIFIED | `app.css:3663-3678` (exact Frame treatment; the 8 `--color-surface-alt` occurrences in app.css are all outside the preview rule region); `rich-toolbar.js:683-700` (chip via createElement/createTextNode), `:707-736` (`body.innerHTML = window.MdRender.render(val)` — the file's only innerHTML write; empty state + fallback via textContent). Executed: `tests/46.1-frame-no-surface-alt.test.js` (region-scoped source audit + sole-sink guard) passes. |
| 4 | **Formatting bar INERT during preview (UI-SPEC Amendment 2026-07-19 — the contract; supersedes return-and-apply):** while previewing, every control except the mode switcher is dead — dispatch drops the click BEFORE the focus-anchor block (no mode change, no formatting, no dropdown, no focus steal); strip dims to `opacity: 0.4`; controls carry `aria-disabled="true"` composed with the heading-line and empty-history dims; leaving preview RE-DERIVES per-control state; the switcher never dims | ✓ VERIFIED | `rich-toolbar.js:1025-1032` (`if (_previewOpen && _previewField === (barField \|\| _focused)) return;` sits before the focus-anchor block), `:519-575` (aria-disabled composition + re-derivation in refreshButtonState), `:917` (setMode re-derives on mode change); `app.css:5525-5527` (dim scoped to `.rich-toolbar-scroll .rich-toolbar-btn`; switcher is a sibling outside the strip). Executed: `tests/46.1-format-in-preview.test.js` — retargeted to the Amendment ("clicking Bold while previewing does nothing — still previewing, value unchanged"; aria-disabled on/off cases) — passes. Amendment commit `db8aceb` present in history. Per the Amendment this retargeting is contract-conformance, NOT drift. |
| 5 | **Export Step-2 content-driven sizing + always-visible toolbar (carried 46-16 items 2 / 11-layout):** card is `block-size: auto` capped `94vh/94dvh` (maximized `92`), the `280px` `.export-editor` floor is the sizing floor, NO bare `50dvh` anywhere in app.css; the persistent bar is `flex-shrink: 0` + `position: sticky; inset-block-start: 0` inside the scrolling edit area | ✓ VERIFIED | `app.css:3360-3379` (content-driven card + caps), `:3590-3591` (`.export-editor { min-block-size: 280px }`), `:3419-3425` (sticky, flex-shrink:0); `grep -n "50dvh" app.css` → zero. Executed in real WebKit: probe sets A (bar unclipped), B (editor ≥140px visible), C at 1000×700 (genuine-overflow precondition + bar stays pinned after scroll-to-bottom), D (maximize ≥0.85×viewport) all PASS. |
| 6 | **Carried 46-16 items 12/13 under the new design:** every persistent-bar control resolves its target from the clicked control's OWN bar (`_barField`) — works on FIRST click with no prior focus; entering preview is visible-on-open (in-place swap makes it inherent; `revealPreviewPane` retained as a harmless no-op safety, retargeted to the Frame, not deleted blindly) | ✓ VERIFIED | `rich-toolbar.js:1007-1023` (bar→field resolution ahead of mode/format dispatch), `:794-821` (revealPreviewPane kept, export-scoped, live-rect + vertical-only = RTL-safe). Executed: probe E1 "preview opened on first click with no prior focus (Gap 12)" PASS; E2 "pane revealed below the pinned bar (bar-relative)" PASS; E3 content check PASS. |
| 7 | **Export lifecycle:** closing the modal resets preview via public `RichToolbar.resetPreview()` (Edit restored, no forced focus) so reopening Step 2 never shows a stale Frame; the discard model as rebuilt and live-verified in UAT round 3 (`d8b9e5a`): closing a dirty export prompts (`export.discard.title/body/yes/no`), Back is silent + lossless, Continue with an unchanged selection is lossless, the discard decision fires at the SECTION-TOGGLE moment (`export.discard.bodySections`) | ✓ VERIFIED | `export-modal.js:685-698` (dirty-close confirm + resetPreview on close), `:1026-1056` (selection-change discard + dirty tracking vs `generatedMarkdown`); `rich-toolbar.js:865-873` (resetPreview → closePreview, bar UI included, never focuses). Executed: `tests/46.1-export-lifecycle.test.js` (4 cases: reset-on-close; Back→Continue-unchanged no dialog + undo intact; section-toggle fires dialog, cancel reverts; confirm rebuilds + clears dirty) passes. Note: plan 03's original truth said the dialog fires on Continue-from-step-1; the shipped model moved it to the section-toggle moment in UAT round 3, re-verified live by Ben inside the open device gate — in-phase revision, tests retargeted, not drift. |
| 8 | **Four-locale copy contract + HTML defaults:** every new/changed key ships in en/he/de/cs — `toolbar.previewChip`/`previewEmptyTitle`/`previewEmptyBody`/`backToEdit`/`preview`/`modeAria`, `export.next1`+`next2` = "Continue", `export.discard.*` incl. the new `bodySections`, ephemeral note + Step-2 helper reference the Edit/Preview switch; `export.done` stays "Done" (Ben-ratified deviation); `add-session.html` defaults agree with EN | ✓ VERIFIED | Parity grep: each key present ×1 (or matching count) in all four `i18n-*.js`. EN values match UI-SPEC verbatim (`i18n-en.js:501-542`), including the accepted title+body empty-state split ("Nothing to preview yet" / "Start typing above."). `add-session.html:461-462` defaults equal the EN values; stepper labels Choose · Edit · Export at `:435-445`. |
| 9 | **RED-first scaffold green + machine gate + prohibitions:** the 14 `46.1-*.test.js` files (4 planned scaffold + export-lifecycle + the UAT-round suites) are auto-discovered by `tests/run-all.js` glob and pass; full suite **205 passed / 0 failed / 205 total** (executed this verification, exit 0 — matches `ls tests/*.test.js | wc -l` = 205, so every file ran); no test asserts module internals (`_previewOpen`/`_previewField`/`_focused` — zero refs in the scaffold files), no `.click()` on controls (real `mousedown` events) | ✓ VERIFIED | `node tests/run-all.js` executed by this verifier: `Suite: 205 passed, 0 failed, 205 total`. Prohibition greps ran clean (see Prohibitions table). |
| 10 | **Bold/italic toggle per the ratified 11-clause behavior contract** (`46.1-BOLD-SEMANTICS-RESEARCH.md`, Ben-ratified 2026-07-19: D1 swap-on-collision, D2 empty-pair caret, D3 mixed→apply): state-based toggle over real grammar spans, character-identical to `md-render.js` `applyInline` | ✓ VERIFIED | `text-edit.js:112-386` (`emphasisSelection`/`emphasisCaret` operating on the tokenized span grammar; header documents the character-identical grammar contract); `rich-toolbar.js:477,1053-1054` routes bold/italic through it. Contract research doc present (243 lines, 11 clauses). Executed: `tests/46.1-emphasis-toggle.test.js`, `tests/46.1-emphasis-noop-zero-editinsert.test.js`, `tests/46.1-emphasis-emit-acceptance.test.js` all pass in the suite (incl. the sweep/fuzz-involution hardening from `eaea002`). |

**Score:** 10/10 truths verified (0 present, behavior-unverified — every state-transition truth is exercised by a passing behavioral test or real-engine probe, not presence alone)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `assets/rich-toolbar.js` | Switcher, setMode, in-place swap Frame, inert-preview dispatch, Ctrl/Cmd+E, resetPreview, preview-aware focusout | ✓ VERIFIED | 1281 lines; all mechanisms read at source (lines cited above); sole-innerHTML-sink invariant holds; one-shot outside-mousedown dismissal for note fields (`:769-792`), persistent export bar excluded by design. |
| `assets/export-modal.js` | resetPreview-on-close, discard guard, persistent bar mount | ✓ VERIFIED | `RichToolbar.mount([...], { persistent: true })` at `:85`; lifecycle wiring verified (truth 7). |
| `assets/app.css` | Frame treatment, strip/swap rules, dim-in-preview, content-driven card, 280px floor, 44px phone targets | ✓ VERIFIED | All rule blocks read (lines cited above); `.rich-toolbar-swap-btn { min-height: 44px }` in the phone block (`:5656`); chip uses logical `inset-inline-start` (RTL-safe); heading-menu positioning uses PHYSICAL rect left/top per the recorded RTL gotcha (`rich-toolbar.js:639-647`). |
| `assets/i18n-en.js` / `-he.js` / `-de.js` / `-cs.js`, `add-session.html` | Four-locale copy + defaults | ✓ VERIFIED | Truth 8. |
| `assets/help-content-en.js` | Affected EN topics describe the shipped model | ✓ VERIFIED | Round-4 restructure: "Writing session notes" section (`:193`) with a dedicated "Formatting session notes" topic (`:209-229`) describing the current-state switch, Ctrl/Cmd+E, the framed PREVIEW view, and the Amendment's resting-buttons model ("The formatting buttons rest while previewing; Edit or Ctrl/Cmd+E returns you to writing"); review-export topics updated (`:350`, `:365`). The retired return-and-apply gesture copy is gone from the EN corpus. |
| `assets/changelog-content-en.js` | EN changelog entry (v1.4.0 extended, no bump — preprod-only, Ben's decision) | ✓ VERIFIED | v1.4.0 entry (`:72-91`) reworked to the current-state Edit/Preview switch + framed preview, "one clean view at a time", Ctrl/Cmd+E — matches shipped behavior, old next-state-button wording gone. |
| 14 test files `tests/46.1-*.test.js` + retargeted `46-persistent-bar-dispatch` / `30-export-stepper` | Falsifiable coverage, outcomes-only | ✓ VERIFIED | All in the passing 205; prohibition greps clean. |
| `tests/webkit/46-export-step2-layout.mjs` (set E) + `tests/webkit/46.1-list-exit-deletion.mjs` | Real-engine geometry/behavior gates | ✓ VERIFIED | Both EXECUTED by this verifier (not trusted from SUMMARY) — see Probe Execution. |
| `46.1-BOLD-SEMANTICS-RESEARCH.md`, `.planning/todos/pending/2026-07-19-triple-star-breaks-pipeline-join-invariant.md` | Ratified contract + filed out-of-scope defect | ✓ VERIFIED | Both present on disk. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `buildToolbar()` | both surfaces (shared note-field bar + persistent export bars) | single builder emits strip + switcher | ✓ WIRED | One builder (`rich-toolbar.js:222-289`); export mounts it `persistent: true` (`export-modal.js:85`) — both surfaces get the switcher for free. |
| Mode segments | `setMode(mode, barField ‖ _focused)` | `_dispatch` bar→field resolution BEFORE the focus-anchor block | ✓ WIRED | `rich-toolbar.js:1020-1024`; probe E1 proves first-click-no-focus in real WebKit. |
| `setMode` | `openPreview`/`closePreview` | plan-02 controller calls plan-03 swap internals | ✓ WIRED | `setMode` preview branch → `togglePreview`-equivalent open/close (`:894-930`); editor-hide ownership lives in open/closePreview. |
| `MdRender.render` | `.rich-toolbar-preview-body` | sole innerHTML sink, body only (chip survives re-renders) | ✓ WIRED | `:732` is the file's only innerHTML write; `tests/46.1-frame-no-surface-alt.test.js` guards it. |
| `export-modal.js` close path | `RichToolbar.resetPreview()` | public API (`:1279`), called at `:697-698` | ✓ WIRED | Reopen starts in Edit with a fresh render — `tests/46.1-export-lifecycle.test.js` case 1. |
| `tests/run-all.js` | the 14 new test files | `tests/*.test.js` glob, no config edit | ✓ WIRED | 205 files on disk = 205 run = 205 passed. WebKit probes are manual-only, never in npm test — confirmed (no reference in run-all). |
| Every preview close path | bar UI reset | `closePreview` resets segments + `is-preview` + re-derives control state | ✓ WIRED | `:846-862` (the round-4 `e144ebf` fix); `tests/46.1-preview-reset-on-field-change.test.js` in the passing suite. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Preview Frame body | `ta.value` | the live textarea being previewed (7 note fields / export editor) | Yes — real field text through escape-first MdRender; probe E3 asserts the pane rendered the field's ACTUAL text in a real engine | ✓ FLOWING |
| Export editor | `editor.value` ← `generatedMarkdown` | `buildFilteredSessionMarkdown(selected)` — real session data | Yes — dirty tracking compares against the generated buffer (`export-modal.js:1056`) | ✓ FLOWING |
| Switcher labels / chip / empty state | `t()` keys | four i18n dictionaries with EN fallbacks | Yes — keys exist in all four locales (truth 8) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full jsdom suite (single run) | `node tests/run-all.js` | `Suite: 205 passed, 0 failed, 205 total` | ✓ PASS |
| Old target-state toggle dead everywhere | `grep -rn 'data-action="preview"' assets/ tests/` | zero matches | ✓ PASS |
| No bare 50dvh (Gap-10 root cause) | `grep -n "50dvh" assets/app.css` | zero matches | ✓ PASS |
| Test-internals prohibition | grep `_previewOpen\|_previewField\|_focused` in the 4 scaffold test files | zero matches | ✓ PASS |
| `.click()` prohibition | grep `.click()` in the 4 scaffold test files | zero matches | ✓ PASS |
| Four-locale parity | per-key grep across `i18n-{en,he,de,cs}.js` | every new/changed key present in all four | ✓ PASS |
| Debt markers | `grep -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across phase-touched shipped files | zero matches | ✓ PASS |
| Build identity vs approved gate build | `git diff --stat 3d5cbaf..HEAD` | `.planning/` only — shipped source identical | ✓ PASS |

### Probe Execution

Both phase-declared WebKit probes were EXECUTED by this verifier (SUMMARY claims not trusted):

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `tests/webkit/46-export-step2-layout.mjs` | `node tests/webkit/46-export-step2-layout.mjs` | 71 PASS; **set E 12/12 GREEN** (E1-E3 preview-reveal, E4-E7 in-place swap EN, E8+E4-E7 Hebrew RTL); sets A/B/D green at both viewports; set C green at 1000×700. **Exactly 2 FAILs**, both the ACCEPTED set-C precondition vacuity REDs at 1440×820 (EN + DE, `scrollHeight-clientHeight=0` — the content-driven card fits the probe's 64-line document so the edit area never overflows; documented in the probe header, commit `e1bd8b8`, Ben signed off). Exit 1 is the probe's own strict gate on those two accepted REDs. | ✓ PASS (matches the signed-off expectation exactly — no unexpected RED) |
| `tests/webkit/46.1-list-exit-deletion.mjs` | `node tests/webkit/46.1-list-exit-deletion.mjs` | `ALL ASSERTIONS PASSED — list-exit deletion gate GREEN` (A0-A3 real-keystroke list exit via `execCommand('delete')` path, B outdent-button pure deletion) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| RTXT-01 | 01,02,03,04,05,06,07 | Formatting toolbar over the note fields (re-presentation) | ✓ SATISFIED | Toolbar set unchanged + new switcher; truths 1/4/9; device gate re-verified on real hardware. |
| RTXT-04 | 01,02,03,04,05,06,07 | Live preview toggle while editing (re-presentation) | ✓ SATISFIED | Truths 2/3/4/6; probe set E; device gate. |
| RTXT-05 | 01,03,06,07 | Nested lists render in preview and PDF | ✓ SATISFIED | `tests/46.1-mdrender-indent.test.js` in the passing suite; 46.1-07 device gate confirmed nested lists in preview AND a real PDF (Hebrew RTL included). |

No orphaned requirements: ROADMAP maps exactly RTXT-01/04/05 to Phase 46.1 (re-presentation of capability shipped in Phase 46; REQUIREMENTS.md keeps them Complete under Phase 46 — consistent, no new data-tier requirements per the phase header).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `assets/export-modal.js`, `add-session.html` | e.g. 161, 475; 82, 616 | Pre-existing planning-ID comments (Phase 34/37/45 `D-NN` refs) | ℹ️ Info | Predate this phase (`git blame`: `fc0dd6c8` 2026-07-07, `6c7fc313` 2026-07-13). This phase ADDED none: `rich-toolbar.js` and `app.css` (the files this phase rewrote) grep clean of `Gap-N`/`D-NN`/`46.1` tokens in comments — the per-plan comment-hygiene prohibition holds for all phase-added lines. Legacy cleanup is out of this phase's scope. |

No blockers, no warnings. Zero debt markers in phase-touched files.

### Human Verification Required

**None outstanding.** All visual/on-device criteria are closed by the phase's own real-device ratification gate (locked-process step 3), recorded in `46.1-07-SUMMARY.md`: Ben's final **"approved" 2026-07-20 on pre-prod build `3d5cbaf`** (sg-prpr origin) — MacBook installed-Safari PWA + iPhone Safari, LTR + Hebrew RTL, dark mode, real PDF with nested lists, full note-field (items 1-4) + export (items 5-9) checklist, plus re-verification of the carried Phase-46 items 2/11-layout/12/13 under the new design. Held open across four UAT fix rounds, each re-deployed and re-verified live before sign-off. This verifier confirmed the working tree's shipped source is byte-identical to `3d5cbaf`, so the gate's approval covers exactly the code verified here. Per phase precedent (Phase 45), a closed and evidence-backed device gate is cited, not re-opened.

### Accepted Limitations (signed off — NOT gaps)

1. **Set-C harness vacuity at 1440×820** — the two WebKit-probe set-C precondition REDs are a documented harness limitation (probe header, commit `e1bd8b8`): the content-driven card fits the 64-line test document, so the non-vacuity guard cannot arm at that viewport. Set C stays armed and GREEN at 1000×700. Re-confirmed by execution this verification.
2. **Hand-typed leading spaces before `## x` render literal in BOTH pipelines** — strict markdown kept deliberately; preview and PDF agree (no divergence), accepted manual-edit sharp edge.
3. **Hand-typed `***` pipeline divergence** — pre-existing defect, out of phase scope, filed as `.planning/todos/pending/2026-07-19-triple-star-breaks-pipeline-join-invariant.md` (confirmed present).

### Contract Supersessions Honored (not drift)

- **UI-SPEC Amendment 2026-07-19:** inert-formatting-bar-in-preview supersedes the ratified mockup's return-and-apply. Code (`db8aceb`), tests (`46.1-format-in-preview.test.js` retargeted), and the EN docs corpora (help + changelog describe the resting-buttons model; gesture copy retired) all conform to the Amendment. Verified as the contract, per the Amendment's own text.
- **UAT-round revisions inside the open gate** (docs restructure, discard-guard rebuild `d8b9e5a`, four bug fixes `e144ebf`/`e3a1460`/`3374dfa`/`73c01f9`, bold/italic contract rebuild `042afc5..eaea002`): in-phase scope, each live-verified by Ben on pre-prod before the final approval; tests retargeted accordingly. The plan-frontmatter truths they superseded (plan-02 "return-and-apply", plan-03 "Continue-from-step-1 discard prompt") are verified in their amended, Ben-ratified form.

### Gaps Summary

No gaps. All 10 merged must-have truths verify against the actual codebase by source reading AND execution: the full 205-file suite passes (run by this verifier, not trusted from SUMMARY), both WebKit probes were executed with results matching the signed-off expectations exactly (set E 12/12 GREEN; only the two accepted set-C vacuity REDs), all plan prohibitions hold under grep, four-locale parity is complete, the docs Definition of Done is satisfied in the EN corpora of record, and the device gate's human criteria are closed with the verified tree byte-identical to the approved build `3d5cbaf`.

---

_Verified: 2026-07-19T22:43:37Z_
_Verifier: Claude (gsd-verifier)_
