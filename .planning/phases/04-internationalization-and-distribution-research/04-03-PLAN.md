---
phase: 04-internationalization-and-distribution-research
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - assets/app.css
  - assets/overview.js
  - assets/add-session.js
  - assets/add-client.js
  - .planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md
autonomous: true
requirements: [I18N-04, DIST-01, DIST-02]

must_haves:
  truths:
    - "Hebrew RTL displays correctly on all 5 pages with no layout breaks"
    - "Client search input aligns correctly in RTL mode"
    - "Issue rows in add-session (severity inputs, remove buttons) render correctly in RTL"
    - "New Phase 3 features (client types, referral source, session fields) work in RTL"
    - "Distribution decision document exists with Cloudflare Pages and Lemon Squeezy rationale"
  artifacts:
    - path: ".planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md"
      provides: "Hosting and payment decision with rationale for Phase 5 implementation"
      contains: "Cloudflare Pages"
    - path: "assets/app.css"
      provides: "Any RTL fixes needed (may be unchanged if no issues found)"
  key_links:
    - from: "DISTRIBUTION-DECISION.md"
      to: "Phase 5 plans (DIST-03, DIST-04, DIST-05)"
      via: "Decision rationale informs implementation"
      pattern: "Lemon Squeezy|Cloudflare Pages"
---

<objective>
Validate and fix Hebrew RTL layout across all 5 pages (focusing on Phase 2-3 features), and write the distribution decision document for hosting (Cloudflare Pages) and payment (Lemon Squeezy).

Purpose: I18N-04 ensures all features work correctly in Hebrew RTL mode. DIST-01/DIST-02 produce the documented decisions that Phase 5 will implement. These are independent tasks grouped together as they share Wave 2 timing.
Output: RTL fixes (if any needed), distribution decision document.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-internationalization-and-distribution-research/04-CONTEXT.md
@.planning/phases/04-internationalization-and-distribution-research/04-RESEARCH.md
@.planning/phases/04-internationalization-and-distribution-research/04-01-SUMMARY.md

@assets/app.css
@assets/overview.js
@assets/add-session.js
@assets/add-client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: RTL validation and fixes for Hebrew mode across all pages</name>
  <files>assets/app.css, assets/overview.js, assets/add-session.js, assets/add-client.js</files>
  <action>
Systematically validate Hebrew RTL rendering across all 5 pages. CSS logical properties were migrated in Phase 2, so most layout should work. Focus on Phase 2-3 features that may have RTL issues.

**Validation checklist (test each in Hebrew mode):**

1. **Overview page (index.html):**
   - Client search input: text right-aligned, search icon position correct
   - Client table columns: text aligned correctly (names right-aligned)
   - Stats cards: numbers and labels aligned correctly
   - Client modal: buttons, stats, notes display correctly
   - Daily greeting and quote: text right-aligned, quote author left-aligned

2. **Add Session page (add-session.html):**
   - Client dropdown: text right-aligned
   - Inline client form fields: labels and inputs aligned RTL
   - Issue rows: severity number inputs positioned correctly, remove (X) buttons on correct side
   - Textareas (Limiting Beliefs, Additional Techniques, Important Points, Next Session Info): text right-aligned
   - Session type toggle (Practice/Online/Other): buttons ordered correctly
   - All form labels: right-aligned

3. **Add Client page (add-client.html):**
   - Client type toggles (Adult/Child/Animal/Other): button order and active state
   - Referral source dropdown and fields
   - "Other" type text input field
   - Form labels right-aligned

4. **Sessions page (sessions.html):**
   - Filter bar: elements ordered correctly RTL
   - Sessions table: column headers and data right-aligned
   - Heart Shield badge positioning

5. **Reporting page (reporting.html):**
   - Stats cards layout mirrored
   - Labels and values aligned

6. **All pages:**
   - Nav links: order renders correctly in RTL
   - Brand title/subtitle: right-aligned
   - Theme toggle: positioned correctly

**For each issue found:**
- Fix using CSS logical properties in app.css (never use directional properties like margin-left)
- If JS generates inline styles with directional properties, replace with logical equivalents or CSS classes
- Scan JS files for `style.marginLeft`, `style.paddingLeft`, `style.textAlign = "left"` or similar directional inline styles

**Search specifically for inline directional styles in JS:**
```
grep -n "marginLeft\|paddingLeft\|marginRight\|paddingRight\|textAlign.*left\|textAlign.*right\|style\.left\|style\.right" assets/*.js
```

Fix any directional inline styles found. If no RTL issues are found, document that RTL validation passed with no fixes needed.
  </action>
  <verify>
    <automated>cd /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager && echo "=== Checking for directional inline styles in JS ===" && ! grep -rn "marginLeft\|paddingLeft\|marginRight\|paddingRight" assets/overview.js assets/add-session.js assets/add-client.js assets/sessions.js 2>/dev/null && echo "PASS: No directional inline styles in JS" || echo "WARNING: Directional inline styles found (may be intentional)" && echo "=== Checking CSS for remaining directional properties ===" && grep -c "margin-left\|margin-right\|padding-left\|padding-right\|text-align.*left\|text-align.*right\|border-left\|border-right" assets/app.css | tr -d '\n' && echo " directional CSS properties (review if > 0)"</automated>
  </verify>
  <done>Hebrew RTL validated across all 5 pages. Any layout breaks in Phase 2-3 features fixed using CSS logical properties. No directional inline styles remain in JS files. All pages render correctly in both LTR and RTL modes.</done>
</task>

<task type="auto">
  <name>Task 2: Write distribution decision document</name>
  <files>.planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md</files>
  <action>
Create a distribution decision document capturing the locked decisions from CONTEXT.md for Phase 5 implementation. This is a documentation deliverable, not code.

Structure:
```markdown
# Distribution Decision: Sessions Garden

**Decided:** {date}
**Decided by:** Sapir (product owner) with research support
**Implements:** DIST-01 (Hosting), DIST-02 (Payment)
**Implementation phase:** Phase 5

## Hosting: Cloudflare Pages

**Decision:** Host Sessions Garden on Cloudflare Pages (free tier).

**Rationale:**
- Zero monthly cost (free tier: unlimited requests, 500 builds/month, unlimited bandwidth)
- Static site — no server, no backend, no runtime needed
- Automatic HTTPS
- Global CDN for fast loading worldwide
- Simple deployment: push to GitHub -> auto-deploys
- Custom domain support via CNAME

**Domain:**
- Primary: Custom domain (~10 EUR/year via Cloudflare Registrar)
- Fallback: *.pages.dev free subdomain during initial launch

**Implementation notes for Phase 5:**
- Connect GitHub repo to Cloudflare Pages
- Set build output directory (root — no build step needed)
- Configure custom domain when ready
- No environment variables needed (pure static site)

## Payment: Lemon Squeezy

**Decision:** Use Lemon Squeezy as Merchant of Record for payment processing.

**Rationale:**
- Merchant of Record model: Lemon Squeezy is the legal seller
- Handles EU VAT collection, calculation, reporting, and remittance automatically
- No monthly fees — only per-transaction: 5% + $0.50
- Built-in license key generation and delivery
- One-time purchase product setup (no recurring billing complexity)
- Receipts issued automatically to customers

**Business model:**
- One-time purchase (not subscription)
- Price TBD by Sapir
- Customer pays -> receives license key via email -> enters key in app to unlock

**Access control:**
- License key validated locally in the app (no server calls for daily use)
- Key format and validation logic to be implemented in Phase 5 (DIST-03)
- Lemon Squeezy generates keys automatically on purchase

**EU VAT compliance:**
- Lemon Squeezy as MoR handles all VAT obligations
- No VAT registration needed for Sapir personally
- Receipts with correct VAT amounts issued automatically

**Implementation notes for Phase 5:**
- Create Lemon Squeezy account
- Create product with one-time purchase pricing
- Enable license key generation for the product
- Get checkout link / embed for landing page
- Implement license key validation in app (DIST-03)

## Cost Summary

| Item | Cost | Frequency |
|------|------|-----------|
| Cloudflare Pages hosting | Free | - |
| Custom domain | ~10 EUR | Annual |
| Lemon Squeezy | 5% + $0.50 per sale | Per transaction |
| **Total fixed cost** | **~10 EUR/year** | |

## Key Priorities Met

1. **Simplicity for Sapir:** Push to GitHub = deployed. Lemon Squeezy handles payments/VAT/receipts.
2. **Reliability for customers:** Cloudflare CDN, automatic HTTPS, global availability.
3. **Minimal cost:** ~10 EUR/year fixed. No monthly fees.
```
  </action>
  <verify>
    <automated>test -f /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/.planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md && grep -q "Cloudflare Pages" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/.planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md && grep -q "Lemon Squeezy" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/.planning/phases/04-internationalization-and-distribution-research/DISTRIBUTION-DECISION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Distribution decision document exists with Cloudflare Pages hosting rationale, Lemon Squeezy payment rationale, EU VAT handling confirmation, cost summary, and implementation notes for Phase 5.</done>
</task>

</tasks>

<verification>
1. Open all 5 pages in Hebrew mode — no layout breaks, all features render correctly RTL
2. Client search, issue rows, session fields, client type toggles all work in RTL
3. DISTRIBUTION-DECISION.md exists with complete rationale for both hosting and payment
4. Distribution document covers EU VAT handling confirmation
</verification>

<success_criteria>
- Hebrew RTL validated on all 5 pages with no layout breaks
- Any directional inline style issues in JS are fixed
- Distribution decision document complete and committed
- Phase 5 has clear implementation notes for hosting and payment setup
</success_criteria>

<output>
After completion, create `.planning/phases/04-internationalization-and-distribution-research/04-03-SUMMARY.md`
</output>
