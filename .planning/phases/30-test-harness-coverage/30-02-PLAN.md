---
phase: 30-test-harness-coverage
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - tests/_helpers/jsdom-pdf-env.js
  - tests/_helpers/app-stub.js
autonomous: true
requirements: [TEST-01, TEST-03]
must_haves:
  truths:
    - "A single shared jsdom env builder exists that stubs getContext→null and loads jsPDF deterministically (D-04, TEST-01)"
    - "A reusable App.* stub with a __calls spy lets the god-module tests execute the real handlers and assert observable side effects (D-09, TEST-03)"
    - "App.initCommon() resolves a Promise so the DOMContentLoaded handlers that await it do not hang or throw (D-09 landmine)"
  artifacts:
    - path: tests/_helpers/jsdom-pdf-env.js
      provides: "buildJsdomEnv() → { dom, win }; getContext→null; jspdf/bidi/heebo/pdf-export load; deterministic setCreationDate/setFileId pinning per jsPDF instance"
      exports: ["buildJsdomEnv"]
    - path: tests/_helpers/app-stub.js
      provides: "createAppStub(overrides) → App.* surface with __calls spy; initCommon resolves; installNavGuard no-op"
      exports: ["createAppStub"]
  key_links:
    - from: tests/_helpers/jsdom-pdf-env.js
      to: node_modules/jsdom
      via: "require('jsdom').JSDOM directly — replaces the legacy /tmp jsdom env fallback"
      pattern: "require\\('jsdom'\\)"
    - from: tests/_helpers/app-stub.js
      to: tests/_helpers/mock-portfolio-db.js
      via: "mirrors the __calls Map-of-arg-arrays spy shape for observable side-effect assertions"
      pattern: "__calls"
---

<objective>
Build the two shared test helpers the god-module and PDF tests depend on:
1. `tests/_helpers/jsdom-pdf-env.js` — the ONE shared jsdom "fake browser" env (D-04) that stubs `HTMLCanvasElement.getContext`→null (the fix the 7 PDF tests are missing) and loads jsPDF + bidi + Heebo font + pdf-export with deterministic output pinning.
2. `tests/_helpers/app-stub.js` — the reusable `App.*` stub surface (D-09) with a `__calls` spy, so the Phase 30 characterization tests can load the real `settings.html`/`add-session.html` pages, inject this stub, fire `DOMContentLoaded`, and drive the REAL handlers while asserting observable side effects (D-08).

Both are test-only helpers under `tests/_helpers/`. Neither modifies any `assets/*` production file.

Purpose: These helpers are the harness that makes "go broad" (D-07) feasible without re-stubbing per test. The jsdom env unblocks TEST-01; the App stub unblocks the TEST-03 real-page approach (D-09) that survives the Phase 31 rename (D-08/D-12).
Output: `tests/_helpers/jsdom-pdf-env.js` (exports `buildJsdomEnv`), `tests/_helpers/app-stub.js` (exports `createAppStub`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared jsdom PDF env helper (D-04)</name>
  <files>tests/_helpers/jsdom-pdf-env.js</files>
  <read_first>
    - tests/quick-260620-q8m-pdf-paragraph-linebreaks.test.js (lines 61-120 — the already-green reference: the inline buildJsdomEnv, the JSDOM constructor options, the getContext stub, the jspdf/bidi/heebo asset evals, and the WrappedJsPDF deterministic-pin wrapper)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§jsdom-pdf-env.js — the load-bearing excerpt + the "text is an OWN property per instance, pin in the WrappedJsPDF wrapper not on prototype" detail at line 52; the require('jsdom') replacement)
    - .planning/codebase/TESTING.md (§PDF Tests — the established getContext→null pattern and deterministic date/fileId pinning)
    - assets/pdf-export.js (the production module the helper evals — read-only; do NOT modify)
  </read_first>
  <action>
    Extract the inline `buildJsdomEnv` from the reference test into a shared module exporting `buildJsdomEnv()` that returns `{ dom, win }` and:
    (a) acquires JSDOM via `require('jsdom').JSDOM` directly — node resolves it from the installed devDependency (30-01), replacing the legacy `JSDOM_PATH` environment fallback that pointed at the `/tmp` node-modules jsdom convention;
    (b) constructs a JSDOM with `runScripts: 'outside-only'`, `pretendToBeVisual: false`, and a `file://` URL under the repo root (mirror the reference);
    (c) stubs `win.HTMLCanvasElement.prototype.getContext` to return null BEFORE any eval — this is the exact fix the 7 broken PDF tests are missing (`getContext` count is 0 in all 7 today, 2 in the reference);
    (d) evals, in order, `assets/jspdf.min.js`, `assets/bidi.min.js`, `assets/fonts/heebo-base64.js`, `assets/fonts/heebo-bold-base64.js`, and `assets/pdf-export.js` into the window;
    (e) installs the WrappedJsPDF wrapper that applies the deterministic pins per jsPDF INSTANCE — `setCreationDate` to the pinned date string and `setFileId` to the pinned all-zero id — right after constructing each `new OriginalJsPDF(args)`, NOT on the prototype (jsPDF installs `text` as an own property on each instance, per PATTERNS line 52).
    Keep the helper's surface minimal: export `buildJsdomEnv` (and, if convenient, the pinned-constant values). Do NOT modify any `assets/*` file — the helper only reads and evals them.
  </action>
  <verify>
    <automated>node -e "const {buildJsdomEnv}=require('./tests/_helpers/jsdom-pdf-env.js');const a=require('assert');const {dom,win}=buildJsdomEnv();a.ok(dom&&win);a.strictEqual(win.HTMLCanvasElement.prototype.getContext(),null);a.ok(win.jspdf||win.jsPDF);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Requiring the helper and calling `buildJsdomEnv()` returns `{ dom, win }` where `win.HTMLCanvasElement.prototype.getContext()` returns null and the jsPDF global is defined (falsifiable: if jsdom is not installed, or the getContext stub is absent, or the asset evals fail, the smoke exits non-zero).
    - The helper acquires jsdom via `require('jsdom')` (node_modules), not via any `/tmp` path (falsifiable: `grep -L "require('jsdom')" tests/_helpers/jsdom-pdf-env.js` would flag a missing direct require).
    - No `assets/*` file is modified.
  </acceptance_criteria>
  <done>`buildJsdomEnv()` builds a jsdom window with getContext stubbed and jsPDF loaded with deterministic pinning, sourced from the installed jsdom devDependency.</done>
</task>

<task type="auto">
  <name>Task 2: Build the reusable App.* stub with a spy surface (D-09)</name>
  <files>tests/_helpers/app-stub.js</files>
  <read_first>
    - tests/_helpers/mock-portfolio-db.js (lines 50-128 — the `__calls` Map-of-arg-arrays spy shape, deep-copied args, and `assertNoWrites`; mirror this shape)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§Shared Patterns → "Spy-instrumented mock" — the grep-verified App.* surface and per-method counts; the initCommon-must-resolve and installNavGuard-no-op landmines)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Module B effort estimate + §D-09 verdict landmines — the App.* methods add-session.js/settings.js call)
    - assets/app.js (lines 116-126 setLanguage, 1346-1347 window.App surface — read-only reference for realistic default return values; do NOT modify)
  </read_first>
  <action>
    Create a `createAppStub(overrides)` factory exporting an `App`-shaped object that mirrors the `mock-portfolio-db.js` `__calls` spy (a Map of method name → array of deep-copied arg-arrays) so tests can assert observable side effects (e.g. that `App.showToast` was called with a given message).
    Expose the grep-verified surface the two god modules call: `t` (i18n shorthand — return the key, or look up a per-test-provided map), `getSectionLabel`, `showToast`, `getSeverityValue` (map a severity label to its numeric value — needed by the issue-delta test), `formatDate`, `applyTranslations`, `confirmDialog`, `isSectionEnabled`, `createSeverityScale`, `getLanguage`, `setLanguage`, `unlockBodyScroll`, `lockBodyScroll`, `setSubmitLabel`, `readFileAsDataURL`, `initBirthDatePicker`, `formatSessionType`, and CRITICALLY `initCommon` returning an already-RESOLVED Promise (landmine: the page handlers `await App.initCommon()` — if it does not resolve the handler hangs), plus `installNavGuard` as a no-op (landmine).
    Give each method a reasonable default return value and allow per-test overrides via the `overrides` argument. The factory must NOT modify `assets/app.js` — it is a standalone test double.
    Note for downstream tests: a global `BroadcastChannel` is referenced by add-session.js — tests inject a no-op stub for it on the jsdom window; document this expectation in the helper's doc block.
  </action>
  <verify>
    <automated>node -e "const {createAppStub}=require('./tests/_helpers/app-stub.js');const a=require('assert');const s=createAppStub();a.strictEqual(typeof s.t,'function');a.strictEqual(typeof s.getSeverityValue,'function');a.ok(s.initCommon() instanceof Promise);s.showToast('x');a.strictEqual(s.__calls.get('showToast').length,1);s.initCommon().then(()=>console.log('ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - `createAppStub()` returns an object exposing every listed `App.*` method (falsifiable: a missing method name fails the typeof asserts).
    - `initCommon()` returns a Promise that RESOLVES (falsifiable: a non-resolving/throwing initCommon fails the `.then` smoke).
    - Calling a spied method records its args into `__calls` (falsifiable: `__calls.get('showToast').length === 1` after one call).
    - No `assets/*` file is modified. (Full behavioral exercise of the stub happens via the consuming tests in plans 30-04/30-05/30-06.)
  </acceptance_criteria>
  <done>`createAppStub()` provides the full App.* surface with a working spy and a resolving initCommon, ready for the real-page god-module tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workbench → production | The new helpers live under `tests/_helpers/`; never deployed (Cloudflare serves static `/assets/*` only) |
| test → production source | Helpers EVAL/READ `assets/*` files into a jsdom sandbox but never write them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | `tests/_helpers/*` shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` is never in the Cloudflare static deploy; no production change |
| T-30-03 | Tampering | helper evals production `assets/*` into jsdom | low | accept | Read-only eval inside an isolated jsdom window; no write-back to source; the modules are first-party in-repo code |

No new dependency is added by this plan (jsdom installed in 30-01); no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/_helpers/jsdom-pdf-env.js` (NEW) — exports `buildJsdomEnv()` → `{ dom, win }`; getContext→null; jspdf/bidi/heebo/pdf-export load; deterministic `setCreationDate`/`setFileId` pinning per instance (D-04, TEST-01)
- `tests/_helpers/app-stub.js` (NEW) — exports `createAppStub(overrides)` → App.* surface with `__calls` spy; `initCommon()` resolves; `installNavGuard` no-op (D-09, TEST-03)

(Full phase manifest is listed in 30-01-PLAN.md.)
</artifacts>

<verification>
- jsdom-pdf-env smoke (Task 1 assert) exits 0: getContext stubbed, jsPDF loaded from the installed devDependency.
- app-stub smoke (Task 2 assert) exits 0: full method surface, working spy, resolving initCommon.
- `npm test` still green (the new helpers are not `*.test.js`, so the runner does not execute them).
- No `assets/*` production file modified.
</verification>

<success_criteria>
- The shared jsdom env helper exists and stubs getContext (D-04) — unblocks TEST-01's 7-test migration in 30-03.
- The App.* stub exists with a spy and a resolving initCommon (D-09) — unblocks the TEST-03 real-page tests in 30-04/05/06.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-02-SUMMARY.md` when done.
</output>
