---
phase: 30-test-harness-coverage
plan: 10
type: execute
wave: 2
depends_on: ["30-07"]
files_modified:
  - tests/30-export-stepper.test.js
  - tests/30-section-visibility.test.js
  - tests/30-issue-delta.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-export-stepper.test.js gains cases that execute the REAL add-session.js export modal and assert exportCloseDialog hides the modal, the MD + PDF download buttons dispatch (asserted as SEAM checks: PDFExport.triggerDownload called, not full-render coverage), exportUpdatePreview renders #exportPreview from an INJECTED MdRender stub (proving the real MdRender branch at add-session.js:1269-1274 ran, not the editor.value fallback), and the mobile-tabs path runs under matchMedia matches:true (closes GAP-10 / region B3 residuals)"
    - "tests/30-section-visibility.test.js gains a past-session case that executes sectionHasData + the past-session branch (asserts a disabled section WITH data is visible+badged while a disabled section with NO data is hidden) AND a cross-module section-label case that evals the REAL app.js getSectionLabel against a seeded custom therapistSetting label so add-session renders the therapist-visible custom title end-to-end (no stubbed getSectionLabel) — guarding the Phase-31 D-13c glue cleanup (closes GAP-11 / region B4)"
    - "tests/30-issue-delta.test.js gains cases asserting the MAX_ISSUES=3 cap (addIssueBtn disabled at 3) and the updateRemoveButtons toggle (hidden at 1 row, enabled at 2) (closes GAP-14 / region B5)"
    - "every added/strengthened case in this plan has a recorded mutation-kill (G1): the guarded behavior reverted in a scratch copy makes the test exit non-zero, restored → exit 0, noted in verify + SUMMARY"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-export-stepper.test.js
    - tests/30-section-visibility.test.js
    - tests/30-issue-delta.test.js
  key_links:
    - "all added cases extend the existing real-page jsdom buildEnv already in each file (real add-session.js execution); they update the file's EXPECTED_COUNT guard to match the new case count"
    - "assertions read observable DOM (modal visibility, #exportPreview render, button disabled/hidden state), never internal function names"
  prohibitions:
    - "no added case reads an assets/* file with fs.readFileSync for assertion purposes without executing it (the existing files already eval the module — keep that)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "do not regress or delete the existing green cases in these three files; only ADD cases and bump EXPECTED_COUNT"
    - "do not modify any assets/* production file"
---

<objective>
Strengthen three EXISTING real-page add-session tests by adding the residual-behavior cases the verification flagged as unexecuted: GAP-10 (B3 export-stepper residuals — exportCloseDialog, MD/PDF download dispatch, exportUpdatePreview's #exportPreview render, and the mobile-tabs path that today never runs because matchMedia is stubbed matches:false), GAP-11 (B4 — the past-session branch and sectionHasData, which the new-session-only cases short-circuit before reaching), and GAP-14 (B5 — the MAX_ISSUES=3 cap and the updateRemoveButtons toggle, never directly asserted).

Each file already executes the real module via the established buildEnv; this plan ADDS cases (and bumps each file's EXPECTED_COUNT guard) without disturbing the green cases. D-08/D-09 throughout. Two cases are HARDENED beyond the original plan: GAP-10's preview now injects a real MdRender stub (so the MdRender branch — not the editor.value fallback — is proven) and names the download checks as seams; GAP-11 adds a real cross-module section-label case (evals the real app.js getSectionLabel) so the therapist-visible custom title is guarded end-to-end.

This plan is **wave 2** and depends on 30-07 Task 0 (it consumes the post-extension shared helpers).

Purpose: close the medium/low add-session holes by exercising the previously-unreached branches.
Output: 3 modified `tests/30-*.test.js` files, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-export-stepper.test.js
@tests/30-section-visibility.test.js
@tests/30-issue-delta.test.js
@tests/_helpers/mock-navigator-share.js
@assets/add-session.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-10 — add export-stepper residual cases (region B3)</name>
  <files>tests/30-export-stepper.test.js</files>
  <read_first>
    - assets/add-session.js:1180-1835 (export modal): exportCloseDialog, exportHandleDownloadPdf (→ window.PDFExport.triggerDownload at :1359), exportHandleDownloadMd, exportUpdatePreview, exportApplyMobileTabs/exportWireMobileTabs
    - assets/add-session.js:1265-1275 (exportUpdatePreview: `if (window.MdRender && typeof window.MdRender.render === "function") preview.innerHTML = window.MdRender.render(editor.value); else preview.textContent = editor.value;` — without an injected MdRender stub the test only exercises the tautological editor.value fallback, NOT the real MdRender branch)
    - tests/30-export-stepper.test.js (existing buildEnv, the matchMedia stub currently returns matches:false; the EXPECTED_COUNT guard at the end of file)
    - tests/30-export-markdown.test.js:20-32 (note: #exportPreview is filled by exportUpdatePreview; assert its rendered content/visibility, distinct from #exportEditor.value)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-10 entry)
  </read_first>
  <behavior>
    - exportCloseDialog: clicking the export close control hides the export modal.
    - Download dispatch (SEAM checks): clicking the step-3 MD download and PDF download controls reaches the download seam — assert window.PDFExport.triggerDownload was called for PDF (add-session.js:1359) and the MD download anchor/dispatch fired; name these as SEAM checks, not full-render coverage.
    - exportUpdatePreview (real MdRender branch): with an INJECTED window.MdRender stub whose render() returns a recognizable marker, toggling sections updates #exportPreview to reflect the STUB's output (proving the MdRender branch at :1269-1271 ran, not the editor.value fallback).
    - Mobile tabs: with matchMedia matches:true, exportApplyMobileTabs/exportWireMobileTabs runs and the mobile tab layout is applied (observable DOM).
  </behavior>
  <action>
    Add cases to the existing file reusing its buildEnv. For the mobile case, build with a matchMedia stub returning matches:true (parameterize the existing helper or add a local variant) so the mobile path executes. For the preview case, INJECT a window.MdRender stub (render: (md) => '<p data-mdrender="1">'+md+'</p>' or similar recognizable wrapper) BEFORE boot, toggle a section, and assert #exportPreview.innerHTML reflects the STUB's wrapper output (so the real MdRender branch is proven, not the fallback). Open the export dialog via the real button, then: click exportClose → assert the modal is hidden; advance to step 3, click MD download + PDF download → assert each reached the seam (PDFExport.triggerDownload spy for PDF; the MD anchor/dispatch for MD), named as seam checks; in the matches:true env, assert the mobile-tabs DOM is applied. Reuse mock-navigator-share only where a path needs it. Bump EXPECTED_COUNT by the number of added cases. Keep all existing cases intact.
  </action>
  <verify>
    <automated>node tests/30-export-stepper.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-stepper.test.js` exits 0 with EXPECTED_COUNT increased to match the new total
    - STRUCTURAL: file still executes the real module (`win.eval(readAsset('assets/add-session.js'))`) AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-export-stepper.test.js` is non-empty
    - BEHAVIORAL: new assertions on modal hidden after exportCloseDialog; on the PDF download SEAM (PDFExport.triggerDownload called) + the MD download dispatch; on #exportPreview reflecting the INJECTED MdRender stub's output (real MdRender branch, not the editor.value fallback); on the mobile-tabs DOM under matchMedia matches:true
    - existing stepper cases remain present and green
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, neutralise the MdRender branch (force the editor.value fallback) → the #exportPreview-reflects-MdRender case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>B3 export-stepper residual paths (close, MD/PDF download seam, real MdRender preview render, mobile tabs) execute and are pinned by observable DOM; a Phase-31 break of any would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-11 — add past-session / sectionHasData case (region B4)</name>
  <files>tests/30-section-visibility.test.js</files>
  <read_first>
    - assets/add-session.js:901-940 (sectionHasData), :955-959 (new-session short-circuit), :960-969 (past-session branch incl. visible+badge when has data), :978 (applySectionLabels) and :839-887 (markdown builders call App.getSectionLabel(sectionKey, defaultI18nKey) — the therapist-visible title reader)
    - assets/app.js:58-61 (getSectionLabel returns the customLabel from _sectionLabelCache when present), :614-622 (the cache is loaded from PortfolioDB.getAllTherapistSettings during init) — this is the REAL cross-module link the test must exercise instead of the stubbed getSectionLabel
    - tests/30-issue-delta.test.js:98-125 (the pattern for evaling the REAL app.js into the jsdom window then add-session.js)
    - tests/30-section-visibility.test.js (existing buildEnv + EXPECTED_COUNT; current cases drive the NEW-session path only, :155/:179)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-11 entry)
  </read_first>
  <behavior>
    - Past-session, disabled section WITH data: when editingSession is truthy (applySectionVisibility(true)), a disabled section that HAS data renders visible + badged.
    - Past-session, disabled section with NO data: renders is-hidden.
    - Cross-module section-label (real, end-to-end): with the REAL app.js getSectionLabel in play and PortfolioDB seeded with a custom therapistSetting label for a section, add-session renders that therapist-visible CUSTOM title (not the default i18n key, not a stubbed id) — guarding the Phase-31 D-13c glue cleanup from silently regressing the visible title.
  </behavior>
  <action>
    Add a past-session case: boot the page in a past-session/editing state (editingSession truthy → applySectionVisibility(true)) so sectionHasData and the :960-969 branch actually run. Seed the form so one disabled section has data and another disabled section has none. Assert the with-data disabled section is visible and shows its badge, and the no-data disabled section has the is-hidden class.
    Add a cross-module section-label case (R11): build a variant env that evals the REAL assets/app.js (so window.App.getSectionLabel is the real reader, NOT createAppStub's id-returning stub) with PortfolioDB seeded so getAllTherapistSettings returns a custom label for a section; let app.js init populate _sectionLabelCache; then eval add-session.js and assert the rendered therapist-visible section title (the .section heading / markdown label produced via App.getSectionLabel) equals the seeded CUSTOM label. This exercises the real app.js→add-session.js title link end-to-end. If — and only if — wiring the real reader proves out of budget for this task, instead add a LOUD note in this plan's notes and the SUMMARY explicitly downgrading B4's cross-module verdict from "real" to "stubbed reader; residual D-13c risk" (do NOT silently treat the stubbed getSectionLabel as acceptable).
    Reuse the existing buildEnv where possible; bump EXPECTED_COUNT. Keep the existing new-session cases.
  </action>
  <verify>
    <automated>node tests/30-section-visibility.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-section-visibility.test.js` exits 0 with EXPECTED_COUNT increased
    - STRUCTURAL: file still executes the real module AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-section-visibility.test.js` is non-empty
    - BEHAVIORAL: new assertion that a disabled-with-data section is visible + badged in the past-session branch; new assertion that a disabled-no-data section is is-hidden
    - CROSS-MODULE: either (a) a case evals the REAL app.js getSectionLabel + a seeded custom therapistSetting label and asserts add-session renders the CUSTOM title end-to-end, OR (b) a LOUD downgrade note (in the plan notes + SUMMARY) explicitly stating the cross-module reader stays stubbed and flagging the residual D-13c risk — silent acceptance of the stubbed reader is NOT allowed
    - existing new-session cases remain present and green
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, force the past-session has-data section to hide (drop the :960-969 visible+badge logic) → the past-session case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>B4 sectionHasData + the past-session visibility branch execute and are pinned by observable DOM, and the cross-module section-label is either pinned end-to-end via the real app.js reader or its residual risk is loudly flagged; a Phase-31 break of the has-data-keeps-visible rule would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-14 — add issue-cap + remove-button toggle cases (region B5)</name>
  <files>tests/30-issue-delta.test.js</files>
  <read_first>
    - assets/add-session.js:502-505 (MAX_ISSUES=3 cap), :507-514 (updateRemoveButtons is-hidden/disabled toggle)
    - tests/30-issue-delta.test.js (existing buildEnv that eval's app.js for the real severity pair; the addIssueBtn click at the existing case; EXPECTED_COUNT)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-14 entry)
  </read_first>
  <behavior>
    - Issue cap: adding issue rows up to 3 disables addIssueBtn (addIssueBtn.disabled === true at 3 rows).
    - Remove-button toggle: at 1 row the remove button is hidden; at 2 rows it is enabled/visible.
  </behavior>
  <action>
    Add cases to the existing file reusing its buildEnv. Click addIssueBtn to grow rows to 3 → assert addIssueBtn.disabled === true. In a separate sequence, assert the remove button is hidden at 1 row and enabled/visible at 2 rows (observable disabled/hidden/class state). Bump EXPECTED_COUNT. Keep the existing delta/payload/validation cases.
  </action>
  <verify>
    <automated>node tests/30-issue-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-issue-delta.test.js` exits 0 with EXPECTED_COUNT increased
    - STRUCTURAL: file still executes the real module AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-issue-delta.test.js` is non-empty
    - BEHAVIORAL: new assertion that addIssueBtn.disabled is true at the 3-issue cap; new assertion that the remove button is hidden at 1 row and enabled at 2 rows
    - existing delta/payload/validation cases remain present and green
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, raise/remove the MAX_ISSUES cap (so addIssueBtn never disables at 3) → the cap case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>B5 issue cap + remove-button toggle are pinned by observable DOM state; a Phase-31 break of the cap or toggle would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:** none

**Modified files:**
- tests/30-export-stepper.test.js (GAP-10 residual cases + EXPECTED_COUNT bump)
- tests/30-section-visibility.test.js (GAP-11 past-session case + EXPECTED_COUNT bump)
- tests/30-issue-delta.test.js (GAP-14 cap/toggle cases + EXPECTED_COUNT bump)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A. |
</threat_model>

<verification>
- `node tests/30-export-stepper.test.js`, `node tests/30-section-visibility.test.js`, `node tests/30-issue-delta.test.js` each exit 0 with bumped EXPECTED_COUNT
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each file
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
The three medium/low add-session residual regions (B3 stepper residuals, B4 past-session, B5 cap/toggle) execute their previously-unreached branches and are guarded by observable assertions; existing cases stay green; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-10-SUMMARY.md` when done.
</output>
