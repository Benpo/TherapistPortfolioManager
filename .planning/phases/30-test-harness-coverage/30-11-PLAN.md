---
phase: 30-test-harness-coverage
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/30-autogrow-wiring.test.js
  - tests/30-field-copy.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-autogrow-wiring.test.js executes the REAL assets/add-session.js under jsdom and asserts that firing an input event on a .session-textarea sets style.height (auto then computed px) via the delegated listener, and that every .session-textarea is sized on boot/populate (closes GAP-12 / region B1 wiring — keeping the existing computeGrowHeight leaf test)"
    - "tests/30-field-copy.test.js executes the REAL assets/add-session.js under jsdom and asserts that clicking a single per-field copy button copies that field's text (buildFieldCopyText output) to the clipboard (closes GAP-13 / region B2 per-field copy)"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-autogrow-wiring.test.js
    - tests/30-field-copy.test.js
  key_links:
    - "both use the real-page jsdom buildEnv (real add-session.js execution); autogrow is driven by a real input event on a .session-textarea (the delegated listener at add-session.js:131-138), not by calling the leaf helper"
    - "field-copy asserts the captured clipboard argument, never internal function names"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism (the existing quick-260516-rna autogrow test is source-sliced for the wiring — this plan REPLACES that gap with execution, it does not copy it)"
    - "do not widen window.__addSessionTestHooks; drive autoGrow via the real input event, not the hook"
    - "do not modify any assets/* production file; keep the existing computeGrowHeight leaf test untouched"
---

<objective>
Close two narrow add-session holes with small new executing tests: GAP-12 (B1 autogrow WIRING — autoGrow's reset-to-auto-then-set DOM mutation, growAllSessionTextareas' iteration, the delegated input listener, and the populateSession grow call are currently covered ONLY by source-text regex; only the leaf computeGrowHeight is executed) and GAP-13 (B2 per-field copy — buildFieldCopyText is reachable via the per-field copy button but no test ever clicks one; export tests click only the whole-session copy/export).

Both follow D-08/D-09: drive the real page and assert observable output (style.height / clipboard arg).

Purpose: pin the autogrow wiring and per-field copy so Phase 31 cannot silently break them.
Output: 2 new `tests/30-*.test.js` files, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-export-markdown.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@assets/add-session.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-12 — characterize autogrow wiring via real input events (region B1)</name>
  <files>tests/30-autogrow-wiring.test.js</files>
  <read_first>
    - assets/add-session.js:17-34 (computeGrowHeight pure leaf — already pinned by quick-260516-rna; autoGrow at :25-29 does style.height='auto' then computed px; growAllSessionTextareas at :31-33 iterates .session-textarea), :131-138 (delegated input listener calls autoGrow(target)), :2172 (populateSession grow call)
    - tests/30-export-markdown.test.js:88-145 (buildEnv: real add-session.html + win.eval, captured DOMContentLoaded handler, settle())
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-12 entry — note quick-260516-rna:58-96 is source-sliced for the wiring; this test EXECUTES instead)
  </read_first>
  <behavior>
    - Delegated input listener: after boot, setting a .session-textarea value and dispatching an input event sets that textarea's style.height (transiently 'auto', then a computed px from scrollHeight within the min/max).
    - growAll on populate/boot: every .session-textarea is sized (style.height set) after the boot/populate path runs.
  </behavior>
  <action>
    Build the real-page env (buildEnv pattern). After boot+settle, select a real .session-textarea, set its value to a multi-line string, dispatch a real input Event, settle() → assert style.height is set to a px value (and that the reset-to-auto path ran, e.g. by asserting a non-empty computed height consistent with computeGrowHeight's min floor). Then assert that all .session-textarea nodes have a style.height set after boot/populate. Drive everything through real events — do NOT call the leaf helper or widen __addSessionTestHooks. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-autogrow-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-autogrow-wiring.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND dispatches a real input event on a .session-textarea AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-autogrow-wiring.test.js` is non-empty
    - BEHAVIORAL: assertion that style.height is set on the textarea after the input event; assertion that .session-textarea nodes are sized after boot/populate
    - the test does not widen __addSessionTestHooks (`grep -c '__addSessionTestHooks' tests/30-autogrow-wiring.test.js` is 0) and does not call computeGrowHeight directly
    - the existing quick-260516-rna-textarea-autogrow leaf test is left untouched and still green
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion
    - `npm test` stays green
  </acceptance_criteria>
  <done>B1 autogrow wiring (delegated listener + growAll) executes and is pinned by observable style.height; a Phase-31 break of the listener wiring would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-13 — characterize per-field copy (region B2)</name>
  <files>tests/30-field-copy.test.js</files>
  <read_first>
    - assets/add-session.js:1575 (per-field copy button → buildFieldCopyText); the markdown builders at 730-1180
    - tests/30-export-markdown.test.js:28-49,114-145 (the clipboard-spy approach: force win.isSecureContext=true, install navigator.clipboard.writeText spy, click the copy control, assert the captured argument)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-13 entry)
  </read_first>
  <behavior>
    - Per-field copy: clicking a single field's copy button copies that field's text (the buildFieldCopyText output for that field) to the clipboard — distinct from the whole-session copy.
  </behavior>
  <action>
    Build the real-page env. Populate a specific session field with distinctive content. Force win.isSecureContext=true and install a capturing navigator.clipboard.writeText spy (the 30-export-markdown pattern). Click the per-field copy button for that field, settle() → assert the captured clipboard argument equals the expected per-field copy text (contains the field's content, scoped to that field — NOT the whole-session markdown). Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-field-copy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-field-copy.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-field-copy.test.js` is non-empty
    - BEHAVIORAL: assertion on the captured clipboard writeText argument for a single per-field copy (contains that field's content, scoped to the field)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion, assertions on internal function names
    - `npm test` stays green
  </acceptance_criteria>
  <done>B2 per-field copy executes and is pinned by the observable clipboard argument; a Phase-31 break of per-field copy would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-autogrow-wiring.test.js (GAP-12, region B1)
- tests/30-field-copy.test.js (GAP-13, region B2)

**Modified files:** none (no assets/* changes; quick-260516-rna leaf autogrow test retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-11 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A. |
</threat_model>

<verification>
- `node tests/30-autogrow-wiring.test.js` and `node tests/30-field-copy.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
B1 autogrow wiring and B2 per-field copy are guarded by tests that execute the real module via real events and assert observable output; the existing leaf autogrow test stays green; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-11-SUMMARY.md` when done.
</output>
