---
phase: 30-test-harness-coverage
plan: 05
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-export-markdown.test.js
  - tests/30-export-stepper.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The exported session markdown (observed via the real export preview/copy output) has the correct section order, inclusion, scale labels, and headings — captured by an EXECUTING test, replacing the source-slicing anti-pattern (D-08, D-09, TEST-03c)"
    - "The export-modal stepper drives active-step class 1→2→3 and the preview text updates with section toggles (TEST-03d)"
    - "Both tests execute the real add-session.js export modal under jsdom and survive an internal rename but fail on an observable-output change (D-08, D-12)"
  artifacts:
    - path: tests/30-export-markdown.test.js
      provides: "Executing markdown-builder characterization via the real export preview/copy output"
    - path: tests/30-export-stepper.test.js
      provides: "Executing export-modal stepper state-machine + preview characterization"
  key_links:
    - from: tests/30-export-markdown.test.js
      to: assets/add-session.js
      via: "loads add-session.html + add-session.js, opens the export dialog, reads the rendered markdown preview"
      pattern: "exportUpdatePreview"
    - from: tests/30-export-stepper.test.js
      to: tests/_helpers/mock-navigator-share.js
      via: "injects createShareMock as navigator.canShare/share, asserts files-only share dispatch"
      pattern: "createShareMock"
---

<objective>
Characterize the highest-risk, lowest-covered region of `assets/add-session.js` — the export modal (Phase 31 RFCT-02's exact extraction target) — before the refactor, via EXECUTING jsdom real-page tests (D-09) that replace the existing source-slicing "tests" (the D-08/D-12 false-confidence anti-pattern):
1. `tests/30-export-markdown.test.js` — the markdown builders (the copy/export output a therapist sees): correct section order (insights after trapped, per the 260615 fix), included/excluded sections, scale labels, headings (TEST-03c).
2. `tests/30-export-stepper.test.js` — the 3-step export stepper state machine (active-step 1→2→3) and preview updates on section toggles, plus the files-only share dispatch (TEST-03d).

Both assert OBSERVABLE output only (D-08): the rendered export-preview/copy text, the active-step class, the share-mock calls — never internal function names.

Decision (resolves RESEARCH Open Question #1, for the D-12 architect pass): this phase changes NO production code, so the markdown-builder output is observed through the REAL export preview / copy path rather than by widening `window.__addSessionTestHooks`. `add-session.js`/`add-session.html` are loaded read-only.

Purpose: Establish the pre-refactor green baseline for the add-session export modal — the riskiest Phase 31 extraction (RFCT-02) — guarded by tests that execute the module and survive its internal rearrangement.
Output: `tests/30-export-markdown.test.js`, `tests/30-export-stepper.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export markdown characterization — EXECUTING, not sliced (TEST-03c, D-08/D-09)</name>
  <files>tests/30-export-markdown.test.js</files>
  <read_first>
    - assets/add-session.js (markdown builders, lines 730-1180 — `buildSessionMarkdown`, `buildFilteredSessionMarkdown`, `getCurrentSessionDataForExport`, `exportUpdatePreview`; section order mirrors the form DOM order; read-only, do NOT modify)
    - add-session.html (the session form fragment + the export dialog container the handlers bind to, incl. the data-section-key elements and the preview element)
    - tests/_helpers/app-stub.js (inject as `win.App`; the module calls App.t/getSectionLabel/showToast/etc.)
    - tests/_helpers/jsdom-pdf-env.js (use only if add-session.js references the jsPDF global at eval time; otherwise omit for the lighter markdown test)
    - tests/quick-260615-export-section-order.test.js (the ANTI-PATTERN being replaced — it `fs.readFileSync`+regex-matches function bodies; D-08 violation)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-export-markdown.test.js + §Anti-Patterns to Avoid)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Markdown builders row; §D-09 verdict for add-session.js)
  </read_first>
  <action>
    Build a jsdom real-page test that EXECUTES the export markdown path (NOT source-slicing). Load the real `add-session.html` body into a jsdom window, inject `win.App = createAppStub()` and a no-op `BroadcastChannel` stub on the window, eval `assets/add-session.js`, fire `DOMContentLoaded` (the stub's `initCommon` resolves so the handler completes), and populate the session form DOM with a known dataset (client, session fields, a couple of included sections and an excluded one).
    Observe the emitted markdown through OBSERVABLE output: open the export dialog and read the rendered export PREVIEW element's text (the markdown a therapist sees), OR drive the copy path with a clipboard stub and capture its payload. Assert: the section ORDER mirrors the form DOM order (notably `insights` appears AFTER `trapped`, per the 260615 fix), excluded sections are absent and included sections present, scale labels render, and headings are correct.
    CRITICAL (D-08/D-12): the test must jsdom-LOAD and EXECUTE add-session.js — it must NOT `fs.readFileSync` the asset and regex-match function bodies, and must NOT assert on function names or `indexOf('function ')`. Do NOT widen `window.__addSessionTestHooks` or otherwise modify add-session.js — observe the markdown through the real preview/copy output only.
    Open with the root-cause / falsifiability doc block; use the PASS/FAIL + exit-0/1 scaffold. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-export-markdown.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` exits 0 against the current `assets/add-session.js`.
    - The observed export markdown (via the real preview/copy output) has the correct section order (insights after trapped), correct inclusion/exclusion, scale labels, and headings.
    - The test jsdom-LOADS and EXECUTES add-session.js — it performs no source-slicing and has no `indexOf('function')`/regex-on-source assertions (falsifiable: a reviewer can confirm the test never `fs.readFileSync`s the asset for assertion).
    - The test FAILS if the emitted markdown changes (e.g. section order reverts) but SURVIVES an internal rename of a builder function (D-08/D-12).
    - No `assets/*` file modified; `window.__addSessionTestHooks` not widened.
  </acceptance_criteria>
  <done>The export markdown output is pinned by an executing observable-behavior test that replaces the source-slicing anti-pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Export-modal stepper state machine + preview (TEST-03d)</name>
  <files>tests/30-export-stepper.test.js</files>
  <read_first>
    - assets/add-session.js (export modal stepper, lines 1180-1835 — `openExportDialog`, `exportSetActiveStep`, `exportUpdatePreview`, `exportApplyMobileTabs`/`exportWireMobileTabs`, `exportHandleDownloadPdf`/`exportHandleDownloadMd`/`exportHandleShare`, `exportProbeShareSupport`; read-only, do NOT modify)
    - add-session.html (the export dialog markup — the stepper step elements and the section-toggle controls)
    - tests/_helpers/app-stub.js (inject as `win.App`)
    - tests/_helpers/jsdom-pdf-env.js (the PDF download path needs the jsPDF global — load it via the shared helper)
    - tests/_helpers/mock-navigator-share.js (lines 24-30 — `createShareMock({canShareReturns, shareReturns})`; assert on `mock.calls`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-export-stepper.test.js)
  </read_first>
  <action>
    Build a jsdom real-page test for the export-modal stepper state machine. Load `add-session.html` + `assets/add-session.js`, inject `win.App = createAppStub()`, a no-op `BroadcastChannel`, the jsPDF global (via the shared helper, for the PDF dispatch path), and `createShareMock()` as `navigator.canShare`/`navigator.share`. Open the export dialog (`openExportDialog`) and assert the OBSERVABLE stepper state machine: the active-step class transitions 1→2→3 as the user advances through the steps (`exportSetActiveStep`), and the preview text updates when section toggles change (`exportUpdatePreview`). Optionally assert the share dispatch calls the share mock with files-only (per the 260615 share fix) by inspecting `shareMock.calls`.
    Assert observable active-step class + preview text + share-mock calls ONLY (D-08) — never internal function names. Open with the falsifiability doc block; use the PASS/FAIL + exit-0/1 scaffold. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-export-stepper.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-stepper.test.js` exits 0 against the current `assets/add-session.js`.
    - Opening the dialog and advancing steps yields the active-step class transitioning 1→2→3 (observable); toggling a section updates the preview text (observable); if asserted, the share dispatch passes files-only to the share mock.
    - The test EXECUTES the stepper under jsdom and FAILS if the step transitions or preview update break, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` file modified; `window.__addSessionTestHooks` not widened.
  </acceptance_criteria>
  <done>The export-modal stepper state machine and preview updates are pinned by an executing observable-behavior test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | Both tests LOAD `add-session.html` + `assets/add-session.js` (+ jsPDF) into jsdom read-only; no write-back; `__testExports` NOT widened |
| test → share/clipboard | `navigator.share`/clipboard are in-repo mocks; nothing leaves the test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test files shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | export tests assert internal shape (false confidence, the D-12 risk) | medium | mitigate | D-08/D-12: tests jsdom-EXECUTE add-session.js and assert observable output (preview text, active-step class, share-mock calls); acceptance requires "no source-slicing, survives internal rename, fails on observable change" |
| T-30-06 | Tampering | widening `__addSessionTestHooks` enlarges the surface Phase 31 must preserve | low | mitigate | Decision: do NOT widen the test-hook surface; observe via the real preview/copy/share path (resolves RESEARCH Open Q#1) |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-export-markdown.test.js` (NEW — TEST-03c, D-08/D-09): executing markdown-builder characterization via the real export preview/copy output (replaces source-slicing)
- `tests/30-export-stepper.test.js` (NEW — TEST-03d): executing export-modal stepper state-machine + preview characterization

Scope note (D-13b/D-14): the export modal is `consolidate-in-P31` (RFCT-02 extraction target) — this plan pins its behavior, it does NOT refactor it. (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-export-markdown.test.js` exits 0; executes the module (no slicing); falsifiable on a section-order regression (Task 1).
- `node tests/30-export-stepper.test.js` exits 0; falsifiable on a broken step transition/preview (Task 2).
- Both survive an internal rename (D-08/D-12); `add-session.js` and `__testExports` unchanged.
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (add-session export modal): the markdown builders and the export stepper are captured as a pre-refactor green baseline that guards Phase 31's export-modal extraction (RFCT-02), with executing tests that replace the source-slicing anti-pattern.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-05-SUMMARY.md` when done.
</output>
