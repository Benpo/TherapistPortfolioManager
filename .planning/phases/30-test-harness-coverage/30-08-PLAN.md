---
phase: 30-test-harness-coverage
plan: 08
type: execute
wave: 2
depends_on: ["30-07"]
files_modified:
  - tests/30-snippet-wiring.test.js
  - tests/30-snippet-import-merge.test.js
  - tests/30-settings-saved-notice.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-snippet-wiring.test.js executes the REAL assets/settings.js IIFE-2 boot under jsdom and asserts the editor screen wiring — openEditor→edit→handleSave persists via the now-real PortfolioDB.addSnippet (add) / updateSnippet (edit) + shows the saved toast, handleDelete calls the real deleteSnippet + toasts, and renderSnippetList reflects the persisted set through App.getSnippets/refreshSnippetCache — via observable DOM/persistence-call args (closes GAP-03 editor half / region A2, keeping the 8 existing leaf tests)"
    - "tests/30-snippet-import-merge.test.js drives the REAL import plumbing (file-input change → FileReader → collision modal → applyImport, settings.js:1716-1766/1980-1984) on a seeded colliding trigger so detectImportCollisions(rows, App.getSnippets()) takes the REPLACE branch (settings.js:1837-1851): asserts updateSnippet is called with the PRESERVED existingId and the colliding trigger appears ONCE in the rendered list (closes GAP-03 import half / region A2)"
    - "tests/30-settings-saved-notice.test.js executes the REAL assets/settings.js IIFE-1 under jsdom and asserts the saved-notice pill appears on Save and auto-dismisses, and that the disable-confirm branch fires and gates persistence (closes GAP-07 / region A1)"
    - "every new test in this plan has a recorded mutation-kill (G1): the guarded behavior reverted in a scratch copy makes the test exit non-zero, restored → exit 0, noted in verify + SUMMARY"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-snippet-wiring.test.js
    - tests/30-snippet-import-merge.test.js
    - tests/30-settings-saved-notice.test.js
  key_links:
    - "depends on 30-07 Task 0: uses the now-real PortfolioDB.addSnippet/deleteSnippet/updateSnippet (store-backed spies) and App.getSnippets/refreshSnippetCache (app.js:87-104 contract) — afterSnippetMutation→refreshSnippetCache→renderSnippetList makes writes observable in the list with no per-test plumbing"
    - "settings.js registers 5 DOMContentLoaded handlers; invoke ONLY the target IIFE via the captured-listener map (IIFE-2 snippets = captured[1] boot at settings.js:1898; IIFE-1 fields = captured[0]) — never a blanket dispatchEvent"
    - "snippet persistence asserted via the mock-portfolio-db __calls spy (observable record), never via internal function names; the import test drives the real file-input→applyImport path, never the exposed leaf detectImportCollisions"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without also executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "the import-merge test must NOT prove merge by calling the exposed leaf detectImportCollisions on window.__SnippetEditorHelpers and asserting its return — it must drive the REAL plumbing (file-input change → FileReader → collision modal → applyImport)"
    - "do NOT re-add the snippet PortfolioDB methods locally — use the 30-07 Task-0 extensions; the prior 'setSnippet/save' spy named a NONEXISTENT method (real methods are addSnippet/updateSnippet/deleteSnippet)"
    - "do NOT re-test the 8 leaf snippet helpers (isTriggerUnique, validateImportPayload, detectImportCollisions, filterSnippetList, isModifiedSeed, isValidTrigger, hyphenateSpaces, getCrossLangWarning) — they are already pinned; keep those tests untouched"
    - "do not modify any assets/* production file"
---

<objective>
Close the two highest-priority `assets/settings.js` holes: GAP-03 (A2 snippet-settings SCREEN WIRING — the editor open/save/delete, import→applyImport collision merge, list render, and boot() event bindings that Phase 31 rearranges; only the 8 leaf helpers are pinned today) and GAP-07 (A1 saved-notice pill + the disable-confirm branch, which execute as side effects today but have zero assertions).

Both follow D-08 (observable behavior only) and D-09 (load the real settings.html page into jsdom and drive the real IIFE handlers). The snippet wiring is the heaviest single item in this gap round, so it is SPLIT into two tasks/files: Task 1 the editor open/save/delete + list-render flow, Task 2 the import→applyImport collision-MERGE flow (the heaviest, REPLACE-branch path). This keeps neither executor context overloaded.

This plan is **wave 2** and depends on 30-07 Task 0 (the shared-helper extension): it consumes the now-real PortfolioDB.addSnippet/updateSnippet/deleteSnippet and App.getSnippets/refreshSnippetCache rather than re-adding them.

Purpose: protect the settings snippet UI and saved-notice/disable-confirm flows against the Phase-31 extraction.
Output: 3 new `tests/30-*.test.js` files, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-settings-section-roundtrip.test.js
@tests/30-settings-tabnav.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@assets/settings.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-03a — characterize snippet editor open/save/delete + list render (region A2, IIFE-2)</name>
  <files>tests/30-snippet-wiring.test.js</files>
  <read_first>
    - assets/settings.js:1596-1612 (handleSave persist: validateSnippetShape, then `editingSnippet ? updateSnippet(candidate) : addSnippet(candidate)` at :1601-1603, then afterSnippetMutation + closeEditor + showToast("snippets.toast.saved") at :1607), :1614-1634 (handleDelete: confirmDialog → deleteSnippet(snippet.id) at :1627 → afterSnippetMutation + showToast("snippets.toast.deleted"))
    - assets/settings.js:955-961 (afterSnippetMutation → App.refreshSnippetCache → renderSnippetList) and :1116-1122 (renderSnippetList reads App.getSnippets()); boot() at :1898 with the add/save/delete/close bindings at :1916-1949
    - tests/30-settings-section-roundtrip.test.js:94-130 (the settings.html buildEnv: capture all 5 DOMContentLoaded handlers, invoke ONLY captured[1]; the captured.length===5 self-check)
    - tests/_helpers/mock-portfolio-db.js + tests/_helpers/app-stub.js AFTER 30-07 Task 0 (real addSnippet/updateSnippet/deleteSnippet + App.getSnippets/refreshSnippetCache)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-03 entry — note captured[1] is the snippets boot)
  </read_first>
  <behavior>
    - openEditor→edit→handleSave (ADD): clicking add, entering a trigger + expansion, and Save calls the REAL PortfolioDB.addSnippet with the expected trigger/expansion/updatedAt (DB spy), shows the snippets.toast.saved toast (App.showToast spy), and the new trigger appears in the rendered list (App.getSnippets after refreshSnippetCache).
    - handleSave (UPDATE branch): editing a seeded snippet and Save calls updateSnippet (NOT addSnippet) — no duplicate row; updatedAt bumped.
    - handleDelete: confirming delete calls the REAL deleteSnippet(id), shows snippets.toast.deleted, and the row leaves the rendered list.
    - renderSnippetList reflects the persisted set (assert list item contents/count, not the function name).
  </behavior>
  <action>
    Build the settings.html env with the 30-settings-section-roundtrip pattern (capture all 5 DOMContentLoaded handlers, keep the captured.length===5 self-check, invoke ONLY captured[1] — the snippets boot at settings.js:1898 — await it, settle()). Inject the Task-0 mock PortfolioDB (store-backed addSnippet/updateSnippet/deleteSnippet) and the Task-0 App stub (getSnippets/refreshSnippetCache + t returning a distinguishable string for snippets.toast.saved). Drive observable user actions: click the add control, set the trigger + expansion inputs, click Save → assert the DB spy recorded an addSnippet with the expected fields AND App.showToast recorded snippets.toast.saved AND the new trigger is in the rendered list. Open an existing seeded snippet, edit, Save → assert updateSnippet fired (not addSnippet) and no duplicate. Trigger delete (confirmDialog stub true) → assert deleteSnippet fired + snippets.toast.deleted + the row is gone. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-snippet-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-snippet-wiring.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/settings.js'))` AND invokes the captured snippets boot handler (captured[1]) AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-snippet-wiring.test.js` is non-empty
    - BEHAVIORAL: assertion on the addSnippet spy args for the ADD path; assertion that the UPDATE path calls updateSnippet (not addSnippet) with no duplicate; assertion that handleDelete calls deleteSnippet; assertion that App.showToast recorded snippets.toast.saved and snippets.toast.deleted; assertion on rendered snippet-list contents after each mutation
    - the 8 existing leaf snippet tests (24-05-*, quick-260619-okw-*, quick-260620-p3f, quick-260626-h5j) are left untouched and still green
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of settings.js for assertion, assertions on internal function names (openEditor/handleSave/renderSnippetList existence)
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, swap handleSave's add-vs-update branch (always addSnippet) → the UPDATE-branch case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>The snippet editor wiring (open/save add+update, delete, list render, boot bindings) is pinned by observable DOM + persistence; a Phase-31 extraction that broke any of these would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-03b — characterize the import→applyImport collision-MERGE (REPLACE branch)</name>
  <files>tests/30-snippet-import-merge.test.js</files>
  <read_first>
    - assets/settings.js:1716-1766 (handleImport → file input click; handleImportFile: FileReader.readAsText → JSON.parse → validateImportPayload → `cache = App.getSnippets()` at :1748 → `detectImportCollisions(rows, cache)` at :1749 → if collisions, default each decision to "replace" and openCollisionModal), :1980-1984 (the snippetImportApply button → applyImport(pendingImport.rows, .collisions, .decisions))
    - assets/settings.js:1826-1866 (applyImport: REPLACE branch at :1837-1845 preserves `collision.existingId` and calls updateSnippet; the no-collision ADD branch at :1846-1851 calls addSnippet) — the REPLACE branch is reachable ONLY when App.getSnippets() returns an existing colliding snippet, which is why 30-07 Task-0 App.getSnippets (mirrored to DB writes) is required
    - tests/30-settings-section-roundtrip.test.js:94-130 (settings.html buildEnv; captured[1] snippets boot; captured.length===5 self-check)
    - tests/_helpers/mock-portfolio-db.js + app-stub.js after 30-07 Task 0
  </read_first>
  <behavior>
    - REPLACE/merge: with an existing snippet whose trigger COLLIDES with an imported row (so detectImportCollisions(rows, App.getSnippets()) is non-empty and the default decision is "replace"), confirming the import calls updateSnippet with the PRESERVED existingId (settings.js:1842), NOT addSnippet, and the colliding trigger appears EXACTLY ONCE in the rendered list afterward (no duplicate).
  </behavior>
  <action>
    Build the settings.html env (captured[1] snippets boot). Seed mockDb.snippets with a snippet whose trigger will collide (e.g. trigger ";hi", a known existingId), boot, and ensure App.getSnippets() reflects it (refreshSnippetCache after boot, or boot already refreshes). Drive the REAL import plumbing — do NOT call the leaf detectImportCollisions: build a JSON payload (Blob/File) containing a row with the SAME trigger ";hi" (different expansion), dispatch a change event on the import file input with that file (the real handler reads it via FileReader → detectImportCollisions sees the collision → opens the collision modal with the default "replace" decision), settle(), then click the real snippetImportApply button → applyImport runs the REPLACE branch. Assert: the updateSnippet spy was called with id === the seeded existingId (preserved), addSnippet was NOT called for that trigger, and the rendered snippet list contains the trigger ";hi" exactly once. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-snippet-import-merge.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-snippet-import-merge.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/settings.js'))` AND invokes captured[1] AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-snippet-import-merge.test.js` is non-empty
    - DRIVES REAL PLUMBING: the test fires a change event on the import file input (FileReader path) and clicks the real apply button — it must NOT call window.__SnippetEditorHelpers.detectImportCollisions and assert its return as the coverage mechanism
    - BEHAVIORAL: assertion that updateSnippet was called with the PRESERVED existingId (REPLACE branch, settings.js:1842), that addSnippet was NOT called for the colliding trigger, and that the colliding trigger appears EXACTLY ONCE in the rendered list
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of settings.js for assertion, assertions on internal function names, calling the leaf detectImportCollisions to prove merge
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, drop the `id: collision.existingId` preservation in the REPLACE branch (so it adds a new id) → the existingId / once-in-list assertion exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>The import collision-MERGE REPLACE branch is pinned by driving the real file-input→applyImport path and asserting the preserved existingId + single rendered row; a Phase-31 break that turned a collision into a duplicate-add would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-07 — characterize saved-notice + disable-confirm (region A1)</name>
  <files>tests/30-settings-saved-notice.test.js</files>
  <read_first>
    - assets/settings.js:311-347 (showSavedNotice / dismissSavedNotice + NOTICE_AUTO_DISMISS timer), :448 (onSave), :464-480 (computeDisableTransitions confirm-on-disable branch)
    - tests/30-settings-section-roundtrip.test.js:94-130 (settings.html buildEnv; invoke ONLY captured[0] = IIFE-1 fields handler; settle() + fake-timer note)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-07 entry — note 25-11-toast-behavior Scenario 5 is NOT real coverage here; it is being replaced in 30-12)
  </read_first>
  <behavior>
    - Saved notice: after a Save, the #settingsSavedNotice element becomes visible (dataset.active / not hidden), and after the auto-dismiss interval it hides again.
    - Disable-confirm: flipping a section-enable toggle OFF triggers the disable-confirm dialog and gates persistence (no setTherapistSetting write when the confirm is declined; write when accepted).
  </behavior>
  <action>
    Build the settings.html env; capture handlers and invoke ONLY captured[0] (IIFE-1). Use a controllable timer (override the env's setTimeout/clearTimeout or fast-forward) so the auto-dismiss is deterministic. Case 1: perform a Save via the real onSave path → assert #settingsSavedNotice is visible, then advance the timer → assert it is hidden. Case 2: toggle a section-enable control OFF with App.confirmDialog stubbed to resolve false → assert NO persistence write (assertNoWrites or DB spy count unchanged); repeat with confirmDialog true → assert the write occurs. Assert observable DOM/persistence only. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-settings-saved-notice.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-saved-notice.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/settings.js'))` AND invokes captured[0] AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-settings-saved-notice.test.js` is non-empty
    - BEHAVIORAL: assertion that the saved-notice element becomes visible on Save and hidden after the auto-dismiss timer; assertion that the disable-confirm dialog gates persistence (no write on decline, write on accept) via the DB spy / assertNoWrites
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of settings.js for assertion, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of settings.js, remove the disable-confirm gate (persist regardless of the confirm result) → the decline-no-write case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>A1 saved-notice timing and disable-confirm gating are pinned by observable DOM + persistence; a Phase-31 break of the notice or the confirm gate would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-snippet-wiring.test.js (GAP-03a — editor open/save/delete + list render, region A2)
- tests/30-snippet-import-merge.test.js (GAP-03b — import→applyImport REPLACE/merge, region A2)
- tests/30-settings-saved-notice.test.js (GAP-07, region A1)

**Consumes (from 30-07 Task 0, not modified here):** tests/_helpers/mock-portfolio-db.js (addSnippet/updateSnippet/deleteSnippet), tests/_helpers/app-stub.js (getSnippets/refreshSnippetCache)

**Modified files:** none (no assets/* changes; the 8 leaf snippet tests retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-08 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A (no DB schema changes). |
</threat_model>

<verification>
- `node tests/30-snippet-wiring.test.js` and `node tests/30-settings-saved-notice.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
The settings snippet screen wiring (A2) and the saved-notice + disable-confirm flows (A1) are guarded by tests that execute the real module and assert observable behavior; the 8 leaf snippet tests remain green; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-08-SUMMARY.md` when done.
</output>
