---
phase: 30-test-harness-coverage
plan: 04
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-settings-section-roundtrip.test.js
  - tests/30-settings-tabnav.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Editing a section title, saving, and reloading persists and re-renders the value — the documented save/load round-trip gap is closed (D-07, D-08, D-09, TEST-03a)"
    - "The ?tab= URL param selects the active settings tab, switching tabs writes the URL, and an invalid tab falls back to default (TEST-03b)"
    - "Both tests EXECUTE the real settings.js under jsdom and survive an internal rename but fail on an observable-output change (D-08, D-12)"
  artifacts:
    - path: tests/30-settings-section-roundtrip.test.js
      provides: "jsdom real-page save→reload round-trip over settings.js IIFE-1"
    - path: tests/30-settings-tabnav.test.js
      provides: "jsdom/vm tab-nav observable behavior over settings.js IIFE-3"
  key_links:
    - from: tests/30-settings-section-roundtrip.test.js
      to: tests/_helpers/mock-portfolio-db.js
      via: "asserts mockDb.__calls.get('setTherapistSetting') captured the persisted record"
      pattern: "setTherapistSetting"
    - from: tests/30-settings-section-roundtrip.test.js
      to: assets/settings.js
      via: "loads settings.html + settings.js, fires DOMContentLoaded, drives the real onSave handler"
      pattern: "DOMContentLoaded"
---

<objective>
Characterize the genuinely-uncovered observable behavior of `assets/settings.js` before the Phase 31 refactor (D-07 "go broad", scoped by D-10 to the NONE/uncovered rows only):
1. `tests/30-settings-section-roundtrip.test.js` — the section-title save/load round-trip (IIFE-1), the documented gap (TESTING.md "What Is NOT Tested: Settings save/load round-trips"), via the jsdom real-page approach (D-09).
2. `tests/30-settings-tabnav.test.js` — the `?tab=` URL-param tab navigation (IIFE-3), currently uncovered.

Both tests assert OBSERVABLE behavior only (D-08): what is persisted, what re-renders, which tab is active, what the URL becomes — never internal function names. They EXECUTE the real module under jsdom (D-09), so they fail on an observable-output change but survive the Phase 31 internal rename (D-08/D-12). Per D-10 the already-pinned IIFE-2/4/5 (snippets/backups/photos, ~2,200 lines covered by the 25-xx suite) are NOT re-tested. No `assets/*` file is modified — settings.js/settings.html are loaded read-only.

Purpose: Establish the pre-refactor green baseline for settings.js that the Phase 31 extraction (RFCT-01) is guarded by.
Output: `tests/30-settings-section-roundtrip.test.js`, `tests/30-settings-tabnav.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section-title save/load round-trip (TEST-03a, D-09 jsdom real-page)</name>
  <files>tests/30-settings-section-roundtrip.test.js</files>
  <read_first>
    - assets/settings.js (IIFE-1 SettingsPage, lines 14-688 — the `DOMContentLoaded` handler at :643 runs `loadAndRender`; `onSave` at :448 persists via `PortfolioDB.setTherapistSetting`; reads via `PortfolioDB.getAllTherapistSettings`; read-only, do NOT modify)
    - settings.html (the section-title editor form fragment — the inputs and Save/Discard controls the handlers bind to)
    - tests/_helpers/mock-portfolio-db.js (lines 50-128 — `createMockPortfolioDB`, the `__calls.get('setTherapistSetting')` arg-array spy, `getAllTherapistSettings` read-stub, `assertNoWrites`)
    - tests/_helpers/app-stub.js (the App.* stub from 30-02 — inject as `win.App`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-settings-section-roundtrip.test.js — the mock-DB wiring excerpt + the settings.js anchors)
    - .planning/phases/30-test-harness-coverage/30-RESEARCH.md (§Settings save/load round-trip code example; §D-09 verdict for settings.js IIFE-1)
  </read_first>
  <action>
    Build a jsdom real-page test (Pattern 2). Load the real `settings.html` body fragment into a jsdom window, inject `win.PortfolioDB = createMockPortfolioDB({ therapistSettings: [seeded section labels] })` and `win.App = createAppStub()`, eval `assets/settings.js`, and fire `DOMContentLoaded` so the :643 handler runs `loadAndRender` and renders the section-title rows from `getAllTherapistSettings`.
    Then drive the REAL handlers to prove the round-trip: edit a section-title input's value, trigger the Save control (the `onSave` path at :448), and assert the mock DB captured the persisted record — `mockDb.__calls.get('setTherapistSetting')` has at least one call whose argument carries the edited value. Then re-render (re-run the load path or re-fire load) and assert the rendered input shows the saved value (the reload half of the round-trip).
    Assert OBSERVABLE persistence and re-render ONLY (D-08): the captured DB record and the rendered value — never which internal function wrote it, and never a function name. Open the file with the root-cause / falsifiability doc block per `feedback-behavior-verification`; use the repo PASS/FAIL + exit-0/1 scaffold. Load settings.js/settings.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-settings-section-roundtrip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-section-roundtrip.test.js` exits 0 against the current `assets/settings.js`.
    - Editing a section title and saving persists a record observable via `mockDb.__calls.get('setTherapistSetting')` carrying the edited value; a reload re-renders that value (the round-trip).
    - The test jsdom-LOADS and EXECUTES settings.js (it does NOT `fs.readFileSync`+regex the source) and would FAIL if the saved value were dropped or not re-rendered, but SURVIVE an internal rename of the writing/render function (D-08/D-12 — verifiable by renaming an internal helper in a scratch copy).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The settings section-title save→reload round-trip is pinned by an executing observable-behavior test, closing the documented gap.</done>
</task>

<task type="auto">
  <name>Task 2: Settings tab navigation via ?tab= (TEST-03b)</name>
  <files>tests/30-settings-tabnav.test.js</files>
  <read_first>
    - assets/settings.js (IIFE-3 tab nav, lines 2035-2113 — `readUrlTab`, `writeUrlTab`, `boot`, `activate`; read-only, do NOT modify)
    - settings.html (the tab markup — the tab buttons/panels and their identifying attributes)
    - tests/_helpers/app-stub.js (inject as `win.App`)
    - tests/25-11-i18n-parity.test.js (the vm/jsdom loader + PASS/FAIL + exit-0/1 scaffold)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-settings-tabnav.test.js — assert observable active-tab class + window.location.search, never internal fn names)
  </read_first>
  <action>
    Build a test (jsdom real-page preferred) for settings.js IIFE-3. Load `settings.html` + `assets/settings.js` into a jsdom window whose URL carries `?tab=<valid-tab>`, fire `DOMContentLoaded`, and assert the OBSERVABLE active-tab state — the active class / `aria-selected` on the tab element matching the param. Then simulate switching to a different tab (activate it) and assert `window.location.search` was updated to the new `?tab=` value. Then load with an INVALID `?tab=` value and assert it falls back to the default tab (observable active class).
    Assert observable active-tab and URL state ONLY (D-08) — never internal function names. Open with the falsifiability doc block; use the PASS/FAIL + exit-0/1 scaffold. Load settings.js/settings.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-settings-tabnav.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-settings-tabnav.test.js` exits 0 against the current `assets/settings.js`.
    - A valid `?tab=` selects the matching active tab; switching tabs writes the new `?tab=` to `window.location.search`; an invalid value falls back to the default tab — all asserted via observable DOM/URL state.
    - The test EXECUTES settings.js under jsdom and FAILS if tab selection or URL write breaks, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>Settings tab navigation behavior (?tab= select, URL write, invalid fallback) is pinned by an executing observable-behavior test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | Both tests LOAD `settings.html` + `assets/settings.js` into jsdom read-only; no write-back |
| test → storage | `PortfolioDB` is the in-repo mock spy; no real IndexedDB; no data leaves the test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test files shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | a characterization test that asserts internal shape (false confidence) | medium | mitigate | D-08/D-12: tests jsdom-EXECUTE settings.js and assert observable output only; acceptance requires "survives internal rename, fails on observable change" |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-settings-section-roundtrip.test.js` (NEW — TEST-03a, D-09): jsdom real-page save→reload round-trip over settings.js IIFE-1
- `tests/30-settings-tabnav.test.js` (NEW — TEST-03b): observable tab-nav behavior over settings.js IIFE-3

Scope note (D-10/D-13a/D-13c/D-14): settings.js IIFE-2 (snippets), IIFE-4 (backups), IIFE-5 (photos) are already pinned by the 25-xx/24-xx suite and are NOT re-tested; out-of-scope files (app.js, license.js, etc.) are not touched — those are the v1.3 backlog per D-14. The settings.js glue duplication (`t`/`showToast`/`getCurrentLang`) surfaced by research (D-13c) is a Phase 31 consolidation candidate, preserved here as context only — NOT Phase 30 work (this plan pins behavior; it refactors nothing). (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-settings-section-roundtrip.test.js` exits 0 and is falsifiable on a dropped/altered saved value (Task 1).
- `node tests/30-settings-tabnav.test.js` exits 0 and is falsifiable on broken tab selection/URL write (Task 2).
- Both tests execute settings.js under jsdom (not source-sliced) and survive an internal rename (D-08/D-12).
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (settings.js): the section-title save/load round-trip and the tab navigation are captured as a pre-refactor green baseline that guards Phase 31's settings extraction (RFCT-01).
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-04-SUMMARY.md` when done.
</output>
