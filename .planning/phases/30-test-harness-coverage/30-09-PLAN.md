---
phase: 30-test-harness-coverage
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/30-backups-helper-gate.test.js
  - tests/30-photos-optimize-loop.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-backups-helper-gate.test.js executes the REAL assets/settings.js IIFE-4 under jsdom and asserts the frequency helper-text wiring (helperOff/helperOn), the password-ack gate-block rejection (error shown + select reverted + no save-toast when not acked), and the ack un-check force-off (closes GAP-08 / region A4)"
    - "tests/30-photos-optimize-loop.test.js executes the REAL _optimizeAllPhotosLoop body from assets/settings.js (via window.__PhotosTabHelpers) and asserts persist-only-if-strictly-smaller, savedBytes accumulation, the real dataURLToBlob/blobToDataURL round-trip, and the no-shrink no-op + one handleOptimize success path (inline preview pill + success toast) (closes GAP-09 / region A5)"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-backups-helper-gate.test.js
    - tests/30-photos-optimize-loop.test.js
  key_links:
    - "GAP-08 uses real-page jsdom invoking ONLY the IIFE-4 backups handler; GAP-09 uses vm-exec calling window.__PhotosTabHelpers._optimizeAllPhotosLoop directly with injected stubs (resize returns a strictly-smaller blob, REAL dataURLToBlob/blobToDataURL)"
    - "both assert observable output (DOM text/visibility, persisted bytes, returned counts), never internal function names"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without also executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "do NOT monkey-patch _optimizeAllPhotosLoop to reject (that is the existing 25-11 gap); EXECUTE the real loop body"
    - "do not re-test the already-pinned A4 custom-days-visibility / schedule-saved-toast or A5 render/verdict/language-rerender behaviors; do not modify any assets/* production file"
---

<objective>
Strengthen the two `assets/settings.js` regions whose named behaviors never actually execute: GAP-08 (A4 backups — the frequency helper-text wiring, the password-ack gate-block REJECTION path, and the ack un-check force-off; both existing covering tests pre-seed passwordAcked='true' so the rejection branch is unhit) and GAP-09 (A5 photos — the real `_optimizeAllPhotosLoop` body, the dataURL adapters, and the optimize SUCCESS path; every existing test either declines the confirm or monkey-patches the loop to reject, so the body never runs).

GAP-08 follows D-09 (real-page jsdom, invoke ONLY the IIFE-4 handler). GAP-09 follows the vm-exec pattern (call the exported `__PhotosTabHelpers._optimizeAllPhotosLoop` directly with injected stubs and the REAL dataURL adapters) so the loop body genuinely executes.

Purpose: pin the backup gating and the photo-optimize loop so Phase 31 cannot silently break them.
Output: 2 new `tests/30-*.test.js` files, green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-settings-section-roundtrip.test.js
@tests/25-11-toast-behavior.test.js
@assets/settings.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-08 — characterize backups helper-text + password-gate rejection (region A4)</name>
  <files>tests/30-backups-helper-gate.test.js</files>
  <read_first>
    - assets/settings.js:2135-2344 (IIFE-4 Backups tab); refreshFrequencyHelper mode→helperOff/helperOn at :2166-2173; password-ack gate-block at :2203-2226 (schedulePasswordError shown + select reverted + return false); ack-checkbox un-ack force-off at :2316-2329
    - tests/30-settings-section-roundtrip.test.js:94-130 (settings.html buildEnv; capture handlers, invoke ONLY the IIFE-4 backups handler = captured[3])
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-08 entry — note 25-04/25-05 load assets/backup.js, a DIFFERENT module; do not rely on them)
  </read_first>
  <behavior>
    - Helper text: after a frequency mode change, the schedule frequency helper element's text/data-i18n reflects helperOff vs helperOn for the off vs non-off modes.
    - Password-gate rejection: with passwordAcked seeded 'false', dispatching a change to a non-off frequency shows schedulePasswordError, reverts the select to its prior value, and produces NO schedule save-toast.
    - Ack un-check force-off: un-checking the ack checkbox forces the schedule back off (observable select/state).
  </behavior>
  <action>
    Build the settings.html env; capture handlers and invoke ONLY captured[3] (IIFE-4 backups). Seed the persisted backup settings so passwordAcked='false'. Case 1: change the frequency select mode and assert the helper element's resolved text/data-i18n (helperOff for off, helperOn for non-off). Case 2: with passwordAcked='false', dispatch change to a non-off frequency → assert schedulePasswordError is un-hidden, the select value reverts, and App.showToast did NOT record the schedule saved-toast key. Case 3: un-check the ack checkbox → assert the schedule is forced off. Assert observable DOM / toast-spy only. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-backups-helper-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-backups-helper-gate.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/settings.js'))` AND invokes captured[3] AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-backups-helper-gate.test.js` is non-empty
    - BEHAVIORAL: assertion on the frequency helper element text/data-i18n for off vs non-off; assertion that the password-gate rejection shows the error + reverts the select + emits NO saved-toast; assertion of the ack un-check force-off
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of settings.js for assertion, assertions on internal function names; must NOT assert against assets/backup.js
    - `npm test` stays green
  </acceptance_criteria>
  <done>A4 helper-text wiring + password-gate rejection + ack force-off are pinned by observable DOM/toast; a Phase-31 break of the gate would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-09 — execute the photos optimize-loop body + dataURL adapters + success path (region A5)</name>
  <files>tests/30-photos-optimize-loop.test.js</files>
  <read_first>
    - assets/settings.js:2451 (dataURLToBlob), :2461 (blobToDataURL), :2500-2543 (_optimizeAllPhotosLoop body: persist only if strictly smaller, savedBytes accumulation), exports on window.__PhotosTabHelpers at :2545; handleOptimize success path inline preview pill + success toast at :2856-2883
    - tests/25-11-toast-behavior.test.js:38,329-333,430-460 (the vm.runInContext load of settings.js + access to ctx.sandbox.window.__PhotosTabHelpers — copy this vm-exec pattern, but DO NOT monkey-patch the loop; call the REAL _optimizeAllPhotosLoop)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-09 entry — note 25-06 loads crop.js, 25-07 loads db.js; both off-region)
  </read_first>
  <behavior>
    - Strictly-smaller persistence: with resize returning a blob strictly smaller than the input, _optimizeAllPhotosLoop persists the optimized photo (updateClient called with the smaller dataURL) and accumulates savedBytes.
    - No-shrink no-op: with resize returning a blob the same size or larger, the loop does NOT persist (no updateClient write for that client) and savedBytes does not grow.
    - dataURL round-trip: the REAL dataURLToBlob/blobToDataURL pair is exercised inside the loop (not stubbed).
    - Success path (one case): a handleOptimize run with confirmDialog→true renders the inline preview pill text + success toast.
  </behavior>
  <action>
    Copy the 25-11 vm-exec load (vm.createContext + vm.runInContext(settingsSrc)) so window.__PhotosTabHelpers is available WITHOUT monkey-patching the loop. Seed clients with photoData. Case 1: call h._optimizeAllPhotosLoop(getAllClients, updateClient, resize→strictly-smaller-blob, h.blobToDataURL, h.dataURLToBlob) and assert updateClient received the smaller dataURL AND the returned savedBytes/success count reflects the shrink. Case 2: resize→same-or-larger-blob → assert NO updateClient write + savedBytes unchanged (no-shrink no-op). Use the REAL h.dataURLToBlob/h.blobToDataURL (do not stub them). Case 3: drive one handleOptimize success path (confirmDialog→true) and assert the inline preview pill text + success toast (real-page jsdom or the vm sandbox's exposed handler, whichever the existing 25-11 pattern supports). Standard doc block (root-cause: the loop body never executed before) + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-photos-optimize-loop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-photos-optimize-loop.test.js` exits 0
    - STRUCTURAL: loads settings.js via `vm.runInContext` (or win.eval) AND calls the REAL `window.__PhotosTabHelpers._optimizeAllPhotosLoop` (not a monkey-patched replacement) AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-photos-optimize-loop.test.js` is non-empty
    - BEHAVIORAL: assertion that a strictly-smaller resize triggers an updateClient persist with the smaller dataURL + savedBytes accumulates; assertion that a no-shrink resize results in NO persist + no savedBytes growth; assertion exercising the REAL dataURLToBlob/blobToDataURL (no stub of those two); assertion on the success-path preview pill text + success toast
    - the test does NOT overwrite/monkey-patch `_optimizeAllPhotosLoop` (`grep -c '__PhotosTabHelpers._optimizeAllPhotosLoop =' tests/30-photos-optimize-loop.test.js` is 0)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, regex-over-source assertions, assertions on internal function names; must NOT assert against assets/crop.js or assets/db.js
    - `npm test` stays green
  </acceptance_criteria>
  <done>The photos optimize-loop body, the dataURL adapters, and the optimize success path actually execute and are pinned by observable persistence + DOM; a Phase-31 break of the shrink-only persist or savedBytes math would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-backups-helper-gate.test.js (GAP-08, region A4)
- tests/30-photos-optimize-loop.test.js (GAP-09, region A5)

**Modified files:** none (no assets/* changes; 25-04/25-05/25-06/25-07/25-12 backup/photo tests retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval/vm-load trusted local production modules into an isolated context; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-09 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats. Schema: N/A. |
</threat_model>

<verification>
- `node tests/30-backups-helper-gate.test.js` and `node tests/30-photos-optimize-loop.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file; the optimize-loop test does not monkey-patch the loop
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
A4 backups gating and A5 photos optimize-loop are guarded by tests that execute the real code (not a stubbed/declined path) and assert observable behavior; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-09-SUMMARY.md` when done.
</output>
