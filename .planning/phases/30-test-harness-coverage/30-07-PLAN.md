---
phase: 30-test-harness-coverage
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_helpers/mock-portfolio-db.js
  - tests/_helpers/app-stub.js
  - tests/_helpers/base64-codec.js
  - tests/30-form-dirty-revert.test.js
  - tests/30-read-mode.test.js
  - tests/30-client-spotlight.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "the shared test doubles are extended ONCE (Task 0, G2): tests/_helpers/mock-portfolio-db.js gains store-backed spy methods getSession/getClient/addSnippet/deleteSnippet (addSnippet/deleteSnippet in WRITE_METHODS; getAllSnippets reads back the live store), tests/_helpers/app-stub.js gains getSnippets()+refreshSnippetCache() modelling app.js:87-104, and tests/_helpers/base64-codec.js provides a faithful Buffer-backed atob/btoa/Blob/FileReader codec — every wave-2 plan consumes these rather than re-adding them"
    - "tests/30-form-dirty-revert.test.js executes the REAL assets/add-session.js under jsdom on a seeded ?sessionId= EDITING session (so lastSavedSnapshot is populated and revertSessionForm does not early-return at add-session.js:710-711) and asserts window.PortfolioFormDirty() transitions false→true on edit and true→false on revert, with form fields restored and the cancel-button label flipping confirm.cancel ⇄ session.discard, and the captured nav-guard isDirty callback tracking PortfolioFormDirty() (closes GAP-01 / region B6)"
    - "tests/30-read-mode.test.js executes the REAL assets/add-session.js under jsdom on a ?sessionId= past session loaded via the now-real PortfolioDB.getSession, with body.classList.contains('read-mode') asserted as a HARD precondition (fails loudly if read-mode entry did not happen) before asserting submit hidden + edit/copy/export shown, inputs disabled, and the edit-client modal opens/closes (closes GAP-02 / region B7)"
    - "tests/30-client-spotlight.test.js executes the REAL assets/add-session.js under jsdom and, in a ?clientId case, asserts the client dropdown is populated/name-sorted (clients seeded OUT OF alphabetical order so the localeCompare sort is falsifiable)/pre-selected and populateSpotlight renders name/age/photo/notes; in a separate ?sessionId case asserts updateSessionTitle sets .section-title + document.title (closes GAP-04 / region B8, keeping 24-06)"
    - "every new/strengthened test in this plan has a recorded mutation-kill (G1): the guarded behavior reverted in a scratch copy makes the test exit non-zero, restored → exit 0, noted in verify + SUMMARY"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/_helpers/mock-portfolio-db.js
    - tests/_helpers/app-stub.js
    - tests/_helpers/base64-codec.js
    - tests/30-form-dirty-revert.test.js
    - tests/30-read-mode.test.js
    - tests/30-client-spotlight.test.js
  key_links:
    - "Task 0 (wave 1) extends the shared helpers ONCE; the three test tasks (and all of wave 2: 30-08..30-12) consume the now-real getSession/getClient/addSnippet/deleteSnippet + App.getSnippets/refreshSnippetCache + the base64 codec — no plan re-adds them locally"
    - "each new test captures the single add-session.js DOMContentLoaded handler and awaits it (the 30-export-markdown buildEnv pattern) — never a blanket dispatchEvent"
    - "real-page assertions read observable DOM / persistence-call args, never internal function names"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without also executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "no assertion on internal function names (assert rendered output, not that renderX exists/was called)"
    - "do not widen window.__addSessionTestHooks or modify any assets/* production file"
---

<objective>
Write three NEW jsdom real-page characterization tests that close the THREE high-risk holes in `assets/add-session.js` where a Phase-31 break currently passes the net GREEN: B6 form dirty/revert (GAP-01, currently zero coverage), B7 read mode + edit-client modal (GAP-02, currently zero coverage), and B8 client/spotlight wiring (GAP-04, currently only `renderSpotlightSessionInfo` pinned by 24-06).

These regions are exactly the screen wiring the Phase-31 refactor rearranges, so per D-08 (observable behavior only) and D-09 (load the real page into jsdom and drive the real handlers) each test must EXECUTE the real module and assert observable output.

This plan is **wave 1**: its first task (Task 0) extends the SHARED test doubles ONCE (G2) so the six wave-2 plans (30-08..30-12) can drive the real PortfolioDB/App methods instead of re-adding them locally and colliding on the shared files. The three add-session characterization tests then run in this same plan after Task 0.

Purpose: complete the add-session safety net so the Phase-31 extraction is actually guarded, and lay down the shared-helper surface the rest of the gap round depends on.
Output: 3 extended shared helpers (Task 0) + 3 new `tests/30-*.test.js` files, all green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-export-markdown.test.js
@tests/30-issue-delta.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@assets/add-session.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Shared-helper extension — extend the test doubles ONCE (G2)</name>
  <files>tests/_helpers/mock-portfolio-db.js, tests/_helpers/app-stub.js, tests/_helpers/base64-codec.js</files>
  <read_first>
    - tests/_helpers/mock-portfolio-db.js (current spy style: WRITE_METHODS→makeWriteSpy records __calls + resolves; READ_METHODS→makeReadSpy returns the configured arrays; today only updateSnippet exists for snippets and READ_METHODS are getAll* only — no getSession/getClient/addSnippet/deleteSnippet)
    - tests/_helpers/app-stub.js (DEFAULT_RETURNS surface + the overrides-passthrough loop; today there is NO getSnippets / refreshSnippetCache)
    - assets/app.js:87-104 (the REAL contract to mirror: getSnippets() returns `_snippetCache.slice()` SYNCHRONOUSLY; refreshSnippetCache() is async and does `_snippetCache = await PortfolioDB.getAllSnippets()` then dispatches app:snippets-changed)
    - assets/settings.js:955-961 (afterSnippetMutation: await App.refreshSnippetCache() → renderSnippetList() reads App.getSnippets() — this is why a write becomes visible in the list), :1601-1603 (addSnippet on add / updateSnippet on edit), :1627 (deleteSnippet), :1748 + :1827 (detectImportCollisions / applyImport both read App.getSnippets())
    - assets/add-session.js:1804 (read-mode load path calls PortfolioDB.getSession(sessionId))
    - assets/settings.js:2451-2468 (dataURLToBlob uses atob+Blob; blobToDataURL uses FileReader.readAsDataURL — the codec the GAP-09 loop must run faithfully)
  </read_first>
  <action>
    Extend the SHARED doubles additively so the wave-2 plans use real methods, never re-add them.
    mock-portfolio-db.js: back the mock with an in-memory store seeded from opts (sessions, clients, snippets). Add four methods that BOTH record into `__calls` (mirror the existing makeWriteSpy/makeReadSpy style) AND touch that store: getSession(id) + getClient(id) (look up the seeded store by id, record the call, resolve the record or null); addSnippet(s) (push into the snippet store, record) and deleteSnippet(id) (remove from the snippet store, record). Register getSession/getClient on the read-call ledger and addSnippet/deleteSnippet in WRITE_METHODS so assertNoWrites covers them. Make getAllSnippets read back the LIVE snippet store (so a write is observable on the next read). Keep updateSnippet's existing behavior but have it mutate the same store by id.
    app-stub.js: add getSnippets() returning a synchronous slice of an internal `_snippetCache`, and refreshSnippetCache() that is async and sets `_snippetCache = await window.PortfolioDB.getAllSnippets()` — i.e. the exact app.js:87-104 contract (this is the ONE intentional PortfolioDB read in the stub, matching production; document it in the doc block). Spy-record both like the rest of the surface. After this, settings.js afterSnippetMutation → refreshSnippetCache → renderSnippetList reflects writes with no per-test plumbing.
    base64-codec.js (NEW): export a faithful Buffer-backed atob, btoa, a minimal Blob (size = total byteLength of its parts), and a FileReader whose readAsDataURL emits a REAL base64 data URL computed from the blob bytes (NOT a constant). Top-of-file note: GAP-09 may alternatively run under real-page jsdom (which supplies these natively); this module is the deterministic vm-sandbox option.
    Additive only — do NOT change any existing exported behavior, so 30-01..06 and the 25-xx consumers stay green.
  </action>
  <verify>
    <automated>node tests/30-settings-section-roundtrip.test.js && node tests/30-issue-delta.test.js && node tests/24-06-spotlight-session-info.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/_helpers/mock-portfolio-db.js exports getSession, getClient, addSnippet, deleteSnippet; addSnippet/deleteSnippet are in WRITE_METHODS (assertNoWrites covers them); getAllSnippets reads back the live store; updateSnippet mutates the same store by id
    - tests/_helpers/app-stub.js exports getSnippets (sync slice) + refreshSnippetCache (async, pulls from window.PortfolioDB.getAllSnippets) matching app.js:87-104; both spy-recorded
    - tests/_helpers/base64-codec.js exports faithful atob/btoa/Blob/FileReader where a round-trip blob→dataURL→blob preserves byte length (NOT constant-length output)
    - additive only: `node tests/30-settings-section-roundtrip.test.js`, `node tests/30-issue-delta.test.js`, `node tests/24-06-spotlight-session-info.test.js` (existing consumers of these helpers) still exit 0
    - `npm test` stays green
    - Mutation-kill recorded (G1): in a scratch copy, make the new FileReader.readAsDataURL return a constant data URL; a round-trip assertion (blob of known size → dataURL → decoded byte length === original) goes RED; restored → GREEN; the mutation + result noted in the SUMMARY.
  </acceptance_criteria>
  <done>The four PortfolioDB methods, the two App snippet-cache methods, and the faithful base64 codec exist ONCE in the shared helpers; every wave-2 plan consumes them; existing consumers stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: GAP-01 — characterize form dirty/revert (region B6)</name>
  <files>tests/30-form-dirty-revert.test.js</files>
  <read_first>
    - assets/add-session.js:679-724 (snapshotFormState, revertSessionForm, updateCancelButtonLabel, window.PortfolioFormDirty) — all inside the DOMContentLoaded closure at add-session.js:44
    - assets/add-session.js:710-711 (revertSessionForm EARLY-RETURNS when `!lastSavedSnapshot` — on a clean `?-URL` env nothing is snapshotted, so the whole revert test would be a vacuous no-op), :1803-1820 (the ?sessionId EDITING path is what populates lastSavedSnapshot — note the snapshot is taken inside `Promise.resolve().then(...)` at :1816-1817, so it must be awaited/settled before editing)
    - assets/add-session.js:58-62 (App.installNavGuard is called UNCONDITIONALLY at boot with an `isDirty` callback — merely "was called" proves nothing; the meaningful wiring is whether that isDirty callback tracks PortfolioFormDirty())
    - tests/30-issue-delta.test.js:81-135 (buildEnv that eval's app.js then add-session.js, seeds mockDb, captures + awaits the single DOMContentLoaded handler) — and tests/_helpers/mock-portfolio-db.js getSession (now real after Task 0) for seeding the editing session
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-01 entry)
  </read_first>
  <behavior>
    - Snapshot→edit→revert (EDITING session): boot on a seeded `?sessionId=<id>` so PortfolioDB.getSession loads the session and lastSavedSnapshot is populated; editing a session field flips PortfolioFormDirty() to true; clicking the cancel/revert control restores the original field values (from the snapshot) and flips PortfolioFormDirty() back to false.
    - Cancel-button label flip: when dirty, the cancel control label resolves to t('session.discard'); when clean, to t('confirm.cancel').
    - Nav-guard wiring (not "was called"): capture the `isDirty` callback passed to App.installNavGuard at boot and assert it returns true when the form is dirty and false when clean — i.e. the guard reads the live PortfolioFormDirty() state.
  </behavior>
  <action>
    Build the env on a SEEDED EDITING session: seed mockDb (Task-0 getSession) with one session + its client, set the jsdom URL to add-session.html?sessionId=<seededId>, capture + await the single DOMContentLoaded handler, settle() (so the :1816 Promise.resolve().then snapshot lands and lastSavedSnapshot is set). Drive snapshotFormState/revertSessionForm/updateCancelButtonLabel ONLY through observable user actions: change a real form field value + dispatch input, read window.PortfolioFormDirty()===true; click the real cancel/revert control, re-read window.PortfolioFormDirty()===false and assert the field value reverted to the snapshot value. Stub App.t so session.discard and confirm.cancel return distinguishable strings, then assert the button's textContent matches the expected key per dirty state. For the nav guard: pass an `installNavGuard` override into createAppStub that CAPTURES the options object, then assert the captured `isDirty()` returns true while dirty and false after revert (do NOT assert merely that installNavGuard "was called"). Open with the standard doc block (root-cause / fix / falsifiability) and the end-of-file EXPECTED_COUNT guard. Add `.catch(e=>{console.error(e);process.exit(1);})` on the top-level async IIFE.
  </action>
  <verify>
    <automated>node tests/30-form-dirty-revert.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-form-dirty-revert.test.js` exits 0
    - STRUCTURAL (executes real module): the file contains a real loader — `win.eval(readAsset('assets/add-session.js'))` (or jsdom DOMContentLoaded drive) — AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-form-dirty-revert.test.js` is non-empty
    - PRECONDITION (no vacuous no-op): the test boots on a seeded `?sessionId=` editing session and asserts lastSavedSnapshot took effect (e.g. PortfolioFormDirty()===false after the post-load settle); a clean `?-URL` env is NOT used
    - BEHAVIORAL: assertion that `window.PortfolioFormDirty()` is true after edit and false after revert; assertion that a field value is restored to the snapshot value after revert; assertion on the cancel button's resolved label text (session.discard vs confirm.cancel); assertion that the captured nav-guard `isDirty()` callback returns true when dirty and false when clean (NOT merely that installNavGuard was called)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, any `fs.readFileSync(...add-session.js...)` used for assertion, any assertion referencing the literal internal function names as proof of coverage, any assertion that App.installNavGuard "was invoked" used as the wiring proof
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, neutralise revertSessionForm's snapshot restore (e.g. early-return before populateSession) → `node tests/30-form-dirty-revert.test.js` exits non-zero; restored → exits 0; the mutation + result noted in the plan verify + SUMMARY
    - file is discovered by tests/run-all.js (matches `30-*.test.js`); `npm test` stays green
  </acceptance_criteria>
  <done>B6 form dirty/revert is pinned by executing observable behavior on a real EDITING session; a Phase-31 move that broke the dirty flag, the revert restoration, the label flip, or the nav-guard isDirty wiring would FAIL this test; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-02 — characterize read mode + edit-client modal (region B7)</name>
  <files>tests/30-read-mode.test.js</files>
  <read_first>
    - assets/add-session.js:178-283 (setReadMode, applyCopyLabels, resizeReadModeTextareas, clearReadModeTextareas, openEditClientModal/closeEditClientModal) — inside the DOMContentLoaded closure
    - assets/add-session.js:1803-1812 (read-mode is entered ONLY when `sessionId && Number.isInteger(sessionId)` AND `editingSession = await PortfolioDB.getSession(sessionId)` resolves a record → then setReadMode(true) at :1811; this is why Task-0's real getSession + a seeded session are required, and why read-mode entry must be a HARD precondition)
    - tests/30-issue-delta.test.js:81-135 (buildEnv pattern; note matchMedia + BroadcastChannel stubs) and tests/_helpers/mock-portfolio-db.js getSession (real after Task 0)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-02 entry; note: the export tests click #exportSessionBtn/#copySessionBtn directly and BYPASS setReadMode — this test must let DOMContentLoaded run on a real ?sessionId= URL)
  </read_first>
  <behavior>
    - Read-mode rendering of a past session: with a ?sessionId pointing at a seeded past session loaded through the real PortfolioDB.getSession, after boot body.classList contains read-mode; the submit control is hidden; the edit + copy + export controls are visible; session inputs are readOnly/disabled; severity/issue buttons are disabled.
    - Edit-client modal: invoking the edit-client open control populates and shows the modal; the close control hides it.
  </behavior>
  <action>
    Build the env with the buildEnv pattern but set the jsdom URL to add-session.html?sessionId=<seededId> and seed mockDb (Task-0 getSession) with that session (and its client) so the real DOMContentLoaded handler takes the past-session path and calls setReadMode. Await the handler, settle(). FIRST assert `body.classList.contains('read-mode') === true` as a HARD PRECONDITION with a loud message (if it is false, read-mode entry did not happen — getSession mis-wired or sessionId not parsed — and every downstream assertion would otherwise pass vacuously against the default new-session DOM; fail the case here). Then assert observable DOM: submit button hidden vs edit/copy/export visible (via element hidden/display/aria, not function names); a representative session input has readOnly===true or disabled===true. Then click the real edit-client open control, settle(), assert the edit-client modal is shown and populated; click close, settle(), assert hidden. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-read-mode.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-read-mode.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-read-mode.test.js` is non-empty
    - HARD PRECONDITION: `body.classList.contains('read-mode')` is asserted true with a loud failure message BEFORE any read-only/visibility assertion, so the case cannot pass against the default new-session DOM if read-mode entry never happened
    - BEHAVIORAL: assertion that the submit control is hidden while edit/copy/export are visible; assertion that an input is readOnly/disabled; assertion that the edit-client modal toggles visible→hidden across open/close
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, neutralise setReadMode's body-class / submit-hide wiring (e.g. early-return in setReadMode) → the test exits non-zero (the hard precondition or the visibility assertion fails); restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>B7 read mode + edit-client modal is pinned by observable DOM, gated on a real read-mode entry; a Phase-31 break of the read-only visibility wiring or the modal toggle would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-04 — characterize client dropdown / spotlight / title wiring (region B8)</name>
  <files>tests/30-client-spotlight.test.js</files>
  <read_first>
    - assets/add-session.js:1865-2173 (loadClients, getSelectedClient, getClientDisplayName, populateSpotlight, updateSessionTitle, populateSession) — renderSpotlightSessionInfo is ALREADY pinned by tests/24-06-spotlight-session-info.test.js; KEEP that test, do not duplicate it
    - assets/add-session.js:1881-1898 (loadClients builds the select: placeholder, a `__new__` option, then `clients.sort((a,b)=>a.name.localeCompare(...))` appended, then pre-selects `selectId`) — seed clients OUT OF alphabetical order so the sort assertion is falsifiable
    - assets/add-session.js:1808 (updateSessionTitle fires ONLY on the `?sessionId` editing branch, NOT on the `?clientId` boot — so the title assertion belongs in a separate ?sessionId case)
    - tests/30-issue-delta.test.js:74-134 (buildEnv that eval's assets/app.js FIRST then add-session.js; clientId/sessionId URL handling; mock seeding)
    - tests/_helpers/mock-portfolio-db.js (seed clients with names/photo/notes; getAllClients drives loadClients; getSession real after Task 0 for the ?sessionId case)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-04 entry; note 30-issue-delta OVERRIDES loadClients output so it cannot be relied on for these assertions)
  </read_first>
  <behavior>
    - loadClients dropdown population (?clientId case): after boot, the client <select> has a placeholder option + a "__new__" option + the seeded clients NAME-SORTED, with the correct client pre-selected when a ?clientId is present. Clients are seeded out of alphabetical order so the sort is observable.
    - populateSpotlight (?clientId case): the spotlight renders the client name, age, photo, and notes from the seeded record.
    - updateSessionTitle (?sessionId case): in a separate editing-session boot, the .section-title text and document.title reflect the loaded session/client (this branch is reached only via ?sessionId).
  </behavior>
  <action>
    Build the env with the 30-issue-delta buildEnv shape (eval app.js first for the real App surface, then add-session.js), seeding mockDb with 2-3 clients given DISTINCT names in a deliberately NON-alphabetical seed order (e.g. ["Zoe","Adi","Maya"]) and one client carrying photo + notes. Case 1 (?clientId=<seeded>): assert the client <select> option set/order is placeholder + __new__ + the clients in ALPHABETICAL order (not seed order — proves the localeCompare sort ran), the pre-selected option matches the ?clientId, and the spotlight DOM shows that client's name/age/photo/notes. Case 2 (?sessionId=<seeded editing session> via Task-0 getSession): assert document.title AND the .section-title element text reflect the loaded session/client (updateSessionTitle ran on the sessionId branch). Assert OBSERVABLE DOM/selection state only — never that loadClients/populateSpotlight/updateSessionTitle were "called". Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-client-spotlight.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-client-spotlight.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-client-spotlight.test.js` is non-empty
    - FALSIFIABLE SORT: clients are seeded out of alphabetical order and the dropdown assertion checks alphabetical output order (so a dropped/broken localeCompare sort fails the case)
    - BEHAVIORAL: ?clientId case asserts the client <select> options (placeholder + __new__ + name-sorted clients + pre-selection) and rendered spotlight content (name/age/photo/notes); a SEPARATE ?sessionId case asserts document.title AND the .section-title element text (updateSessionTitle on the sessionId branch)
    - tests/24-06-spotlight-session-info.test.js is left unchanged and still green (renderSpotlightSessionInfo coverage retained)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion, assertions on internal function names
    - Mutation-kill recorded (G1): in a scratch copy of add-session.js, remove the localeCompare sort (or neutralise updateSessionTitle) → the relevant case exits non-zero; restored → exits 0; noted in verify + SUMMARY
    - `npm test` stays green
  </acceptance_criteria>
  <done>B8 client/spotlight/title wiring is pinned by observable DOM and selection state, with the title assertion correctly scoped to the ?sessionId branch and a falsifiable name-sort; a Phase-31 break of dropdown population, spotlight render, or title update would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/_helpers/base64-codec.js (Task 0 / G2 — faithful Buffer-backed atob/btoa/Blob/FileReader, consumed by GAP-09 in 30-09)
- tests/30-form-dirty-revert.test.js (GAP-01, region B6)
- tests/30-read-mode.test.js (GAP-02, region B7)
- tests/30-client-spotlight.test.js (GAP-04, region B8)

**Modified files (Task 0 / G2 — shared helper extension, additive only):**
- tests/_helpers/mock-portfolio-db.js (add store-backed getSession/getClient/addSnippet/deleteSnippet; getAllSnippets reads live store; addSnippet/deleteSnippet in WRITE_METHODS)
- tests/_helpers/app-stub.js (add getSnippets/refreshSnippetCache modelling app.js:87-104)

(no assets/* changes; 24-06 retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats — tests eval trusted in-repo assets only. |
</threat_model>

<verification>
- `node tests/30-form-dirty-revert.test.js`, `node tests/30-read-mode.test.js`, `node tests/30-client-spotlight.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file (real-module execution proven)
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
The three add-session high-risk regions (B6, B7, B8) are each guarded by a test that executes the real module and asserts observable behavior; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-07-SUMMARY.md` when done.
</output>
