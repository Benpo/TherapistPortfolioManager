---
phase: 30-test-harness-coverage
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/30-form-dirty-revert.test.js
  - tests/30-read-mode.test.js
  - tests/30-client-spotlight.test.js
autonomous: true
gap_closure: true
requirements: [TEST-03]
must_haves:
  truths:
    - "tests/30-form-dirty-revert.test.js executes the REAL assets/add-session.js under jsdom and asserts window.PortfolioFormDirty() transitions false→true on edit and true→false on revert, with form fields restored and the cancel-button label flipping confirm.cancel ⇄ session.discard (closes GAP-01 / region B6)"
    - "tests/30-read-mode.test.js executes the REAL assets/add-session.js under jsdom on a ?sessionId= past session and asserts body has the read-mode class, submit hidden + edit/copy/export shown, inputs disabled, and the edit-client modal opens/closes (closes GAP-02 / region B7)"
    - "tests/30-client-spotlight.test.js executes the REAL assets/add-session.js under jsdom and asserts the client dropdown is populated/ordered/pre-selected, populateSpotlight renders name/age/photo/notes, and updateSessionTitle sets .section-title + document.title (closes GAP-04 / region B8, keeping 24-06)"
    - "npm test stays green (exit 0) after this plan"
  artifacts:
    - tests/30-form-dirty-revert.test.js
    - tests/30-read-mode.test.js
    - tests/30-client-spotlight.test.js
  key_links:
    - "each new test captures the single add-session.js DOMContentLoaded handler and awaits it (the 30-export-markdown buildEnv pattern) — never a blanket dispatchEvent"
    - "real-page assertions read observable DOM / persistence-call args, never internal function names"
  prohibitions:
    - "no new test reads an assets/* file with fs.readFileSync for assertion purposes without also executing it (vm/eval/jsdom)"
    - "no SRC.indexOf( / SRC.slice( / regex-over-source as a coverage mechanism"
    - "no assertion on internal function names (assert rendered output, not that renderX exists/was called)"
    - "do not widen window.__addSessionTestHooks or modify any assets/* production file"
---

<objective>
Write three NEW jsdom real-page characterization tests that close the THREE high-risk holes in `assets/add-session.js` where a Phase-31 break currently passes the net GREEN: B6 form dirty/revert (GAP-01, currently zero coverage), B7 read mode + edit-client modal (GAP-02, currently zero coverage), and B8 client/spotlight wiring (GAP-04, currently only `renderSpotlightSessionInfo` pinned by 24-06).

These regions are exactly the screen wiring the Phase-31 refactor rearranges, so per D-08 (observable behavior only) and D-09 (load the real page into jsdom and drive the real handlers) each test must EXECUTE the real module and assert observable output.

Purpose: complete the add-session safety net so the Phase-31 extraction is actually guarded.
Output: 3 new `tests/30-*.test.js` files, all green via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-harness-coverage/30-VERIFICATION.md
@tests/30-export-markdown.test.js
@tests/30-issue-delta.test.js
@tests/_helpers/app-stub.js
@tests/_helpers/mock-portfolio-db.js
@assets/add-session.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-01 — characterize form dirty/revert (region B6)</name>
  <files>tests/30-form-dirty-revert.test.js</files>
  <read_first>
    - assets/add-session.js:679-718 (snapshotFormState, revertSessionForm, updateCancelButtonLabel, window.PortfolioFormDirty) — all inside the DOMContentLoaded closure at add-session.js:44
    - tests/30-export-markdown.test.js:88-145 (the canonical buildEnv: real add-session.html + win.eval(readAsset('assets/add-session.js')), captured DOMContentLoaded handler, createAppStub, createMockPortfolioDB, matchMedia stub, settle())
    - tests/_helpers/app-stub.js (installNavGuard no-op landmine 2; initCommon resolves)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-01 entry)
  </read_first>
  <behavior>
    - Snapshot→edit→revert: after the page boots, editing a session field flips PortfolioFormDirty() to true; clicking the cancel/revert control restores the original field values and flips PortfolioFormDirty() back to false.
    - Cancel-button label flip: when dirty, the cancel control label resolves to t('session.discard'); when clean, to t('confirm.cancel').
    - Dirty flag drives the nav guard: App.installNavGuard is invoked (assert via the app-stub __calls spy) consistent with the dirty state.
  </behavior>
  <action>
    Build the env with the 30-export-markdown buildEnv pattern (clean ?-URL, empty clients/sessions). Capture the single DOMContentLoaded handler, await it, settle(). Drive snapshotFormState/revertSessionForm/updateCancelButtonLabel ONLY through observable user actions: set a real form field value + dispatch input, read window.PortfolioFormDirty(); click the real cancel/revert button, re-read window.PortfolioFormDirty() and the restored field value and the cancel button's resolved label text. Stub App.t so session.discard and confirm.cancel return distinguishable strings, then assert the button's textContent matches the expected key per dirty state. Use the app-stub __calls spy to assert App.installNavGuard was wired. Open with the standard doc block (root-cause / fix / falsifiability) and the end-of-file EXPECTED_COUNT guard. Add `.catch(e=>{console.error(e);process.exit(1);})` on the top-level async IIFE.
  </action>
  <verify>
    <automated>node tests/30-form-dirty-revert.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-form-dirty-revert.test.js` exits 0
    - STRUCTURAL (executes real module): the file contains a real loader — `win.eval(readAsset('assets/add-session.js'))` (or jsdom DOMContentLoaded drive) — AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-form-dirty-revert.test.js` is non-empty
    - BEHAVIORAL: at least one assertion on `window.PortfolioFormDirty()` returning true after edit and false after revert; at least one assertion that a field value is restored after revert; at least one assertion on the cancel button's resolved label text (session.discard vs confirm.cancel)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, any `fs.readFileSync(...add-session.js...)` used for assertion, any assertion referencing the literal internal function names as proof of coverage
    - file is discovered by tests/run-all.js (matches `30-*.test.js`); `npm test` stays green
  </acceptance_criteria>
  <done>B6 form dirty/revert is pinned by executing observable behavior; a Phase-31 move that broke the dirty flag, the revert restoration, or the label flip would FAIL this test; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-02 — characterize read mode + edit-client modal (region B7)</name>
  <files>tests/30-read-mode.test.js</files>
  <read_first>
    - assets/add-session.js:178-283 (setReadMode, applyCopyLabels, resizeReadModeTextareas, clearReadModeTextareas, openEditClientModal/closeEditClientModal) — inside the DOMContentLoaded closure
    - tests/30-export-markdown.test.js:88-145 (buildEnv pattern; note matchMedia + BroadcastChannel stubs)
    - tests/_helpers/mock-portfolio-db.js (seed a past session + a client so a ?sessionId= load enters read mode)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-02 entry; note: the export tests click #exportSessionBtn/#copySessionBtn directly and BYPASS setReadMode — this test must let DOMContentLoaded run on a real ?sessionId= URL)
  </read_first>
  <behavior>
    - Read-mode rendering of a past session: with a ?sessionId pointing at a seeded past session, after boot body.classList contains read-mode; the submit control is hidden; the edit + copy + export controls are visible; session inputs are readOnly/disabled; severity/issue buttons are disabled.
    - Edit-client modal: invoking the edit-client open control populates and shows the modal; the close control hides it.
  </behavior>
  <action>
    Build the env with the buildEnv pattern but set the jsdom URL to add-session.html?sessionId=<seededId> and seed mockDb with that session (and its client) so the real DOMContentLoaded handler takes the past-session path and calls setReadMode. Await the handler, settle(). Assert observable DOM: body.classList.contains('read-mode'); submit button hidden vs edit/copy/export visible (assert via element hidden/display/aria, not function names); a representative session input has readOnly===true or disabled===true. Then click the real edit-client open control, settle(), assert the edit-client modal is shown and populated; click close, settle(), assert hidden. Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-read-mode.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-read-mode.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-read-mode.test.js` is non-empty
    - BEHAVIORAL: assertion that `body.classList.contains('read-mode')` is true; assertion that the submit control is hidden while edit/copy/export are visible; assertion that an input is readOnly/disabled; assertion that the edit-client modal toggles visible→hidden across open/close
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion, assertions on internal function names
    - `npm test` stays green
  </acceptance_criteria>
  <done>B7 read mode + edit-client modal is pinned by observable DOM; a Phase-31 break of the read-only visibility wiring or the modal toggle would FAIL; an internal rename keeps it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-04 — characterize client dropdown / spotlight / title wiring (region B8)</name>
  <files>tests/30-client-spotlight.test.js</files>
  <read_first>
    - assets/add-session.js:1865-2173 (loadClients, getSelectedClient, getClientDisplayName, populateSpotlight, updateSessionTitle, populateSession) — renderSpotlightSessionInfo is ALREADY pinned by tests/24-06-spotlight-session-info.test.js; KEEP that test, do not duplicate it
    - tests/30-issue-delta.test.js:74-134 (buildEnv that eval's assets/app.js FIRST then add-session.js; clientId/sessionId URL handling; mock seeding)
    - tests/_helpers/mock-portfolio-db.js (seed clients with names/photo/notes; getAllClients drives loadClients)
    - .planning/phases/30-test-harness-coverage/30-VERIFICATION.md (GAP-04 entry; note 30-issue-delta OVERRIDES loadClients output so it cannot be relied on for these assertions)
  </read_first>
  <behavior>
    - loadClients dropdown population: after boot, the client <select> has a placeholder option + a "__new__" option + the seeded clients name-sorted, with the correct client pre-selected when a ?clientId is present.
    - populateSpotlight: on a ?clientId URL, the spotlight renders the client name, age, photo, and notes from the seeded record.
    - updateSessionTitle: the .section-title text and document.title reflect the selected client.
    - populateSession: when editing, the form fields are filled from the session record.
  </behavior>
  <action>
    Build the env with the 30-issue-delta buildEnv shape (eval app.js first if needed for any App surface, then add-session.js), seeding mockDb with 2-3 clients (distinct names, one with photo + notes). Case 1: clean clientId-less load → assert the client select option set/order includes placeholder + __new__ + name-sorted clients. Case 2: load with ?clientId=<seeded> → assert the pre-selected option AND that the spotlight DOM shows the client's name/age/photo/notes AND that document.title + the .section-title element reflect the selection. Case 3 (optional within budget): ?sessionId edit → assert populateSession filled a representative field. Assert OBSERVABLE DOM/selection state only — never that loadClients/populateSpotlight were "called". Standard doc block + EXPECTED_COUNT guard + top-level `.catch`.
  </action>
  <verify>
    <automated>node tests/30-client-spotlight.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-client-spotlight.test.js` exits 0
    - STRUCTURAL: contains `win.eval(readAsset('assets/add-session.js'))` AND `grep -E 'vm|eval|jsdom|runInContext' tests/30-client-spotlight.test.js` is non-empty
    - BEHAVIORAL: assertion on the client <select> options (placeholder + __new__ + name-sorted clients + pre-selection); assertion on rendered spotlight content (name/age/photo/notes); assertion on document.title AND the .section-title element text
    - tests/24-06-spotlight-session-info.test.js is left unchanged and still green (renderSpotlightSessionInfo coverage retained)
    - FORBIDDEN present (must be absent): `SRC.indexOf(`, `SRC.slice(`, fs.readFileSync of add-session.js for assertion, assertions on internal function names
    - `npm test` stays green
  </acceptance_criteria>
  <done>B8 client/spotlight/title wiring is pinned by observable DOM and selection state; a Phase-31 break of dropdown population, spotlight render, or title update would FAIL; an internal rename keeps it GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files:**
- tests/30-form-dirty-revert.test.js (GAP-01, region B6)
- tests/30-read-mode.test.js (GAP-02, region B7)
- tests/30-client-spotlight.test.js (GAP-04, region B8)

**Modified files:** none (no assets/* changes; 24-06 retained unchanged)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → assets/* | tests READ + eval trusted local production modules into an isolated jsdom window; no untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07 | (n/a) | test-only / dev-tooling scope | low | accept | No new runtime surface, no new endpoints, no new data handling. ASVS L1. No high-severity threats — tests eval trusted in-repo assets only. |
</threat_model>

<verification>
- `node tests/30-form-dirty-revert.test.js`, `node tests/30-read-mode.test.js`, `node tests/30-client-spotlight.test.js` each exit 0
- `grep -E 'vm|eval|jsdom|runInContext'` non-empty for each new file (real-module execution proven)
- `npm test` → full suite green, exit 0
</verification>

<success_criteria>
The three add-session high-risk regions (B6, B7, B8) are each guarded by a test that executes the real module and asserts observable behavior; the suite stays green via the single command.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-07-SUMMARY.md` when done.
</output>
