---
phase: 30-test-harness-coverage
plan: 06
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-issue-delta.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The issue severity before→after delta is set by CLICKING the real App.createSeverityScale widget (firing its onChange), and the observable deltaEl reflects after−before across positive (e.g. +4), negative/zero, and no-change cases (D-07, D-08, TEST-03e, F-B)"
    - "Adding/removing issue rows then submitting sessionForm yields the expected addSession payload issues shape; an empty issue row blocks submission (showToast issueMissing + no write) via validateIssues (TEST-03e, F-D)"
    - "The test executes the real add-session.js issue handlers under jsdom via the async capture-and-await handler pattern (no vacuous green) and survives an internal rename but fails on an observable-output change (D-08, D-12, F-A)"
  artifacts:
    - path: tests/30-issue-delta.test.js
      provides: "Executing characterization of issue severity delta (via the real severity widget) + addSession payload shape (via the sessionForm submit seam) + empty-validation"
  key_links:
    - from: tests/30-issue-delta.test.js
      to: assets/app.js
      via: "uses the REAL App.createSeverityScale/getSeverityValue (loaded from app.js, injected via createAppStub overrides) so updateDelta is reachable through observable DOM clicks"
      pattern: "createSeverityScale"
    - from: tests/30-issue-delta.test.js
      to: tests/_helpers/mock-portfolio-db.js
      via: "fires sessionForm submit and asserts mockDb.__calls.get('addSession')[0][0].issues; assertNoWrites for the empty/invalid case"
      pattern: "addSession"
---

<objective>
Characterize the uncovered issue-management region of `assets/add-session.js` (lines 502-675) before the Phase 31 refactor (D-07, scoped by D-10), via an EXECUTING jsdom real-page test (D-09) that asserts observable output only (D-08):
- the issue severity before→after DELTA (`updateDelta`), driven through the REAL `App.createSeverityScale` widget (F-B): the delta is `afterValue − beforeValue` plus a sign and a display toggle, and it is reachable ONLY by clicking the real scale (which fires `onChange` → `updateDelta`) and read back via the real `App.getSeverityValue` — NOT by calling `updateDelta` directly or by a label→number stub,
- the issues PAYLOAD shape, observed through the real `sessionForm` submit seam (F-D): `getIssuesPayload` is reachable only via the submit handler (:1684), which persists via `PortfolioDB.addSession`, so the test asserts the SHAPE on `mockDb.__calls.get('addSession')[0][0].issues`,
- the empty-row VALIDATION that blocks submission (`validateIssues`): an empty issue row → `App.showToast("", "toast.issueMissing")` and NO write (`assertNoWrites`).

All in one file, `tests/30-issue-delta.test.js`. The test EXECUTES the real handlers under jsdom and survives the Phase 31 internal rename but fails on an observable-output change (D-08/D-12). `add-session.js`/`add-session.html` are loaded read-only; no production code changes.

Honest rating note (F-B): the add-session logic this file pins is `afterValue − beforeValue` + sign + display toggle, plus the payload map/filter and a one-line `payload.length > 0` validation — keep the test, but do not oversell it as High×High; its value is guarding the Phase 31 rename, not exercising deep logic.

Purpose: Pin the issue-management observable behavior as part of the add-session pre-refactor green baseline that guards Phase 31.
Output: `tests/30-issue-delta.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Issue severity delta computation (TEST-03e)</name>
  <files>tests/30-issue-delta.test.js</files>
  <read_first>
    - assets/add-session.js (issue management, lines 502-675 — `createIssueBlock` builds `beforeScale`/`afterScale` via `App.createSeverityScale(initial, () => updateDelta(issueRef.obj))` and a `deltaEl`; `updateDelta` (:527-546) reads `App.getSeverityValue(beforeScale/afterScale)`, computes `afterValue − beforeValue`, sets `deltaEl.textContent = sign + delta`, toggles `delta-positive`/`delta-negative`, and `deltaEl.style.display = "none"` on zero; read-only, do NOT modify)
    - assets/app.js (lines 915-950 — the REAL `createSeverityScale(initialValue, onChange)`: clicking a `.severity-button` sets `wrap.dataset.value` and fires `onChange(i)`; and `getSeverityValue(wrapper)` reads `wrapper.dataset.value`. This pair loads cleanly in jsdom — the 30-03 RTL guard proves it; read-only, do NOT modify)
    - add-session.html (the issue-list container markup — the add-issue control and the row template the handlers clone)
    - tests/25-06-crop-only.test.js (THE async analog — `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE; the `docListeners` capture-and-await pattern for the async `DOMContentLoaded` handler; end-of-file `process.exit`)
    - tests/_helpers/app-stub.js (inject as `win.App`; supply the REAL severity pair via `overrides` — see action)
    - tests/24-06-spotlight-session-info.test.js (partial existing coverage of the issue area — reference for the seeded shape)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-issue-delta.test.js)
  </read_first>
  <action>
    Create `tests/30-issue-delta.test.js` as a jsdom real-page test using the ASYNC runner from `tests/25-06-crop-only.test.js`. The page handler is `document.addEventListener("DOMContentLoaded", async () => { await App.initCommon(); ... })` (add-session.js:44) — a synchronous `dispatchEvent` returns before the async continuation runs and yields a vacuous green (F-A). So: BEFORE evaling add-session.js, override `win.document.addEventListener` to record listeners into a `docListeners` map; after eval, `await` the captured `DOMContentLoaded` handler; after any async-driven event, `await new Promise(r => setTimeout(r, 0))`. Wrap every case in `await test('...', async () => {...})` inside an `(async () => {...})()` IIFE.
    Severity pair (F-B — CRITICAL): do NOT use a stubbed `getSeverityValue`. `updateDelta` reads severity ONLY through `App.getSeverityValue` and fires ONLY via the scale's `onChange`, so a label→number stub makes the delta unreachable through observable DOM. Instead obtain the REAL pair: eval `assets/app.js` into the jsdom window (it loads cleanly — the RTL guard does this), capture `realApp = win.App`, then inject `win.App = createAppStub({ createSeverityScale: realApp.createSeverityScale, getSeverityValue: realApp.getSeverityValue })` so the rest stays stubbed but the severity widget is real.
    Setup: load the real `add-session.html` body, inject the merged App (above) and a no-op `BroadcastChannel`, eval `assets/add-session.js`, and `await` the captured `DOMContentLoaded` handler (which calls `createIssueBlock()` once).
    Drive the REAL widget: in the rendered before-scale and after-scale of an issue row, CLICK the `.severity-button` for the desired value (this sets `dataset.value` and fires `onChange` → `updateDelta`). Then assert the OBSERVABLE `deltaEl`:
    - positive case (e.g. before 2 → after 6): `deltaEl.textContent === "+4"`, `deltaEl.classList.contains("delta-positive")` true, `deltaEl.style.display !== "none"`.
    - negative case (before 6 → after 2): textContent `"-4"`, `delta-negative` class.
    - no-change case (before n → after n): `deltaEl.style.display === "none"` (hidden on zero).
    Set before/after by CLICKING the real scale widget — NOT by calling `updateDelta`/internal functions and NOT by writing `dataset.value` directly.
    End-of-file guard (F-A): assert `assert.strictEqual(passed + failed, EXPECTED_COUNT)` (literal count across Task 1 + Task 2 cases) then `process.exit(failed === 0 ? 0 : 1)`.
    Assert observable delta output ONLY (D-08) — never internal function names. Open with the root-cause / falsifiability doc block. Load add-session.js/add-session.html/app.js unmodified.
  </action>
  <verify>
    <automated>node tests/30-issue-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-issue-delta.test.js` exits 0 against the current `assets/add-session.js`, with the observable severity delta equal to the expected after−before difference across the positive (`"+4"`, `delta-positive`), negative (`"-4"`, `delta-negative`), and no-change (hidden on zero) cases.
    - Before/after severities are set by CLICKING the REAL `App.createSeverityScale` widget (firing its change) — NOT by calling `updateDelta`/internal functions and NOT by writing `dataset.value` directly; the test asserts `deltaEl.textContent`, the `delta-positive`/`delta-negative` class, and hidden-on-zero (F-B).
    - The severity pair is the REAL `createSeverityScale`/`getSeverityValue` loaded from `assets/app.js` and injected via `createAppStub` overrides (falsifiable: a label→number stub leaves the delta unreachable and the assertions fail).
    - The async handler is captured-and-awaited (no blanket `dispatchEvent`); the end-of-file `assert.strictEqual(passed + failed, EXPECTED_COUNT)` guard fails the run if the async assertions never executed (F-A).
    - The test jsdom-LOADS and EXECUTES add-session.js (no source-slicing) and FAILS if the delta computation changes, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The issue severity before→after delta is pinned by an executing observable-behavior test that drives the real severity widget.</done>
</task>

<task type="auto">
  <name>Task 2: Issue payload shape + empty-row validation (TEST-03e, same file)</name>
  <files>tests/30-issue-delta.test.js</files>
  <read_first>
    - assets/add-session.js (the `sessionForm` submit seam, lines 1683-1771 — `getIssuesPayload`/`validateIssues` are reachable ONLY here: submit reads `clientSelect.value` + `sessionDate.value`, then `getIssuesPayload()`; if `!validateIssues(payload)` it calls `App.showToast("", "toast.issueMissing")` and returns; otherwise the new-session path persists via `PortfolioDB.addSession({ ..., issues: issuesPayload, ... })` at :1747; `getIssuesPayload` :662-671 maps `{name, before, after}` and filters out rows with no `name`; `validateIssues` :673-675 is `payload.length > 0`; read-only, do NOT modify)
    - add-session.html (the issue-list + the `#sessionForm`, `#clientSelect`, `#sessionDate` controls)
    - tests/_helpers/mock-portfolio-db.js (lines 50-128 — `createMockPortfolioDB` with `addSession` in WRITE_METHODS; `__calls.get('addSession')` arg-array spy; `assertNoWrites`)
    - tests/_helpers/app-stub.js (inject as `win.App`; `showToast` is spied via `__calls`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-issue-delta.test.js — "Assert severity before/after delta value, payload shape, empty-validation block"; §30-settings-section-roundtrip mock-DB wiring as the seam-based analog)
  </read_first>
  <action>
    Extend the SAME `tests/30-issue-delta.test.js` with payload + validation cases, reusing the async jsdom real-page setup from Task 1 (the real severity pair + the captured-and-awaited handler). The payload/validation are observable ONLY through the `sessionForm` submit seam (F-D), so wire the mock DB: inject `win.PortfolioDB = createMockPortfolioDB()` BEFORE evaling add-session.js.
    Valid case: set `clientSelect.value` to a valid client id (seed a matching `<option>` if needed) and `sessionDate.value` to a valid date; populate one or two issue rows via the real widgets — set a non-empty issue NAME (required: `getIssuesPayload` filters out nameless rows) and click the real severity scale for before/after; optionally add then remove a row. Fire the `sessionForm` submit handler (real `.dispatchEvent(new win.Event('submit', {cancelable:true}))` or invoking the captured submit listener), `await` the flush, and assert the OBSERVABLE payload SHAPE on `mockDb.__calls.get('addSession')[0][0].issues` — the right number of issue objects, each with `{name, before, after}` and the expected values (e.g. the remaining row after one add + one remove).
    Empty/invalid case: with a single EMPTY issue row (no name), set a valid client + date, fire submit, `await` the flush, and assert BOTH (a) `App.showToast` received `"toast.issueMissing"` (inspect `win.App.__calls.get('showToast')` for a call whose args include that key) AND (b) `assertNoWrites(mockDb)` — submission was blocked, nothing persisted.
    Assert observable payload + validation outcome ONLY (D-08) — never an internal flag/function name. Keep using the file's async runner + the shared end-of-file assertion-count guard (F-A). Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-issue-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-issue-delta.test.js` exits 0 with the added cases: a valid submit yields `mockDb.__calls.get('addSession')[0][0].issues` with the expected count + `{name, before, after}` fields (after one add and one remove); the payload is observed through the real `sessionForm` submit seam, not by calling `getIssuesPayload` directly (F-D).
    - The empty/invalid case asserts BOTH `App.showToast` received `"toast.issueMissing"` AND `assertNoWrites(mockDb)` — submission blocked, nothing persisted (F-D).
    - `win.PortfolioDB = createMockPortfolioDB()` is wired before eval; the submit is fired on the real `#sessionForm` (no internal-function calls); the async flush is awaited.
    - The test executes the real handlers (no source-slicing) and FAILS on a payload-shape or validation regression, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The issues payload shape (via the addSession submit seam) and empty-row validation (toast + no-write) are pinned by executing observable-behavior cases in the same test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | The test LOADS `add-session.html` + `assets/add-session.js` into jsdom read-only; no write-back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test file shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | the test asserts internal shape (false confidence, the D-12 risk) | medium | mitigate | D-08/D-12: the test jsdom-EXECUTES the issue handlers and asserts observable output (delta value, payload shape, validation outcome); acceptance requires "survives internal rename, fails on observable change" |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-issue-delta.test.js` (NEW — TEST-03e): executing characterization of issue severity delta (driven via the REAL `App.createSeverityScale` widget from app.js — F-B) + addSession payload shape (via the `sessionForm` submit seam + `mock-portfolio-db` — F-D) + empty-row validation (toast + `assertNoWrites`)

Scope note (D-10/D-14): only the uncovered issue-management observable behavior is pinned; out-of-scope files are not touched. (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-issue-delta.test.js` exits 0 covering delta via the real severity widget (Task 1) + addSession payload/validation via the submit seam (Task 2).
- The delta is driven by clicking the REAL `App.createSeverityScale`/`getSeverityValue` from app.js (F-B); the payload is observed via `mockDb.__calls.get('addSession')` (F-D).
- The async handler is captured-and-awaited with the end-of-file assertion-count guard (no vacuous green — F-A).
- The test executes add-session.js under jsdom (not source-sliced) and survives an internal rename (D-08/D-12).
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (add-session issue management): the severity delta, payload shape, and empty-row validation are captured as a pre-refactor green baseline that guards Phase 31.
- Stub-masking limitation (F-J): aside from the real severity pair, the surrounding App surface (`t`/`getSectionLabel`/`showToast`/…) is stubbed and `initCommon` is a no-op, so green here means the issue-management ORCHESTRATION (delta wiring, payload assembly, validation gate) is preserved — not that every therapist-visible label is correct. The Phase 31 D-13c glue cleanup could regress unrelated real output while this test stays green.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-06-SUMMARY.md` when done.
</output>
