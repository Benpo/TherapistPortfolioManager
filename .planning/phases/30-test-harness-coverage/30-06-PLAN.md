---
phase: 30-test-harness-coverage
plan: 06
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - tests/30-issue-delta.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The issue severity before→after delta is computed correctly across positive, negative/zero, and no-change cases (D-07, D-08, TEST-03e)"
    - "Adding/removing issue rows yields the expected getIssuesPayload shape, and an empty issue row blocks submission via validateIssues (TEST-03e)"
    - "The test executes the real add-session.js issue handlers under jsdom and survives an internal rename but fails on an observable-output change (D-08, D-12)"
  artifacts:
    - path: tests/30-issue-delta.test.js
      provides: "Executing characterization of issue severity delta + payload shape + empty-validation"
  key_links:
    - from: tests/30-issue-delta.test.js
      to: assets/add-session.js
      via: "loads add-session.html + add-session.js, drives the real issue-row handlers, asserts delta/payload/validation"
      pattern: "updateDelta"
    - from: tests/30-issue-delta.test.js
      to: tests/_helpers/app-stub.js
      via: "App.getSeverityValue maps severity labels to numeric values for the delta"
      pattern: "getSeverityValue"
---

<objective>
Characterize the highest value-per-effort uncovered region of `assets/add-session.js` — issue management (lines 502-662) — before the Phase 31 refactor (D-07, scoped by D-10), via an EXECUTING jsdom real-page test (D-09) that asserts observable output only (D-08):
- the issue severity before→after DELTA computation (`updateDelta`),
- the issues PAYLOAD shape (`getIssuesPayload`) after add/remove of issue rows,
- the empty-row VALIDATION that blocks submission (`validateIssues`).

All in one file, `tests/30-issue-delta.test.js`. The test EXECUTES the real handlers under jsdom and survives the Phase 31 internal rename but fails on an observable-output change (D-08/D-12). `add-session.js`/`add-session.html` are loaded read-only; no production code changes.

Purpose: Pin the issue-management observable behavior as part of the add-session pre-refactor green baseline that guards Phase 31.
Output: `tests/30-issue-delta.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-test-harness-coverage/30-RESEARCH.md
@.planning/phases/30-test-harness-coverage/30-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Issue severity delta computation (TEST-03e)</name>
  <files>tests/30-issue-delta.test.js</files>
  <read_first>
    - assets/add-session.js (issue management, lines 502-662 — `createIssueBlock`, `updateDelta`, `updateAddIssueState`, `updateRemoveButtons`; `App.getSeverityValue` maps a severity label to its number; read-only, do NOT modify)
    - add-session.html (the issue-list container markup — the add-issue control and the row template the handlers clone)
    - tests/_helpers/app-stub.js (inject as `win.App`; ensure `getSeverityValue` returns realistic numeric values for the seeded severities)
    - tests/24-06-spotlight-session-info.test.js (partial existing coverage of the issue area — reference for the seeded shape)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-issue-delta.test.js)
  </read_first>
  <action>
    Create `tests/30-issue-delta.test.js` as a jsdom real-page test. Load the real `add-session.html` body, inject `win.App = createAppStub()` (with `getSeverityValue` mapping severity labels to numbers) and a no-op `BroadcastChannel`, eval `assets/add-session.js`, and fire `DOMContentLoaded` (initCommon resolves so the handler completes).
    Drive the REAL issue handlers: add an issue row, set its before and after severity selects, and assert the OBSERVABLE computed delta value that `updateDelta` renders/stores (the before→after severity difference a therapist sees). Cover three cases: a positive delta (improvement), a negative-or-zero delta, and a no-change case.
    Assert observable delta output ONLY (D-08) — never internal function names. Open with the root-cause / falsifiability doc block; use the PASS/FAIL + exit-0/1 scaffold. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-issue-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-issue-delta.test.js` exits 0 against the current `assets/add-session.js`, with the observable severity delta equal to the expected before−after difference across the positive, negative/zero, and no-change cases.
    - The test jsdom-LOADS and EXECUTES add-session.js (no source-slicing) and FAILS if the delta computation changes, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The issue severity before→after delta is pinned by an executing observable-behavior test.</done>
</task>

<task type="auto">
  <name>Task 2: Issue payload shape + empty-row validation (TEST-03e, same file)</name>
  <files>tests/30-issue-delta.test.js</files>
  <read_first>
    - assets/add-session.js (issue management, lines 502-662 — `getIssuesPayload`, `validateIssues`, `removeIssue`, `updateRemoveButtons`, `updateAddIssueState`; read-only, do NOT modify)
    - add-session.html (the issue-list + submit/validation controls)
    - tests/_helpers/app-stub.js (inject as `win.App`)
    - .planning/phases/30-test-harness-coverage/30-PATTERNS.md (§30-issue-delta.test.js — "Assert severity before/after delta value, payload shape, empty-validation block")
  </read_first>
  <action>
    Extend the SAME `tests/30-issue-delta.test.js` with payload + validation cases, reusing the jsdom real-page setup from Task 1. Drive the real add/remove issue-row handlers (`createIssueBlock`, `removeIssue`, `updateRemoveButtons`) and assert the OBSERVABLE `getIssuesPayload` output shape — the issues array a save would persist (the right number of issue objects with the expected fields after one add and one remove). Then assert that `validateIssues` BLOCKS submission when an issue row is empty — observable via the validation outcome (submission prevented / the block surfaced), not via an internal flag name.
    Assert observable payload + validation outcome ONLY (D-08). Keep using the file's PASS/FAIL + exit-0/1 scaffold. Load add-session.js/add-session.html unmodified.
  </action>
  <verify>
    <automated>node tests/30-issue-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-issue-delta.test.js` exits 0 with the added cases: adding then removing issue rows yields the expected observable `getIssuesPayload` shape (correct count + fields), and an empty issue row blocks submission (observable validation outcome).
    - The test executes the real handlers (no source-slicing) and FAILS on a payload-shape or validation regression, but SURVIVES an internal rename (D-08/D-12).
    - No `assets/*` production file modified.
  </acceptance_criteria>
  <done>The issues payload shape and empty-row validation are pinned by executing observable-behavior cases in the same test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production source | The test LOADS `add-session.html` + `assets/add-session.js` into jsdom read-only; no write-back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information disclosure | new test file shipped to prod | low | mitigate | Inherited from 30-01: `tests/**` excluded from the Cloudflare static deploy |
| T-30-05 | Tampering | the test asserts internal shape (false confidence, the D-12 risk) | medium | mitigate | D-08/D-12: the test jsdom-EXECUTES the issue handlers and asserts observable output (delta value, payload shape, validation outcome); acceptance requires "survives internal rename, fails on observable change" |

No new dependency; no production change; no `high` severity items.
</threat_model>

<artifacts>
## Artifacts this plan produces
- `tests/30-issue-delta.test.js` (NEW — TEST-03e): executing characterization of issue severity delta + payload shape + empty-row validation

Scope note (D-10/D-14): only the uncovered issue-management observable behavior is pinned; out-of-scope files are not touched. (Full phase manifest in 30-01-PLAN.md.)
</artifacts>

<verification>
- `node tests/30-issue-delta.test.js` exits 0 covering delta (Task 1) + payload/validation (Task 2).
- The test executes add-session.js under jsdom (not source-sliced) and survives an internal rename (D-08/D-12).
- `npm test` stays green; no `assets/*` modified.
</verification>

<success_criteria>
- TEST-03 (add-session issue management): the severity delta, payload shape, and empty-row validation are captured as a pre-refactor green baseline that guards Phase 31.
</success_criteria>

<output>
Create `.planning/phases/30-test-harness-coverage/30-06-SUMMARY.md` when done.
</output>
