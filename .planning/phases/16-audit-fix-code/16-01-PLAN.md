---
phase: 16-audit-fix-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sw.js
  - license.html
  - impressum.html
  - datenschutz.html
  - demo.html
  - index.html
  - landing.html
  - add-client.html
  - add-session.html
  - sessions.html
  - reporting.html
  - disclaimer.html
autonomous: true
requirements: [FIX-01, FIX-07]

must_haves:
  truths:
    - "SW caches the 3 actual Rubik font weights, not the non-existent Variable file"
    - "App logo and shared botanical illustrations are precached for offline use"
    - "All 11 HTML pages register the service worker"
    - "All 11 HTML pages include a Content-Security-Policy meta tag"
  artifacts:
    - path: "sw.js"
      provides: "Corrected font cache + image assets + bumped cache version"
      contains: "sessions-garden-v19"
    - path: "license.html"
      provides: "SW registration + CSP meta tag"
      contains: "Content-Security-Policy"
    - path: "impressum.html"
      provides: "SW registration + CSP meta tag"
      contains: "serviceWorker"
    - path: "datenschutz.html"
      provides: "SW registration + CSP meta tag"
      contains: "serviceWorker"
    - path: "demo.html"
      provides: "SW registration + CSP meta tag"
      contains: "Content-Security-Policy"
  key_links:
    - from: "sw.js"
      to: "assets/fonts/Rubik-Regular.woff2"
      via: "PRECACHE_URLS array"
      pattern: "Rubik-Regular\\.woff2"
    - from: "all HTML pages"
      to: "sw.js"
      via: "serviceWorker.register"
      pattern: "serviceWorker\\.register"
---

<objective>
Fix the service worker font cache mismatch (references non-existent Rubik-Variable.woff2 instead of the 3 actual weight files), add app-critical images to precache, register the SW on the 4 pages that lack it, and add Content-Security-Policy meta tags to all 11 HTML pages.

Purpose: Ensure offline capability works correctly (fonts load, images cached) and add baseline security headers.
Output: Updated sw.js + all 11 HTML files with SW registration and CSP.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-audit-fix-code/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SW font cache, add image assets, bump cache version, add SW registration to 4 pages</name>
  <files>sw.js, license.html, impressum.html, datenschutz.html, demo.html</files>
  <read_first>sw.js, license.html, impressum.html, datenschutz.html, demo.html, index.html</read_first>
  <action>
In sw.js:
1. Line 12: Change `'sessions-garden-v18'` to `'sessions-garden-v19'`
2. Line 51: Replace `'/assets/fonts/Rubik-Variable.woff2'` with these 3 entries:
   - `'/assets/fonts/Rubik-Regular.woff2'`
   - `'/assets/fonts/Rubik-SemiBold.woff2'`
   - `'/assets/fonts/Rubik-Bold.woff2'`
3. After the font entries (before the demo.html block), add these image assets:
   - `'/assets/logo.png'`
   - `'/assets/illustrations/גינה.png'`
   - `'/assets/illustrations/משפך.png'`

In each of these 4 files (license.html, impressum.html, datenschutz.html, demo.html), add the SW registration snippet before the closing `</body>` tag:
```html
<script>if('serviceWorker' in navigator){window.addEventListener('load',function(){navigator.serviceWorker.register('/sw.js').catch(function(e){console.warn('SW registration failed:',e)});});}</script>
```
Use the exact same one-liner pattern already present in index.html.
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && grep -c "Rubik-Variable" sw.js && echo "FAIL: Variable still present" || echo "PASS: Variable removed" && grep -c "Rubik-Regular" sw.js && grep -c "Rubik-SemiBold" sw.js && grep -c "Rubik-Bold" sw.js && grep -c "logo.png" sw.js && grep -c "sessions-garden-v19" sw.js && for f in license.html impressum.html datenschutz.html demo.html; do grep -c "serviceWorker" "$f" || echo "FAIL: $f missing SW"; done</automated>
  </verify>
  <acceptance_criteria>
    - sw.js contains `sessions-garden-v19` (not v18)
    - sw.js contains `Rubik-Regular.woff2`, `Rubik-SemiBold.woff2`, `Rubik-Bold.woff2`
    - sw.js does NOT contain `Rubik-Variable`
    - sw.js contains `logo.png` in PRECACHE_URLS
    - license.html contains `serviceWorker.register`
    - impressum.html contains `serviceWorker.register`
    - datenschutz.html contains `serviceWorker.register`
    - demo.html contains `serviceWorker.register`
  </acceptance_criteria>
  <done>SW caches correct fonts and images; all 4 missing pages now register SW; cache version bumped to v19</done>
</task>

<task type="auto">
  <name>Task 2: Add Content-Security-Policy meta tag to all 11 HTML pages</name>
  <files>index.html, landing.html, license.html, disclaimer.html, impressum.html, datenschutz.html, demo.html, add-client.html, add-session.html, sessions.html, reporting.html</files>
  <read_first>index.html, landing.html, license.html, disclaimer.html, impressum.html, datenschutz.html, demo.html, add-client.html, add-session.html, sessions.html, reporting.html</read_first>
  <action>
Add this CSP meta tag inside the `<head>` section of ALL 11 HTML files, immediately after the `<meta charset>` tag:

```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' https://api.lemonsqueezy.com; font-src 'self'">
```

The 11 files are: index.html, landing.html, license.html, disclaimer.html, impressum.html, datenschutz.html, demo.html, add-client.html, add-session.html, sessions.html, reporting.html.

Notes on the policy:
- `'unsafe-inline'` for script-src and style-src is required because pages use inline `<script>` blocks for theme/gate checks
- `data:` and `blob:` for img-src are needed for photo handling (canvas crop, data URLs)
- `connect-src` allows the Lemon Squeezy API call from license.js
- `font-src 'self'` allows loading self-hosted Rubik fonts
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && count=$(grep -rl "Content-Security-Policy" *.html | wc -l) && echo "CSP found in $count HTML files" && test "$count" -eq 11 && echo "PASS" || echo "FAIL: expected 11"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Content-Security-Policy" index.html` returns 1
    - `grep -c "Content-Security-Policy" landing.html` returns 1
    - `grep -c "Content-Security-Policy" license.html` returns 1
    - `grep -c "Content-Security-Policy" disclaimer.html` returns 1
    - `grep -c "Content-Security-Policy" impressum.html` returns 1
    - `grep -c "Content-Security-Policy" datenschutz.html` returns 1
    - `grep -c "Content-Security-Policy" demo.html` returns 1
    - `grep -c "Content-Security-Policy" add-client.html` returns 1
    - `grep -c "Content-Security-Policy" add-session.html` returns 1
    - `grep -c "Content-Security-Policy" sessions.html` returns 1
    - `grep -c "Content-Security-Policy" reporting.html` returns 1
    - All CSP tags include `connect-src 'self' https://api.lemonsqueezy.com`
  </acceptance_criteria>
  <done>All 11 HTML pages have identical CSP meta tags restricting resource loading to self + required exceptions</done>
</task>

</tasks>

<verification>
- `grep "Rubik-Variable" sw.js` returns nothing
- `grep -c "Rubik-Regular\|Rubik-SemiBold\|Rubik-Bold" sw.js` returns 3
- `grep -c "sessions-garden-v19" sw.js` returns 1
- All 11 HTML files contain `serviceWorker.register`
- All 11 HTML files contain `Content-Security-Policy`
</verification>

<success_criteria>
SW caches correct font files (3 weights, not Variable); app images are precached; all 11 HTML pages register SW and include CSP meta tag. Cache version bumped to v19.
</success_criteria>

<output>
After completion, create `.planning/phases/16-audit-fix-code/16-01-SUMMARY.md`
</output>
