---
phase: 16-audit-fix-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/landing.js
  - landing.html
  - assets/landing.css
  - assets/demo.js
  - demo.html
autonomous: true
requirements: [FIX-05, FIX-06]

must_haves:
  truths:
    - "Landing page postMessage uses explicit origin, not wildcard"
    - "Broken botanical image path in landing.html is corrected"
    - "Landing CSS botanical positioning uses logical properties for RTL"
    - "Demo page validates postMessage origin before processing"
    - "Demo page localStorage gate uses sessionStorage so values don't persist"
  artifacts:
    - path: "assets/landing.js"
      provides: "Explicit origin postMessage calls"
      contains: "window.location.origin"
    - path: "landing.html"
      provides: "Correct image path for botanical"
      contains: "illustrations/גינה 2.png"
    - path: "assets/demo.js"
      provides: "Origin validation on message handler"
      contains: "event.origin"
    - path: "demo.html"
      provides: "sessionStorage instead of localStorage for gate bypass"
      contains: "sessionStorage"
  key_links:
    - from: "assets/landing.js"
      to: "demo.html iframe"
      via: "postMessage with explicit origin"
      pattern: "window\\.location\\.origin"
    - from: "assets/demo.js"
      to: "message event handler"
      via: "origin validation guard"
      pattern: "event\\.origin.*window\\.location\\.origin"
---

<objective>
Harden security on landing page and demo page: replace wildcard postMessage origins with explicit window.location.origin, add origin validation to demo message handler, fix demo localStorage gate bypass by switching to sessionStorage, fix broken botanical image path, and convert landing CSS botanical positioning to logical properties for RTL.

Purpose: Close security audit findings (HIGH severity postMessage issues, MEDIUM gate bypass) and fix broken assets.
Output: Updated landing.js, landing.html, landing.css, demo.js, demo.html
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-audit-fix-code/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page postMessage hardening, broken image fix, RTL CSS logical properties</name>
  <files>assets/landing.js, landing.html, assets/landing.css</files>
  <read_first>assets/landing.js, landing.html, assets/landing.css</read_first>
  <action>
In assets/landing.js:
1. Find ALL `postMessage` calls that use `'*'` as the target origin. There should be at least 2 (around lines 491 and 603 based on audit). Replace each `'*'` with `window.location.origin`. Example:
   - Before: `demoIframe.contentWindow.postMessage({ type: 'demo-lang', lang: lang }, '*')`
   - After: `demoIframe.contentWindow.postMessage({ type: 'demo-lang', lang: lang }, window.location.origin)`
   Do a thorough search for ALL `'*'` arguments in postMessage calls throughout the file.

In landing.html:
1. Find the broken image reference `src="./assets/גינה 2.png"` (around line 278). Change it to `src="./assets/illustrations/גינה 2.png"` (add the `illustrations/` subdirectory).

In assets/landing.css:
1. Around line 211: Change `left: -10px` to `inset-inline-start: -10px` (for `.hero-botanical-left` or equivalent selector)
2. Around line 221: Change `right: -10px` to `inset-inline-end: -10px` (for `.hero-botanical-right` or equivalent selector)
3. In the media query section (around lines 244-245): Apply the same `left` -> `inset-inline-start` and `right` -> `inset-inline-end` conversions for the responsive variants of these botanical positioning rules.
Note: Read the file first to confirm exact selectors and line numbers, as they may have shifted.
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && grep -n "'\\*'" assets/landing.js | head -5 && echo "---" && grep -c "window.location.origin" assets/landing.js && echo "---" && grep "גינה 2" landing.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'\\*'" assets/landing.js` returns 0 matches (no wildcard postMessage)
    - `grep -c "window.location.origin" assets/landing.js` returns at least 2
    - `grep "גינה 2" landing.html` shows path includes `illustrations/`
    - `grep "inset-inline-start" assets/landing.css` returns at least 1 match
    - `grep "inset-inline-end" assets/landing.css` returns at least 1 match
  </acceptance_criteria>
  <done>Landing page postMessage uses explicit origins; broken image path fixed; botanical CSS uses logical properties</done>
</task>

<task type="auto">
  <name>Task 2: Demo page postMessage origin validation and sessionStorage gate fix</name>
  <files>assets/demo.js, demo.html</files>
  <read_first>assets/demo.js, demo.html</read_first>
  <action>
In assets/demo.js:
1. Find the `message` event listener (around line 11 or wherever `window.addEventListener('message', ...)` is). At the very top of the handler function, before any processing, add this origin validation guard:
   ```javascript
   if (event.origin !== window.location.origin) return;
   ```
   This ensures only same-origin messages are processed, preventing cross-origin injection.

In demo.html:
1. Find the inline `<script>` block that sets localStorage keys for the demo gate bypass (around lines 8-9). It should contain lines like:
   - `localStorage.setItem('portfolioTermsAccepted', ...)`
   - `localStorage.setItem('portfolioLicenseActivated', ...)`
2. Change ALL `localStorage.setItem` calls in this inline script to `sessionStorage.setItem`:
   - `sessionStorage.setItem('portfolioTermsAccepted', 'true')`
   - `sessionStorage.setItem('portfolioLicenseActivated', 'true')`
   This ensures the demo gate bypass only lasts for the current tab session and does not persist, preventing users from bypassing the real license gate after visiting the demo.
3. If there are any corresponding `localStorage.getItem` calls in the same inline script block, change those to `sessionStorage.getItem` as well.
  </action>
  <verify>
    <automated>cd /Users/ben/Claude-Code-Sandbox/TherapistPortfolioManager_app && grep -n "event.origin" assets/demo.js && echo "---" && grep -n "localStorage.*portfolioTerms\|localStorage.*portfolioLicense" demo.html | head -5 && echo "---" && grep -n "sessionStorage" demo.html | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "event.origin" assets/demo.js` returns at least 1 match showing the validation guard
    - `grep "localStorage.setItem.*portfolioTerms" demo.html` returns 0 matches
    - `grep "localStorage.setItem.*portfolioLicense" demo.html` returns 0 matches
    - `grep "sessionStorage.setItem" demo.html` returns at least 2 matches (terms + license)
  </acceptance_criteria>
  <done>Demo page validates postMessage origin; localStorage gate bypass fixed with sessionStorage so demo values don't persist across sessions</done>
</task>

</tasks>

<verification>
- `grep "'\\*'" assets/landing.js` returns 0 results
- `grep "event.origin" assets/demo.js` shows origin check
- `grep "localStorage.*portfolioTerms" demo.html` returns 0 results
- `grep "sessionStorage" demo.html` returns matches
- Broken image path corrected
</verification>

<success_criteria>
All postMessage calls use explicit origins. Demo page validates incoming message origins. Demo localStorage gate bypass fixed with sessionStorage. Broken image path corrected. Landing CSS uses logical properties for botanical positioning.
</success_criteria>

<output>
After completion, create `.planning/phases/16-audit-fix-code/16-02-SUMMARY.md`
</output>
