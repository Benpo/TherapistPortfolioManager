---
phase: 41-replayable-guided-tour
plan: 03
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - assets/tour.js
  - assets/tour.css
  - tests/41-launch-explicit.test.js
  - tests/41-fallback-degradation.test.js
  - tests/41-resume-state.test.js
autonomous: true
requirements: [TOUR-01, TOUR-02, TOUR-03]
must_haves:
  truths:
    - "window.Tour exposes start()/resume()/isActive() and runs ONLY on an explicit start() call — nothing in tour.js auto-runs on load (TOUR-01)."
    - "render() resolves each step anchor through the INJECTABLE seam Tour._isAnchorVisible(el) (default: el && el.offsetParent !== null, exposed for test stubbing — architect-gate A5): visible → spotlight ring + tethered tooltip; missing/hidden → centered fallback modal carrying the SAME step text + a working 'Take me there' link — NEVER a silent skip (TOUR-02)."
    - "Tour.start() is PAGE-AWARE (architect-gate A2): if the current page differs from STEPS[0].page it persists sg.tourResume {tourId, stepIndex:0} and navigates (location.href) to STEPS[0].page so resume() renders step 1's SPOTLIGHT there — a launch from sessions/reporting/add-session must NOT land on the fallback modal (D-02: a fresh empty app shows spotlights, not fallbacks)."
    - "Next on a step whose page differs from the current page writes sessionStorage {tourId, stepIndex} then navigates (location.href); on load, Tour.resume() reads-and-continues the run (TOUR-03 / D-06)."
    - "The fallback modal's 'Take me there' link persists sg.tourResume {tourId, stepIndex:<current>} BEFORE navigating to takeMeThereHref (architect-gate A3), so resume() re-renders the SAME step on the target page (anchor now present → spotlight) — the recovery action never silently terminates the tour."
    - "sessionStorage is the ONLY resume tier and is cleared on finish and on mid-tour close; a relaunch with no resume key always restarts from step 1 (D-09 / Pitfall 6)."
    - "All tour copy is injected via textContent; the only innerHTML is a compile-time-literal inline SVG with zero interpolation (V5 / T-39-05)."
    - "The full-viewport dim overlay intercepts pointer events so the underlying page is inert (D-07) WITHOUT freezing scroll — the engine does NOT call App.lockBodyScroll for spotlight steps (its .is-modal-open position:fixed would freeze the viewport and defeat scrollIntoView for below-the-fold anchors, steps 5-6; architect-gate A4). Scroll stays free; the engine scrollIntoView's each anchor then repositions spotlight+tooltip on scroll/resize (sketch :1115-1120, rAF-debounced). App.lockBodyScroll MAY be reused only for the centered fallback modal (no anchor to track)."
  prohibitions:
    - statement: "No tour step is ever silently skipped when its anchor is missing or hidden — the fallback modal renders instead."
      status: resolved
      verification: "tests/41-fallback-degradation.test.js — remove/hide an anchor, assert the fallback modal + Take-me-there renders and stepIndex does not advance past it"
    - statement: "Resume state never persists across launches (no localStorage resume tier, no offer-to-resume)."
      status: resolved
      verification: "tests/41-resume-state.test.js — relaunch with an empty sessionStorage resolves to step 1"
  artifacts:
    - "assets/tour.js — bespoke IIFE engine (window.Tour), STEPS[10], render loop ported from sketch 003"
    - "assets/tour.css — spotlight, tethered tooltip, RTL-safe arrow, fallback modal (tokens + logical props only)"
    - "tests/41-launch-explicit.test.js, 41-fallback-degradation.test.js, 41-resume-state.test.js"
  key_links:
    - "STEPS[].anchor values match the Plan 02 data-tour contract exactly"
    - "STEPS[].i18nKey / fallbackBody / takeMeThere resolve against the Plan 01 help.tour.* keys"
    - "Tour.resume() is called from app.js initCommon (wired in Plan 05); Tour.start() is called from launch surfaces (Plan 05)"
---

<objective>
Build the bespoke tour engine core: `assets/tour.js` (IIFE `window.Tour`) and `assets/tour.css`, porting the render loop from the validated sketch `.planning/sketches/003-tour-fallback/index.html` and adapting it to the 10-step full-spine route with cross-page navigation + sessionStorage resume (the genuinely new work — the sketch is single-page).

Purpose: This is the highest-fragility surface in the milestone. It delivers explicit-launch-only running (TOUR-01), graceful anchor degradation that never silently skips (TOUR-02), and cross-page resume with relaunch-from-step-1 semantics (TOUR-03 / D-09). Language re-render, exit choice, finish card, and bottom-sheet come in Plan 04; wiring into launch surfaces + coordinator in Plan 05.
Output: tour.js + tour.css + three write-first behavior tests.
</objective>

<artifacts_produced>
Symbols this plan creates:
- Global: `window.Tour` with public methods `start()`, `resume()`, `isActive()`, the injectable visibility seam `_isAnchorVisible(el)` (default `el && el.offsetParent !== null`; exposed so jsdom tests can stub BOTH branches — architect-gate A5), and `_`-prefixed test seams (e.g. `_getStepIndex`, `_resolveStep`) — planner's discretion on exact seam names, follow the attention-coordinator.js `_getSurface` precedent
- Module internals: `STEPS` array (10 entries: {id, page, anchor, i18nKey, takeMeThereHref, screenName}), `render()`, `next()`, `prev()`, `clearTourChrome()`, `endTour()`
- sessionStorage key: `sg.tourResume` (JSON {tourId, stepIndex}); localStorage flag `sg.tourCompleted` is set by Plan 04 finish — this plan only clears sg.tourResume
- New files: `assets/tour.js`, `assets/tour.css`
- New tests: `tests/41-launch-explicit.test.js`, `tests/41-fallback-degradation.test.js`, `tests/41-resume-state.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-RESEARCH.md
@.planning/phases/41-replayable-guided-tour/41-PATTERNS.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/sketches/003-tour-fallback/index.html
@.planning/sketches/003-tour-fallback/README.md
@assets/attention-coordinator.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the RED engine behavior tests (launch-explicit, fallback-degradation, resume-state)</name>
  <read_first>
    - .planning/phases/41-replayable-guided-tour/41-RESEARCH.md (Validation Architecture — the Req→Test map + Wave 0 gaps)
    - tests/40-i18n-parity.test.js (vm-sandbox load idiom for evaluating real source into a jsdom-ish context)
    - .planning/codebase/TESTING.md (runner contract: node assert, jsdom v29, exit 0/1, no layout engine)
    - assets/attention-coordinator.js (the module-shape + storage-helper idiom the engine mirrors)
  </read_first>
  <behavior>
    - launch-explicit: loading tour.js does NOT start a tour (no chrome mounted, isActive() false) until Tour.start() is called; after start() ON step 1's page, isActive() true and step-1 chrome exists. Additionally (architect-gate A2): calling Tour.start() while the current page is NOT STEPS[0].page persists sg.tourResume = {tourId, stepIndex:0} and sets the navigation target to STEPS[0].page (assert the sessionStorage write + the attempted href; stub window.location so jsdom does not actually navigate) — it does NOT immediately mount step-1 chrome on the wrong page.
    - fallback-degradation: drive BOTH render branches via the injectable seam (architect-gate A5). (a) Stub Tour._isAnchorVisible → true with the anchor present: render() mounts the SPOTLIGHT chrome and NO fallback modal. (b) Stub Tour._isAnchorVisible → false (or remove the anchor): render() mounts the centered fallback modal containing the step's text + a "Take me there" link; stepIndex does NOT advance past the missing step (no silent skip). Activating "Take me there" persists sg.tourResume = {tourId, stepIndex:<current>} BEFORE navigating to takeMeThereHref (architect-gate A3) — assert the resume-key write carries the CURRENT stepIndex.
    - resume-state: crossing to a step on another page (next()) writes sessionStorage sg.tourResume = {tourId, stepIndex}; Tour.resume() with that key present continues at stepIndex; Tour.resume() (or start() on step 1's page) with sg.tourResume ABSENT resolves to step 1 (D-09); endTour() clears sg.tourResume.
  </behavior>
  <action>
    Author three jsdom/vm behavior tests that load assets/tour.js into a sandbox with a stubbed window.I18N (a minimal dict resolving the help.tour.* keys) and a jsdom document. tests/41-launch-explicit.test.js proves no-auto-run + explicit start (TOUR-01) AND the A2 page-aware start: with the sandbox's current page set to a non-step-1 page (e.g. STEPS pointing step 1 at index.html while the doc simulates sessions.html), Tour.start() writes sg.tourResume {stepIndex:0} and targets STEPS[0].page rather than mounting a fallback (stub/capture window.location.href assignment; assert the sessionStorage write deterministically). tests/41-fallback-degradation.test.js drives BOTH branches through Tour._isAnchorVisible stubbing (A5): true→spotlight-present/no-fallback, false→fallback+Take-me-there present and stepIndex unchanged (never a silent skip, TOUR-02); it also asserts the Take-me-there activation writes sg.tourResume with the current stepIndex before navigation (A3). tests/41-resume-state.test.js exercises the next() sessionStorage round-trip and the D-09 relaunch-from-step-1 + clear-on-end semantics (TOUR-03). Use the module's `_`-prefixed seams to read stepIndex/resolve steps. Author all three RED (tour.js does not exist yet) — each must exit 1 until Task 2. Do NOT stub the engine's own logic beyond the documented _isAnchorVisible seam and window.location; drive the real ported functions. NOTE jsdom has no layout engine (getBoundingClientRect returns zeros) AND hardcodes offsetParent===null for every element — so branch selection MUST be driven via the _isAnchorVisible seam, never via real offsetParent; assert node presence + text + href + state, NOT pixel geometry (geometry is the Plan 07 WebKit gate).
  </action>
  <verify>
    <automated>for t in launch-explicit fallback-degradation resume-state; do node tests/41-$t.test.js; test $? -eq 1 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist and each exits 1 (RED) standalone (tour.js absent)
    - launch-explicit asserts the A2 page-aware start: start() off step-1's page writes sg.tourResume {stepIndex:0} and targets STEPS[0].page (grep the file for the sessionStorage assertion + a location stub)
    - fallback-degradation drives BOTH branches via Tour._isAnchorVisible stubbing (grep for `_isAnchorVisible`), asserts spotlight-present-on-true AND fallback-present-on-false, that stepIndex did not advance (no-skip), and that Take-me-there writes sg.tourResume with the current stepIndex (A3)
    - resume-state asserts the absent-key path resolves to step 1 (D-09) and that endTour clears sg.tourResume
    - No test asserts getBoundingClientRect pixel values and no test relies on real offsetParent for branch selection (jsdom-no-layout / offsetParent-null discipline)
  </acceptance_criteria>
  <done>Three RED behavior tests pin TOUR-01/02/03 core semantics before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement assets/tour.js engine core</name>
  <read_first>
    - .planning/sketches/003-tour-fallback/index.html (PORT SOURCE — STEPS ~818-852, render ~910-1022, tooltip/arrow geometry ~922-952, fallback modal ~954-1021, clearTourChrome ~902-908, scroll/resize listeners ~1115-1118)
    - assets/attention-coordinator.js (lines 40-82 — IIFE shape, 'use strict', four-slot banner, lsGet/lsSet/ssGet/ssSet storage helpers, t() i18n resolver, isDemo(), the window.X footer)
    - assets/app.js (lines 1671-1685 — lockBodyScroll/unlockBodyScroll via window.App; REUSE ONLY for the centered fallback modal, NOT for spotlight steps — see A4; line 476-588 initHelpEntry for the makeEl+data-i18n+textContent idiom and the _listenerInstalled idempotency pattern)
    - assets/app.css (lines 1-19 — the --z-* token scale (max --z-banner:500) for the A10 tour z-index tokens, and the `.is-modal-open { position: fixed }` scroll-freeze that A4 says spotlight steps must NOT trigger)
    - .planning/phases/41-replayable-guided-tour/41-PATTERNS.md (Pattern Assignments for assets/tour.js — the exact snippets to adapt)
    - tests/41-launch-explicit.test.js, tests/41-fallback-degradation.test.js, tests/41-resume-state.test.js (the contract to satisfy)
  </read_first>
  <action>
    Create assets/tour.js as a bespoke IIFE assigning window.Tour, with a four-slot responsibility banner (mirror attention-coordinator.js). Copy the lsGet/lsSet/ssGet/ssSet + t() helpers from the coordinator idiom (do not import). Define STEPS as the 10-entry declarative array; each entry = {id, page, anchor:'[data-tour="…"]', i18nKey:'help.tour.step.<name>', takeMeThereHref, screenName} using the Plan 02 anchor values (overview/add-client/add-session/session-setup/session-heart/session-save/sessions/reporting/backup/help) mapped to pages index/add-session/sessions/reporting.html, with steps backup+help continuing on reporting.html.
    VISIBILITY SEAM (architect-gate A5): expose `_isAnchorVisible(el)` on window.Tour with the default implementation `return !!(el && el.offsetParent !== null);`. render() MUST call this seam (never inline the offsetParent check) so jsdom tests — where offsetParent is hardcoded null for every element — can stub it to force BOTH branches.
    render() (port sketch 910-1022): resolve el=document.querySelector(step.anchor); visible = Tour._isAnchorVisible(el); if visible → mount spotlight ring (fixed, box-shadow accent ring + 9999px dim, logical inset-block/inline-start from getBoundingClientRect) and the tethered tooltip + arrow with data-arrow + --arrow-x (D-04, logical properties only so the arrow flips for free in RTL), else → centered fallback modal that sets its body from help.tour.fallbackBody with {screen} replaced by step.screenName and wires a "Take me there" link that FIRST persists ssSet('sg.tourResume', JSON.stringify({tourId, stepIndex})) with the CURRENT stepIndex and THEN sets location.href = step.takeMeThereHref (architect-gate A3 — so resume() re-renders the SAME step on the target page where the anchor should be present → spotlight; NEVER navigate without persisting resume state, NEVER advance past a missing anchor). All copy via t()+textContent; the ONLY innerHTML is a compile-time-literal inline SVG (reuse the colored export glyph literal for step-6 tooltip only, zero interpolation).
    start() (architect-gate A2 — page-aware): set stepIndex=0; if the current page !== STEPS[0].page, persist ssSet('sg.tourResume', JSON.stringify({tourId, stepIndex:0})) then navigate location.href = STEPS[0].page (resume() renders step 1's spotlight on arrival — a launch from sessions/reporting/add-session must reach step 1's page, NOT drop onto the fallback modal, D-02); otherwise render() step 1 in place. Explicit only — nothing auto-runs on load.
    next(): if STEPS[stepIndex+1].page !== current page → ssSet('sg.tourResume', JSON.stringify({tourId, stepIndex: stepIndex+1})) then location.href = that step's page; else stepIndex++ and render(). prev() decrements within-page. resume(): read+parse sg.tourResume; if present and STEPS[stepIndex].page matches the current page, set stepIndex and render(); on any parse error, page mismatch, or absent key, do nothing (a fresh launch calls start() → step 1, D-09). endTour(): clearTourChrome(), ssRemove('sg.tourResume'), release the inert overlay.
    PAGE-INERT WITHOUT SCROLL FREEZE (architect-gate A4): mount a full-viewport fixed overlay layer that INTERCEPTS pointer events (clicks/taps) so the underlying page is inert (D-07); the spotlight ring is pointer-events:none (purely visual cut-out) and the tooltip/fallback chrome is pointer-events:auto above it. Do NOT call window.App.lockBodyScroll for spotlight steps — its `.is-modal-open { position: fixed }` (app.css:14-19) freezes the viewport, which would make scrollIntoView unable to reach the below-the-fold anchors on steps 5-6 (add-session.html:204 / :366). Page scroll therefore stays free; for each spotlight step scrollIntoView the anchor, then reposition the spotlight+tooltip on scroll + resize while active, rAF-debounced (port sketch :1115-1120). App.lockBodyScroll/unlockBodyScroll (typeof-guarded) MAY be reused ONLY for the centered fallback modal (no anchor to track, so freezing is harmless) — spotlight steps must not freeze the body.
    Modern evergreen baseline, no legacy shims (D-15). Expose start/resume/isActive + _isAnchorVisible + `_`-prefixed seams the tests use.
  </action>
  <verify>
    <automated>node tests/41-launch-explicit.test.js && node tests/41-fallback-degradation.test.js && node tests/41-resume-state.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - The three Plan-03 behavior tests all exit 0 (including the A2 page-aware-start, A3 take-me-there-persists-resume, and A5 both-branch assertions)
    - grep -c "window.Tour" assets/tour.js returns >=1; grep -c "_isAnchorVisible" assets/tour.js returns >=1 and the default impl uses offsetParent (grep -c "offsetParent" >=1) — render() calls the seam, not an inline offsetParent check (A5)
    - grep -c "innerHTML" assets/tour.js is 0 OR every match is the compile-time SVG literal (no variable interpolation into innerHTML)
    - STEPS contains 10 entries and every anchor value matches a Plan 02 data-tour value (grep for '[data-tour="overview"]' … '[data-tour="help"]')
    - Page inertness comes from a pointer-events overlay, NOT a scroll freeze (A4): the spotlight path does NOT call App.lockBodyScroll; grep -c "scrollIntoView" assets/tour.js >=1 AND a scroll+resize reposition listener is installed (grep for a rAF-debounced scroll/resize handler). Any App.lockBodyScroll reference is confined to the fallback-modal branch
    - start() is page-aware and the fallback "Take me there" writes sg.tourResume before navigating (grep for ssSet('sg.tourResume' in both the start() off-page path and the take-me-there handler)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>window.Tour renders spotlight/tooltip vs fallback correctly, navigates+resumes across pages, restarts from step 1 on relaunch; all three behavior tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Author assets/tour.css core surfaces (spotlight, tooltip, arrow, fallback)</name>
  <read_first>
    - .planning/sketches/003-tour-fallback/index.html (the <style> block ~292-323 for spotlight/tooltip/arrow geometry to port)
    - assets/tokens.css (semantic --color-* / --shadow-* palette — the ONLY color source; no literal hex. NOTE: --space-*, --text-*, --radius-md and --tap-target-min are NOT defined here — they are local to help.css/.help-root and app.css, so tour.css must define its own copy — architect-gate A8)
    - assets/help.css (lines ~22-52 — the .help-root local token-scale block: --space-xs..3xl, --text-body/label/heading/display, --radius-md:12px, --tap-target-min:44px — the exact pattern tour.css reproduces under its own tour root class)
    - assets/app.css (lines 1-11 — the --z-* token scale, max --z-banner:500; the A10 tour z-index tokens sit deliberately above it)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Interaction Contract — exact spotlight box-shadow, tooltip min/max-width 16rem/22rem, z-index 9100, arrow 14px, tap-target 44px, color discipline: accent ONLY on Next/Done + spotlight ring)
  </read_first>
  <action>
    Create assets/tour.css scoped under a tour root class. FIRST author a LOCAL TOKEN-SCALE BLOCK on that tour root class (architect-gate A8), mirroring help.css's .help-root block: tokens.css supplies the semantic --color-*/--shadow-* palette GLOBALLY, but --space-*, --text-*, --radius-md and --tap-target-min are NOT in tokens.css — they are local to help.css/app.css, and help.css is absent on 7 of the 8 chrome pages tour.css loads on (Plan 06), so var(--tap-target-min) etc. would resolve to nothing unless tour.css DEFINES them itself. Define at minimum: --space-sm/md/lg (+ --space-xl for the Plan 04 finish card), --radius-md:12px, --tap-target-min:44px, --text-display:1.75rem (Plan 04 finish headline), AND the A10 z-index tokens --z-tour-overlay:9000 / --z-tour-tooltip:9100 (values intentionally exceed the app's --z-banner:500 because the tour must overlay ALL app chrome incl. banners/modals — add a one-line comment justifying the departure from the app.css --z-* scale).
    Then author: the spotlight (position:fixed, z-index:var(--z-tour-overlay), border-radius var(--radius-md), pointer-events:none, box-shadow 0 0 0 4px var(--color-primary), 0 0 0 9999px var(--color-modal-overlay-bg), transition all .32s cubic-bezier(.4,0,.2,1)); the pointer-events-capturing inert overlay layer (position:fixed inset:0, z-index:var(--z-tour-overlay), pointer-events:auto — the A4 page-inert layer that does NOT freeze scroll); the tethered tooltip (position:fixed, z-index:var(--z-tour-tooltip), min-width:16rem, max-width:22rem, --color-surface bg, 1px --color-border, var(--radius-md), var(--shadow-modal), padding-block/padding-inline var(--space-md) — logical properties only); the RTL-safe arrow (14x14 rotated square via data-arrow="top|bottom|none" + logical inset-inline-start:var(--arrow-x), border-inline-start+border-block-start = --color-border); the centered fallback modal card (--color-surface, --radius-md, --shadow-modal, --space-lg scrim padding). Color discipline is a hard rule: --color-primary ONLY on the spotlight ring and the Next/Done forward button; Previous step / Close tour / "Take me there" use neutral --color-surface-secondary-btn / --color-text. focus-visible = 2px solid var(--color-primary) + 2px offset on every interactive control; every control min-height/min-width 44px (var(--tap-target-min)). No physical left/right, no literal hex; dark mode resolves automatically via [data-theme="dark"] token vars (author nothing dark-specific). Bottom-sheet + finish-card + exit-choice styles are Plan 04 — do not author them here.
  </action>
  <verify>
    <automated>node tests/run-all.js && ! grep -nE '#[0-9a-fA-F]{3,6}\b' assets/tour.css && ! grep -nE '\b(left|right):' assets/tour.css</automated>
  </verify>
  <acceptance_criteria>
    - assets/tour.css exists; grep -c 'var(--color-primary)' assets/tour.css >=2 (spotlight ring + forward button)
    - No literal hex color in tour.css (the grep above finds none)
    - No physical left:/right: declarations (logical properties only)
    - --tap-target-min is DEFINED locally, not just used (architect-gate A8): grep -c -- '--tap-target-min:' assets/tour.css >=1 AND grep -c 'var(--tap-target-min)' assets/tour.css >=1
    - The A10 z-index tokens are defined: grep -c -- '--z-tour-overlay:' assets/tour.css >=1 and '--z-tour-tooltip:' >=1, and the tooltip uses var(--z-tour-tooltip) (no bare 9100 literal on a z-index declaration)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>Spotlight/tooltip/arrow/fallback styles ship token-only + RTL-safe; accent reserved to ring + forward action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Tour copy injected into dynamically-mounted overlay nodes — the one XSS trust boundary. |
| tour overlay → underlying page | The dim overlay intercepts clicks; underlying page must stay inert (no hidden action fires). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | tour copy interpolation | high | mitigate | All tour copy via textContent; the only innerHTML is a compile-time-literal inline SVG with zero interpolation (V5 / T-39-05). Enforced by the acceptance grep on innerHTML. |
| T-41-03 | Tampering | state desync under overlay | medium | mitigate | D-07 full-viewport pointer-events overlay intercepts all clicks outside the tour chrome (page inert) so tour state cannot desync from user actions underneath. Inertness comes from the overlay, NOT App.lockBodyScroll — spotlight steps keep scroll free so scrollIntoView can reach below-the-fold anchors (architect-gate A4). |
| T-41-04 | Spoofing | clickjacking via overlay | low | accept | Overlay is same-origin app chrome (not an iframe); existing X-Frame-Options: SAMEORIGIN unchanged. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (see 41-RESEARCH.md Package Legitimacy Audit). N/A. |
</threat_model>

<verification>
- `node tests/41-launch-explicit.test.js`, `41-fallback-degradation.test.js`, `41-resume-state.test.js` all exit 0
- `node tests/run-all.js` full suite green
- tour.css: no literal hex, no physical left/right, accent reserved to ring + forward action
- Geometry/RTL correctness is deferred to the Plan 07 Playwright-WebKit gate (jsdom cannot measure layout)
</verification>

<success_criteria>
window.Tour runs explicit-only, degrades gracefully with a never-silent fallback, navigates + resumes across pages, and restarts from step 1 on relaunch — all proven by green behavior tests; core CSS is token-only and RTL-safe.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-03-SUMMARY.md` when done.
</output>
