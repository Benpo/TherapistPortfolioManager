---
phase: 41-replayable-guided-tour
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - add-session.html
  - sessions.html
  - reporting.html
  - assets/app.js
  - tests/41-anchor-presence.test.js
autonomous: true
requirements: [TOUR-02, TOUR-03]
must_haves:
  truths:
    - "All 10 tour anchors carry a dedicated data-tour attribute on always-present chrome (nav-free page elements + two header buttons), never a data row (D-02)."
    - "The anchor-presence rot-guard test asserts each of the 10 data-tour values is present in its declared source file — a removed anchor fails the suite (TOUR-02 rot guard)."
    - "Steps 1-3 anchor on index.html chrome; steps 4-6 on add-session.html form zones; step 7 on sessions.html; step 8 on reporting.html; steps 9-10 on the app.js-mounted header buttons — matching the 10-step full-spine route (D-01)."
  artifacts:
    - "data-tour attributes on: #greeting-card, #addClientBtn, #addSessionBtn (index.html); session setup card, [data-accordion=heart-shield], .session-save-button (add-session.html); .section-title (sessions.html, reporting.html); #backupCloudBtn + .help-entry-btn (app.js)"
    - "tests/41-anchor-presence.test.js — source-scan rot guard over the 10-anchor contract"
  key_links:
    - "Each data-tour value is the exact selector the tour engine STEPS[] array (Plan 03) queries — the contract both sides share"
    - "#backupCloudBtn / .help-entry-btn anchors are set via setAttribute in mountBackupCloudButton / initHelpEntry (app.js) — chrome created in JS, not page HTML"
---

<objective>
Add the 10 `data-tour="…"` anchor attributes to real, always-present chrome and ship the anchor-presence rot-guard test. Today `grep -rn "data-tour"` returns zero across all pages (Pitfall 1) — the anchors are the contract the tour engine binds to, so they land first.

Purpose: Establishes the locale-independent, RTL-stable, refactor-safe anchor contract (data-tour attributes, never #id/text selectors). The rot-guard test guarantees a future page refactor that removes an anchor fails the suite instead of silently degrading the tour to fallback modals on a fresh app (D-02 says a brand-new empty app must show spotlights, not fallbacks).
Output: 4 edited page shells + 2 header-button anchors in app.js + the rot-guard test.
</objective>

<artifacts_produced>
Symbols this plan creates:
- HTML attributes (page markup): `data-tour="overview"` (#greeting-card), `data-tour="add-client"` (#addClientBtn), `data-tour="add-session"` (#addSessionBtn) in index.html; `data-tour="session-setup"` (setup card), `data-tour="session-heart"` ([data-accordion="heart-shield"]), `data-tour="session-save"` (.session-save-button) in add-session.html; `data-tour="sessions"` (.section-title) in sessions.html; `data-tour="reporting"` (.section-title) in reporting.html
- app.js setAttribute calls: `data-tour="backup"` on #backupCloudBtn (mountBackupCloudButton), `data-tour="help"` on .help-entry-btn (initHelpEntry)
- New file: `tests/41-anchor-presence.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/phases/41-replayable-guided-tour/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the RED anchor-presence rot-guard test</name>
  <read_first>
    - tests/40-precache.test.js (the fs source-scan shape to mirror — read files, assert literals present, exit 0/1)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Interaction Contract — the step schema + degradation rows that define the anchor contract)
    - .planning/phases/41-replayable-guided-tour/41-PATTERNS.md (the 10-anchor mapping the engine STEPS[] will mirror)
  </read_first>
  <action>
    Create tests/41-anchor-presence.test.js as an fs source-scan (mirror tests/40-precache.test.js). Hardcode the 10-anchor contract table: each row = { id, value, sourceFile } from the <artifacts_produced> map (steps 1-3 → index.html; 4-6 → add-session.html; 7 → sessions.html; 8 → reporting.html; 9-10 → assets/app.js). For each row, read sourceFile and assert the data-tour token is bound to that value using a regex tolerant of BOTH the HTML attribute form (data-tour="value") AND the JS setAttribute form (setAttribute('data-tour', 'value')) — e.g. match /data-tour['"=\s,)]+value/ on the file text. Fail with a per-anchor message naming the missing value + file. Exit 0 all-present, 1 on any miss (run-all.js contract). Author RED: no anchors exist yet, so it MUST fail until Task 2. Do not weaken to green.
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - tests/41-anchor-presence.test.js exists and, run standalone, exits 1 (RED) — no anchors present yet
    - The contract table in the test names all 10 values: overview, add-client, add-session, session-setup, session-heart, session-save, sessions, reporting, backup, help
    - The value→file mapping matches <artifacts_produced> (backup + help mapped to assets/app.js; the rest to their page files)
    - The match regex accepts the setAttribute form (grep the test source for `setAttribute` or a comment noting both forms)
  </acceptance_criteria>
  <done>Standalone run exits 1 listing all 10 missing anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 10 data-tour anchors to real chrome</name>
  <read_first>
    - index.html (#greeting-card ~line 70, #addClientBtn ~line 102, #addSessionBtn ~line 106)
    - add-session.html (form setup fields ~lines 67-88 wrapping #clientSelect / #sessionTypeGroup / #sessionDate; the [data-accordion="heart-shield"] section ~line 204; the .session-save-button submit ~line 366)
    - sessions.html (.section-title with data-i18n="sessions.title" ~line 51)
    - reporting.html (.section-title with data-i18n="reporting.title" ~line 51)
    - assets/app.js (mountBackupCloudButton ~line 618, btn.id='backupCloudBtn' ~line 629; initHelpEntry ~line 476, the "?" btn created ~line 484 with class help-entry-btn)
  </read_first>
  <action>
    Add the eight page-markup anchors as plain HTML attributes: data-tour="overview" on #greeting-card; data-tour="add-client" on #addClientBtn; data-tour="add-session" on #addSessionBtn (index.html). In add-session.html, add data-tour="session-setup" to the wrapper element enclosing the client select (#clientSelect), the session-format toggle group (#sessionTypeGroup), and the session date (#sessionDate) — the first form section/card inside #sessionForm; add data-tour="session-heart" to the div[data-accordion="heart-shield"]; add data-tour="session-save" to the .session-save-button submit button. In sessions.html add data-tour="sessions" to the .section-title (data-i18n="sessions.title"); in reporting.html add data-tour="reporting" to the .section-title (data-i18n="reporting.title"). For the two header buttons created in app.js, add setAttribute('data-tour', 'backup') on the #backupCloudBtn element in mountBackupCloudButton, and setAttribute('data-tour', 'help') on the help-entry button in initHelpEntry (the element that gets class help-entry-btn). Anchors are inert presentation attributes only — no behavior change to any element. All anchors bind always-present chrome (D-02): the section-title headings and filter chrome persist even when the page is empty (verified: sessions.html / reporting.html render the .card + .section-title with no data).
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/41-anchor-presence.test.js exits 0 (all 10 anchors present)
    - grep -c 'data-tour="overview"' index.html returns 1; grep -c 'data-tour="session-heart"' add-session.html returns 1; grep -c "setAttribute('data-tour', 'backup')" assets/app.js returns 1
    - The #addSessionBtn and #addClientBtn on index.html retain their id + existing classes (anchor is additive)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>All 10 data-tour anchors resolve in their declared source; rot guard green; no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page markup → tour engine | data-tour attributes are inert selectors the engine queries; no data crosses, no behavior attaches here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-02 | Tampering | data-tour anchors | low | accept | Anchors are inert HTML attributes / setAttribute calls with compile-time-literal values — no user input, no behavior. Rot guard prevents silent removal. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (see 41-RESEARCH.md Package Legitimacy Audit). N/A. |
</threat_model>

<verification>
- `node tests/41-anchor-presence.test.js` exits 0
- `node tests/run-all.js` full suite green
- `grep -rn "data-tour" index.html add-session.html sessions.html reporting.html assets/app.js` returns 10 matches total
</verification>

<success_criteria>
All 10 tour anchors present on always-present chrome; rot-guard test green; no element behavior changed.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-02-SUMMARY.md` when done.
</output>
