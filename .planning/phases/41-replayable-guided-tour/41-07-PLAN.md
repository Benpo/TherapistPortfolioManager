---
phase: 41-replayable-guided-tour
plan: 07
type: execute
wave: 5
depends_on: ["41-05", "41-06"]
files_modified:
  - tests/webkit/41-rtl-geometry.mjs
autonomous: false
requirements: [TOUR-02, TOUR-04]
must_haves:
  truths:
    - "A Playwright-WebKit probe drives the live tour and asserts, in real WebKit layout, that RTL (Hebrew) flips the tether arrow to the correct side and the tooltip/spotlight geometry stays on-anchor and inside the viewport (TOUR-04, the criterion jsdom cannot verify)."
    - "The mid-tour language switch (EN↔HE↔DE↔CS) re-renders cleanly in real WebKit with no overflow and no stale geometry."
    - "One real-Safari UAT pass confirms cross-page resume, no silent skip on a removed anchor, and the full 10-step spine — signed off by Ben."
  artifacts:
    - "tests/webkit/41-rtl-geometry.mjs — ad-hoc Playwright-WebKit RTL/geometry probe (NOT part of npm test; documented manual gate)"
  key_links:
    - "The probe exercises the shipped tour.js/tour.css on a locally served build (Playwright 1.61.1 + WebKit binary verified installed, D-14)"
    - "This is the phase gate: full npm suite green + WebKit probe pass + real-Safari sign-off before /gsd-verify-work"
---

<objective>
Close the D-14 verification gate: an ad-hoc Playwright-WebKit probe for the TOUR-04 RTL criterion + spotlight/tooltip geometry that jsdom structurally cannot check (no layout engine), plus one human real-Safari UAT pass.

Purpose: WebKit-only rendering bugs bit Phases 37 (viewBox-only SVG = 0×0) and 40 (welcome overlay blowout in shipping Safari). The tour's tethered tooltip + RTL arrow-flip + spotlight geometry is exactly this bug class. Chromium-first dev is fine for speed, but the phase gate must run WebKit and a real Safari before this fragile surface ships (D-14). Playwright + WebKit binaries are verified installed locally; the probe is an ad-hoc gate, NOT in npm test.
Output: the WebKit probe script + a blocking human-verify checkpoint.
</objective>

<artifacts_produced>
Symbols this plan creates:
- New file: `tests/webkit/41-rtl-geometry.mjs` (Playwright-WebKit ESM probe; run via `node tests/webkit/41-rtl-geometry.mjs`, not `npm test`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-RESEARCH.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@assets/tour.js
@assets/tour.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Playwright-WebKit RTL/geometry probe</name>
  <files>tests/webkit/41-rtl-geometry.mjs</files>
  <read_first>
    - .planning/phases/41-replayable-guided-tour/41-RESEARCH.md (Validation Architecture — the D-14 WebKit gate row; Environment Availability — Playwright 1.61.1 global + webkit binary cached; Pitfall 2 + Pitfall 5)
    - assets/tour.js (the STEPS + render/tooltip/arrow surface the probe drives; the data-arrow attribute + --arrow-x the probe reads)
    - assets/tour.css (the arrow + spotlight selectors to query getBoundingClientRect on)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (RTL/dark row — the exact geometry expectations)
  </read_first>
  <action>
    Create tests/webkit/41-rtl-geometry.mjs as an ESM Playwright script importing webkit from the GLOBAL playwright install (not a project devDependency — do NOT add playwright to package.json). Serve the repo over a local static server (or use file:// if the app tolerates it; prefer a tiny http server on a port) so tour.js/tour.css load as shipped. The probe: launch WebKit, open index.html, start the tour (window.Tour.start()), then for a spotlit step assert getBoundingClientRect of the spotlight overlaps the anchor and the tooltip is fully inside the viewport; switch to Hebrew (App.setLanguage('he') / dispatch app:language) and assert the tour re-renders AND the tether arrow is on the RTL-correct side (data-arrow / --arrow-x resolves to the mirrored inline position) with no viewport overflow; cycle EN→HE→DE→CS asserting a clean re-render each time (Pitfall 2). Print PASS/FAIL per assertion and exit non-zero on any failure. Add a header comment documenting: this is an AD-HOC manual gate (run: node tests/webkit/41-rtl-geometry.mjs), NOT part of npm test, requires the global Playwright + WebKit binary (npx playwright install webkit if stale). Keep it out of tests/run-all.js discovery (place under tests/webkit/, which the runner does not scan — verify the runner's glob).
  </action>
  <verify>
    <automated>node tests/webkit/41-rtl-geometry.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/webkit/41-rtl-geometry.mjs exists and runs under WebKit, exiting 0 when RTL arrow-flip + tooltip/spotlight geometry are correct
    - It imports webkit from the global playwright (playwright is NOT added to package.json — grep package.json shows no new dep)
    - node tests/run-all.js still discovers + passes the normal suite unchanged (the webkit probe is NOT in that glob)
    - The probe asserts the Hebrew arrow side differs from the English arrow side (real RTL flip, not just direction:rtl)
  </acceptance_criteria>
  <done>WebKit probe passes: RTL arrow-flip + on-anchor geometry + clean 4-language re-render in real WebKit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-Safari UAT sign-off</name>
  <action>Present the built tour for a real-Safari (preferably installed-PWA) human verification pass covering the eight checks below; block on Ben's sign-off before /gsd-verify-work. Route any RTL/geometry/resume/silent-skip issues as gap-closure fixes.</action>
  <what-built>
    The full replayable guided tour is now wired end-to-end: 10-step spine tour (index → add-session → sessions → reporting), explicit launch from the welcome CTA + "?" popover "Take the tour" row, graceful spotlight↔fallback degradation, cross-page sessionStorage resume, mid-tour language re-render, exit choice + low-precedence re-entry reminder, and a finish card. The WebKit probe (Task 1) passed the RTL/geometry gate.
  </what-built>
  <how-to-verify>
    On a real device, prefer an installed PWA in Safari (the D-14 real-Safari pass + offline check):
    1. Launch the tour from the welcome overlay's "Take the guided tour" CTA. Confirm the welcome overlay does NOT reappear next launch (Pitfall 8 — sg.welcomeSeen recorded).
    2. Walk all 10 steps. At step 3→4 confirm it auto-navigates to add-session.html and RESUMES at step 4 (cross-page, TOUR-03). Same across 6→7 and 7→8.
    3. On any step, switch language (globe) to Hebrew: the tour re-renders in Hebrew, RTL, arrow on the correct side, no overflow. Cycle EN/HE/DE/CS. (TOUR-04)
    4. Temporarily nothing to remove — instead confirm a fresh empty app shows SPOTLIGHTS on all 10 steps (not fallback modals). Then, to see the fallback, resize very narrow / or trust the anchor-presence + fallback-degradation tests for the rot path.
    5. Mid-tour, hit Close → choose "Remind me later". Reopen the app in a new browser session: confirm the low-precedence "Ready to finish the tour?" card appears (offer only — it does not auto-run). Separately, "I'll explore myself" must never remind again.
    6. Finish the tour: confirm the finish card shows "Browse the help center" + "Add your first client" (accent) + "Start your first session", and relaunching from "?" restarts at step 1 (D-09).
    7. Offline: turn off the network on the installed PWA and confirm the tour still opens (precache).
    8. Confirm the "?" "Take the tour" row is ABSENT in the demo (open demo.html).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any RTL/geometry/resume/silent-skip issues to route as gap-closure fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | The WebKit probe drives the shipped app read-only; it creates/mutates no client data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-08 | Tampering | WebKit-only rendering defect ships unverified | high | mitigate | The D-14 Playwright-WebKit gate + a real-Safari UAT pass catch the Phase-37/40 WebKit bug class before ship. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — Playwright is the GLOBAL install, not added to package.json (see 41-RESEARCH.md). N/A. |
</threat_model>

<verification>
- `node tests/webkit/41-rtl-geometry.mjs` exits 0 (WebKit RTL/geometry gate)
- `node tests/run-all.js` full suite green (probe excluded from the glob)
- Blocking human real-Safari UAT pass (installed PWA, offline, all 8 steps) signed off by Ben
</verification>

<success_criteria>
RTL arrow-flip + on-anchor tooltip/spotlight geometry pass in real WebKit; the full 10-step tour, cross-page resume, mid-tour language switch, reminder-offer, finish card, offline, and demo-exclusion pass a real-Safari human check.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-07-SUMMARY.md` when done.
</output>
