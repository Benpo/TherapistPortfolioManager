---
phase: 41-replayable-guided-tour
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/41-tour-i18n-parity.test.js
autonomous: true
requirements: [TOUR-01, TOUR-04]
must_haves:
  truths:
    - "Every help.tour.* key (10 step titles + 10 step bodies + chrome + fallback + exit choice + reminder card + finish card + help.entry.takeTour) resolves in en/he/de/cs with a non-empty trimmed string (D-11)."
    - "The set of help.tour.* keys is identical across all four locale files — no locale drifts a key in or out (parity gate)."
    - "Step titles/headings are authored as nouns/gerunds, never bare imperatives, so Hebrew produces natural noun/infinitive forms (Pitfall 9 / Phase 24 D-05)."
    - "No help.tour.* value contains a pictographic emoji (project-wide convention)."
  artifacts:
    - "tests/41-tour-i18n-parity.test.js — 4-locale presence/non-empty/parity/no-emoji gate"
    - "help.tour.* key block appended to assets/i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js"
  key_links:
    - "help.tour.step.* + help.tour.fallbackBody + help.tour.finish.* consumed by the tour engine (Plan 03/04) via t()"
    - "help.entry.takeTour consumed by initHelpEntry (Plan 05); help.tour.reminder.* consumed by the coordinator reminder surface (Plan 05)"
---

<objective>
Author the complete tour copy contract — ~30 `help.tour.*` i18n keys (10 step titles, 10 step bodies, chrome strings, fallback, mid-tour exit choice, re-entry reminder card, finish card) plus the `help.entry.takeTour` popover label — in ALL FOUR locales (EN/HE/DE/CS) this phase (D-11), behind a RED-first 4-locale parity gate.

Purpose: The engine, wiring, and coordinator surfaces in later plans all reference these keys by name. Shipping copy in all four locales now is what lets TOUR-04's mid-tour language switch demo properly in every language. EN is the canonical draft (from 41-UI-SPEC.md); HE/DE/CS are authored to the same key set and flagged for the TOUR-01 native-speaker-agent pass.
Output: Four edited i18n locale files + one new parity test.
</objective>

<artifacts_produced>
Symbols/strings this plan creates (for downstream drift verification):
- i18n keys (all four locales): `help.tour.step.overview`, `help.tour.step.addClient`, `help.tour.step.startSession`, `help.tour.step.setup`, `help.tour.step.heart`, `help.tour.step.save`, `help.tour.step.sessions`, `help.tour.step.reporting`, `help.tour.step.backup`, `help.tour.step.help` (each with a `.title` + `.body` — see key list below), `help.tour.next`, `help.tour.back`, `help.tour.close`, `help.tour.done`, `help.tour.counter`, `help.tour.fallbackBody`, `help.tour.takeMeThere`, `help.tour.exit.title`, `help.tour.exit.remindLater`, `help.tour.exit.exploreMyself`, `help.tour.reminder.title`, `help.tour.reminder.start`, `help.tour.reminder.dismiss`, `help.tour.finish.title`, `help.tour.finish.body`, `help.tour.finish.addClient`, `help.tour.finish.startSession`, `help.tour.finish.helpCenter`, `help.entry.takeTour`
- New file: `tests/41-tour-i18n-parity.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/SessionsGarden-DNA-EN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the RED 4-locale tour-copy parity gate</name>
  <read_first>
    - tests/40-i18n-parity.test.js (the exact vm-sandbox loader + presence/non-empty/parity/no-emoji assertion shape to mirror)
    - assets/i18n-en.js (lines ~595-610 — existing help.entry / help.welcome block, so the new keys sit in the same neighborhood)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Copywriting Contract — the authoritative key list to guard)
  </read_first>
  <action>
    Create tests/41-tour-i18n-parity.test.js by mirroring tests/40-i18n-parity.test.js verbatim in structure: load all four assets/i18n-{en,he,de,cs}.js into one vm sandbox (window.I18N) via runInContext, then for every locale x every guarded key assert (1) presence, (2) non-empty after trim, (3) parity — the set of guarded keys present is identical across all four files, (4) no-emoji on values. Hardcode the EXPECTED key list = the full help.tour.* set plus help.entry.takeTour from the artifacts list above (30 keys total). Exit 0 on full pass, 1 on any failure (the tests/run-all.js spawnSync contract). Author RED: the keys do not exist yet (Task 2 adds them), so this MUST fail until Task 2 — do not weaken it to green. Add a header banner stating it is authored RED and naming Task 2 as the file that flips it green.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - tests/41-tour-i18n-parity.test.js exists and, run standalone, exits 1 (RED) because the keys are absent
    - The test loads all four locale files via vm.runInContext (no require of i18n files)
    - The expected-key array contains exactly the 30 keys listed in <artifacts_produced> (grep the file: it references `help.tour.step.overview` and `help.entry.takeTour`)
    - The test asserts cross-locale parity (identical key set across en/he/de/cs), not just en presence
  </acceptance_criteria>
  <done>Standalone run exits 1 with a message naming the missing help.tour.* keys.</done>
</task>

<task type="auto">
  <name>Task 2: Author the ~30 help.tour.* keys in all four locales</name>
  <read_first>
    - assets/i18n-en.js (insert the new block immediately after the existing help.welcome.* block, ~line 604)
    - assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js (find the matching help.* neighborhood in each; keep object-literal structure + trailing commas consistent with each file)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Copywriting Contract table — the canonical EN draft for all 10 steps + chrome + fallback + exit + reminder + finish)
    - .planning/SessionsGarden-DNA-EN.md (voice: calm, warm, garden-branded — NOT SaaS product-tour energy)
    - .planning/phases/41-replayable-guided-tour/41-RESEARCH.md (Pitfall 9 — noun/infinitive forms for natural Hebrew)
  </read_first>
  <action>
    Append the full help.tour.* key block to all four i18n files using the EN draft copy from the UI-SPEC Copywriting Contract as canonical. Keys and their EN draft values (TOUR-01 D-03: title + 1-2 calm sentences, <=~40 words each; garden voice): the 10 steps map to help.tour.step.{overview,addClient,startSession,setup,heart,save,sessions,reporting,backup,help} each with a `.title` and `.body`; chrome = help.tour.next ("Next"), help.tour.done ("Done"), help.tour.back ("Previous step"), help.tour.close ("Close tour"), help.tour.counter ("Step {n} of {total}"); fallback = help.tour.fallbackBody ("This is on the {screen} screen.") + help.tour.takeMeThere ("Take me there"); exit choice = help.tour.exit.title ("Leaving the tour?"), help.tour.exit.remindLater ("Remind me later"), help.tour.exit.exploreMyself ("I'll explore myself"); reminder card = help.tour.reminder.title ("Ready to finish the tour?"), help.tour.reminder.start ("Start"), help.tour.reminder.dismiss ("Dismiss"); finish card = help.tour.finish.title ("You're all set"), help.tour.finish.body, help.tour.finish.addClient ("Add your first client"), help.tour.finish.startSession ("Start your first session"), help.tour.finish.helpCenter ("Browse the help center"); plus help.entry.takeTour ("Take the tour"). Step 6 body MUST name the export capability (D-01). Titles are nouns/gerunds ("Adding a client", "The heart of a session"), never bare imperatives, so Hebrew reads naturally (Pitfall 9 / Phase 24 D-05). Author HE (noun/infinitive house style), DE (Sie register), CS (formal register) to the SAME key set. No emojis. The `{screen}`, `{n}`, `{total}` placeholders are literal tokens the engine substitutes — keep them verbatim in every locale. Add a comment flagging HE/DE/CS as pending the TOUR-01 native-speaker-agent verification pass (Phase 40-01 precedent).
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/41-tour-i18n-parity.test.js exits 0 (all 30 keys present, non-empty, parity across 4 locales, no emoji)
    - grep -c '"help.tour.step.overview.title"' assets/i18n-en.js returns 1 in each of the four locale files (repeat per locale)
    - The `{screen}` token appears in help.tour.fallbackBody in all four locale values
    - Step 6 body value (help.tour.step.save.body) names export / PDF in EN
    - node tests/run-all.js stays green (no regression to the existing suite)
  </acceptance_criteria>
  <done>Parity gate green; all four locales carry the identical 30-key tour-copy set with placeholders preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → DOM | Tour copy is a static bundled dictionary; downstream engine renders it via textContent only (never innerHTML). This plan ships only the data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | help.tour.* values rendered downstream | low | mitigate | Values are compile-time-literal strings in bundled i18n files (no user input); the engine (Plan 03/04) renders them via textContent — enforced there. This plan introduces no interpolation of untrusted data. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (bespoke vanilla, zero new deps — see 41-RESEARCH.md Package Legitimacy Audit). Supply-chain vector N/A. |
</threat_model>

<verification>
- `node tests/41-tour-i18n-parity.test.js` exits 0
- `node tests/run-all.js` full suite green
- Manual: HE/DE/CS values flagged for the TOUR-01 native-speaker-agent pass before phase close
</verification>

<success_criteria>
All 30 tour-copy keys present, non-empty, and parity-locked across en/he/de/cs; placeholders `{screen}`/`{n}`/`{total}` preserved verbatim; no emoji; suite green.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-01-SUMMARY.md` when done.
</output>
