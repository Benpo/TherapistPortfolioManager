---
phase: 41-replayable-guided-tour
plan: 04
type: execute
wave: 3
depends_on: ["41-03"]
files_modified:
  - assets/tour.js
  - assets/tour.css
  - tests/41-lang-rerender.test.js
autonomous: true
requirements: [TOUR-01, TOUR-04]
must_haves:
  truths:
    - "On the app:language CustomEvent, an active tour tears down all tour chrome and re-renders the current step in the new language + direction (cleanup-then-replace), subscribed exactly once (TOUR-04)."
    - "Closing mid-tour shows a concise two-option exit choice: 'Remind me later' sets sg.tourRemindLater='1'; 'I'll explore myself' sets a persistent sg.tourNeverRemind='1' — the tour itself still only RUNS on explicit start (TOUR-01 intact) (D-08)."
    - "The final step shows a finish card with a 'Browse the help center' link plus 'Add your first client' (the single accent action) and 'Start your first session' (neutral); finishing sets sg.tourCompleted='1' and clears sg.tourResume (D-10)."
    - "Below the bottom-sheet breakpoint the tooltip becomes a bottom-pinned sheet (no tooltip-collision math); the spotlight still highlights the anchor (D-05)."
  prohibitions:
    - statement: "The re-entry reminder flags (sg.tourRemindLater / sg.tourNeverRemind / sg.tourCompleted) do not share keys with the pending security-note cadence-backoff todo."
      status: resolved
      verification: "grep the flag names — they are tour-scoped sg.tour* keys, disjoint from any security-note flag (D-08 constraint)"
  artifacts:
    - "assets/tour.js extended — app:language listener, mid-tour exit choice, finish card, bottom-sheet responsive branch"
    - "assets/tour.css extended — bottom-sheet, finish card, exit-choice styles"
    - "tests/41-lang-rerender.test.js — text-level re-render on app:language"
  key_links:
    - "sg.tourRemindLater / sg.tourNeverRemind / sg.tourCompleted are read by the coordinator reminder surface registered in Plan 05"
    - "app:language is the same event initHelpEntry + backup-cloud already listen to (app.js:126 dispatch)"
    - "Finish-card 'Add your first client' → add-client.html; 'Start your first session' → add-session.html; 'Browse the help center' → help.html"
---

<objective>
Extend the tour engine with the four polish surfaces: mid-tour language re-render (TOUR-04), the concise exit choice + persistent flags (D-08), the finish card (D-10), and the small-screen bottom-sheet (D-05).

Purpose: TOUR-04 is a locked requirement — switching language mid-tour must re-render cleanly in the new language and direction. The exit choice sets the flags the Plan 05 coordinator reminder reads; the finish card closes the loop with the help-center handoff + first-action buttons; the bottom-sheet keeps the tour usable on cramped viewports without collision math. Geometry/RTL correctness is verified in the Plan 07 WebKit gate — this plan pins the text-level re-render in jsdom.
Output: extended tour.js + tour.css + the language re-render test.
</objective>

<artifacts_produced>
Symbols this plan creates:
- tour.js additions: an `app:language` document listener (cleanup-then-replace), an exit-choice mount function, a finish-card mount function, a bottom-sheet responsive branch, `endTour(reason)` extension writing sg.tourCompleted on finish
- localStorage flags (created here, read in Plan 05): `sg.tourRemindLater`, `sg.tourNeverRemind`, `sg.tourCompleted`
- tour.css additions: `.tour-bottom-sheet` (or equivalent) responsive rules, finish-card styles, exit-choice styles
- New test: `tests/41-lang-rerender.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-RESEARCH.md
@.planning/phases/41-replayable-guided-tour/41-PATTERNS.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/sketches/003-tour-fallback/index.html
@assets/tour.js
@assets/tour.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the RED language re-render test</name>
  <read_first>
    - assets/tour.js (the current render()/clearTourChrome()/isActive() surface to drive)
    - assets/app.js (line 126 — the app:language CustomEvent dispatch shape; lines 573-587 — the initHelpEntry _listenerInstalled once-only idempotency to mirror)
    - tests/41-launch-explicit.test.js (the Plan-03 jsdom/vm harness + I18N stub shape to reuse)
  </read_first>
  <behavior>
    - After Tour.start(), dispatching document app:language with a different lang re-renders the CURRENT step's tooltip/fallback text using the new locale's help.tour.* values (text-level assertion).
    - The listener is installed exactly once — repeated start()/stop() cycles do not stack duplicate listeners.
    - When the tour is NOT active, an app:language dispatch is a no-op (no chrome mounted).
  </behavior>
  <action>
    Create tests/41-lang-rerender.test.js reusing the Plan-03 jsdom/vm harness. Stub window.I18N with two locales whose help.tour.step.* values differ, start the tour, dispatch a document-level app:language CustomEvent switching locale, and assert the mounted tour chrome's textContent now equals the new locale's value (cleanup-then-replace worked). Assert idempotency (dispatch twice → single re-render path, no duplicate nodes) and the inactive no-op. jsdom has NO layout engine — assert TEXT + node identity only, never arrow-side/pixel geometry (that is the Plan 07 WebKit gate). Author RED (the app:language listener does not exist yet) — exit 1 until Task 2.
  </action>
  <verify>
    <automated>node tests/41-lang-rerender.test.js; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - tests/41-lang-rerender.test.js exists and exits 1 (RED) standalone
    - It asserts textContent changes to the new locale value after an app:language dispatch
    - It asserts single-listener idempotency and the inactive no-op
    - It makes no getBoundingClientRect/arrow-side assertion
  </acceptance_criteria>
  <done>RED test pins the TOUR-04 text-level re-render before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tour.js — language re-render, exit choice, finish card, bottom-sheet</name>
  <read_first>
    - assets/tour.js (extend the existing IIFE; reuse clearTourChrome/render/endTour from Plan 03)
    - .planning/sketches/003-tour-fallback/index.html (Variant C cleanup-then-replace language cycle ~902-910; the finish/exit chrome patterns)
    - assets/app.js (line 126 app:language dispatch; 573-587 _listenerInstalled once-only pattern)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Copywriting Contract exit/reminder/finish rows; Interaction Contract bottom-sheet + finish-card + language rows)
    - tests/41-lang-rerender.test.js (contract to satisfy)
  </read_first>
  <action>
    Extend assets/tour.js. (1) Language re-render (TOUR-04): subscribe once to document 'app:language' (guard with a static _langListenerInstalled flag mirroring initHelpEntry); on fire, if Tour.isActive() → clearTourChrome() then render() so copy re-resolves via t() and geometry/arrow re-measure and re-flip for RTL (logical props flip for free). (2) Mid-tour exit choice (D-08): the Close control opens a concise two-button prompt using help.tour.exit.title / help.tour.exit.remindLater / help.tour.exit.exploreMyself — "Remind me later" sets lsSet('sg.tourRemindLater','1') then endTour; "I'll explore myself" sets lsSet('sg.tourNeverRemind','1') then endTour. The tour still only RUNS on explicit start (do not auto-start anything here). Keep these three flag names disjoint from any security-note key (D-08). (3) Finish card (D-10): on the last step's forward action, mount a garden-voice card from help.tour.finish.* with a "Browse the help center" link (./help.html), an accent "Add your first client" action (./add-client.html), and a neutral "Start your first session" action (./add-session.html); one accent control only; set lsSet('sg.tourCompleted','1') and clear sg.tourResume when the finish card mounts/closes. (4) Bottom-sheet (D-05): below a width breakpoint (planner's discretion — use ~640px) render the tooltip as a bottom-pinned sheet (toggle a class / branch geometry) with no tooltip-collision math; the spotlight still highlights the anchor. All copy via textContent; reuse lsGet/lsSet/ssRemove helpers already in the file. Keep the Plan-03 behavior tests green.
  </action>
  <verify>
    <automated>node tests/41-lang-rerender.test.js && node tests/41-launch-explicit.test.js && node tests/41-fallback-degradation.test.js && node tests/41-resume-state.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/41-lang-rerender.test.js exits 0; all three Plan-03 tests still exit 0
    - grep -c "app:language" assets/tour.js >=1 and the listener is guarded once-only (grep for a _langListenerInstalled-style flag)
    - grep -c "sg.tourRemindLater" assets/tour.js and "sg.tourNeverRemind" and "sg.tourCompleted" each return >=1
    - grep -c "help.tour.finish" assets/tour.js >=1 (finish card wired); the finish card links ./help.html, ./add-client.html, ./add-session.html
    - No new innerHTML with variable interpolation (textContent discipline preserved)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>Mid-tour language switch re-renders cleanly; exit choice sets the reminder flags; finish card handoff works; bottom-sheet branch present.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tour.css — bottom-sheet, finish card, exit choice</name>
  <read_first>
    - assets/tour.css (extend the Plan-03 tour-scoped stylesheet; keep the same root-class scope + token discipline)
    - assets/tokens.css (semantic tokens — the only color source)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Interaction Contract bottom-sheet rules; Color section — accent reserved to finish-card's single primary action + focus outline; Typography Display role 1.75rem/700 for finish headline only)
  </read_first>
  <action>
    Extend assets/tour.css with: the bottom-sheet variant (below ~640px media query — inset-inline:0, bottom-pinned, border-start-start-radius/border-start-end-radius only, no arrow); the finish-card (surface card, --space-xl vertical rhythm, Display headline 1.75rem/700, one accent action button for "Add your first client", neutral for the other two); the exit-choice prompt (concise two-button row, neutral buttons). Logical properties only; semantic tokens only (no literal hex, no physical left/right); 44px tap targets + 2px accent focus-visible on every new control; weights limited to 400 + 700. Dark mode resolves automatically via token vars — author nothing dark-specific.
  </action>
  <verify>
    <automated>node tests/run-all.js && ! grep -nE '#[0-9a-fA-F]{3,6}\b' assets/tour.css && ! grep -nE '\b(left|right):' assets/tour.css</automated>
  </verify>
  <acceptance_criteria>
    - assets/tour.css contains a max-width media query for the bottom-sheet (grep -c '@media' assets/tour.css >=1)
    - No literal hex and no physical left:/right: in tour.css
    - Finish-card headline uses the Display size 1.75rem (grep the file)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>Bottom-sheet, finish-card, and exit-choice styles ship token-only + RTL-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Re-rendered copy (exit/finish/reminder) injected into overlay nodes — textContent only. |
| tour flags → coordinator | sg.tour* localStorage flags are the decoupling seam the Plan 05 reminder reads; must not collide with security-note keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | re-render / finish / exit copy | high | mitigate | All copy via textContent; no variable innerHTML (acceptance grep). |
| T-41-05 | Tampering | reminder flags collide with security-note cadence-backoff | low | mitigate | sg.tourRemindLater / sg.tourNeverRemind / sg.tourCompleted are tour-scoped and disjoint from any security-note flag (D-08 constraint, verified by grep). |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase. N/A. |
</threat_model>

<verification>
- `node tests/41-lang-rerender.test.js` exits 0; all Plan-03 tests still green
- `node tests/run-all.js` full suite green
- tour.css: no literal hex, no physical left/right, bottom-sheet media query present
- RTL geometry of the re-render is verified in Plan 07 (WebKit), not here
</verification>

<success_criteria>
Mid-tour language switch re-renders cleanly (text-level proven); exit choice sets the reminder flags without touching security-note keys; finish card completes the help-center handoff; bottom-sheet keeps small screens usable.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-04-SUMMARY.md` when done.
</output>
