---
phase: 41-replayable-guided-tour
plan: 05
type: execute
wave: 4
depends_on: ["41-02", "41-04"]
files_modified:
  - assets/app.js
  - assets/attention-coordinator.js
  - tests/41-demo-gate.test.js
autonomous: true
requirements: [TOUR-01, TOUR-03]
must_haves:
  truths:
    - "The '?' popover gains a 'Take the tour' row (help.entry.takeTour) that calls Tour.start(); it is HIDDEN when window.name==='demo-mode' (D-13 / D-16)."
    - "The welcome overlay's primary CTA runs the existing dismiss() bookkeeping (records sg.welcomeSeen + sg.whatsNewLastSeenVersion on the non-replay path) AND calls Tour.start() instead of navigating to help.html (D-12 / Pitfall 8)."
    - "initCommon calls Tour.resume() (typeof-guarded) so a cross-page navigation resumes the tour on the next page load (TOUR-03)."
    - "A 'tour-reminder' surface is registered with the AttentionCoordinator at the LOWEST precedence slot; it OFFERS to start (Start/Dismiss) and never auto-runs the tour (TOUR-01 intact / D-08)."
  prohibitions:
    - statement: "The tour never auto-runs — every launch path (welcome CTA, '?' row, reminder Start) requires an explicit user click; the reminder's show() only mounts an offer card."
      status: resolved
      verification: "tests/41-demo-gate.test.js asserts no tour chrome mounts without an explicit start; the reminder eligible()/show() offers, run() never calls Tour.start()"
  artifacts:
    - "'Take the tour' item appended to the initHelpEntry items array (demo-gated) in assets/app.js"
    - "Tour.resume() call added to initCommon in assets/app.js"
    - "Welcome CTA rewire + 'tour-reminder' register() + PRECEDENCE append in assets/attention-coordinator.js"
    - "tests/41-demo-gate.test.js — '?' tour row hidden in demo"
  key_links:
    - "Tour.start / Tour.resume are the window.Tour methods built in Plan 03/04 (typeof-guarded so a page without tour.js never throws)"
    - "The reminder eligible() reads sg.tourRemindLater / sg.tourCompleted / sg.tourNeverRemind set by the Plan 04 exit choice"
    - "'tour-reminder' appended to PRECEDENCE after 'mobile-hint' (lowest) so no future Phase-42 whats-new surface is starved"
---

<objective>
Wire the tour into its three explicit launch surfaces and the re-entry reminder: the "?" popover "Take the tour" row (demo-gated), the welcome-overlay CTA rewire (D-12), the cross-page resume hook in initCommon (TOUR-03), and the coordinator-governed "tour-reminder" surface (D-08).

Purpose: The engine is inert until something calls Tour.start()/resume(). This plan connects the launch surfaces the coordinator + help-entry were explicitly built to extend (~70% of this phase is integration into existing seams). The reminder OFFERS to finish next session but never auto-runs — TOUR-01 stays intact.
Output: app.js + attention-coordinator.js edits + the demo-gate test. (Page-shell script/link tags + sw.js precache are Plan 06.)
</objective>

<artifacts_produced>
Symbols this plan creates:
- app.js: a `{ labelKey: 'help.entry.takeTour', action }` item in the initHelpEntry items array (demo-filtered); a `Tour.resume()` invocation inside initCommon
- attention-coordinator.js: `'tour-reminder'` appended to the PRECEDENCE array; a `register({ id:'tour-reminder', eligible, show })` call; a rewired welcome primary CTA
- New test: `tests/41-demo-gate.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-RESEARCH.md
@.planning/phases/41-replayable-guided-tour/41-PATTERNS.md
@assets/app.js
@assets/attention-coordinator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire app.js — "Take the tour" popover row (demo-gated) + Tour.resume in initCommon + RED demo-gate test</name>
  <read_first>
    - assets/app.js (initHelpEntry ~476-588 — the items array ~516-526, the demo seam window.name==='demo-mode' used at ~276/327/362; initCommon ~781-808 where chrome mounts + listeners install)
    - tests/40-precache.test.js and tests/35-demo-* (the demo-exclusion assertion idiom to mirror)
    - assets/attention-coordinator.js (isDemo() at ~80-82 — the exact window.name seam)
  </read_first>
  <action>
    First author tests/41-demo-gate.test.js RED: jsdom-mount the help entry (drive the real initHelpEntry via the App test seam) with window.name==='demo-mode' and assert NO help-entry-item carries data-label-key='help.entry.takeTour'; then with a normal window.name assert the "Take the tour" row IS present. Exit 1 until the code lands. Then edit assets/app.js: in initHelpEntry, append an item { labelKey: 'help.entry.takeTour', action: function(){ if (typeof window.Tour !== 'undefined') window.Tour.start(); } } to the items array, positioned after the 'help.entry.replayWelcome' row (P40 D-17 slot). Demo-gate per D-16: build the items array so the tour row is EXCLUDED when window.name==='demo-mode' (filter it out before the forEach, mirroring the established demo seam) — do not render a dead row. In initCommon, after the chrome mounts, add a typeof-guarded call: if (typeof window.Tour !== 'undefined' && window.Tour.resume) window.Tour.resume(); so a cross-page tour navigation resumes on load (TOUR-03). The new row's label re-translates for free via the existing initHelpEntry._listenerInstalled app:language listener (it re-translates every .help-entry-item[data-label-key]).
  </action>
  <verify>
    <automated>node tests/41-demo-gate.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/41-demo-gate.test.js exits 0 (row present normally, absent in demo)
    - grep -c "help.entry.takeTour" assets/app.js >=1 and the action calls window.Tour.start()
    - grep -c "Tour.resume" assets/app.js >=1 inside initCommon, typeof-guarded
    - The demo gate uses window.name==='demo-mode' (grep the added filter)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>"Take the tour" appears in the "?" popover normally, hidden in demo; initCommon resumes a cross-page run.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the welcome CTA (D-12) + register the coordinator tour-reminder (D-08)</name>
  <read_first>
    - assets/attention-coordinator.js (PRECEDENCE ~45; register ~76-78; run ~90-103; welcome primary CTA build + href ~178-186; dismiss() bookkeeping ~215-227 writing sg.welcomeSeen + sg.whatsNewLastSeenVersion; makeEl/lsGet/lsSet idioms; install-nudge/mobile-hint register examples for the eligible/show shape)
    - .planning/phases/41-replayable-guided-tour/41-RESEARCH.md (Pitfall 8 — the rewired CTA must run dismiss() bookkeeping AND Tour.start(); Pattern 4 — the reminder register shape; Open Question 2 — precedence slot)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (reminder card copy: help.tour.reminder.title/start/dismiss)
  </read_first>
  <action>
    Edit assets/attention-coordinator.js. (1) D-12 welcome CTA rewire: the primary CTA currently is an <a href="./help.html"> whose click calls dismiss(). Rewire so activating it BOTH runs the existing dismiss() bookkeeping (which on the non-replay path records sg.welcomeSeen + sg.whatsNewLastSeenVersion — Pitfall 8) AND calls window.Tour.start() instead of navigating to help.html. Prefer changing the primary from an anchor-nav to an in-place action: keep the same label/class (welcome-cta--primary, help.welcome.ctaTour) but on click call dismiss() then, typeof-guarded, window.Tour.start() — and remove/neutralize the ./help.html href navigation (if kept as an <a>, preventDefault on click so it does not navigate). Do NOT alter the replay-path or the secondary CTA. (2) D-08 reminder: append 'tour-reminder' to the PRECEDENCE array at the LOWEST slot (after 'mobile-hint') so a future Phase-42 whats-new surface is never starved (document the choice in a comment). Add register({ id:'tour-reminder', eligible: function(){ return lsGet('sg.tourRemindLater')==='1' && lsGet('sg.tourCompleted')!=='1' && lsGet('sg.tourNeverRemind')!=='1'; }, show: function(){ mount a "Ready to finish the tour?" card from help.tour.reminder.title with Start + Dismiss buttons via makeEl (textContent only); Start → window.Tour.start(); Dismiss → remove the card } }). The reminder OFFERS only — run()'s one-per-session + demo-off gating is inherited; do NOT re-implement it, and do NOT call Tour.start() anywhere except inside the Start button handler (TOUR-01). Keep the sg.tour* flags disjoint from any security-note key (D-08).
  </action>
  <verify>
    <automated>node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'tour-reminder'" assets/attention-coordinator.js >=2 (PRECEDENCE entry + register id) and it is the last element of PRECEDENCE
    - The welcome primary CTA no longer navigates to ./help.html on click (grep: the click path calls dismiss() and window.Tour.start(); any retained href is preventDefault-guarded)
    - The reminder eligible() reads sg.tourRemindLater / sg.tourCompleted / sg.tourNeverRemind (grep all three)
    - run() is NOT modified to call Tour.start() (the tour only runs from the Start button handler) — grep shows Tour.start() only inside the register show()/Start handler, never in run()
    - node tests/run-all.js stays green (existing coordinator tests unbroken)
  </acceptance_criteria>
  <done>Welcome CTA launches the real tour with dismiss bookkeeping intact; the low-precedence reminder offers to finish next session without ever auto-running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| welcome/reminder copy → DOM | Coordinator-mounted cards inject copy via makeEl/textContent — the XSS boundary. |
| tour launch → run() arbitration | The reminder must offer, never auto-run; run()'s one-per-session + demo-off gate is the arbitration boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | reminder / CTA copy | high | mitigate | Reminder + CTA copy via makeEl/textContent (never innerHTML) — the established coordinator rule. |
| T-41-05 | Tampering | reminder tangles with security-note cadence-backoff | medium | mitigate | tour-reminder is a separate registered surface with disjoint sg.tour* flags; PRECEDENCE-lowest; does not modify the security-note surface (D-08). |
| T-41-06 | Elevation | reminder auto-runs the tour | high | mitigate | Tour.start() is called ONLY from the Start-button handler; run() is unmodified; eligible()/show() merely offer (acceptance grep proves no Tour.start() in run()). |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase. N/A. |
</threat_model>

<verification>
- `node tests/41-demo-gate.test.js` exits 0
- `node tests/run-all.js` full suite green
- Manual trace: welcome CTA → dismiss bookkeeping + Tour.start() (Pitfall 8); reminder Start → Tour.start(); nothing auto-runs
</verification>

<success_criteria>
All three explicit launch paths reach Tour.start(); the "?" tour row hides in demo; cross-page resume fires from initCommon; the low-precedence reminder offers-not-runs and stays clear of the security-note flags.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-05-SUMMARY.md` when done.
</output>
