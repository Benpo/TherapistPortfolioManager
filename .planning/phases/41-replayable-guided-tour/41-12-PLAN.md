---
phase: 41-replayable-guided-tour
plan: 12
type: execute
wave: 7
gap_closure: true
depends_on: ["41-09", "41-11"]
files_modified:
  - assets/app.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/40-i18n-parity.test.js
  - tests/40-app-wiring.test.js
  - tests/41-demo-gate.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TOUR-01]
must_haves:
  truths:
    - "The '?' popover shows exactly three items — Help center, the guided tour, Contact us — with no redundant welcome-screen replay row (UAT gap 8)."
    - "The '?' guided-tour entry is relabelled to the app's existing guided-tour voice (EN 'Onboarding Tour'), avoiding jargon and matching the welcome CTA's 'guided tour' language."
    - "First-run welcome behavior is unchanged: AttentionCoordinator.showWelcome stays intact and is still used by the welcome overlay + the D-12 CTA; only the menu caller of showWelcome(true) is removed."
    - "The reversal of the Phase 40 D-17 / ONBD-02 menu-replay entry is recorded in REQUIREMENTS traceability (a note, not a deletion of history)."
  artifacts:
    - "initHelpEntry items[] without the replayWelcome row (Help center / takeTour / Contact us)"
    - "help.entry.replayWelcome retired x4 locales; help.entry.takeTour relabelled x4 locales"
    - "40-i18n-parity, 40-app-wiring, 41-demo-gate tests realigned; REQUIREMENTS.md ONBD-02/D-17 reversal note"
  key_links:
    - "showWelcome() must remain exported by AttentionCoordinator — the first-run overlay and the welcome-CTA rewire depend on it; only the menu item that called showWelcome(true) is removed."
    - "Removing help.entry.replayWelcome touches Phase 40 guard tests (40-i18n-parity NEW_KEYS, 40-app-wiring replay-row tests) and the 41-demo-gate ordering — all must be realigned or the suite goes red."
---

<!-- These i18n key / item literals appear in <action>/<acceptance_criteria> prose
     because they are the exact keys/rows being removed and are negative-grepped
     (== 0). Sanctioned per the comment-text-discipline gate; the executor leaves
     no such token behind as a stray comment in the edited files. -->
<!-- planner-discipline-allow: help.entry.replayWelcome -->
<!-- planner-discipline-allow: replayWelcome -->
<!-- planner-discipline-allow: showWelcome(true) -->

<objective>
Trim the "?" help popover to three non-redundant items and align the tour entry's label with the app's guided-tour voice (UAT gap 8). Today the popover carries both a "Replay welcome" row (Phase 40 D-17 / ONBD-02 — help.entry.replayWelcome → AttentionCoordinator.showWelcome(true)) and a "Take the tour" row (Phase 41 D-13) — two redundant onboarding-replay entries. Per Ben's decision 2026-07-08 this REVERSES the D-17/ONBD-02 menu-replay slice: remove the welcome-screen replay row (retire its key in all four locales) and relabel the tour row to the existing guided-tour voice (EN "Onboarding Tour"). The result is Help center / the guided tour / Contact us = 3 items. First-run welcome is untouched — AttentionCoordinator.showWelcome stays; only its menu caller goes away. The reversal is recorded in REQUIREMENTS traceability as a note (history preserved).

Purpose: two onboarding-replay entries confused the practitioner (the welcome screen should not reappear after first run — only the replayable tour should). This closes the last UAT gap and keeps the "?" a small, honest set.
Output: a 3-item popover, the relabelled tour entry x4 locales, realigned Phase-40 guard tests, and the traceability note.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- CHANGED initHelpEntry items[] in assets/app.js — the help.entry.replayWelcome row is removed; the row order is now Help center, guided tour, Contact us (the takeTour demo-filter is unchanged).
- REMOVED key help.entry.replayWelcome from all 4 locale files.
- RELABELLED help.entry.takeTour value: EN "Onboarding Tour"; HE/DE/CS natural equivalents (machine-draft, flagged for 42.1).
- CHANGED tests/40-i18n-parity.test.js — NEW_KEYS no longer lists help.entry.replayWelcome (count 15 → 14).
- CHANGED tests/40-app-wiring.test.js — the three replay-welcome tests (row mounts / click calls showWelcome(true) / app:language re-translates the replay row) removed; the security-note surface tests stay.
- CHANGED tests/41-demo-gate.test.js — the ordering assertion no longer references replayWelcome; asserts takeTour sits after Help center and before Contact us.
- CHANGED .planning/REQUIREMENTS.md — a traceability note on ONBD-02 recording the Phase-41 D-17 menu-replay reversal.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/REQUIREMENTS.md
@assets/app.js
@assets/i18n-en.js
@tests/40-app-wiring.test.js
@tests/41-demo-gate.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign the Phase-40 guard tests + the demo-gate ordering to a 3-item popover</name>
  <files>tests/40-i18n-parity.test.js, tests/40-app-wiring.test.js, tests/41-demo-gate.test.js</files>
  <read_first>
    - tests/40-i18n-parity.test.js (NEW_KEYS lines ~66-79 — remove the replayWelcome entry; update the "15 keys" count comment)
    - tests/40-app-wiring.test.js (the three replay-welcome tests at lines ~106, ~133, ~157; and the security-note tests at ~173/~194/~209 that MUST stay — remove only the three replay-welcome ones)
    - tests/41-demo-gate.test.js (the ordering block lines ~94-101 that computes indexOf('help.entry.replayWelcome') and asserts takeTour after it)
    - assets/i18n-en.js (help.entry.center / help.entry.takeTour / help.entry.contact — the day-one + tour rows that remain)
  </read_first>
  <action>
    tests/40-i18n-parity.test.js: remove 'help.entry.replayWelcome' from the NEW_KEYS array and update the count comment (welcome 4 + replayWelcome 1 + install 7 + mobileHint 3 = 15 → welcome 4 + install 7 + mobileHint 3 = 14). Leave every other key and all assertions unchanged.
    tests/40-app-wiring.test.js: delete the three tests that assert the replay-welcome row (the "initHelpEntry mounts a Replay-welcome button" test, the "clicking Replay welcome calls showWelcome(true)" test, and the "app:language re-translates the Replay-welcome row" test) and any now-unused replay-only helper wiring. Keep the security-note surface tests ("bootAttentionSurfaces registers the security-note surface AND calls run()" and the two eligible() tests) exactly as they are — they are unrelated to this reversal.
    tests/41-demo-gate.test.js: rewrite the ordering assertion so it no longer references replayWelcome. Instead assert the takeTour row sits AFTER the Help center row (help.entry.center) and BEFORE the Contact us row (help.entry.contact) — the stable day-one anchors. Keep the "present on a normal page" and "absent in demo-mode" assertions intact.
    These edits describe the target 3-item popover; run against current code they stay green (the reframed demo-gate ordering already holds; the removed tests simply no longer assert the old behavior).
  </action>
  <verify>
    <automated>node tests/40-i18n-parity.test.js && node tests/40-app-wiring.test.js && node tests/41-demo-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help.entry.replayWelcome" tests/40-i18n-parity.test.js` = 0 and the count comment reads 14 (not 15).
    - `grep -c "replayWelcome\|showWelcome(true)\|Replay welcome\|Replay-welcome" tests/40-app-wiring.test.js` = 0 (the three replay tests removed) while the three security-note tests remain (grep for "security-note" still present).
    - tests/41-demo-gate.test.js no longer references replayWelcome (grep = 0); it asserts takeTour indexOf > center indexOf AND takeTour indexOf < contact indexOf.
    - `node tests/40-i18n-parity.test.js && node tests/40-app-wiring.test.js && node tests/41-demo-gate.test.js` all exit 0 against current (pre-Task-2) code (the reframed assertions already hold; removed tests no longer assert old behavior).
  </acceptance_criteria>
  <done>The Phase-40 guard tests and the demo-gate ordering describe a 3-item popover and stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the replay-welcome row from the "?" popover (assets/app.js)</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js initHelpEntry items[] lines ~517-534 (the four rows: center, replayWelcome, takeTour, contact; the demo filter ~538-540; the mount loop ~541-563)
    - assets/attention-coordinator.js (confirm AttentionCoordinator.showWelcome is exported and used by the welcome overlay + the D-12 CTA — it MUST NOT be removed)
    - .planning/phases/41-replayable-guided-tour/41-UAT.md (gap 8 missing: remove the replayWelcome item; the showWelcome(true) menu caller goes away; first-run welcome unchanged; result = 3 items)
  </read_first>
  <action>
    In initHelpEntry, remove the items[] entry with labelKey 'help.entry.replayWelcome' (the object whose action calls AttentionCoordinator.showWelcome(true)) — and its explanatory comment block. Leave the remaining three rows in order: help.entry.center (Help center link), help.entry.takeTour (guided-tour action), help.entry.contact (Contact us link). Leave the demo-mode filter that strips takeTour, the mount loop, the popover open/close wiring, and the app:language re-translate listener untouched. Do NOT touch AttentionCoordinator.showWelcome (first-run welcome overlay + the welcome CTA still call it) — only the menu caller of showWelcome(true) is being removed. Leave no comment naming the retired replayWelcome row.
  </action>
  <verify>
    <automated>node tests/40-app-wiring.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help.entry.replayWelcome" assets/app.js` = 0 and `grep -c "showWelcome(true)" assets/app.js` = 0 (the menu caller is gone).
    - initHelpEntry items[] has exactly three rows: help.entry.center, help.entry.takeTour, help.entry.contact (grep confirms all three labelKeys present, replayWelcome absent).
    - AttentionCoordinator.showWelcome is still defined/exported in assets/attention-coordinator.js (grep confirms — first-run untouched) and the welcome-CTA rewire path is unchanged.
    - `node tests/run-all.js` green (the removed Phase-40 replay tests from Task 1 mean no test now expects the row; count drops by the 3 removed tests but 0 failures).
  </acceptance_criteria>
  <done>The "?" popover mounts exactly three rows (Help center / guided tour / Contact us); showWelcome remains for first-run; the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the replayWelcome key + relabel takeTour (4 locales) + REQUIREMENTS traceability note</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, .planning/REQUIREMENTS.md</files>
  <read_first>
    - assets/i18n-en.js (help.entry.replayWelcome line ~599 = "Onboarding screen"; help.entry.takeTour line ~647 = "Take the tour")
    - assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js (the matching help.entry.replayWelcome + help.entry.takeTour lines; each file's encoding convention — CS \u-escaped)
    - .planning/REQUIREMENTS.md (the ONBD-02 row line ~93 + the Traceability table ~83-108 — add a note, do not delete the row)
    - .planning/phases/41-replayable-guided-tour/41-UAT.md (gap 8 missing: relabel takeTour to the guided-tour voice — Ben suggested "Onboarding Tour"; align with the welcome CTA's "guided tour"; REVERSES D-17/ONBD-02, note in REQUIREMENTS)
  </read_first>
  <action>
    In all four locale files: remove the help.entry.replayWelcome key entirely (it is no longer referenced anywhere). Relabel help.entry.takeTour: EN value becomes "Onboarding Tour" (Ben's explicit suggestion; aligns with the welcome CTA's guided-tour voice, avoids SaaS jargon); author natural equivalents in HE (noun form), DE (Sie register), CS (formal, \u-escaped), flagged DRAFT for the Phase 42.1 native pass. Keep the help.entry.center / help.entry.label / help.entry.contact keys unchanged and keep the key set parity-identical across all four files.
    In .planning/REQUIREMENTS.md: add a traceability NOTE (do not delete ONBD-02's history). Under or beside the ONBD-02 row / the Traceability table, add a note stating that the Phase-40 D-17 "?"-menu welcome-replay entry (help.entry.replayWelcome) was retired in Phase 41 gap-closure per Ben's decision 2026-07-08 — the first-run welcome overlay and AttentionCoordinator.showWelcome are unchanged; the "?" popover now offers the replayable guided tour as the single onboarding-replay entry. Keep ONBD-02 marked Complete (its first-run + re-open-via-tour contract still holds; only the redundant welcome-screen replay row is removed).
  </action>
  <verify>
    <automated>node tests/40-i18n-parity.test.js && node tests/41-tour-i18n-parity.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help.entry.replayWelcome" assets/i18n-en.js assets/i18n-he.js assets/i18n-de.js assets/i18n-cs.js` totals 0 across the four files.
    - `grep -c '"help.entry.takeTour": "Onboarding Tour"' assets/i18n-en.js` = 1; help.entry.takeTour is present + non-empty in he/de/cs (grep each).
    - `node tests/40-i18n-parity.test.js` exits 0 (NEW_KEYS no longer expects replayWelcome) and `node tests/41-tour-i18n-parity.test.js` exits 0 (takeTour parity holds with the new label).
    - .planning/REQUIREMENTS.md contains a note referencing the ONBD-02 / D-17 menu-replay reversal dated 2026-07-08; ONBD-02 remains marked Complete.
    - `node tests/run-all.js` green.
  </acceptance_criteria>
  <done>The replayWelcome key is retired in 4 locales, the tour entry reads "Onboarding Tour", the "?" is a 3-item menu, and REQUIREMENTS records the ONBD-02/D-17 reversal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → popover DOM | Row labels are injected via textContent (T-39-05); this plan removes a key and relabels one — no markup, no interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-12a | Tampering | Removing the replay row accidentally breaks first-run welcome (showWelcome) | medium | mitigate | showWelcome is explicitly preserved; the Phase-40 welcome-overlay tests (40-welcome-overlay.test.js, untouched) still cover showWelcome(false)/(true) directly. |
| T-39-05 | Information disclosure (XSS) | popover row labels | low | accept | Labels remain textContent from the i18n dict; no change to the injection mechanism. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/40-i18n-parity.test.js`, `node tests/40-app-wiring.test.js`, `node tests/41-demo-gate.test.js`, `node tests/41-tour-i18n-parity.test.js` → all exit 0.
- `node tests/run-all.js` → full suite green (no failures; the three removed Phase-40 replay tests reduce the count but nothing fails).
- `grep -c "help.entry.replayWelcome"` across app.js + 4 locales + the realigned tests = 0; showWelcome still present in attention-coordinator.js.
</verification>

<success_criteria>
The "?" popover is a 3-item, non-redundant menu (Help center / Onboarding Tour / Contact us), the welcome-screen replay row is gone in code + all four locales, first-run welcome is untouched, and the ONBD-02 / D-17 reversal is recorded in REQUIREMENTS traceability.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-12-SUMMARY.md` when done.
</output>