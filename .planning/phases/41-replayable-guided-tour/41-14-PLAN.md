---
phase: 41-replayable-guided-tour
plan: 14
type: execute
wave: 9
gap_closure: true
depends_on: []
files_modified:
  - assets/tour.js
  - assets/tour.css
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/webkit/41-rtl-geometry.mjs
autonomous: true
requirements: [TOUR-01, TOUR-04]
must_haves:
  truths:
    - "On a taller-than-viewport anchor (the Fields and Snippets settings panels, steps 4-5) the step box (tooltip with the step-count) is ALWAYS fully inside the viewport — never a wordless dim overlay you must scroll to escape (R2-1)."
    - "The engine brings the anchor's TOP into view (scroll up), not its center, for tall anchors — Ben's 'always scrolled all the way up'; the big whole-panel spotlight is KEPT as-is."
    - "The page stays inert during the tour — no in-tour clicking (D-07 STANDS; the R2-2/R2-3 interactive-overlay + click-to-advance ideas are explicitly NOT built)."
    - "The strengthened WebKit probe FAILS against the current center-scroll code (box off-screen on a tall panel) and PASSES after the scroll-to-top + tooltip-clamp fix (falsifiability)."
    - "Step 9 (save) points at the app's REAL export control: the fake monochrome upload SVG is gone; the real export icon (the 📤 / U+1F4E4 used by #exportSessionBtn) is what shows, and the copy names the Export button in plain words (R2-4)."
    - "Step 12 (help) leads with the Help center as 'there's more to explore', then a high-level tease (backups/snippets/Heart-Wall/export + the Reporting tab) — not Reporting-first (R2-5)."
    - "The 4-locale tour-copy parity gate stays green (keys unchanged; EN values updated; HE/DE/CS machine-draft kept in sync, flagged for the Phase 42.1 native pass; no emoji in any i18n string)."
  artifacts:
    - "assets/tour.js renderSpotlight/positionSpotlight: tall-anchor scroll uses block:'start' (or window scroll to the anchor top) and the tooltip's final top/left is CLAMPED fully into the viewport with a margin so the step-count box is always visible; spotlight ring size unchanged (big boxes kept)."
    - "assets/tour.js save-step glyph: the injected generic upload SVG replaced by the REAL export icon (📤 / &#128228;); prefer textContent (plain-text emoji — no innerHTML/raw-markup needed) and update any 'exactly one innerHTML'/'no inline SVG' grep gate accordingly."
    - "assets/i18n-en.js help.tour.step.save.body + help.tour.step.help.body rewritten to the Ben-approved provisional copy; HE/DE/CS machine-draft updated to match (DRAFT-flagged)."
    - "tests/webkit/41-rtl-geometry.mjs: a NEW box-in-viewport assertion on a taller-than-viewport anchor (drive the tour to the Snippets/Fields step, assert the tooltip rect is fully within innerWidth/innerHeight) — RED before the fix, GREEN after."
  key_links:
    - "The tooltip clamp and the scroll-to-anchor-top must agree: clamp guarantees visibility for ANY anchor; scroll-to-top makes the tall-panel case land naturally at the top. Both ship together."
    - "The save glyph is the app's real 📤 (add-session.html #exportSessionBtn span.button-icon &#128228;) — verify against that source so it cannot drift to a foreign icon again."
    - "Copy VALUES change only; the help.tour.step.* KEY set is unchanged, so the 41-tour-i18n-parity gate (presence/parity/placeholder/no-emoji) stays green without a key realignment."
---

<objective>
Close the Round-2 (2026-07-09) replay gaps on the shipped v3 tour, keeping the interaction model Ben + Sapir locked: the tour stays NON-interactive (page inert, D-07 stands — the clicking / interactive-spotlight / click-to-advance ideas are NOT built). Three fixes:

1. **Mechanism (the real work) — box always visible on tall panels (R2-1).** On the long settings panels (Custom Fields, Text Snippets), the anchor is taller than the laptop viewport, so today `renderSpotlight`'s `scrollIntoView({block:'center'})` centers the panel's middle and the tethered step box lands off-screen — you see a dim overlay with no words until you scroll to the top/bottom. Fix: for a tall anchor bring its TOP into view (scroll up, Ben's directive) and CLAMP the tooltip fully inside the viewport so the step-count box is always on screen. Keep the big whole-panel spotlight as-is (the "just highlight the header" idea is explicitly dropped).

2. **Step 9 export icon + copy (R2-4).** 41-10 inlined a generic monochrome upload SVG in the save tooltip; it is NOT the app's real export icon (which is the colourful 📤 on #exportSessionBtn), so it reads as foreign. Drop the fake glyph; show the REAL 📤; reword the body to name the Export button in plain words.

3. **Step 12 finish → Help-first (R2-5).** The v3 finish/help step leads with Reporting; Ben wants it to lead with the Help center ("we didn't cover everything — there's more to explore, and the Help center details it all"), then a high-level tease of the breadth (backups & how often they run, the snippet library, the Heart-Wall, export formats, and the Reporting tab).

Note on copy: the EN wording here is Ben-approved but PROVISIONAL — Ben will hand final texts later, no planning needed for that. Do not over-invest in wordsmithing; make the story correct and the mechanism solid. HE/DE/CS stay machine-draft (Phase 42.1 native pass).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@assets/tour.js
@assets/tour.css
@assets/i18n-en.js
@tests/webkit/41-rtl-geometry.mjs
</context>

<tasks>

<task type="test-first">
  <name>Task 1: RED — box-in-viewport assertion on a taller-than-viewport anchor</name>
  <files>tests/webkit/41-rtl-geometry.mjs</files>
  <read_first>
    - tests/webkit/41-rtl-geometry.mjs (the existing WebKit probe harness — how it serves the repo, starts window.Tour, and reads getBoundingClientRect)
    - assets/tour.js (renderSpotlight line ~314-390: scrollIntoView block:'center' + positionSpotlight + the one-rAF re-measure; positionSpotlight ~196)
    - settings.html (the Fields/Snippets panels the tour anchors to — data-tour="fields" / data-tour="snippets"; these are the tall anchors)
  </read_first>
  <action>
    Extend the WebKit probe with a NEW assertion group that drives the tour to a taller-than-viewport settings anchor (the Snippets step — or Fields — after the tour auto-navigates to settings and the step's tab-activation runs) and asserts the STEP BOX (the .sg-tour-tooltip carrying the step-count) is FULLY within the viewport: rect.top >= 0 AND rect.left >= 0 AND rect.bottom <= window.innerHeight AND rect.right <= window.innerWidth (allow a small margin). Use a viewport short enough that the panel is genuinely taller than it (e.g. set the WebKit context viewport height to ~600 so the long panel overflows). This MUST FAIL on the current center-scroll engine (the box is pushed off-screen mid-panel). Print PASS/FAIL per assertion; exit non-zero on failure. Keep it out of tests/run-all.js discovery (stays under tests/webkit/).
    If driving the tour all the way to the settings step in the probe proves brittle, an acceptable alternative is a focused synthetic case: mount a deliberately taller-than-viewport anchor, call the engine's render for a step bound to it, and assert the same box-in-viewport invariant — as long as it exercises the REAL renderSpotlight/positionSpotlight path and fails before the fix.
  </action>
  <verify>
    <automated>node tests/webkit/41-rtl-geometry.mjs   # expect NON-zero exit now (RED) — the new box-in-viewport assertion fails on center-scroll</automated>
  </verify>
  <acceptance_criteria>
    - The new assertion exercises a real taller-than-viewport anchor and the real engine render path
    - It FAILS (probe exits non-zero) against the current code, isolating the R2-1 defect
    - The existing probe assertions (spotlight branch, RTL physical side, 4-language re-render) still run and pass
  </acceptance_criteria>
  <done>The strengthened probe fails on exactly the tall-anchor box-visibility gap and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — scroll-to-anchor-top + tooltip viewport clamp (keep big spotlight, page stays inert)</name>
  <files>assets/tour.js, assets/tour.css</files>
  <read_first>
    - assets/tour.js (renderSpotlight ~314-390, positionSpotlight ~196-235)
    - assets/tour.css (.sg-tour-spotlight / .sg-tour-tooltip / .sg-tour-arrow positioning)
  </read_first>
  <action>
    In renderSpotlight/positionSpotlight: for a tall anchor (anchor rect height >= ~0.7 * window.innerHeight, or its bottom would fall off-screen after a center scroll) scroll so the anchor's TOP comes into view — use scrollIntoView({block:'start'}) or an explicit scroll to the anchor top — instead of block:'center'. Then CLAMP the tooltip's final physical top/left so the whole tooltip stays inside the viewport with a small margin (top >= margin; bottom <= innerHeight - margin; left/right likewise), re-clamping in the one-rAF post-scroll re-measure. Do NOT shrink the spotlight ring — the big whole-panel highlight is kept (Ben). Do NOT add any interactivity: the overlay stays a single inert click-blocker (D-07 stands); no 4-panel frame, no advance-on-anchor-click. Preserve the existing first-paint snap (.sg-tour-instant) and the RTL physical-coordinate positioning from 41-08. Adjust tour.css only if the clamp needs a max-height/overflow tweak on the tooltip; keep it minimal.
    Re-run the probe until GREEN.
  </action>
  <verify>
    <automated>node tests/webkit/41-rtl-geometry.mjs   # expect exit 0 (GREEN) — box now fully in viewport on the tall panel, all prior assertions still pass</automated>
    <automated>node tests/run-all.js   # full jsdom suite still green</automated>
  </verify>
  <acceptance_criteria>
    - The tall-anchor box-in-viewport assertion PASSES; the spotlight branch + RTL physical-side + 4-language assertions still PASS
    - node tests/run-all.js remains all-green (no regression)
    - Spotlight ring is NOT shrunk (grep/inspect: no new max-height clamp on .sg-tour-spotlight); overlay remains a single inert layer (no new interactive/pointer-events:auto hole)
  </acceptance_criteria>
  <done>On the Fields/Snippets steps the step box is always fully on screen with the anchor top in view; every other geometry assertion still holds; page stays inert.</done>
</task>

<task type="auto">
  <name>Task 3: Step 9 real export icon + Step 12 Help-first copy (provisional, 4 locales, parity green)</name>
  <files>assets/tour.js, assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/tour.js (the save-step glyph injection ~349-360 — the generic SVG string to replace)
    - add-session.html lines ~55-62 (#exportSessionBtn — the REAL export icon is the span.button-icon &#128228; = 📤)
    - assets/i18n-en.js (help.tour.step.save.body ~625; help.tour.step.help.body ~631)
    - any test asserting the save glyph shape (e.g. an 'exactly one innerHTML' / 'no inline SVG' gate added in 41-10) — update it to match the real-icon mechanism
  </read_first>
  <action>
    (a) STEP 9 icon: replace the injected generic upload SVG with the app's REAL export icon (📤 / &#128228;). Prefer glyph.textContent = the emoji (plain text — removes the raw-markup/innerHTML surface entirely); if a test asserts the old SVG/innerHTML, update it to assert the real icon via the new mechanism. Verify the icon matches add-session.html #exportSessionBtn so it cannot drift again.
    (b) STEP 9 copy (EN, provisional — Ben-approved): help.tour.step.save.body →
        "When you save, the session settles safely into this device — nothing leaves it. Open any saved session later and share it as a client-ready PDF using the Export button up top."
        (the real 📤 renders from the glyph mechanism; the false "this is its icon" phrase is gone).
    (c) STEP 12 copy (EN, provisional — Ben-approved): help.tour.step.help.body →
        "That's the whole path — though not all of Sessions Garden. Behind this question mark lives the Help center: plain-language guides to everything we didn't walk — how often your backups run, the snippet library, the Heart-Wall, choosing an export format, and more. There's more in the app to meet, too, like the Reporting tab, quietly turning your sessions into numbers over time. It's here on every page — and this tour waits alongside it, whenever you'd like to walk again."
    (d) Mirror (b) and (c) into HE/DE/CS as machine-draft (keep the existing DRAFT flag/convention from 41-11), so no locale carries stale save/help copy. Do NOT add or remove any i18n KEY — values only. Keep all i18n strings emoji-free (the 📤 is injected by the engine, never in the copy string).
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js   # 4-locale parity + no-emoji stays green (keys unchanged)</automated>
    <automated>node tests/run-all.js   # full suite green (any updated glyph test included)</automated>
  </verify>
  <acceptance_criteria>
    - The save tooltip shows the REAL 📤 (matches #exportSessionBtn); no generic upload SVG remains in tour.js; the false "this is its icon" deixis is gone
    - Step 12 EN copy leads with the Help center + breadth tease (Help-first, not Reporting-first)
    - 4-locale parity gate green; no emoji in any i18n VALUE; help.tour.step.* key set unchanged
    - node tests/run-all.js all green
  </acceptance_criteria>
  <done>Save step points at the real export icon in plain words; finish step is Help-first with a breadth tease; all four locales present; suite + parity green.</done>
</task>

</tasks>

<verification>
Phase-gate re-check happens in the re-run of 41-13 (automated suite + WebKit probe on the final tour), then Ben replays EN and — separately, after EN is locked — provides the Hebrew pass. This plan's own gate: the strengthened WebKit probe is RED before Task 2 and GREEN after; the jsdom suite and the 4-locale parity gate stay green throughout.
</verification>
