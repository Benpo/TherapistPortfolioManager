---
phase: 41-replayable-guided-tour
plan: 06
type: execute
wave: 4
depends_on: ["41-02", "41-04"]
files_modified:
  - index.html
  - add-client.html
  - add-session.html
  - sessions.html
  - reporting.html
  - report.html
  - settings.html
  - help.html
  - sw.js
  - tests/41-precache.test.js
autonomous: true
requirements: [TOUR-03, TOUR-04]
must_haves:
  truths:
    - "assets/tour.js and assets/tour.css are loaded on ALL EIGHT app-chrome pages that load app.js + attention-coordinator.js (index, add-client, add-session, sessions, reporting, report, settings, help) — tour.js as a <script> after app.js, tour.css as a <link> in the head (Pitfall 7). demo.html is excluded (D-16). Rationale (architect-gate A1): the launch surfaces render on EVERY chrome page — the welcome CTA (attention-coordinator.js, on all 8) and the '?' 'Take the tour' row + reminder Start (initHelpEntry, on all 8) — so window.Tour MUST be present wherever a launch can fire, or those surfaces are dead UI. Anchors themselves stay on the 4 step-pages only (Plan 02, unchanged); the extra pages load the engine solely so their launch surfaces resolve + Tour.start() can page-navigate to step 1 (A2)."
    - "/assets/tour.js and /assets/tour.css are in sw.js PRECACHE_URLS so the tour works fully offline on an installed PWA (HELP-07 precedent)."
    - "The precache test asserts both new assets are in PRECACHE_URLS AND every one of the eight chrome pages references the same paths (no drift); demo.html is excluded."
  artifacts:
    - "<link rel=stylesheet href=./assets/tour.css> + <script src=./assets/tour.js> on all eight chrome pages (index, add-client, add-session, sessions, reporting, report, settings, help)"
    - "/assets/tour.js + /assets/tour.css appended to sw.js PRECACHE_URLS"
    - "tests/41-precache.test.js — precache + 8-page-reference no-drift guard"
  key_links:
    - "tour.js must load AFTER app.js (window.App.lockBodyScroll + the initCommon Tour.resume() call depend on App being defined first)"
    - "CACHE_NAME auto-rolls from INTEGRITY_TOKEN — no manual bump — but the PRECACHE_URLS edit is mandatory or the tour 404s offline"
---

<objective>
Ship the tour engine to production offline: add the `assets/tour.js` `<script>` and `assets/tour.css` `<link>` to all EIGHT app-chrome pages (index, add-client, add-session, sessions, reporting, report, settings, help; demo.html excluded per D-16) and add both to `sw.js` PRECACHE_URLS, guarded by a precache no-drift test.

Purpose: Pitfall 7 — a new `assets/*.js` that is not precached breaks the tour for installed-PWA users offline (a HELP-07-class regression). Architect-gate A1: the tour must load on every page that renders a launch surface, not just the 4 step-pages, or the welcome CTA / '?' 'Take the tour' row / reminder Start become dead UI on add-client, report, settings, help (each loads app.js + attention-coordinator.js — verified). help.css does NOT load on the non-help chrome pages (verified — only tokens.css + app.css; help.html already loads help.css), so tour.css is a genuinely new dependency and its own precache entry. CACHE_NAME auto-rolls from the deploy integrity token, so no manual cache bump is needed — only the URL-list edit.
Output: 8 page shells + sw.js + the precache test.
</objective>

<artifacts_produced>
Symbols this plan creates:
- Page-shell tags: `<link rel="stylesheet" href="./assets/tour.css">` (head) + `<script src="./assets/tour.js"></script>` (after ./assets/app.js) on index.html, add-client.html, add-session.html, sessions.html, reporting.html, report.html, settings.html, help.html
- sw.js: `/assets/tour.js` + `/assets/tour.css` entries in PRECACHE_URLS
- New test: `tests/41-precache.test.js`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-CONTEXT.md
@.planning/phases/41-replayable-guided-tour/41-RESEARCH.md
@sw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the RED precache + page-reference no-drift test</name>
  <read_first>
    - tests/40-precache.test.js (the exact fs-scan idiom: read sw.js, isolate the PRECACHE_URLS array, assert entries present + page <script> references match, demo.html excluded)
    - sw.js (PRECACHE_URLS ~33-97 — the Phase 39 help.js/help.css block is the analog for where the tour entries go)
  </read_first>
  <action>
    Create tests/41-precache.test.js by mirroring tests/40-precache.test.js. Hardcode the CHROME_PAGES set = [index.html, add-client.html, add-session.html, sessions.html, reporting.html, report.html, settings.html, help.html] (the eight pages that load app.js + attention-coordinator.js, so the launch surfaces render — architect-gate A1). Assert: (1) sw.js PRECACHE_URLS contains '/assets/tour.js' AND '/assets/tour.css'; (2) each of the eight CHROME_PAGES references both ./assets/tour.js (as a <script>) and ./assets/tour.css (as a <link>) — the referenced paths match the precached paths with no drift; (3) demo.html does NOT get the tour script or link (consistent with the D-16 demo seam — the tour is not in demo). Exit 0 all-present, 1 on any miss (run-all.js contract). Author RED (tags + precache entries not added yet) — exit 1 until Task 2.
  </action>
  <verify>
    <automated>node tests/41-precache.test.js; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - tests/41-precache.test.js exists and exits 1 (RED) standalone
    - It asserts both /assets/tour.js and /assets/tour.css in PRECACHE_URLS
    - It hardcodes the eight-page CHROME_PAGES set and asserts each references both assets, and that demo.html is excluded
  </acceptance_criteria>
  <done>RED precache guard pins the offline contract before the tags land.</done>
</task>

<task type="auto">
  <name>Task 2: Add tour.js/tour.css tags to the 8 chrome pages + PRECACHE_URLS entries</name>
  <read_first>
    - index.html (head <link> block ~42-43; script order tail — ./assets/app.js at ~361, tour.js goes right after)
    - add-session.html (app.js at ~592 — tour.js after it)
    - sessions.html (app.js at ~117 — tour.js after it)
    - reporting.html (app.js at ~104 — tour.js after it)
    - add-client.html (tokens/app css at ~22-23; ./assets/app.js at ~196 — tour.js after it)
    - report.html (tokens/app css at ~22-23; ./assets/app.js at ~109 — tour.js after it)
    - settings.html (tokens/app css at ~22-23; ./assets/app.js at ~375 — tour.js after it)
    - help.html (tokens/app/help css at ~22-24; ./assets/app.js at ~125 — tour.js after it; tour.css is additive alongside the existing help.css link)
    - sw.js (PRECACHE_URLS help block ~90-97 — append the tour entries alongside)
  </read_first>
  <action>
    On each of the eight app-chrome pages — index.html, add-client.html, add-session.html, sessions.html, reporting.html, report.html, settings.html, help.html — add <link rel="stylesheet" href="./assets/tour.css" /> in the <head> next to the existing tokens.css/app.css links (on help.html it sits alongside the existing help.css link), and add <script src="./assets/tour.js"></script> immediately AFTER the ./assets/app.js script tag (tour.js depends on window.App being defined + on initCommon calling Tour.resume). These eight are exactly the pages that load app.js + attention-coordinator.js, i.e. every page a launch surface renders on (architect-gate A1) — so no launch surface is ever dead. In sw.js, append '/assets/tour.js' and '/assets/tour.css' to PRECACHE_URLS next to the Phase 39 help block. Do NOT add the tour tags to demo.html (D-16). CACHE_NAME auto-rolls from INTEGRITY_TOKEN — no manual bump; the deploy stamps it. Note the repo gotcha: a PRECACHE_URLS-only edit ships the offline entry without a CACHE_NAME chore commit because the token derives it.
  </action>
  <verify>
    <automated>node tests/41-precache.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/41-precache.test.js exits 0
    - grep -c "assets/tour.js" returns 1 for EACH of index.html, add-client.html, add-session.html, sessions.html, reporting.html, report.html, settings.html, help.html; same for assets/tour.css
    - grep -c "/assets/tour.js" sw.js returns 1 and "/assets/tour.css" returns 1
    - grep -c "assets/tour.js" demo.html returns 0 (demo excluded, D-16)
    - In each of the eight pages, the tour.js <script> appears after the app.js <script> (ordering preserved)
    - node tests/run-all.js stays green
  </acceptance_criteria>
  <done>tour.js/tour.css load on all eight app-chrome pages after app.js and precache offline; demo excluded; no launch surface is dead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → SW precache | Static asset list; no user input. A missing entry degrades to a 404 offline (availability), not a security issue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-07 | Denial of Service | tour 404s offline | medium | mitigate | /assets/tour.js + /assets/tour.css added to PRECACHE_URLS; precache test guards the entry + page-reference no-drift (Pitfall 7). |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (see 41-RESEARCH.md Package Legitimacy Audit). N/A. |
</threat_model>

<verification>
- `node tests/41-precache.test.js` exits 0
- `node tests/run-all.js` full suite green
- Real offline availability is confirmed in Plan 07's UAT (installed PWA, offline navigation)
</verification>

<success_criteria>
tour.js + tour.css load after app.js on all eight app-chrome pages (index, add-client, add-session, sessions, reporting, report, settings, help) and are precached for offline; demo.html excluded; no launch surface is dead (A1); 8-page no-drift precache test green.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-06-SUMMARY.md` when done.
</output>
