---
phase: 41-replayable-guided-tour
plan: 09
type: execute
wave: 6
gap_closure: true
depends_on: []
files_modified:
  - index.html
  - add-session.html
  - sessions.html
  - reporting.html
  - assets/app.js
  - tests/41-anchor-presence.test.js
autonomous: true
requirements: [TOUR-02, TOUR-03]
must_haves:
  truths:
    - "A single always-present anchor wraps BOTH home action buttons (data-tour=begin on the .inline-actions row) so the merged 'Two ways to begin' step lights one calm target (D-01 merge)."
    - "The Sessions and Reporting arrival steps can light the NAV TAB that brought the user there — data-tour=nav-sessions / nav-reporting live on the renderNav links, on every page (D-02 chrome-only)."
    - "Settings is reachable by the tour — data-tour=settings on the header gear (the one control invisible from the main nav)."
    - "The heart step lights the emotions accordion (open by default) so the lit section matches the copy's first noun (deixis honesty)."
    - "The anchor-presence rot guard enforces the NEW 10-anchor contract and the retired anchors are gone from source."
  artifacts:
    - "data-tour=begin on index.html .inline-actions row; data-tour=nav-sessions / nav-reporting in renderNav; data-tour=settings on .settings-gear-btn"
    - "data-tour=session-heart moved to the emotions accordion; per-button add-client/add-session + sessions/reporting section-title anchors removed"
    - "tests/41-anchor-presence.test.js updated to the new 10-anchor contract table"
  key_links:
    - "The engine STEPS[] (41-10) binds to these exact data-tour values; if an anchor is missing a fresh empty app degrades to a fallback modal instead of a spotlight (D-02)."
    - "renderNav is JS chrome rendered on every page — nav-sessions/nav-reporting are the same dynamic-anchor pattern as backup/help (mounted via app.js)."
---

<objective>
Recompose the tour's anchor contract to the Ben-approved 41-STORYLINE.md so the storyline's re-anchored steps have real targets: add data-tour="begin" on the home two-button row (the merged step, D-01), move data-tour="session-heart" to the emotions accordion (deixis honesty), add data-tour="nav-sessions" / "nav-reporting" on the nav links so arrival steps light the tab that brought the user there (the core UAT gap 1/2 "arrive via the tab" fix), add data-tour="settings" on the header gear (UAT gap 6 — Settings was unreachable from the tour), and retire the now-unused per-button + section-title anchors. Update the anchor-presence rot guard to the new contract first (RED), then land the anchors (GREEN) — mirroring how 41-02 shipped this contract originally.

Purpose: these attribute changes are the structural spine the storyline needs; without nav-sessions/nav-reporting and settings there is no way to narrate the page changes or point at the gear. All anchors are always-present chrome (D-02) so a brand-new empty app still shows spotlights, never fallbacks.
Output: the new data-tour anchor set across 4 page shells + renderNav + the gear, and the rot guard that enforces it.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- NEW anchor `data-tour="begin"` on the `.inline-actions` row in index.html.
- NEW anchors `data-tour="nav-sessions"` (Sessions link) and `data-tour="nav-reporting"` (Reporting link) in the renderNav template in assets/app.js.
- NEW anchor `data-tour="settings"` on the `.settings-gear-btn` link in initSettingsLink (assets/app.js).
- MOVED anchor `data-tour="session-heart"` from the heart-shield accordion to the emotions accordion in add-session.html.
- REMOVED anchors: `data-tour="add-client"` / `data-tour="add-session"` (index.html buttons); `data-tour="sessions"` (sessions.html section title); `data-tour="reporting"` (reporting.html section title).
- CHANGED test tests/41-anchor-presence.test.js — ANCHORS[] table now: overview, begin (index.html); session-setup, session-heart, session-save (add-session.html); nav-sessions, nav-reporting, backup, settings, help (assets/app.js).
</artifacts_produced>

<!-- These data-tour literals appear in <action> prose because they are the exact
     attribute tokens the executor must add/remove/move; several are also the
     subject of negative greps (retired anchors, grep == 0). Sanctioned per the
     comment-text-discipline gate — the executor is instructed to leave NO such
     token behind as a comment in the edited files. -->
<!-- planner-discipline-allow: data-tour="begin" -->
<!-- planner-discipline-allow: data-tour="add-client" -->
<!-- planner-discipline-allow: data-tour="add-session" -->
<!-- planner-discipline-allow: data-tour="sessions" -->
<!-- planner-discipline-allow: data-tour="reporting" -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-02-SUMMARY.md
@index.html
@add-session.html
@assets/app.js
@tests/41-anchor-presence.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the anchor-presence rot guard to the new 10-anchor contract (RED)</name>
  <files>tests/41-anchor-presence.test.js</files>
  <read_first>
    - tests/41-anchor-presence.test.js (the current ANCHORS[] table lines ~48-59 + the header doc-comment lines ~13-21 that enumerate the old step→file mapping; the match regex tolerates both HTML attribute and JS setAttribute binding forms)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 + §5 (the authoritative new anchor set and which anchors are retired/moved/added)
  </read_first>
  <action>
    Replace the ANCHORS[] contract table with the NEW 10-anchor set and its source files, keeping the array length at 10:
    overview → index.html; begin → index.html; session-setup → add-session.html; session-heart → add-session.html; session-save → add-session.html; nav-sessions → assets/app.js; nav-reporting → assets/app.js; backup → assets/app.js; settings → assets/app.js; help → assets/app.js.
    Remove the add-client, add-session (index.html) and sessions, reporting (section-title) rows. Update the header doc-comment step→file mapping to match (steps 1-2 index.html; 3-5 add-session.html; 6-10 assets/app.js chrome created in JS via renderNav/setAttribute). Keep the dual-form match regex and the 30-fake-test-detector ALLOWLIST registration untouched. This will FAIL (RED) until Tasks 2+3 land the anchors — that is expected.
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js; echo "exit:$? (RED expected until Tasks 2+3)"</automated>
  </verify>
  <acceptance_criteria>
    - ANCHORS[] lists exactly these 10 values in this order: overview, begin, session-setup, session-heart, session-save, nav-sessions, nav-reporting, backup, settings, help.
    - `grep -c "add-client\|'add-session'\|value: 'sessions'\|value: 'reporting'" tests/41-anchor-presence.test.js` returns 0 (retired anchors removed from the contract).
    - nav-sessions, nav-reporting, settings map to sourceFile 'assets/app.js'; begin maps to 'index.html'.
    - Run before Tasks 2+3, the test exits non-zero and the FAIL lines name begin / nav-sessions / nav-reporting / settings (RED proves it guards the new contract).
  </acceptance_criteria>
  <done>The rot guard enforces the new 10-anchor contract and fails RED until the anchors land.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose the HTML anchors (index / add-session / sessions / reporting)</name>
  <files>index.html, add-session.html, sessions.html, reporting.html</files>
  <read_first>
    - index.html (the greeting card line 71 with data-tour="overview" — keep; the .inline-actions row line 102 wrapping #addClientBtn line 103 and #addSessionBtn line 107)
    - add-session.html (the heart-shield accordion line 205 with data-accordion="heart-shield" + data-tour="session-heart"; the emotions accordion line 246 `class="accordion-section is-active" data-accordion="emotions"` — open by default)
    - sessions.html (line 52 .section-title … data-tour="sessions"); reporting.html (line 52 .section-title … data-tour="reporting")
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §5 (delta table — the exact add/remove/move instructions)
  </read_first>
  <action>
    index.html: add the attribute data-tour="begin" to the `.inline-actions` row div at line ~102 (the div that wraps both buttons). Remove the data-tour="add-client" attribute from #addClientBtn (line ~103) and the data-tour="add-session" attribute from #addSessionBtn (line ~107) — remove ONLY those two attributes; leave the buttons' id + classes + labels + icons intact. Leave data-tour="overview" on the greeting card untouched. Do NOT leave any comment referencing the retired token names.
    add-session.html: remove the data-tour="session-heart" attribute from the heart-shield accordion (line ~205) and add data-tour="session-heart" to the emotions accordion div at line ~246 (the div with class "accordion-section is-active" and data-accordion="emotions"). Net effect: one attribute moves; the emotions accordion is open by default so it is visible → spotlight branch.
    sessions.html: remove the data-tour="sessions" attribute from the .section-title on line ~52 (keep data-i18n + the heading text). reporting.html: remove the data-tour="reporting" attribute from the .section-title on line ~52 (keep data-i18n + text). These section-title anchors are replaced by the renderNav nav-tab anchors in Task 3.
  </action>
  <verify>
    <automated>echo "begin=$(grep -c 'data-tour=\"begin\"' index.html) retiredIdx=$(grep -c 'data-tour=\"add-client\"\|data-tour=\"add-session\"' index.html) heartAS=$(grep -c 'data-tour=\"session-heart\"' add-session.html) sess=$(grep -c 'data-tour=\"sessions\"' sessions.html) rep=$(grep -c 'data-tour=\"reporting\"' reporting.html)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="begin"' index.html` = 1; `grep -c 'data-tour="add-client"' index.html` = 0; `grep -c 'data-tour="add-session"' index.html` = 0.
    - `grep -c 'data-tour="session-heart"' add-session.html` = 1 AND that single match is on the emotions accordion (the line also containing `data-accordion="emotions"`), not the heart-shield accordion.
    - `grep -c 'data-tour="sessions"' sessions.html` = 0; `grep -c 'data-tour="reporting"' reporting.html` = 0.
    - The #addClientBtn and #addSessionBtn retain their id, classes, label spans, and icon spans (grep confirms both ids still present in index.html).
    - No comment referencing "add-client" / "add-session" left behind in index.html (grep for those tokens in index.html returns 0 total).
  </acceptance_criteria>
  <done>The home two-button row carries data-tour="begin", the heart anchor sits on the emotions accordion, and the retired per-button + section-title anchors are gone from the page shells.</done>
</task>

<task type="auto">
  <name>Task 3: Add the renderNav tab anchors + the settings-gear anchor (assets/app.js) → rot guard GREEN</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js renderNav (lines ~137-156 — the placeholder.innerHTML template with the Overview/Sessions/Reporting/Add Client/Add Session links; the Sessions link is line ~143, the Reporting link is line ~144)
    - assets/app.js initSettingsLink (lines ~391-455 — the `link.className = 'header-control-btn settings-gear-btn'` at line ~399; the compile-time gear SVG; the App.installNavGuard call)
    - assets/app.js the existing tour-anchor pattern: initHelpEntry line ~487 `btn.setAttribute('data-tour', 'help')` and mountBackupCloudButton line ~644 `btn.setAttribute('data-tour', 'backup')` — mirror this JS-anchor idiom for the gear
    - tests/41-anchor-presence.test.js (the updated contract from Task 1 — this task turns it GREEN)
  </read_first>
  <action>
    renderNav: in the placeholder.innerHTML template string, add the attribute data-tour="nav-sessions" to the Sessions link (the `<a href="./sessions.html" data-nav="sessions" …>`) and data-tour="nav-reporting" to the Reporting link (the `<a href="./reporting.html" data-nav="reporting" …>`). Leave the Overview, Add Client, Add Session links and the data-nav/data-i18n attributes untouched. These anchors render on every page (renderNav runs in initCommon), which is why the arrival steps can light the tab.
    initSettingsLink: after the `link.className = 'header-control-btn settings-gear-btn'` assignment, add `link.setAttribute('data-tour', 'settings');` with a one-line comment matching the existing help/backup anchor comments (Phase 41 tour anchor — inert selector for the guided-tour engine). Do NOT alter the gear SVG, the navGuard wiring, the insertion order, or the language re-render listener.
    After both edits the anchor-presence rot guard (Task 1) must be GREEN because nav-sessions/nav-reporting resolve via the renderNav template form (data-tour="value") and settings resolves via the setAttribute form — both matched by the test's dual-form regex.
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="nav-sessions"' assets/app.js` ≥ 1 (on the Sessions renderNav link) and `grep -c 'data-tour="nav-reporting"' assets/app.js` ≥ 1 (on the Reporting renderNav link).
    - `grep -c "setAttribute('data-tour', 'settings')" assets/app.js` = 1 (on the .settings-gear-btn link).
    - `node tests/41-anchor-presence.test.js` exits 0 (all 10 new-contract anchors resolve — rot guard GREEN).
    - `node tests/run-all.js` passes (count unchanged at 152 — no behavior regression; the section-title data-i18n and nav data-nav attributes are intact).
    - The gear SVG, App.installNavGuard call, and initSettingsLink._listenerInstalled block are byte-unchanged (grep confirms the navGuard call still present).
  </acceptance_criteria>
  <done>renderNav carries the nav-sessions/nav-reporting tab anchors and the header gear carries data-tour="settings"; the anchor-presence rot guard is GREEN on the full new contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page shell / renderNav → tour engine | data-tour attributes are inert presentation selectors; they carry no user input and change no element behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-09a | Tampering | Silent anchor drop on a future refactor degrades the tour to fallback modals on a fresh app (violates D-02) | medium | mitigate | The anchor-presence rot guard (Task 1) is updated to the new 10-anchor contract and fails the suite if any anchor is dropped. |
| T-41-01 | Information disclosure (XSS) | renderNav innerHTML template | low | accept | renderNav already assigns a compile-time literal template (no user interpolation); adding two static data-tour attributes introduces no interpolation. Unchanged posture. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-anchor-presence.test.js` → exits 0 on the new 10-anchor contract (RED after Task 1, GREEN after Tasks 2+3).
- `node tests/run-all.js` → full suite green (152 passed), no regression from the attribute moves.
- Grep gates in each task's acceptance_criteria pass.
</verification>

<success_criteria>
The storyline's anchor set is live: data-tour="begin" on the home two-button row, session-heart on the emotions accordion, nav-sessions/nav-reporting on the nav tabs, settings on the header gear, and the retired per-button + section-title anchors removed — all enforced by the updated rot guard on a fresh empty app.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-09-SUMMARY.md` when done.
</output>