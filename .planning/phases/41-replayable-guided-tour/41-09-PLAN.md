---
phase: 41-replayable-guided-tour
plan: 09
type: execute
wave: 6
gap_closure: true
depends_on: []
files_modified:
  - index.html
  - add-session.html
  - sessions.html
  - reporting.html
  - settings.html
  - assets/app.js
  - tests/41-anchor-presence.test.js
autonomous: true
requirements: [TOUR-02, TOUR-03]
must_haves:
  truths:
    - "The three settings panels each carry a static tour anchor — data-tour=personalize on #settingsTabPersonalize, data-tour=fields on #settingsTabFields, data-tour=snippets on #settingsTabSnippets — so the settings-first chapter (v3 steps 3-5) has real spotlight targets."
    - "The whole main menu is one spotlight target — data-tour=nav on the .app-nav element in renderNav — for the 'Ready to begin' step (v3 step 6, whole-menu spotlight)."
    - "The Sessions arrival step can light the NAV TAB that brought the user there — data-tour=nav-sessions on the renderNav Sessions link, on every page (D-02 chrome-only); there is NO nav-reporting anchor (Reporting is not a step in v3)."
    - "Settings is reachable by the tour — data-tour=settings on the header gear (the one control invisible from the main nav)."
    - "The heart step lights the emotions accordion (open by default) so the lit section matches the copy's first noun (deixis honesty)."
    - "The anchor-presence rot guard enforces the NEW 12-anchor v3 contract; the retired per-button (index.html) and section-title (sessions.html/reporting.html) anchors are gone from source, and the nav vs nav-sessions match is boundary-safe."
  artifacts:
    - "data-tour=personalize/fields/snippets on the three settings.html panels; data-tour=nav on .app-nav and data-tour=nav-sessions on the Sessions link in renderNav; data-tour=settings on .settings-gear-btn"
    - "data-tour=session-heart moved to the emotions accordion; per-button add-client/add-session (index.html) + sessions/reporting section-title anchors removed"
    - "tests/41-anchor-presence.test.js updated to the new 12-anchor contract table with a hyphen-safe value boundary"
  key_links:
    - "The engine STEPS[] (41-10) binds to these exact data-tour values; if an anchor is missing (or a settings panel is not activated) the app degrades to a fallback modal instead of a spotlight (D-02)."
    - "renderNav is JS chrome rendered on every page — nav + nav-sessions are the same dynamic-anchor pattern as backup/help; the settings panel anchors are static HTML the engine reaches only after the per-step tab activation (41-10)."
---

<objective>
Recompose the tour's anchor contract to the Ben-approved 41-STORYLINE.md v3 (settings-first, 12 steps) so the storyline's re-anchored steps have real targets: add data-tour="personalize"/"fields"/"snippets" on the three settings.html panels (the new steps 3-5), add data-tour="nav" on the .app-nav whole-menu element in renderNav (step 6), add data-tour="nav-sessions" on the renderNav Sessions link so the Sessions arrival step lights the tab that brought the user there (UAT gap 1/2 "arrive via the tab" fix), add data-tour="settings" on the header gear (UAT gap 6 — Settings was unreachable), move data-tour="session-heart" to the emotions accordion (deixis honesty), and retire the now-unused per-button (index.html) + section-title (sessions.html/reporting.html) anchors. There is NO data-tour="begin" and NO data-tour="nav-reporting" in v3 (the home begin beat is replaced by the whole-menu step 6; Reporting is mentioned in the finish copy, not visited). Update the anchor-presence rot guard to the new 12-anchor contract first (RED), then land the anchors (GREEN) — mirroring how 41-02 shipped this contract originally.

Purpose: these attribute changes are the structural spine the v3 storyline needs; without the three settings-panel anchors, the whole-menu nav anchor, nav-sessions, and settings there is no way to narrate the settings-first chapter, light the menu, or point at the gear. The chrome anchors (nav/nav-sessions/settings/backup/help) are always-present (D-02); the settings-panel anchors are reached after the engine's per-step tab activation (41-10).
Output: the new data-tour anchor set across index/add-session/sessions/reporting/settings shells + renderNav + the gear, and the rot guard that enforces the 12-anchor v3 contract.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- NEW anchors `data-tour="personalize"` on `#settingsTabPersonalize`, `data-tour="fields"` on `#settingsTabFields`, `data-tour="snippets"` on `#settingsTabSnippets` (settings.html panels).
- NEW anchors `data-tour="nav"` on the `.app-nav` element and `data-tour="nav-sessions"` on the Sessions link — both in the renderNav template in assets/app.js.
- NEW anchor `data-tour="settings"` on the `.settings-gear-btn` link in initSettingsLink (assets/app.js).
- MOVED anchor `data-tour="session-heart"` from the heart-shield accordion to the emotions accordion in add-session.html.
- REMOVED anchors: `data-tour="add-client"` / `data-tour="add-session"` (index.html buttons); `data-tour="sessions"` (sessions.html section title); `data-tour="reporting"` (reporting.html section title).
- CHANGED test tests/41-anchor-presence.test.js — ANCHORS[] table now the 12 v3 values in step order: overview (index.html); settings (assets/app.js); personalize, fields, snippets (settings.html); nav (assets/app.js); session-setup, session-heart, session-save (add-session.html); nav-sessions, backup, help (assets/app.js). The value-boundary match is made hyphen-safe so "nav" does not spuriously match "nav-sessions".
</artifacts_produced>

<!-- These data-tour literals appear in <action> prose because they are the exact
     attribute tokens the executor must add/remove/move; several are also the
     subject of negative greps (retired anchors, grep == 0). Sanctioned per the
     comment-text-discipline gate — the executor is instructed to leave NO such
     token behind as a comment in the edited files. -->
<!-- planner-discipline-allow: data-tour="add-client" -->
<!-- planner-discipline-allow: data-tour="add-session" -->
<!-- planner-discipline-allow: data-tour="sessions" -->
<!-- planner-discipline-allow: data-tour="reporting" -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-02-SUMMARY.md
@index.html
@add-session.html
@settings.html
@assets/app.js
@tests/41-anchor-presence.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the anchor-presence rot guard to the new 12-anchor v3 contract (RED)</name>
  <files>tests/41-anchor-presence.test.js</files>
  <read_first>
    - tests/41-anchor-presence.test.js (the current ANCHORS[] table lines ~48-59 + the header doc-comment lines ~13-21 that enumerate the old step→file mapping; the value-boundary match regex at line ~78 that currently ends in a bare `\b`; the dual-form prefix `data-tour['"=\s,)]+`)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 + §4 (the authoritative new 12-anchor set and which anchors are retired/moved/added)
  </read_first>
  <action>
    Replace the ANCHORS[] contract table with the NEW 12-anchor v3 set and its source files, in step order:
    overview → index.html; settings → assets/app.js; personalize → settings.html; fields → settings.html; snippets → settings.html; nav → assets/app.js; session-setup → add-session.html; session-heart → add-session.html; session-save → add-session.html; nav-sessions → assets/app.js; backup → assets/app.js; help → assets/app.js.
    Remove the add-client, add-session (index.html) and sessions, reporting (section-title) rows; do NOT add any nav-reporting row (Reporting is not a step in v3). Update the header doc-comment step→file mapping to match (step 1 index.html; step 2 settings gear in assets/app.js; steps 3-5 settings.html panels; step 6 nav in assets/app.js renderNav; steps 7-9 add-session.html; steps 10-12 assets/app.js chrome — nav-sessions/backup/help). Make the value-boundary hyphen-safe: change the trailing bare `\b` in the match regex to a negative lookahead that also excludes a hyphen (e.g. `(?![\w-])`) so the `nav` row cannot be satisfied by `data-tour="nav-sessions"`. Keep the dual-form match prefix (`data-tour['"=\s,)]+`) and the 30-fake-test-detector ALLOWLIST registration untouched. This will FAIL (RED) until Tasks 2+3 land the anchors — that is expected.
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js; echo "exit:$? (RED expected until Tasks 2+3)"</automated>
  </verify>
  <acceptance_criteria>
    - ANCHORS[] lists exactly these 12 values in this order: overview, settings, personalize, fields, snippets, nav, session-setup, session-heart, session-save, nav-sessions, backup, help.
    - `grep -c "add-client\|'add-session'\|value: 'sessions'\|value: 'reporting'\|nav-reporting" tests/41-anchor-presence.test.js` returns 0 (retired anchors + the dropped nav-reporting are absent from the contract).
    - personalize/fields/snippets map to sourceFile 'settings.html'; settings/nav/nav-sessions/backup/help map to 'assets/app.js'; overview maps to 'index.html'.
    - The value-boundary is hyphen-safe: the match regex no longer ends in a bare `\b` (grep shows `(?![\w-])` or an equivalent negative lookahead) so `nav` and `nav-sessions` are distinct rows.
    - Run before Tasks 2+3, the test exits non-zero and the FAIL lines name personalize / fields / snippets / nav / nav-sessions / settings (RED proves it guards the new contract).
  </acceptance_criteria>
  <done>The rot guard enforces the new 12-anchor v3 contract (hyphen-safe boundary) and fails RED until the anchors land.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose the HTML anchors (index / add-session / sessions / reporting / settings)</name>
  <files>index.html, add-session.html, sessions.html, reporting.html, settings.html</files>
  <read_first>
    - index.html (the greeting card line ~71 with data-tour="overview" — keep; #addClientBtn line ~103 with data-tour="add-client" and #addSessionBtn line ~107 with data-tour="add-session" — remove those two attributes)
    - add-session.html (the heart-shield accordion line ~205 with data-accordion="heart-shield" + data-tour="session-heart"; the emotions accordion line ~246 `class="accordion-section is-active" data-accordion="emotions"` — open by default)
    - sessions.html (line ~52 .section-title … data-tour="sessions"); reporting.html (line ~52 .section-title … data-tour="reporting")
    - settings.html (the three tab panels: #settingsTabPersonalize line ~125 — default-active, no `hidden`; #settingsTabFields line ~67 — has `hidden`; #settingsTabSnippets line ~170 — has `hidden`; each is a `role="tabpanel"` div)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §4 (delta table — the exact add/remove/move instructions)
  </read_first>
  <action>
    index.html: remove the data-tour="add-client" attribute from #addClientBtn (line ~103) and the data-tour="add-session" attribute from #addSessionBtn (line ~107) — remove ONLY those two attributes; leave each button's id + classes + labels + icons intact. Leave data-tour="overview" on the greeting card untouched. There is no home "begin" anchor in v3 — do not add a begin anchor. Do NOT leave any comment referencing the retired token names.
    add-session.html: remove the data-tour="session-heart" attribute from the heart-shield accordion (line ~205) and add data-tour="session-heart" to the emotions accordion div at line ~246 (the div with class "accordion-section is-active" and data-accordion="emotions"). Net effect: one attribute moves; the emotions accordion is open by default so it is visible → spotlight branch.
    settings.html: add data-tour="personalize" to the #settingsTabPersonalize panel div (line ~125), data-tour="fields" to the #settingsTabFields panel div (line ~67), and data-tour="snippets" to the #settingsTabSnippets panel div (line ~170). Add ONLY the one attribute to each existing panel div — leave id/role/class/aria-labelledby/hidden intact (the engine un-hides the Fields/Snippets panels by activating their tab before measuring — 41-10; the Personalize panel is default-active).
    sessions.html: remove the data-tour="sessions" attribute from the .section-title on line ~52 (keep data-i18n + the heading text). reporting.html: remove the data-tour="reporting" attribute from the .section-title on line ~52 (keep data-i18n + text). These section-title anchors are replaced by the renderNav nav-tab anchor in Task 3.
  </action>
  <verify>
    <automated>echo "addClient=$(grep -c 'data-tour=\"add-client\"' index.html) addSession=$(grep -c 'data-tour=\"add-session\"' index.html) heart=$(grep -c 'data-tour=\"session-heart\"' add-session.html) personalize=$(grep -c 'data-tour=\"personalize\"' settings.html) fields=$(grep -c 'data-tour=\"fields\"' settings.html) snippets=$(grep -c 'data-tour=\"snippets\"' settings.html) sess=$(grep -c 'data-tour=\"sessions\"' sessions.html) rep=$(grep -c 'data-tour=\"reporting\"' reporting.html)"</automated>
  </verify>
  <acceptance_criteria>
    - Retired per-button anchors gone: `grep -c 'data-tour="add-client"' index.html` = 0 and `grep -c 'data-tour="add-session"' index.html` = 0; and no home begin anchor: `grep -c 'data-tour="begin"' index.html` = 0.
    - The greeting card keeps its anchor (positive): `grep -c 'data-tour="overview"' index.html` = 1.
    - `grep -c 'data-tour="session-heart"' add-session.html` = 1 AND that single match is on the emotions accordion (the line also containing `data-accordion="emotions"`), not the heart-shield accordion.
    - `grep -c 'data-tour="personalize"' settings.html` = 1 (on #settingsTabPersonalize), `grep -c 'data-tour="fields"' settings.html` = 1 (on #settingsTabFields), `grep -c 'data-tour="snippets"' settings.html` = 1 (on #settingsTabSnippets).
    - Section-title anchors gone: `grep -c 'data-tour="sessions"' sessions.html` = 0; `grep -c 'data-tour="reporting"' reporting.html` = 0.
    - The #addClientBtn and #addSessionBtn retain their id/classes/labels/icons (grep confirms both ids still present in index.html); the three settings panels retain id/role/aria-labelledby/hidden (grep confirms #settingsTabFields still carries `hidden`).
    - No comment referencing "add-client" / "add-session" left behind in index.html (grep for those tokens in index.html returns 0 total).
  </acceptance_criteria>
  <done>The three settings panels carry personalize/fields/snippets anchors, the heart anchor sits on the emotions accordion, and the retired per-button + section-title anchors are gone from the page shells.</done>
</task>

<task type="auto">
  <name>Task 3: Add the renderNav whole-menu + Sessions-tab anchors and the settings-gear anchor (assets/app.js) → rot guard GREEN</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js renderNav (lines ~137-156 — the placeholder.innerHTML template; the `<nav class="app-nav">` opening tag at line ~141; the Sessions link `<a href="./sessions.html" data-nav="sessions" …>` at line ~143; the Reporting link at line ~144)
    - assets/app.js initSettingsLink (lines ~391-455 — the `link.className = 'header-control-btn settings-gear-btn'` at line ~399; the compile-time gear SVG; the App.installNavGuard call)
    - assets/app.js the existing tour-anchor pattern: initHelpEntry line ~487 `btn.setAttribute('data-tour', 'help')` and mountBackupCloudButton `btn.setAttribute('data-tour', 'backup')` — mirror this JS-anchor idiom for the gear
    - tests/41-anchor-presence.test.js (the updated 12-anchor contract from Task 1 — this task turns it GREEN)
  </read_first>
  <action>
    renderNav: in the placeholder.innerHTML template string, add the attribute data-tour="nav" to the `<nav class="app-nav">` opening tag (the whole-menu spotlight target for v3 step 6) and data-tour="nav-sessions" to the Sessions link (the `<a href="./sessions.html" data-nav="sessions" …>`). Do NOT add a nav-reporting anchor (Reporting is not a step in v3). Leave the Overview/Reporting/Add Client/Add Session links and the data-nav/data-i18n attributes untouched. These anchors render on every page (renderNav runs in initCommon), which is why the whole-menu step and the Sessions arrival step have targets everywhere.
    initSettingsLink: after the `link.className = 'header-control-btn settings-gear-btn'` assignment, add `link.setAttribute('data-tour', 'settings');` with a one-line comment matching the existing help/backup anchor comments (Phase 41 tour anchor — inert selector for the guided-tour engine). Do NOT alter the gear SVG, the navGuard wiring, the insertion order, or the language re-render listener.
    After both edits the anchor-presence rot guard (Task 1) must be GREEN: nav / nav-sessions resolve via the renderNav template form (data-tour="value"), settings resolves via the setAttribute form, and the settings.html panels + the moved heart anchor from Task 2 complete the 12-anchor contract.
  </action>
  <verify>
    <automated>node tests/41-anchor-presence.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Positive: `grep -c 'data-tour="nav"' assets/app.js` ≥ 1 (on the `.app-nav` element) and `grep -c 'data-tour="nav-sessions"' assets/app.js` ≥ 1 (on the Sessions renderNav link).
    - The dropped Reporting nav anchor is absent: `grep -c 'data-tour="nav-reporting"' assets/app.js` = 0.
    - `grep -c "setAttribute('data-tour', 'settings')" assets/app.js` = 1 (on the .settings-gear-btn link).
    - `node tests/41-anchor-presence.test.js` exits 0 (all 12 new-contract anchors resolve — rot guard GREEN; the hyphen-safe boundary means the `nav` row is satisfied by the .app-nav anchor, not by nav-sessions).
    - `node tests/run-all.js` passes with zero failures (the anchor-presence guard now runs 12 checks instead of 10; the section-title data-i18n and nav data-nav attributes are intact — no behavior regression).
    - The gear SVG, App.installNavGuard call, and initSettingsLink._listenerInstalled block are unchanged (grep confirms the navGuard call still present).
  </acceptance_criteria>
  <done>renderNav carries the whole-menu (nav) + Sessions-tab (nav-sessions) anchors and the header gear carries data-tour="settings"; the anchor-presence rot guard is GREEN on the full 12-anchor v3 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page shell / renderNav → tour engine | data-tour attributes are inert presentation selectors; they carry no user input and change no element behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-09a | Tampering | Silent anchor drop on a future refactor degrades the tour to fallback modals on a fresh app (violates D-02) | medium | mitigate | The anchor-presence rot guard (Task 1) is updated to the new 12-anchor v3 contract (hyphen-safe boundary) and fails the suite if any anchor is dropped. |
| T-41-01 | Information disclosure (XSS) | renderNav innerHTML template | low | accept | renderNav already assigns a compile-time literal template (no user interpolation); adding two static data-tour attributes introduces no interpolation. Unchanged posture. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-anchor-presence.test.js` → exits 0 on the new 12-anchor v3 contract (RED after Task 1, GREEN after Tasks 2+3).
- `node tests/run-all.js` → full suite green with zero failures (the rot guard now runs 12 checks instead of 10), no regression from the attribute moves.
- Grep gates in each task's acceptance_criteria pass.
</verification>

<success_criteria>
The v3 storyline's anchor set is live: personalize/fields/snippets on the three settings panels, nav on the whole menu and nav-sessions on the Sessions tab (renderNav), settings on the header gear, session-heart on the emotions accordion, and the retired per-button + section-title anchors removed (no begin, no nav-reporting) — all enforced by the updated 12-anchor rot guard on a fresh empty app.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-09-SUMMARY.md` when done.
</output>