---
phase: 41-replayable-guided-tour
plan: 10
type: execute
wave: 7
gap_closure: true
depends_on: ["41-08", "41-09"]
files_modified:
  - assets/tour.js
  - tests/41-resume-state.test.js
autonomous: true
requirements: [TOUR-01, TOUR-03]
must_haves:
  truths:
    - "The engine walks the EXACT 10-step storyline spine in order: overview → begin → session-setup → session-heart → session-save → nav-sessions → nav-reporting → backup → settings → help (D-01 re-approved 2026-07-08)."
    - "The spotlight travels forward only; the two old adjacent home-page button steps are merged into the single 'begin' beat (UAT gaps 3, 7)."
    - "The save-step tooltip shows the export glyph inline so 'this is its icon' is literally true — honest deixis (UAT gap 5)."
    - "Launching the tour from a non-Overview page navigates home to index.html and resumes at step 1's spotlight — never a fallback modal (approved decision 4; already provided by A2 page-aware start(), preserved here)."
    - "D-06 auto-navigation, TOUR-02 degradation, and TOUR-03 cross-page resume are preserved through the recomposition."
  artifacts:
    - "assets/tour.js STEPS[] recomposed to the 10 storyline entries (ids/anchors/i18nKeys/screenName/takeMeThereHref)"
    - "assets/tour.js save-step export glyph — a compile-time-literal inline SVG appended after the body, gated to step.id === 'session-save'"
    - "tests/41-resume-state.test.js updated to the new index→add-session page boundary"
  key_links:
    - "STEPS[].anchor binds to the 41-09 data-tour set; STEPS[].i18nKey binds to the 41-11 help.tour.step.* keys — all three must agree for a coherent run."
    - "The export glyph is the one sanctioned innerHTML in tour.js: a compile-time literal, zero interpolation, same trust-boundary rule as the app.js '?' glyph (T-41-01)."
---

<!-- These anchor selector literals appear in <action>/<acceptance_criteria> prose
     because they are the exact STEPS[].anchor tokens being removed; they are also
     negative-grepped (grep == 0) to prove the old composition is gone. Sanctioned
     per the comment-text-discipline gate; the executor leaves no such token behind
     as a stray comment in tour.js. -->
<!-- planner-discipline-allow: [data-tour="add-client"] -->
<!-- planner-discipline-allow: [data-tour="add-session"] -->
<!-- planner-discipline-allow: [data-tour="sessions"] -->
<!-- planner-discipline-allow: [data-tour="reporting"] -->

<objective>
Recompose the engine's STEPS[] array to the exact Ben-approved 41-STORYLINE.md §3 spine and make the save step's deixis honest by inlining the export glyph in its tooltip. This is the composition half of the storyline fix (the copy half is 41-11, the anchors are 41-09): the old route (overview, add-client, add-session, session-setup, heart, save, sessions, reporting, backup, help) becomes the storyline route (overview, begin, session-setup, session-heart, session-save, nav-sessions, nav-reporting, backup, settings, help) — merging the two home-page button steps into one 'begin' beat, re-anchoring the arrival steps to the nav tabs, and adding the Settings step (D-01 re-approved). Count stays 10, so D-09 relaunch-restart, D-06 auto-navigation, and TOUR-02 degradation are all preserved. The launch-from-non-Overview behavior (approved decision 4) is already delivered by the A2 page-aware start() shipped in 41-03 — this plan preserves and explicitly re-verifies it rather than re-implementing.

Purpose: without the recomposed STEPS[] the storyline copy (41-11) and anchors (41-09) have nothing to drive; this is the spine that makes "the path a session travels" walkable end to end.
Output: the storyline STEPS[] + the save-step export glyph + the realigned resume-state behavior test.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- CHANGED `STEPS` array in assets/tour.js — 10 entries in storyline order with these fields per entry (id | page | anchor | i18nKey | screenName | takeMeThereHref):
  1. overview | index.html | overview | help.tour.step.overview | Overview | ./index.html
  2. begin | index.html | begin | help.tour.step.begin | Overview | ./index.html
  3. session-setup | add-session.html | session-setup | help.tour.step.setup | Session | ./add-session.html
  4. session-heart | add-session.html | session-heart | help.tour.step.heart | Session | ./add-session.html
  5. session-save | add-session.html | session-save | help.tour.step.save | Session | ./add-session.html
  6. nav-sessions | sessions.html | nav-sessions | help.tour.step.sessions | Sessions | ./sessions.html
  7. nav-reporting | reporting.html | nav-reporting | help.tour.step.reporting | Reporting | ./reporting.html
  8. backup | reporting.html | backup | help.tour.step.backup | Reporting | ./reporting.html
  9. settings | reporting.html | settings | help.tour.step.settings | Reporting | ./reporting.html
  10. help | reporting.html | help | help.tour.step.help | Reporting | ./reporting.html
- NEW behavior: the save-step tooltip appends a compile-time-literal export/download inline SVG glyph after the body (gated to step.id === 'session-save').
- CHANGED module banner comment in assets/tour.js — documents the single sanctioned inline-SVG export glyph (replaces the current "No inline SVG is used in this plan" line).
- CHANGED tests/41-resume-state.test.js — cross-page boundary + step indices realigned to the new STEPS[].
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-03-SUMMARY.md
@assets/tour.js
@tests/41-resume-state.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose STEPS[] to the storyline spine (assets/tour.js)</name>
  <files>assets/tour.js</files>
  <read_first>
    - assets/tour.js (the STEPS array lines ~95-106; the render()/renderSpotlight()/renderFallback() consumers; start()/resume()/next()/prev() which read STEPS[].page + takeMeThereHref; the module banner lines ~1-19)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the 10-step table — authoritative id/anchor/page order) + §5 (deltas)
    - .planning/phases/41-replayable-guided-tour/41-09-PLAN.md (the data-tour anchor set this STEPS[] binds to)
  </read_first>
  <action>
    Replace the 10 STEPS[] entries with the storyline spine exactly as listed in this plan's <artifacts_produced> (id/page/anchor/i18nKey/screenName/takeMeThereHref). Concretely: rename entry 2 from add-client to begin (anchor [data-tour="begin"], i18nKey help.tour.step.begin) and DELETE the former entry 3 (add-session) so the two home-page button steps become one 'begin' beat; keep session-setup/session-heart/session-save (add-session.html) — note session-heart now lights the emotions accordion via the 41-09 anchor move, no STEPS change needed for that; rename the sessions entry's anchor to [data-tour="nav-sessions"] and the reporting entry's anchor to [data-tour="nav-reporting"] (ids nav-sessions / nav-reporting, i18nKeys help.tour.step.sessions / help.tour.step.reporting unchanged); keep backup and help; INSERT a new entry between backup and help: id settings, page reporting.html, anchor [data-tour="settings"], i18nKey help.tour.step.settings, screenName Reporting, takeMeThereHref ./reporting.html. Result: exactly 10 entries in the storyline order. Leave the render loop, degradation branch, start()/resume()/next()/prev(), and the sg.tourResume tier untouched — the array is the only edit. Do NOT reference the removed add-client/add-session/sessions/reporting anchor selectors anywhere in STEPS[] and leave no stray comment naming them.
  </action>
  <verify>
    <automated>node tests/41-launch-explicit.test.js && node tests/41-fallback-degradation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'begin'" assets/tour.js` = 1 and `grep -c "id: 'settings'" assets/tour.js` = 1 (the merged beat + the new Settings step exist).
    - `grep -c "id: 'nav-sessions'" assets/tour.js` = 1 and `grep -c "id: 'nav-reporting'" assets/tour.js` = 1.
    - The STEPS array has exactly 10 entries: `node tests/41-launch-explicit.test.js` exits 0 (its "STEPS must have 10 entries" assertion holds) — merge of two + insert of one nets 10.
    - `grep -c 'data-tour=\"add-client\"\|data-tour=\"add-session\"' assets/tour.js` = 0 (old per-button anchors gone from STEPS[]); the old section-title anchor selectors for sessions/reporting are replaced by the nav-* selectors.
    - `node tests/41-fallback-degradation.test.js` exits 0 (its stepIndex-4 Take-me-there test reads steps[4].takeMeThereHref dynamically — session-save/./add-session.html — so it stays green with no edit).
    - start()/resume()/next()/prev() and the sg.tourResume tier are byte-unchanged (grep confirms RESUME_KEY = 'sg.tourResume' and the page-crossing next() persist-then-navigate block are intact).
  </acceptance_criteria>
  <done>STEPS[] walks the storyline spine (10 entries, begin merge + settings insert + nav-tab re-anchors); launch-explicit + fallback-degradation stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the export glyph in the save-step tooltip (assets/tour.js)</name>
  <files>assets/tour.js</files>
  <read_first>
    - assets/tour.js (renderSpotlight lines ~272-314 — where the title/body/row are appended to tooltipEl; the makeEl helper; the module banner "TRUST BOUNDARY" note that currently states no inline SVG is used)
    - assets/app.js (the "?" glyph compile-time-literal SVG in initHelpEntry lines ~493-500 — the sanctioned innerHTML pattern to mirror: stroke=currentColor, viewBox 0 0 24 24, zero interpolation)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 step 5 + §5 "Step 'save' DE-FALSIFIED" (the glyph ships inside the tooltip, after the body; compile-time literal, same trust-boundary rule as the "?" glyph)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Icon library row: the export glyph reuses the existing colored export icon, named + shown in the tooltip, not re-drawn)
  </read_first>
  <action>
    In renderSpotlight, AFTER the body paragraph is appended and ONLY when step.id === 'session-save', append a small glyph element (e.g. a span with class 'sg-tour-glyph', aria-hidden="true") whose innerHTML is a compile-time-literal inline SVG of the export/download icon — reuse the app's existing export/download glyph markup (stroke=currentColor, viewBox 0 0 24 24, ~18px), authored exactly like the app.js "?" glyph literal: a single concatenated string constant with ZERO interpolation of any variable. This is the one sanctioned innerHTML in tour.js; it satisfies T-41-01 because there is no dynamic data in the string. Update the module banner "TRUST BOUNDARY" comment: replace the line stating no inline SVG is used with a note that the save step renders ONE compile-time-literal export glyph via innerHTML (zero interpolation) and all other copy remains textContent. Do not add the glyph to any other step. Keep the glyph inside the tour root so tour.css tokens apply; do not introduce a literal hex color (currentColor inherits the tooltip text color).
  </action>
  <verify>
    <automated>node tests/41-lang-rerender.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session-save" assets/tour.js` shows the glyph is gated on step.id === 'session-save' (the glyph append is inside an `id === 'session-save'` guard, verified by reading renderSpotlight).
    - `grep -c "innerHTML" assets/tour.js` = 1 (exactly one innerHTML assignment — the export glyph) and it is a static string literal (no `+ variable` / template interpolation on that assignment).
    - The module banner no longer claims "No inline SVG is used" (grep for that phrase returns 0) and documents the single compile-time-literal glyph.
    - No literal hex color introduced in tour.js (the SVG uses stroke="currentColor").
    - `node tests/run-all.js` stays green (152 passed) — the glyph is DOM-only and does not affect the text-level lang-rerender test.
  </acceptance_criteria>
  <done>The save-step tooltip shows a compile-time-literal export glyph after the body; the trust-boundary comment is corrected; the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Realign the resume-state behavior test to the new page boundary</name>
  <files>tests/41-resume-state.test.js</files>
  <read_first>
    - tests/41-resume-state.test.js (sub-tests A-D — A drives a cross-page next() from index.html; B lands on add-session.html with a resume key; each hardcodes a stepIndex + an anchor in the injected body + a page)
    - assets/tour.js (the recomposed STEPS[] from Task 1 — the new index→add-session boundary is between step index 1 (begin, index.html) and step index 2 (session-setup, add-session.html))
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (step order for index mapping)
  </read_first>
  <action>
    Update every hardcoded stepIndex / injected-anchor / page reference so the cross-page assertions match the NEW STEPS[]. Concretely: the index→add-session page crossing is now step index 1 (begin, index.html) → step index 2 (session-setup, add-session.html). For sub-test A (cross-page next() persists resume + navigates), set the current page to index.html at step index 1, and assert the persisted resume stepIndex is 2 and the navigation targets ./add-session.html. For sub-test B (resume continues on the matching page), land on add-session.html with a resume key stepIndex 2 and inject a body containing the session-setup anchor, asserting resume() continues at index 2. Leave sub-test C (absent key → step 1, D-09) and sub-test D (endTour clears the resume key) semantics intact — only adjust any anchor/page literal they inject so it matches a real step on that page. Update the sub-test comments to name the new steps (begin / session-setup) instead of add-session.
  </action>
  <verify>
    <automated>node tests/41-resume-state.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/41-resume-state.test.js` exits 0 against the recomposed STEPS[].
    - Sub-test A asserts a cross-page next() from index.html persists sg.tourResume with stepIndex 2 and navigates to ./add-session.html (the new boundary).
    - Sub-test B lands on add-session.html with a resume stepIndex 2 (session-setup) and resume() continues there.
    - The old references to "add-session anchor" / stepIndex 3 as the index→add-session boundary are gone (grep for the old boundary comment returns 0).
    - `node tests/run-all.js` green (152 passed).
  </acceptance_criteria>
  <done>The resume-state test asserts cross-page resume against the new begin→session-setup boundary; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Step copy is textContent (T-41-01). The save-step export glyph is the ONE innerHTML — a compile-time literal with zero interpolation. |
| STEPS[] → navigation | takeMeThereHref values are static in-repo relative paths; next()/start() assign them to location.href (no user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Information disclosure (XSS) | save-step export glyph innerHTML | low | mitigate | The glyph is a single compile-time string literal with zero variable interpolation, mirroring the sanctioned app.js "?" glyph pattern; all other tour copy stays textContent. Verified by grep (exactly one static innerHTML). |
| T-41-10a | Tampering | Silent step drop / mis-order breaks the taught path | medium | mitigate | launch-explicit (length 10) + resume-state (cross-page boundary) + the 41-13 human-verify replay guard the composition. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-launch-explicit.test.js`, `node tests/41-fallback-degradation.test.js`, `node tests/41-resume-state.test.js`, `node tests/41-lang-rerender.test.js` → all exit 0 against the recomposed spine.
- `node tests/run-all.js` → full suite green (152 passed).
- Grep gates in each task's acceptance_criteria pass; exactly one static innerHTML in tour.js.
</verification>

<success_criteria>
The engine walks the exact storyline spine (overview → begin → session-setup → session-heart → session-save → nav-sessions → nav-reporting → backup → settings → help), the save-step tooltip carries the export glyph so its deixis is honest, launching from a non-Overview page still opens at home step 1, and cross-page resume is proven against the new boundary.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-10-SUMMARY.md` when done.
</output>