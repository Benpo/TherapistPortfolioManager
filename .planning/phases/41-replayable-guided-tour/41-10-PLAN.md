---
phase: 41-replayable-guided-tour
plan: 10
type: execute
wave: 7
gap_closure: true
depends_on: ["41-08", "41-09"]
files_modified:
  - assets/tour.js
  - tests/41-launch-explicit.test.js
  - tests/41-resume-state.test.js
  - tests/41-tab-activation.test.js
autonomous: true
requirements: [TOUR-01, TOUR-03]
must_haves:
  truths:
    - "The engine walks the EXACT 12-step v3 storyline spine in order: overview → settings → personalize → fields → snippets → nav → session-setup → session-heart → session-save → nav-sessions → backup → help (settings-first, approved 2026-07-09; D-01's count superseded)."
    - "The engine ACTIVATES a step's settings tab before measuring its anchor: steps 3-5 declare an activation selector (#settingsTabPersonalizeBtn / #settingsTabFieldsBtn / #settingsTabSnippetsBtn) the engine clicks on entry so the hidden Fields/Snippets panels are on screen (honest deixis); degradation is unchanged — anchor still missing/hidden after activation → centered fallback modal. Step 3's activation is deliberately DEFENSIVE beyond STORYLINE v3 §4 (which needs it only for steps 4-5): personalize is default-active on a fresh page load, but a resume/replay reaching step 3 while another Settings tab is active would otherwise find the panel hidden and degrade to the fallback modal instead of the intended spotlight; the click is idempotent when the tab is already active."
    - "The spotlight travels forward only across four pages: index → settings → add-session → sessions (3 navigations, all D-06 auto-navigated)."
    - "The save-step tooltip shows the export glyph inline so 'this is its icon' is literally true — honest deixis (UAT gap 5)."
    - "Launching the tour from a non-Overview page navigates home to index.html and resumes at step 1's spotlight — never a fallback modal (already provided by the A2 page-aware start(), preserved here)."
    - "D-06 auto-navigation, TOUR-02 degradation, and TOUR-03 cross-page resume (now covering settings.html) are preserved through the recomposition."
  artifacts:
    - "assets/tour.js STEPS[] recomposed to the 12 storyline entries (id/page/anchor/i18nKey/screenName/takeMeThereHref + an activate selector on the three settings-panel steps)"
    - "assets/tour.js render() gains a null-safe per-step tab activation (clicks step.activate before querying the anchor)"
    - "assets/tour.js save-step export glyph — a compile-time-literal inline SVG appended after the body, gated to step.id === 'session-save'"
    - "tests/41-launch-explicit.test.js STEPS length 10 → 12; tests/41-resume-state.test.js realigned to the new index→settings boundary; tests/41-tab-activation.test.js NEW behavior test (activation fires; fallback survives a missing anchor)"
  key_links:
    - "STEPS[].anchor binds to the 41-09 data-tour set; STEPS[].i18nKey binds to the 41-11 help.tour.step.* keys (suffixes overview/settings/personalize/fields/snippets/ready/setup/heart/save/sessions/backup/help) — all three must agree for a coherent run."
    - "STEPS[].activate binds to the settings.html tab-button ids (#settingsTab*Btn); the click un-hides the panel the anchor lives on before the engine measures it."
    - "The export glyph is the one sanctioned innerHTML in tour.js: a compile-time literal, zero interpolation, same trust-boundary rule as the app.js '?' glyph (T-41-01)."
---

<!-- These anchor selector literals appear in <action>/<acceptance_criteria> prose
     because they are the exact STEPS[].anchor tokens being removed; they are also
     negative-grepped (grep == 0) to prove the old composition is gone. Sanctioned
     per the comment-text-discipline gate; the executor leaves no such token behind
     as a stray comment in tour.js. -->
<!-- planner-discipline-allow: [data-tour="add-client"] -->
<!-- planner-discipline-allow: [data-tour="add-session"] -->
<!-- planner-discipline-allow: [data-tour="sessions"] -->
<!-- planner-discipline-allow: [data-tour="reporting"] -->

<objective>
Recompose the engine's STEPS[] array to the exact Ben-approved 41-STORYLINE.md v3 §3 spine (settings-first, 12 steps), add the per-step tab-activation capability the settings walk-in needs, and make the save step's deixis honest by inlining the export glyph in its tooltip. This is the composition half of the storyline fix (the copy half is 41-11, the anchors are 41-09): the old route (overview, add-client, add-session, session-setup, heart, save, sessions, reporting, backup, help) becomes the v3 route (overview, settings, personalize, fields, snippets, nav, session-setup, session-heart, session-save, nav-sessions, backup, help) — a Settings chapter moved to the FRONT (steps 2-6), the two home-page button steps dropped, the Sessions arrival re-anchored to the nav tab, and the Reporting step removed (mentioned in the finish copy instead). Count goes 10 → 12 (Ben released the 10-step cap 2026-07-09; D-01's count is superseded). D-09 relaunch-restart, D-06 auto-navigation, and TOUR-02 degradation are all preserved. The launch-from-non-Overview behavior is already delivered by the A2 page-aware start() shipped in 41-03 — this plan preserves and explicitly re-verifies it. The NEW engine capability: a step may declare an activation selector the engine clicks on entry (read-only view switch) BEFORE measuring the anchor, so the Fields/Snippets panels (hidden by default) are truly on screen when their step speaks (honest deixis, §2).

Purpose: without the recomposed STEPS[] + tab activation the storyline copy (41-11) and anchors (41-09) have nothing to drive; this is the spine that makes "first make it yours, then walk the path a session travels" walkable end to end.
Output: the 12-step v3 STEPS[] + the per-step tab activation + the save-step export glyph + the realigned launch/resume/activation behavior tests.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- CHANGED `STEPS` array in assets/tour.js — 12 entries in v3 storyline order (id | page | anchor | i18nKey | screenName | takeMeThereHref | activate?):
  1.  overview      | index.html       | [data-tour="overview"]      | help.tour.step.overview    | Overview | ./index.html       | —
  2.  settings      | index.html       | [data-tour="settings"]      | help.tour.step.settings    | Overview | ./index.html       | —
  3.  personalize   | settings.html    | [data-tour="personalize"]   | help.tour.step.personalize | Settings | ./settings.html    | #settingsTabPersonalizeBtn
  4.  fields        | settings.html    | [data-tour="fields"]        | help.tour.step.fields      | Settings | ./settings.html    | #settingsTabFieldsBtn
  5.  snippets      | settings.html    | [data-tour="snippets"]      | help.tour.step.snippets    | Settings | ./settings.html    | #settingsTabSnippetsBtn
  6.  nav           | settings.html    | [data-tour="nav"]           | help.tour.step.ready       | Settings | ./settings.html    | —
  7.  session-setup | add-session.html | [data-tour="session-setup"] | help.tour.step.setup       | Session  | ./add-session.html | —
  8.  session-heart | add-session.html | [data-tour="session-heart"] | help.tour.step.heart       | Session  | ./add-session.html | —
  9.  session-save  | add-session.html | [data-tour="session-save"]  | help.tour.step.save        | Session  | ./add-session.html | —
  10. nav-sessions  | sessions.html    | [data-tour="nav-sessions"]  | help.tour.step.sessions    | Sessions | ./sessions.html    | —
  11. backup        | sessions.html    | [data-tour="backup"]        | help.tour.step.backup      | Sessions | ./sessions.html    | —
  12. help          | sessions.html    | [data-tour="help"]          | help.tour.step.help        | Sessions | ./sessions.html    | —
  (steps 3-5 carry an `activate` selector — the settings tab button the engine clicks on step entry; all other steps omit the field.)
- NEW behavior in render(): a null-safe per-step tab activation — before querying step.anchor, if step.activate is set, `document.querySelector(step.activate)` and (if found) `.click()` it, so the target panel is un-hidden before measurement. Missing button → guarded no-op → the existing degradation branch still applies.
- NEW behavior: the save-step tooltip appends a compile-time-literal export/download inline SVG glyph after the body (gated to step.id === 'session-save').
- CHANGED module banner comment in assets/tour.js — documents the 12-step spine across index/settings/add-session/sessions, the per-step tab-activation capability, and the single sanctioned inline-SVG export glyph (replaces the "10-step" and "No inline SVG is used" lines).
- CHANGED tests/41-launch-explicit.test.js — the STEPS length assertion 10 → 12.
- CHANGED tests/41-resume-state.test.js — cross-page boundary + step indices realigned to the new index→settings boundary.
- NEW tests/41-tab-activation.test.js — behavior test proving the activation click fires before measurement AND the fallback still renders when the anchor is absent after activation.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-03-SUMMARY.md
@settings.html
@assets/tour.js
@tests/41-launch-explicit.test.js
@tests/41-resume-state.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose STEPS[] to the 12-step v3 spine + add null-safe per-step tab activation (assets/tour.js)</name>
  <files>assets/tour.js, tests/41-launch-explicit.test.js</files>
  <read_first>
    - assets/tour.js (the STEPS array lines ~95-106; render() lines ~240-270 — `var el = document.querySelector(step.anchor)` at line ~259 is where the anchor is measured; start()/resume()/next()/prev() which read STEPS[].page + takeMeThereHref; the module banner lines ~1-64 that say "10 steps" and "No inline SVG is used")
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the 12-step table — authoritative id/anchor/page order) + §4-§5 (deltas + the per-step tab-activation capability)
    - .planning/phases/41-replayable-guided-tour/41-09-PLAN.md (the data-tour anchor set this STEPS[] binds to)
    - settings.html (the tab-button ids the activation clicks: #settingsTabPersonalizeBtn, #settingsTabFieldsBtn, #settingsTabSnippetsBtn — the buttons that un-hide the #settingsTabFields / #settingsTabSnippets panels)
    - tests/41-launch-explicit.test.js (line ~90 `assert.strictEqual(steps.length, 10, ...)` — the length assertion to bump to 12)
  </read_first>
  <action>
    Replace the STEPS[] entries with the 12-step v3 spine exactly as listed in this plan's <artifacts_produced> (id/page/anchor/i18nKey/screenName/takeMeThereHref, plus an `activate` selector on steps 3-5). Concretely: keep entry 1 (overview); replace the former add-client entry with the gear step (id settings, anchor [data-tour="settings"], page index.html, i18nKey help.tour.step.settings); DELETE the former add-session entry; INSERT the settings chapter after the gear step — personalize (anchor [data-tour="personalize"], page settings.html, i18nKey help.tour.step.personalize, activate '#settingsTabPersonalizeBtn'), fields ([data-tour="fields"], activate '#settingsTabFieldsBtn'), snippets ([data-tour="snippets"], activate '#settingsTabSnippetsBtn'), then nav (id nav, [data-tour="nav"], settings.html, i18nKey help.tour.step.ready); keep session-setup/session-heart/session-save on add-session.html (session-heart now lights the emotions accordion via the 41-09 anchor move — no STEPS change for that); rename the sessions entry's anchor to [data-tour="nav-sessions"] (id nav-sessions, i18nKey help.tour.step.sessions, page sessions.html); DELETE the reporting entry (no Reporting step in v3); set the backup + help pages to sessions.html (they are dynamic chrome present on every page) with takeMeThereHref ./sessions.html. Result: exactly 12 entries in the v3 order.
    In render(), add a null-safe per-step tab activation: AFTER `active = true` and BEFORE `var el = document.querySelector(step.anchor)`, if `step.activate` is set do `var tab = document.querySelector(step.activate); if (tab && typeof tab.click === 'function') tab.click();` — this un-hides the settings panel the anchor lives on before the engine measures it. A missing button is a guarded no-op, so the degradation branch (anchor missing/hidden → fallback modal) is unchanged. Leave the render loop, degradation branch, renderSpotlight/renderFallback, start()/resume()/next()/prev(), and the sg.tourResume tier otherwise untouched. Update the module banner: it now walks 12 steps across index/settings/add-session/sessions and a step may declare an activation selector clicked on entry. Do NOT reference the removed add-client/add-session/sessions(section-title)/reporting anchor selectors anywhere in STEPS[] and leave no stray comment naming them.
    tests/41-launch-explicit.test.js: bump the STEPS length assertion from 10 to 12 (line ~90). Leave the other assertions (dynamic on steps[0].page) unchanged.
  </action>
  <verify>
    <automated>node tests/41-launch-explicit.test.js && node tests/41-fallback-degradation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'settings'" assets/tour.js` = 1, `grep -c "id: 'personalize'" assets/tour.js` = 1, `grep -c "id: 'fields'" assets/tour.js` = 1, `grep -c "id: 'snippets'" assets/tour.js` = 1, `grep -c "id: 'nav'" assets/tour.js` = 1, `grep -c "id: 'nav-sessions'" assets/tour.js` = 1 (the new settings chapter + the re-anchored arrival exist).
    - The STEPS array has exactly 12 entries: `node tests/41-launch-explicit.test.js` exits 0 with its length assertion now reading 12.
    - Positive: `grep -c "help.tour.step.ready" assets/tour.js` = 1 (the nav step's i18nKey is present).
    - The Reporting step is gone: `grep -c "help.tour.step.reporting" assets/tour.js` = 0.
    - Old per-button + section-title anchor selectors gone from STEPS[]: `grep -c 'data-tour=\"add-client\"\|data-tour=\"add-session\"\|data-tour=\"reporting\"' assets/tour.js` = 0 and `grep -c 'data-tour=\"sessions\"' assets/tour.js` = 0 (step 10 uses the nav-sessions selector instead).
    - The three settings steps carry an activate selector: `grep -c "activate:" assets/tour.js` ≥ 3 and `grep -c "settingsTabFieldsBtn" assets/tour.js` = 1; render() clicks step.activate before querying step.anchor (verified by reading render()).
    - `node tests/41-fallback-degradation.test.js` exits 0 (its stepIndex-4 Take-me-there test reads steps[4].takeMeThereHref dynamically — now snippets/./settings.html — and the activation click is a guarded no-op when the tab button is absent, so the fallback still renders).
    - start()/resume()/next()/prev() and the sg.tourResume tier are unchanged (grep confirms RESUME_KEY = 'sg.tourResume' and the page-crossing next() persist-then-navigate block are intact).
  </acceptance_criteria>
  <done>STEPS[] walks the 12-step v3 spine (settings-first: gear→personalize→fields→snippets→nav chapter, nav-tab arrival, no Reporting step) and render() activates the settings tab before measuring; launch-explicit (length 12) + fallback-degradation stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the export glyph in the save-step tooltip (assets/tour.js)</name>
  <files>assets/tour.js</files>
  <read_first>
    - assets/tour.js (renderSpotlight lines ~272-314 — where the title/body/row are appended to tooltipEl; the makeEl helper; the module banner "TRUST BOUNDARY" note that currently states no inline SVG is used)
    - assets/app.js (the "?" glyph compile-time-literal SVG in initHelpEntry lines ~493-500 — the sanctioned innerHTML pattern to mirror: stroke=currentColor, viewBox 0 0 24 24, zero interpolation)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 step 9 (session-save) + §4 KEPT row "save-step de-falsification + export glyph in tooltip" (the glyph ships inside the tooltip, after the body; compile-time literal, same trust-boundary rule as the "?" glyph)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Icon library row: the export glyph reuses the existing colored export icon, named + shown in the tooltip, not re-drawn)
  </read_first>
  <action>
    In renderSpotlight, AFTER the body paragraph is appended and ONLY when step.id === 'session-save', append a small glyph element (e.g. a span with class 'sg-tour-glyph', aria-hidden="true") whose innerHTML is a compile-time-literal inline SVG of the export/download icon — reuse the app's existing export/download glyph markup (stroke=currentColor, viewBox 0 0 24 24, ~18px), authored exactly like the app.js "?" glyph literal: a single concatenated string constant with ZERO interpolation of any variable. This is the one sanctioned innerHTML in tour.js; it satisfies T-41-01 because there is no dynamic data in the string. Update the module banner "TRUST BOUNDARY" comment: replace the line stating no inline SVG is used with a note that the save step renders ONE compile-time-literal export glyph via innerHTML (zero interpolation) and all other copy remains textContent. Do not add the glyph to any other step. Keep the glyph inside the tour root so tour.css tokens apply; do not introduce a literal hex color (currentColor inherits the tooltip text color).
  </action>
  <verify>
    <automated>node tests/41-lang-rerender.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session-save" assets/tour.js` shows the glyph is gated on step.id === 'session-save' (the glyph append is inside an `id === 'session-save'` guard, verified by reading renderSpotlight).
    - `grep -c "innerHTML" assets/tour.js` = 1 (exactly one innerHTML assignment — the export glyph) and it is a static string literal (no `+ variable` / template interpolation on that assignment).
    - The module banner no longer claims "No inline SVG is used" (grep for that phrase returns 0) and documents the single compile-time-literal glyph.
    - No literal hex color introduced in tour.js (the SVG uses stroke="currentColor").
    - `node tests/run-all.js` stays green with zero failures — the glyph is DOM-only and does not affect the text-level lang-rerender test (which drives step 0/overview only).
  </acceptance_criteria>
  <done>The save-step tooltip shows a compile-time-literal export glyph after the body; the trust-boundary comment is corrected; the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Realign the resume-state test to the index→settings boundary + add the tab-activation behavior test</name>
  <files>tests/41-resume-state.test.js, tests/41-tab-activation.test.js</files>
  <read_first>
    - tests/41-resume-state.test.js (sub-tests A-D — A drives a cross-page next() from index.html and asserts `steps[2].page !== steps[3].page`; B lands on add-session.html with a resume key + injected session-setup anchor; C absent key → step 1; D endTour clears the key)
    - assets/tour.js (the recomposed STEPS[] from Task 1 — the FIRST cross-page boundary is now step index 1 (settings, index.html) → step index 2 (personalize, settings.html); the render() tab-activation added in Task 1)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (step order / page mapping: steps 0-1 index.html, steps 2-5 settings.html, steps 6-8 add-session.html, steps 9-11 sessions.html)
    - tests/41-fallback-degradation.test.js + tests/41-launch-explicit.test.js (the jsdom buildEnv idiom + the _isAnchorVisible / _navigate / _setStepIndex / _render seams to reuse in the new activation test)
  </read_first>
  <action>
    tests/41-resume-state.test.js — realign the cross-page assertions to the NEW STEPS[]. The former index→session-form crossing at step index 2→3 no longer crosses a page (steps 2-5 are all settings.html); the first cross-page boundary is now step index 1 (settings, index.html) → step index 2 (personalize, settings.html). For sub-test A: set the current page to index.html at step index 1, call next(), assert the persisted resume stepIndex is 2 and the navigation targets ./settings.html; replace the `steps[2].page !== steps[3].page` fixture assumption with `steps[1].page !== steps[2].page`. For sub-test B: land on settings.html with a resume key stepIndex 2 and inject a body containing the personalize anchor (`<div data-tour="personalize">…</div>`), force _isAnchorVisible → true, and assert resume() continues at index 2 with chrome mounted (the personalize step's activate selector is absent in the fixture → guarded no-op, so the forced-visible anchor still renders the spotlight). Leave sub-tests C (absent key → step 1, D-09) and D (endTour clears the key) intact — step 0 is still overview. Update the sub-test comments to name the new steps (settings / personalize).
    tests/41-tab-activation.test.js — NEW behavior test (reuse the buildEnv jsdom idiom from tests/41-fallback-degradation.test.js: JSDOM, real i18n-en + tour.js, the _isAnchorVisible / _setStepIndex / _render seams, an EXPECTED_COUNT scenario-count guard). Test 1 (activation fires before measurement): on settings.html, inject BOTH a fake tab button `<button id="settingsTabFieldsBtn"></button>` with a click listener that increments a counter AND a `<div data-tour="fields">…</div>` panel; drive the engine to the fields step (`_setStepIndex(3)` then `_render()`), force _isAnchorVisible → true, and assert the tab button's click fired (counter === 1) AND the spotlight mounted (`.sg-tour-spotlight` present) — proving activation ran before the anchor was measured. Test 2 (fallback survives a missing anchor after activation): on settings.html inject the `#settingsTabFieldsBtn` button but NO `[data-tour="fields"]` panel, force _isAnchorVisible → false, `_setStepIndex(3)` then `_render()`, and assert a centered fallback modal (`.sg-tour-modal-scrim`) renders (no silent skip) and `_getStepIndex()` is still 3 — degradation is unchanged by activation. Print PASS/FAIL per scenario; exit non-zero on any failure. It lives at tests/41-tab-activation.test.js so tests/run-all.js globs it.
  </action>
  <verify>
    <automated>node tests/41-resume-state.test.js && node tests/41-tab-activation.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/41-resume-state.test.js` exits 0 against the recomposed STEPS[].
    - Sub-test A asserts a cross-page next() from index.html at step index 1 persists sg.tourResume with stepIndex 2 and navigates to ./settings.html, using `steps[1].page !== steps[2].page` as the fixture assumption.
    - Sub-test B lands on settings.html with a resume stepIndex 2 (personalize) and resume() continues there with chrome mounted; `grep -c "settings.html" tests/41-resume-state.test.js` ≥ 1 confirms the new landing page (the boundary is index→settings now).
    - tests/41-tab-activation.test.js exists and both scenarios pass: the activation click fires before measurement (spotlight mounts), and a missing anchor after activation still yields the fallback modal with no step-index advance.
    - `node tests/run-all.js` green with zero failures (the new activation test adds its own passing scenarios).
  </acceptance_criteria>
  <done>The resume-state test asserts cross-page resume against the new index→settings boundary, the new tab-activation behavior test proves activation fires and degradation still holds, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Step copy is textContent (T-41-01). The save-step export glyph is the ONE innerHTML — a compile-time literal with zero interpolation. |
| STEPS[] → navigation | takeMeThereHref values are static in-repo relative paths; next()/start() assign them to location.href (no user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Information disclosure (XSS) | save-step export glyph innerHTML | low | mitigate | The glyph is a single compile-time string literal with zero variable interpolation, mirroring the sanctioned app.js "?" glyph pattern; all other tour copy stays textContent. Verified by grep (exactly one static innerHTML). |
| T-41-10a | Tampering | Silent step drop / mis-order breaks the taught path | medium | mitigate | launch-explicit (length 12) + resume-state (index→settings cross-page boundary) + the new tab-activation behavior test + the 41-13 human-verify replay guard the composition. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-launch-explicit.test.js` (length 12), `node tests/41-fallback-degradation.test.js`, `node tests/41-resume-state.test.js` (index→settings boundary), `node tests/41-tab-activation.test.js` (NEW), `node tests/41-lang-rerender.test.js` → all exit 0 against the recomposed spine.
- `node tests/run-all.js` → full suite green with zero failures (the new activation test adds passing scenarios).
- Grep gates in each task's acceptance_criteria pass; exactly one static innerHTML in tour.js.
</verification>

<success_criteria>
The engine walks the exact 12-step v3 spine (overview → settings → personalize → fields → snippets → nav → session-setup → session-heart → session-save → nav-sessions → backup → help), activates the Fields/Snippets tabs before speaking about them, the save-step tooltip carries the export glyph so its deixis is honest, launching from a non-Overview page still opens at home step 1, and cross-page resume is proven against the new index→settings boundary.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-10-SUMMARY.md` when done.
</output>