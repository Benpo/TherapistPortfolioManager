---
phase: 41-replayable-guided-tour
plan: 11
type: execute
wave: 6
gap_closure: true
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/41-tour-i18n-parity.test.js
autonomous: true
requirements: [TOUR-01]
must_haves:
  truths:
    - "Every step's copy is authored verbatim from 41-STORYLINE.md v3 §3 (the single source of truth) so the story reads as 'first make it yours, then walk the path a session travels' — the UAT gap 7 promise/screen mismatch is gone."
    - "Every page change is narrated by one of the two approved bridge patterns — 'pressed together' (the gear step's 'Let's step inside', the nav step's 'Let's open a session together') and 'arrive via the tab' (the Sessions arrival body 'We've arrived through the Sessions tab') — UAT gaps 1, 2."
    - "The settings chapter sells itself: step 4 (fields) says sections can be COMPLETELY renamed and made one's own plus safe deactivation ('nothing you disable is ever deleted'); step 5 (snippets) sells the coolest, most time-saving feature, generic to ANY repetitive documentation — no emotions-only framing, no niche examples (Ben 2026-07-09)."
    - "The save-step copy is honest: it says 'this is its icon' pointing at the glyph inside the tooltip, not at an off-screen control (UAT gap 5); the finish step mentions Reporting ('One tab along, Reporting…') and the tour replaying under the '?' (Reporting is not a step in v3)."
    - "No left/right directional words in any locale (RTL mirrors for free); copy quotes each locale's real nav.*/settings.tab.* labels; all 4 locales stay key-parity green (D-11)."
  artifacts:
    - "help.tour.step.settings/personalize/fields/snippets/ready .title+.body (NEW, x4 locales); rewritten overview/setup/heart/save/sessions/backup/help bodies; retired help.tour.step.addClient.* + startSession.* + reporting.*"
    - "tests/41-tour-i18n-parity.test.js STEP_IDS updated to the 12-id storyline set (count 39 → 43)"
  key_links:
    - "STEP_IDS in the parity test must equal the 41-10 STEPS[] i18nKey suffixes (overview, settings, personalize, fields, snippets, ready, setup, heart, save, sessions, backup, help) or the run resolves key-name strings."
    - "EN is canonical; HE/DE/CS are machine-draft flagged for the Phase 42.1 native-speaker pass (explicitly deferred, not in this phase)."
---

<!-- These retired i18n key literals appear in <action>/<acceptance_criteria> prose
     because they are the exact keys being removed and are negative-grepped (== 0).
     Sanctioned per the comment-text-discipline gate. -->
<!-- planner-discipline-allow: help.tour.step -->
<!-- planner-discipline-allow: help.tour.step.addClient -->
<!-- planner-discipline-allow: help.tour.step.startSession -->
<!-- planner-discipline-allow: help.tour.step.reporting -->

<objective>
Recompose the tour copy to the Ben-approved 41-STORYLINE.md v3 §3 (settings-first, 12 steps) so the story reads coherently to an end user (Ben's binding directive: "make sure the storyline is delivered in a good, reasonable way which makes sense to the end user"). The v3 table is the single source of truth for every step's title and body: step 1 is reframed from the false "running a session end to end" promise to the two-part promise (make this place yours, then walk the path a session travels); a NEW settings chapter (steps 2-6: the gear "Making it yours", "Your formats and dates", "Your fields", "Your words", "Ready to begin") carries the making-it-yours beats; the setup step gains an arrival confirmation ("Here we are"); the save step is de-falsified to point at the in-tooltip glyph; the Sessions arrival carries the "arrive via the tab" bridge; backup gains the recency-color detail; and the finish step mentions Reporting ("One tab along, Reporting…") and the tour replaying under the "?". The old add-client/start-session/reporting step keys are retired. All four locales are authored this phase (D-11); HE/DE/CS are machine-draft flagged for the Phase 42.1 native pass. The 4-locale parity gate stays green.

Purpose: the copy is where the narrative lives — this plan turns the recomposed spine (41-10) and anchors (41-09) into a story that makes sense on screen, which is the acceptance bar Ben set. Ben's copy emphases (fields "beyond language" + safe deactivation; snippets sold as the coolest generic time-saver) are binding acceptance criteria.
Output: the storyline copy in 4 locales + the parity gate realigned to the new 12-id step set.
</objective>

<artifacts_produced>
Symbols this plan creates / changes (all in assets/i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js — EN canonical, HE/DE/CS machine-draft):
- NEW keys: help.tour.step.settings.title/.body, help.tour.step.personalize.title/.body, help.tour.step.fields.title/.body, help.tour.step.snippets.title/.body, help.tour.step.ready.title/.body.
- REMOVED keys: help.tour.step.addClient.title/.body, help.tour.step.startSession.title/.body, help.tour.step.reporting.title/.body.
- REWRITTEN values (same keys): help.tour.step.overview.body (reframe), help.tour.step.setup.title/.body, help.tour.step.heart.title/.body, help.tour.step.save.title/.body (de-falsified), help.tour.step.sessions.body (arrive-via-tab), help.tour.step.backup.body (recency-color), help.tour.step.help.body (Reporting mention + tour replay).
- CHANGED test tests/41-tour-i18n-parity.test.js — STEP_IDS = [overview, settings, personalize, fields, snippets, ready, setup, heart, save, sessions, backup, help] (12 ids → 24 step keys; TOUR_KEYS total 39 → 43).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/phases/41-replayable-guided-tour/41-01-SUMMARY.md
@assets/i18n-en.js
@tests/41-tour-i18n-parity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign the 4-locale parity gate STEP_IDS to the 12-id storyline set (RED)</name>
  <files>tests/41-tour-i18n-parity.test.js</files>
  <read_first>
    - tests/41-tour-i18n-parity.test.js (STEP_IDS lines ~67-70; TOUR_KEYS assembly ~76-102; the count comment ~65-66; the presence/parity/placeholder/no-emoji assertions)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the 12-step id order)
    - .planning/phases/41-replayable-guided-tour/41-10-PLAN.md (the STEPS[] i18nKey suffixes this STEP_IDS must mirror)
  </read_first>
  <action>
    Replace the STEP_IDS array with the 12-id storyline set in order: overview, settings, personalize, fields, snippets, ready, setup, heart, save, sessions, backup, help. Remove 'addClient', 'startSession', and 'reporting'; add 'settings' (position 2), 'personalize' (3), 'fields' (4), 'snippets' (5), 'ready' (6). The step-key total becomes 24 (12 ids x title+body) so the TOUR_KEYS total becomes 43 — update the count comment on line ~65-66 (24 step + 5 chrome + 2 fallback + 3 exit + 3 reminder + 5 finish + 1 takeTour = 43). Leave the chrome/fallback/exit/reminder/finish/takeTour key list, the placeholder-token assertions ({screen}/{n}/{total}), and the no-emoji scan unchanged. This will FAIL (RED) until Task 2/3 add the new keys and remove addClient/startSession/reporting — expected.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js; echo "exit:$? (RED expected until Task 2/3)"</automated>
  </verify>
  <acceptance_criteria>
    - STEP_IDS equals exactly: overview, settings, personalize, fields, snippets, ready, setup, heart, save, sessions, backup, help (12 ids, in this order).
    - `grep -c "addClient\|startSession\|reporting" tests/41-tour-i18n-parity.test.js` = 0.
    - The count comment totals 43 keys (24 step + 19 chrome/fallback/exit/reminder/finish/takeTour).
    - Run before Task 2/3, the test exits non-zero (missing help.tour.step.settings.* / .personalize.* / .fields.* / .snippets.* / .ready.*) — documented RED.
  </acceptance_criteria>
  <done>The parity gate enforces the 12-id storyline set (43 keys) and fails RED until the copy lands.</done>
</task>

<task type="auto">
  <name>Task 2: Author the EN canonical storyline copy (assets/i18n-en.js)</name>
  <files>assets/i18n-en.js</files>
  <read_first>
    - assets/i18n-en.js (the help.tour.step.* block lines ~609-628 + help.tour.* chrome ~629-647 — the exact keys to rewrite/add/remove)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the Title (EN) + Body (EN, in-voice) columns — the AUTHORITATIVE text to author verbatim per step) + §2 design principles (honest deixis, no left/right, quote real labels) + §4 copy-emphasis row (Ben 2026-07-09)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Copywriting Contract — calm garden voice, ≤~40 words, noun/gerund titles, no-emoji)
    - settings.html (the snippets section ~170-200 — a trigger-prefix text-expansion mechanism: "type your trigger prefix followed by a tag name in any session note"; the Fields banner ~78 "Disabling a section never deletes existing data" — verify copy claims against the real UI, no overclaiming)
  </read_first>
  <action>
    Author the EN values verbatim from the 41-STORYLINE.md v3 §3 table (Title/Body columns) for each of the 12 steps — the storyline is the single source of truth; do not paraphrase or re-scope. Specifically:
    - help.tour.step.overview: keep title "Welcome to your garden"; body → the reframed step-1 copy (names the greeting card as the home page; the two-part promise: make this place yours, then "the path a session travels"; plus permission to leave).
    - ADD help.tour.step.settings.title = "Making it yours"; body → storyline step 2 (behind the small gear lives Settings; ends with the pressed-together bridge "Let's step inside together").
    - ADD help.tour.step.personalize.title = "Your formats and dates"; body → storyline step 3 (arrival "Here we are — Settings"; session formats + date format in one beat).
    - ADD help.tour.step.fields.title = "Your fields"; body → storyline step 4 — Ben-binding: sections can be COMPLETELY renamed and made one's own ("make the form truly your own"), switch off what you don't use, and safe deactivation ("Nothing you disable is ever deleted" — mirrors the real settings banner claim).
    - ADD help.tour.step.snippets.title = "Your words"; body → storyline step 5 — Ben-binding: SELL Text Snippets as the coolest, most time-saving feature, generic to ANY repetitive documentation (a sentence, a keyword, a whole closing note), reused in any session. Keep it generic — no emotions-only framing, no niche examples. Verify the insertion claim against the real snippets UI (trigger-prefix expansion) — do not overclaim a one-click insert.
    - ADD help.tour.step.ready.title = "Ready to begin"; body → storyline step 6 (the menu travels with you on every page; names Add Client + Add Session; ends with the pressed-together bridge "Let's open a session together").
    - help.tour.step.setup.title = "Setting the scene"; body → storyline step 7 (arrival "Here we are — a fresh session"; client/date/format = the very formats just named; inline client creation).
    - help.tour.step.heart.title = "The heart of the session"; body → storyline step 8 (each trapped emotion; the severity scales + Heart-Wall named as "nearby").
    - help.tour.step.save.title = "Saving your work"; body → storyline step 9 (save = local-only, nothing leaves the device; share a saved session as a client-ready PDF; "this is its icon" points at the in-tooltip glyph, not an off-screen control).
    - help.tour.step.sessions.body → storyline step 10 (arrive-via-tab: "We've arrived through the Sessions tab — it's here in the menu, on every page"; the gallery of saved work). Keep title "Looking back".
    - help.tour.step.backup.body → storyline step 11 (the small cloud creates an encrypted backup; its color signals when one is due). Keep title "Keeping a copy".
    - help.tour.step.help.body → storyline step 12 (the finish: "One tab along, Reporting…"; guides, answers, and this tour again live under the question mark). Keep title "You're ready".
    - REMOVE help.tour.step.addClient.title/.body, help.tour.step.startSession.title/.body, help.tour.step.reporting.title/.body (retired — no home-button steps, no Reporting step in v3).
    Quote real on-screen labels as they exist in i18n-en.js (Settings, Text Snippets, Add Client, Add Session, Sessions) — do not invent label wording. Use NO left/right directional words (the storyline uses "one tab along", "nearby", "behind this small gear"). Do not touch the help.tour chrome/fallback/exit/reminder/finish keys or help.entry.* (that is 41-12's scope).
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'help.tour.step.settings.title' assets/i18n-en.js` = 1, `help.tour.step.personalize.title` = 1, `help.tour.step.fields.title` = 1, `help.tour.step.snippets.title` = 1, `help.tour.step.ready.title` = 1 (the 5 new keys present).
    - `grep -c 'help.tour.step.addClient\|help.tour.step.startSession\|help.tour.step.reporting' assets/i18n-en.js` = 0 (retired keys gone).
    - The reframed overview body contains "the path a session travels" (grep) and does NOT contain "end to end" (grep = 0) — UAT gap 7 fix.
    - The settings body contains the pressed-together bridge "step inside"; the ready body contains "open a session together" and names both "Add Client" and "Add Session"; the sessions body contains "through the Sessions tab"; the help body contains "One tab along" and "Reporting" — the two approved bridge patterns + the Reporting mention.
    - The fields body contains "Nothing you disable is ever deleted" and a rename claim ("make the form truly your own" or "Rename"); the snippets body contains "Text Snippets" and the generic examples "a sentence, a keyword" — Ben's binding emphases.
    - The save body contains "this is its icon" and does NOT contain "look for this export icon" (grep = 0) — honest deixis.
    - No left/right directional words in the new step bodies: grepping the step-value lines for `\b(left|right)\b` finds zero matches.
    - EN parity portion of `node tests/41-tour-i18n-parity.test.js` shows the EN locale has all 24 step keys (the test stays RED overall only until HE/DE/CS are done — Task 3).
  </acceptance_criteria>
  <done>EN canonical copy authored verbatim from the v3 storyline table: reframed overview, the settings chapter (gear/personalize/fields/snippets/ready), arrival setup, honest save, the tab bridge, backup recency-color, and a Reporting-mentioning tour-replay finish — no left/right words, real labels quoted, Ben's fields + snippets emphases honored.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the copy changes in HE / DE / CS (machine-draft, flagged for 42.1) → parity GREEN</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (the EN canonical from Task 2 — the meaning to translate)
    - assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js (the existing help.tour.step.* blocks + each file's convention — HE/DE raw UTF-8, CS \u-escaped per 41-01-SUMMARY; each file's real nav.*/overview.* label strings to quote)
    - .planning/phases/41-replayable-guided-tour/41-01-SUMMARY.md (D-11 4-locale precedent; HE noun/infinitive forms per Phase 24 D-05; DRAFT-flag convention for the 42.1 native pass)
  </read_first>
  <action>
    In each of i18n-he.js, i18n-de.js, i18n-cs.js apply the SAME key changes as EN: add help.tour.step.settings/personalize/fields/snippets/ready .title+.body, remove help.tour.step.addClient.* + startSession.* + reporting.*, and rewrite the overview/setup/heart/save/sessions/backup/help values to match the EN meaning. Author machine-draft translations (flagged for the Phase 42.1 native-speaker pass — the native pass is explicitly deferred, NOT done here). Rules: HE uses noun/infinitive forms (Phase 24 D-05), DE the Sie register, CS the formal register and \u-escaping per file convention. Quote each locale's OWN real nav.*/settings.tab.* label strings (do not re-translate the UI labels — read them from that locale file: the Snippets tab label, Add Client, Add Session, Sessions). Preserve Ben's binding emphases in each locale (fields: complete rename + "nothing you disable is ever deleted"; snippets: the coolest generic time-saver, no emotions-only framing). Use NO left/right directional words in any locale (the storyline's direction-neutral phrasing mirrors for free in RTL). Keep the key set identical across all four files so parity stays green.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/41-tour-i18n-parity.test.js` exits 0 (presence + non-empty + 4-locale key parity + placeholder tokens + no-emoji all pass) — GREEN.
    - Each of he/de/cs has help.tour.step.settings.* / .personalize.* / .fields.* / .snippets.* / .ready.* and NO help.tour.step.addClient / startSession / reporting (grep each file: the 5 new keys present, the 3 retired keys = 0).
    - `node tests/run-all.js` green with zero failures — no other test regresses from the copy change.
    - HE/DE/CS values are flagged DRAFT in-file for the 42.1 native pass (consistent with the 41-01 convention).
  </acceptance_criteria>
  <done>All four locales carry the v3 storyline copy at key parity; the parity gate is GREEN and the full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Static bundled copy rendered via textContent at the engine seam (T-41-01). This plan ships only string data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Information disclosure (XSS) | i18n copy → tour DOM | low | mitigate | Copy is bundled static data; the render seam (tour.js) injects it via textContent. No markup in these strings. Verified downstream by the engine's textContent contract. |
| T-41-11a | Tampering | Locale key drift (a locale missing/extra a key) breaks TOUR-04 language switch | medium | mitigate | The 4-locale parity gate (Task 1) fails the suite on any presence/parity gap. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-tour-i18n-parity.test.js` → exits 0 (RED after Task 1, GREEN after Tasks 2+3): 43 keys x 4 locales, parity + placeholders + no-emoji.
- `node tests/run-all.js` → full suite green with zero failures.
- EN acceptance greps prove the storyline bridge phrases + Ben's fields/snippets emphases + honest deixis + no left/right words.
</verification>

<success_criteria>
The tour copy reads as the v3 storyline intends in all four locales: reframed opening, the settings chapter (gear/personalize/fields/snippets/ready) with Ben's fields + snippets emphases, arrival confirmations, honest save deixis, the two narrated page-change bridges, backup recency-color, and a Reporting-mentioning tour-replay finish — no left/right words, real labels quoted, 4-locale parity green (43 keys).
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-11-SUMMARY.md` when done.
</output>