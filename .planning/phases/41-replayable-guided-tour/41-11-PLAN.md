---
phase: 41-replayable-guided-tour
plan: 11
type: execute
wave: 6
gap_closure: true
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/41-tour-i18n-parity.test.js
autonomous: true
requirements: [TOUR-01]
must_haves:
  truths:
    - "Every step's copy is authored verbatim from 41-STORYLINE.md §3 (the single source of truth), so the story reads as 'the path a session travels' — the UAT gap 7 promise/screen mismatch is gone."
    - "Every page change is narrated by one of the two approved bridge patterns — 'pressed together' (begin body announces the move) and 'arrive via the tab' (sessions/reporting bodies say we came through the tab) — UAT gaps 1, 2."
    - "The save-step copy is honest: it says 'this is its icon' pointing at the glyph inside the tooltip, not at an off-screen control (UAT gap 5)."
    - "A new Settings step teaches the gear (UAT gap 6); the finish step mentions the tour replays under the '?'."
    - "No left/right directional words in any locale (RTL mirrors for free); copy quotes each locale's real nav.*/overview.* labels; all 4 locales stay key-parity green (D-11)."
  artifacts:
    - "help.tour.step.begin.* + help.tour.step.settings.* (NEW, x4 locales); rewritten overview/setup/heart/save/sessions/reporting/backup/help bodies; retired help.tour.step.addClient.* + help.tour.step.startSession.*"
    - "tests/41-tour-i18n-parity.test.js STEP_IDS updated to the storyline id set"
  key_links:
    - "STEP_IDS in the parity test must equal the 41-10 STEPS[] i18nKey suffixes (overview, begin, setup, heart, save, sessions, reporting, backup, settings, help) or the run resolves key-name strings."
    - "EN is canonical; HE/DE/CS are machine-draft flagged for the Phase 42.1 native-speaker pass (explicitly deferred, not in this phase)."
---

<!-- These retired i18n key literals appear in <action>/<acceptance_criteria> prose
     because they are the exact keys being removed and are negative-grepped (== 0).
     Sanctioned per the comment-text-discipline gate. -->
<!-- planner-discipline-allow: help.tour.step -->
<!-- planner-discipline-allow: help.tour.step.addClient -->
<!-- planner-discipline-allow: help.tour.step.startSession -->

<objective>
Recompose the tour copy to the Ben-approved 41-STORYLINE.md §3 so the story reads coherently to an end user (Ben's binding directive: "make sure the storyline is delivered in a good, reasonable way which makes sense to the end user"). The storyline table is the single source of truth for every step's title and body: step 1 is reframed from the false "running a session end to end" promise to "the path a session travels", the two home-page button steps merge into "Two ways to begin", the setup step gains an arrival confirmation ("Here we are"), the save step is de-falsified to point at the in-tooltip glyph, the sessions/reporting steps carry the "arrive via the tab" / "one tab along" bridges, backup gains the recency-color detail, a NEW Settings step ("Making it yours") is added, and the finish step notes the tour replays under the "?". All four locales are authored this phase (D-11); HE/DE/CS are machine-draft flagged for the Phase 42.1 native pass. The 4-locale parity gate stays green.

Purpose: the copy is where the narrative lives — this plan turns the recomposed spine (41-10) and anchors (41-09) into a story that makes sense on screen, which is the acceptance bar Ben set.
Output: the storyline copy in 4 locales + the parity gate realigned to the new step id set.
</objective>

<artifacts_produced>
Symbols this plan creates / changes (all in assets/i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js — EN canonical, HE/DE/CS machine-draft):
- NEW keys: help.tour.step.begin.title, help.tour.step.begin.body, help.tour.step.settings.title, help.tour.step.settings.body.
- REMOVED keys: help.tour.step.addClient.title, help.tour.step.addClient.body, help.tour.step.startSession.title, help.tour.step.startSession.body.
- REWRITTEN values (same keys): help.tour.step.overview.body (reframe), help.tour.step.setup.title/.body, help.tour.step.heart.title/.body, help.tour.step.save.title/.body (de-falsified), help.tour.step.sessions.body (arrive-via-tab), help.tour.step.reporting.body (one-tab-along), help.tour.step.backup.body (recency-color), help.tour.step.help.body (mentions tour replay).
- CHANGED test tests/41-tour-i18n-parity.test.js — STEP_IDS = [overview, begin, setup, heart, save, sessions, reporting, backup, settings, help].
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
@.planning/phases/41-replayable-guided-tour/41-01-SUMMARY.md
@assets/i18n-en.js
@tests/41-tour-i18n-parity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign the 4-locale parity gate STEP_IDS to the storyline id set (RED)</name>
  <files>tests/41-tour-i18n-parity.test.js</files>
  <read_first>
    - tests/41-tour-i18n-parity.test.js (STEP_IDS lines ~67-70; TOUR_KEYS assembly ~76-102; the count comment ~65-66; the presence/parity/placeholder/no-emoji assertions)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the step id order)
    - .planning/phases/41-replayable-guided-tour/41-10-PLAN.md (the STEPS[] i18nKey suffixes this STEP_IDS must mirror)
  </read_first>
  <action>
    Replace the STEP_IDS array with the storyline id set in order: overview, begin, setup, heart, save, sessions, reporting, backup, settings, help. Remove 'addClient' and 'startSession'; add 'begin' (position 2) and 'settings' (position 9). The step-key total stays 20 (10 ids x title+body) so the TOUR_KEYS total stays 39 — update the count comment on line ~65-66 to reflect begin/settings replacing addClient/startSession (still 39). Leave the chrome/fallback/exit/reminder/finish/takeTour key list, the placeholder-token assertions ({screen}/{n}/{total}), and the no-emoji scan unchanged. This will FAIL (RED) until Task 2/3 add the begin/settings keys and remove addClient/startSession — expected.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js; echo "exit:$? (RED expected until Task 2/3)"</automated>
  </verify>
  <acceptance_criteria>
    - STEP_IDS equals exactly: overview, begin, setup, heart, save, sessions, reporting, backup, settings, help (10 ids, in this order).
    - `grep -c "addClient\|startSession" tests/41-tour-i18n-parity.test.js` = 0.
    - The count comment still totals 39 keys (20 step + 19 chrome/fallback/exit/reminder/finish/takeTour).
    - Run before Task 2/3, the test exits non-zero (missing help.tour.step.begin.* / help.tour.step.settings.*) — documented RED.
  </acceptance_criteria>
  <done>The parity gate enforces the storyline id set and fails RED until the copy lands.</done>
</task>

<task type="auto">
  <name>Task 2: Author the EN canonical storyline copy (assets/i18n-en.js)</name>
  <files>assets/i18n-en.js</files>
  <read_first>
    - assets/i18n-en.js (the help.tour.step.* block lines ~609-628 + help.tour.* chrome ~629-647 — the exact keys to rewrite/add/remove)
    - .planning/phases/41-replayable-guided-tour/41-STORYLINE.md §3 (the Title (EN) + Body (EN, in-voice) columns — the AUTHORITATIVE text to author verbatim per step) + §2 design principles (honest deixis, no left/right, quote real labels)
    - .planning/phases/41-replayable-guided-tour/41-UI-SPEC.md (Copywriting Contract — voice, ≤~40 words, noun/gerund titles)
  </read_first>
  <action>
    Author the EN values verbatim from the 41-STORYLINE.md §3 table (Title/Body columns) for each step — the storyline is the single source of truth; do not paraphrase or re-scope. Specifically:
    - help.tour.step.overview.body → reframed step-1 copy: names the greeting card as the home page and promises "the path a session travels" (a new client, the work itself, a safe copy), plus permission to leave. Keep the title "Welcome to your garden".
    - ADD help.tour.step.begin.title = "Two ways to begin" and help.tour.step.begin.body = storyline step 2 (names Add Client and Add Session, ends with the pressed-together bridge "Next, we'll open a session together").
    - REMOVE help.tour.step.addClient.title, help.tour.step.addClient.body, help.tour.step.startSession.title, help.tour.step.startSession.body.
    - help.tour.step.setup.title = "Setting the scene"; body = storyline step 3 (arrival confirmation "Here we are" + setup fields + inline client creation).
    - help.tour.step.heart.title = "The heart of the session"; body = storyline step 4 (emotions as the first noun; severity + Heart-Wall named as "nearby").
    - help.tour.step.save.title = "Saving your work"; body = storyline step 5 (save = local-only; export named; "this is its icon" pointing at the in-tooltip glyph — no "look for this export icon" on-screen deixis).
    - help.tour.step.sessions.body = storyline step 6 (arrive-via-tab: "We've arrived through the Sessions tab — it's here in the menu, on every page"). Keep title "Looking back".
    - help.tour.step.reporting.body = storyline step 7 ("One tab along sits Reporting — where we are now"). Keep title "Seeing the whole picture".
    - help.tour.step.backup.body = storyline step 8 (recency-color: the cloud's color signals when a backup is due). Keep title "Keeping a copy".
    - ADD help.tour.step.settings.title = "Making it yours" and help.tour.step.settings.body = storyline step 9 (behind the gear: rename session formats, shape snippets, tend photos and backups; "worth a quiet visit").
    - help.tour.step.help.body = storyline step 10 (guides, answers, and this tour again live under the question mark). Keep title "You're ready".
    Quote real on-screen labels as they exist in i18n-en.js (Add Client, Add Session, Sessions, Reporting) — do not invent label wording. Use NO left/right directional words (use "one tab along", "nearby", "behind this gear"). Do not touch the help.tour chrome/fallback/exit/reminder/finish keys or help.entry.* (that is 41-12's scope).
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'help.tour.step.begin.title' assets/i18n-en.js` = 1 and `grep -c 'help.tour.step.settings.title' assets/i18n-en.js` = 1.
    - `grep -c 'help.tour.step.addClient' assets/i18n-en.js` = 0 and `grep -c 'help.tour.step.startSession' assets/i18n-en.js` = 0 (retired keys gone).
    - The reframed overview body contains the phrase "the path a session travels" (grep) and does NOT contain "end to end" (grep = 0) — UAT gap 7 fix.
    - The begin body contains the pressed-together bridge phrase "open a session together" (grep); the sessions body contains "through the Sessions tab" (grep); the reporting body contains "One tab along" (grep) — the two approved bridge patterns.
    - The save body contains "this is its icon" and does NOT contain "look for this export icon" (grep = 0) — honest deixis.
    - No left/right directional words in the new step bodies: reading the step-value block, none of the bodies contain a standalone "left" or "right" word (the storyline uses "one tab along" / "nearby" / "behind this gear"). Confirm by grepping the step-value lines for the words `\b(left|right)\b` and finding zero matches.
    - EN parity portion of `node tests/41-tour-i18n-parity.test.js` shows the EN locale has all 20 step keys (test still RED overall only if HE/DE/CS not yet done — Task 3).
  </acceptance_criteria>
  <done>EN canonical copy authored verbatim from the storyline table: reframed overview, merged begin, arrival setup, honest save, tab bridges, backup recency-color, new settings, tour-replay finish — no left/right words, real labels quoted.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the copy changes in HE / DE / CS (machine-draft, flagged for 42.1) → parity GREEN</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (the EN canonical from Task 2 — the meaning to translate)
    - assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js (the existing help.tour.step.* blocks + each file's convention — HE/DE raw UTF-8, CS \u-escaped per 41-01-SUMMARY; each file's real nav.*/overview.* label strings to quote)
    - .planning/phases/41-replayable-guided-tour/41-01-SUMMARY.md (D-11 4-locale precedent; HE noun/infinitive forms per Phase 24 D-05; DRAFT-flag convention for the 42.1 native pass)
  </read_first>
  <action>
    In each of i18n-he.js, i18n-de.js, i18n-cs.js apply the SAME key changes as EN: add help.tour.step.begin.title/.body + help.tour.step.settings.title/.body, remove help.tour.step.addClient.* + help.tour.step.startSession.*, and rewrite the overview/setup/heart/save/sessions/reporting/backup/help values to match the EN meaning. Author machine-draft translations (flagged for the Phase 42.1 native-speaker pass — the native pass is explicitly deferred, NOT done here). Rules: HE uses noun/infinitive forms (Phase 24 D-05), DE the Sie register, CS the formal register and \u-escaping per file convention. Quote each locale's OWN real nav.*/overview.* label strings (do not re-translate the UI labels — read them from that locale file). Use NO left/right directional words in any locale (the storyline's direction-neutral phrasing mirrors for free in RTL). Keep the key set identical across all four files so parity stays green.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js && node tests/run-all.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/41-tour-i18n-parity.test.js` exits 0 (presence + non-empty + 4-locale key parity + placeholder tokens + no-emoji all pass) — GREEN.
    - Each of he/de/cs has help.tour.step.begin.* and help.tour.step.settings.* and NO help.tour.step.addClient / startSession (grep each file: begin/settings = present, addClient/startSession = 0).
    - `node tests/run-all.js` green (152 passed) — no other test regresses from the copy change.
    - HE/DE/CS values are flagged DRAFT in-file for the 42.1 native pass (consistent with the 41-01 convention).
  </acceptance_criteria>
  <done>All four locales carry the storyline copy at key parity; the parity gate is GREEN and the full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → tour DOM | Static bundled copy rendered via textContent at the engine seam (T-41-01). This plan ships only string data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Information disclosure (XSS) | i18n copy → tour DOM | low | mitigate | Copy is bundled static data; the render seam (tour.js) injects it via textContent. No markup in these strings. Verified downstream by the engine's textContent contract. |
| T-41-11a | Tampering | Locale key drift (a locale missing/extra a key) breaks TOUR-04 language switch | medium | mitigate | The 4-locale parity gate (Task 1) fails the suite on any presence/parity gap. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan. N/A. |
</threat_model>

<verification>
- `node tests/41-tour-i18n-parity.test.js` → exits 0 (RED after Task 1, GREEN after Tasks 2+3): 39 keys x 4 locales, parity + placeholders + no-emoji.
- `node tests/run-all.js` → full suite green (152 passed).
- EN acceptance greps prove the storyline bridge phrases + honest deixis + no left/right words.
</verification>

<success_criteria>
The tour copy reads as the storyline intends in all four locales: reframed opening, merged begin beat, arrival confirmations, honest save deixis, the two narrated page-change bridges, backup recency-color, a Settings step, and a tour-replay finish — no left/right words, real labels quoted, 4-locale parity green.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-11-SUMMARY.md` when done.
</output>