---
phase: 41-replayable-guided-tour
plan: 13
type: execute
wave: 8
gap_closure: true
depends_on: ["41-08", "41-09", "41-10", "41-11", "41-12"]
files_modified: []
autonomous: false
requirements: [TOUR-01, TOUR-02, TOUR-03, TOUR-04]
must_haves:
  truths:
    - "The full npm suite is green and the strengthened WebKit RTL/geometry probe passes on the fully-recomposed tour (all gap fixes integrated)."
    - "Replaying the full 12-step v3 tour in EN AND HE reads as one coherent story on screen — the UAT that failed (8 checks) now passes (D-14 real-Safari-class verification, EN + HE)."
    - "Every page change is narrated (pressed-together + arrive-via-the-tab); the settings-first chapter is walked (gear → personalize → fields → snippets → ready) with the Fields/Snippets tabs VISIBLY activated before their copy speaks; the spotlight sits on-anchor in both directions; the '?' is a 3-item menu; Reporting is mentioned only in the finish copy (not visited)."
  artifacts:
    - "A green full-suite run + a green WebKit probe run recorded in the summary"
    - "A human sign-off that the v3 storyline reads coherently in EN + HE (the re-run of the failed UAT)"
  key_links:
    - "This is the phase gate: full npm suite green + WebKit probe pass + storyline human-verify (EN+HE) before the phase can be marked resolved."
    - "Plan 07's broader real-Safari checkpoint remains paused; this plan re-runs the specific storyline UAT that failed and is expected to pass now that gaps 1-8 are closed."
---

<objective>
Close the gap-closure loop with the end-to-end verification Ben's directive demands: after the geometry fix (41-08), the 12-anchor recomposition (41-09), the 12-step STEPS[] recomposition + tab activation + export glyph (41-10), the v3 storyline copy (41-11), and the "?" menu trim (41-12), confirm the whole automated suite is green, the strengthened WebKit RTL/geometry probe passes on the final tour, and — the acceptance bar Ben set — a human replays the full 12-step v3 tour (settings-first) in EN and HE and confirms the STORY reads coherently on screen. This is a re-run of the UAT that failed (41-UAT.md's 8 checks), remediated per 41-STORYLINE.md v3 (approved 2026-07-09); with gaps 1-8 closed it is expected to pass. The check is explicitly re-runnable so the storyline delivery can be signed off, not just per-step correctness.

Purpose: the UAT gap set was a narrative failure ("the screens just jump", "settings is not mentioned", "the box is not fully aligned", "add client on the wrong side in Hebrew"). Per-plan greens do not prove the story reads well; only replaying the whole tour does. This plan is that proof.
Output: a green suite + green WebKit probe + a human EN/HE storyline sign-off.
</objective>

<artifacts_produced>
Symbols this plan creates / changes:
- No source files modified (verification + human sign-off only).
- Produces 41-13-SUMMARY.md recording: full-suite result, WebKit probe result, and Ben's EN/HE storyline sign-off (or a routed gap list if it fails).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-replayable-guided-tour/41-STORYLINE.md
@.planning/phases/41-replayable-guided-tour/41-UAT.md
@.planning/phases/41-replayable-guided-tour/41-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated suite + strengthened WebKit probe — both green on the integrated tour</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/41-replayable-guided-tour/41-08-PLAN.md (the WebKit probe run command + its off-center RTL + post-settle assertions)
    - tests/run-all.js (the suite runner + its glob — confirm the webkit probe is excluded)
    - .planning/phases/41-replayable-guided-tour/41-UAT.md (the 8 checks this phase must satisfy)
  </read_first>
  <action>
    Run the full automated suite and the ad-hoc WebKit probe against the fully-integrated tour (all of 41-08..41-12 applied). Confirm: (1) node tests/run-all.js passes with zero failures; (2) node tests/webkit/41-rtl-geometry.mjs exits 0 (spotlight branch selected, off-center-anchor RTL side correct in Hebrew, post-settle geometry on-anchor, clean 4-language re-render); (3) the tour-specific behavior + parity + anchor + activation + demo-gate + precache tests are all green (41-anchor-presence, 41-tour-i18n-parity, 41-launch-explicit, 41-fallback-degradation, 41-resume-state, 41-tab-activation, 41-lang-rerender, 41-demo-gate, 41-precache, plus the realigned 40-i18n-parity / 40-app-wiring). If any fails, STOP and record the failing assertion for gap routing rather than proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>node tests/run-all.js 2>&1 | tail -5 && node tests/webkit/41-rtl-geometry.mjs; echo "webkit-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/run-all.js` exits 0 with zero failures (the count reflects the three Phase-40 replay tests removed in 41-12 plus the new tab-activation scenarios added in 41-10; no failures).
    - `node tests/webkit/41-rtl-geometry.mjs` exits 0: spotlight branch selected on a present anchor, Hebrew off-center-anchor spotlight on the correct physical side, post-settle geometry on-anchor, clean EN/HE/DE/CS re-render.
    - The tour anchor-presence rot guard (12-anchor v3 contract), the 4-locale parity gate (12-id storyline set, 43 keys), the tab-activation behavior test, and the demo-gate 3-item ordering all pass.
    - If any check fails, the summary records the exact failing assertion and NO human checkpoint is requested (route as a further gap instead).
  </acceptance_criteria>
  <done>The full suite and the strengthened WebKit probe are both green on the integrated, recomposed tour.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Storyline coherence sign-off — replay the full tour in EN + HE (the re-run of the failed UAT)</name>
  <action>Present the fully-recomposed tour for a human replay in EN and HE (prefer a real Safari / installed PWA for the RTL step); block on Ben's sign-off that the storyline reads coherently on screen. This is the re-run of the failed UAT (41-UAT.md's 8 checks). Route any step whose framing, geometry, or page-change bridge still reads wrong as a further gap-closure fix rather than shipping.</action>
  <what-built>
    The tour is fully recomposed to the Ben-approved 41-STORYLINE.md v3 (settings-first, 12 steps): a reframed step 1 with the two-part promise (make this place yours → "the path a session travels"), a settings-first chapter (the gear "Making it yours" → the Personalization panel "Your formats and dates" → "Your fields" → "Your words"/snippets — with the Fields and Snippets tabs ACTIVATED by the engine before their copy speaks — → the whole-menu "Ready to begin"), three deep session-form steps (setup arrival naming the very formats just shaped, the emotions-anchored heart, an honest save with the export glyph inside the tooltip), ONE nav-tab arrival step (Sessions, "arrive via the tab"), and a two-beat header close (backup recency-color → the help/tour-replay finish that mentions Reporting "one tab along" without visiting it). Geometry is fixed (first-paint snap + physical RTL coordinates), and the "?" menu is trimmed to three items (Help center / Onboarding Tour / Contact us). Task 1 confirmed the suite + WebKit probe are green.
  </what-built>
  <how-to-verify>
    Replay the full 12-step tour and confirm the STORY reads coherently on screen — this is the UAT that failed (41-UAT.md), remediated per storyline v3, expected to pass now. The walk order is: overview → settings gear → personalize → fields → snippets → nav ("Ready to begin") → setup → heart → save → nav-sessions → backup → help. Pages: index → settings → add-session → sessions.
    1. From the "?" menu, launch "Onboarding Tour". Confirm the "?" popover shows exactly THREE items (Help center / Onboarding Tour / Contact us) — no "Onboarding screen"/welcome-replay row. (gap 8)
    2. Step 1 (greeting card): the copy names the home page and makes the two-part promise — make this place yours, then "the path a session travels" — it no longer promises "running a session end to end". The spotlight sits EXACTLY on the greeting card on first paint (no grow-from-corner, no offset). (gaps 2, 7)
    3. Step 2 ("Making it yours"): the header GEAR is lit; the copy says Settings lives behind it and announces the move ("Let's step inside together"); Next auto-navigates to the Settings page — the pressed-together bridge. (gaps 6, 1)
    4. Steps 3-5 (the settings chapter): step 3 arrives on the already-visible Personalization panel ("Here we are — Settings"; session formats + date format in one beat); on step 4 the tour VISIBLY SWITCHES to the Custom-field-names tab BEFORE the copy speaks about it, and the copy says sections can be completely renamed ("make the form truly your own") and safely switched off ("Nothing you disable is ever deleted"); on step 5 it visibly switches to the Text Snippets tab and SELLS snippets as the time-saving corner for ANY repetitive writing (a sentence, a keyword, a whole closing note — no emotions-only framing). Spot-check these copy emphases — they are Ben's binding directives. (gap 6 + v3 §4)
    5. Step 6 ("Ready to begin"): the WHOLE menu is spotlit (not a single link); the copy names Add Client + Add Session as the two entry points and announces the move ("Let's open a session together"); Next auto-navigates to the session form — the second pressed-together bridge. (gaps 3, 1)
    6. Steps 7-9 on the session form: "Here we are — a fresh session" arrival, with the format field paying off the settings chapter (the very formats just named); the heart step lights the emotions section (matching the copy's first noun, severity + Heart-Wall "nearby"); the save step says "this is its icon" pointing at the export glyph shown INSIDE the tooltip (not an off-screen PDF icon). (gaps 5, 7)
    7. Step 10 (Sessions arrival): the FIRST lit thing is the Sessions nav tab and the copy says we came through it ("We've arrived through the Sessions tab") — the page change feels taught, not teleported. (gaps 1, 2)
    8. Steps 11-12 (two-beat header close): backup names the cloud's recency-color; the finish ("You're ready") mentions Reporting "one tab along" WITHOUT visiting it and notes that guides + this tour replay live under the "?". Done → the finish card ("You're all set" + Add your first client / Start your first session / Browse the help center). (gap 6 closure + v3 §5 decision 2)
    9. Switch to Hebrew (globe) mid-tour on an OFF-CENTER step (e.g. the gear, the snippets panel, or the save button): the spotlight + tooltip land on the CORRECT physical side — not mirrored. Cycle EN/HE/DE/CS; each re-renders cleanly, RTL arrow on the correct side, no overflow, and the settings-tab activation still holds after the re-render. (gap 4 blocker, TOUR-04)
    10. Confirm the spotlight travels forward only (no zigzag/backtrack) and the pacing reads calm → one door → three making-it-yours beats → doorway back → three form beats → Sessions arrival → two-beat close.
    Prefer a real Safari / installed PWA for step 9 (the D-14 real-WebKit class).
  </how-to-verify>
  <resume-signal>Type "approved" if the story reads coherently in EN + HE, or describe any step whose framing/geometry/bridge still reads wrong to route as a further gap-closure fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness / human → app | Verification-only; the suite and the human replay drive the shipped app read-only and mutate no client data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-13a | Tampering | A narrative/geometry regression ships because only per-plan greens were checked | high | mitigate | This plan re-runs the full failed UAT (EN + HE storyline replay) plus the WebKit probe as the phase gate before resolution. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the WebKit probe uses the pre-existing pinned local Playwright. N/A. |
</threat_model>

<verification>
- `node tests/run-all.js` → full suite green.
- `node tests/webkit/41-rtl-geometry.mjs` → exits 0 (off-center RTL + post-settle + 4-language re-render).
- Blocking human replay of the full 12-step v3 tour in EN + HE signed off by Ben (the re-run of the failed UAT).
</verification>

<success_criteria>
The full suite and WebKit probe are green on the integrated tour, and Ben confirms the storyline reads coherently on screen in EN and HE — closing the UAT that failed. Any remaining issue is routed as a further gap rather than silently shipped.
</success_criteria>

<output>
Create `.planning/phases/41-replayable-guided-tour/41-13-SUMMARY.md` when done.
</output>