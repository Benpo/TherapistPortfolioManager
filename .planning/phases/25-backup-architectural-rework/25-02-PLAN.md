---
phase: 25-backup-architectural-rework
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - index.html
  - assets/overview.js
  - assets/app.js
  - assets/app.css
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/25-02-modal-structure.test.js
  - tests/25-02-checklist-store-parity.test.js
  - assets/backup.js
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-10, D-26, D-27, D-28, D-29]
tags: [backup, ui, modal, header, i18n, single-source-of-truth]

must_haves:
  truths:
    - "The 5-button overview cluster (index.html:99-115) is collapsed to EXACTLY two children: #addClientBtn and #addSessionBtn. The Send / Auto-backup / Export / Import / Send-to-Myself buttons are removed from the overview surface."
    - "A circular cloud icon button (#backupCloudBtn) is mounted into #headerActions on every page (overview, add-client, add-session, settings, ...) BEFORE the existing settings gear in LTR (visual order: brand … cloud, gear)."
    - "The cloud icon button has an aria-label sourced from i18n key overview.backupRestore and a title attribute reading 'Last backup · {relative}' (or 'Last backup · never') localized."
    - "Clicking the cloud icon opens a modal with title 'Backup & Restore' (28px display) and a subtitle 'Last backup: {relative}' driven by localStorage.portfolioLastExport."
    - "The modal contains: Backup contents block (5 checkmark rows) → Export block (prominent, two buttons) → Import block (secondary, with destructive warning callout) → Test password sub-card placeholder (wired in Plan 03) → Settings footer link."
    - "The Export block 'Export backup' button calls the same encrypt-aware flow currently triggered by exportBtn (BackupManager.exportEncryptedBackup → fallback to exportBackup)."
    - "The Export block 'Share backup' button is hidden by default and only revealed when BackupManager.isShareSupported(probe) returns true AND the user took the SKIP-encryption path. After an ENCRYPTED export completes, the Share button is hidden — Plan 08's refactor re-enables it for the encrypted path once the encrypted blob is plumbed through."
    - "The Import block file input accepts .zip,.json,.sgbackup and routes through the existing BackupManager.importBackup with the existing replace-confirm dialog."
    - "The Backup contents checklist is rendered from a single static array constant `BACKUP_CONTENTS_KEYS` exported by backup.js so a runtime test can prove it matches exportBackup's manifest store list."
    - "The cloud icon's BACKGROUND COLOR encodes recency state (Plan 04 owns the state-color rules + state-update wiring); Plan 02 ships only the base markup, the base .header-icon-btn / .backup-cloud-btn rules, and the initial 'never' state class on mount."
  artifacts:
    - path: "index.html"
      provides: "New backup-modal markup; overview cluster reduced to Add Client + Add Session only; #headerActions container is unchanged in markup (cloud icon is JS-mounted)"
      contains: "id=\"backupModal\""
    - path: "assets/app.js"
      provides: "New mountBackupCloudButton helper that creates #backupCloudBtn and inserts it into #headerActions BEFORE the settings gear; called from initCommon so the icon appears on every page"
    - path: "assets/overview.js"
      provides: "Modal handlers (open/close, Export, Share, Import, Settings link); subtitle renderer; Share-button visibility logic that hides on encrypted-export completion (D-04 honest body — broken UX deferred to Plan 08); window.openBackupModal + window.renderLastBackupSubtitle exposed for Plans 04/05"
    - path: "assets/app.css"
      provides: "New .header-icon-btn (circular 44×44 base), .backup-cloud-btn (cloud-specific overrides), .backup-modal-card, .backup-modal-section, .backup-modal-section--export, .backup-modal-section--import, .backup-modal-section--test, .backup-contents-list, .backup-contents-item utility classes. NOTE: the four state-color modifiers (.backup-cloud-btn--never/--fresh/--warning/--danger) are owned by Plan 04 — Plan 02 ships only the base styles."
    - path: "assets/backup.js"
      provides: "Exposes BACKUP_CONTENTS_KEYS as a public read-only array + computeBackupRecencyState() helper used by mount-time class assignment (full state-update wiring lives in Plan 04)"
      exports: ["BACKUP_CONTENTS_KEYS", "computeBackupRecencyState"]
    - path: "tests/25-02-modal-structure.test.js"
      provides: "Asserts the modal markup contains all required IDs, the old overview backup buttons are gone, the inline-actions cluster contains EXACTLY [#addClientBtn, #addSessionBtn], and #headerActions is the canonical insertion target referenced by mountBackupCloudButton"
    - path: "tests/25-02-checklist-store-parity.test.js"
      provides: "BACKUP_CONTENTS_KEYS matches the keys exportBackup writes into the manifest (D-09 + D-29)"
  key_links:
    - from: "#backupCloudBtn (header, every page)"
      to: "openBackupModal()"
      via: "click handler attached in mountBackupCloudButton"
      pattern: "backupCloudBtn.*addEventListener\\(\\s*['\"]click"
    - from: "App.initCommon (assets/app.js)"
      to: "mountBackupCloudButton(headerActionsEl)"
      via: "called alongside initSettingsLink so the icon mounts on every page"
      pattern: "mountBackupCloudButton\\("
    - from: "#backupModalExport (button inside modal)"
      to: "BackupManager.exportEncryptedBackup → exportBackup fallback"
      via: "click handler that mirrors current exportBtn handler at overview.js:66-94"
      pattern: "BackupManager\\.exportEncryptedBackup"
    - from: "#backupModalShare (button inside modal)"
      to: "BackupManager.shareBackup(blob, filename)"
      via: "called AFTER export resolves with the user's chosen blob (skip-encryption path only in Plan 02; encrypted path enabled in Plan 08)"
      pattern: "BackupManager\\.shareBackup"
    - from: "Modal subtitle"
      to: "localStorage.portfolioLastExport"
      via: "renderLastBackupSubtitle() called when openBackupModal() runs"
      pattern: "localStorage\\.getItem\\(\\s*['\"]portfolioLastExport"
    - from: "#backupCloudBtn (initial mount class)"
      to: "BackupManager.computeBackupRecencyState()"
      via: "mountBackupCloudButton reads the helper to assign the initial backup-cloud-btn--{state} class; Plan 04 owns the post-mount update wiring"
      pattern: "computeBackupRecencyState\\("
---

<objective>
Move the backup entry point OUT of the overview body and INTO the top header as a circular cloud icon button. The 5-button overview cluster collapses to exactly `[Add Client] [Add Session]` — every other backup affordance (Export / Import / Send / Auto-backup / Send-to-Myself) is removed. The new cloud icon is mounted into `#headerActions` BEFORE the settings gear and is the single entry point to the unified Backup & Restore modal that hosts Export (prominent), Import (secondary), and a Test-password sub-card placeholder. The modal also surfaces the backup contents checklist (D-09) and the last-backup-at indicator (D-10).

Purpose: Backup is admin work that should be reachable from anywhere, not occupy primary content real-estate (D-08 updated 2026-05-15). The header icon doubles as the always-visible status surface — its background color (Plan 04) encodes recency. One tap target, one status surface, no separate chip element. D-05/D-06/D-07 collapse the cluster; D-09/D-10/D-26 raise the bar on in-modal explanation and habit reinforcement.

Output: One new modal (markup + CSS), one new header icon button mounted on every page via `App.mountBackupCloudButton`, the existing Send / Auto-backup buttons and their handlers removed, the existing Export / Import / Send-to-Myself overview buttons and handlers removed (their behavior moved into modal handlers — no behavior change to the encrypt-aware export path or the replace-on-import flow), nine new i18n keys in 4 locales, and a single-source-of-truth contents key list backed by a parity test (closes D-09 + D-29).

Honest-body principle (D-02): the encrypted-then-share UX is intentionally deferred to Plan 08 — Plan 02 ships with the Share button HIDDEN after an encrypted export completes (rather than visible-but-broken). Plan 08's refactor of `exportEncryptedBackup` to return the encrypted blob+filename is what enables the encrypted-share path; until that lands, the Share button is gated to the skip-encryption path only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-backup-architectural-rework/25-CONTEXT.md
@.planning/phases/25-backup-architectural-rework/25-RESEARCH.md
@.planning/phases/25-backup-architectural-rework/25-UI-SPEC.md
@index.html
@assets/overview.js
@assets/app.js
@assets/backup.js
@assets/app.css
@assets/i18n-en.js

<interfaces>
<!-- Existing identifiers the executor will use directly. -->

From assets/backup.js (Plan 01 ships shareBackup; this plan adds BACKUP_CONTENTS_KEYS + computeBackupRecencyState):
  - exportBackup() -> Promise<{ blob, filename }>
  - exportEncryptedBackup() -> Promise<true | false | 'cancel'>
  - triggerDownload(blob, filename) -> void
  - importBackup(file) -> Promise<manifest>
  - shareBackup(blob, filename) -> Promise<{ ok, via }>     (NEW from Plan 01)
  - isShareSupported(file) -> boolean                       (NEW from Plan 01)
  - isAutoBackupSupported() -> boolean                      (still exists; D-11 keeps the picker — moved to Plan 05's Settings tab)
  - isAutoBackupActive() -> boolean
  - autoSaveToFolder(blob, filename) -> Promise<boolean>
  - computeBackupRecencyState() -> 'never' | 'fresh' | 'warning' | 'danger'   (NEW in this plan; pure helper that reads localStorage.portfolioLastExport + getScheduleIntervalMs heuristic. Plan 04 ships the FULL pure-function variant `getChipState({ now, lastExport, intervalMs })` plus `getScheduleIntervalMs()`; this thin wrapper exists in Plan 02 only so the cloud icon can be assigned an initial class at mount. Plan 04 will replace the Plan-02 stub body with a delegating call: `return getChipState({ now: Date.now(), lastExport: Number(localStorage.getItem('portfolioLastExport')), intervalMs: getScheduleIntervalMs() });`. For Plan 02 alone — before Plan 04 ships — the body uses a minimal inline default: schedule OFF only, fresh ≤7 days, warning ≤14, danger >14, never if no key. This deliberate temporary stub lets the icon land in Wave 2 without depending on Wave 4.)

From assets/app.js (already wired, do not duplicate):
  - App.confirmDialog({ titleKey, messageKey, confirmKey, cancelKey, tone }) -> Promise<boolean>   (line 550)
  - App.showToast(message, key) -> void                                                              (line 532)
  - App.lockBodyScroll() / App.unlockBodyScroll() -> void
  - App.t(key) -> string
  - App.applyTranslations(rootEl) -> void
  - initSettingsLink() at line 331 — model for mountBackupCloudButton (same insertion-target pattern, same hot-reload guard, same language-change re-translation listener pattern)
  - initCommon() — currently calls initSettingsLink; Plan 02 adds a call to mountBackupCloudButton here so the icon mounts on every page

From assets/overview.js — handlers that MOVE into modal handlers (lines 66-150):
  - exportBtn click handler (lines 66-95) — preserve all behavior; relocate to backupModalExport handler
  - importInput change handler (lines 97-127) — preserve all behavior; relocate to backupModalImport handler
  - sendBackupBtn handler (lines 129-139) — DELETE entirely (Plan 01 removed sendToMyself; the new Share path is handled by the new modal Share button which calls BackupManager.shareBackup with the just-exported blob)
  - autoBackupBtn handler (lines 141-150) — DELETE entirely (D-11 moves the folder picker to Settings → Backups tab, which Plan 05 builds)

From index.html — markup that gets replaced/added:
  - The entire `.inline-actions` div at lines 99-115 has its children REPLACED. KEEP the wrapping `<div class="inline-actions" style="margin-bottom:1rem;">` (RTL-safe, already audited Phase 16-02; the two remaining buttons still need a flex row).
  - Replacement children (EXACTLY these two, in this order): #addClientBtn (UNCHANGED), #addSessionBtn (UNCHANGED). NO other action buttons. NO chip slot. NO Backup & Restore button on the overview body.
  - The `#headerActions` container at line 65 is UNCHANGED in markup (it remains an empty `<div class="header-actions" id="headerActions"></div>`); the cloud icon is JS-mounted into it by `App.mountBackupCloudButton` (mirrors how the gear is mounted by `initSettingsLink` at app.js:331).

The cloud icon button to be JS-created (canonical contract for executor):
  <button type="button"
          id="backupCloudBtn"
          class="header-icon-btn backup-cloud-btn backup-cloud-btn--never"
          aria-label="{ t('overview.backupRestore') }"
          title="{ t('overview.chip.lastBackup') } · { formatRelativeTime(...) || t('overview.chip.never') }">
    <!-- inline cloud SVG, single-color via currentColor -->
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
         stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <!-- cloud outline glyph -->
      <path d="M17 18 H7 a4 4 0 0 1 -1 -7.9 6 6 0 0 1 11.6 -1.5 4 4 0 0 1 -.6 9.4 z"/>
    </svg>
  </button>

  Notes:
  - Touch target: 44×44 (Phase 21 MOB-04). The SVG renders at 20×20 inside the 44×44 button.
  - Initial state class is computed at mount from BackupManager.computeBackupRecencyState() (returns one of 'never'|'fresh'|'warning'|'danger'); the Plan-02 mount applies `backup-cloud-btn--{state}`.
  - The title attribute is the textual equivalent of the color signal (a11y safe). The aria-label is the entry-point label only ("Backup & Restore"); the title carries the recency text. This way screen-readers hear the button purpose without the dynamic time string interrupting navigation, while sighted hover users still see the time.
  - Note on i18n keys: `overview.chip.lastBackup` and `overview.chip.never` are SHIPPED IN PLAN 04 (they belong to the recency-text wiring). Plan 02's `mountBackupCloudButton` constructs the title using a guarded lookup — if the keys are missing (Plan 04 not yet shipped), it falls back to a literal "Last backup · never" / "Last backup · {relative}" so the icon never renders with an undefined-key string. Plan 04 lands the localized keys; the title then resolves correctly.
  - Click: BackupManager / App-level → opens the unified modal via `window.openBackupModal()` (overview.js still owns the modal handlers; the global is exposed for cross-page invocation since the icon lives on every page but the modal markup ships in index.html for now — pages other than overview.html will see the icon, click it, and be navigated to overview.html?openBackup=1 OR have the modal hoisted later. Plan 02's scope: overview.html only — the icon mount is global but the click handler on non-overview pages routes to `./index.html?openBackup=1`; overview.js reads the query param on load and auto-opens the modal. This is the single deviation from "modal lives in DOM on every page" — minimal, deferrable to Plan 06 if it becomes a problem.)

Existing CSS classes the modal composes from (do NOT redefine):
  - .modal, .modal-overlay, .modal-card, .modal-close (lines 1500-1600 in app.css)
  - .button, .button.secondary, .button.ghost, .button.danger, .button.icon-swap
  - .label, .helper-text, .section-title, .section-helper
  - .success-text (the checkmark pattern used by the contents list)
  - .form-field
  - .inline-actions (the existing flex container — keep as-is, replace children only)
  - .header-control-btn (existing 36×36 circular header control used by globe/theme/gear; the new .header-icon-btn is a parallel 44×44 variant introduced in this plan — DO NOT modify .header-control-btn)

Backup contents checklist source-of-truth (D-09 + D-29):
  Add to assets/backup.js (inside the IIFE, alongside the public API):
    var BACKUP_CONTENTS_KEYS = ['clients', 'sessions', 'snippets', 'therapistSettings', 'photos'];
  Expose on the public API: `BACKUP_CONTENTS_KEYS: BACKUP_CONTENTS_KEYS,`
  This same array is consumed by Plan 08's round-trip test as the canonical list of stores that must round-trip losslessly.

i18n keys to ADD in this plan (16 keys × 4 locales = 64 lines). NOTE the key `overview.backupRestore` is now used for the cloud icon's `aria-label` AND the modal title — there is NO visible button text on the overview cluster anymore.

  - overview.backupRestore  → "Backup & Restore" / "גיבוי ושחזור" / "Backup & Wiederherstellung" / "Záloha a obnova"
        Used as: aria-label of #backupCloudBtn AND the modal title's data-i18n value.
  - backup.modal.title      → "Backup & Restore" / "גיבוי ושחזור" / "Backup & Wiederherstellung" / "Záloha a obnova"
  - backup.modal.lastBackup → "Last backup: {relative}" / "גיבוי אחרון: {relative}" / "Letztes Backup: {relative}" / "Poslední záloha: {relative}"
  - backup.modal.lastBackupNever → "Last backup: never" / "גיבוי אחרון: מעולם לא" / "Letztes Backup: nie" / "Poslední záloha: nikdy"
  - backup.contents.heading → "What's in your backup" / "מה כלול בגיבוי" / "Was im Backup enthalten ist" / "Co je v záloze"
  - backup.contents.helper  → "Every export includes everything below. Restoring replaces all current data with this list." / (HE/DE/CS — see Task 1 string list)
  - backup.contents.item.clients   → "Clients" / "לקוחות" / "Klienten" / "Klienti"
  - backup.contents.item.sessions  → "Sessions" / "מפגשים" / "Sitzungen" / "Sezení"
  - backup.contents.item.snippets  → "Snippets" / "קטעי טקסט" / "Textbausteine" / "Úryvky"
  - backup.contents.item.settings  → "Settings" / "הגדרות" / "Einstellungen" / "Nastavení"
  - backup.contents.item.photos    → "Photos (cropped, optimized)" / "תמונות (חתוכות, ממוטבות)" / "Fotos (zugeschnitten, optimiert)" / "Fotky (oříznuté, optimalizované)"
  - backup.export.heading   → "Export backup" / "ייצוא גיבוי" / "Backup exportieren" / "Exportovat zálohu"
  - backup.export.helper    → "Save a copy of all your data. Encrypt it with a password (recommended), or save it unprotected." / (4 locales)
  - backup.action.export    → "Export backup" / "ייצוא גיבוי" / "Backup exportieren" / "Exportovat zálohu"
  - backup.import.heading   → "Import backup" / "ייבוא גיבוי" / "Backup importieren" / "Importovat zálohu"
  - backup.import.helper    → "Restoring replaces all current data with the contents of the backup file. This cannot be undone." / (4 locales)
  - backup.import.warning   → "⚠ Replaces all current data" / "⚠ מחליף את כל הנתונים הנוכחיים" / "⚠ Ersetzt alle aktuellen Daten" / "⚠ Nahradí všechna aktuální data"
  - backup.action.import    → "Choose backup file" / "בחירת קובץ גיבוי" / "Backup-Datei wählen" / "Vybrat soubor zálohy"
  - backup.modal.scheduleFooter → "Set up a schedule in Settings → Backups so you don't have to remember." / (4 locales)

Hebrew strings use noun/infinitive (D-27); the schedule footer uses gender-neutral phrasing matching Phase 18 convention.

Relative-time formatter (NEW small helper inside overview.js):
  function formatRelativeTime(timestampMs) — returns localized "3 days ago" / "1 hour ago" / "moments ago" / null when timestamp is missing.
  Use Intl.RelativeTimeFormat(localStorage.getItem('portfolioLang') || 'en', { numeric: 'auto' }).
  Map elapsed ms to the largest unit (day if >24h; hour if >1h; minute otherwise) and call rtf.format(-n, unit).
  When localStorage.portfolioLastExport is absent or NaN → return null and the renderer uses backup.modal.lastBackupNever.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse overview cluster, ship Backup & Restore modal markup, base CSS (.header-icon-btn + .backup-cloud-btn base; modal classes), i18n strings, and the BACKUP_CONTENTS_KEYS + computeBackupRecencyState source of truth</name>
  <read_first>
    - index.html lines 47-66 (.app-header markup; #headerActions is at line 65 — the canonical insertion target for the cloud icon)
    - index.html lines 95-185 (the inline-actions cluster being collapsed + the existing #clientModal pattern to mirror modal markup)
    - assets/app.css lines 120-145 (existing .header-control-btn — DO NOT modify; the new .header-icon-btn is a parallel 44×44 variant)
    - assets/app.css lines 1100-1200 (existing .inline-actions); lines 1500-1620 (existing .modal/.modal-card/.modal-close patterns); lines 3500-3520 (existing .settings-tab pattern for tab styling reference)
    - assets/backup.js lines 506-606 (exportBackup — to align BACKUP_CONTENTS_KEYS with the manifest top-level keys: clients, sessions, snippets, therapistSettings, plus 'photos' which lives as files inside the ZIP rather than in the manifest)
    - assets/overview.js lines 50-150 (current handlers being moved/removed)
    - assets/app.js lines 320-395 (initSettingsLink — the canonical model for the new mountBackupCloudButton helper; same insertion-target pattern, same double-mount guard, same language-event re-translation listener)
    - assets/i18n-en.js lines 25-35, 165-175, 240-265, 305-315 (existing key positions for ordering)
    - .planning/phases/25-backup-architectural-rework/25-UI-SPEC.md "Layout Specifics" → "Header entry point (D-08, updated 2026-05-15)" + "Backup & Restore modal layout" + "Component Inventory" (exact CSS classes — note `.header-icon-btn` + `.backup-cloud-btn`)
    - .planning/phases/25-backup-architectural-rework/25-CONTEXT.md (D-05..D-10, D-13, D-14, D-26, D-29)
    - tests/24-05-import-validator.test.js (vm sandbox + assert pattern for the parity test)
  </read_first>
  <behavior>
    RED — write the structure + parity tests first:

    tests/25-02-checklist-store-parity.test.js
    - Loads assets/backup.js in a vm sandbox.
    - Reads sandbox.window.BackupManager.BACKUP_CONTENTS_KEYS.
    - Asserts: BACKUP_CONTENTS_KEYS contains exactly ['clients', 'sessions', 'snippets', 'therapistSettings', 'photos'] (any order; use Set equality).
    - Asserts: every key in BACKUP_CONTENTS_KEYS that is also a top-level manifest store appears in the exportBackup manifest object literal source. Implementation: read assets/backup.js as text, extract the manifest object literal block (between `var manifest = {` at line ~576 and the closing `};` ~6 lines later), and assert that for each store-key in BACKUP_CONTENTS_KEYS except 'photos', the manifest source contains the literal key followed by a colon (e.g. /\bclients:/). 'photos' is checked separately by asserting `photosFolder` appears in exportBackup's body.
    - Asserts: BackupManager.computeBackupRecencyState is a function (typeof check). With localStorage cleared it returns 'never'.
    - Exits 0 on full pass, 1 on any failure.

    tests/25-02-modal-structure.test.js
    - Reads index.html as text (no vm sandbox needed for these checks).
    - Asserts the file does NOT contain id="sendBackupBtn", id="autoBackupBtn", id="exportBtn", id="importInput", or id="backupRestoreBtn" (the overview-body Backup & Restore button is GONE — the entry point now lives in the header).
    - Asserts the file CONTAINS id="backupModal", id="backupModalExport", id="backupModalShare", id="backupModalImportInput", id="backupModalScheduleLink", id="backupModalLastBackup", id="backupModalClose".
    - Asserts the `<div class="header-actions" id="headerActions"></div>` container is present (the cloud icon is JS-mounted, not authored in markup).
    - Asserts the `.inline-actions` block at line ~99 contains EXACTLY two button children with ids #addClientBtn and #addSessionBtn — no other ids inside .inline-actions. Implementation: extract the substring from the opening `<div class="inline-actions"` to its matching closing `</div>` and assert that `id="addClientBtn"` and `id="addSessionBtn"` each appear exactly once and that no other `id="..."` attribute appears in that substring.
    - Asserts the file contains data-i18n="backup.modal.title", data-i18n="backup.contents.heading", data-i18n="backup.export.heading", data-i18n="backup.import.heading", data-i18n="backup.import.warning".
    - Asserts the modal contains exactly 5 elements with class="backup-contents-item" (one per store key).
    - Loads assets/app.js in a vm sandbox and asserts `typeof App.mountBackupCloudButton === 'function'` (the helper is exposed on App so cross-page initCommon can call it). Also asserts that grepping assets/app.js source shows mountBackupCloudButton being called from initCommon (or wherever initSettingsLink is called) — single grep against the file text for `mountBackupCloudButton(`.

    GREEN — make all assertions pass via the implementation steps below.

    1. assets/backup.js — add the source-of-truth constant + the recency-state helper near the public API:
       Inside the IIFE, BEFORE the return statement at line ~997, add:
         var BACKUP_CONTENTS_KEYS = ['clients', 'sessions', 'snippets', 'therapistSettings', 'photos'];

         function computeBackupRecencyState() {
           // Plan 02 stub: schedule OFF only (Plan 04 replaces this body to delegate to getChipState
           // with intervalMs from getScheduleIntervalMs — the full coupled-to-schedule logic).
           // Until Plan 04 lands, this stub gives the cloud icon a sensible initial color at mount.
           try {
             var raw = localStorage.getItem('portfolioLastExport');
             if (!raw) return 'never';
             var ts = Number(raw);
             if (isNaN(ts)) return 'never';
             var elapsed = Date.now() - ts;
             var DAY = 24 * 60 * 60 * 1000;
             if (elapsed <= 7 * DAY) return 'fresh';
             if (elapsed <= 14 * DAY) return 'warning';
             return 'danger';
           } catch (_) {
             return 'never';
           }
         }

       Add to the public-API return:
         `BACKUP_CONTENTS_KEYS: BACKUP_CONTENTS_KEYS,`
         `computeBackupRecencyState: computeBackupRecencyState,`

    2. index.html — REPLACE the children of the existing `.inline-actions` block at lines 99-115:
       KEEP the outer `<div class="inline-actions" style="margin-bottom:1rem;">` wrapper.
       New cluster children (EXACTLY two, in this order):
       - #addClientBtn (unchanged — copy from current line 100-103)
       - #addSessionBtn (unchanged — copy from current line 104-107)

       REMOVE: exportBtn, importInput label wrapper, sendBackupBtn, autoBackupBtn, AND the old `<input id="importInput">`. Do NOT add any Backup & Restore button into .inline-actions — the entry point lives in the header now.

       APPEND a new modal block after the closing `</div>` of `.app-shell` and before any other modal in index.html (mirror the placement style of #clientModal at lines 185+):

       <div id="backupModal" class="modal is-hidden" role="dialog" aria-modal="true" aria-labelledby="backupModalTitle">
         <div class="modal-overlay"></div>
         <div class="modal-card backup-modal-card">
           <button class="modal-close" type="button" aria-label="Close" id="backupModalClose"></button>

           <div class="backup-modal-header">
             <h2 id="backupModalTitle" class="backup-modal-title" data-i18n="backup.modal.title">Backup & Restore</h2>
             <p class="backup-modal-subtitle" id="backupModalLastBackup" data-i18n="backup.modal.lastBackupNever">Last backup: never</p>
           </div>

           <section class="backup-modal-section backup-modal-section--contents">
             <h3 class="backup-modal-section-heading" data-i18n="backup.contents.heading">What's in your backup</h3>
             <p class="backup-modal-section-helper helper-text" data-i18n="backup.contents.helper">Every export includes everything below. Restoring replaces all current data with this list.</p>
             <ul class="backup-contents-list">
               <li class="backup-contents-item"><span class="backup-contents-check" aria-hidden="true">✓</span> <span data-i18n="backup.contents.item.clients">Clients</span></li>
               <li class="backup-contents-item"><span class="backup-contents-check" aria-hidden="true">✓</span> <span data-i18n="backup.contents.item.sessions">Sessions</span></li>
               <li class="backup-contents-item"><span class="backup-contents-check" aria-hidden="true">✓</span> <span data-i18n="backup.contents.item.snippets">Snippets</span></li>
               <li class="backup-contents-item"><span class="backup-contents-check" aria-hidden="true">✓</span> <span data-i18n="backup.contents.item.settings">Settings</span></li>
               <li class="backup-contents-item"><span class="backup-contents-check" aria-hidden="true">✓</span> <span data-i18n="backup.contents.item.photos">Photos (cropped, optimized)</span></li>
             </ul>
           </section>

           <section class="backup-modal-section backup-modal-section--export">
             <h3 class="backup-modal-section-heading" data-i18n="backup.export.heading">Export backup</h3>
             <p class="backup-modal-section-helper helper-text" data-i18n="backup.export.helper">Save a copy of all your data. Encrypt it with a password (recommended), or save it unprotected.</p>
             <div class="backup-modal-button-row">
               <button type="button" class="button" id="backupModalExport" data-i18n="backup.action.export">Export backup</button>
               <button type="button" class="button ghost is-hidden" id="backupModalShare" data-i18n="backup.action.share">Share backup</button>
             </div>
           </section>

           <section class="backup-modal-section backup-modal-section--import">
             <h3 class="backup-modal-section-heading" data-i18n="backup.import.heading">Import backup</h3>
             <p class="backup-modal-section-helper helper-text" data-i18n="backup.import.helper">Restoring replaces all current data with the contents of the backup file. This cannot be undone.</p>
             <div class="backup-modal-import-warning" role="alert">
               <span data-i18n="backup.import.warning">⚠ Replaces all current data</span>
             </div>
             <div class="backup-modal-button-row">
               <label class="button ghost import-label">
                 <span data-i18n="backup.action.import">Choose backup file</span>
                 <input id="backupModalImportInput" type="file" accept=".zip,.json,.sgbackup" class="is-hidden" />
               </label>
             </div>
           </section>

           <section class="backup-modal-section backup-modal-section--test" id="backupModalTestPasswordSection">
             <!-- Test-password sub-card markup is wired in Plan 03; this section is intentionally empty for now -->
           </section>

           <footer class="backup-modal-footer">
             <a href="./settings.html?tab=backups" id="backupModalScheduleLink" class="text-link" data-i18n="backup.modal.scheduleFooter">Set up a schedule in Settings → Backups so you don't have to remember.</a>
           </footer>
         </div>
       </div>

       The `#headerActions` container at line 65 is UNCHANGED in markup. The cloud icon is JS-mounted by mountBackupCloudButton (Task 2 of this plan).

    3. assets/app.css — append a new section at the end of the file with a `/* Phase 25 — Backup & Restore (header icon + modal) */` band comment. Define:

       /* Header cloud icon button — 44×44 circular touch target; sibling of .header-control-btn (36×36).
          Plan 02 ships ONLY the base + the --never default. Plan 04 ships the four state-color modifiers
          (.backup-cloud-btn--fresh / --warning / --danger and the override of --never's exact tokens). */
       .header-icon-btn {
         width: 44px;
         height: 44px;
         border-radius: 50%;
         display: inline-flex;
         align-items: center;
         justify-content: center;
         padding: 0;
         border: 1px solid transparent;
         background: transparent;
         color: var(--color-text);
         cursor: pointer;
         transition: background-color 0.15s, color 0.15s, border-color 0.15s;
       }
       .header-icon-btn:focus-visible {
         outline: 2px solid var(--color-primary);
         outline-offset: 2px;
       }
       .header-icon-btn svg { display: block; }

       .backup-cloud-btn { /* cloud-specific overrides (none beyond base for now); Plan 04 adds state colors */ }
       .backup-cloud-btn--never {
         background-color: var(--color-surface-subtle, var(--color-surface-alt));
         color: var(--color-text-muted);
         border-color: var(--color-border);
       }

       /* Modal — Phase 25 Backup & Restore */
       .backup-modal-card { max-width: 560px; max-height: 90dvh; overflow-y: auto; padding: var(--space-xl, 32px); }
       .backup-modal-header { margin-bottom: var(--space-lg, 24px); }
       .backup-modal-title { font-size: 1.75rem; font-weight: 700; line-height: 1.2; margin: 0 0 var(--space-xs, 4px) 0; }
       .backup-modal-subtitle { font-size: 0.875rem; font-weight: 700; color: var(--color-text-muted, #666); margin: 0; }
       .backup-modal-section { padding: var(--space-md, 16px) 0; }
       .backup-modal-section + .backup-modal-section { border-block-start: 1px solid var(--color-border-soft, rgba(0,0,0,0.08)); }
       .backup-modal-section--contents { background: var(--color-surface-alt, #fdf7ed); border-radius: 12px; padding: var(--space-md, 16px); }
       .backup-modal-section--export { padding-top: var(--space-lg, 24px); }
       .backup-modal-section--import .backup-modal-section-heading { font-size: 1.125rem; }  /* smaller than 22px export heading per UI-SPEC */
       .backup-modal-section-heading { font-size: 1.4rem; font-weight: 700; line-height: 1.2; margin: 0 0 var(--space-sm, 8px) 0; }
       .backup-modal-section-helper { margin: 0 0 var(--space-md, 16px) 0; }
       .backup-modal-button-row { display: flex; gap: var(--space-sm, 8px); flex-wrap: wrap; }
       .backup-modal-import-warning { background: var(--color-danger-bg, #f8d7da); color: var(--color-danger-text, #721c24); border-inline-start: 4px solid var(--color-danger, #ea4b4b); padding: var(--space-sm, 8px) var(--space-md, 16px); margin-bottom: var(--space-md, 16px); border-radius: 4px; font-weight: 700; }
       .backup-contents-list { list-style: none; padding: 0; margin: 0; }
       .backup-contents-item { display: flex; align-items: center; gap: var(--space-xs, 4px); padding: var(--space-xs, 4px) 0; font-weight: 700; }
       .backup-contents-check { color: var(--color-success-text, #1e6b3a); font-weight: 700; }
       .backup-modal-footer { margin-top: var(--space-lg, 24px); padding-top: var(--space-md, 16px); border-block-start: 1px solid var(--color-border-soft, rgba(0,0,0,0.08)); }

    4. assets/i18n-{en,he,de,cs}.js — add all 19 keys listed in the <interfaces> table above (overview.backupRestore, backup.modal.title, backup.modal.lastBackup, backup.modal.lastBackupNever, backup.contents.heading, backup.contents.helper, 5× backup.contents.item.*, backup.export.heading, backup.export.helper, backup.action.export, backup.action.share, backup.import.heading, backup.import.helper, backup.import.warning, backup.action.import, backup.modal.scheduleFooter). Place them adjacent to the existing backup.* block (around line 250-275 in EN). Do NOT remove `overview.export` or `overview.import` yet — Plan 02 itself supersedes their UI use, but other code paths (the existing 7-day banner at app.js:737-816) reference backup.banner.* keys and may surface "Export" again in error messaging. The two existing keys are now functionally orphaned for the cluster but harmless; a sweep can happen in Plan 08's single-source refactor.

       NOTE on key reuse: `overview.backupRestore` is now the i18n source for TWO surfaces — (1) the cloud icon's `aria-label` (no visible text), and (2) the modal title's `data-i18n` (visible text inside the modal). Same string, two consumers, single key. This is intentional.

    Do NOT yet wire JS handlers — Task 2 of this plan does that. The DOM and string layer must be complete first so the parity + structure tests pass before any handler logic touches it.
  </behavior>
  <action>
    Implement steps 1-4 above exactly. Place the new modal markup right after the closing `</div>` that wraps `.app-shell` (after line ~270 in index.html, BEFORE the closing `</body>`). Place the new CSS at the bottom of assets/app.css with a `/* Phase 25 — Backup & Restore (header icon + modal) */` band comment.

    Honor the locked CSS conventions: use the existing semantic tokens (--color-surface-alt, --color-surface-subtle if defined else fallback to --color-surface-alt, --color-text-muted, --color-border, --color-success-text, --color-danger-bg, --color-danger-text, --color-danger, --color-border-soft, --space-xs/sm/md/lg/xl) — do NOT introduce new colors or new spacing values. UI-SPEC.md "Color" section is the authority. Do NOT modify the existing `.header-control-btn` rules — `.header-icon-btn` is a parallel class with a larger 44×44 footprint and a transparent default background (color is the load-bearing signal — Plan 04 adds the state-color modifiers).

    For all 19 i18n keys: insert in EXACT key-name order (consistent across all 4 files). Hebrew uses noun/infinitive forms throughout (e.g., "ייצוא גיבוי" not "ייצא גיבוי", "בחירת קובץ גיבוי" not "בחר קובץ גיבוי"). German uses "Backup" loanword (already established in existing keys). Czech uses formal infinitives ("Exportovat zálohu").

    The {relative} placeholder in `backup.modal.lastBackup` is a literal string substituted at render time by Task 2 (.replace('{relative}', formattedString)).
  </action>
  <verify>
    <automated>node tests/25-02-modal-structure.test.js</automated>
    <automated>node tests/25-02-checklist-store-parity.test.js</automated>
    <automated>grep -E "BACKUP_CONTENTS_KEYS\s*:\s*BACKUP_CONTENTS_KEYS" assets/backup.js</automated>
    <automated>grep -E "computeBackupRecencyState\s*:\s*computeBackupRecencyState" assets/backup.js</automated>
    <automated>for L in en he de cs; do for K in "overview.backupRestore" "backup.modal.title" "backup.contents.heading" "backup.export.heading" "backup.import.heading" "backup.import.warning" "backup.action.export" "backup.action.share" "backup.action.import" "backup.modal.scheduleFooter"; do grep -F "\"$K\":" "assets/i18n-$L.js" >/dev/null || { echo "MISSING $K in $L"; exit 1; }; done; done; echo ALL_KEYS_PRESENT</automated>
    <automated>grep -v '^#' index.html | grep -c 'id="sendBackupBtn"\|id="autoBackupBtn"\|id="exportBtn"\|id="backupRestoreBtn"\|id="importInput"' | grep -qx 0 && echo OLD_IDS_PURGED</automated>
    <automated>grep -c "backup-contents-item" index.html | grep -qE "^[5-9]$|^[1-9][0-9]+$" && echo CONTENTS_ITEMS_PRESENT</automated>
    <automated>grep -E '^\.header-icon-btn\b|^\.backup-cloud-btn(--never)?\b' assets/app.css | wc -l | grep -qE "^[3-9]$|^[1-9][0-9]+$" && echo HEADER_ICON_BASE_CSS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - tests/25-02-modal-structure.test.js exits 0.
    - tests/25-02-checklist-store-parity.test.js exits 0.
    - assets/backup.js public API includes `BACKUP_CONTENTS_KEYS` AND `computeBackupRecencyState` (verified by grep).
    - index.html does NOT contain id="sendBackupBtn", id="autoBackupBtn", id="exportBtn", id="backupRestoreBtn", or id="importInput" (grep returns 0).
    - index.html DOES contain id="backupModal", id="backupModalExport", id="backupModalShare", id="backupModalImportInput", id="backupModalScheduleLink", id="backupModalLastBackup", id="backupModalClose" (grep returns 1+ for each).
    - The `.inline-actions` block at line ~99 contains EXACTLY two button children: #addClientBtn and #addSessionBtn — no other action buttons (verified by the structure test substring scan).
    - The `<div class="header-actions" id="headerActions"></div>` container at line 65 is unchanged in markup (the cloud icon is JS-mounted, not authored in HTML).
    - index.html contains exactly 5 occurrences of class="backup-contents-item".
    - All 19 new i18n keys appear in all 4 locale files (the verify grep loop checks the 10 most distinctive ones).
    - assets/app.css contains the `.header-icon-btn`, `.backup-cloud-btn--never`, `.backup-modal-card`, `.backup-modal-section--export`, `.backup-modal-section--import`, `.backup-modal-import-warning`, `.backup-contents-list`, `.backup-contents-item` selectors (grep each).
    - The `.header-icon-btn` CSS uses 44px width/height (Phase 21 MOB-04 minimum touch target) — not 36px; this is the explicit divergence from `.header-control-btn`.
    - No new color literals introduced — every CSS color value uses var(--color-*) or var() fallback to a token (verifiable via `grep -E "(background|color|border):\s*#[0-9a-fA-F]" assets/app.css` showing no NEW lines with hex literals in the Phase 25 section; pre-existing rules are out of scope).
    - The Hebrew strings use noun/infinitive forms (manual review during checker pass; spot check `ייצוא גיבוי` and `בחירת קובץ גיבוי` are present, NOT `ייצא` or `בחר` imperatives).
  </acceptance_criteria>
  <done>
    The 5-button overview cluster is collapsed to exactly Add Client + Add Session. The Backup & Restore modal exists in the DOM, hidden, with all required IDs and the static contents checklist. CSS gives the modal its UI-SPEC layout (Export prominent, Import secondary, destructive warning callout, contents block with checkmarks). The base `.header-icon-btn` 44×44 circular button class and `.backup-cloud-btn--never` default state class are defined (state-color modifiers ship in Plan 04). 19 i18n keys ship in 4 locales. The single-source-of-truth BACKUP_CONTENTS_KEYS is exported and the parity test enforces D-29's "must update whenever a new IDB store is added" rule programmatically. BackupManager.computeBackupRecencyState is exposed (Plan-02 stub body; Plan 04 replaces with the full schedule-coupled variant).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the cloud icon into #headerActions on every page (App.mountBackupCloudButton in app.js, called from initCommon); wire the modal handlers in overview.js (open/close, last-backup subtitle, Export, Share probe, Import); honor honest-body principle (hide Share on encrypted-export); remove old overview handlers; expose window.openBackupModal + window.renderLastBackupSubtitle</name>
  <read_first>
    - assets/app.js lines 320-395 (initSettingsLink — model for mountBackupCloudButton; same insertion-target pattern + double-mount guard + language-event re-translation)
    - assets/app.js lines 397-510 (initCommon — the canonical call site; mountBackupCloudButton lands alongside initSettingsLink)
    - assets/overview.js full file (especially lines 50-150 for the handlers being moved/deleted)
    - assets/app.js lines 532-650 (App.confirmDialog signature, App.showToast, App.lockBodyScroll/unlockBodyScroll)
    - assets/backup.js lines 643-671 (exportEncryptedBackup return contract: true/false/'cancel')
    - assets/backup.js public API after Task 1 (now includes shareBackup, isShareSupported, BACKUP_CONTENTS_KEYS, computeBackupRecencyState)
    - .planning/phases/25-backup-architectural-rework/25-UI-SPEC.md "Layout Specifics" → "Header entry point (D-08, updated 2026-05-15)" (markup contract for the icon) + "Backup & Restore modal layout" (vertical stack 1-7 — defines exact behavior of each button)
    - .planning/phases/25-backup-architectural-rework/25-CONTEXT.md (D-02 honest body, D-04, D-05, D-06, D-07, D-08, D-10, D-13, D-14)
    - index.html (post-Task-1) — confirm IDs match the handler names you wire
  </read_first>
  <behavior>
    Tests for this task land in Plan 04 (the full state-update behavior test for the icon's color class plus the banner-suppression behavior test). Plan 02 Task 2's verification is via:
    - grep gates (below) confirming the helper exists, is called from initCommon, and the modal handlers are wired
    - manual smoke at the checker pass (clicking the icon opens the modal; clicking X / overlay / Esc closes it; Export/Import flows preserve their pre-Phase-25 behavior; subtitle renders correctly)
    - the structure test from Task 1 already asserts `typeof App.mountBackupCloudButton === 'function'` and that the source contains a call to mountBackupCloudButton from initCommon

    No RED test added in this task — all behavior gates (icon color class on mount, click → modal, recency-text in title) are subsumed by Plan 04's test suite.
  </behavior>
  <action>
    PART A — assets/app.js: add `mountBackupCloudButton` and call it from `initCommon`.

    A1. Add a new function `mountBackupCloudButton` IMMEDIATELY AFTER `initSettingsLink` (currently at app.js:331-395). Mirror the existing helper's structure exactly:

      /**
       * Phase 25 (D-08, updated 2026-05-15) — mount the Backup & Restore cloud icon button
       * into #headerActions BEFORE the existing settings gear, on every page that calls
       * initCommon. Idempotent: skips if a .backup-cloud-btn is already mounted.
       *
       * The icon is a 44×44 circular button (Phase 21 MOB-04 minimum touch target).
       * Initial state class is computed from BackupManager.computeBackupRecencyState()
       * — Plan 04 ships the post-mount state-update wiring (visibilitychange + post-export
       * hooks); this mount applies the state at first render only.
       *
       * On click: opens the unified Backup & Restore modal via window.openBackupModal()
       * if defined (overview.js exposes it). On non-overview pages where the modal markup
       * is not in the DOM, the click navigates to ./index.html?openBackup=1 — overview.js
       * reads the query param on load and auto-opens the modal. (This single deviation
       * from "modal lives in DOM on every page" is intentional and minimal; if it becomes
       * a problem the modal markup can be hoisted to a shared partial in a follow-up plan.)
       */
      function mountBackupCloudButton() {
        var actions = document.getElementById('headerActions') || document.querySelector('.header-actions');
        if (!actions) return;
        if (actions.querySelector('.backup-cloud-btn')) return;  // double-mount guard

        var btn = document.createElement('button');
        btn.type = 'button';
        btn.id = 'backupCloudBtn';

        // Initial state class — computeBackupRecencyState() returns 'never' | 'fresh' | 'warning' | 'danger'.
        // Defensive: BackupManager may not be loaded on legal/standalone pages; default to 'never'.
        var initialState = 'never';
        try {
          if (typeof BackupManager !== 'undefined' && typeof BackupManager.computeBackupRecencyState === 'function') {
            initialState = BackupManager.computeBackupRecencyState() || 'never';
          }
        } catch (_) { /* keep default */ }
        btn.className = 'header-icon-btn backup-cloud-btn backup-cloud-btn--' + initialState;

        // a11y: aria-label is the entry-point label; title carries the dynamic recency text.
        var ariaLabel = (typeof t === 'function' ? t('overview.backupRestore') : '') || 'Backup & Restore';
        btn.setAttribute('aria-label', ariaLabel);

        // Title (recency text). Plan 04 owns the localized recency keys; until those land,
        // fall back to a literal so the icon never shows undefined-key strings.
        var labelLastBackup = (typeof t === 'function' ? t('overview.chip.lastBackup') : '') || 'Last backup';
        var labelNever      = (typeof t === 'function' ? t('overview.chip.never')      : '') || 'never';
        var sep = ' · ';  // " · "
        var relText = labelNever;
        try {
          var raw = localStorage.getItem('portfolioLastExport');
          if (raw && typeof window.formatRelativeTime === 'function') {
            var rel = window.formatRelativeTime(Number(raw));
            if (rel) relText = rel;
          }
        } catch (_) { /* keep never */ }
        btn.setAttribute('title', labelLastBackup + sep + relText);

        // Inline cloud SVG — 20×20 inside the 44×44 button, single-color via currentColor.
        btn.innerHTML = ''
          + '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
          +   ' stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'
          + '<path d="M17 18 H7 a4 4 0 0 1 -1 -7.9 6 6 0 0 1 11.6 -1.5 4 4 0 0 1 -.6 9.4 z"/>'
          + '</svg>';

        // Click: open modal if available; else navigate to overview with auto-open query param.
        btn.addEventListener('click', function () {
          if (typeof window.openBackupModal === 'function') {
            window.openBackupModal();
          } else {
            window.location.href = './index.html?openBackup=1';
          }
        });

        // Insert BEFORE the settings gear (visual order in LTR: brand … cloud, gear).
        var gear = actions.querySelector('.settings-gear-btn');
        if (gear && gear.parentNode === actions) {
          actions.insertBefore(btn, gear);
        } else {
          // Gear not yet mounted (initSettingsLink runs after this) — insert at start so
          // the eventual gear lands AFTER the cloud regardless of mount order.
          actions.insertBefore(btn, actions.firstChild);
        }

        // Re-translate aria-label on language switch — registered exactly once.
        if (!mountBackupCloudButton._listenerInstalled) {
          document.addEventListener('app:language', function () {
            var existing = document.getElementById('backupCloudBtn');
            if (existing && typeof t === 'function') {
              existing.setAttribute('aria-label', t('overview.backupRestore') || 'Backup & Restore');
            }
          });
          mountBackupCloudButton._listenerInstalled = true;
        }
      }

    A2. In `initCommon` (around app.js:497-499 where `initSettingsLink()` is currently called), ADD a call to `mountBackupCloudButton()` IMMEDIATELY BEFORE `initSettingsLink()` so the cloud lands first and the gear's existing insertion logic stays untouched. Both helpers are idempotent and order-tolerant due to the gear-aware insertion in mountBackupCloudButton, but conventionally cloud-before-gear matches the LTR visual order.

    A3. Expose the helper on the App public API so the structure test in Task 1 can assert `typeof App.mountBackupCloudButton === 'function'`. Add to the App return object: `mountBackupCloudButton: mountBackupCloudButton,`.

    PART B — assets/overview.js: modal handlers, subtitle renderer, old-handler removal, query-param auto-open.

    1. DELETE lines 66-95 (the existing exportBtn click handler) — its behavior moves to a new openExportFlow() function.

    2. DELETE lines 97-127 (the existing importInput change handler) — its behavior moves to a new openImportFlow() function attached to #backupModalImportInput.

    3. DELETE lines 129-139 (sendBackupBtn handler — the button itself was removed in Task 1).

    4. DELETE lines 141-150 (autoBackupBtn handler — the button itself was removed in Task 1; folder picker moves to Plan 05).

    5. ADD a new helper at the MODULE TOP (before DOMContentLoaded; Plan 04 will rely on this hoisted location too):

       function formatRelativeTime(timestampMs) {
         if (!timestampMs || isNaN(timestampMs)) return null;
         const elapsed = Date.now() - timestampMs;
         const rtf = new Intl.RelativeTimeFormat(localStorage.getItem('portfolioLang') || 'en', { numeric: 'auto' });
         const dayMs = 24*60*60*1000;
         const hourMs = 60*60*1000;
         const minMs = 60*1000;
         if (elapsed >= dayMs) return rtf.format(-Math.floor(elapsed / dayMs), 'day');
         if (elapsed >= hourMs) return rtf.format(-Math.floor(elapsed / hourMs), 'hour');
         if (elapsed >= minMs) return rtf.format(-Math.floor(elapsed / minMs), 'minute');
         return rtf.format(-Math.floor(elapsed / 1000), 'second');
       }
       window.formatRelativeTime = formatRelativeTime;  // mountBackupCloudButton in app.js reads this

    6. ADD renderLastBackupSubtitle() that updates #backupModalLastBackup:

       function renderLastBackupSubtitle() {
         const el = document.getElementById('backupModalLastBackup');
         if (!el) return;
         const ts = Number(localStorage.getItem('portfolioLastExport'));
         const rel = formatRelativeTime(ts);
         if (rel === null) {
           el.setAttribute('data-i18n', 'backup.modal.lastBackupNever');
           el.textContent = App.t('backup.modal.lastBackupNever');
         } else {
           el.removeAttribute('data-i18n');  // dynamic content, no static i18n binding
           el.textContent = App.t('backup.modal.lastBackup').replace('{relative}', rel);
         }
       }

    7. ADD openBackupModal() / closeBackupModal():

       function openBackupModal() {
         const modal = document.getElementById('backupModal');
         if (!modal) return;
         renderLastBackupSubtitle();
         probeShareSupport();
         modal.classList.remove('is-hidden');
         App.lockBodyScroll();
         App.applyTranslations(modal);
       }

       function closeBackupModal() {
         const modal = document.getElementById('backupModal');
         if (!modal) return;
         modal.classList.add('is-hidden');
         App.unlockBodyScroll();
       }

    8. ADD probeShareSupport() that mirrors add-session.js:exportProbeShareSupport but uses BackupManager.isShareSupported with a probe File:

       function probeShareSupport() {
         const btn = document.getElementById('backupModalShare');
         if (!btn) return;
         try {
           const probe = new File([new Blob(['x'], { type: 'application/octet-stream' })], 'probe.sgbackup', { type: 'application/octet-stream' });
           if (BackupManager.isShareSupported(probe)) {
             btn.classList.remove('is-hidden');
           } else {
             btn.classList.add('is-hidden');
           }
         } catch (_) {
           btn.classList.add('is-hidden');
         }
       }

    9. ADD openExportFlow(opts) — `opts.afterExport` is an optional callback that receives `{ blob, filename }` so the Share button can chain after Export. Mirror the existing exportBtn behavior at the deleted lines 66-95 verbatim, but capture the resulting blob+filename for the optional after-export hook.

       HONEST-BODY PRINCIPLE (D-02): in the encrypted branch (encrypted === true), the function CANNOT produce a blob to chain into Share without re-prompting passphrase. Instead of leaving a visible-but-broken Share button, this plan EXPLICITLY HIDES the Share button after the encrypted-export "Done" state. Plan 08's refactor of exportEncryptedBackup to return the blob+filename is what re-enables the encrypted-share path.

       async function openExportFlow(opts) {
         opts = opts || {};
         const shareBtn = document.getElementById('backupModalShare');
         try {
           const encrypted = await BackupManager.exportEncryptedBackup();
           if (encrypted === 'cancel') return null;
           let producedBlob = null, producedFilename = null;
           if (encrypted === false) {
             // SKIP-encryption path: export, capture blob, leave Share button visibility to capability probe
             const { blob, filename } = await BackupManager.exportBackup();
             BackupManager.triggerDownload(blob, filename);
             if (BackupManager.isAutoBackupActive()) {
               await BackupManager.autoSaveToFolder(blob, filename);
             }
             producedBlob = blob;
             producedFilename = filename;
             // Re-probe Share capability (button stays visible if canShare returns true)
             probeShareSupport();
           } else {
             // encrypted === true: file already downloaded inside exportEncryptedBackup
             // We do NOT have the encrypted blob in scope — Plan 08 refactors this.
             // HONEST BODY: hide the Share button explicitly so the user does not see a broken affordance.
             // Plan 08 unconditionally re-enables it by returning the encrypted blob from exportEncryptedBackup.
             if (shareBtn) shareBtn.classList.add('is-hidden');
           }
           App.showToast('', 'toast.exportSuccess');
           renderLastBackupSubtitle();  // refresh the subtitle after a fresh export
           if (typeof opts.afterExport === 'function' && producedBlob) {
             await opts.afterExport({ blob: producedBlob, filename: producedFilename });
           }
           return { blob: producedBlob, filename: producedFilename };
         } catch (err) {
           console.error('Backup export failed:', err);
           let msg = (err && err.message) ? err.message : String(err);
           if (msg.includes('subtle') || msg.includes('crypto')) {
             msg = 'Encrypted backup requires HTTPS or localhost. Try accessing via localhost instead of IP.';
           }
           App.showToast(msg, 'toast.exportError');
           return null;
         }
       }

       NOTE on encrypt+share: per D-04, ALL share paths must inherit the encryption choice. Plan 02 ships the Share button working ONLY for the skip-encryption path; the encrypted path explicitly hides the button (no broken UX). Plan 08 closes this by refactoring exportEncryptedBackup to return the encrypted blob+filename, allowing openExportFlow to forward it to shareBackup. Document this hand-off in the SUMMARY.

    10. ADD openImportFlow(file) — preserve the existing importInput change-handler behavior, retargeted to the modal's import input:

        async function openImportFlow(file) {
          if (!file) return;
          if (window.name === 'demo-mode') {
            App.showToast('', 'toast.importDisabledDemo');
            return;
          }
          try {
            const confirmed = await App.confirmDialog({
              messageKey: 'backup.confirmReplace',
              confirmKey: 'confirm.import',
              cancelKey: 'confirm.cancel'
            });
            if (!confirmed) return;
            await BackupManager.importBackup(file);
            App.showToast('', 'toast.importSuccess');
            await loadOverview();
            renderLastBackupSubtitle();
            closeBackupModal();
          } catch (err) {
            if (err === null) return; // passphrase modal cancelled
            console.error('Import failed:', err);
            const msg = (err && err.message) ? err.message : '';
            App.showToast(msg || App.t('toast.importError'));
          }
        }

    11. WIRE the modal handlers in the DOMContentLoaded handler. The Backup & Restore button is GONE from the overview body — there is no #backupRestoreBtn to wire here. The cloud icon mounted by app.js calls window.openBackupModal directly. This block wires only the modal-interior handlers + the auto-open query-param behavior:

        // Auto-open modal if navigated from another page via the cloud icon (?openBackup=1)
        try {
          var params = new URLSearchParams(window.location.search);
          if (params.get('openBackup') === '1') {
            // Defer until the rest of the page boots so applyTranslations etc. are ready.
            setTimeout(function () { openBackupModal(); }, 0);
            // Strip the query param without reloading.
            params.delete('openBackup');
            var newUrl = window.location.pathname + (params.toString() ? '?' + params.toString() : '') + window.location.hash;
            window.history.replaceState({}, '', newUrl);
          }
        } catch (_) { /* defensive: bad URL state, just skip */ }

        const backupModalClose = document.getElementById('backupModalClose');
        if (backupModalClose) backupModalClose.addEventListener('click', closeBackupModal);

        const backupModalOverlay = document.querySelector('#backupModal .modal-overlay');
        if (backupModalOverlay) backupModalOverlay.addEventListener('click', closeBackupModal);

        const backupModalExport = document.getElementById('backupModalExport');
        if (backupModalExport) backupModalExport.addEventListener('click', () => openExportFlow());

        const backupModalShare = document.getElementById('backupModalShare');
        if (backupModalShare) backupModalShare.addEventListener('click', async () => {
          const result = await openExportFlow({
            afterExport: async ({ blob, filename }) => {
              await BackupManager.shareBackup(blob, filename);
            }
          });
        });

        const backupModalImportInput = document.getElementById('backupModalImportInput');
        if (backupModalImportInput) backupModalImportInput.addEventListener('change', async () => {
          const file = backupModalImportInput.files && backupModalImportInput.files[0];
          await openImportFlow(file);
          backupModalImportInput.value = '';
        });

        // Esc key closes modal
        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape' && !document.getElementById('backupModal').classList.contains('is-hidden')) {
            closeBackupModal();
          }
        });

    12. EXPORT openBackupModal globally so the cloud icon mount in app.js, Plan 04's state-update wiring, and Plan 05's interval-end prompt can call it:

        window.openBackupModal = openBackupModal;
        window.renderLastBackupSubtitle = renderLastBackupSubtitle;

    13. KEEP the existing exportBtn / importInput element references at lines 60-61 — they are now `null` (the elements no longer exist post-Task-1). The addEventListener calls were removed by steps 1-2. Remove the dead `const exportBtn = ...` and `const importInput = ...` lines for cleanliness if they are otherwise unused; keep if removing creates noise in the diff.
  </action>
  <verify>
    <automated>grep -E "function mountBackupCloudButton" assets/app.js</automated>
    <automated>grep -E "mountBackupCloudButton\s*:\s*mountBackupCloudButton" assets/app.js && echo HELPER_EXPOSED</automated>
    <automated>grep -E "mountBackupCloudButton\(\)" assets/app.js | wc -l | grep -qE "^[2-9]$|^[1-9][0-9]+$" && echo HELPER_INVOKED_FROM_INITCOMMON</automated>
    <automated>grep -v '^//\|^ *\*' assets/overview.js | grep -c "sendBackupBtn\|autoBackupBtn" | grep -qx 0 && echo OLD_HANDLERS_PURGED</automated>
    <automated>grep -E "function openBackupModal|function closeBackupModal|function openExportFlow|function openImportFlow|function probeShareSupport|function renderLastBackupSubtitle|function formatRelativeTime" assets/overview.js | wc -l | grep -qx 7 && echo NEW_HANDLERS_PRESENT</automated>
    <automated>grep -E "backupModalExport.*addEventListener" assets/overview.js && grep -E "backupModalShare.*addEventListener" assets/overview.js && grep -E "backupModalImportInput.*addEventListener" assets/overview.js</automated>
    <automated>grep -E "BackupManager\.shareBackup\(" assets/overview.js</automated>
    <automated>grep -E "window\.openBackupModal\s*=" assets/overview.js && grep -E "window\.renderLastBackupSubtitle\s*=" assets/overview.js && grep -E "window\.formatRelativeTime\s*=" assets/overview.js</automated>
    <automated>grep -E "shareBtn\.classList\.add\(\s*['\"]is-hidden['\"]\s*\)" assets/overview.js && echo SHARE_HIDE_ON_ENCRYPTED_PRESENT</automated>
    <automated>grep -E "openBackup=1|openBackup'" assets/overview.js && echo AUTO_OPEN_QUERY_PARAM_WIRED</automated>
    <automated>grep -E "backupCloudBtn" assets/app.js && echo CLOUD_BTN_ID_REFERENCED</automated>
  </verify>
  <acceptance_criteria>
    - assets/app.js declares `function mountBackupCloudButton`, exposes it on the App public API, and calls it from `initCommon` (verified by grep counting at least 2 occurrences of `mountBackupCloudButton(` — one definition, one call site).
    - mountBackupCloudButton creates a `<button id="backupCloudBtn" class="header-icon-btn backup-cloud-btn backup-cloud-btn--{state}">` element where {state} is the result of BackupManager.computeBackupRecencyState() (or 'never' if BackupManager is unavailable).
    - The button is inserted into #headerActions BEFORE the settings gear in LTR (visual order: brand … cloud, gear). The insertion logic is gear-aware: if the gear exists, insertBefore; otherwise insertBefore firstChild so the eventual gear lands after.
    - The button has aria-label sourced from `t('overview.backupRestore')` and a title attribute reading `{labelLastBackup} · {relativeTime}` or `{labelLastBackup} · {never}` — with literal-string fallbacks if Plan 04's recency keys are not yet shipped.
    - mountBackupCloudButton is idempotent (early-returns if `.backup-cloud-btn` is already mounted in actions).
    - Clicking the cloud icon invokes `window.openBackupModal()` if defined; otherwise navigates to `./index.html?openBackup=1`.
    - All seven new functions (formatRelativeTime, renderLastBackupSubtitle, openBackupModal, closeBackupModal, openExportFlow, openImportFlow, probeShareSupport) exist in assets/overview.js.
    - assets/overview.js contains zero references to `sendBackupBtn` or `autoBackupBtn` (handler bodies + element references purged).
    - openBackupModal is exposed on `window` for cross-plan/cross-page invocation (mountBackupCloudButton + Plan 04's state updater + Plan 05's schedule fire all depend on this).
    - renderLastBackupSubtitle is exposed on `window` (Plan 04's icon updater and Plan 05's schedule fire both refresh this).
    - formatRelativeTime is hoisted to module top AND exposed on `window` so mountBackupCloudButton in app.js can read it for the icon's title attribute.
    - The Share button click handler chains: openExportFlow with `afterExport` callback that calls BackupManager.shareBackup — verified by grep showing both calls in the same handler.
    - Import flow preserves the App.confirmDialog destructive-replace prompt verbatim (same messageKey/confirmKey/cancelKey).
    - The Esc keydown handler is present and guarded by `!classList.contains('is-hidden')` check.
    - The auto-open query-param logic (`?openBackup=1`) is wired in DOMContentLoaded so cross-page navigation from the cloud icon (on add-client / add-session / settings pages where the modal markup is not in the DOM) lands on overview.html with the modal pre-opened.
    - **HONEST-BODY (D-02): Encrypted export completes with download confirmation only — NO Share button rendered.** When openExportFlow's `encrypted === true` branch runs, the Share button (#backupModalShare) is explicitly hidden via `classList.add('is-hidden')`. The user sees only the standard download confirmation. (Verified by grep on `shareBtn.classList.add('is-hidden')` inside openExportFlow's encrypted branch.) Plan 08's refactor of `exportEncryptedBackup` to return blob+filename is what re-enables the Share button for the encrypted path; until Plan 08 lands, the Share button is gated to the skip-encryption path only — broken UX is NEVER shipped.
    - **Skip-encryption export keeps Share visible** when capability probe passes: the `encrypted === false` branch calls `probeShareSupport()` which leaves the Share button visible if `BackupManager.isShareSupported(probe)` returns true.
    - Manual smoke (DEFERRED to checker pass):
      - On overview.html: cloud icon appears in the header BEFORE the gear; clicking it opens the modal.
      - On add-client.html / add-session.html / settings.html: cloud icon appears in the header BEFORE the gear; clicking it navigates to index.html?openBackup=1 and the modal auto-opens after the page loads.
      - The X close, overlay click, and Esc key all close the modal.
      - The subtitle reads "Last backup: never" on a fresh install and updates after an Export.
      - The Share button is hidden on browsers without canShare and visible on Chrome desktop / Safari macOS for the SKIP-encryption path; Share button is HIDDEN after the encrypted-export path completes (Plan 08 will re-enable it).
  </acceptance_criteria>
  <done>
    The cloud icon button is mounted into #headerActions on every page (overview, add-client, add-session, settings, etc.) by `App.mountBackupCloudButton`, called from `initCommon` alongside `initSettingsLink`. The icon's initial color class is set from BackupManager.computeBackupRecencyState() at mount time; Plan 04 ships the post-mount state-update wiring (visibilitychange, post-export, schedule-change). The icon's click opens the unified Backup & Restore modal (or navigates to overview with auto-open query param on non-overview pages). The modal handlers are wired in overview.js: Export goes through the Phase 22-15 encrypt-or-skip flow without behavior change; Share inherits the export's blob and routes through Plan 01's shareBackup for the SKIP-encryption path; for the ENCRYPTED path the Share button is explicitly hidden (honest body — no broken UX shipped) until Plan 08's refactor lands; Import goes through the existing replace-confirm + importBackup flow. Old overview handlers and their dead element references are purged. window.openBackupModal + window.renderLastBackupSubtitle + window.formatRelativeTime are exposed for app.js (cloud icon mount), Plan 04 (icon state updater), and Plan 05 (schedule fire).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                                    | Description                                                                            |
|---------------------------------------------|----------------------------------------------------------------------------------------|
| Header cloud icon → Backup modal            | User invokes the only entry point; no automated open (except `?openBackup=1` query-param round-trip from cross-page navigation, which is itself triggered only by the user clicking the icon on another page) |
| Modal → BackupManager (Export / Share / Import) | Existing trust boundaries preserved (encrypt modal, replace-confirm)                |
| File input #backupModalImportInput → BackupManager.importBackup | Same boundary as the prior overview importInput; same defenses (normalizeManifest, ALLOWED_KEYS, validateSnippetShape) apply |
| #backupModalScheduleLink → settings.html?tab=backups | Plain anchor; no privilege change                                                      |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                       |
|------------|------------------------|----------------------------------------|-------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-25-02-01 | T1 Encryption bypass   | openExportFlow + Share button chain    | mitigate    | The Share button handler invokes openExportFlow first, which goes through exportEncryptedBackup → optional skip-encryption confirm. Share is only invoked AFTER the export resolves with a non-null blob (skip-encryption path only in Plan 02). The encrypted-then-share path is intentionally non-functional in this plan — and the Share button is EXPLICITLY HIDDEN in that branch (no visible-but-broken affordance). Plan 08 refactors exportEncryptedBackup to return the encrypted blob+filename, which then enables the encrypted-share path AND re-enables the Share button visibility for the encrypted post-export state. The entry-point change (overview button → header icon) does NOT alter this surface; the modal contents, the encrypt-modal flow, and the Share-button visibility logic are all unchanged. |
| T-25-02-02 | T Tampering / V5 Input Validation | #backupModalImportInput accept attribute | mitigate    | accept=".zip,.json,.sgbackup" matches the prior overview importInput; BackupManager.importBackup performs the actual extension/format detection and routes through normalizeManifest + ALLOWED_KEYS + validateSnippetShape (existing Phase 22+24 defenses, untouched). |
| T-25-02-03 | T Tampering            | BACKUP_CONTENTS_KEYS drift             | mitigate    | tests/25-02-checklist-store-parity.test.js asserts BACKUP_CONTENTS_KEYS matches the manifest object literal in exportBackup. A new IDB store added to db.js → exportBackup but not BACKUP_CONTENTS_KEYS → test FAILS. This enforces D-29 programmatically (Pitfall 4 closure). |
| T-25-02-04 | I Information Disclosure | Modal subtitle + icon title reading localStorage | accept | Reads only the public localStorage.portfolioLastExport timestamp (already used by the 7-day banner since Phase 19). The icon title surface is a textual equivalent of the color signal — no NEW disclosure beyond what the modal subtitle already exposed when the user opened it. |
| T-25-02-05 | T Tampering            | `?openBackup=1` query-param auto-open  | accept      | Worst case: a malicious link auto-opens the Backup modal on overview.html. The modal does not perform any action without further user interaction (Export / Share / Import all require a separate click). No data is exposed and no destructive action is triggered by the auto-open alone. The query param is stripped via history.replaceState immediately after consumption. |
</threat_model>

<verification>
- Both new tests pass.
- All grep gates in `<verify>` blocks pass.
- The cloud icon renders correctly in the header on every page (overview, add-client, add-session, settings) with the correct insertion order (cloud BEFORE gear) — manual checker pass; the structure test catches helper-existence drift.
- The Backup & Restore modal renders correctly in Chrome, Safari macOS, iPhone Safari, Firefox (manual checker pass — automated layout testing is out of scope; the structural test catches markup drift).
- After clicking the cloud icon → Export → choosing skip encryption → file downloads, the modal subtitle updates from "Last backup: never" to "Last backup: moments ago" without a page reload AND the Share button remains visible (capability-gated).
- After clicking the cloud icon → Export → entering passphrase → encrypted file downloads, the modal subtitle updates AND the Share button is HIDDEN (honest body — Plan 08 will re-enable it for the encrypted path).
- After clicking the cloud icon on a non-overview page (e.g., settings.html), the browser navigates to index.html?openBackup=1 and the modal auto-opens after the page loads, then the query param is stripped from the URL.
- After Import succeeds, the overview client list refreshes (loadOverview() is called) and the modal closes.
</verification>

<success_criteria>
- Five-button overview cluster collapsed to two (Add Client, Add Session). No backup affordance lives on the overview body.
- Cloud icon button mounted into #headerActions on every page BEFORE the settings gear, with a 44×44 touch target and an `aria-label` of "Backup & Restore".
- Backup & Restore modal opens (from the cloud icon, or from the auto-open query param on non-overview pages), displays last-backup time, contents checklist, Export + Share + Import sections.
- Export preserves Phase 22-15 encrypt/skip-encryption modal flow byte-for-byte.
- Share is hidden on unsupported browsers, visible on supported browsers ONLY when the user took the skip-encryption path; HIDDEN after the encrypted-export path completes (honest body — Plan 08 closes the encrypted-share gap).
- Import preserves the existing destructive-replace confirm and the BackupManager.importBackup defensive path.
- BACKUP_CONTENTS_KEYS is the single source of truth for the modal checklist; a parity test enforces D-29.
- 19 i18n keys ship in 4 locales with Hebrew noun/infinitive forms.
- BackupManager.computeBackupRecencyState is exposed (Plan-02 stub body; Plan 04 swaps in the schedule-coupled body).
- `App.mountBackupCloudButton` exists and is called from `initCommon`.
- `window.openBackupModal` + `window.renderLastBackupSubtitle` + `window.formatRelativeTime` are exposed for app.js, Plan 04, and Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/25-backup-architectural-rework/25-02-SUMMARY.md` documenting:
- The exact lines removed from overview.js (with the old line ranges) and the new functions added (including the hoisted formatRelativeTime now exposed on window)
- The new mountBackupCloudButton helper in app.js (file:line where it lives + where it's called from initCommon + the gear-aware insertBefore logic)
- The cloud icon DOM contract (id="backupCloudBtn", classes, aria-label source key, title format, SVG glyph, click behavior including the cross-page `?openBackup=1` fallback)
- The collapsed overview cluster (now exactly Add Client + Add Session — listed by id for traceability)
- The new modal markup block (DOM structure, IDs)
- The new CSS section (utility class names + the tokens referenced — explicitly noting that the four state-color modifiers `.backup-cloud-btn--{fresh|warning|danger}` are owned by Plan 04, NOT this plan; Plan 02 only ships the base `.header-icon-btn` + `.backup-cloud-btn` + `--never` default)
- The 19 i18n keys delivered (table or list, with one HE example per locale to confirm noun/infinitive convention)
- BACKUP_CONTENTS_KEYS exposed; parity test wired
- BackupManager.computeBackupRecencyState exposed as a Plan-02 stub (schedule OFF only); Plan 04 swaps in the full schedule-coupled body
- Encrypt-then-share LIMITATION: the Share button is explicitly HIDDEN after the encrypted-export branch (honest body principle, D-02). Plan 08's single-source refactor of exportEncryptedBackup returns the encrypted blob+filename, which then enables the encrypted-share path AND re-enables the Share button visibility for the encrypted post-export state.
- Hand-off to Plan 03: #backupModalTestPasswordSection is empty in this plan — Plan 03 fills it with the test-password sub-card
- Hand-off to Plan 04: the cloud icon is mounted with an initial state class only; Plan 04 owns the post-mount state-update wiring (visibilitychange, post-export, schedule-change), the four state-color CSS modifier classes, the `App.updateBackupCloudState(buttonEl)` helper, and the swap of computeBackupRecencyState's body to delegate to getChipState + getScheduleIntervalMs. NO chip element exists anywhere — the icon IS the status surface.
- Hand-off to Plan 05: window.openBackupModal is the entry point the schedule fire will call; renderLastBackupSubtitle will be invoked after a scheduled export completes; the cloud icon's color will refresh via Plan 04's update helper after each scheduled export
- Hand-off to Plan 08: BACKUP_CONTENTS_KEYS is the canonical store-list for the round-trip test; Plan 08 also re-enables the Share button for the encrypted path after refactoring exportEncryptedBackup to return the encrypted blob+filename
</output>
</content>
</invoke>