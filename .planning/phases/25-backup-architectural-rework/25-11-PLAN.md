---
phase: 25-backup-architectural-rework
plan: 11
type: execute
wave: 8
gap_closure: true
depends_on: []
files_modified:
  - assets/i18n-he.js
  - assets/i18n-en.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - assets/settings.js
  - tests/25-11-i18n-parity.test.js
  - tests/25-11-hardcoded-english-removed.test.js
autonomous: true
requirements: []
tags: [i18n, hebrew, gender-neutral, copy, gap-closure, behavior-test]
user_setup: []

must_haves:
  truths:
    - "i18n-he.js line 271 (security.persistent.body) and line 285 (backup.passphrase.tooSimple) use noun/infinitive forms (no masculine-singular imperatives) — CR-03 fix"
    - "photos.deleteAll.confirm.body (he) is rephrased to remove the parsing ambiguity Ben flagged in UAT-C1"
    - "photos.usage.body i18n key exists in all 4 locales (UAT-C3) and replaces the hardcoded English 'Photos use {size} of your browser storage' template literal in settings.js"
    - "Hebrew copy in UAT-C4 — backup.contents.helper (split per UAT-C5), backup.modal.title, photos.deleteAll.action label, photos.optimize.heading — sounds natural and avoids the flagged stilted/jargon phrasings"
    - "photos.helper (he) — UAT-F2 — rewritten to therapist-friendly framing per Ben's draft"
    - "settings.page.helper (he) — UAT-F3 — includes device-scope AND backup-coverage mention per Ben's draft"
    - "WR-01 — 4 hardcoded English toasts in settings.js routed through i18n keys"
    - "4-locale parity: every NEW key exists in en/he/de/cs i18n files (D-28)"
  artifacts:
    - path: "assets/i18n-he.js"
      provides: "Rephrased gender-neutral strings + new keys"
    - path: "assets/i18n-en.js"
      provides: "Canonical EN copy + new keys"
    - path: "assets/i18n-de.js"
      provides: "DE translations of new + revised keys"
    - path: "assets/i18n-cs.js"
      provides: "CS translations of new + revised keys"
    - path: "assets/settings.js"
      provides: "Toast calls routed through i18n keys instead of hardcoded English literals"
    - path: "tests/25-11-i18n-parity.test.js"
      provides: "Behavior test: every new key exists in all 4 locales"
    - path: "tests/25-11-hardcoded-english-removed.test.js"
      provides: "Behavior test: settings.js no longer passes English literals as the first arg to App.showToast on the 4 known paths"
  key_links:
    - from: "settings.js Save handler catch (line 521)"
      to: "App.showToast('', 'settings.save.failed')"
      via: "i18n key (not English literal)"
      pattern: "settings.save.failed"
    - from: "photos optimize unavailable / failure / delete-all failure paths"
      to: "App.showToast('', 'photos.optimize.*' / 'photos.deleteAll.failed')"
      via: "i18n key"
      pattern: "photos.optimize.unavailable|photos.optimize.failed|photos.deleteAll.failed"
---

<objective>
Close the localization gaps from `25-VERIFICATION.md` (CR-03), `25-UAT-FINDINGS.md` (UAT-C1, C2-text-only, C3, C4, C5, F2, F3), and `25-REVIEW.md` (WR-01).

**Scope of this plan:**
1. Replace 2 Hebrew masculine-singular imperatives at i18n-he.js:271 and :285 (CR-03 — verifier blocker).
2. Rewrite ambiguous Hebrew strings (UAT-C1 delete-all confirm; UAT-C4 four awkward phrasings; UAT-C5 contents-helper split).
3. Add a new `photos.usage.body` i18n key in all 4 locales to replace the hardcoded English template literal in settings.js (UAT-C3).
4. Rewrite Hebrew Photos tab subtitle (UAT-F2) and Settings page header (UAT-F3) per Ben's preferred drafts.
5. Route 4 hardcoded English error toasts in settings.js through i18n keys (WR-01: lines 521, 2385, 2413, 2458).
6. Add 4-locale parity for every new key (D-28).

**Hebrew strings used: Ben's preferred drafts.** Where Ben supplied a draft (UAT-C1, UAT-F2, UAT-F3), use it verbatim. Where Ben suggested a direction without an exact draft (UAT-C4), pick from his options noting the chosen variant in the SUMMARY.

**EN/DE/CS parallel touch-ups:** for keys whose Hebrew rewording fundamentally changes meaning (e.g. UAT-C5 splitting one sentence into two), update EN/DE/CS in parallel to keep the contract aligned across locales.

**Out of scope:** the placeholder-substitution code fix for `{n}` and `{size}` in the optimize confirm (UAT-C2). That's a code bug in settings.js handler logic, NOT a string bug — addressed in Plan 25-12 (settings behavior plan).

Purpose: Release-blocking grammar correctness for the primary HE audience (female therapists), plus the polish gaps Ben flagged during live UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-backup-architectural-rework/25-CONTEXT.md
@.planning/phases/25-backup-architectural-rework/25-VERIFICATION.md
@.planning/phases/25-backup-architectural-rework/25-UAT-FINDINGS.md
@.planning/phases/25-backup-architectural-rework/25-REVIEW.md
@.planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md
@assets/i18n-he.js
@assets/i18n-en.js
@assets/i18n-de.js
@assets/i18n-cs.js
@assets/settings.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests — i18n parity + hardcoded-English-removed</name>
  <read_first>
    - tests/25-02-checklist-store-parity.test.js (existing parity test — replicate scaffolding)
    - tests/24-04-app-cache.test.js (vm-sandbox conventions)
    - assets/settings.js lines 519-523 (Save failed toast)
    - assets/settings.js lines 2380-2415 (optimize unavailable + failed toasts)
    - assets/settings.js lines 2450-2460 (delete-all failure toast)
    - assets/i18n-he.js lines 270-290 (CR-03 target lines)
    - assets/i18n-he.js lines 180-200 (photos confirm strings — UAT-C1 target)
  </read_first>
  <behavior>
    Test A (`tests/25-11-i18n-parity.test.js`) — assert each NEW key exists in all 4 locales:
    - `photos.usage.body` (UAT-C3)
    - `settings.save.failed` (WR-01)
    - `photos.optimize.unavailable` (WR-01)
    - `photos.optimize.failed` (WR-01)
    - `photos.deleteAll.failed` (WR-01)
    Plus: assert key existence parity — every key in i18n-en.js exists in i18n-he/de/cs.js (current invariant; this lock-down catches drift).

    Test B (`tests/25-11-hardcoded-english-removed.test.js`) — read assets/settings.js as source; assert ZERO occurrences of the 4 known English literals as showToast first-arg:
    - `App.showToast('Save failed'`
    - `App.showToast('Optimize is unavailable`
    - `App.showToast('Could not optimize photos'`
    - `App.showToast('Could not delete photos'`
    Use literal `grep -F`-style substring search inside the file text; FAIL if any of the 4 literals is found.

    Test C (`tests/25-11-hardcoded-english-removed.test.js` continued) — assert i18n-he.js line content (or whole-file substring search) for:
    - i18n-he.js does NOT contain ` השתמש בגיבויים ` (the masc-sg imperative in security.persistent.body)
    - i18n-he.js does NOT contain ` ונעל את המכשיר` (the masc-sg imperative)
    - i18n-he.js does NOT contain `.תוסיף\b` or other obvious imperatives in newly-added keys (best-effort; primary check is the two known offenders)
    - i18n-he.js DOES contain `מומלץ להשתמש` (the infinitive replacement)
    - i18n-he.js DOES contain `לנעול` (the infinitive replacement)

    All three tests MUST FAIL on the current codebase: Test A fails because new keys don't exist; Test B fails because English literals are present; Test C fails because the imperatives are still there.
  </behavior>
  <action>
    Create `tests/25-11-i18n-parity.test.js` and `tests/25-11-hardcoded-english-removed.test.js`. Both follow the vm-sandbox / direct-file-read pattern used in `tests/25-02-checklist-store-parity.test.js`. Run each test; both MUST fail. Commit message: `test(25-11): RED — i18n parity + hardcoded English + Hebrew imperatives`.
  </action>
  <verify>
    <automated>node tests/25-11-i18n-parity.test.js; A=$?; node tests/25-11-hardcoded-english-removed.test.js; B=$?; test $A -ne 0 -a $B -ne 0</automated>
  </verify>
  <done>Both test files exist and FAIL on the current codebase. Commit lands as RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Hebrew rephrasings (CR-03 + UAT-C1, C4, C5, F2, F3) + new keys in he locale</name>
  <read_first>
    - assets/i18n-he.js (full file — every line modified or added must respect existing key order conventions)
    - .planning/phases/25-backup-architectural-rework/25-UAT-FINDINGS.md sections C1, C4, C5, F2, F3 (Ben's drafts)
    - .planning/phases/25-backup-architectural-rework/25-REVIEW.md CR-03 (exact replacement text for line 271 and 285)
    - .planning/phases/24-pre-launch-final-cleanup/24-CONTEXT.md D-05 (Hebrew noun/infinitive convention — applies to all new strings)
    - tests/25-11-hardcoded-english-removed.test.js (RED test for he imperatives — must pass after this task)
  </read_first>
  <action>
    Edit `assets/i18n-he.js` in place. Apply these exact changes:

    **(CR-03) Line 271 — security.persistent.body:** replace the current value with:
    `כל הנתונים שלך נשמרים רק בדפדפן הזה — שום דבר לא נשלח לשרת כלשהו. מחיקת נתוני הדפדפן או הסרת האפליקציה מוחקת את כל הרשומות לצמיתות. מומלץ להשתמש בגיבויים מוצפנים ולנעול את המכשיר כדי להגן על הפרטיות של הלקוחות שלך.`

    **(CR-03) Line 285 — backup.passphrase.tooSimple:** replace with:
    `הסיסמה פשוטה מדי. מומלץ להשתמש בשילוב של אותיות ומספרים.`

    **(UAT-C1) photos.deleteAll.confirm.body — line ~195:** replace the current ambiguous body with Ben's draft:
    `פעולה זו תסיר את כל תמונות הלקוחות מהדפדפן. רשומות הלקוחות והמפגשים יישמרו, אך לא ניתן יהיה לשחזר את התמונות.`

    **(UAT-C4) Four awkward Hebrew rephrasings — find each key by EN value, replace with the simpler form:**
    - `שמירה של עותק מכל הנתונים` → `שמירת עותק של כל הנתונים` (likely in `backup.export.helper` or similar — locate by grep)
    - `תמונות (חתוכות, ממוטבות)` → drop the parenthetical OR use `תמונות (לאחר חיתוך ואופטימיזציה)` (locate the backup-contents-list "Photos" entry by grep — likely `backup.contents.item.photos` or inline in modal markup)
    - `מיטוב תמונות קיימות` → `אופטימיזציה של תמונות קיימות` (likely `photos.optimize.heading` — Ben prefers the loanword)
    - Audit any other key containing `מיטוב` or `ממוטב` and replace with `אופטימיזציה` / `מותאם` consistently throughout the photos.* and settings.* namespaces.

    **(UAT-C5) backup.contents.helper — line 314:** SPLIT the current sentence. The "restore replaces current data" half belongs in `backup.import.helper` (line 324) which already says it. Update line 314 to ONLY describe contents:
    `כל ייצוא כולל את כל הפריטים שלהלן.`
    (Drop the second sentence entirely; it now lives only in backup.import.helper.)

    **(UAT-F2) photos.helper (line ~155 in he locale, where "Manage how client photos…" lives):** replace with Ben's draft:
    `כיווץ ומחיקה של תמונות לקוחות כדי לפנות מקום ולהאיץ את האפליקציה.`

    **(UAT-F3) settings.page.helper — line 349:** replace with Ben's draft:
    `התאמת האפליקציה לסגנון העבודה שלך. השינויים נשמרים במכשיר זה וכלולים בגיבוי.`

    **(UAT-C3 — Hebrew side) photos.usage.body — NEW key:** add after the existing `photos.usage.unavailable` key. Value:
    `התמונות תופסות {size} מאחסון הדפדפן.`

    **(WR-01 — Hebrew side) 4 NEW error keys:** add to the appropriate namespace blocks:
    - `settings.save.failed`: `השמירה נכשלה. ניתן לנסות שוב.`
    - `photos.optimize.unavailable`: `אופטימיזציית התמונות אינה זמינה — רכיבי העזר לא נטענו.`
    - `photos.optimize.failed`: `לא ניתן היה לבצע אופטימיזציה לתמונות.`
    - `photos.deleteAll.failed`: `לא ניתן היה למחוק את התמונות.`

    All new Hebrew strings use noun/infinitive forms (no masculine-singular imperatives) per D-27.

    Do NOT touch en/de/cs in this task — they're handled in Task 3.

    Commit message: `feat(25-11): GREEN — Hebrew gender-neutral + UAT rephrasings (CR-03, UAT-C1/C3/C4/C5/F2/F3)`.
  </action>
  <verify>
    <automated>node tests/25-11-hardcoded-english-removed.test.js &amp;&amp; grep -c "השתמש בגיבויים" assets/i18n-he.js | grep -q "^0$" &amp;&amp; grep -c "מומלץ להשתמש" assets/i18n-he.js | grep -qE "^[1-9]"</automated>
  </verify>
  <done>Hebrew imperatives removed from lines 271+285; UAT-C1 ambiguity resolved; UAT-C3/C5/F2/F3 rephrasings applied per Ben's drafts; new keys exist in i18n-he.js. The test from Task 1 partially turns GREEN (the Hebrew portion); the parity test still fails until Task 3 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — EN/DE/CS parity for new keys + parallel rephrasings (UAT-C5)</name>
  <read_first>
    - assets/i18n-en.js (canonical EN reference)
    - assets/i18n-de.js
    - assets/i18n-cs.js
    - assets/i18n-he.js (just-updated version from Task 2 — for the contract)
    - tests/25-11-i18n-parity.test.js (must turn GREEN after this task)
  </read_first>
  <action>
    Add the 5 new keys (`photos.usage.body`, `settings.save.failed`, `photos.optimize.unavailable`, `photos.optimize.failed`, `photos.deleteAll.failed`) to each of `assets/i18n-en.js`, `assets/i18n-de.js`, `assets/i18n-cs.js`. Use the same JSON key positioning as the matching he locale (alphabetical or grouped — match existing file convention).

    Canonical EN values:
    - `photos.usage.body`: `"Photos use {size} of your browser storage."`
    - `settings.save.failed`: `"Could not save your settings. Please try again."`
    - `photos.optimize.unavailable`: `"Photo optimization is unavailable — helper files didn't load."`
    - `photos.optimize.failed`: `"Could not optimize photos."`
    - `photos.deleteAll.failed`: `"Could not delete photos."`

    DE values (native-feel German, no machine translation tone):
    - `photos.usage.body`: `"Fotos belegen {size} des Browser-Speichers."`
    - `settings.save.failed`: `"Einstellungen konnten nicht gespeichert werden. Bitte erneut versuchen."`
    - `photos.optimize.unavailable`: `"Foto-Optimierung ist nicht verfügbar — Hilfsdateien wurden nicht geladen."`
    - `photos.optimize.failed`: `"Fotos konnten nicht optimiert werden."`
    - `photos.deleteAll.failed`: `"Fotos konnten nicht gelöscht werden."`

    CS values:
    - `photos.usage.body`: `"Fotografie zabírají {size} úložiště prohlížeče."`
    - `settings.save.failed`: `"Nastavení se nepodařilo uložit. Zkuste to znovu."`
    - `photos.optimize.unavailable`: `"Optimalizace fotografií není dostupná — pomocné soubory se nenačetly."`
    - `photos.optimize.failed`: `"Fotografie se nepodařilo optimalizovat."`
    - `photos.deleteAll.failed`: `"Fotografie se nepodařilo smazat."`

    **(UAT-C5 parallel)** Update `backup.contents.helper` in EN/DE/CS to match the he split (only contents description, no "restore" sentence):
    - EN: `"Every export includes everything below."`
    - DE: `"Jeder Export enthält alles Folgende."`
    - CS: `"Každý export obsahuje vše níže."`

    (The `backup.import.helper` already carries the "restore replaces all current data" half in all 4 locales — no change needed there. Verify by grep before editing.)

    Commit message: `feat(25-11): GREEN — 4-locale parity for new i18n keys + UAT-C5 split`.
  </action>
  <verify>
    <automated>node tests/25-11-i18n-parity.test.js</automated>
  </verify>
  <done>i18n-parity test transitions RED → GREEN. All 5 new keys exist in all 4 locales. UAT-C5 split applied consistently across en/he/de/cs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — settings.js toast calls switched to i18n keys (WR-01)</name>
  <read_first>
    - assets/settings.js lines 519-523 (Save handler catch)
    - assets/settings.js lines 2380-2415 (optimize unavailable + failed)
    - assets/settings.js lines 2450-2462 (delete-all failure)
    - assets/settings.js line ~2310 (the hardcoded English `Photos use {size} of your browser storage` template literal — UAT-C3)
    - tests/25-11-hardcoded-english-removed.test.js (must FULLY pass after this task)
  </read_first>
  <action>
    Replace four `App.showToast(<english literal>, '')` calls with `App.showToast('', '<i18n key>')`:
    - Line 521: `App.showToast("Save failed", "")` → `App.showToast("", "settings.save.failed")`
    - Line 2385: `App.showToast('Optimize is unavailable — photo helpers not loaded', '')` → `App.showToast("", "photos.optimize.unavailable")`
    - Line 2413: `App.showToast('Could not optimize photos', '')` → `App.showToast("", "photos.optimize.failed")`
    - Line 2458: `App.showToast('Could not delete photos', '')` → `App.showToast("", "photos.deleteAll.failed")`

    **(UAT-C3 — code side)** Locate the hardcoded `Photos use {size} of your browser storage` template literal in settings.js (search around line 2310, the photos storage estimator output). Replace with an `App.t('photos.usage.body').replace('{size}', readHumanBytes(usedBytes))` call (or whatever the local helper for byte-formatting is named). The fallback path (when `App.t` is unavailable) should fall through to `photos.usage.unavailable` rather than the hardcoded English. Keep the existing `photos.usage.unavailable` empty/no-photos path intact.

    Commit message: `fix(25-11): WR-01 + UAT-C3 — settings.js toasts and usage line route through i18n`.
  </action>
  <verify>
    <automated>node tests/25-11-hardcoded-english-removed.test.js &amp;&amp; node tests/25-07-photo-bytes-estimator.test.js &amp;&amp; node tests/25-07-delete-all-photos.test.js</automated>
  </verify>
  <done>Both new Plan 25-11 tests are fully GREEN. Plan 07's behavior tests still pass (no regression in the optimize/delete loops). UAT-C3 hardcoded English is gone; UAT-C2 placeholder code bug remains for Plan 25-12 to fix.</done>
</task>

</tasks>

<verification>
- `node tests/25-11-i18n-parity.test.js` passes (all 5 new keys exist in all 4 locales)
- `node tests/25-11-hardcoded-english-removed.test.js` passes (zero hardcoded English in the 4 known paths; Hebrew imperatives removed)
- `node tests/25-02-checklist-store-parity.test.js` still passes (no regression in earlier i18n contracts)
- `node tests/25-07-photo-bytes-estimator.test.js` and `node tests/25-07-delete-all-photos.test.js` still pass
- `grep -v '^#' assets/i18n-he.js | grep -c 'השתמש בגיבויים'` returns 0 (CR-03 line 271 cleared)
- `grep -v '^#' assets/i18n-he.js | grep -c 'מומלץ להשתמש'` returns at least 2 (both replacement strings in place)
- All 4 locale files have identical key counts (verifiable by `gsd-sdk query frontmatter.validate` or a simple `node -e` parity check inside the parity test)
</verification>

<success_criteria>
- CR-03 closed: Hebrew strings line 271 + 285 use noun/infinitive forms.
- UAT-C1/C3/C4/C5/F2/F3 closed: Hebrew rephrasings match Ben's drafts; new photos.usage.body key in all 4 locales.
- WR-01 closed: 4 hardcoded English toasts routed through i18n.
- D-28 4-locale parity maintained across all newly-added keys.
- Behavior tests make the i18n contract falsifiable for future regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/25-backup-architectural-rework/25-11-SUMMARY.md` recording: the 6 Hebrew strings changed (with before/after Hebrew quotes), the 5 new keys × 4 locales (table), the 4 settings.js call-site refactors with line numbers, and confirmation that 25-VERIFICATION.md CR-03 status flips from FAILED → SATISFIED.
</output>
