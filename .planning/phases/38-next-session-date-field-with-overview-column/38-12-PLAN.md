---
phase: 38-next-session-date-field-with-overview-column
plan: 12
type: execute
wave: 2
depends_on: [38-10, 38-11]
files_modified:
  - assets/app.js
  - assets/app.css
  - assets/add-session.js
  - tests/38-12-toast-tone-focus.test.js
autonomous: false
gap_closure: true
requirements: [NEXT-01, NEXT-08]
must_haves:
  truths:
    - "A blocked save (incomplete next-session date) shows a warning-toned toast that is visually distinct from the success toast and stays visible longer than the 1.8s success toast"
    - "An error toast bound to a field scrolls to and focuses that field (the incomplete-date block focuses #nextSessionDate)"
    - "Success and info toasts are completely unchanged — same single style, same 1.8s auto-dismiss, no focus side effect"
  artifacts:
    - "assets/app.js — showToast gains a backward-compatible third options param ({ tone, focus })"
    - "assets/app.css — a .toast--error variant using the dark-safe --color-warning-* tokens"
    - "assets/add-session.js — the incomplete-date guard and the session/client form error toasts adopt the error tone (field-bound ones also pass the focus target)"
    - "tests/38-12-toast-tone-focus.test.js — falsifiable behavior test for tone + duration + focus, and default-path no-regression"
  key_links:
    - "showToast(message, key, options) stays positionally backward compatible so every existing 2-arg caller and existing toast test is unaffected; only opt-in callers get error tone + focus"
    - "error tone uses --color-warning-* (has dark-mode overrides) — NOT the light-only --color-danger-*"
---

<objective>
Fix the block-message visibility complaint (UAT test 8). The 38-09 incomplete-date guard reused App.showToast (app.js:838, add-session.js:1167), which is single-style: success and error render identically (same corner pill, 1.8s auto-dismiss), so the block warning looks exactly like "completed successfully" and nobody notices it — and it is physically far from the offending field. Ben's confirmed direction (2026-07-07): give showToast an error tone (visually distinct, longer duration) and an auto-focus/scroll to the problematic field, designed as a GENERALIZED API ("for all error toasts this is relevant"), not a one-off. Success/info toasts stay unchanged.

Purpose: Make blocking warnings unmistakable and self-locating, closing the last open Phase 38 UAT gap, while keeping the toast system otherwise intact.
Output: A backward-compatible showToast tone+focus API, a dark-safe `.toast--error` style, migrated error call sites in the session/client forms, and a falsifiable behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# UAT test 8 self-diagnosis (single-style showToast, no error variant, no inline field-error pattern)
@.planning/phases/38-next-session-date-field-with-overview-column/38-UAT.md

# Files being changed
@assets/app.js
@assets/app.css
@assets/add-session.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the RED behavior test for tone + duration + focus (and default no-regression)</name>
  <files>tests/38-12-toast-tone-focus.test.js</files>
  <action>
    Write tests/38-12-toast-tone-focus.test.js FIRST and confirm it FAILS (RED). This is genuine runtime behavior (class toggling, timer duration, focus/scroll side effects), so it is a real jsdom/vm behavior test, not a source gate. Follow the exit-0/1 + console PASS/FAIL contract so tests/run-all.js auto-discovers it.

    Load the real showToast: evaluate assets/date-format.js then assets/app.js in a Node vm sandbox exactly as tests/37-date-format.test.js already does (the same `.replace(/^App\./gm, 'window.App.')` app.js load), and read window.App.showToast. Extend the sandbox document stub so getElementById("toast") returns a toast node with a real classList (add/remove/contains over a Set) and a mutable textContent, and provide a separate field node exposing focus() and scrollIntoView() spies (record call counts). Provide a setTimeout spy in the sandbox that RECORDS the delay argument passed on each call (so the test can read the scheduled auto-dismiss duration) while still being drainable. Stub App.t to echo the key.

    Assert three scenarios:
      1. Error tone + focus: call App.showToast("", "toast.nextSessionDateIncomplete", { tone: "error", focus: fieldNode }). Assert the toast node's classList contains the error class (class name "toast--error"); assert the recorded auto-dismiss delay is STRICTLY GREATER than 1800 (the error toast lingers longer than the success toast); assert fieldNode.focus() was called exactly once AND fieldNode.scrollIntoView() was called (the field is brought into view and focused).
      2. Default success toast unchanged: on a FRESH sandbox call App.showToast("", "toast.sessionSaved") with no options. Assert the toast node classList does NOT contain "toast--error"; assert the recorded delay === 1800; assert no focus/scrollIntoView side effect occurred.
      3. Backward-compat message path: App.showToast("hello world") sets the toast textContent to "hello world" and does not throw (no options, no key).
    Add an EXPECTED_COUNT vacuous-green guard asserting all three scenarios ran.

    Run the file now: it MUST be RED (showToast today ignores a third arg, never adds toast--error, always uses 1800, and never focuses). Do not weaken to green here.
  </action>
  <verify>
    <automated>node tests/38-12-toast-tone-focus.test.js; test $? -ne 0 &amp;&amp; echo "RED-as-expected"</automated>
  </verify>
  <done>tests/38-12-toast-tone-focus.test.js exists and fails RED: no error class, fixed 1800 duration, no focus side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement showToast tone+focus (app.js) + dark-safe .toast--error style (app.css)</name>
  <files>assets/app.js, assets/app.css</files>
  <behavior>
    - showToast(message, key, options) is positionally backward compatible; options.tone === "error" adds the "toast--error" class and uses a longer auto-dismiss than 1800ms; options.focus (an element) is scrolled into view and focused. Absent/undefined options → identical to today (single style, 1800ms, no focus).
    - .toast--error is visually distinct from the base success toast using --color-warning-* tokens and remains legible in dark mode. Test 38-12 scenarios 1-3 pass; existing toast tests stay green.
  </behavior>
  <action>
    1. assets/app.js — extend showToast (currently at ~:838) to accept an optional third parameter, an options object with tone and focus. Keep the (message, key) positional signature EXACTLY as-is so the ~30 existing 2-arg callers and the existing toast tests (which stub or drive showToast) are unaffected. Implementation:
       - At the top of the function, always reset tone state: remove the "toast--error" class from the toast element (so a prior error tone never sticks to a later success toast).
       - Keep the existing textContent assignment and the "is-visible" add exactly as they are.
       - If options.tone === "error", add the "toast--error" class and set the auto-dismiss timeout to a longer duration than the success default. Define the two durations as named locals/constants (e.g. a success duration of 1800 — unchanged — and a distinctly longer error duration such as 4000); the success path MUST remain 1800 so no existing behavior/test drifts.
       - If options.focus is a truthy element, call its scrollIntoView (guarded — only if it is a function) and then focus() it, so the offending field is brought into view and focused. Guard every access so a missing element or a non-element focus target cannot throw.
       - Update the JSDoc to document the new options param, its tone values, and the focus behavior. Keep showToast exported on the App surface exactly as now (it already is).
    2. assets/app.css — add a `.toast--error` variant near the existing .toast rules (~:1268). It must be visually distinct from the base success toast (which uses --color-toast-bg): set the background to --color-warning-bg and the text color to --color-warning-text (optionally a --color-warning-text hairline border). Use ONLY the --color-warning-* tokens — they have dark-mode overrides (tokens.css light #fff3cd/#856404, dark #3a2f00/#ffd966, WCAG-checked). Do NOT use --color-danger-*: it is light-only by design (reserved for the confirm dialog) and would be unreadable in dark mode. Do not alter the base .toast or .toast.is-visible rules.

    Re-run tests/38-12-toast-tone-focus.test.js (now GREEN scenarios 1-3) and the full suite.
  </action>
  <verify>
    <automated>node tests/38-12-toast-tone-focus.test.js</automated>
    <automated>node tests/run-all.js</automated>
  </verify>
  <done>showToast supports an opt-in error tone (distinct class + longer duration) and field focus/scroll while the default path is byte-for-byte unchanged (1800ms, single style, no focus); .toast--error is dark-safe via --color-warning-*; test 38-12 and the full suite are green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the session/client form error toasts to the error tone (field-bound ones focus the field)</name>
  <files>assets/add-session.js</files>
  <action>
    Adopt the new error tone at the add-session.js error toasts ONLY (Ben's scope: the incomplete-date block + the session/client form validation toasts). Success and info toasts stay exactly as they are — do NOT touch clientSaved (:519), copied (:1001), clientCreated (:1094), sessionUpdated (:1201), sessionSaved (:1221), or sessionDeleted (:1256). Do NOT touch error toasts in other files (settings.js etc.) — out of scope for this gap.

    For each error toast below, pass the new third options argument. Field-bound ones (the user must fix a specific control) also pass that control as options.focus so the toast scrolls to and focuses it. Look up each focus target with document.getElementById / the existing module refs; the ids are confirmed present in add-session.html.

      - PRIMARY — incomplete next-session date guard at :1167 (toast.nextSessionDateIncomplete): tone "error" + focus the #nextSessionDate element (the guard already holds nextSessionDateEl — reuse it).
      - selectClient at :1118 (no client chosen): tone "error" + focus #clientSelect.
      - errorRequired at :1123 (session date missing): tone "error" + focus #sessionDate.
      - issueMissing at :1129 (an issue row incomplete): tone "error" + focus the offending issue input — reuse validateIssues / getIssuesPayload to find the first incomplete issue field; if a specific field cannot be resolved cleanly, pass the issues section container so the user is scrolled to it.
      - heartShieldRequired at :1139 (no shieldRemoved choice): tone "error" + focus the first shieldRemoved radio (input[name='shieldRemoved']).
      - errorRequired in the inline/edit client form at :476 and :1062 (required client field missing): tone "error" + focus the first required client field (#inlineClientFirstName for the inline-add form; the matching first-required field for the edit form).
      - errorGeneric at :456 and :1225 (DB/network failure, no single field to fix): tone "error" only, NO focus target.

    Keep the first showToast argument as the empty string "" and the i18n key unchanged for every migrated call (no new i18n strings — the keys already exist in all four languages). Only ADD the options object.

    Run the full suite green.
  </action>
  <verify>
    <automated>node tests/run-all.js</automated>
    <automated>node tests/38-09*.test.js 2>/dev/null || node tests/run-all.js</automated>
  </verify>
  <done>The incomplete-date guard and the session/client form validation toasts render with the error tone; field-bound ones focus/scroll to their control; DB-error toasts get the tone without a focus target; success/info toasts and all other files are untouched; the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device verification — warning visibility, scroll-to-field, success unchanged</name>
  <files>(no source files — manual field-verification gate)</files>
  <action>MANUAL VERIFICATION ONLY (not auto-passable — felt visibility, scroll behavior, and dark/RTL legibility need a human eye). Pause execution and hand off to Ben to verify per the how-to-verify steps. Do not mark this gap closed until Ben confirms.</action>
  <what-built>An error-toned, longer-lived toast variant with auto-focus/scroll to the offending field, wired into the incomplete-date block and the session/client form validation toasts. Unit tests prove the class, duration, and focus/scroll behavior; the felt visibility and the actual scroll-to-field need a human eye on device.</what-built>
  <how-to-verify>
    In the app (English is fine; then spot-check Hebrew RTL):
    1. Open the add/edit session form, type only ONE segment of the next-session date (a partial date) in real Safari, and press Save. Confirm the block toast is clearly a WARNING (distinct color from the green/neutral "saved" toast), stays up noticeably longer, AND the page scrolls to / focuses the #nextSessionDate field.
    2. Trigger a couple of the other form errors (e.g. save with no client selected, or with a required field empty) and confirm the same error tone appears and the relevant field is focused/scrolled to.
    3. Save a valid session and confirm the SUCCESS toast is unchanged (same style, ~1.8s, no focus jump).
    4. Toggle dark mode and re-check the error toast is legible (warning tokens), not an unreadable light-on-light box.
    5. Switch to Hebrew and confirm the error toast reads naturally in RTL.
  </how-to-verify>
  <resume-signal>Type "approved" if the warning is clearly distinct, longer, and jumps to the field while success toasts are unchanged, or describe what still isn't visible/close enough.</resume-signal>
  <verify><human-check>Ben confirms: a partial next-session date save shows a distinct, longer warning toast that scrolls to/focuses #nextSessionDate; other form errors behave the same; success toasts are unchanged; error toast is legible in dark mode and reads naturally in Hebrew RTL.</human-check></verify>
  <done>Ben types "approved" after warning visibility + scroll-to-field + success-unchanged + dark/RTL legibility are confirmed on device; UAT test 8 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form validation → toast + focus | Validation failures surface a localized toast and move focus to a form control; no untrusted data is newly rendered (i18n keys unchanged, empty message arg) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-12-01 | Tampering | showToast options.focus | low | mitigate | focus/scrollIntoView are guarded to elements only; a missing/invalid target is a no-op, never throws — no new injection or navigation surface |
| T-38-12-02 | Denial of service | error-toast duration | low | accept | Longer auto-dismiss is a fixed constant and still auto-clears; cannot be driven by user input |
| T-38-12-03 | Information disclosure | toast message content | low | accept | Migrated calls keep the empty message arg + existing i18n keys; no new user data rendered in the toast |

No package-manager installs in this plan — no supply-chain (T-*-SC) row required.
</threat_model>

<verification>
- `node tests/38-12-toast-tone-focus.test.js` — RED in Task 1, GREEN after Task 2 (tone class + longer duration + focus/scroll; default path unchanged).
- `node tests/run-all.js` — full suite green, including the existing toast-behavior tests (they stub/drive showToast and rely on the unchanged 2-arg path).
- Human checkpoint approved: warning is visibly distinct + longer + scrolls-to-field; success toasts unchanged; dark mode + Hebrew legible.
</verification>

<success_criteria>
Blocking warnings render in a distinct, longer-lived error tone and scroll to/focus the offending field; success/info toasts are byte-for-byte unchanged; the API is a backward-compatible third options param reusable by any error toast; automated tests and the full suite are green.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-12-SUMMARY.md` when done.
</output>
