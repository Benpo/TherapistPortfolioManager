---
phase: 38-next-session-date-field-with-overview-column
plan: 04
type: execute
wave: 2
depends_on: ["38-01", "38-03"]
files_modified:
  - add-session.html
  - assets/add-session.js
autonomous: true
requirements: [NEXT-01, NEXT-02]
must_haves:
  truths:
    - "A native date input id=nextSessionDate class=input with label session.form.nextSessionDate sits BELOW the #customerSummary note inside the existing data-section-key=nextSession section; the note stays exactly where it is (D-05, D-06, D-07)"
    - "nextSessionDate is read on save and written into BOTH the update payload and the add payload; populated on edit from session.nextSessionDate; reset to empty on new/clear; and captured in snapshotFormState so Cancel->Discard restores it (D-05; NEXT-01; research Pitfall 2)"
    - "The date input min attribute equals the current #sessionDate value (same-day allowed), is removed when #sessionDate is empty, and is re-applied at new-session default, at populate time, and on every #sessionDate change - NOT min=today (D-08; NEXT-02)"
    - "The stored value is a YYYY-MM-DD string on the schemaless IndexedDB session record with no migration; existing sessions simply lack the key (D-05, D-06)"
  artifacts:
    - "add-session.html (#nextSessionDate field markup)"
    - "assets/add-session.js (read/save/populate/reset/snapshot wiring + syncNextSessionMin())"
  key_links:
    - "syncNextSessionMin() reads #sessionDate.value then sets/removes #nextSessionDate.min (D-08); defined at TOP-LEVEL module scope (column-0), NOT inside the DOMContentLoaded closure — populateSession (:1651) sits OUTSIDE the closure (:85-:1280) and must reach it"
    - "snapshotFormState (add-session.js:720-746, INSIDE the DOMContentLoaded closure) and populateSession (:1651-1668, top-level OUTSIDE it) live in DIFFERENT scopes; both must carry nextSessionDate (Pitfall 2) but share no scope"
    - "tests/38-next-session.test.js (Plan 38-01) flips GREEN when this lands"
    - "tests/30-form-dirty-revert.test.js snapshot-capture assertion (Plan 38-02) flips GREEN when this lands"
---

<objective>
Add the optional `nextSessionDate` field to the add/edit-session form and wire it end-to-end through save, add, populate, reset, snapshot/revert, and the dynamic `min` constraint (NEXT-01, NEXT-02).

Purpose: Delivers the structured next-session date on the session record. The dynamic `min` (the session's own date, not today) and the snapshotFormState capture are the two correctness details that distinguish this from a naive field add.
Output: A working, revert-safe date field stored as YYYY-MM-DD with no migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-CONTEXT.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- Session-record field `nextSessionDate` (YYYY-MM-DD, optional); form element `#nextSessionDate`
- Helper `syncNextSessionMin()` (add-session.js)
- i18n key `session.form.nextSessionDate` (added in Plan 38-03)
- (other phase-38 symbols: `mostRecentSession`, `nextSession` sort key, overview `<th>`/`<option>`, `.next-session-cell`/`.is-overdue`/`.next-overdue-dot`, tests/38-*.test.js)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the #nextSessionDate input to the nextSession section in add-session.html</name>
  <files>add-session.html</files>
  <read_first>
    - add-session.html (:328-339 the data-section-key="nextSession" section holding the #customerSummary textarea; :142-143 #inlineClientBirthDate and :522 #editClientBirthDate for the class="input" type="date" shape; :86 #sessionDate uses input-pill — do NOT copy the pill)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md (§4 exact markup: new .form-field with margin-top:1rem, label data-i18n="session.form.nextSessionDate", input class="input" id="nextSessionDate" type="date", not required)
  </read_first>
  <action>Inside add-session.html's data-section-key="nextSession" section (:328-339), add a NEW .form-field BELOW the existing #customerSummary textarea's .form-field (after the textarea at :337, before the section's closing div at :339). Mirror the birthdate date-input shape: a .form-field with a .label carrying data-i18n="session.form.nextSessionDate" and an input with class="input" id="nextSessionDate" type="date". Use class="input" (like #inlineClientBirthDate / #editClientBirthDate), NOT input-pill (reserved for the hero #sessionDate). Give the wrapping .form-field a margin-top:1rem to match the field-to-field gap. Do not mark it required and do not add any "(optional)" hint. Keep the #customerSummary textarea and its field-header exactly where they are.</action>
  <verify>
    <automated>grep -q 'id="nextSessionDate"' add-session.html && grep -q 'type="date"' add-session.html && grep -q 'data-i18n="session.form.nextSessionDate"' add-session.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - add-session.html contains an input with id="nextSessionDate" type="date" class="input" (not input-pill), not marked required
    - The input's label uses data-i18n="session.form.nextSessionDate"
    - The new field appears AFTER the #customerSummary textarea and still inside data-section-key="nextSession" (note stays first, date below)
    - No placeholder/"(optional)" hint text is added
  </acceptance_criteria>
  <done>The #nextSessionDate date input renders below the note inside the nextSession section, mirroring the birthdate input shape. (D-06, D-07)</done>
</task>

<task type="auto">
  <name>Task 2: Wire nextSessionDate through save/add/populate/reset/snapshot + dynamic min in add-session.js</name>
  <files>assets/add-session.js</files>
  <read_first>
    - assets/add-session.js (:118 #sessionDate grab; :129 #customerSummary grab; :506-513 new-session default incl. sessionDate.value = window.DateFormat.todayLocalISO(); :720-746 snapshotFormState incl. :739 customerSummary capture; :751-753 revertSessionForm -> populateSession; :1114 read-on-save; :1136 update payload; :1155 add payload; :1651-1668 populateSession incl. :1660 sessionDate.value and :1664 customerSummary.value)
    - add-session.html (the #nextSessionDate element added in Task 1; #sessionDate at :86)
    - assets/date-format.js (window.DateFormat.todayLocalISO — used only for the session date default; nextSessionDate itself has no default value)
    - tests/38-next-session.test.js (Plan 38-01 — the behavior contract this must satisfy)
  </read_first>
  <action>Mirror the #customerSummary wiring at every site AND add the two refinements CONTEXT omitted (snapshotFormState capture + dynamic min). Specifically: (1) Read on save near :1114 — read document.getElementById("nextSessionDate")?.value or "" (no .trim(); native date value is already clean). (2) Add nextSessionDate to BOTH the updateSession payload object (:1136 area, beside customerSummary) and the addSession payload object (:1155 area, beside customerSummary). (3) In populateSession (:1657-1664 area) grab #nextSessionDate and set its .value = session.nextSessionDate or "". (4) In snapshotFormState (:720-746, beside the customerSummary capture at :739) add nextSessionDate: <the #nextSessionDate element> ? el.value : "" so revertSessionForm restores it. (5) On new-session/clear (:506-513 area) set #nextSessionDate.value = "". (6) Add a syncNextSessionMin() helper that reads #sessionDate.value (via document.getElementById, NOT a closure variable) and, when non-empty, sets #nextSessionDate.min to that value (same-day allowed), otherwise REMOVES the min attribute — never min=today. SCOPE CONSTRAINT (architect-gate finding): define syncNextSessionMin() at TOP-LEVEL module scope, column-0, alongside populateSession/populateSpotlight/computeGrowHeight — NOT inside the DOMContentLoaded closure (:85-:1280). populateSession (:1651) is a top-level function OUTSIDE that closure (proof: it re-grabs sessionDate via getElementById at :1653 instead of closing over the :118 const); a closure-scoped helper would throw ReferenceError mid-populate and abort the whole edit-session load and the Cancel→Discard revert. Call syncNextSessionMin() at the new-session default (after sessionDate.value is set, :511 area), at populate time (after sessionDate.value = session.date, :1660 area), and register a "change" listener on #sessionDate that calls syncNextSessionMin(). Guard every getElementById with optional access so a missing element never throws.</action>
  <verify>
    <automated>node tests/38-next-session.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/38-next-session.test.js` passes (was RED in Plan 38-01): save persists nextSessionDate, populate sets .value, new-session resets to "", and dynamic min equals #sessionDate.value with the attribute REMOVED when the session date is empty
    - `node tests/30-form-dirty-revert.test.js` passes including the new nextSessionDate snapshot-capture assertion
    - nextSessionDate appears in BOTH the updateSession and addSession payloads and in the snapshotFormState return
    - A "change" listener on #sessionDate re-applies min via syncNextSessionMin(); no code sets min to today
    - syncNextSessionMin is defined at column-0 top-level in assets/add-session.js (grep '^function syncNextSessionMin' matches; grep '^  function syncNextSessionMin\|^    function syncNextSessionMin' does not) — reachable from populateSession outside the DOMContentLoaded closure; editing an existing session throws no ReferenceError
    - `node tests/run-all.js` shows no NEW failures attributable to add-session.js (pre-existing unrelated RED from other Wave 2 plans excepted)
  </acceptance_criteria>
  <done>nextSessionDate round-trips through save/add/populate/reset/snapshot and the dynamic min tracks the session date. (D-05, D-08, NEXT-01, NEXT-02)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → session record | The date value crosses from the form into the IndexedDB session object |
| session date → min constraint | #sessionDate drives the allowable range of #nextSessionDate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | #nextSessionDate input | high | mitigate | Native `<input type="date">` (D-06) constrains the value to YYYY-MM-DD; no free-text parse path; min bound to the session date (D-08) |
| T-38-06 | Tampering | snapshotFormState/revert | medium | mitigate | Snapshot captures nextSessionDate so revert cannot silently blank a saved value (Pitfall 2), guarded by tests/30-form-dirty-revert.test.js |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node tests/38-next-session.test.js` GREEN (field + dynamic min)
- `node tests/30-form-dirty-revert.test.js` GREEN (snapshot capture)
- No code path sets #nextSessionDate.min to today; min tracks #sessionDate and is removed when empty
</verification>

<success_criteria>
The optional next-session date field is stored on the session record, revert-safe, with a dynamic session-relative min — NEXT-01 and NEXT-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-04-SUMMARY.md` when done.
</output>
