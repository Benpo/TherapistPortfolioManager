---
phase: 38-next-session-date-field-with-overview-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/38-next-session.test.js
  - tests/38-next-overdue.test.js
autonomous: true
requirements: [NEXT-01, NEXT-02, NEXT-05, NEXT-08]
must_haves:
  truths:
    - "tests/38-next-session.test.js exercises the REAL add-session page (jsdom) and asserts the new #nextSessionDate field is saved into the session record, populated on edit, and reset on new/clear (D-05, D-06 — native date value never free text; NEXT-01)"
    - "tests/38-next-session.test.js asserts the dynamic min = the session's own #sessionDate value (same-day allowed), unset when #sessionDate is empty, and re-applied on #sessionDate change (D-08 — NOT min=today; NEXT-02)"
    - "tests/38-next-overdue.test.js is TZ-pinned to America/New_York with an offset self-check that aborts loudly if inert (never false-GREEN), and asserts overdue is TRUE for a yesterday-local date, FALSE for today-local (boundary), FALSE for tomorrow (D-04 strictly-before-today; NEXT-05, NEXT-08)"
    - "Both new test files are RED-by-design at authoring time (the field/cue do not exist yet) and are written to flip GREEN only when the real implementation lands — not weakened to pass"
  artifacts:
    - "tests/38-next-session.test.js"
    - "tests/38-next-overdue.test.js"
  key_links:
    - "38-next-session.test.js harness mirrors tests/30-form-dirty-revert.test.js (real add-session jsdom page)"
    - "38-next-overdue.test.js TZ inert-guard mirrors tests/37-date-format.test.js:27-46 (pin TZ, assert EDT offset, exit(1) if not)"
    - "overdue comparison uses window.DateFormat.parseLocal + todayLocalISO (assets/date-format.js) — the canonical local-time engine, never new Date(isoString)"
---

<objective>
Author the two NEW falsifiable behavior tests that gate the next-session field logic BEFORE any implementation runs: field save/populate/reset + dynamic `min` (NEXT-01/02) and the TZ-pinned overdue-boundary comparison (NEXT-05/08).

Purpose: Per MEMORY `feedback-behavior-verification`, runtime-behavior code requires falsifiable behavior tests written BEFORE implementation. Per MEMORY `feedback-checker-nyquist-false-positive`, these are real gates. The overdue-today boundary in particular is a wall-clock-local off-by-one hazard (the exact Phase 37 bug class) and MUST be TZ-pinned with an inert-guard so it can never pass vacuously.
Output: Two RED-by-design test files that the Wave 2 form + overview plans flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification — they do not exist before this phase):
- Session-record field: `nextSessionDate` (YYYY-MM-DD string, optional)
- Form element id: `#nextSessionDate`
- Helper functions: `syncNextSessionMin()` (add-session.js), `mostRecentSession(sessions)` (overview.js)
- i18n keys: `overview.table.nextSession`, `overview.filter.sort.nextSession`, `session.form.nextSessionDate`, `overview.table.nextSession.overdue`
- Sort key: `nextSession` (value in `SORT_DEFAULT_DIR`)
- CSS: `.next-session-cell`, `.next-session-cell.is-overdue`, `.next-overdue-dot`
- DOM: overview `<th data-sort-key="nextSession">`, `<option value="nextSession">`
- New test files: `tests/38-next-session.test.js`, `tests/38-next-overdue.test.js`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/38-next-session.test.js — field save/populate/reset + dynamic min (RED)</name>
  <files>tests/38-next-session.test.js</files>
  <read_first>
    - tests/30-form-dirty-revert.test.js (harness to mirror — real add-session.js jsdom page boot, form-state capture pattern)
    - assets/add-session.js (:129 element grab, :506-513 new-session default, :720-746 snapshotFormState, :1114 read-on-save, :1125-1160 add/update payloads, :1651-1668 populateSession — the sites the field will be wired into)
    - add-session.html (:328-339 data-section-key="nextSession" section, :86 #sessionDate input shape)
    - assets/date-format.js (window.DateFormat.parseLocal / todayLocalISO — the engine the min/overdue logic depends on)
    - tests/run-all.js (exit-0/1 auto-discovery contract — no new runner)
  </read_first>
  <behavior>
    - Save: entering a value in #nextSessionDate and saving persists session.nextSessionDate === that YYYY-MM-DD string (NEXT-01)
    - Populate: loading an existing session with a nextSessionDate sets #nextSessionDate.value to it; a session lacking the key leaves the input empty (no crash) (NEXT-01, D-05)
    - Reset: starting a NEW session clears #nextSessionDate.value to "" (NEXT-01)
    - Dynamic min (D-08 / NEXT-02): after the session date is set, #nextSessionDate.min === #sessionDate.value (same-day allowed); when #sessionDate is emptied the min attribute is REMOVED (not set to today); changing #sessionDate re-applies min to the new value
  </behavior>
  <action>Create tests/38-next-session.test.js modeled on the tests/30-form-dirty-revert.test.js harness (boot the real assets/add-session.js against a jsdom add-session.html page; inject window.DateFormat via the date-format.js the other tests load). Assert the four behaviors in the behavior block against the REAL module output — never a stub of the field. Reference the session-record field name `nextSessionDate`, the element id `#nextSessionDate`, and the sibling `#sessionDate` exactly. The min assertion must check the attribute value equals the session date string AND that emptying the session date REMOVES the attribute (assert min is absent, NOT equal to today) so a mistaken `min=today` implementation fails. At authoring time these assertions are RED (field + syncNextSessionMin do not exist yet) — write them to pass ONLY once Plan 38-04 lands; do not weaken. Register nothing new in run-all.js (auto-discovered by filename).</action>
  <verify>
    <automated>node tests/38-next-session.test.js; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/38-next-session.test.js` exits non-zero (RED) because #nextSessionDate / syncNextSessionMin are not yet implemented — NOT because of a harness/syntax error (the file loads the real add-session.js and reaches the assertions)
    - The file contains assertions for all four behaviors: save persists `nextSessionDate`, populate sets `.value`, new-session resets to "", and dynamic `min` equals `#sessionDate.value` with attribute REMOVED when the session date is empty
    - The min test asserts the attribute is ABSENT when session date is empty (proves the implementation is not `min=today`)
    - Test uses `window.DateFormat.parseLocal` / `todayLocalISO` for any date construction (no `new Date("YYYY-MM-DD")`)
  </acceptance_criteria>
  <done>tests/38-next-session.test.js exists, is falsifiable, RED-by-design for the four NEXT-01/02 behaviors, and will flip GREEN when Plan 38-04 wires the field. (D-05, D-06, D-08)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/38-next-overdue.test.js — TZ-pinned overdue boundary (RED)</name>
  <files>tests/38-next-overdue.test.js</files>
  <read_first>
    - tests/37-date-format.test.js (:27-46 TZ inert-guard — pin process.env.TZ=America/New_York via re-exec, compute a known EDT offset, exit(1) loudly if the offset did not take; :208-211 boundary assertions)
    - assets/date-format.js (window.DateFormat.parseLocal, todayLocalISO — the local-time engine the overdue comparison must use)
    - assets/overview.js (:630 empty-cell `-` pattern, :704-709 textContent-only render note) — for the shape the cue logic will live in
    - .planning/phases/38-next-session-date-field-with-overview-column/38-UI-SPEC.md (§2 overdue cue: strictly-before-today, today NOT overdue)
  </read_first>
  <behavior>
    - Inert-guard: process.env.TZ is pinned to America/New_York and a self-check of a known date's getTimezoneOffset aborts with a non-zero exit if the offset did not take (test can never pass vacuously) — NEXT-08
    - Overdue TRUE for a nextSessionDate strictly before today-local (e.g. yesterday) — NEXT-05
    - Overdue FALSE for today-local (the boundary: today itself is NOT overdue) — NEXT-05, D-04
    - Overdue FALSE for a future date (tomorrow) — NEXT-05
    - Empty/missing nextSessionDate is never overdue (renders `-`, no marker) — D-04
  </behavior>
  <action>Create tests/38-next-overdue.test.js. Mirror the TZ inert-guard in tests/37-date-format.test.js:27-46 exactly: pin `process.env.TZ` to `America/New_York`, assert a known-date `getTimezoneOffset()` equals the expected EDT value, and `process.exit(1)` (loud abort) if it does not — so an environment that ignored TZ fails instead of passing. Define the overdue predicate under test as `window.DateFormat.parseLocal(nextSessionDate) < todayLocalDate` where `todayLocalDate = window.DateFormat.parseLocal(window.DateFormat.todayLocalISO())` (build "today" from the local ISO, never `new Date().toISOString().slice(0,10)`). Assert TRUE for yesterday-local, FALSE for today-local (the boundary case that guards the off-by-one), FALSE for tomorrow, and no-overdue for empty. Compute yesterday/today/tomorrow from `todayLocalISO()` so the test is not date-brittle. RED-by-design if the overdue helper does not yet exist in a shared location; if the predicate is authored inline in the test (pure logic), keep the assertions falsifiable so a `<=` (today-inclusive) implementation would FAIL the today-boundary case. Do not weaken the boundary assertion.</action>
  <verify>
    <automated>node tests/38-next-overdue.test.js 2>&1 | grep -qi 'overdue\|boundary\|America/New_York'</automated>
  </verify>
  <acceptance_criteria>
    - The file pins `process.env.TZ` to `America/New_York` and aborts loudly (non-zero exit) if a known date's `getTimezoneOffset()` is not the expected EDT value (inert-guard present — mirrors 37-date-format.test.js:27-46)
    - Asserts overdue TRUE for yesterday-local, FALSE for today-local (boundary), FALSE for tomorrow, and never-overdue for empty
    - "Today" is derived via `window.DateFormat.todayLocalISO()` + `parseLocal` — no `new Date().toISOString().slice(0,10)` and no `new Date("YYYY-MM-DD")`
    - The today-boundary assertion would FAIL a `<=` (today-inclusive) implementation — proving it guards the strictly-before rule
  </acceptance_criteria>
  <done>tests/38-next-overdue.test.js exists, is TZ-pinned with a non-vacuous inert-guard, and locks the strictly-before-today overdue boundary (today NOT overdue). (D-04, NEXT-05, NEXT-08)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real module | Test loads and executes the real add-session.js / date engine; a broken harness could pass vacuously |
| CI time zone → date logic | The overdue comparison is wall-clock-local; an un-pinned TZ makes the boundary test non-deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Repudiation | tests/38-next-overdue.test.js | high | mitigate | TZ pinned to America/New_York + getTimezoneOffset self-check that exits non-zero if inert (mirrors 37-date-format.test.js) — no false-GREEN |
| T-38-02 | Tampering | tests/38-next-session.test.js | medium | mitigate | Assert against REAL add-session.js output (no field stub); min-absent assertion rejects a `min=today` mistake |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages; RESEARCH.md Package Legitimacy Audit = not applicable |
</threat_model>

<verification>
- `node tests/38-next-session.test.js` runs the real add-session.js and is RED for the four NEXT-01/02 behaviors (falsifiable, not a load error)
- `node tests/38-next-overdue.test.js` inert-guard passes (offset self-check) and the boundary assertions are RED-by-design
- Neither file weakens to green; both are picked up by tests/run-all.js auto-discovery
</verification>

<success_criteria>
Both new falsifiable test files exist, are TZ-safe where date-sensitive, and gate NEXT-01/02/05/08 before implementation.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-01-SUMMARY.md` when done.
</output>
