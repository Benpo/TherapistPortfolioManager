---
phase: 38-next-session-date-field-with-overview-column
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/37-overview-sort.test.js
  - tests/30-export-markdown.test.js
  - tests/30-section-visibility.test.js
  - tests/35-demo-seed.test.js
  - tests/30-form-dirty-revert.test.js
autonomous: true
requirements: [NEXT-03, NEXT-04, NEXT-06, NEXT-07, NEXT-08]
must_haves:
  truths:
    - "tests/37-overview-sort.test.js allSortKeys assertion (:164-165) includes 'nextSession', and new cases assert nextSession defaults to ASCENDING (soonest first), empty/missing next-dates sort to the BOTTOM under BOTH directions, and the header ↔ #clientSortSelect two-way sync covers the new key (D-01, D-03; NEXT-03, NEXT-04)"
    - "tests/30-export-markdown.test.js asserts the next-session DATE line renders in the export markdown alongside the note, AND a date-only session (note empty, date present) still renders the nextSession block (D-09 empty-note gate becomes note-OR-date; NEXT-06)"
    - "tests/30-section-visibility.test.js asserts the nextSession include-toggle defaults ON for a date-only session (content check keys off note OR date), and excluding the section omits BOTH note and date (D-09; NEXT-06)"
    - "tests/35-demo-seed.test.js asserts recent seeded sessions carry a relative nextSessionDaysAgo helper that applyRelativeDates converts to a self-freshening nextSessionDate which is near-future (>= the session's own computed date, not all overdue) (D-12; NEXT-07)"
    - "tests/30-form-dirty-revert.test.js gains an assertion that snapshotFormState captures nextSessionDate so Cancel→Discard restores it (research Pitfall 2)"
    - "All edits are RED-by-design until the Wave 2 implementation lands — not weakened to green"
  artifacts:
    - "tests/37-overview-sort.test.js (extended)"
    - "tests/30-export-markdown.test.js (extended)"
    - "tests/30-section-visibility.test.js (extended)"
    - "tests/35-demo-seed.test.js (extended)"
    - "tests/30-form-dirty-revert.test.js (extended)"
  key_links:
    - "sort test derives most-recent value the SAME way the render does (date/createdAt/id tiebreak) — not lastSession's reduce-max (D-01)"
    - "blanks-to-bottom assertion runs under BOTH ascending and descending (early-return escapes dir*base) (D-03)"
    - "backup round-trip of nextSessionDate follows the snippet-prefix-backup-roundtrip.test.js whole-object pattern (D-11)"
---

<objective>
Extend the existing test suites to gate the overview column + sort (NEXT-03/04), the export date + date-only rendering (NEXT-06), demo seeding (NEXT-07), and the snapshot-revert capture — all BEFORE implementation.

Purpose: These are the falsifiable behavior gates for the display/sort/export/demo surfaces. The blanks-to-bottom and most-recent-derivation rules are the two subtle correctness traps (research Pitfalls 1 & 5); locking them in tests first prevents a copy-the-lastSession-reduce-max regression.
Output: Five extended test files, RED-by-design, that Wave 2 flips GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md
@.planning/phases/38-next-session-date-field-with-overview-column/38-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols created by Phase 38 (exclude from cross-plan drift verification):
- Session-record field `nextSessionDate`; form element `#nextSessionDate`
- Helpers `mostRecentSession(sessions)` (overview.js), `syncNextSessionMin()` (add-session.js)
- i18n keys `overview.table.nextSession`, `overview.filter.sort.nextSession`, `session.form.nextSessionDate`, `overview.table.nextSession.overdue`
- Sort key `nextSession`; CSS `.next-session-cell`, `.is-overdue`, `.next-overdue-dot`
- DOM `<th data-sort-key="nextSession">`, `<option value="nextSession">`
- New test files `tests/38-next-session.test.js`, `tests/38-next-overdue.test.js`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/37-overview-sort.test.js — nextSession key, ascending default, blanks-to-bottom both directions, sync (RED)</name>
  <files>tests/37-overview-sort.test.js</files>
  <read_first>
    - tests/37-overview-sort.test.js (:106 allSortKeys helper, :146-165 sortable-headers test + the deepEquals assertion at :164-165, :216 dropdown-drives-state test — the sync analog)
    - assets/overview.js (:260 SORT_DEFAULT_DIR, :302-319 comparator + `return dir * base`, :619-626 most-recent date/createdAt/id tiebreak, :630 lastSession cell value, :667-697 cell append order)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Pattern 1 & 2 — most-recent derivation ≠ reduce-max; blanks escape dir*base via early-return)
  </read_first>
  <behavior>
    - allSortKeys(win) now equals ['name','sessions','lastSession','nextSession'] (the Next Session column is sortable; Type + Actions remain non-sortable) (NEXT-03/04)
    - Selecting/activating the nextSession sort defaults to ASCENDING (soonest next-date first) — opposite of lastSession's descending (D-03)
    - Rows whose most-recent session has an empty/missing nextSessionDate sort to the BOTTOM under BOTH ascending and descending directions (D-03)
    - The sorted value matches the DISPLAYED value: derived from the most-recent session (date/createdAt/id tiebreak), NOT a reduce-max across sessions (D-01)
    - Header click ↔ #clientSortSelect stay in sync for nextSession (mirrors the lastSession sync test at :216)
  </behavior>
  <action>Extend tests/37-overview-sort.test.js. Update the allSortKeys deepStrictEqual at :164-165 to `['name','sessions','lastSession','nextSession']` and update its message. Add a test that sets sort to `nextSession`, asserts the resulting header order is ascending-by-next-date (soonest first) and that rows with a blank most-recent nextSessionDate appear LAST; then flip direction to descending and assert the blanks are STILL last (this is the assertion that fails if blanks ride through `dir * base`). Construct fixtures where a client's MOST-RECENT session has a blank next-date but an OLDER session has one — assert that client sorts as blank (guards the reduce-max regression per D-01). Add a header↔#clientSortSelect sync case for `nextSession` mirroring the :216 lastSession test. Keep the derivation matching the render tiebreak (date desc, then createdAt desc, then id desc). RED-by-design until Plan 38-05 adds the `nextSession` sort branch, `mostRecentSession()`, and the `<th>`/`<option>`; do not weaken.</action>
  <verify>
    <automated>node tests/37-overview-sort.test.js; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The allSortKeys assertion reads `['name','sessions','lastSession','nextSession']`
    - A blanks-to-bottom case asserts blank next-dates are last under ASCENDING and under DESCENDING (two directions)
    - A fixture proves the sorted value comes from the most-recent session (a newer blank overrides an older non-blank) — the reduce-max regression guard
    - `node tests/37-overview-sort.test.js` is RED (the nextSession sort branch / `<th>` do not exist yet), failing on the new assertions rather than a load error
  </acceptance_criteria>
  <done>tests/37-overview-sort.test.js locks the sort key, ascending default, blanks-to-bottom under both directions, most-recent derivation, and sync — RED until Plan 38-05. (D-01, D-03)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend export + section-visibility tests — date line + date-only render + toggle gating (RED)</name>
  <files>tests/30-export-markdown.test.js, tests/30-section-visibility.test.js</files>
  <read_first>
    - tests/30-export-markdown.test.js (the live-builder markdown assertions — the harness that exercises export-modal.js buildExportMarkdown)
    - tests/30-section-visibility.test.js (the per-section include-toggle default assertions)
    - assets/export-modal.js (:142-144 nextSession content check keyed off #customerSummary; :208/:274-276 copy builder gate; :410-412 export builder gate — all currently gate on note length only)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Pitfall 4 — empty-note gate must become note-OR-date)
  </read_first>
  <behavior>
    - A session with BOTH note and next-date renders the note AND a formatted date line under the "Information for Next Session" heading (NEXT-06, D-09)
    - A date-only session (note empty, nextSessionDate present) still renders the nextSession block with the date (NEXT-06, D-09)
    - Excluding the nextSession section omits BOTH the note and the date (D-09)
    - The nextSession include-toggle defaults ON when only a date is present (content check keys off note OR date) (D-09)
  </behavior>
  <action>Extend tests/30-export-markdown.test.js to assert the export markdown contains the formatted next-session date (via the same window.DateFormat/App.formatDate path the export uses) when a nextSessionDate is set, and that a date-only session (empty #customerSummary, populated #nextSessionDate) still emits the nextSession heading + date line. Extend tests/30-section-visibility.test.js to assert the `nextSession` content-check / toggle-default returns true for a date-only session and that de-selecting the section drops both note and date. Assert against the real export-modal.js builders (both copy and export paths per D-09). RED-by-design until Plan 38-06 changes the gates to `(note OR date present)` and appends the date line; do not weaken.</action>
  <verify>
    <automated>node tests/30-export-markdown.test.js; rc=$?; node tests/30-section-visibility.test.js; test $rc -ne 0 -o $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/30-export-markdown.test.js asserts a formatted next-session date appears in the export markdown and that a date-only session still renders the block
    - tests/30-section-visibility.test.js asserts the nextSession toggle defaults ON for a date-only session and OFF-excluded drops both note and date
    - Both files are RED-by-design (export builders still gate on note length only), failing on the new assertions not on load errors
  </acceptance_criteria>
  <done>Export + section-visibility tests lock the date line, date-only rendering, and section-toggle gating — RED until Plan 38-06. (D-09, NEXT-06)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend demo-seed + form-dirty-revert tests — seeded near-future dates + snapshot capture (RED)</name>
  <files>tests/35-demo-seed.test.js, tests/30-form-dirty-revert.test.js</files>
  <read_first>
    - tests/35-demo-seed.test.js (:61-63 permitted-seed-key shape validator; :200-211 DEMO-06 daysAgo relative-date assertions over assets/demo-seed-data.json)
    - assets/demo-seed.js (:18 isoDaysAgo(n,now); :28-40 applyRelativeDates converts daysAgo→date and deletes daysAgo, exposed via window.__demoSeedHelpers — the self-freshening seam the new nextSessionDaysAgo helper must mirror)
    - assets/demo-seed-data.json (recent seeded session objects with per-session integer daysAgo; customerSummary notes ~:125/:151 — where nextSessionDaysAgo will be added)
    - tests/30-form-dirty-revert.test.js (snapshotFormState capture + revertSessionForm assertions — add the nextSessionDate capture guard here)
    - assets/add-session.js (:720-746 snapshotFormState, :751-753 revert delegates to populateSession)
    - .planning/phases/38-next-session-date-field-with-overview-column/38-RESEARCH.md (Pitfall 2 — snapshot must capture nextSessionDate)
  </read_first>
  <behavior>
    - A few recent seeded sessions carry a relative `nextSessionDaysAgo` helper (negative = future), and after applyRelativeDates each yields a `nextSessionDate` that is near-future — i.e. >= the session's own computed `date` (so the demo column self-freshens and is not all-overdue) (NEXT-07, D-12)
    - The seed-shape validator permits the new `nextSessionDaysAgo` key (added to its allowlist) and the post-seam object carries `nextSessionDate` (not the helper key)
    - snapshotFormState() output includes a nextSessionDate key so Cancel→Discard restores the field to its last-saved value instead of blanking it (research Pitfall 2)
  </behavior>
  <action>Extend tests/35-demo-seed.test.js: add `nextSessionDaysAgo` to the permitted-seed-key allowlist (:61-63); assert at least a couple of seeded sessions carry an integer `nextSessionDaysAgo`; and assert that after running the real window.__demoSeedHelpers.applyRelativeDates (mirroring the DEMO-06 daysAgo pattern) each such session has a `nextSessionDate` that is >= its own computed `date` (near-future / not overdue), allowing at most one deliberately-past value to showcase the overdue cue. This relative model — not a static absolute date — is what keeps the demo from drifting all-overdue over time. Extend tests/30-form-dirty-revert.test.js to assert the object returned by snapshotFormState (or the restored state after revert) includes `nextSessionDate` equal to the field's value, so an omission (the Pitfall 2 gap) fails. RED-by-design until Plan 38-07 extends applyRelativeDates + seeds the JSON and Plan 38-04 adds the snapshot capture; do not weaken.</action>
  <verify>
    <automated>node tests/35-demo-seed.test.js; rc=$?; node tests/30-form-dirty-revert.test.js; test $rc -ne 0 -o $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/35-demo-seed.test.js permits `nextSessionDaysAgo`, asserts recent sessions carry it, and asserts the post-applyRelativeDates `nextSessionDate` is ≥ the session's own computed `date` (near-future; at most one past)
    - tests/30-form-dirty-revert.test.js asserts snapshot/revert round-trips `nextSessionDate` (the Pitfall 2 guard)
    - Both files are RED-by-design, failing on the new assertions not on load errors
  </acceptance_criteria>
  <done>Demo-seed + revert tests lock the self-freshening relative next-date model and snapshot capture — RED until Plans 38-07/38-04. (D-11, D-12, NEXT-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real builders/store | Tests execute the real overview.js sort + export-modal.js builders; a stubbed assertion could pass vacuously |
| seed JSON → demo render | Seeded dates flow to the demo overview column; wrong values misrepresent the overdue cue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-03 | Repudiation | tests/37-overview-sort.test.js | high | mitigate | Assertions run the REAL comparator + fixtures that would pass a reduce-max bug FAIL (most-recent derivation guard); blanks tested under both directions |
| T-38-04 | Tampering | tests/30-export-markdown.test.js | medium | mitigate | Assert against the live export builders (copy + export), not a stub, so a note-only gate regression fails |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md audit = not applicable) |
</threat_model>

<verification>
- Each extended file is RED-by-design on its new assertions and fails for the right reason (missing implementation, not a load error)
- The reduce-max regression guard and the blanks-both-directions guard are present in 37-overview-sort.test.js
- No test is weakened to green; all flip GREEN only when the Wave 2 implementation lands
</verification>

<success_criteria>
Five extended test files gate NEXT-03/04/06/07/08 with falsifiable, correctness-trap-aware assertions before implementation.
</success_criteria>

<output>
Create `.planning/phases/38-next-session-date-field-with-overview-column/38-02-SUMMARY.md` when done.
</output>
