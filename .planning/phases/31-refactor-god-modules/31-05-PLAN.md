---
phase: 31-refactor-god-modules
plan: 05
type: execute
wave: 4
depends_on: ["31-04"]
files_modified:
  - assets/export-modal.js
  - assets/add-session.js
  - add-session.html
  - sw.js
  - tests/30-export-markdown.test.js
  - tests/30-export-stepper.test.js
autonomous: true
requirements: [RFCT-02, RFCT-03]
must_haves:
  truths:
    - "buildSessionMarkdown payload (issues section, insights placement) via copySessionBtn/exportSessionBtn real .click() lands on the clipboard unchanged — proven by tests/30-export-markdown.test.js"
    - "Export stepper 1->2->3, close, download-seam, MdRender branch, mobile-tabs work via real clicks — proven by tests/30-export-stepper.test.js"
    - "Per-field scoped copy payload unchanged — proven by tests/30-field-copy.test.js; issues->markdown delta/cap unchanged — proven by tests/30-issue-delta.test.js"
    - "The export module reads live mutable state (editingSession/isReadMode/sessionId) via ctx accessor closures (one source of truth in add-session.js) — no ReferenceError on first click"
  artifacts:
    - assets/export-modal.js
    - window.__exportModalInit
  key_links:
    - "add-session.js calls window.__exportModalInit(ctx) at its export-button wiring point (~add-session.js:1582) passing accessor closures for editingSession/sessionId/isReadMode/getIssuesPayload + DOM els"
    - "export-modal.js loads BEFORE add-session.js (add-session.html script order + test eval order) so __exportModalInit exists when add-session.js boots"
    - "the :2071 console.warn 'Phase 24 Plan 06' log string is rewritten phase-free (D-05)"
---

<objective>
RFCT-02 (extraction 3 of 3, D-07 order: export-modal LAST — the hardest) — Extract the export-modal + markdown builders (`add-session.js` ~730-1835: `buildSessionMarkdown` @776, `buildFilteredSessionMarkdown` @1078, `stripRequired` @772, and the stepper/modal handlers @1220-1801) out of the single `DOMContentLoaded` closure (`add-session.js:44-1863`) into a NEW page-private file `assets/export-modal.js`.

This is a CLOSURE problem, not a coverage problem: the export region captures ~10 closure-locals declared at `add-session.js:76-104` + `getIssuesPayload` @662 (counts in the 765-1835 region: sessionDate x19, editingSession x15, clientSelect x7, insightsInput x6, customerSummaryInput x6, sessionId x4, getIssuesPayload x3, ...). A cut-paste throws ReferenceError. Use the context-injection `init(ctx)` handshake (RESEARCH Pattern 2 / PATTERNS.md): export-modal.js exposes a private `window.__exportModalInit`, and add-session.js calls it passing accessor closures for the MUTABLE state and the DOM els. DOM elements may be re-`getElementById`'d in the new file; mutable JS state (editingSession/isReadMode/sessionId/lastSavedSnapshot) MUST be threaded via accessors (never re-read — that would fork state).

Per D-06 atomic protocol; per D-02 behavior-preserving move.
Output: `assets/export-modal.js` (init(ctx) handshake) + slimmed add-session.js + the :2071 log-string fix + wiring + updated export test loaders.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files: `assets/settings-snippets.js` (plan 03), `assets/settings-photos.js` (plan 04), `assets/export-modal.js` (this plan); tests `tests/31-openDB-pooling.test.js`, `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js` (plans 01-02). NEW symbol: `window.__exportModalInit` (private handshake — the ONLY new global this phase adds, and it is internal, not a feature API). This plan's artifacts: `assets/export-modal.js` + `window.__exportModalInit`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-RESEARCH.md
@.planning/phases/31-refactor-god-modules/31-PATTERNS.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/add-session.js
@add-session.html
@tests/30-export-markdown.test.js
@tests/30-export-stepper.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/export-modal.js with the init(ctx) context-injection handshake</name>
  <files>assets/export-modal.js</files>
  <read_first>
    - assets/add-session.js:76-104 (closure-local declarations: clientSelect, sessionDate=getElementById("sessionDate"), insightsInput=getElementById("sessionInsights"), customerSummaryInput=getElementById("customerSummary"), submitButton, sessionId, and the MUTABLE `let editingSession`/`let isReadMode`/`let lastSavedSnapshot`) and :662 (getIssuesPayload)
    - assets/add-session.js:772-776 (stripRequired, buildSessionMarkdown), :1078 (buildFilteredSessionMarkdown), :1220-1801 (the export modal handlers: exportSetActiveStep / exportUpdatePreview / exportHandle* / download-seam / MdRender branch / mobile-tabs)
    - assets/add-session.js:37-42 (the `Object.assign(window.__addSessionTestHooks || {}, {...})` guarded private-hook idiom — mirror this `typeof window !== "undefined"` guard for `window.__exportModalInit`)
    - .planning/phases/31-refactor-god-modules/31-PATTERNS.md §"assets/export-modal.js" (the exact ctx shape + accessor template) and 31-RESEARCH.md §"Pattern 2" + §"Pitfall 1"
  </read_first>
  <action>Create assets/export-modal.js as an anonymous page-private IIFE that defines `function initExportModal(ctx) { ... }` and registers it via `window.__exportModalInit = initExportModal;` behind a `typeof window !== "undefined"` guard (mirroring add-session.js:37-42). MOVE into initExportModal (verbatim logic): stripRequired, buildSessionMarkdown, buildFilteredSessionMarkdown, and the export stepper/modal handlers from add-session.js ~772-1801. Rewrite their references to closure state per the ctx contract: ctx = { getEditingSession, getSessionId, isReadMode, getIssuesPayload, els: { sessionDate, clientSelect, insightsInput, customerSummaryInput } }. For MUTABLE JS state, call the accessors at point-of-use (e.g. replace `editingSession` reads with `ctx.getEditingSession()`, `sessionId` with `ctx.getSessionId()`, `isReadMode` with `ctx.isReadMode()`); these are live reads so behavior is identical. For DOM elements, either read them from `ctx.els.*` or re-`getElementById` the SAME static IDs from add-session.html (IDs unchanged) — keep one consistent choice. Do NOT re-read mutable JS state via the DOM. The export functions are NOT registered on `__addSessionTestHooks` (tests drive them through real button clicks) — do not add new test hooks. Apply RFCT-03 cleanups within the moved region only (add-session.js has 0 `var`, so no var conversion; rewrite any `// Phase N` comments in the moved region into what-it-does comments; tagged "export-modal:" prefix on any genuinely-silent catch).</action>
  <verify>
    <automated>node -e "new (require('vm').Script)(require('fs').readFileSync('assets/export-modal.js','utf8')); console.log('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/export-modal.js` exists, parses, and registers `window.__exportModalInit` behind a `typeof window !== "undefined"` guard.
    - It contains the moved `buildSessionMarkdown` / `buildFilteredSessionMarkdown` / stepper handlers, and every former reference to `editingSession`/`sessionId`/`isReadMode` is now a `ctx.get*()`/`ctx.isReadMode()` accessor call (grep shows no bare `editingSession`/`isReadMode` identifier reads inside the module except via ctx).
    - It exposes NO public feature global beyond the private `__exportModalInit` handshake (no `window.ExportModal = ...`).
  </acceptance_criteria>
  <done>export-modal.js holds the export logic behind an init(ctx) handshake reading mutable state via accessors; it parses and exposes only the private __exportModalInit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire add-session.js to call __exportModalInit(ctx), slim the closure, fix the :2071 log string, wire HTML + sw.js</name>
  <files>assets/add-session.js, add-session.html, sw.js</files>
  <read_first>
    - assets/add-session.js:1582-1591 (where copySessionBtn/exportSessionBtn click handlers are currently wired — the natural call site for `window.__exportModalInit({...})`)
    - assets/add-session.js:76-104 + :662 (the live identifiers the ctx accessors must close over: editingSession, sessionId, isReadMode, getIssuesPayload, sessionDate, clientSelect, insightsInput, customerSummaryInput)
    - assets/add-session.js:2071 (`console.warn("Phase 24 Plan 06: failed to load sessions for spotlight:", err)` — the D-05 log-string fix)
    - add-session.html:606-607 (pdf-export.js then add-session.js) and sw.js:47 (`'/assets/add-session.js'`)
    - 31-PATTERNS.md §"assets/export-modal.js" (the call-site template) and §"Wiring"
  </read_first>
  <action>In add-session.js, DELETE the export region that moved to export-modal.js (the builders + stepper/modal handlers ~772-1801), keeping the closure-local declarations (76-104), getIssuesPayload (662), and the non-export logic (form, read-mode, issues, spotlight loader 1865-2173) intact. At the export-button wiring point (~1582), REPLACE the old inline binding with a single call: `window.__exportModalInit({ getEditingSession: () => editingSession, getSessionId: () => sessionId, isReadMode: () => isReadMode, getIssuesPayload, els: { sessionDate, clientSelect, insightsInput, customerSummaryInput } });` — the accessor closures read the LIVE mutable values so editingSession updates inside add-session.js remain visible to the export module. Fix the :2071 log string: rewrite `console.warn("Phase 24 Plan 06: failed to load sessions for spotlight:", err)` to a phase-free message (e.g. `console.warn("spotlight: failed to load sessions:", err)`) per D-05. In add-session.html add `<script src="./assets/export-modal.js"></script>` immediately BEFORE the add-session.js tag (:607, after pdf-export.js :606) so `__exportModalInit` exists when add-session.js boots. In sw.js add `'/assets/export-modal.js',` to PRECACHE_URLS adjacent to the `'/assets/add-session.js'` entry (:47). Do NOT bump CACHE_NAME/APP_VERSION (plan 06).</action>
  <verify>
    <automated>node -e "new (require('vm').Script)(require('fs').readFileSync('assets/add-session.js','utf8')); console.log('parse-ok')" && grep -c "export-modal.js" add-session.html && grep -c "'/assets/export-modal.js'" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/add-session.js` parses and contains exactly one `window.__exportModalInit({` call passing accessor closures for editingSession/sessionId/isReadMode + getIssuesPayload + the four els.
    - `grep -n 'buildSessionMarkdown\|buildFilteredSessionMarkdown\|exportSetActiveStep' assets/add-session.js` returns NOTHING (export logic fully moved out).
    - `grep -n 'Phase 24 Plan 06' assets/add-session.js` returns NOTHING at the :2071 log string (the production log string is phase-free).
    - `grep -n 'export-modal.js' add-session.html` shows one tag positioned BEFORE the add-session.js tag; `grep -n "export-modal.js" sw.js` shows one PRECACHE entry.
  </acceptance_criteria>
  <done>add-session.js calls __exportModalInit with live accessors, the export region is removed, the :2071 log string is phase-free, and export-modal.js is wired before add-session.js + precached.</done>
</task>

<task type="auto">
  <name>Task 3: Update export test loaders (eval export-modal.js BEFORE add-session.js) and prove suite green</name>
  <files>tests/30-export-markdown.test.js, tests/30-export-stepper.test.js</files>
  <read_first>
    - tests/30-export-markdown.test.js:~132 and tests/30-export-stepper.test.js:~146 (the `win.eval(readAsset('assets/add-session.js'))` loader lines; these tests drive real `.click()` on copySessionBtn/exportSessionBtn)
    - 31-RESEARCH.md §"Pitfall 2" + §"Validation Architecture → Extraction 3" (export fns are NOT on __addSessionTestHooks — a broken extraction fails loudly via real clicks)
    - the full export-test set to enumerate: `grep -rln "assets/add-session.js" tests/*.test.js`
  </read_first>
  <action>After Tasks 1-2, run `npm test` — the export tests will go red (unbound buttons / ReferenceError if injection is wrong, or missing __exportModalInit). Enumerate every test that loads add-session.js source AND exercises export behavior: at minimum 30-export-markdown.test.js, 30-export-stepper.test.js, 30-field-copy.test.js, 30-issue-delta.test.js (confirm via grep). In EACH, add `win.eval(readAsset('assets/export-modal.js'))` IMMEDIATELY BEFORE the existing `win.eval(readAsset('assets/add-session.js'))` line so `window.__exportModalInit` exists when add-session.js boots and calls it. Do NOT add export-modal.js to add-session tests that do not touch export (e.g. 30-read-mode, 30-autogrow-wiring, 30-client-spotlight, 30-form-dirty-revert, 30-section-visibility) UNLESS npm test shows them red — add only where needed to keep behavior. Run `npm test` until green and confirm the export assertions still execute for real (clipboard payload equality in 30-export-markdown, activeStep class transitions in 30-export-stepper) — a ReferenceError on first click means the ctx accessor wiring is wrong, fix Task 2 rather than stubbing.</action>
  <verify>
    <automated>node tests/30-export-markdown.test.js && node tests/30-export-stepper.test.js && node tests/30-field-copy.test.js && node tests/30-issue-delta.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js`, `node tests/30-export-stepper.test.js`, `node tests/30-field-copy.test.js`, `node tests/30-issue-delta.test.js` each exit 0.
    - Each export-exercising test `win.eval(readAsset('assets/export-modal.js'))` BEFORE `win.eval(readAsset('assets/add-session.js'))` (grep confirms the ordering).
    - `npm test` exits 0 (full suite green — D-06 commit-when-green gate satisfied).
    - The export assertions are non-vacuous: clipboard-payload equality and activeStep-class transitions still run (no ReferenceError on first .click()).
  </acceptance_criteria>
  <done>Export test loaders eval export-modal.js before add-session.js, the real-click export assertions pass through the new init(ctx) wiring, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Relocation of export/markdown logic into a page-private module behind an internal init(ctx) handshake. No new endpoints, inputs, or data flows; clipboard/share/PDF paths unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-05-A | Availability | export buttons throw ReferenceError if mutable state is cut-pasted instead of injected | high | mitigate | Context-injection (Pattern 2): mutable state threaded via ctx accessor closures, never re-read; export-modal.js loads before add-session.js; 30-export-markdown/30-export-stepper fail loudly on first real .click() if wiring is wrong. |
| T-31-05-A2 | Availability | export-modal.js loads after add-session.js -> __exportModalInit undefined at boot | high | mitigate | Script tag placed immediately BEFORE add-session.js in add-session.html; test eval-order evals export-modal.js before add-session.js (acceptance-gated). |
| T-31-05-T | Tampering | refactor widens public window.* surface | low | mitigate | Only the private `__exportModalInit` handshake is added (internal, guarded); no public feature API; no new test hooks. |
| T-31-05-A3 | Availability | service worker serves stale asset list missing export-modal.js | low | mitigate | PRECACHE_URLS entry added (Task 2); version bump consolidated in plan 06. |

The two `high` items are AVAILABILITY (broken-build) risks fully mitigated by the load-order + injection design and caught by the existing real-click tests — NOT new external attack surface. No new high-severity security/external threat is introduced (security_block_on: high — the mitigations are gated by acceptance criteria + the green suite).
</threat_model>

<verification>
- `node tests/30-export-markdown.test.js`, `node tests/30-export-stepper.test.js`, `node tests/30-field-copy.test.js`, `node tests/30-issue-delta.test.js` exit 0.
- `npm test` full suite exits 0.
- `grep -n 'buildSessionMarkdown\|exportSetActiveStep' assets/add-session.js` returns nothing; `grep -n '__exportModalInit' assets/export-modal.js assets/add-session.js` shows the definition + the one call.
- `grep -n 'Phase 24 Plan 06' assets/add-session.js` returns nothing (log string phase-free).
- `grep -n 'export-modal.js' add-session.html sw.js` shows the tag (before add-session.js) + PRECACHE entry.
</verification>

<success_criteria>
The export-modal + markdown builders are extracted to export-modal.js behind an init(ctx) context-injection handshake (mutable state via live accessors, no ReferenceError), add-session.js calls it with live closures, the :2071 log string is phase-free, wiring + test loaders are updated, and the real-click export suite stays green.
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-05-SUMMARY.md` when done.
</output>
