---
phase: 31-refactor-god-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/31-openDB-pooling.test.js
  - assets/db.js
autonomous: true
requirements: [RFCT-03]
must_haves:
  truths:
    - "Repeated openDB() returns a working IDBDatabase handle (a getAll on a store succeeds) — proven by tests/31-openDB-pooling.test.js"
    - "After db.onversionchange fires and closes the connection, the next openDB() returns a FRESH working handle (cache invalidated) — proven by tests/31-openDB-pooling.test.js"
    - "Concurrent openDB() calls do not double-seed and do not deadlock on the migrateOldDB->openDB recursion path — proven by tests/31-openDB-pooling.test.js"
    - "migrateOldDB() runs at most once per page load (its _migrationDone guard is preserved) — proven by tests/31-openDB-pooling.test.js"
  artifacts:
    - tests/31-openDB-pooling.test.js
  key_links:
    - "openDB() caches _dbPromise ONLY after `await migrateOldDB()` so the migrateOldDB->openDB recursion (db.js:67) resolves without deadlock"
    - "db.onversionchange (db.js:329) and request.onerror (db.js:346) both null out _dbPromise before/with db.close()"
---

<objective>
RFCT-03 — Add connection pooling to `assets/db.js` `openDB()` (cache the resolved `IDBDatabase`), the ONLY runtime-lifecycle change in this phase and the highest-risk cleanup. Per D-03/D-06 this is test-first: write a characterization test that locks the CURRENT observable lifecycle behavior (passes on today's no-pool code), THEN apply pooling so the same test stays green.

Purpose: Fewer redundant `indexedDB.open` calls across the 23 `openDB()` call sites, with cache-invalidation that prevents reuse of a closed connection. This is the riskiest RFCT-03 item because no current test exercises the cached-connection path, and a naive pooling shape would deadlock on the `migrateOldDB()`->`openDB()` recursion.
Output: `tests/31-openDB-pooling.test.js` (new characterization test) + pooled `openDB()` in `db.js`.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files: `assets/settings-snippets.js` (plan 03), `assets/settings-photos.js` (plan 04), `assets/export-modal.js` (plan 05), and the new tests `tests/31-openDB-pooling.test.js` (this plan), `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js` (plan 02).
NEW symbol: `window.__exportModalInit` (private handshake, plan 05). NO other new public `window.*` globals.
NEW wiring: `<script>` tags for the 3 new files in `settings.html`/`add-session.html`; 3 new `sw.js` `PRECACHE_URLS` entries; `APP_VERSION` bump in `assets/version.js` (plan 06).
This plan's artifact: `tests/31-openDB-pooling.test.js` + the pooled `openDB()` in `db.js`. NO new public global.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-RESEARCH.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/db.js
@tests/24-04-idb-migration.test.js
@tests/29-01-crashlog-capture.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterization test for openDB cached-connection lifecycle (RED-then-GREEN on current code)</name>
  <files>tests/31-openDB-pooling.test.js</files>
  <read_first>
    - assets/db.js:1-100 (migrateOldDB at :15, _migrationDone/_seedingDone guards) and :285-412 (openDB at :292 does `await migrateOldDB()` then `indexedDB.open`; onversionchange->close at :329-332; onerror at :346; seedSnippetsIfNeeded idempotency at :361-412)
    - tests/24-04-idb-migration.test.js (the existing raw-IndexedDB harness pattern for exercising db.js directly — reuse its indexedDB shim setup)
    - tests/29-01-crashlog-capture.test.js (Phase 29 pattern: drives PortfolioDB over a raw IDB shim, asserts observable store contents only)
    - .planning/phases/31-refactor-god-modules/31-RESEARCH.md §"openDB() pooling — the shape and the invalidation" and §"Common Pitfalls → Pitfall 3"
  </read_first>
  <behavior>
    - Test A (pooled reuse): two sequential `PortfolioDB.openDB()` calls each return a handle on which a `getAll` against an existing store (e.g. "clients" or "snippets") succeeds. Assert OBSERVABLE success (transaction resolves / array returned), never internal `_dbPromise` identity.
    - Test B (invalidate-on-close, the falsifiable canary): obtain a handle, fire its `onversionchange` (which calls `db.close()`), then call `openDB()` again and assert the NEW handle yields a successful `getAll` (a closed handle would throw InvalidStateError). On TODAY's no-pool code this passes trivially (every call re-opens); after pooling it passes ONLY if invalidation nulls the cache — this is the assertion that catches a missing invalidation.
    - Test C (no double-seed, no deadlock): issue two concurrent `openDB()` calls (Promise.all) on a fresh DB with `window.SNIPPETS_SEED` set; assert the snippets store ends with exactly the seed count (seed runs once via `_seedingDone`/`_seedingPromise`) and BOTH promises resolve (no hang). Include the old-DB-present path so the `migrateOldDB()`->`openDB()` recursion is exercised and proven not to deadlock.
    - Test D (migrate once): with an old `emotion_code_portfolio` DB present, assert the migration side-effect (old DB consumed / `_migrationDone`) occurs exactly once across multiple `openDB()` calls — observed via the resulting store state, not internal flags.
    - EXPECTED_COUNT guard: assert all 4 scenarios actually ran (mirror the suite's `EXPECTED_COUNT` convention so a dropped `await test(...)` cannot pass vacuously).
  </behavior>
  <action>Create tests/31-openDB-pooling.test.js using the raw-IndexedDB harness from tests/24-04-idb-migration.test.js (load assets/db.js into a sandbox with an indexedDB shim, drive window.PortfolioDB.openDB). Implement Tests A-D from the behavior block, asserting ONLY observable outcomes (transaction success, returned records, final store counts, both-promises-settled). Do NOT reference `_dbPromise`, `_migrationDone`, or `_seedingDone` by name in assertions — they are internal. Run the file against the UNCHANGED db.js first: it MUST pass (it characterizes current behavior). Add the EXPECTED_COUNT scenario-count guard like tests/25-11-toast-behavior.test.js:355. This test is the protective tripwire for Task 2.</action>
  <verify>
    <automated>node tests/31-openDB-pooling.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/31-openDB-pooling.test.js` exits 0 against the CURRENT (un-pooled) `db.js` (characterization baseline established).
    - Test B explicitly closes a handle via `onversionchange` and then asserts a subsequent `openDB()` handle performs a successful store read.
    - Test C runs two concurrent `openDB()` calls including the old-DB-present path and asserts both settle with seed count unchanged (single seed).
    - The file contains an `EXPECTED_COUNT` scenario-count guard that fails if any `await test(...)` is dropped.
    - No assertion references `_dbPromise`/`_migrationDone`/`_seedingDone` (observable-behavior-only per D-08).
  </acceptance_criteria>
  <done>tests/31-openDB-pooling.test.js exists, passes on current db.js, and falsifiably asserts the invalidate-on-close path (Test B) plus no-double-seed/no-deadlock (Test C).</done>
</task>

<task type="auto">
  <name>Task 2: Apply deadlock-safe connection pooling to db.js openDB() (+ touched-region RFCT-03 cleanups)</name>
  <files>assets/db.js</files>
  <read_first>
    - assets/db.js:1-21 (the module-scope `let _migrationDone/_seedingDone/_seedingPromise`; add `let _dbPromise = null;` beside them)
    - assets/db.js:292-348 (openDB body — the exact onupgradeneeded/onblocked/onsuccess/onerror structure to preserve)
    - assets/db.js:15-67 (migrateOldDB sets `_migrationDone = true` at :21 BEFORE awaiting, and itself calls `await openDB()` at :67 — this is the recursion the pooling MUST NOT deadlock)
    - .planning/phases/31-refactor-god-modules/31-RESEARCH.md §"openDB() pooling" (note: the research's `_dbPromise = migrateOldDB().then(...)` shape is UNSAFE here — see action)
  </read_first>
  <action>Add a module-scope `let _dbPromise = null;`. In openDB(): keep `await migrateOldDB();` as the FIRST line (unchanged), THEN add `if (_dbPromise) return _dbPromise;` AFTER that await, THEN assign `_dbPromise = new Promise((resolve, reject) => { ...existing indexedDB.open body... });` and `return _dbPromise;`. CRITICAL ordering rationale: migrateOldDB() calls openDB() recursively at db.js:67; placing the cache-check AFTER `await migrateOldDB()` lets the inner recursive call open+cache the handle (migrateOldDB short-circuits its own second migrate via `_migrationDone`), and the outer call then returns the same cached promise — caching BEFORE/around `migrateOldDB()` (the research's `migrateOldDB().then(...)` shape) would deadlock. Wire cache invalidation in TWO places inside the new promise body: in `db.onversionchange` (db.js:329) set `_dbPromise = null;` immediately before `db.close();`; in `request.onerror` (db.js:346) set `_dbPromise = null;` before `reject(request.error)`. Do NOT change onupgradeneeded, onblocked, or the seedSnippetsIfNeeded call. Then apply RFCT-03 touched-region cleanups WITHIN the openDB function only: convert any `var` in the openDB region to `const`/`let` (db.js has 10 `var` total — convert only those inside this function, `const` where never reassigned), and add a tagged `console.warn`/`console.error` with a "db.openDB:" prefix to any genuinely-silent `catch` in the touched region (additive only — do not swallow or rethrow differently). Leave all other db.js regions untouched (full-file var sweep is out of scope).</action>
  <verify>
    <automated>node tests/31-openDB-pooling.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/31-openDB-pooling.test.js` exits 0 AFTER the pooling change (Test B proves invalidation works; Test C proves no deadlock).
    - `npm test` exits 0 (full suite green — all db.js consumers still work).
    - `assets/db.js` contains exactly `_dbPromise = null;` invalidation in BOTH `db.onversionchange` and `request.onerror`; verify with `grep -c '_dbPromise = null' assets/db.js` returning 2.
    - The `await migrateOldDB();` call remains the first statement of openDB() and the `if (_dbPromise) return _dbPromise;` check appears AFTER it (verify by reading the function — the migrate call must not be wrapped inside the cached promise).
    - No `var` remains inside the openDB function body (`grep -n` the function region shows only `const`/`let`).
  </acceptance_criteria>
  <done>openDB() pools a single `Promise<IDBDatabase>`, invalidates on version-change and error, preserves the migrate recursion without deadlock, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving refactor of a local offline-first PWA; no new endpoints, inputs, or data flows. The IndexedDB store schema, keys, and `PortfolioDB.*` API surface are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01-A | Denial of Service | `openDB()` returns a closed/stale cached connection | medium | mitigate | Invalidate `_dbPromise` on `onversionchange`->close and on `onerror`; characterization Test B proves a fresh working handle is returned after close |
| T-31-01-D | Denial of Service | `migrateOldDB()`->`openDB()` recursion deadlocks under pooling | medium | mitigate | Cache-check placed AFTER `await migrateOldDB()`; Test C exercises the old-DB-present recursion path and asserts both concurrent calls settle |

No new high-severity threats. No new attack surface, inputs, or trust boundaries introduced (security_block_on: high — nothing at or above threshold).
</threat_model>

<verification>
- `node tests/31-openDB-pooling.test.js` exits 0 both BEFORE (Task 1, baseline) and AFTER (Task 2, pooled) the change.
- `npm test` full suite exits 0 after Task 2.
- `grep -c '_dbPromise = null' assets/db.js` == 2.
</verification>

<success_criteria>
RFCT-03 openDB pooling delivered test-first: a new characterization test locks the cached-connection lifecycle (reuse, invalidate-on-close, no-double-seed, migrate-once), openDB() pools a single connection with deadlock-safe ordering and dual invalidation, and the full green suite stays green.
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-01-SUMMARY.md` when done.
</output>
