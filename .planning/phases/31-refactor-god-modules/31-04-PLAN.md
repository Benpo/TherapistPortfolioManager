---
phase: 31-refactor-god-modules
plan: 04
type: execute
wave: 3
depends_on: ["31-03"]
files_modified:
  - assets/settings-photos.js
  - assets/settings.js
  - settings.html
  - sw.js
  - tests/30-photos-optimize-loop.test.js
  - tests/25-11-toast-behavior.test.js
  # Photos hook-consumers that load the settings.js source (frontmatter now
  # matches Task 3 prose). 25-07 needs only the runtime settings-photos.js
  # loader; the four 25-12-* below ALSO source-slice a symbol that moves to
  # settings-photos.js, so each needs BOTH the loader AND a source-slice repoint:
  - tests/25-07-delete-all-photos.test.js
  - tests/25-12-optimize-estimate-floor.test.js
  - tests/25-12-optimize-verdict.test.js
  - tests/25-12-optimize-stale-estimate.test.js
  - tests/25-12-photos-usage-language-rerender.test.js
  # THIRD coupling mechanism — PURE-STATIC source audits (no eval/boot; they read
  # the settings.js source STRING and run a PRESENCE check for a symbol that moves
  # out at the Photos extraction). Repoint each positive check to the combined
  # post-move source (settings.js + settings-photos.js). NOTE: 25-11-hardcoded-
  # english-removed is DISTINCT from 25-11-toast-behavior above:
  - tests/25-08-single-source-audit.test.js
  - tests/25-11-hardcoded-english-removed.test.js
autonomous: true
requirements: [RFCT-01, RFCT-03]
must_haves:
  truths:
    - "_optimizeAllPhotosLoop body + dataURL adapters + handleOptimize success produce the EXACT same savedBytes after the move — proven by tests/30-photos-optimize-loop.test.js (exact savedBytes assertion)"
    - "Delete-all photos still works — proven by tests/25-07-delete-all-photos.test.js"
    - "Storage-usage line re-renders on language change — proven by tests/25-12-photos-usage-language-rerender.test.js"
    - "window.__PhotosTabHelpers resolves from settings-photos.js (helpers IIFE moved first, formerly settings.js:2545) so the UI IIFE reads a defined hook"
    - "settings.js no longer contains the two photos IIFEs (2370-2969) — this is the Photos/StorageUsage cohesive unit named in D-01 (extraction scope); slimmed file retains section-titles, tab-nav, backups — proven by full npm test green"
  artifacts:
    - assets/settings-photos.js
  key_links:
    - "settings-photos.js places the helpers IIFE (was 2370-2571, sets __PhotosTabHelpers) ABOVE the UI IIFE (was 2585-2969, reads __PhotosTabHelpers) — same file, helpers-first order preserved"
    - "settings.html loads settings-photos.js after snippets.js and before settings.js; sw.js PRECACHE_URLS includes /assets/settings-photos.js"
    - "every test that LOADS settings.js to read __PhotosTabHelpers (incl. vm.runInContext loaders) ALSO loads settings-photos.js"
    - "the four 25-12-* photos tests that SOURCE-SLICE a moved symbol out of the settings.js source string (ESTIMATE_DISPLAY_FLOOR_BYTES, OPTIMIZE_RECOMMEND_THRESHOLD_BYTES, `async function handleOptimize`, `function bindPhotosTab`) have that slice REPOINTED to the settings-photos.js source (the symbol left settings.js), in addition to the runtime loader"
    - "the Photos move drifts the DOMContentLoaded handler count 4->3; every count/index-coupled settings.js loader was ALREADY made count-INDEPENDENT in plan 03 (incl. 30-photos-optimize-loop), so npm test stays green across the 4->3 drift with NO re-hardcoding (no `!== 4`/`!== 3`, no new positional index)"
    - "the PURE-STATIC source audits that PRESENCE-check a moved symbol in the settings.js source string keep passing because their check is repointed to the combined post-move source: 25-08-single-source-audit (`CropModule.resizeToMaxDimension(` consumer + `PortfolioDB.updateClient(` >=2 call-sites, both moved to settings-photos.js) and 25-11-hardcoded-english-removed (the positive `\"photos.usage.body\"` reference at its :113-116 check, currently satisfied only by the settings.js:2958 comment which moves out) — assertions repointed, never weakened, and the negative/absence gates in those files stay green untouched"
---

<objective>
RFCT-01 (extraction 2 of 3, D-07 order: Photos AFTER Snippets) — Move the TWO coupled Photos/StorageUsage IIFEs out of `settings.js` into a NEW page-private file `assets/settings-photos.js`. The helpers IIFE (`settings.js:2370-2571`, sets `window.__PhotosTabHelpers` at :2545) and the UI IIFE (`settings.js:2585-2969`, reads `__PhotosTabHelpers`, registers `bindPhotosTab` boot at :2949) are coupled via that hook, so they MUST move TOGETHER and keep their relative order (helpers IIFE physically first).

Per D-06 atomic protocol; per D-02 behavior-preserving move. Reuses the extraction pattern proven in plan 03.

Purpose: Capture the ~600-line Photos win; further slim settings.js.
Output: `assets/settings-photos.js` (two ordered IIFEs) + slimmed settings.js + wiring + updated photos test loaders.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files: `assets/settings-snippets.js` (plan 03), `assets/settings-photos.js` (this plan), `assets/export-modal.js` (plan 05); tests `tests/31-openDB-pooling.test.js`, `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js` (plans 01-02). NEW symbol `window.__exportModalInit` (plan 05). This plan's artifact: `assets/settings-photos.js` (preserves the existing `window.__PhotosTabHelpers` + `window.__photosOptimizeResultTimer` hooks — NO new public global).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-RESEARCH.md
@.planning/phases/31-refactor-god-modules/31-PATTERNS.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/settings.js
@settings.html
@tests/30-photos-optimize-loop.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/settings-photos.js by moving BOTH photos IIFEs (helpers-first) verbatim (+ touched-region RFCT-03 cleanups)</name>
  <files>assets/settings-photos.js, assets/settings.js</files>
  <read_first>
    - assets/settings.js:2370-2571 (the helpers IIFE — anonymous `(function () {`, ending `})();`, registering `window.__PhotosTabHelpers = {...}` at :2543-2571 incl. `_deleteAllPhotosLoop`, `_optimizeAllPhotosLoop`, `humanBytes`, `dataURLToBlob`, `blobToDataURL`, `estimatePhotoSavings`, and the threshold constants)
    - assets/settings.js:2585-2969 (the UI IIFE — anonymous IIFE, its own `$`/`tt` helpers, reads `window.__PhotosTabHelpers`, registers `document.addEventListener('DOMContentLoaded', bindPhotosTab)` at :2949; also any `window.__photosOptimizeResultTimer` hook)
    - .planning/phases/31-refactor-god-modules/31-PATTERNS.md §"assets/settings-photos.js — TWO coupled IIFEs" (the helpers-first ordering requirement + the blank-line/comment separator)
    - 31-RESEARCH.md §"Verified live structure — settings.js IIFE boundaries" (Photos is TWO IIFEs, not one — they must move together)
  </read_first>
  <action>Create assets/settings-photos.js. Move the helpers IIFE (settings.js:2370-2571) FIRST, then the UI IIFE (settings.js:2585-2969) BELOW it, preserving the blank-line/comment separator between them so the UI IIFE reads the `window.__PhotosTabHelpers` the helpers IIFE assigned synchronously at eval time. Move BOTH verbatim — keep them anonymous DOMContentLoaded IIFEs, keep each file's own `$`/`tt` helper copies, keep the `__PhotosTabHelpers` and `__photosOptimizeResultTimer` hooks byte-for-byte. Then DELETE the exact 2370-2969 region from settings.js. Apply RFCT-03 cleanups WITHIN the moved photos regions only: `var`->`const`/`let` (only where never reassigned), rewrite `// Phase N` archaeology comments into what-it-does comments (D-05), add a "settings-photos:" tagged prefix to any genuinely-silent catch (additive). Do NOT alter optimize/delete/estimate logic — the exact-savedBytes test will catch any byte drift.</action>
  <verify>
    <automated>node -e "new (require('vm').Script)(require('fs').readFileSync('assets/settings-photos.js','utf8')); new (require('vm').Script)(require('fs').readFileSync('assets/settings.js','utf8')); console.log('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/settings-photos.js` exists, parses, and contains `window.__PhotosTabHelpers = {` ABOVE the UI IIFE's `bindPhotosTab` registration (helpers-first order — verify by reading the file).
    - It is two anonymous IIFEs (no `window.SettingsPhotos =` or other new public global).
    - `grep -n '__PhotosTabHelpers\|_optimizeAllPhotosLoop\|bindPhotosTab' assets/settings.js` returns NOTHING (both photos IIFEs removed from settings.js).
    - settings.js still parses and still contains section-titles/tab-nav/backups.
  </acceptance_criteria>
  <done>Both photos IIFEs live in settings-photos.js in helpers-first order with hooks intact; settings.js loses the 2370-2969 block; both files parse.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings-photos.js into settings.html + sw.js PRECACHE</name>
  <files>settings.html, sw.js</files>
  <read_first>
    - settings.html:341-342 (snippets.js, settings-snippets.js [added in plan 03], settings.js) — settings-photos.js slots into this same pre-settings.js block
    - sw.js:67 (`'/assets/settings.js'`) and the settings-snippets.js entry added in plan 03
    - 31-PATTERNS.md §"Service-worker precache" + the pre-commit CACHE_NAME-bump gotcha
  </read_first>
  <action>In settings.html, add `<script src="./assets/settings-photos.js"></script>` (no `defer`) in the pre-settings.js block alongside the settings-snippets.js tag (order among settings-snippets.js / settings-photos.js does not matter — both are self-contained IIFEs reading window.* globals; both must precede settings.js). In sw.js, add `'/assets/settings-photos.js',` to PRECACHE_URLS adjacent to the settings-snippets.js entry. Do NOT bump CACHE_NAME/APP_VERSION (plan 06 consolidates the version bump).</action>
  <verify>
    <automated>grep -c "settings-photos.js" settings.html && grep -c "'/assets/settings-photos.js'" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'settings-photos.js' settings.html` shows exactly one `<script>` tag, before settings.js, no `defer`.
    - `grep -n "settings-photos.js" sw.js` shows exactly one PRECACHE_URLS entry.
  </acceptance_criteria>
  <done>settings-photos.js is loaded before settings.js on settings.html and precached.</done>
</task>

<task type="auto">
  <name>Task 3: Update photos test loaders (win.eval AND vm.runInContext) + repoint source-text slices AND pure-static source audits of moved symbols + confirm 25-11-toast EXPECTED_COUNT guard; prove suite green</name>
  <files>tests/30-photos-optimize-loop.test.js, tests/25-11-toast-behavior.test.js, tests/25-07-delete-all-photos.test.js, tests/25-12-optimize-estimate-floor.test.js, tests/25-12-optimize-verdict.test.js, tests/25-12-optimize-stale-estimate.test.js, tests/25-12-photos-usage-language-rerender.test.js, tests/25-08-single-source-audit.test.js, tests/25-11-hardcoded-english-removed.test.js</files>
  <read_first>
    - tests/30-photos-optimize-loop.test.js:224 (`vm.runInContext(settingsSrc, sandbox, { filename: 'assets/settings.js' })` — this loader uses vm, NOT win.eval; it reads window.__PhotosTabHelpers which now lives in settings-photos.js) and :226 + :337 (its handler-count guard `captured.length !== 5` and `captured[4]` selector were ALREADY made count-INDEPENDENT in plan 03 — DO NOT re-hardcode them here; only the settings-photos.js runtime loader is added in this plan)
    - tests/25-11-toast-behavior.test.js:440 + :466 (it MONKEY-PATCHES `ctx.sandbox.window.__PhotosTabHelpers._optimizeAllPhotosLoop` and `._deleteAllPhotosLoop` — that hook MOVES to settings-photos.js, so 25-11 ALSO needs the settings-photos.js loader, not just the EXPECTED_COUNT check) and :354-355 (`EXPECTED_COUNT = 5` — ALREADY PRESENT; the WR-05 fix is in place) and :604-606 (the guard)
    - RUNTIME-LOADER photos tests (read/patch __PhotosTabHelpers after eval — add the settings-photos.js eval into the SAME sandbox): tests/25-07-delete-all-photos.test.js:102-104 (`src = readFileSync('assets/settings.js')` then `vm.runInContext(src, ...)`; reads `__PhotosTabHelpers._deleteAllPhotosLoop` at :111 — LOADER ONLY, it does not source-slice).
    - SOURCE-SLICE photos tests (these read the settings.js source STRING and slice out a symbol that MOVES to settings-photos.js — the slice itself breaks, independent of the runtime hook; repoint the slice to read the settings-photos.js source): tests/25-12-optimize-estimate-floor.test.js:51 (`settingsSrc = readFileSync('assets/settings.js')`) + :82 (`settingsSrc.match(/var ESTIMATE_DISPLAY_FLOOR_BYTES = .../)` → const moves to settings-photos.js); tests/25-12-optimize-verdict.test.js:48 + :187 (`OPTIMIZE_RECOMMEND_THRESHOLD_BYTES` match → moves); tests/25-12-optimize-stale-estimate.test.js:46 + :76-82 (`settingsSrc.indexOf('async function handleOptimize')` + `.slice(...)` → handleOptimize moves; throws `handleOptimize function missing from settings.js` if not repointed); tests/25-12-photos-usage-language-rerender.test.js:48 + :73-76 (`settingsSrc.indexOf('function bindPhotosTab')` → bindPhotosTab moves). Each of these four ALSO evals settings.js at runtime and reads/boots through __PhotosTabHelpers, so they need BOTH the source-slice repoint AND the settings-photos.js runtime loader.
    - PURE-STATIC source-audit tests (THIRD coupling mechanism — they NEVER eval/boot; they read the settings.js source STRING and run a PRESENCE check on a symbol that moves out — gated by NEITHER the `captured.*` grep NOR the runtime-loader grep): tests/25-08-single-source-audit.test.js:43 (`settingsJs = readSrc('assets/settings.js')`) + :108 (`/CropModule\.resizeToMaxDimension\s*\(/.test(settingsJs)` — the only real call is settings.js:2837, in the moved UI IIFE) + :117-119 (`updateClientHits = settingsJs.match(/PortfolioDB\.updateClient\s*\(/g).length; check(... >= 2)` — both real call-sites settings.js:2836,2923 are in the moved region); tests/25-11-hardcoded-english-removed.test.js:39 (`settingsSrc = readSource('assets/settings.js')`) + :113-116 (positive check `settingsSrc.indexOf('"photos.usage.body"') !== -1` — currently satisfied ONLY by the settings.js:2958 comment, which is inside the moved UI IIFE 2585-2969, so it disappears from settings.js after the move → the test THROWS). Both files' NEGATIVE/absence gates (e.g. 25-08's `!canvasApiRegex.test(settingsJs)`, 25-11's forbidden-literal `indexOf === -1` checks) stay green after the move (absent stays absent) — do NOT touch those; only the POSITIVE presence checks need repointing.
    - 31-RESEARCH.md §"Pitfall 2" and §"Pitfall 5"; 31-VALIDATION.md §"Per-Task Verification Map" (photos row protective tests)
  </read_first>
  <action>After Tasks 1-2, run `npm test` — photos tests reading `window.__PhotosTabHelpers` will go red because settings.js no longer defines it. Enumerate EVERY test that loads the settings.js source for photos behavior, covering BOTH patterns: `grep -rn "assets/settings.js" tests/*.test.js` and inspect each for `vm.runInContext(...)` OR `win.eval(readAsset('assets/settings.js'))`. For each such photos test, add a matching load of `assets/settings-photos.js` into the SAME sandbox/window so `__PhotosTabHelpers` is populated — for the vm loader, add `vm.runInContext(readAsset('assets/settings-photos.js'), sandbox, { filename: 'assets/settings-photos.js' })`; settings-photos.js is self-contained so it can load before or after settings.js as long as the App/PortfolioDB globals the sandbox injects are present. The concrete set of settings.js-source-loading photos tests is: `30-photos-optimize-loop` (loader only; its count/index selection was already made robust in plan 03 — do NOT touch it), `25-11-toast-behavior` (loader only; see below), `25-07-delete-all-photos` (loader only — it evals the source but does NOT source-slice), and the FOUR source-slice tests `25-12-optimize-estimate-floor`, `25-12-optimize-verdict`, `25-12-optimize-stale-estimate`, `25-12-photos-usage-language-rerender`. SOURCE-SLICE REPOINT (mandatory for the four 25-12-* above): each reads the settings.js source STRING and slices out a symbol that has MOVED into settings-photos.js — `ESTIMATE_DISPLAY_FLOOR_BYTES` (estimate-floor), `OPTIMIZE_RECOMMEND_THRESHOLD_BYTES` (verdict), `async function handleOptimize` (stale-estimate), `function bindPhotosTab` (usage-language-rerender). That slice/match/indexOf throws or returns null after the move regardless of the runtime hook, so REPOINT the source-text assertion to read the `assets/settings-photos.js` source (e.g. add `const photosSrc = readAsset('assets/settings-photos.js')` and run the existing slice/match/indexOf against `photosSrc` for the moved symbol), while still also adding the settings-photos.js RUNTIME loader into the sandbox for their __PhotosTabHelpers reads/boots. Do NOT delete or weaken any of these assertions — they must continue to assert the same thing against the symbol's new home. The backups/schedule settings.js loaders (`25-12-custom-days-visibility`, `25-12-schedule-saved-toast`) test the backups IIFE (still in settings.js) and the static-grep test `25-12-folder-picker-removed` asserts ABSENCE — none are photos hook-consumers, so leave them unchanged unless the grep-and-run shows one red. For 25-11-toast-behavior.test.js: it monkey-patches `ctx.sandbox.window.__PhotosTabHelpers._optimizeAllPhotosLoop` (:440) and `._deleteAllPhotosLoop` (:466) — that hook now lives in settings-photos.js, so 25-11 ALSO needs a `vm.runInContext(readAsset('assets/settings-photos.js'), sandbox, { filename: 'assets/settings-photos.js' })` into the SAME sandbox so `__PhotosTabHelpers` exists to patch (without it the patch assignment targets `undefined` and the test goes red); SEPARATELY confirm its `EXPECTED_COUNT = 5` guard still equals the number of `await test(...)` scenarios (do NOT add a duplicate) — that part closes WR-05. HANDLER-COUNT 4->3 DRIFT: the Photos move drops the settings.js DOMContentLoaded handler count from 4 (post-Snippets) to 3. Every count/index-coupled settings.js loader was ALREADY rewritten to count-INDEPENDENT identity selection in plan 03 (including 30-photos-optimize-loop), so they must survive this drift untouched — re-run `grep -rn "captured.length\|captured\[" tests/*.test.js` and confirm NO settings.js loader was re-hardcoded to a fixed count (`!== 4`/`!== 3`) or a new positional index during this plan; if any settings.js loader is still count/index-coupled, that is a plan-03 escape — make it identity-based here rather than hardcoding. PURE-STATIC SOURCE AUDITS (THIRD coupling mechanism — neither the `captured.*` grep nor the runtime-loader grep catches these): two tests read the settings.js source as a STRING and PRESENCE-check a symbol that moves out — `25-08-single-source-audit` (`CropModule.resizeToMaxDimension(` consumer at :108 and `PortfolioDB.updateClient(` >=2 call-sites at :117-119 — all moved to settings-photos.js) and `25-11-hardcoded-english-removed` (the positive `"photos.usage.body"` reference at :113-116, satisfied today only by the settings.js:2958 comment that moves out). For EACH, bind the new-home source (`const photosSrc = readSrc('assets/settings-photos.js')` / `readSource('assets/settings-photos.js')`, matching the file's own read helper) and repoint ONLY the positive presence check to scan the COMBINED post-move source — e.g. `(settingsJs + photosSrc)` for 25-08's regex/`.match` and `(settingsSrc + photosSrc).indexOf(...)` for 25-11's i18n-key check — preserving identical assertion strength (still requires the symbol present; 25-08 still requires updateClient `>= 2` call-sites total). Do NOT touch the NEGATIVE/absence gates in either file (they stay green — absent stays absent). THIRD ENUMERATION GATE (binding — so no fourth surprise class): beyond the `captured.*` grep and the runtime-loader grep, run `grep -rn "readFileSync.*settings\|readSrc('assets/settings\|readSource('assets/settings\|readAsset('assets/settings" tests/*.test.js` to list every test that binds the settings.js source to a variable, then grep those for `.match(\|\.test(\|\.indexOf(\|\.includes(\|\.slice(` against that variable, and repoint any POSITIVE check on a symbol that moves at THIS extraction (handleOptimize, refreshPhotosTab, bindPhotosTab, _optimizeAllPhotosLoop, _deleteAllPhotosLoop, the threshold consts, the CropModule/updateClient call-sites, `"photos.usage.body"`) to the combined post-move source. Run `npm test` until green and confirm 30-photos-optimize-loop's EXACT savedBytes assertion still passes (proving the moved `_optimizeAllPhotosLoop` body is byte-identical) and no photos test went vacuous-green; the binding proof the third-mechanism enumeration was complete is full `npm test` green after the Photos move.</action>
  <verify>
    <automated>node tests/30-photos-optimize-loop.test.js && node tests/25-11-toast-behavior.test.js && node tests/25-08-single-source-audit.test.js && node tests/25-11-hardcoded-english-removed.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-photos-optimize-loop.test.js` exits 0 and its exact-savedBytes assertion executes (the moved optimize loop is byte-identical).
    - Every test that loads the settings.js source to read/patch `__PhotosTabHelpers` now also loads `assets/settings-photos.js` into the same sandbox/window (grep confirms `settings-photos.js` appears in those loader files, INCLUDING `tests/25-11-toast-behavior.test.js` which patches `__PhotosTabHelpers._optimizeAllPhotosLoop`/`._deleteAllPhotosLoop`).
    - SOURCE-SLICE REPOINT: each of `25-12-optimize-estimate-floor`, `25-12-optimize-verdict`, `25-12-optimize-stale-estimate`, `25-12-photos-usage-language-rerender` exits 0 with its source-text assertion repointed to the `assets/settings-photos.js` source for its moved symbol (`ESTIMATE_DISPLAY_FLOOR_BYTES` / `OPTIMIZE_RECOMMEND_THRESHOLD_BYTES` / `handleOptimize` / `bindPhotosTab`) — the assertion still executes and still proves the same thing (no assertion deleted or weakened). `tests/25-07-delete-all-photos.test.js` exits 0 with only the runtime loader added.
    - 4->3 DRIFT, NO RE-HARDCODING: `grep -rn "captured.length\|captured\[" tests/*.test.js` shows no settings.js loader newly hardcoded to a fixed count (`!== 4`/`!== 3`) or a new positional index in this plan; `30-photos-optimize-loop` still uses the plan-03 identity selector (untouched here).
    - PURE-STATIC AUDITS REPOINTED: `node tests/25-08-single-source-audit.test.js` and `node tests/25-11-hardcoded-english-removed.test.js` each exit 0; their positive presence checks now scan the COMBINED source (settings.js + settings-photos.js) for the moved symbols (`CropModule.resizeToMaxDimension(`, `PortfolioDB.updateClient(` >=2, `"photos.usage.body"`), at unchanged assertion strength, and their negative/absence gates were left untouched.
    - THIRD-MECHANISM ENUMERATION COMPLETE: the source-text-audit grep (`grep -rn "readFileSync.*settings\|readSrc('assets/settings\|readSource('assets/settings\|readAsset('assets/settings" tests/*.test.js`, then filter to `.match`/`.test`/`.indexOf`/`.includes`/`.slice` on a moved symbol) surfaces no remaining un-repointed positive check; full `npm test` green is the binding proof.
    - `tests/25-11-toast-behavior.test.js` loads settings-photos.js so its `__PhotosTabHelpers` monkey-patch targets a defined hook, retains its single `EXPECTED_COUNT = 5` guard (no duplicate added), and exits 0.
    - `npm test` exits 0 (full suite green — D-06 commit-when-green gate satisfied).
  </acceptance_criteria>
  <done>Photos test loaders (vm + win.eval) load settings-photos.js, the four 25-12-* source-slice assertions are repointed to the settings-photos.js source, the two PURE-STATIC audits (25-08, 25-11-hardcoded-english) have their positive presence checks repointed to the combined source, the exact-savedBytes tripwire still fires, the plan-03 identity selectors survive the 4->3 drift with no re-hardcoding, 25-11-toast's vacuous-green guard is confirmed, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verbatim relocation of two coupled page-private IIFEs. No new endpoints, inputs, data flows, or trust boundaries. Photo store + navigator.storage usage unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04-A | Availability | UI IIFE reads an undefined `__PhotosTabHelpers` if move order/wiring is wrong | medium | mitigate | Helpers IIFE moved physically FIRST in the same file; both load before settings.js; 30-photos-optimize-loop + delete-all tests fail loudly on a broken hook. |
| T-31-04-A2 | Availability | Service worker serves a stale asset list missing settings-photos.js | low | mitigate | PRECACHE_URLS entry added (Task 2); version bump consolidated in plan 06. |
| T-31-04-T | Tampering | Refactor widens public window.* surface | low | mitigate | Anonymous IIFEs; only pre-existing `__PhotosTabHelpers`/`__photosOptimizeResultTimer` hooks exposed — no new feature global. |

No new high-severity threats; behavior preserved; no widened public surface (security_block_on: high — nothing at threshold).
</threat_model>

<verification>
- `node tests/30-photos-optimize-loop.test.js`, `node tests/25-07-delete-all-photos.test.js`, `node tests/25-11-toast-behavior.test.js` exit 0.
- The four source-slice photos tests exit 0 with their slice repointed to settings-photos.js: `node tests/25-12-optimize-estimate-floor.test.js`, `node tests/25-12-optimize-verdict.test.js`, `node tests/25-12-optimize-stale-estimate.test.js`, `node tests/25-12-photos-usage-language-rerender.test.js`.
- The two PURE-STATIC source audits exit 0 with positive checks repointed to the combined source: `node tests/25-08-single-source-audit.test.js`, `node tests/25-11-hardcoded-english-removed.test.js`.
- `npm test` full suite exits 0.
- `grep -n '__PhotosTabHelpers\|bindPhotosTab' assets/settings.js` returns nothing; `grep -n '__PhotosTabHelpers' assets/settings-photos.js` returns the hook.
- `grep -n 'settings-photos.js' settings.html sw.js` shows the tag + PRECACHE entry.
</verification>

<success_criteria>
Both Photos/StorageUsage IIFEs are extracted (helpers-first) to settings-photos.js with hooks preserved, wired into settings.html + sw.js, photo behavior proven byte-identical by the exact-savedBytes test and the rest of the green suite, the 25-11 vacuous-green guard confirmed, and settings.js further slimmed.
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-04-SUMMARY.md` when done.
</output>
