---
phase: 31-refactor-god-modules
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - assets/settings-snippets.js
  - assets/settings.js
  - settings.html
  - sw.js
  - tests/30-snippet-wiring.test.js
  - tests/30-snippet-import-merge.test.js
  - tests/24-05-import-validator.test.js
  - tests/24-05-list-filter.test.js
  - tests/24-05-modified-seed.test.js
  - tests/24-05-trigger-dedupe.test.js
  - tests/quick-260619-okw-cross-lang-warning.test.js
  - tests/quick-260619-okw-trigger-unicode.test.js
  - tests/quick-260620-p3f-pending-tag-commit.test.js
  - tests/quick-260626-h5j-trigger-autoconvert.test.js
autonomous: true
requirements: [RFCT-01, RFCT-03]
must_haves:
  truths:
    - "Snippet editor open -> add/update/delete -> list re-render works unchanged after the move — proven by tests/30-snippet-wiring.test.js (add-vs-update branch, persisted args, rendered #snippetList)"
    - "Snippet import collision REPLACE/MERGE via real FileReader works unchanged — proven by tests/30-snippet-import-merge.test.js"
    - "window.__SnippetEditorHelpers resolves from settings-snippets.js (moved byte-for-byte, formerly settings.js:863) so the leaf-helper tests 24-04/24-05 still pass through the hook"
    - "settings.js no longer contains the snippets IIFE (712-2018); the slimmed file retains section-titles, tab-nav, backups — proven by full npm test green"
  artifacts:
    - assets/settings-snippets.js
  key_links:
    - "settings.html loads settings-snippets.js AFTER snippets.js/app.js/db.js and immediately BEFORE settings.js (cross-IIFE window.* chain: App, PortfolioDB, Snippets, SNIPPETS_SEED must resolve first)"
    - "sw.js PRECACHE_URLS includes /assets/settings-snippets.js"
    - "EVERY test that loads assets/settings.js for snippet behavior — across BOTH `win.eval` AND `vm.runInContext` loaders — ALSO loads assets/settings-snippets.js (the `window.__SnippetEditorHelpers` hook left settings.js, so the 8 `vm.runInContext` tests that read it via `if (!helpers) process.exit(1)` go red without the loader), and 30-snippet-wiring/30-snippet-import-merge capture the snippets boot handler by an extraction-robust delta (not a fixed captured.length===5 / captured[1])"
---

<objective>
RFCT-01 (extraction 1 of 3, D-07 order: Snippets FIRST) — Move the self-contained SnippetsUI IIFE (`settings.js:712-2018`, ~1,300 lines: editor + tag-chip autocomplete, list + filter chips, import/export + collision modal, 8 validators) verbatim into a NEW page-private file `assets/settings-snippets.js`, preserving the `window.__SnippetEditorHelpers` hook exactly. This is the lowest-risk extraction (already a self-contained IIFE with test hooks) and proves the pattern for Photos (plan 04) and export-modal (plan 05).

Per D-06: atomic per-unit — move -> update test loaders -> wire -> full `npm test` GREEN -> commit only when green. Per D-02: behavior-preserving MOVE, not a shrink — relocate as-is plus cosmetic RFCT-03 cleanups only.

Purpose: De-bloat god-module settings.js; establish the extraction-and-rewiring pattern.
Output: `assets/settings-snippets.js` + slimmed `settings.js` + wiring + updated snippet test loaders.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files: `assets/settings-snippets.js` (this plan), `assets/settings-photos.js` (plan 04), `assets/export-modal.js` (plan 05); tests `tests/31-openDB-pooling.test.js`, `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js` (plans 01-02). NEW symbol `window.__exportModalInit` (plan 05). NEW wiring: `<script>` tags + 3 `sw.js` PRECACHE entries + `APP_VERSION` bump (plan 06). This plan's artifact: `assets/settings-snippets.js` (preserves the existing `window.__SnippetEditorHelpers` hook — NO new public global).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-RESEARCH.md
@.planning/phases/31-refactor-god-modules/31-PATTERNS.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/settings.js
@settings.html
@tests/30-snippet-wiring.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/settings-snippets.js by moving the SnippetsUI IIFE verbatim (+ touched-region RFCT-03 cleanups)</name>
  <files>assets/settings-snippets.js, assets/settings.js</files>
  <read_first>
    - assets/settings.js:698-713 (the cross-IIFE dependency doc-comment header to carry over verbatim) and :712 (anonymous `(function () { "use strict";` IIFE start)
    - assets/settings.js:861-874 (the `window.__SnippetEditorHelpers = {...}` hook — MOVE BYTE-FOR-BYTE; this is the live assignment, NOT the :717 comment) and :2016 (`document.addEventListener("DOMContentLoaded", boot);` the IIFE's boot registration) up to the IIFE close ~:2018
    - assets/settings.js:937-967 (the LOCAL glue wrappers `t`/`getCurrentLang`/`showToast` — the D-04 dedup target) and :973 (the `$` helper)
    - .planning/phases/31-refactor-god-modules/31-PATTERNS.md §"assets/settings-snippets.js" (the exact header/hook/glue snippets) and §"Anti-Patterns" (var->const only where never reassigned)
  </read_first>
  <action>Create assets/settings-snippets.js. Move the ENTIRE anonymous IIFE at settings.js:712-2018 verbatim into it, including: the cross-IIFE dependency doc-comment header (698-713), the `"use strict";`, the `window.__SnippetEditorHelpers` hook assignment (861-874, with its `typeof window !== "undefined"` guard), the `boot` function and its `document.addEventListener("DOMContentLoaded", boot)` registration, and the file's own `$` selector copy (973) — keep it self-contained (planner discretion: own copy, behavior identical). Keep it an ANONYMOUS DOMContentLoaded IIFE (do NOT wrap in `window.Something =`). Then DELETE that exact 712-2018 region from settings.js so the slimmed file retains only section-titles (14-688), tab-nav (2035-2113), and backups (2135-2344). Apply RFCT-03 cleanups WITHIN the moved snippet region only: (a) D-04 glue dedup — replace calls to the local `t(...)`/`showToast(...)`/`getCurrentLang(...)` wrappers with `App.t(...)`/`App.showToast("", ...)`/`App.getLanguage(...)` canonicals and remove the now-unused local wrapper definitions; if any single replacement makes the suite diverge (Task 3 red), REVERT that one wrapper and add a one-line note in the SUMMARY (D-04); (b) convert `var`->`const`/`let` only inside the moved region (`const` where never reassigned, `let` otherwise — never blind-convert a reassigned `var`); (c) rewrite any `// Phase N Plan M` archaeology comments in the moved region into plain what-it-does comments (D-05); (d) add a tagged `console.warn` prefix (e.g. "settings-snippets:") to any genuinely-silent catch in the moved region (additive only). Do NOT touch the retained settings.js regions beyond removing the moved block.</action>
  <verify>
    <automated>node -e "new (require('vm').Script)(require('fs').readFileSync('assets/settings-snippets.js','utf8')); new (require('vm').Script)(require('fs').readFileSync('assets/settings.js','utf8')); console.log('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/settings-snippets.js` exists and parses (vm.Script compiles without SyntaxError); `assets/settings.js` still parses.
    - `assets/settings-snippets.js` contains the `window.__SnippetEditorHelpers = {` assignment (grep confirms) and is an anonymous IIFE (does NOT contain `window.SettingsSnippets =` or any new public-global assignment besides the pre-existing `__SnippetEditorHelpers` hook).
    - `grep -n 'SnippetEditorHelpers\|openEditor\|handleSave\|renderSnippetList' assets/settings.js` returns NOTHING (the snippet block is fully removed from settings.js).
    - settings.js still contains the section-titles, tab-nav, and backups handlers (grep for `bindBackupsTab` and the tab-nav boot still present).
  </acceptance_criteria>
  <done>The SnippetsUI IIFE lives in settings-snippets.js with its hook intact; settings.js is slimmed to section-titles/tab-nav/backups; both files parse.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings-snippets.js into settings.html + sw.js PRECACHE</name>
  <files>settings.html, sw.js</files>
  <read_first>
    - settings.html:317-342 (load order: snippets-seed.js, db.js, version.js, app.js ... snippets.js@341 `defer`, settings.js@342)
    - sw.js:67 (`'/assets/settings.js',`) — the PRECACHE_URLS array; CACHE_NAME at sw.js:19 is derived from the deploy INTEGRITY_TOKEN (version bump handled in plan 06)
    - .planning/phases/31-refactor-god-modules/31-PATTERNS.md §"Wiring — ordered <script src>" and §"Service-worker precache"
  </read_first>
  <action>In settings.html, add `<script src="./assets/settings-snippets.js"></script>` on its own line BETWEEN snippets.js (:341) and settings.js (:342) — without `defer`, matching settings.js's synchronous timing (the snippet IIFE must boot in the same DOMContentLoaded pass as before, AFTER app.js/db.js/snippets.js/snippets-seed.js have registered their window.* globals). In sw.js, add `'/assets/settings-snippets.js',` to PRECACHE_URLS immediately after the `'/assets/settings.js',` entry (:67). Do NOT bump CACHE_NAME/APP_VERSION here — that is the consolidated plan-06 chore (the pre-commit hook skips the CACHE_NAME bump when sw.js is in the diff; plan 06 does the single manual version bump after all three extractions land).</action>
  <verify>
    <automated>grep -c "settings-snippets.js" settings.html && grep -c "'/assets/settings-snippets.js'" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'settings-snippets.js' settings.html` shows exactly one `<script>` tag, positioned after snippets.js and before settings.js.
    - `grep -n "settings-snippets.js" sw.js` shows exactly one PRECACHE_URLS entry.
    - The new tag has NO `defer` attribute (matches settings.js timing).
  </acceptance_criteria>
  <done>settings-snippets.js is loaded in dependency order on settings.html and precached by the service worker.</done>
</task>

<task type="auto">
  <name>Task 3: Update ALL snippet test loaders (win.eval + vm.runInContext) for the moved hook, extraction-robust handler capture, and prove suite green</name>
  <files>tests/30-snippet-wiring.test.js, tests/30-snippet-import-merge.test.js, tests/24-05-import-validator.test.js, tests/24-05-list-filter.test.js, tests/24-05-modified-seed.test.js, tests/24-05-trigger-dedupe.test.js, tests/quick-260619-okw-cross-lang-warning.test.js, tests/quick-260619-okw-trigger-unicode.test.js, tests/quick-260620-p3f-pending-tag-commit.test.js, tests/quick-260626-h5j-trigger-autoconvert.test.js</files>
  <read_first>
    - tests/30-snippet-wiring.test.js:99-131 (the `captured` array, `win.eval(readAsset('assets/settings.js'))` at :123, the `captured.length !== 5` guard at :125, and `snippetsBoot: captured[1]` at :130)
    - tests/30-snippet-import-merge.test.js:89-116 (the SAME pattern: eval at :109, guard at :111, `captured[1]` at :116)
    - tests/24-05-list-filter.test.js:29-53 (the `vm.runInContext` loader shape to MIRROR: the bare sandbox already provides `window: {}` + `document.addEventListener() {}` at :29-40, so `assets/settings-snippets.js` (an anonymous DOMContentLoaded IIFE) loads cleanly; :48 evals settings.js, :55 reads `sandbox.window.__SnippetEditorHelpers`) — the 8 `vm.runInContext` tests below read `__SnippetEditorHelpers` and `process.exit(1)` if it is missing, so each needs a companion settings-snippets.js eval into the SAME sandbox
    - .planning/phases/31-refactor-god-modules/31-PATTERNS.md §"Test-loader update" (note the handler-count-guard warning) and 31-RESEARCH.md §"Pitfall 2"
  </read_first>
  <action>After Tasks 1-2, run `npm test` — every test that loads settings.js to read the moved `window.__SnippetEditorHelpers` hook WILL go red (the snippets IIFE is no longer in settings.js). Enumerate ALL settings.js loaders across BOTH patterns — `grep -rln 'settings.js' tests/` then inspect each for `win.eval(readAsset('assets/settings.js'))` OR `vm.runInContext(...settings.js...)` — and add a load of `assets/settings-snippets.js` to every test that EXERCISES the moved snippet code (the per-file fix is mechanical: the bare `vm.runInContext` sandbox already provides `window`/`document.addEventListener` per 24-05-list-filter:29-40, so one added `vm.runInContext(readAsset('assets/settings-snippets.js'), sandbox, { filename: 'assets/settings-snippets.js' })` line — BEFORE the existing settings.js eval — suffices; for the two `win.eval` snippet tests, add `win.eval(readAsset('assets/settings-snippets.js'))` BEFORE the settings.js eval). The 8 `vm.runInContext` tests that read `__SnippetEditorHelpers` and `process.exit(1)` on miss are: `24-05-import-validator`, `24-05-list-filter`, `24-05-modified-seed`, `24-05-trigger-dedupe`, `quick-260619-okw-cross-lang-warning`, `quick-260619-okw-trigger-unicode`, `quick-260620-p3f-pending-tag-commit`, `quick-260626-h5j-trigger-autoconvert` (confirm the exact set with the grep — add the loader wherever `npm test` goes red, never blindly to settings.js tests that do NOT read the hook). SEPARATELY, for the two boot-handler tests (`30-snippet-wiring`, `30-snippet-import-merge`) replace the fragile `captured.length===5` + `captured[1]` capture with an EXTRACTION-ROBUST delta (it will break again when Photos extracts in plan 04 otherwise): snapshot `const beforeSnip = captured.length;`, then `win.eval(readAsset('assets/settings-snippets.js'));`, assert `captured.length - beforeSnip === 1` and set the snippets boot to `captured[captured.length - 1]`, THEN `win.eval(readAsset('assets/settings.js'));`. Keep all global injections (App stub, mock PortfolioDB, Snippets.getPrefix, SNIPPETS_SEED, BroadcastChannel no-op) unchanged. Run `npm test` until green and confirm no snippet test went vacuous-green: the add-vs-update and REPLACE/MERGE assertions and the 24-05/quick-* helper assertions must still execute (assertion/scenario counts unchanged from before the move).</action>
  <verify>
    <automated>node tests/30-snippet-wiring.test.js && node tests/30-snippet-import-merge.test.js && node tests/24-05-list-filter.test.js && node tests/24-05-import-validator.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-snippet-wiring.test.js`, `node tests/30-snippet-import-merge.test.js`, and each of the 8 `vm.runInContext` snippet tests (`24-05-import-validator`, `24-05-list-filter`, `24-05-modified-seed`, `24-05-trigger-dedupe`, `quick-260619-okw-cross-lang-warning`, `quick-260619-okw-trigger-unicode`, `quick-260620-p3f-pending-tag-commit`, `quick-260626-h5j-trigger-autoconvert`) each exit 0.
    - Every test that reads `__SnippetEditorHelpers` now loads `assets/settings-snippets.js` into the SAME sandbox/window BEFORE the settings.js eval (grep confirms `settings-snippets.js` appears in all 10 loader files).
    - 30-snippet-wiring and 30-snippet-import-merge no longer reference `captured.length !== 5` or `captured[1]`; both use the `beforeSnip` delta + `captured[captured.length - 1]` capture and assert the +1 handler delta.
    - `npm test` exits 0 (full suite green) — commit-when-green gate for D-06 satisfied.
    - The snippet tests' scenario/assertion counts are unchanged vs. pre-move (no vacuous-green): the add-vs-update branch, import REPLACE/MERGE cases, and the 24-05/quick-* helper assertions still run.
  </acceptance_criteria>
  <done>All snippet test loaders (both win.eval and vm.runInContext) load settings-snippets.js for the moved hook, the two boot-handler tests capture in an extraction-robust way, the moved code is exercised for real, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verbatim relocation of an existing page-private IIFE. No new endpoints, inputs, data flows, or trust boundaries. Snippet store schema and PortfolioDB.* API unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-03-T | Tampering | Refactor silently widens the public window.* surface | low | mitigate | Extracted module stays an anonymous DOMContentLoaded IIFE; only the pre-existing `__SnippetEditorHelpers` test hook is exposed — no new public/feature global (acceptance criteria gate). |
| T-31-03-A | Availability | Service worker serves a stale asset list missing settings-snippets.js | low | mitigate | PRECACHE_URLS entry added (Task 2); CACHE_NAME/version bump consolidated in plan 06. |

No new high-severity threats; behavior preserved; public surface NOT widened (security_block_on: high — nothing at threshold).
</threat_model>

<verification>
- `node tests/30-snippet-wiring.test.js` and `node tests/30-snippet-import-merge.test.js` exit 0.
- All 8 `vm.runInContext` snippet tests that read `__SnippetEditorHelpers` now load settings-snippets.js: `grep -rln 'settings-snippets.js' tests/24-05-import-validator.test.js tests/24-05-list-filter.test.js tests/24-05-modified-seed.test.js tests/24-05-trigger-dedupe.test.js tests/quick-260619-okw-cross-lang-warning.test.js tests/quick-260619-okw-trigger-unicode.test.js tests/quick-260620-p3f-pending-tag-commit.test.js tests/quick-260626-h5j-trigger-autoconvert.test.js` lists all 8.
- `npm test` full suite exits 0.
- `grep -n 'openEditor\|handleSave\|renderSnippetList' assets/settings.js` returns nothing; `grep -n '__SnippetEditorHelpers' assets/settings-snippets.js` returns the hook.
- `grep -n 'settings-snippets.js' settings.html sw.js` shows the script tag + PRECACHE entry.
</verification>

<success_criteria>
The SnippetsUI IIFE is extracted verbatim to settings-snippets.js with its test hook preserved, wired into settings.html + sw.js, snippet behavior proven unchanged by the (loader-updated, extraction-robust) green suite, and settings.js is slimmed to section-titles/tab-nav/backups.
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-03-SUMMARY.md` when done.
</output>
