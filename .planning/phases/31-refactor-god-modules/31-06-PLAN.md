---
phase: 31-refactor-god-modules
plan: 06
type: execute
wave: 5
depends_on: ["31-05"]
files_modified:
  - assets/version.js
  - sw.js
autonomous: false
requirements: [RFCT-01, RFCT-02, RFCT-03]
user_setup: []
must_haves:
  truths:
    - "All three new files (settings-snippets.js, settings-photos.js, export-modal.js) are present in sw.js PRECACHE_URLS, so installed PWAs fetch them after the cache rolls — verified by grep"
    - "APP_VERSION is bumped (1.2.1 -> 1.2.2) so the deploy-derived CACHE_NAME rolls and installed PWAs pick up the new precache list (the pre-commit hook skips the bump when sw.js is in the diff)"
    - "The full npm test suite is green AND the D-08 human smoke-test of the 3 extracted features passes — the phase's no-observable-change contract is met"
  artifacts: []
  key_links:
    - "version.js APP_VERSION drives the deploy INTEGRITY_TOKEN which sw.js:19 CACHE_NAME derives from — bumping it rolls the PWA precache"
    - "the 3 new <script> entries (plans 03/04/05) + 3 PRECACHE entries together preserve the offline-first load surface"
---

<objective>
Phase finalization — consolidate the service-worker version bump (the manual follow-up the pre-commit hook skips when sw.js is in the diff, per reference-pre-commit-sw-bump), audit that all three extracted files are precached and page-scoped, run the full green gate, and execute the D-08 manual UAT smoke-test phase gate.

Three separate extraction commits (plans 03/04/05) each edited PRECACHE_URLS without bumping CACHE_NAME (the hook skips it). This plan does the single consolidated APP_VERSION bump so installed PWAs roll their cache and fetch the new files, then verifies the phase's "no observable behavior change" contract end-to-end.

Purpose: Ship the refactor safely to installed PWAs + close the phase with the human gate the no-observable-change premise requires.
Output: bumped APP_VERSION + audited PRECACHE + green suite + human smoke-test sign-off.
</objective>

## Artifacts this phase produces (full phase set, for audit)

NEW files (created in prior plans, audited here): `assets/settings-snippets.js`, `assets/settings-photos.js`, `assets/export-modal.js`; `tests/31-openDB-pooling.test.js`, `tests/31-overview-render-hardening.test.js`, `tests/31-sessions-render-hardening.test.js`. NEW symbol: `window.__exportModalInit`. NEW wiring (audited here): 3 `<script>` tags (settings.html x2, add-session.html x1), 3 `sw.js` PRECACHE entries, and the `APP_VERSION` bump in `assets/version.js` (this plan). NO new public feature global.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-refactor-god-modules/31-CONTEXT.md
@.planning/phases/31-refactor-god-modules/31-VALIDATION.md
@assets/version.js
@sw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump APP_VERSION + audit PRECACHE coverage + page-scoping + final full suite</name>
  <files>assets/version.js, sw.js</files>
  <read_first>
    - assets/version.js:25 (`var APP_VERSION = '1.2.1';` — the hand-set semver to bump) and :331 (the exports object)
    - sw.js:19 (`const CACHE_NAME = 'sessions-garden-' + self.AppVersion.INTEGRITY_TOKEN;`) and the PRECACHE_URLS array (the 3 entries added in plans 03/04/05)
    - memory reference-pre-commit-sw-bump (the hook skips CACHE_NAME bump when sw.js is in the diff -> manual bump here) and project-version-bump-convention
    - .planning/phases/31-refactor-god-modules/31-RESEARCH.md §"Open Questions" Q2 (the new files must appear only on their own pages)
  </read_first>
  <action>Bump `APP_VERSION` in assets/version.js from `'1.2.1'` to `'1.2.2'` (single hand-set semver bump; leave the derived integrity/cache machinery untouched — it recomputes from the new version at deploy). Audit PRECACHE_URLS in sw.js contains all THREE new entries: `/assets/settings-snippets.js`, `/assets/settings-photos.js`, `/assets/export-modal.js` (add any that a prior plan missed). Audit page-scoping per RESEARCH Q2: confirm `grep -rn "settings-snippets\|settings-photos" *.html` shows them ONLY in settings.html, and `grep -rn "export-modal" *.html` shows it ONLY in add-session.html. Run the FULL `npm test` one final time and confirm green.</action>
  <verify>
    <automated>grep -c "1.2.2" assets/version.js && grep -c "settings-snippets.js\|settings-photos.js\|export-modal.js" sw.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "APP_VERSION" assets/version.js` shows `'1.2.2'` (bumped from 1.2.1).
    - `grep -c "/assets/settings-snippets.js\|/assets/settings-photos.js\|/assets/export-modal.js" sw.js` returns 3 (all precached).
    - `grep -rln "settings-snippets\|settings-photos" *.html` lists ONLY settings.html; `grep -rln "export-modal" *.html` lists ONLY add-session.html.
    - `npm test` exits 0 (full suite green).
  </acceptance_criteria>
  <done>APP_VERSION is bumped to 1.2.2, all three new files are precached and page-scoped to their own HTML, and the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-08 manual UAT smoke-test of the 3 extracted features (phase gate)</name>
  <what-built>The two god modules (settings.js, add-session.js) were decomposed into settings-snippets.js, settings-photos.js, and export-modal.js with NO intended observable behavior change, plus RFCT-03 cleanups (openDB pooling; overview/sessions innerHTML hardening). The full automated suite is green and the cache version is bumped to 1.2.2.</what-built>
  <action>This is a human verification checkpoint — no automated implementation. Pause and present the smoke-test steps below to the developer; resume only on explicit approval.</action>
  <how-to-verify>
    Per D-08, run a short human smoke-test of the 3 extracted features in a browser (serve locally or open the app):
    1. Settings -> Snippets: create a snippet, edit it, delete it; then import a snippets file that COLLIDES with an existing trigger and exercise BOTH the REPLACE and the MERGE collision paths. Expect the list to update correctly each time and a save toast to appear.
    2. Settings -> Photos: run "optimize all photos"; confirm the saved-bytes / storage-usage line updates; then delete-all photos. Expect the usage line to reflect each change.
    3. add-session -> export an EXISTING EDITED session (open a previously-saved session, change a field, THEN export — not a brand-new session): open the export modal, walk the stepper 1 -> 2 -> 3, confirm the preview renders, then export PDF, copy the Markdown, and use share. Expect the exported content to match the EDITED editor state (the just-changed field must appear) and all three output paths to work. (Exercising an existing edited session — not a new one — is what surfaces an accessor-vs-capture state-fork bug in export-modal.js, which a new-session export would mask.)
    Confirm nothing looks or behaves differently from before the refactor.
  </how-to-verify>
  <resume-signal>Type "approved" if all three features behave identically to before, or describe any observable difference.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA precache freshness | Installed PWAs serve the old asset list until CACHE_NAME rolls. Bumping APP_VERSION rolls it. No new external boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06-A | Availability | Installed PWAs keep serving the pre-refactor asset list (broken module boundaries) because CACHE_NAME never rolled | medium | mitigate | Single consolidated APP_VERSION bump (1.2.1->1.2.2) so the deploy-derived CACHE_NAME rolls; PRECACHE coverage audited; pre-commit-hook skip-bump gotcha explicitly handled here. |
| T-31-06-D | Repudiation/Verification | A residual observable change slips past the automated net (Phase 30 found leaf coverage was over-credited) | low | mitigate | D-08 blocking human smoke-test of the 3 features on top of green npm test. |

No new high-severity threats. Behavior preserved; XSS surface reduced (plan 02). No new attack surface (security_block_on: high — nothing at threshold).
</threat_model>

<verification>
- `grep -n "APP_VERSION" assets/version.js` shows '1.2.2'.
- `grep -c "/assets/settings-snippets.js\|/assets/settings-photos.js\|/assets/export-modal.js" sw.js` == 3.
- `npm test` full suite exits 0.
- D-08 human smoke-test returns "approved".
</verification>

<success_criteria>
APP_VERSION is bumped so installed PWAs roll their cache, all three extracted files are precached and correctly page-scoped, the full automated suite is green, and the D-08 human smoke-test confirms the three extracted features behave identically — completing the phase's no-observable-change contract (the D-09 architect-soundness gate is a downstream process step after planning, not a task here).
</success_criteria>

<output>
Create `.planning/phases/31-refactor-god-modules/31-06-SUMMARY.md` when done.
</output>
