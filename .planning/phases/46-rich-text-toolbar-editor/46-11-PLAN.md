---
phase: 46-rich-text-toolbar-editor
plan: 11
type: execute
wave: 8
depends_on: [46-09, 46-10]
files_modified:
  - assets/rich-toolbar.js
  - assets/snippets.js
  - assets/app.css
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/snippet-enter-yield.test.js
autonomous: true
gap_closure: true
requirements: [RTXT-03, RTXT-04, RTXT-09]
must_haves:
  truths:
    - "The preview toggle shows a label AND an icon reflecting its TARGET state — eye + 'Preview' while editing, pencil + 'Edit' while previewing — on both surfaces (the 7 note fields and the export editor)"
    - "In a list, pressing Enter to accept a snippet suggestion expands the snippet and does NOT also create a new list item"
    - "In a list with no snippet suggestion open, plain Enter still continues the list exactly as before"
    - "Snippets behaviour is otherwise unchanged (RTXT-09 danger zone) — only an additive open-state accessor is added"
  artifacts:
    - assets/rich-toolbar.js
    - assets/snippets.js
    - tests/snippet-enter-yield.test.js
  key_links:
    - "rich-toolbar onFieldKeyDown yields Enter/Tab to the snippets autocomplete via ev.defaultPrevented (the load-bearing signal — snippets binds first and commits synchronously before the toolbar handler runs) plus a belt-and-suspenders isPopoverOpen check"
    - "updatePreviewButton swaps both the icon (eye<->pencil) and the label ('Preview'<->'Edit') on whichever bar owns the button"
---

<objective>
Two focused toolbar fixes from the 46-08 gate. Both touch rich-toolbar.js, so they ship together.

- **Gap 4 — Preview toggle label + icon (both surfaces).** The icon-only eye button is insufficient. Ben's spec: the button shows its TARGET state — eye icon + "Preview" label while editing (click goes to preview), pencil icon + "Edit" label while previewing (click goes back to editing). This applies wherever the toggle appears: the 7 focus-attached note fields AND the persistent export-editor bar (both are the same RichToolbar preview control, so one change covers both).

- **Gap 8 — Snippet-accept Enter must not fire list auto-continue.** Inside a list, accepting a snippet autocomplete with Enter currently BOTH expands the snippet (snippets.js handleKeyDown) AND continues the list (rich-toolbar.js onFieldKeyDown) — because both listeners handle the same keydown and snippets does not stop propagation. Fix direction (Ben-ratified): the list auto-continue YIELDS when the snippets autocomplete is active — snippet accept wins; plain Enter (no suggestion open) still continues the list. This is the RTXT-09 danger zone, so the snippets change is strictly additive.

  **Listener-order reality (verified against source — this shapes the fix):** both keydown listeners are bubble-phase on the same textarea; snippets.js loads first (add-session.html) and binds first, so snippets' handleKeyDown runs FIRST and commits SYNCHRONOUSLY — `preventDefault()` + `commitSelected()` → `hidePopover()` → `_popoverState = null`. By the time rich-toolbar's onFieldKeyDown runs, the popover state is ALREADY cleared. Therefore an open-state check alone can NEVER see the popover open at yield time — `ev.defaultPrevented` is the REQUIRED, load-bearing yield signal (it survives the ordering); the open-state accessor is only belt-and-suspenders for hypothetical capture-phase or registration-order changes.

Purpose: make preview state legible and stop the snippet/list Enter collision.
Output: a labelled target-state preview toggle on both surfaces, and a list-continuation guard that yields to the snippets popover, with regression tests for both Enter paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-04-SUMMARY.md
@assets/rich-toolbar.js
@assets/snippets.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview toggle — target-state label + icon on both surfaces (Gap 4)</name>
  <files>assets/rich-toolbar.js, assets/app.css, assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
Make the preview control show a label AND a target-state icon.

1. Add a `pencil` icon to the `ICONS` map (a simple stroke pencil, same currentColor SVG idiom as the others).

2. Give the preview button a text label. The button is built once in `buildToolbar` (via `makeButton`) and appears on the shared bar AND every persistent bar, so build the label into the button so both bars get it: for the preview action, append a label span (mirroring the heading trigger's `.rich-toolbar-heading-label` pattern) with an initial "Preview" label, and keep the icon as the first child so the icon can be swapped later.

3. Make `updatePreviewButton` set the TARGET state on whichever bar owns the button (`barFor(_focused || _previewField)`):
   - Preview CLOSED (editing): icon = eye, label + title + aria-label = "Preview".
   - Preview OPEN (previewing): icon = pencil, label + title + aria-label = "Edit".
   Swap the icon by replacing the button's leading SVG child (remove the old icon node, insert the new one before the label). Keep the existing `.is-active` accent and `aria-pressed` behaviour.

4. i18n: keep `toolbar.preview` ("Preview") for the editing/target state; add a new key for the previewing/target state label (e.g. `toolbar.backToEdit` = "Edit") in ALL FOUR locales (EN canonical; HE/DE/CS translated natively). NOTE: there is NO general full-key i18n parity gate in the suite — parity tests pin hardcoded per-phase key lists (see the NEW_KEYS idiom in tests/40-i18n-parity.test.js). The pinned-key parity assertion for the new key(s) is added in Task 2's test file. The existing `toolbar.hidePreview` may be retired or left unused (your discretion; if you remove its use, keep the key or drop it consistently across all four locales).

5. CSS (app.css): the preview button now carries a label, so it can no longer be a fixed square. Add a rule (e.g. a `--labeled` modifier class on the preview button, or an attribute selector on `data-action="preview"`) giving it auto inline-size with inline padding and a small gap between icon and label, while KEEPING the 44px minimum touch target height and horizontal-scroll behaviour. Use logical properties and existing tokens/spacing — no new colors. Verify it still fits the horizontally-scrollable compact mobile bar.

Comment hygiene: plain-prose comments only — no planning identifiers in shipped files.
  </action>
  <verify>
    <automated>node --check assets/rich-toolbar.js && node tests/run-all.js</automated>
  </verify>
  <done>The preview button renders an icon + a label; toggling preview swaps eye+"Preview" (editing) to pencil+"Edit" (previewing) with matching title/aria; the new label key exists in all four locales with parity; CSS keeps a 44px touch target; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: List auto-continue yields to the snippets autocomplete on Enter/Tab (Gap 8)</name>
  <files>assets/snippets.js, assets/rich-toolbar.js, tests/snippet-enter-yield.test.js</files>
  <behavior>
    - In a list line with the snippets popover OPEN, an Enter keydown (snippets commits first, synchronously, and calls preventDefault) accepts the snippet — expansion inserted — and does NOT insert a new list item.
    - In a list line with the popover CLOSED, an Enter keydown continues the list (new marker line) exactly as before.
    - Same for Tab: with the popover OPEN, Tab accepts the snippet and does NOT indent the list line; with the popover CLOSED, Tab still indents a list line.
    - window.Snippets.isPopoverOpen() returns true only while the autocomplete popover is shown, false after commit/hide/Escape.
    - The new preview-toggle i18n key(s) from Task 1 exist in all four locales (pinned-key parity).
  </behavior>
  <action>
Fix the Enter/Tab collision so the snippets autocomplete wins when it is active. CRITICAL ORDERING FACT (verified): snippets' keydown listener binds first and commits SYNCHRONOUSLY — by the time the toolbar's handler runs, snippets has already called preventDefault(), committed the expansion, and cleared its popover state. An open-state check alone therefore never fires at yield time; `ev.defaultPrevented` is the signal that survives.

1. snippets.js (ADDITIVE ONLY — RTXT-09 danger zone): add a public `isPopoverOpen()` method returning `!!_popoverState`, and expose it on the module's return object. Change nothing else about snippet detection, expansion, popover, or key handling.

2. rich-toolbar.js `onFieldKeyDown`: at the very TOP of the handler (before the Enter and Tab branches), yield when the key is Enter or Tab AND (`ev.defaultPrevented` OR the snippets popover is open) — return immediately so the toolbar does NOT preventDefault or run list continuation / indent. **`ev.defaultPrevented` is the REQUIRED, load-bearing signal**: snippets binds first and its synchronous commit has already preventDefault'ed and closed the popover before this handler runs. The `window.Snippets && typeof window.Snippets.isPopoverOpen === "function" && window.Snippets.isPopoverOpen()` check is the belt-and-suspenders half, guarding hypothetical capture-phase or registration-order changes — never the primary signal. Plain Enter/Tab (nothing prevented, popover closed) falls through to the existing list mechanics unchanged.

3. Regression test `tests/snippet-enter-yield.test.js`: drive BOTH REAL handlers in REAL registration order — bind Snippets to the textarea FIRST (Snippets.bindTextarea), mount RichToolbar SECOND, on a real jsdom textarea fixture containing a list line, with the popover GENUINELY open (type a partial trigger so detectTrigger shows the popover, or drive handleInput with real snippet data). Dispatch a real Enter keydown and let snippets commit first, exactly as in production; assert the expansion was inserted AND the list did NOT gain a new marker line. Then, with the popover closed, dispatch Enter and assert the list DID continue. Repeat both branches for Tab (open → snippet accepted, no indent; closed on a list line → indent). **Stubbing isPopoverOpen is FORBIDDEN** — a stub-true test passes while an isPopoverOpen-only implementation fails in production (the popover is already closed at toolbar time); the test must reproduce the real ordering. Also assert `isPopoverOpen()` itself flips true while the popover shows and false after commit/hide.

4. Pinned-key parity: in the same test file (or by extending the NEW_KEYS list in tests/40-i18n-parity.test.js — the established idiom), assert the new preview-toggle key(s) from Task 1 exist in ALL FOUR locale files. There is no general full-key parity gate, so the new keys need their own pinned assertion.

Keep the full suite green. Comment hygiene: plain-prose comments only — no planning identifiers in shipped files.
  </action>
  <verify>
    <automated>node tests/snippet-enter-yield.test.js && node tests/run-all.js</automated>
  </verify>
  <done>window.Snippets.isPopoverOpen() exists and is additive-only; the toolbar yields Enter/Tab on ev.defaultPrevented (load-bearing) OR popover-open (belt-and-suspenders); the regression test drives both real handlers in real registration order with a genuinely open popover (no isPopoverOpen stubbing) for Enter AND Tab, both open and closed branches; the new preview-toggle key(s) carry a pinned-key parity assertion across all four locales; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keystroke -> two coordinating keydown handlers | Enter/Tab is claimed by both snippets and the toolbar; the fix must arbitrate deterministically without weakening either feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-11a | Tampering | snippets open-state accessor | low | mitigate | isPopoverOpen() is a read-only additive accessor; no change to snippet detection/expansion (RTXT-09 preserved) |
| T-46-11b | Spoofing | preview label/icon mismatch with state | low | mitigate | updatePreviewButton sets icon+label+title+aria together from one state flag on the owning bar |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs — edits to existing modules + CSS + i18n + one node test |
</threat_model>

<verification>
- node tests/snippet-enter-yield.test.js green (real-registration-order Enter AND Tab, open + closed branches, no stubbed open-state; isPopoverOpen state flips; pinned-key parity for the new preview label key).
- node tests/run-all.js green.
- Real-device confirmation (labelled eye/pencil toggle on note fields AND export; snippet-accept-in-a-list on iPhone) at the 46-14 gate.
</verification>

<success_criteria>
The preview toggle shows a target-state icon + label on both surfaces; accepting a snippet with Enter in a list no longer creates a stray list item; plain Enter still continues the list; snippets otherwise unchanged; suite green.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-11-SUMMARY.md` when done — record the new preview label key, the pencil icon, and the isPopoverOpen yield wiring.
</output>
