---
phase: 46-rich-text-toolbar-editor
plan: 16
type: execute
wave: 2
depends_on: [46-15, 46-17]
files_modified: []
autonomous: false
gap_closure: true
requirements: [RTXT-01, RTXT-02, RTXT-03, RTXT-04, RTXT-05, RTXT-09]
must_haves:
  truths:
    - "Gaps 10 and 11 are confirmed fixed on a real MacBook viewport: the export Step-2 DEFAULT (non-maximized) surface is usable (full-height unclipped toolbar + multi-line editor) and the toolbar stays visible when scrolling a long document to the bottom"
    - "The maximize (~90%) state, the <=768px mobile full-screen takeover, and RTL (Hebrew) mirroring of the pinned toolbar all behave correctly on real devices"
    - "The full round-2 11-item checklist (the five round-1 gap fixes, the deferred 46-08 matrix, and the round-1 in-gate fixes) re-confirms on the real browser/device matrix + a real opened PDF — this pass supersedes the 46-14 re-run"
    - "The round-3 fix (46-17) is confirmed on real devices: with export Step 2 freshly opened and the editor NEVER clicked, every persistent-toolbar control works on first click (Gap 12), and clicking Preview reveals the rendered pane below the pinned bar without manual scrolling (Gap 13) — on a MacBook, the iPhone PWA, and in Hebrew — while note-field preview behaviour is unchanged"
  artifacts: []
  key_links:
    - "This is the closing real-device pass for the round-2 gap fix; no gap-round code lands after it without re-running it — it supersedes 46-14's re-run duty and carries the full 11-item checklist PLUS the gap 10/11 verification"
---

<objective>
The mandatory real-device / real-PDF verification gate for the Phase 46 round-2 gap fix — it SUPERSEDES 46-14's re-run duty. The 46-14 re-run failed checklist item 11 on a MacBook Pro and surfaced gaps 10 (Step-2 default layout collapse) and 11 (toolbar not always visible); 46-15 fixed both with a scoped CSS change proven by a WebKit probe. jsdom has no layout engine and cannot see flex-collapse, sticky pinning, clipping, RTL mirroring, caret/undo, or PDF appearance — so the round is NOT done until a human drives the real surfaces. 46-08 established that no code lands after the gate without re-running it; this plan is that re-run for round 2.

It re-runs the full 46-14 round-2 checklist (11 items) across the target matrix (installed Safari desktop + a real iPhone PWA + Chrome + Firefox + a real opened PDF), adds an explicit gap-10/11 verification item (item 12), AND — after the round-3 fix (46-17) landed — adds a gap-12/13 verification item (item 13: persistent-toolbar controls work on first click, and Preview reveals its pane below the pinned bar). This is a human-verify checkpoint plan — no code. It remains the STANDING closing gate: no gap-round code lands after it without re-running it, and it now also carries the round-3 fixes.

Purpose: close Phase 46 with a clean real-device pass that confirms the export Step-2 surface is usable, its toolbar always visible, every toolbar control live on first click, and the preview actually visible.
Output: a signed-off verification recording pass/fail per item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-14-PLAN.md
@.planning/phases/46-rich-text-toolbar-editor/46-15-PLAN.md
@.planning/phases/46-rich-text-toolbar-editor/46-17-PLAN.md
@.planning/phases/46-rich-text-toolbar-editor/46-UAT.md
</context>

<artifacts_this_phase_produces>
No code artifacts — this plan is the closing verification gate for the round-2 gap fix.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Real-browser / real-iPhone / real-PDF re-verification of the round-2 gap fix</name>
  <read_first>
    - .planning/phases/46-rich-text-toolbar-editor/46-14-PLAN.md (the 11-item round-2 checklist this pass carries forward verbatim)
    - .planning/phases/46-rich-text-toolbar-editor/46-15-PLAN.md (the gap-10/11 CSS fix being verified here)
    - .planning/phases/46-rich-text-toolbar-editor/46-17-PLAN.md (the gap-12/13 JS fix being verified here: persistent-bar dispatch target + export preview reveal)
    - .planning/phases/46-rich-text-toolbar-editor/46-UAT.md (gaps 10 and 11 — the layout failures; gaps 12 and 13 — persistent-bar controls dead until focus, and the preview pane opening below the fold)
  </read_first>
  <action>Pause for a human checkpoint: run the pre-prod environment and drive the built surfaces on the real browser/device matrix (installed Safari desktop + iPhone PWA, Chrome, Firefox) and a real opened PDF, working through the checklist below. Do not close the phase until every item passes or failures route to a further gap-closure round. This is verification only — no code changes.</action>
  <what-built>The Phase 46 round-2 gap fix (46-15): a scoped app.css change that makes the export Step-2 edit surface the sizing floor (a min-block-size floor on `.export-card.is-editor-step`, inner-capped at 90dvh) and pins the persistent toolbar (`flex-shrink:0` + `position:sticky; inset-block-start:0` scoped to the export edit area) — PLUS the round-3 gap fix (46-17): scoped JS changes to `rich-toolbar.js` so every persistent-export-toolbar control resolves its target from its own bar and applies on first click with no prior focus (Gap 12), and opening Preview scrolls the export edit-area container so the pane is visible below the pinned bar (Gap 13). Both sit on top of the full round-1 gap-round work (granular undo/redo stack, labelled target-state preview toggle, snippet-accept-in-a-list Enter fix, emotions before/after opt-out, Heart-Wall export wording, single-editor Step 2, always-docked export toolbar, formatting-tips accordion removed, SW precache delivery). Ships within the unreleased v1.4.0 (no version bump; changelog already revised, help unaffected).</what-built>
  <how-to-verify>
    Run the pre-prod environment on a real installed Safari PWA (desktop) AND a real iPhone PWA, plus desktop Chrome and Firefox, and open a real exported PDF. Work through the checklist; do not close the phase until every item passes or a failure is routed back to gap-closure. This pass SUPERSEDES the 46-14 re-run.

    NEW — ROUND-2 GAP FIX (gaps 10/11 — verify FIRST, at the DEFAULT non-maximized size):
    12a. Export Step 2 at DEFAULT size on a real MacBook viewport (do NOT maximize): open the export, advance to Step 2. The formatting toolbar renders at FULL height with every button fully visible — no clipped sliver. The editor shows multiple lines (not ~1.5 lines). The surface is usable without hitting maximize.
    12b. Toolbar ALWAYS visible on scroll: with a long session document in the editor (or open the preview pane to lengthen the content), scroll the edit area to the BOTTOM — the formatting toolbar stays visible and clickable the whole time (it must never scroll out of view or clip).
    12c. Maximize still correct: hit the maximize toggle — Step 2 expands to ~90% with the roomier layout; restore returns to the usable default. 
    12d. Mobile still correct: on the iPhone PWA, Step 2 is the full-screen keyboard-aware takeover (unchanged) with the toolbar visible.
    12e. RTL pinned bar: switch to Hebrew — the pinned export toolbar mirrors correctly (button order + heading dropdown on the correct side), stays pinned on scroll, and does not overlay or steal clicks from the content beneath it.
    12f. SHORT desktop window: resize the desktop browser window to ~700px tall (still >768px wide, so the mobile takeover does NOT kick in) — Step 2 must stay usable: toolbar full-height and pinned, editor visible and scrollable.
    12g. Verbose locales at default size: repeat the 12a default-size check in German (the verbose ephemeral-edit info-note text wraps taller) AND in Hebrew — the toolbar stays full-height and the editor multi-line in both.
    12h. Cosmetic eyeball: while scrolled with the toolbar stuck, look at the ~8px gap under the bar (content shows through the transparent margin strip). This is a known cosmetic artifact — flag it ONLY if it reads poorly in practice; it is not a pass/fail item.

    NEW — ROUND-3 GAP FIX (gaps 12/13 — the persistent-toolbar controls and the preview reveal):
    13a. First-click controls WITHOUT focusing the editor (Gap 12): open the export, advance to Step 2, and — WITHOUT clicking inside the editor first — click a toolbar button. Test at least Preview, Bold, AND heading (open the "Text ▾" dropdown and choose a level → the line gains a heading marker): each must act on the FIRST click (Preview opens the pane; Bold, with some text selected, wraps it; heading applies the level) — no dead first click, no "click twice" needed. Spot-check one or two more controls (a list, undo) to confirm it is every control, not just Preview.
    13b. Preview reveals its pane (Gap 13): click Preview — the rendered pane appears in view, below the pinned toolbar, WITHOUT any manual scrolling (you can immediately see the formatted result; the pane is not hidden below the fold).
    13c. Toggle back to Edit: click the (now "Edit"/pencil) toggle — it returns to the editor cleanly and NOT disoriented (the editor scroll position is sane — you land back on editable content, not scrolled off into empty space), caret/undo intact. If it lands disoriented, note it — it routes to a follow-up scroll-restore.
    13d. Note-field preview unchanged: on a session note field, the focus-attached toolbar and its preview-below-the-field still behave exactly as before (the preview grows the page; no unexpected scroll jump).
    13e. iPhone + Hebrew: repeat 13a and 13b on the iPhone PWA and in Hebrew — first-click controls work and the preview reveals correctly in both. On the iPhone specifically, tapping Preview must NOT raise the soft keyboard over the revealed pane (preview is view-only — the pane must stay fully visible, not covered by the keyboard).

    GAP-ROUND FIXES (round 1 — re-confirm):
    1. Undo granularity (Gap 3): type several lines in a note field, then undo — undo removes ONE line / one typing burst at a time, NOT the whole field. One toolbar action (bold, list, heading, indent) undoes in exactly one step. On desktop the Undo button and Ctrl/Cmd+Z produce the SAME result; Ctrl/Cmd+Shift+Z redoes. On the iPhone the buttons are the only undo affordance and work. Confirm autoGrow/snippets/preview still react after undo/redo. Check on BOTH a note field and the export editor.
    2. Preview toggle label + icon (Gap 4): on a note field AND the export editor, the toggle shows eye + "Preview" while editing and switches to pencil + "Edit" while previewing (target state), in all four languages.
    3. Snippet-accept in a list (Gap 8): inside a bullet or numbered list, type a snippet trigger to open the suggestion, press Enter to accept — the snippet expands and NO new list item is created; you can keep typing in the same bullet. With no suggestion open, plain Enter still continues the list. Re-check on the iPhone.
    4. Emotions before/after opt-out (Gap 7): in the export, the emotions before/after ratings section is pre-selected (included by default). Uncheck it — the PDF omits the before/after severity block and the copied markdown omits the Issues section. Re-open the export: the choice is remembered. The default (checked) still produces the same PDF as before.
    5. Heart-Wall export wording (Gap 9): export a session flagged as Heart-Wall but NOT released — the Heart-Wall section reads as the chosen explanatory copy (e.g. "identified — not yet released"), never a bare "No". Export a released one — it reads as the chosen released copy. Confirm in the copied markdown AND the opened PDF, including Hebrew.

    DEFERRED 46-08 MATRIX (finish now):
    6. iPhone PWA pass of the core toolbar (markers, shortcuts absent on touch, auto-format, nested lists, live preview) — the items 46-08 deferred.
    7. Real opened PDF: export a session with italic Hebrew AND Latin, bold, and a nested list — italic renders per the shipped 46-02 outcome, bold + nested lists render, Hebrew RTL/bidi correct.
    8. Snippets + autogrow unchanged (RTXT-09) in an enhanced note field on a real browser.
    9. Lists re-test: the list toggle/switch semantics and auto-renumber (delete item 3, paste at end) feel right with caret + undo intact.
    10. RTL: switch to Hebrew — toolbar, heading dropdown, preview, and the export chrome mirror correctly.

    IN-GATE ROUND-1 FIXES (re-verify now they ship together):
    11. Export Step 2 is a single full-width editor (no two-pane), the export toolbar is always visible, the formatting-tips accordion is gone, and the new modules are delivered via the SW precache (no stale-cache).
  </how-to-verify>
  <resume-signal>Reply "phase verified" if all pass, or list the failures (which browser/device, which item) for a further gap-closure round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built code -> real user devices | The gate is the last line catching device-only defects (flex-collapse, sticky pinning, RTL mirroring, PDF appearance, undo/caret) that jsdom's missing layout engine cannot see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-16a | Spoofing | false-GREEN jsdom coverage | medium | mitigate | This gate exists because jsdom cannot see layout/flex/sticky/RTL/PDF; the checklist drives the real surfaces on real devices, and 46-15's WebKit probe already caught the layout collapse |
| T-46-16b | Tampering | sticky export toolbar overlaying / stealing clicks from scrolled content | medium | mitigate | Item 12e explicitly checks the pinned bar does not overlay or steal clicks from content beneath it, in LTR and RTL |
| T-46-16c | Tampering | preview/export render on real WebKit | high | mitigate | Items 2, 4, 5, 7 confirm the escape-first render + export output on real Safari; no raw-value innerHTML reaches users |
| T-46-16d | Tampering | round-3 dispatch mis-targeting (a persistent-bar click editing the wrong field) surviving to a real device | medium | mitigate | Item 13a drives every persistent-toolbar control on first click with no prior focus and confirms it acts on the export editor; item 13d confirms note-field dispatch/preview unchanged — the human gate ratifies the 46-17 jsdom mis-targeting guard on real devices |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs (verification only) |
</threat_model>

<verification>
- Human sign-off across the browser/device matrix on all 11 carried-forward checklist items PLUS item 12 (gaps 10/11 at the default size, on scroll, maximize, mobile, RTL, a ~700px-tall desktop window, and German/Hebrew verbose-chrome passes; 12h is an eyeball-only cosmetic note, not pass/fail) PLUS the new item 13 (gaps 12/13: first-click persistent-toolbar controls and the Preview reveal, on a MacBook, the iPhone PWA, and in Hebrew, with note-field preview unchanged).
- Gaps 10 and 11 confirmed fixed on a real MacBook viewport; gaps 12 and 13 confirmed fixed on real devices (every toolbar control live on first click, Preview visible below the pinned bar); the round-1 gap fixes, the deferred 46-08 matrix, and the round-1 in-gate fixes all re-confirmed on real devices + a real opened PDF.
</verification>

<success_criteria>
Every checklist item — the gap-10/11 verification (item 12), the gap-12/13 verification (item 13), and all 11 carried-forward items — is confirmed on real installed Safari (desktop + iPhone PWA), Chrome, Firefox, and a real opened PDF. The export Step-2 default surface is usable, its toolbar always visible, every toolbar control live on first click, and the Preview pane visible below the pinned bar. Any failure routes to a further gap-closure round before the phase closes.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-16-SUMMARY.md` when done — record pass/fail per checklist item per browser/device, the gap-10/11 result at the default Step-2 size and on scroll, the gap-12/13 result (first-click controls + Preview reveal, on MacBook + iPhone + Hebrew, note-field preview unchanged), the RTL pinned-bar result, the real-PDF result, and any remaining gaps.
</output>
</content>
