---
phase: 46-rich-text-toolbar-editor
plan: 06
type: execute
wave: 4
depends_on: [46-04, 46-02]
files_modified:
  - add-session.html
  - assets/export-modal.js
  - assets/app.css
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [RTXT-01, RTXT-04, RTXT-09]
must_haves:
  truths:
    - "Export-modal Step 2 gets the FULL toolbar (headings included) over the export editor, plus an info-styled ephemeral-edit note stating edits affect only this export and vanish on close (D-03)"
    - "Step 2 opens at ~50% of the viewport by default and carries a visible maximize toggle to ~90% with a roomier layout; the sizing is scoped to the active editor step so Steps 1/3 keep their natural size (issue 6); mobile is a full-screen keyboard-aware takeover (D-16/D-17)"
    - "The inner editor flexes to fill the modal's available space — no fixed resize:vertical textarea inside a fixed shell (D-18); the swap-style Edit/Preview switcher is kept, not a live pane (D-08)"
    - "text-edit.js and rich-toolbar.js are loaded on the page so both the note-field toolbar (46-05) and this export toolbar work"
  artifacts:
    - add-session.html
    - assets/export-modal.js
    - assets/app.css
  key_links:
    - "export-modal.js mounts RichToolbar over #exportEditor with { headings: true } and reuses the existing MdRender-wired exportUpdatePreview + exportApplyMobileTabs swap"
    - "add-session.html loads text-edit.js + rich-toolbar.js before export-modal.js/add-session.js (this plan owns ALL add-session.html edits)"
    - "ALL new export sizing (50%/maximize/flex-fill) is scoped to `.export-card` / `.export-card.is-maximized` / `.export-card.is-editor-step` — the shared `.modal-card` base rule is NEVER edited (crop/confirm/edit-client modals share it, no CSS regression coverage) (issue 8)"
    - "The D-03 info note uses --color-info-bg/--color-info-text; the D-14 italic-disclosure note is added ONLY if 46-02 fell back (RED) — this plan reads 46-02-SUMMARY, hence depends_on includes 46-02 (issue 5)"
---

<objective>
Redesign export-modal Step 2 into the maximum-editing-surface editor Ben asked for, and mount the full toolbar on it. This delivers: the full `RichToolbar` (headings included, D-03) over `#exportEditor`; the info-styled ephemeral-edit note (D-03); the ~50%-default + maximize-to-~90% sizing (D-16 mockup-gate synthesis); mobile full-screen takeover (D-17); an inner editor that flexes to fill the modal (D-18, killing the fixed `resize:vertical` textarea in a fixed shell); and it KEEPS the existing swap-style Edit/Preview switcher (D-08, not a live pane) with an optional keyboard shortcut.

This plan also owns ALL `add-session.html` edits — including the two shared `<script>` tags for `text-edit.js` and `rich-toolbar.js` that BOTH the note-field toolbar (46-05, same page) and this export toolbar need. Adding them here keeps 46-05 and 46-06 file-disjoint and parallel in Wave 4.

D-14 contingency: read `46-02-SUMMARY.md`. If 46-02 fell back to RED (italic flattens), add a one-line italic-disclosure note to Step 2 (`export.italicNote`) AND append the upright-in-PDF clause to the italic tooltip. If 46-02 shipped true italic (GREEN/AMBER), NO disclosure. This is DISTINCT from the always-present D-03 ephemeral-edit note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-CONTEXT.md
@.planning/phases/46-rich-text-toolbar-editor/46-RESEARCH.md
@.planning/phases/46-rich-text-toolbar-editor/46-PATTERNS.md
@.planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md
@.planning/phases/46-rich-text-toolbar-editor/46-03-SUMMARY.md
@.planning/phases/46-rich-text-toolbar-editor/46-04-SUMMARY.md
@.planning/phases/46-rich-text-toolbar-editor/46-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift/source-grounding checks):
- New add-session.html elements: two `<script>` tags (text-edit.js, rich-toolbar.js), a Step-2 info-note element, a maximize toggle button in the modal header, and restructured export-edit-area markup for flex sizing.
- New export-modal.js: RichToolbar mount over #exportEditor, a maximize toggle handler, and (conditionally) the D-14 disclosure wiring.
- New CSS in app.css: `.export-info-note`, a maximized state `.export-card.is-maximized` + a ~50%-default size rule + an `.export-card.is-editor-step` gate (so Steps 1/3 keep base sizing), and a flex-fill export-edit-area rule (with the active `.export-step.is-active` forwarding height) replacing the fixed `resize:vertical` textarea — ALL scoped to `.export-card`, never the shared `.modal-card` base.
- New i18n keys owned here: `export.ephemeralNote` (D-03) and (conditional) `export.italicNote` (D-14).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Load shared modules + mount full toolbar + D-03 info note on Step 2</name>
  <files>add-session.html, assets/export-modal.js</files>
  <read_first>
    - add-session.html (lines 440-470 Step-2 markup: export-mobile-tabs, export-edit-area, #exportEditor, #exportPreview, export-format-help; lines 611-620 script-load order)
    - assets/export-modal.js (lines 558-600 exportUpdatePreview + exportApplyMobileTabs + exportWireMobileTabs; the init region + #exportEditor mount point; buildSessionMarkdown ~171)
    - .planning/phases/46-rich-text-toolbar-editor/46-03-SUMMARY.md + 46-04-SUMMARY.md (RichToolbar.mount signature; preview/switcher coexistence)
    - .planning/phases/46-rich-text-toolbar-editor/46-02-SUMMARY.md (D-13 GREEN/RED outcome for the D-14 disclosure)
    - .planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md (Copywriting: Step-2 info note EN copy; Component Inventory: Step-2 editor + info note)
  </read_first>
  <action>
    In add-session.html add two script tags, text-edit.js then rich-toolbar.js, in the script block BEFORE export-modal.js and add-session.js (anchor after md-render.js) so both toolbar surfaces have the modules. Add a Step-2 info-note element inside the Step-2 container above the editor, informational tone, carrying the D-03 copy via data-i18n key export.ephemeralNote. In export-modal.js mount RichToolbar over #exportEditor with headings true at export-modal init (guard on typeof window.RichToolbar), coexisting with the existing exportUpdatePreview / exportApplyMobileTabs swap switcher — KEEP the swap (D-08), do NOT convert to a live pane. Add export.ephemeralNote to the four i18n files. Read 46-02-SUMMARY: if RED add export.italicNote near the editor and append the upright-in-PDF clause to the italic tooltip; if GREEN/AMBER do neither.
  </action>
  <verify>
    <automated>grep -c "rich-toolbar.js" add-session.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rich-toolbar.js" add-session.html` returns >= 1 AND `grep -c "text-edit.js" add-session.html` returns >= 1, both before the export-modal.js/add-session.js tags.
    - `grep -c "RichToolbar" assets/export-modal.js` returns >= 1 (toolbar mounted over #exportEditor with headings true).
    - The Step-2 info note element exists with data-i18n `export.ephemeralNote`, and that key is present in all four i18n-*.js files.
    - The existing exportUpdatePreview / exportApplyMobileTabs swap switcher is retained (still referenced in export-modal.js) — the live-pane conversion is NOT done (D-08).
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Both shared modules load on add-session.html; the full toolbar mounts over the export editor; the D-03 ephemeral-edit info note ships in 4 locales; the swap switcher is kept; the D-14 disclosure is applied only on RED.</done>
</task>

<task type="auto">
  <name>Task 2: Step-2 sizing — 50% default, maximize-to-90% toggle, flex-fill editor, mobile full-screen (D-16/D-17/D-18)</name>
  <files>add-session.html, assets/app.css, assets/export-modal.js</files>
  <read_first>
    - assets/app.css (line 3348 `.export-card` — the export modal card; SCOPE ALL new sizing to `.export-card` / `.export-card.is-maximized`, NEVER the shared `.modal-card` base at line 1707 which crop/confirm/edit-client modals also use — no CSS regression coverage exists (issue 8); lines 3425-3426 `.export-step` / `.export-step.is-active` — a plain display:block step div will NOT forward height, so the ACTIVE step must become a height-propagating container for #exportEditor to flex-fill, D-18 (issue 7); line 1166 the `resize: vertical` textarea rule; lines 3523-3595 export-edit-area / export-editor / export-preview; lines 1697-1699 `#confirmModal.modal` z-index coupling — keep intact so the discard-edits confirm still sits above (issue 12: this rule is at 1697-1699, NOT ~1600 which is `.client-avatar`))
    - add-session.html (the export modal header region near the Step-2 markup — where a maximize toggle button belongs, next to close)
    - .planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md (Step-2 Size Directions RESOLVED: 50% default + maximize to 90%; Component Inventory Maximize affordance; Spacing xl 32px maximized gutter)
    - .planning/phases/46-rich-text-toolbar-editor/46-CONTEXT.md (D-16 resolved synthesis, D-17 mobile full-screen, D-18 inner editor flexes)
  </read_first>
  <action>
    Add a maximize toggle button to the export modal header (next to close), icon-only, titled via a new i18n key (e.g. export.maximize / export.restore). In app.css SCOPE EVERY new sizing rule to `.export-card` (and `.export-card.is-maximized`) — do NOT touch the shared `.modal-card` base rule (crop/confirm/edit-client modals reuse it and have no CSS regression coverage) (issue 8). Make the export card open at ~50% of the viewport by default on desktop (width/height sized to ~50vw/50vh within existing min/max sensible bounds so it does not look like a separate app), and add an `.export-card.is-maximized` state (toggled by the button) that expands to ~90% with a roomier gutter (32px). STEP SCOPING (issue 6 — Steps 1 and 3 must not inherit Step-2's viewport-driven size and render oddly empty): gate the 50%/maximize/flex-fill sizing so it applies ONLY while the editor step (Step 2) is active — add an `.export-card.is-editor-step` class that export-modal.js toggles ON when Step 2 becomes active and OFF on Steps 1/3, so Steps 1 and 3 keep the existing base `.export-card` natural sizing (min(720px,92vw), auto height). Replace the fixed `resize: vertical` textarea-in-fixed-shell with a flex-fill layout: the ACTIVE Step-2 container (`.export-step.is-active`, app.css:3425) must become a height-propagating flex container (issue 7), the export-edit-area is a flex container, and #exportEditor flexes to fill the available modal height (D-18) at BOTH sizes — remove the `resize: vertical` on the export editor specifically (keep it for any other textarea that relies on it). For mobile: the existing matchMedia(max-width:768px) path becomes a full-screen 100%x100% keyboard-aware takeover (D-17). In export-modal.js wire the maximize button to toggle `.is-maximized` on the `.export-card`, toggle `.is-editor-step` on Step-2 entry/exit in the existing step-navigation code, and re-run exportApplyMobileTabs / any layout refresh; optionally bind a keyboard shortcut for the Edit/Preview swap (discretion). Keep the `#confirmModal.modal` z-index coupling (app.css:1697-1699) intact (the discard-edits confirm still sits above).
  </action>
  <verify>
    <automated>grep -c "is-maximized" assets/app.css assets/export-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - A maximize toggle button exists in the export modal header (add-session.html) with an i18n-keyed title.
    - All new sizing is scoped to `.export-card` (issue 8): `grep -c "export-card.is-maximized" assets/app.css` returns >= 1, and the base `.modal-card` rule (app.css:1707) is NOT modified — the maximize/50%/flex sizing targets `.export-card` selectors only (confirm by reading the diff).
    - `grep -c "is-maximized" assets/export-modal.js` returns >= 1 (state class toggled).
    - Steps 1 and 3 do not inherit Step-2's size (issue 6): the 50%/maximize/flex sizing is gated to the active editor step (e.g. `.export-card.is-editor-step`, toggled on Step-2 entry) so Steps 1/3 keep the base `.export-card` natural sizing — the gating selector exists in app.css AND is toggled in export-modal.js.
    - The active Step-2 container forwards height (issue 7): the `.export-step.is-active` (app.css:3425) path is made a height-propagating container so #exportEditor can flex-fill (D-18).
    - The export editor no longer relies on a fixed `resize: vertical` inside a fixed shell — it flex-fills (grep the export-edit-area flex rule; confirm the export-editor resize:vertical is removed while any unrelated resize:vertical is preserved).
    - The mobile path is a full-screen takeover at <=768px (CSS + the matchMedia branch).
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Step 2 opens at ~50% with a working maximize-to-~90% toggle and a flex-fill editor (active step forwards height); the sizing is scoped to `.export-card`/the editor step so Steps 1/3 keep their natural size and the shared `.modal-card` base is untouched; mobile is full-screen; the confirm-dialog z-order is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note/document markdown -> Step-2 preview | The Step-2 swap preview renders user markdown to HTML — must stay escape-first |
| ephemeral edits -> not persisted | Step-2 edits must never write back to the session (D-03) — the info note sets the expectation and the existing flow discards on close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-06a | Tampering / Elevation | Step-2 preview render | high | mitigate | The kept exportUpdatePreview already routes through MdRender.render (escape-first); the toolbar adds no raw-innerHTML path; V5 output-encoding control preserved |
| T-46-06b | Information Disclosure | ephemeral edits leaking into the saved session | medium | mitigate | Step-2 edits are export-only by existing design; the D-03 info note makes this explicit; no new write-back path is added |
| T-46-06c | Tampering | script-tag load order | low | mitigate | text-edit.js + rich-toolbar.js load before export-modal.js/add-session.js so window.TextEdit/RichToolbar exist at mount; grep-verified ordering |
| T-46-SC | Tampering | npm/pip installs | low | accept | No package installs (HTML/CSS/JS edits). Package Legitimacy Audit verdict: N/A (RESEARCH) |
</threat_model>

<verification>
- `npm test` green.
- Source assertions: both script tags present + ordered; RichToolbar mounted over #exportEditor; swap switcher kept; info note in 4 locales; is-maximized state defined + toggled; export editor flex-fills (no fixed resize in a fixed shell).
- Real-device Step-2 sizing/maximize/mobile-full-screen + snippets-in-export-editor are verified at the phase gate (46-08).
</verification>

<success_criteria>
Export Step 2 is a roomy, toolbar-equipped editor: full toolbar (headings) over #exportEditor, an ephemeral-edit info note, ~50%-default with a maximize-to-~90% toggle, a flex-fill editor (no fixed resize-in-fixed-shell), mobile full-screen, and the kept Edit/Preview swap. Shared modules load in the right order. D-03/D-08/D-16/D-17/D-18 satisfied; D-14 disclosure only if 46-02 fell back.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-06-SUMMARY.md` when done — record the maximize state class, the sizing values chosen, whether the D-14 disclosure was applied (per 46-02 outcome), and confirm the shared-module script tags were added.
</output>
