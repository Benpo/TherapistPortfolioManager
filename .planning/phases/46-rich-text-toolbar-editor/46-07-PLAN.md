---
phase: 46-rich-text-toolbar-editor
plan: 07
type: execute
wave: 5
depends_on: [46-05, 46-06]
files_modified:
  - assets/changelog-content-en.js
  - assets/help-content-en.js
autonomous: true
requirements: [RTXT-01, RTXT-02, RTXT-03, RTXT-04, RTXT-05]
must_haves:
  truths:
    - "The v1.4 changelog entry (EN) tells therapists about the formatting toolbar, keyboard shortcuts, auto-format lists, live preview, and the roomier export editor — benefit-led, no emojis, garden register"
    - "The affected EN help topics describe the new toolbar/preview/list editing so the docs hard-gate is satisfied for every user-facing change this phase ships"
    - "Every new watched module file (text-edit.js, rich-toolbar.js, rubik-italic-base64.js) is listed in a help topic's covers[] so the per-file help demand resolves"
  artifacts:
    - assets/changelog-content-en.js
    - assets/help-content-en.js
  key_links:
    - "The docs-gate (scripts/docs-gate.js) is satisfied by an EN changelog edit + an EN help edit; new watched files must appear in a topic covers[] (per-file help demand) or carry a Help-Unaffected trailer"
    - "EN is the corpus of record; HE/DE/CS follow in a later locale pass (not required to satisfy the gate)"
---

<objective>
Satisfy the Phase 43 docs hard-gate for this heavily user-facing phase: update the EN changelog and the affected EN help topics to describe the rich-text toolbar editor, and register the new watched module files in the appropriate help-topic `covers[]` so the per-file help demand resolves.

The docs-gate (scripts/docs-gate.js) demands, for the push that ships this phase's code: (a) an edit to `assets/changelog-content-en.js` (push-global changelog demand), and (b) per-file HELP coverage for every changed TRIGGER file — satisfied for files already in a topic `covers[]` by ANY `help-content-en.js` edit, and for NEW watched files (text-edit.js, rich-toolbar.js, rubik-italic-base64.js) by adding them to a topic's `covers[]`. This plan makes the honest edits: real topic content + covers[] registration.

The changed watched code files this phase ships: add-session.js, add-session.html, export-modal.js, pdf-export.js, sw.js (already in topic covers[]), plus NEW text-edit.js, rich-toolbar.js, rubik-italic-base64.js. Read HELP-MAP.md cold to find owning topics — do NOT read the whole help corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-CONTEXT.md
@.planning/phases/46-rich-text-toolbar-editor/46-05-SUMMARY.md
@.planning/phases/46-rich-text-toolbar-editor/46-06-SUMMARY.md
@HELP-MAP.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift/source-grounding checks):
- Extended v1.4 entry (or new highlights/categories) in `window.CHANGELOG_CONTENT_EN`.
- Updated/added EN help-topic body text + new `covers[]` entries for text-edit.js, rich-toolbar.js, rubik-italic-base64.js.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EN changelog entry for the rich-text toolbar editor (v1.4)</name>
  <files>assets/changelog-content-en.js</files>
  <read_first>
    - assets/changelog-content-en.js (lines 1-55 schema docstring + copy-status note; the existing v1.4.0 entry at line ~74 — EXTEND its highlights/categories, do NOT add a second v1.4 entry; terminology + no-emoji + register rules)
    - .planning/phases/46-rich-text-toolbar-editor/46-CONTEXT.md (what shipped: toolbar, shortcuts, auto-format, live preview, nested lists, roomier export editor)
    - .planning/phases/46-rich-text-toolbar-editor/46-02-SUMMARY.md (whether true italic in PDF shipped — phrase the italic line accordingly)
  </read_first>
  <action>
    Extend the existing v1.4.0 entry with the Phase 46 user-facing changes: a formatting toolbar over session notes (bold, italic, bullet + numbered lists, headings), desktop keyboard shortcuts, auto-formatting lists while typing, a live preview toggle, and a roomier export editor with a maximize option. Place them under the right categories (new / improved) and refresh the highlights if this is the latest entry. Phrase the italic line per 46-02's outcome: if true italic shipped, say italic now exports as true slanted text (Hebrew + Latin); if it flattened (D-14), do NOT claim true italic. Keep the garden register — benefit-led, warm, calm, client/session terminology, NO emojis. This asset (including comments) is served to clients — no internal/planning framing anywhere.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "1.4.0" assets/changelog-content-en.js` returns >= 1 and the v1.4 entry now mentions the formatting toolbar (grep the entry for a toolbar/formatting phrase).
    - No emojis added (the changelog-integrity test, if present, stays green under `npm test`).
    - `npm test` exits 0 (changelog schema/integrity intact).
  </acceptance_criteria>
  <done>The v1.4 EN changelog entry describes the toolbar editor changes in-register with no emojis; changelog integrity green.</done>
</task>

<task type="auto">
  <name>Task 2: EN help topics + covers[] registration for the new modules</name>
  <files>assets/help-content-en.js</files>
  <read_first>
    - HELP-MAP.md (the topic index — owning topics for add-session.html/js, export-modal.js, pdf-export.js: capturing-emotions/topic-quick-paste + topic-snippets, starting-a-session/topic-new-session, review-export/topic-single-export + topic-export-formats)
    - assets/help-content-en.js (lines 191-235 capturing-emotions topics; lines 159-190 starting-a-session; lines 305-330 review-export; the topic object shape incl. the `covers` array — this is where files are registered)
    - .planning/phases/46-rich-text-toolbar-editor/46-05-SUMMARY.md + 46-06-SUMMARY.md (final behavior: toolbar buttons, shortcuts, auto-format, preview, roomier export editor)
  </read_first>
  <action>
    Update the affected EN help topics so a therapist learns the new editing: in the capturing-emotions section (topic-quick-paste or a suitable formatting-focused topic), describe the formatting toolbar (bold, italic, bullet/numbered lists, headings), the desktop keyboard shortcuts, typing-to-auto-format lists, indent/outdent, and the live preview toggle; in the review-export section (topic-single-export / topic-export-formats), note the roomier Step-2 editor with the same toolbar and a maximize option. Add the NEW watched module files to the covers[] of the topic they belong to: text-edit.js and rich-toolbar.js -> a capturing-emotions formatting topic (they power the note-field toolbar); rubik-italic-base64.js -> topic-single-export (it is the PDF export font). Keep client/session terminology, no emojis. Do NOT read the whole help corpus — HELP-MAP.md is the index.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rich-toolbar.js" assets/help-content-en.js` returns >= 1 AND `grep -c "text-edit.js" assets/help-content-en.js` returns >= 1 AND `grep -c "rubik-italic-base64.js" assets/help-content-en.js` returns >= 1 (all new watched files registered in a covers[]).
    - At least one help topic body now describes the formatting toolbar / live preview (grep for a toolbar/preview phrase in help-content-en.js).
    - `npm test` exits 0 (help-content integrity intact).
    - Dry-run intent: after this plan + the feature plans, a push would satisfy scripts/docs-gate.js — an EN changelog edit AND EN help edit accompany the watched code changes, and every new watched file is in a covers[].
  </acceptance_criteria>
  <done>The affected EN help topics describe the toolbar/preview editing and every new watched module file is registered in a topic covers[]; the docs hard-gate will pass at push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help/changelog content -> shipped to clients | These assets (incl. comments) are served to end users; no internal/planning framing may leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-07a | Information Disclosure | client-served docs content | low | mitigate | Garden register, client/session terminology, no emojis, no internal framing — matches the shipped changelog/help copy conventions; changelog/help integrity tests gate the shape |
| T-46-07b | Repudiation | docs-gate bypass | low | mitigate | This plan makes the honest EN changelog + help + covers[] edits so the layered docs gate (hook + CI) passes without a skip trailer |
| T-46-SC | Tampering | npm/pip installs | low | accept | No package installs (content edits). Package Legitimacy Audit verdict: N/A (RESEARCH) |
</threat_model>

<verification>
- `npm test` green (changelog + help integrity).
- Source assertions: v1.4 entry mentions the toolbar; all three new watched files appear in a help covers[]; a formatting/preview help body exists.
- The docs hard-gate (scripts/docs-gate.js) passes at push for the phase's watched code changes.
</verification>

<success_criteria>
The EN changelog and affected EN help topics document the rich-text toolbar editor, and every new watched module file is registered in a help topic covers[], so the Phase 43 docs hard-gate passes cleanly at push — no skip trailer needed. HE/DE/CS help/changelog translation is a later locale pass (not gate-blocking).
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-07-SUMMARY.md` when done — list the topics edited, the covers[] additions, and confirm a dry docs-gate check would pass for the phase's watched files.
</output>
