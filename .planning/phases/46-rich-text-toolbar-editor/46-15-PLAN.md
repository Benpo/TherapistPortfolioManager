---
phase: 46-rich-text-toolbar-editor
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/app.css
  - tests/webkit/46-export-step2-layout.mjs
autonomous: true
gap_closure: true
requirements: [RTXT-01, RTXT-04, RTXT-05]
must_haves:
  truths:
    - "At a representative MacBook content viewport (~820-980px tall) the export Step-2 DEFAULT (non-maximized) surface renders a full-height, unclipped formatting toolbar and a multi-line editor — no sliver toolbar, no ~1.5-line editor collapse (Gap 10)"
    - "The persistent export toolbar stays fully visible (pinned) when a long document is scrolled to the bottom of the edit area — it is never clipped by flex-compression and never scrolls out of the edit area (Gap 11)"
    - "The ratified 50%-default look is preserved on tall screens (50dvh wins there); the maximize (90dvh) state and the <=768px mobile full-screen takeover behave unchanged"
    - "A falsifiable Playwright-WebKit probe encodes the three assertions above and is RED against current app.css, GREEN after the fix; the full jsdom suite stays 190/190 green"
  artifacts:
    - tests/webkit/46-export-step2-layout.mjs
    - assets/app.css
  key_links:
    - "The EDIT SURFACE is the sizing floor: a min-block-size floor of min(640px, 90vh/90dvh dual-declared) on .export-card.is-editor-step (inner-capped so it can never exceed the modal max-height on any viewport, protecting the mobile landscape takeover) grows the card on short laptop viewports while block-size:50dvh still wins on tall screens (crossover at 1280px viewport height)"
    - "flex-shrink:0 on the export edit-area toolbar prevents flex-compression clipping (Gap 10/11a); position:sticky + inset-block-start:0 scoped to the export edit area pins the bar so scrolling the overflow-y:auto edit area never moves it out of view (Gap 11b) — the bar already carries an opaque surface background + popover z-index"
    - "All CSS is gated to .export-card.is-editor-step / the export edit area — the shared .modal-card base and the 7 add-session note-field toolbars (which live outside .export-card) are provably untouched"
---

<objective>
Round-2 gap fix for Phase 46. The 46-14 real-device re-run (2026-07-15, MacBook Pro, build 00f0c08) failed checklist item 11 and produced two new high-severity gaps (46-UAT.md gaps 10 and 11). This plan fixes BOTH with a single scoped CSS change to the export Step-2 editing surface, proven by a new falsifiable WebKit layout probe.

**Gap 10 — Step-2 default layout collapses on laptop viewports.** `.export-card.is-editor-step` is hard-sized to `block-size: 50dvh` (app.css ~3360). On a ~820-980px laptop viewport that is a ~410-490px card; the fixed Step-2 chrome inside it (title, step indicator, "Step 2 of 3" helper, ephemeral-edit info note, actions row) consumes ~330-380px, so `.export-edit-area` (flex:1, min-block-size:0, overflow-y:auto — app.css ~3384) collapses to ~50-100px. Inside that collapsed column the toolbar (a default-shrinkable flex child with `overflow-y:hidden`, app.css ~5360) compresses and clips its buttons to slivers; the editor's 240px min-height overflows the tiny scroll area showing ~1.5 lines. The fix makes the EDIT SURFACE the sizing floor, not the card.

**Gap 11 — export toolbar not ALWAYS visible.** Ben re-affirmed verbatim: "the formatting toolbar should never be hidden in the export." Two mechanisms still hide the persistent bar: (a) compression clipping per Gap 10 (no flex-shrink:0); (b) scroll-away — rich-toolbar.js docks the persistent bar `beforebegin` the textarea, i.e. INSIDE `.export-edit-area`, which is the `overflow-y:auto` scroll container, so scrolling a document taller than the area moves the bar out of view. The fix pins the bar (sticky) and stops it compressing (flex-shrink:0).

Purpose: make the export Step-2 default surface usable and keep its toolbar always visible, without disturbing the ratified 50%/90% sizing, the mobile takeover, or the 7 note-field toolbars.
Output: a scoped app.css change plus a RED-then-GREEN Playwright-WebKit layout probe.

**Explicitly NOT in this round (architect-review NOT-list):**
- Step-2 chrome (title, step indicator, ephemeral-edit info note, helper line, actions row) is intentionally UNCHANGED this round — no slimming, no reflow.
- On wide desktop windows shorter than ~640px content height the editor stays constrained (the 90dvh cap binds), though the toolbar remains visible and pinned — accepted residual, revisit only if Ben flags it at the 46-16 gate.

**Evidence framing:** probe assertion D (maximize reaches ~90dvh) is a REGRESSION GUARD, not part of the RED evidence — the RED proof rests on assertions A/B/C only. The SUMMARY must state this too.

**Load-bearing invariant (record, do not change):** the three `min-block-size: 0` declarations in the height-forwarding chain — `.modal-card-body` (app.css ~1725, as `min-height: 0`), `.export-card.is-editor-step .export-step.is-active` (~3380), and `.export-card.is-editor-step .export-edit-area` (~3386) — are load-bearing for the innermost-scroller/sticky-pin behaviour. Removing any of them silently breaks the pin (the scroll container stops being the innermost overflow ancestor). This plan relies on them; it must not remove or weaken them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-UAT.md
@.planning/phases/46-rich-text-toolbar-editor/46-11-PLAN.md
@.planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- **tests/webkit/46-export-step2-layout.mjs** (NEW) — an ad-hoc Playwright-WebKit layout probe modelled on `tests/webkit/41-rtl-geometry.mjs`. Resolves the PINNED local Playwright (`/Users/ben/Claude-Code-Sandbox/TPM_Docs/video-pipeline/node_modules/playwright`), serves the repo, drives add-session.html's export Step-2 at TWO viewports — a MacBook-band 1440x820 pass AND a 1000x700 pass (the 90dvh-cap regime) — plus a German-locale pass (verbose chrome), and asserts (1) toolbar unclipped, (2) editor usable height, (3) toolbar pinned after scroll-to-bottom (non-vacuous: overflow precondition asserted), plus a maximize-still-reaches-~90% regression guard. NOT part of `npm test` (the runner only globs top-level `tests/*.test.js`). NOT a shipped asset — not added to sw.js PRECACHE_URLS, raises no docs demand.
- **assets/app.css** — MODIFIED (no new classes, no new tokens, no new colors):
  - `.export-card.is-editor-step` gains a min-block-size floor using the file's dual-declaration idiom: `min-block-size: min(640px, 90vh);` followed by `min-block-size: min(640px, 90dvh);`. Floor calibrated to 640px (architect review): at 600px the probe's scroll-pin assertion had only ~20px of overflow headroom (borderline vacuous); 640px gives real headroom while the 50dvh-wins crossover only moves from 1200px to 1280px viewport height — the ratified ~50% tall-screen look is preserved. `block-size: 50dvh` stays as the preferred height.
  - A new scoped rule for the export edit-area toolbar, e.g. `.export-card.is-editor-step .export-edit-area > .rich-toolbar`, adding `flex-shrink: 0;` `position: sticky;` `inset-block-start: 0;` (the base `.rich-toolbar` already supplies `background: var(--color-surface)` + `z-index: var(--z-popover)`).
- No JS changes, no i18n changes, no new shipped assets, no sw.js change, no APP_VERSION change (stays 1.4.0 per architect ruling).
</artifacts_this_phase_produces>

<docs_disposition>
`assets/app.css` is in the docs gate's CHANGELOG-ONLY tier (recurring plumbing — verified in `scripts/lib/role-table.js` header: "app.js, app.css, tokens.css..."). It therefore raises ONLY the push-global changelog demand and NEVER a per-file help demand, so it needs no `Help-Unaffected:` trailer. The changelog demand is already satisfied by the unpushed range's existing EN changelog edit (round-1 revised the unreleased v1.4.0 entry, commit 8cd345e). These are layout bug fixes to behaviour already documented for v1.4.0 — no user-facing WORDING changes, so NO help/changelog rewrites are planned and NO new trailers are needed. `tests/webkit/46-export-step2-layout.mjs` lives under `tests/` (not a shipped path) and raises no demand. Expected disposition: help unaffected, changelog already covered by the range.
</docs_disposition>

<tasks>

<task type="auto">
  <name>Task 1: Create the falsifiable WebKit layout probe (RED against current app.css)</name>
  <files>tests/webkit/46-export-step2-layout.mjs</files>
  <read_first>
    - tests/webkit/41-rtl-geometry.mjs (the probe template: pinned-Playwright createRequire resolution, the REPO_ROOT static server, the assert()/failures harness, the addInitScript gate-satisfaction pattern, exit-code discipline — reuse ALL of these idioms verbatim in shape)
    - add-session.html lines ~418-467 (the #exportModal markup: `.modal.is-hidden`, `.export-card`, the `.export-step[data-step="2"]` block, `.export-info-note`, `.export-edit-area`, `#exportEditor`)
    - assets/export-modal.js lines ~79-86 (the persistent RichToolbar.mount on #exportEditor at boot) and ~581-605 (exportSetActiveStep(2): the exact classes it toggles — `.is-editor-step` on .export-card, `.is-active` on the step)
    - assets/app.css lines ~3360-3401, ~3564-3572, ~5360-5382 (the current export-card / export-edit-area / export-editor / .rich-toolbar rules this probe must catch collapsing)
  </read_first>
  <action>
Create `tests/webkit/46-export-step2-layout.mjs` — an ad-hoc Playwright-WebKit probe that reproduces the production export Step-2 layout and asserts it is usable. Reuse the STRUCTURE of `tests/webkit/41-rtl-geometry.mjs` exactly: the `PINNED_PLAYWRIGHT` createRequire resolution with the same FATAL fallback message, the tiny REPO_ROOT static file server with the same MIME map + path-traversal guard, the `assert(cond,label,detail)` / `failures` accounting, and `process.exit(0)` only when every assertion passes (non-zero otherwise). This file is NOT run by npm test and is NOT a shipped asset.

Probe setup (run the FULL assertion set A/B/C at TWO viewports — 1440x820 AND 1000x700 — plus a German-locale pass; D runs once at 1440x820):
1. Launch WebKit; open a context at viewport width 1440, height 820 — a representative MacBook Pro content viewport inside the ~820-980px band gap 10 identifies as the failure zone. Then repeat the A/B/C set in a SECOND context at 1000x700, the regime where the floor's 90vh/90dvh inner cap binds (proves the capped floor still yields a usable, pinned surface on short desktop windows). Additionally run ONE of the two viewports a second time with `portfolioLang` set to `de` (German — the most verbose chrome: the ephemeral-edit info note wraps taller) and re-assert A/B/C, so verbose-locale chrome cannot silently eat the editor floor.
2. addInitScript the app-shell gate-satisfaction localStorage keys exactly as 41-rtl-geometry.mjs does (portfolioLicenseActivated=1, portfolioLicenseInstance=webkit-probe, portfolioTermsAccepted=1, sg.welcomeSeen=1, portfolioLang=en), then goto `add-session.html` (the page that hosts #exportModal AND runs the persistent export-toolbar mount at boot). If add-session.html redirects away without a client/session context, seed the minimal localStorage/query-param the page needs to stay (executor determines from the page's boot gate) — the goal is a loaded add-session DOM, no session data is required for layout.
3. waitForFunction until `document.getElementById('exportEditor')` exists AND `window.RichToolbar` is present. If the persistent bar is not already docked (`.export-card .export-edit-area > .rich-toolbar` absent), call `window.RichToolbar.mount([document.getElementById('exportEditor')], { headings: true, persistent: true })` inside page.evaluate to reproduce the exact production dock.
4. In a page.evaluate, reproduce the Step-2 active state EXACTLY as exportSetActiveStep(2) does: remove `is-hidden` from `#exportModal`; add `is-editor-step` to `.export-card`; add `is-active` to `.export-step[data-step="2"]` and remove it from the other steps. Then set `#exportEditor.value` to a long multi-line document (>= 60 short lines) and dispatch a real `input` event on it so autogrow/listeners observe it.

Assertions (each via getBoundingClientRect / clientHeight / scrollHeight in page.evaluate; resolve the bar as `.export-card.is-editor-step .export-edit-area > .rich-toolbar`, the area as `.export-card.is-editor-step .export-edit-area`, the editor as `#exportEditor`):

A. Toolbar unclipped: assert `bar.getBoundingClientRect().height >= 40` (a single 44px touch-target row must not render as a sliver) AND `bar.clientHeight >= bar.scrollHeight - 2` (with `overflow-y:hidden`, scrollHeight exceeding clientHeight means the buttons are vertically clipped).

B. Editor usable: compute `visibleEditorHeight = Math.min(editorRect.bottom, areaRect.bottom) - Math.max(editorRect.top, areaRect.top)` (the editor box overflows the scroll area in the bug, so measure the VISIBLE intersection, not the 240px box). Assert `visibleEditorHeight >= 140`.

C. Toolbar stays visible after scroll: first assert the precondition `area.scrollHeight - area.clientHeight > 20` (the edit area genuinely overflows and can scroll). EXECUTOR NOTE — C must NEVER pass vacuously: verify at runtime that this precondition actually HOLDS; if the fill-the-editor + input autogrow path does not overflow the area (autogrow may size the textarea within it), you MUST exercise the fallback — open the preview pane via the toolbar preview button to force overflow — and re-assert the precondition as a hard assertion (`scrollHeight - clientHeight > 20`) BEFORE testing the pin. Only then set `area.scrollTop = area.scrollHeight`, wait one rAF, and assert the bar is still fully inside the visible edit region: `barRect.top >= areaRect.top - 2` AND `barRect.bottom <= areaRect.bottom + 2` (a docked-in-scroll bar with no pin fails — after scroll its bottom is above areaRect.top).

D. Maximize guard (REGRESSION GUARD only — not part of the RED evidence; the RED proof rests on A/B/C): add `is-maximized` to `.export-card`, wait one rAF, assert `card.getBoundingClientRect().height >= 0.85 * viewportHeight` (the maximize state must still reach ~90dvh — proves the min-block-size floor did not clobber it). Remove `is-maximized` afterward. Runs once, at the 1440x820 viewport.

FALSIFIABILITY (mandatory): after writing the probe, run it against the CURRENT (unmodified) app.css and CONFIRM it is RED — assertions A, B, and C FAIL (the toolbar is a compressed clipped sliver, the editor collapses to ~1.5 lines, and the bar scrolls away). D is a regression guard, NOT part of the RED evidence. Record the RED output in the plan summary. Do NOT modify app.css in this task.

Comment hygiene: this is a test file under tests/ (NOT a shipped path — the watched shipped set is root *.html, assets/**, sw.js, manifest.json), so, like 41-rtl-geometry.mjs, its comments MAY name gaps/plan IDs. Keep them descriptive.
  </action>
  <acceptance_criteria>
    - `tests/webkit/46-export-step2-layout.mjs` exists and resolves Playwright from the PINNED path `/Users/ben/Claude-Code-Sandbox/TPM_Docs/video-pipeline/node_modules/playwright` (never package.json, never a global install).
    - It serves the repo, loads add-session.html with the gate localStorage set, forces the export Step-2 active state (`#exportModal` shown, `.export-card.is-editor-step`, step-2 `.is-active`), mounts/uses the persistent export toolbar, and fills #exportEditor with a >=60-line document.
    - It runs the A/B/C assertion set at BOTH viewports — 1440x820 AND 1000x700 (the 90dvh-cap regime) — plus one German-locale (`portfolioLang=de`) pass of A/B/C at one of the two viewports.
    - It contains assertions A (toolbar height >= 40 AND clientHeight >= scrollHeight-2), B (visible editor height >= 140 via rect intersection), C (HARD-asserted precondition `area.scrollHeight - area.clientHeight > 20` — using the preview-open fallback to force overflow if the autogrow fill path does not overflow — then, after scrollTop=scrollHeight, bar fully inside area rect within 2px; C never passes vacuously), and D (maximize card height >= 0.85*viewport; regression guard only, once at 1440x820).
    - Run against UNMODIFIED app.css, the probe exits non-zero with A, B, and C reported FAIL (RED proof recorded; D is not part of the RED evidence).
    - `node --check tests/webkit/46-export-step2-layout.mjs` passes; the file is NOT referenced by tests/run-all.js and NOT added to sw.js.
  </acceptance_criteria>
  <verify>
    <automated>node --check tests/webkit/46-export-step2-layout.mjs && node tests/webkit/46-export-step2-layout.mjs; echo "EXPECT RED (non-zero) against current app.css — assertions A/B/C fail"</automated>
  </verify>
  <done>The probe exists, node --check passes, and running it against the current source is RED (A/B/C fail) — the falsifiable proof that gaps 10/11 reproduce in real WebKit layout.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the export Step-2 layout — edit surface as sizing floor + pinned unclippable toolbar (GREEN)</name>
  <files>assets/app.css, tests/webkit/46-export-step2-layout.mjs</files>
  <read_first>
    - assets/app.css lines ~3347-3401 (the `.export-card`, `.export-card.is-editor-step`, `.is-maximized.is-editor-step`, `.modal-card-body`, `.export-step.is-active`, `.export-edit-area`, `.export-editor` rules — the exact block to extend)
    - assets/app.css lines ~3706-3725 (the `@media (max-width: 768px)` mobile full-screen takeover — MUST stay behaviourally unchanged)
    - assets/app.css lines ~5360-5382 (the base `.rich-toolbar`: `overflow-y:hidden`, `position:relative`, `z-index:var(--z-popover)`, `background:var(--color-surface)` — the properties the scoped override builds on)
    - assets/app.css lines ~1707-1732 (the `.modal-card` base: display:flex column, max-height:90dvh — the cap the card floor must respect)
    - tests/webkit/46-export-step2-layout.mjs (the probe from Task 1 — the GREEN target)
  </read_first>
  <action>
Apply a scoped CSS change to `assets/app.css` that makes the edit surface the sizing floor and pins the toolbar. Two edits, both gated to the export editor step — do NOT touch the shared `.modal-card` base, the `.is-maximized.is-editor-step` rule, or the `@media (max-width:768px)` block.

1. On `.export-card.is-editor-step` (the rule currently setting `block-size: 50vh` / `block-size: 50dvh`), ADD a `min-block-size` floor using the file's dual-declaration idiom (vh fallback line first, dvh line second — exactly like the existing block-size pair): `min-block-size: min(640px, 90vh);` followed by `min-block-size: min(640px, 90dvh);`. Keep `block-size: 50dvh` as the preferred height. Floor calibration rationale (architect review — record in the SUMMARY): at 600px the probe's scroll-pin assertion (C) had only ~20px of overflow headroom, borderline vacuous; 640px gives real headroom, while the 50dvh-wins crossover only moves from 1200px to 1280px viewport height — the ratified ~50% tall-screen look is preserved. Effect: on tall screens (>=1280px) 50dvh is larger and wins; on ~820-980px laptops the 640px floor grows the card so the interior chrome plus a usable toolbar+editor fit; the inner `90vh`/`90dvh` cap guarantees the floor can NEVER exceed the modal's max-height on any viewport — including mobile landscape — so the `@media (max-width:768px)` 100dvh takeover is never forced taller than the screen.

Invariant (do NOT change, and do not let a "cleanup" remove them): the three zero-min declarations in the height-forwarding chain — `.modal-card-body` `min-height: 0` (~1725), `.export-card.is-editor-step .export-step.is-active` `min-block-size: 0` (~3380), and `.export-card.is-editor-step .export-edit-area` `min-block-size: 0` (~3386) — are load-bearing for the innermost-scroller/sticky-pin behaviour; removing any silently breaks the pin.

2. ADD a new rule scoped to the export edit-area toolbar — `.export-card.is-editor-step .export-edit-area > .rich-toolbar` (this selector matches ONLY the persistent export bar; the 7 note-field toolbars live outside `.export-card` and are provably unaffected): set `flex-shrink: 0;` (the bar can never be flex-compressed/clipped — closes Gap 10's clip + Gap 11a), `position: sticky;` and `inset-block-start: 0;` (the bar stays pinned at the top of the `overflow-y:auto` edit area while a long document scrolls — closes Gap 11b). The base rule already supplies the opaque `background: var(--color-surface)` and `z-index: var(--z-popover)` that keep the pinned bar readable above scrolled content; `inset-block-start` is a logical property so the pin is RTL-safe (block axis is vertical in both directions, and the bar's button order still mirrors via the existing flex/logical layout).

Use logical properties and existing tokens only — no new colors, no new tokens, no new classes. Comment hygiene (locked CONVENTIONS.md §Comments): assets/app.css is a SHIPPED file — comments must be plain prose stating the constraint the code cannot show (e.g. why the floor is capped at 90dvh, why the bar is sticky), with NO planning identifiers (no "Gap 10", "46-15", "D-XX", "CR-XX").

After editing, run the Task-1 probe against the modified app.css and confirm GREEN (assertions A/B/C at both viewports + the German pass, plus the D regression guard, all pass). Then run the full jsdom suite and confirm 190/190 still green (the change is additive CSS; no test scans these selectors, but the verification_constraints require the suite to stay green before committing).
  </action>
  <acceptance_criteria>
    - `.export-card.is-editor-step` carries the dual-declared floor `min-block-size: min(640px, 90vh);` then `min-block-size: min(640px, 90dvh);` AND retains `block-size: 50dvh`; `.export-card.is-maximized.is-editor-step` and the `@media (max-width:768px)` block are BYTE-unchanged.
    - A rule scoped to `.export-card.is-editor-step .export-edit-area > .rich-toolbar` sets `flex-shrink: 0`, `position: sticky`, and `inset-block-start: 0`; no rule targeting the note-field toolbars (outside `.export-card`) is added or changed.
    - The three load-bearing zero-min declarations (`.modal-card-body` min-height:0; `.export-step.is-active` and `.export-edit-area` min-block-size:0 under `.is-editor-step`) remain in place unchanged.
    - No new CSS class, token, or color is introduced; all added properties use logical props / existing tokens.
    - `node tests/webkit/46-export-step2-layout.mjs` exits 0 — assertions A/B/C GREEN at BOTH viewports (1440x820 and 1000x700) plus the German-locale pass, and the D regression guard GREEN.
    - `node tests/run-all.js` reports the full suite green (190/190) with no regressions.
    - No planning identifiers appear in the app.css comments added by this task.
  </acceptance_criteria>
  <verify>
    <automated>node tests/webkit/46-export-step2-layout.mjs && node tests/run-all.js</automated>
  </verify>
  <done>The export Step-2 default surface is usable on a MacBook-band viewport (unclipped full-height toolbar + multi-line editor), the toolbar stays pinned when scrolling a long document to the bottom, maximize still reaches ~90dvh, the ratified 50% look survives on tall screens, the mobile takeover rule is untouched, the probe is GREEN, and the jsdom suite is 190/190.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scrolled export content -> pinned sticky toolbar | The sticky bar overlays the top of the scrolling edit area; it must not steal clicks from, or hide, the content beneath it |
| CSS sizing floor -> device viewport | A min-block-size floor could, if uncapped, force the card taller than a short (mobile landscape) viewport and break the full-screen takeover |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-15a | Tampering | sticky toolbar overlaying scrolled edit content | medium | mitigate | The pin is scoped to `.export-card.is-editor-step .export-edit-area > .rich-toolbar` and sits at inset-block-start:0 of the edit area only (never over the actions row or other steps); the base bar carries an opaque surface background so content reads cleanly behind it; no click-steal confirmed at the 46-16 human gate |
| T-46-15b | Denial of Service | min-block-size floor exceeding a short viewport | medium | mitigate | The floor is inner-capped `min(640px, 90vh/90dvh)` so it can never exceed the modal's max-height on ANY viewport (probe re-asserts A/B/C at 1000x700, the cap-binding regime); the `@media (max-width:768px)` 100dvh takeover rule is left byte-unchanged |
| T-46-15c | Spoofing | false-GREEN layout coverage (jsdom has no layout engine) | high | mitigate | The fix is proven by a Playwright-WebKit probe that is RED against current source and GREEN after — real layout, not jsdom; the closing 46-16 human gate re-confirms on real devices + RTL |
| T-46-15d | Information Disclosure | new innerHTML / raw-value surface | low | accept | CSS-only change plus a read-only measurement probe — no new DOM sink, no innerHTML, no user data path introduced |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs — one app.css edit + one ad-hoc probe resolving the PINNED local Playwright (never added to package.json, never a global install) |
</threat_model>

<verification>
- `node tests/webkit/46-export-step2-layout.mjs` is RED against current app.css (Task 1 — A/B/C fail; D is a regression guard, not RED evidence) and GREEN after the fix (Task 2), with A/B/C at BOTH viewports (1440x820 and 1000x700) plus a German-locale pass: toolbar unclipped, editor visible >= 140px, toolbar pinned after scroll-to-bottom (overflow precondition hard-asserted, never vacuous), maximize still ~90dvh.
- `node tests/run-all.js` green (190/190) after the CSS change.
- The `@media (max-width:768px)` mobile takeover and `.is-maximized.is-editor-step` rules are byte-unchanged (git diff confirms only the two intended additions).
- Real-device confirmation of gaps 10/11 (default-size usable on a MacBook, toolbar full-height + never scrolls away, maximize + mobile + RTL correct) at the 46-16 gate.
</verification>

<success_criteria>
The export Step-2 default (non-maximized) editor is usable on laptop viewports — full-height unclipped toolbar, multi-line editor — and the persistent toolbar is ALWAYS visible (never compressed, never scrolled out of view), while the ratified 50%/90% sizing, the mobile full-screen takeover, and the 7 note-field toolbars remain unchanged. A falsifiable WebKit probe proves the fix RED->GREEN and the jsdom suite stays green.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-15-SUMMARY.md` when done — record the probe's RED output against current source (stating explicitly that RED rests on A/B/C and that D is a regression guard, not RED evidence), its GREEN output after the fix (both viewports + German pass), the 640px floor with its calibration rationale (600px left ~20px of scroll-pin headroom; crossover moved 1200px -> 1280px), the exact scoped toolbar rule added, the load-bearing zero-min invariant note, and confirmation the mobile/maximize rules were untouched and the jsdom suite stayed 190/190.
</output>
</content>
</invoke>
