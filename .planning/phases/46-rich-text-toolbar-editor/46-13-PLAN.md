---
phase: 46-rich-text-toolbar-editor
plan: 13
type: execute
wave: 10
depends_on: [46-09, 46-10, 46-11, 46-12]
files_modified:
  - assets/changelog-content-en.js
  - assets/help-content-en.js
autonomous: true
gap_closure: true
requirements: [RTXT-01, RTXT-02, RTXT-03, RTXT-04, RTXT-05, RTXT-09]
must_haves:
  truths:
    - "The push carrying the gap-round code changes satisfies the docs hard-gate: an EN changelog edit AND an EN help edit accompany it"
    - "The affected help topics reflect the shipped behaviour — granular undo, the labelled preview toggle, the snippet/list Enter fix, the emotions before/after opt-out, and the clearer Heart-Wall export wording"
    - "The UNRELEASED v1.4.0 changelog entry describes the FINAL shipped behaviour as one coherent release — no separate fix entry for behaviour users never saw, and APP_VERSION stays 1.4.0"
  artifacts:
    - assets/changelog-content-en.js
    - assets/help-content-en.js
  key_links:
    - "scripts/docs-gate.js passes for the whole gap-round push (changelog demand + per-file help demand for the non-exempt code files)"
---

<objective>
The docs hard-gate for the Phase 46 gap round. This phase is heavily user-facing and ships as ONE push, so the changelog + affected help topics must land in the same pushed range as the watched code (scripts/docs-gate.js is push-scoped — earlier partial pushes would fail before the docs land, matching the repo's "docs ride with the next code change" convention).

The gap round changed non-exempt (help-demanding) shipped files: rich-toolbar.js + text-edit.js (owned by the capturing-emotions "Capturing emotions fast" / quick-paste help topic), snippets.js (the snippets topic), and export-modal.js (the review-export "Exporting one session" / "Choosing a format" topics). app.css / tokens.css / the i18n dictionaries are in the changelog-only tier (changelog demand only, no per-file help demand). New tests/*.test.js are not shipped. No new shipped asset was added, so sw.js PRECACHE is untouched.

**Version + changelog shape (verified against source — do NOT bump):** v1.4.0 is UNRELEASED — users never experienced the coarse undo or the unlabelled preview toggle, so there is nothing to "fix" in a user-visible sense; a separate 1.4.1 "Fixed" entry would document fixes to behaviour nobody saw. Moreover the suite pins this: tests/changelog-integrity.test.js hardcodes the FIRST entry's version === '1.4.0', and changelog anchors are major-minor unique, so a 1.4.1 entry would both break the first-entry assertion and collide with the existing 1.4.0 entry's "v1-4" anchor. Therefore: APP_VERSION stays 1.4.0 (assets/version.js untouched — SW delivery does not need a version bump; the cache rolls on the deploy-stamped token), and the EXISTING unreleased v1.4.0 entry is REVISED in place to describe the final shipped behaviour as one coherent release.

Purpose: keep the docs contract satisfied so the release ships.
Output: a revised v1.4.0 changelog entry and updated help topics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@HELP-MAP.md
@assets/changelog-content-en.js
@assets/help-content-en.js
@tests/changelog-integrity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise the unreleased v1.4.0 changelog entry to the final shipped behaviour</name>
  <files>assets/changelog-content-en.js</files>
  <action>
Do NOT bump APP_VERSION (assets/version.js stays 1.4.0 and is not touched by this plan) and do NOT add a new changelog entry. v1.4.0 is unreleased, the suite's changelog-integrity test pins the first entry's version to '1.4.0', and anchors are major-minor unique — a 1.4.1 entry would break both AND would document "fixes" to behaviour no user ever saw.

Instead, REVISE the existing unreleased v1.4.0 entry in assets/changelog-content-en.js (EN only — translations follow in a later locale pass) so it describes the FINAL shipped behaviour as one coherent release, in the entry's existing voice/structure:
  - Undo/redo described as it now ships: granular, natural steps (a line or an action at a time) via the toolbar buttons and Ctrl/Cmd+Z.
  - The preview toggle described with its label: shows "Preview" while editing and "Edit" while previewing.
  - The export described with the emotions before/after opt-out: a pre-selected checkbox you can turn off, remembered per therapist.
  - Heart-Wall status in exports described with the clarified wording (released vs identified-but-not-released).
  - The snippet-inside-a-list behaviour is implicitly covered by the lists copy (accepting a suggestion keeps you in the same bullet) — fold it into the existing lists/snippets sentences rather than adding a "fixed" line.
Keep the entry's date/anchor/version fields exactly as they are (version stays 1.4.0); only the descriptive copy changes.

Comment/content hygiene: this is user-facing product copy — no planning identifiers, no process words ("gap", "UAT", "phase"), just the user-facing behaviour.
  </action>
  <verify>
    <automated>node --check assets/changelog-content-en.js && node tests/run-all.js</automated>
  </verify>
  <done>The v1.4.0 entry describes the final shipped behaviour (granular undo, labelled Preview/Edit toggle, emotions opt-out, clarified Heart-Wall wording); no 1.4.1 entry exists; APP_VERSION is still 1.4.0; changelog-integrity test green; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the affected help topics + confirm the docs gate passes</name>
  <files>assets/help-content-en.js</files>
  <action>
Read HELP-MAP.md cold to confirm ownership, then update the EN help topics affected by the gap-round code changes in assets/help-content-en.js:
  - The capturing-emotions / quick-paste topic ("Capturing emotions fast") — reflect the labelled preview toggle (Preview/Edit) and the now-granular undo/redo behaviour where the topic describes the toolbar.
  - The snippets topic — note that accepting a snippet inside a list continues the same bullet (no stray new item).
  - The review-export topics ("Exporting one session" / "Choosing a format") — document that emotions before/after ratings are an opt-out (pre-selected, remembered) and that Heart-Wall status now reads as explicit wording.
Keep edits accurate and in the existing help voice; EN is the corpus of record (translations follow later). Only edit topics that genuinely changed; if any watched code file has NO help surface to update, that is fine — the single EN help edit satisfies the push-scoped help demand (the gate trusts, not verifies, which topic changed), but prefer substantive topic updates over a token edit.

Then confirm the gate is green for the whole gap-round push. Run the docs-gate the way the local hook / CI run it (node scripts/docs-gate.js) over the gap-round commit range; if it reports an unsatisfied per-file help demand for a specific watched file that legitimately has no help surface, add a `Help-Unaffected:` trailer naming that file with a real reason on the tip commit (per the CLAUDE.md contract) rather than inventing help copy. Do not use --no-verify and do not use the emergency skip.
  </action>
  <verify>
    <automated>node --check assets/help-content-en.js && node tests/run-all.js</automated>
  </verify>
  <done>The affected help topics reflect the shipped behaviour; assets/help-content-en.js parses; the docs gate is satisfied for the gap-round push (EN changelog + EN help edits present, per-file demands met or waived with a valid trailer); suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped push -> docs hard-gate | The gate blocks a user-facing push that lacks changelog/help updates; this plan supplies them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-13a | Repudiation | undocumented user-facing changes shipping | medium | mitigate | Changelog + help topics updated in the same push; docs-gate confirms |
| T-46-13b | Tampering | bypassing the gate | low | accept | No --no-verify, no emergency skip; only a valid per-file Help-Unaffected trailer where legitimately unaffected |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs — content/version edits only |
</threat_model>

<verification>
- node scripts/docs-gate.js passes for the gap-round commit range (changelog demand satisfied by the EN changelog edit; help demands satisfied by the EN help edits or a valid trailer).
- node tests/run-all.js green — including tests/changelog-integrity.test.js (first entry still 1.4.0, anchors unique).
- APP_VERSION unchanged at 1.4.0; the revised v1.4.0 entry describes the final shipped behaviour.
</verification>

<success_criteria>
The gap-round push satisfies the docs hard-gate: the unreleased v1.4.0 changelog entry is revised to the final shipped behaviour (one coherent release, no 1.4.1, no version bump), and the affected help topics are updated; suite green.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-13-SUMMARY.md` when done — record the revised changelog copy summary and which help topics were updated (or any Help-Unaffected trailer used).
</output>
