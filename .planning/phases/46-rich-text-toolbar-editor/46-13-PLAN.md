---
phase: 46-rich-text-toolbar-editor
plan: 13
type: execute
wave: 10
depends_on: [46-09, 46-10, 46-11, 46-12]
files_modified:
  - assets/changelog-content-en.js
  - assets/help-content-en.js
  - assets/version.js
autonomous: true
gap_closure: true
requirements: [RTXT-01, RTXT-02, RTXT-03, RTXT-04, RTXT-05, RTXT-09]
must_haves:
  truths:
    - "The push carrying the gap-round code changes satisfies the docs hard-gate: an EN changelog entry AND an EN help edit accompany it"
    - "The affected help topics reflect the shipped behaviour — granular undo, the labelled preview toggle, the snippet/list Enter fix, the emotions before/after opt-out, and the clearer Heart-Wall export wording"
    - "The app version is bumped for this release and the changelog names it"
  artifacts:
    - assets/changelog-content-en.js
    - assets/help-content-en.js
    - assets/version.js
  key_links:
    - "scripts/docs-gate.js passes for the whole gap-round push (changelog demand + per-file help demand for the non-exempt code files)"
---

<objective>
The docs hard-gate for the Phase 46 gap round. This phase is heavily user-facing and ships as ONE push, so the changelog + affected help topics must land in the same pushed range as the watched code (scripts/docs-gate.js is push-scoped — earlier partial pushes would fail before the docs land, matching the repo's "docs ride with the next code change" convention).

The gap round changed non-exempt (help-demanding) shipped files: rich-toolbar.js + text-edit.js (owned by the capturing-emotions "Capturing emotions fast" / quick-paste help topic), snippets.js (the snippets topic), and export-modal.js (the review-export "Exporting one session" / "Choosing a format" topics). app.css / tokens.css / version.js / the i18n dictionaries are in the changelog-only tier (changelog demand only, no per-file help demand). New tests/*.test.js are not shipped. No new shipped asset was added, so sw.js PRECACHE is untouched.

Purpose: keep the docs contract satisfied so the release ships.
Output: a changelog entry, updated help topics, and a version bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@HELP-MAP.md
@assets/changelog-content-en.js
@assets/help-content-en.js
@assets/version.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + changelog entry for the gap round</name>
  <files>assets/version.js, assets/changelog-content-en.js</files>
  <action>
1. Bump `APP_VERSION` in assets/version.js from 1.4.0 to 1.4.1 (the hand-set semver; the cache/integrity hash derives automatically). This is the release carrying the gap-round fixes.

2. Add a new EN changelog entry (assets/changelog-content-en.js) for 1.4.1 summarising the user-facing gap fixes in the app's existing changelog voice/structure — granular, natural undo/redo (undo now reverts a line or a step at a time, not everything); a clearer preview toggle that shows whether you are about to preview or edit; a fix so accepting a snippet inside a list no longer adds a stray list item; the emotions before/after ratings are now an opt-out you can turn off (remembered per therapist); and clearer Heart-Wall wording in exports (distinguishing released from identified-but-not-released). Match the existing entry format (read the newest entry first). EN only — translations follow in a later locale pass.

Comment/content hygiene: this is user-facing product copy — no planning identifiers, no process words ("gap", "UAT", "phase"), just the user-facing change.
  </action>
  <verify>
    <automated>node --check assets/version.js && node --check assets/changelog-content-en.js && node tests/run-all.js</automated>
  </verify>
  <done>APP_VERSION is 1.4.1; a 1.4.1 EN changelog entry exists in the app's changelog voice; both files parse; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the affected help topics + confirm the docs gate passes</name>
  <files>assets/help-content-en.js</files>
  <action>
Read HELP-MAP.md cold to confirm ownership, then update the EN help topics affected by the gap-round code changes in assets/help-content-en.js:
  - The capturing-emotions / quick-paste topic ("Capturing emotions fast") — reflect the labelled preview toggle (Preview/Edit) and the now-granular undo/redo behaviour where the topic describes the toolbar.
  - The snippets topic — note that accepting a snippet inside a list continues the same bullet (no stray new item).
  - The review-export topics ("Exporting one session" / "Choosing a format") — document that emotions before/after ratings are an opt-out (pre-selected, remembered) and that Heart-Wall status now reads as explicit wording.
Keep edits accurate and in the existing help voice; EN is the corpus of record (translations follow later). Only edit topics that genuinely changed; if any watched code file has NO help surface to update, that is fine — the single EN help edit satisfies the push-scoped help demand (the gate trusts, not verifies, which topic changed), but prefer substantive topic updates over a token edit.

Then confirm the gate is green for the whole gap-round push. Run the docs-gate the way the local hook / CI run it (node scripts/docs-gate.js) over the gap-round commit range; if it reports an unsatisfied per-file help demand for a specific watched file that legitimately has no help surface, add a `Help-Unaffected:` trailer naming that file with a real reason on the tip commit (per the CLAUDE.md contract) rather than inventing help copy. Do not use --no-verify and do not use the emergency skip.
  </action>
  <verify>
    <automated>node --check assets/help-content-en.js && node tests/run-all.js</automated>
  </verify>
  <done>The affected help topics reflect the shipped behaviour; assets/help-content-en.js parses; the docs gate is satisfied for the gap-round push (EN changelog + EN help edits present, per-file demands met or waived with a valid trailer); suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped push -> docs hard-gate | The gate blocks a user-facing push that lacks changelog/help updates; this plan supplies them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-13a | Repudiation | undocumented user-facing changes shipping | medium | mitigate | Changelog + help topics updated in the same push; docs-gate confirms |
| T-46-13b | Tampering | bypassing the gate | low | accept | No --no-verify, no emergency skip; only a valid per-file Help-Unaffected trailer where legitimately unaffected |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs — content/version edits only |
</threat_model>

<verification>
- node scripts/docs-gate.js passes for the gap-round commit range (changelog + help demands satisfied).
- node tests/run-all.js green.
- APP_VERSION = 1.4.1; changelog names the release.
</verification>

<success_criteria>
The gap-round push satisfies the docs hard-gate: version bumped to 1.4.1, an EN changelog entry describes the fixes, and the affected help topics are updated; suite green.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-13-SUMMARY.md` when done — record the version, the changelog entry summary, and which help topics were updated (or any Help-Unaffected trailer used).
</output>
