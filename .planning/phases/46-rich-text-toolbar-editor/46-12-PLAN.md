---
phase: 46-rich-text-toolbar-editor
plan: 12
type: execute
wave: 9
depends_on: [46-10, 46-11]
files_modified:
  - assets/export-modal.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/export-heartwall-wording.test.js
autonomous: false
gap_closure: true
requirements: [RTXT-01]
must_haves:
  truths:
    - "When a session is flagged as a Heart-Wall session but NOT released, the export reads as explanatory copy (e.g. 'identified — not yet released'), never a bare 'No' that looks like 'not a Heart-Wall session'"
    - "When the Heart-Wall WAS released, the export reads as clear explanatory copy too (not a bare 'Yes')"
    - "Both the clipboard copy and the PDF (which renders from the same markdown body) show the chosen wording, in all four locales"
    - "The add/edit form radio labels ('Yes'/'No' next to 'Heart-Wall Removed?') are UNCHANGED — only the export wording changes"
  artifacts:
    - assets/export-modal.js
    - tests/export-heartwall-wording.test.js
  key_links:
    - "Both export builders (buildSessionMarkdown copy + buildFilteredSessionMarkdown editor/PDF) emit new export-specific released/not-released keys, not the shared form-radio keys"
---

<objective>
Gap 9 — the Heart-Wall export prints a bare "No" when flagged-but-not-released (pre-existing, likely live in production). When a session is flagged as a Heart-Wall session but the wall was NOT released at the end, the exported Heart-Wall section renders just "No", which readers first take to mean "not a Heart-Wall session" — wrong; it means "identified but not released". Ben wants to choose the replacement wording himself.

Source sites (found in export-modal.js): both builders emit `## <Heart-Wall Session label>` followed by `App.t("session.form.shieldRemoved.yes")` / `.no` — the copy builder (`buildSessionMarkdown`, ~line 248) and the filtered builder that feeds the editor/PDF (`buildFilteredSessionMarkdown`, ~line 388). The PDF renders this from the same markdown body, so fixing the builders fixes the PDF too. Critical constraint: `session.form.shieldRemoved.yes/no` ("Yes"/"No") are ALSO the add/edit form radio labels (add-session.html) — they must NOT change. The fix introduces NEW export-specific keys.

Purpose: replace an ambiguous boolean with copy Ben chooses, distinguishing released vs identified-but-not-released.
Output: export-specific wording in both builders + all four locales, plus a regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/export-modal.js
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ben chooses the Heart-Wall export wording (both states, all locales)</name>
  <action>Pause for Ben's wording choice BEFORE implementing Task 2. Present the decision and options below and record the chosen EN wording for BOTH states (released / not-released) in the SUMMARY so Task 2 emits it and translates it.</action>
  <decision>The exact English wording for the two Heart-Wall export states (translations follow EN).</decision>
  <context>
    The export currently prints a bare "No" (flagged but not released) or "Yes" (released) under the "Heart-Wall Session" heading. Both are ambiguous. Choose explanatory copy for each state. The form radio labels stay "Yes"/"No" — these new strings are export-only. HE/DE/CS translations will follow the chosen EN in this same plan.
  </context>
  <options>
    <option id="option-a">
      <name>Status-forward</name>
      <pros>Released: "Released this session". Not released: "Identified — not yet released". Reads as a clinical status; unambiguous.</pros>
      <cons>Slightly longer.</cons>
    </option>
    <option id="option-b">
      <name>Present/removed</name>
      <pros>Released: "Heart-Wall removed". Not released: "Heart-Wall present — not removed this session". Mirrors the app's "removed" vocabulary.</pros>
      <cons>"present/not removed" is wordier in the not-released case.</cons>
    </option>
    <option id="option-c">
      <name>Detected/pending</name>
      <pros>Released: "Yes — released". Not released: "Detected — release pending". Compact.</pros>
      <cons>"pending" implies a future plan that may not exist.</cons>
    </option>
  </options>
  <resume-signal>Reply "option-a", "option-b", or "option-c" — or give exact wording for both the released and the not-released states.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Emit the chosen export wording in both builders + all four locales (+ regression test)</name>
  <files>assets/export-modal.js, assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, tests/export-heartwall-wording.test.js</files>
  <action>
1. Add TWO new export-specific i18n keys for the Heart-Wall export states (e.g. `session.export.heartWall.released` and `session.export.heartWall.notReleased`) carrying Ben's chosen wording. Add them to ALL FOUR locales — EN is canonical (Ben's exact choice); translate for HE/DE/CS natively (HE stored UTF-8; CS via the file's escape convention). The locale STRUCTURE parity test requires identical keys across all four. Do NOT touch `session.form.shieldRemoved.yes/no` (the form radio labels stay "Yes"/"No").

2. In `buildSessionMarkdown()` (copy path, the Heart-Wall `## section` block): replace the `shieldRemovedCopyValue === "yes" ? shieldRemoved.yes : shieldRemoved.no` value line with `... ? App.t("session.export.heartWall.released") : App.t("session.export.heartWall.notReleased")`.

3. In `buildFilteredSessionMarkdown()` (editor/PDF path, the Heart-Wall `## section` block): make the identical replacement for `shieldRemovedValue`. The PDF renders this heading + line from the markdown body, so this covers the PDF automatically — confirm there is no separate Heart-Wall rendering in pdf-export.js (there is not; the section flows through the markdown body).

4. Audit the adjacent released=yes wording: both states now use explanatory copy (not a bare "Yes"/"No"), satisfying Ben's "audit the released=yes wording while there".

5. Regression test `tests/export-heartwall-wording.test.js`: assert (a) both new keys exist in all four locales with parity; (b) both builders reference the new export keys and NOT `shieldRemoved.yes/no` for the export value line (grep-level or, using the export test hooks / a jsdom harness, that a flagged-but-not-released session renders the not-released copy — never a bare "No" — and a released session renders the released copy). Keep the full suite green.

Comment hygiene: plain-prose comments only — no planning identifiers in shipped files. No new shipped asset — sw.js untouched.
  </action>
  <verify>
    <automated>node tests/export-heartwall-wording.test.js && node tests/run-all.js</automated>
  </verify>
  <done>Flagged-but-not-released exports read as the chosen explanatory copy (no bare "No"); released exports read as the chosen released copy (no bare "Yes"); both copy and PDF paths use the new keys; the form radios are unchanged; new keys exist in all four locales with parity; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session flag -> exported clinical document | The Heart-Wall status wording is read by clients/other practitioners; ambiguity is a clinical-communication defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-12a | Repudiation / Information Disclosure | ambiguous "No" misread as "not a Heart-Wall session" | medium | mitigate | Replace the boolean with explicit released / identified-not-released copy in both export builders, all four locales |
| T-46-12b | Tampering | accidental change to shared form-radio keys | low | mitigate | New export-only keys added; shieldRemoved.yes/no left untouched (test asserts the builders use the new keys) |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs — edits to existing module + i18n + one node test |
</threat_model>

<verification>
- node tests/export-heartwall-wording.test.js green (both states, both builders, parity).
- node tests/run-all.js green including locale-structure parity.
- Real opened-PDF confirmation (flagged-but-not-released Heart-Wall reads correctly, Hebrew included) at the 46-14 gate.
</verification>

<success_criteria>
The Heart-Wall export distinguishes released from identified-but-not-released in Ben's chosen wording across copy + PDF and all four locales; the form radios are unchanged; suite green.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-12-SUMMARY.md` when done — record the chosen wording (both states) and the new i18n keys.
</output>
