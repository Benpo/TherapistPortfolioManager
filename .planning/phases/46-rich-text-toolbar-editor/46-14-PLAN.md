---
phase: 46-rich-text-toolbar-editor
plan: 14
type: execute
wave: 11
depends_on: [46-09, 46-10, 46-11, 46-12, 46-13]
files_modified: []
autonomous: false
gap_closure: true
requirements: [RTXT-01, RTXT-02, RTXT-03, RTXT-04, RTXT-05, RTXT-09]
must_haves:
  truths:
    - "Every gap fix from this round is confirmed on real devices — granular undo, the labelled preview toggle, the snippet/list Enter fix, the emotions before/after opt-out, and the Heart-Wall export wording"
    - "The 46-08 matrix items deferred at round 1 — iPhone PWA pass, real opened PDF, RTL/Hebrew, snippets re-test, lists re-test — are completed on real devices"
    - "The gaps resolved in-gate during round 1 (export Step-2 single editor, always-on export toolbar, formatting-tips accordion removed, SW precache delivery) are re-verified now that they ship together"
  artifacts: []
  key_links:
    - "This is the closing real-device pass; no gap-round code lands after it without re-running it (it re-runs the 46-08 gate plus the new fixes)"
---

<objective>
The mandatory real-device / real-PDF verification gate RE-RUN for the Phase 46 gap round. jsdom is blind to caret, selection, paste, keyboard, undo, PDF, and RTL-visual behaviour, and this repo has shipped false-GREEN jsdom PDF tests before — so the gap round is NOT done until a human drives the real surfaces. 46-08 established that no code lands after the gate without re-running it; this plan is that re-run.

It re-runs the 46-08 checklist across the target matrix (installed Safari desktop + a real iPhone PWA + Chrome + Firefox + a real opened PDF), completes the matrix items 46-08 deferred, re-verifies the four gaps resolved in-gate during round 1 (now that they ship together), and confirms the five gaps fixed this round. This is a human-verify checkpoint plan — no code.

Purpose: close Phase 46 with a clean real-device pass.
Output: a signed-off verification recording pass/fail per item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-08-PLAN.md
@.planning/phases/46-rich-text-toolbar-editor/46-UAT.md
</context>

<artifacts_this_phase_produces>
No code artifacts — this plan is the closing verification gate.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Real-browser / real-iPhone / real-PDF re-verification of the gap round</name>
  <action>Pause for a human checkpoint: drive the built surfaces on the real browser/device matrix (installed Safari desktop + iPhone PWA, Chrome, Firefox) and a real opened PDF, working through the checklist below. Do not close the phase until every item passes or failures route to a further gap-closure round.</action>
  <what-built>The Phase 46 gap-round fixes on top of the full rich-text toolbar editor: a single module-level undo/redo stack (granular, Ctrl+Z intercepted), a labelled target-state preview toggle on note fields AND the export editor, a snippet-accept-in-a-list Enter fix, a pre-selected emotions before/after opt-out in the export, and clearer Heart-Wall export wording. Ships within the (unreleased) v1.4.0 with its changelog entry revised to the final behaviour + help updates.</what-built>
  <how-to-verify>
    Run the pre-prod environment on a real installed Safari PWA (desktop) AND a real iPhone PWA, plus desktop Chrome and Firefox, and open a real exported PDF. Work through the checklist; do not close the phase until every item passes or a failure is routed back to gap-closure.

    GAP-ROUND FIXES:
    1. Undo granularity (Gap 3): type several lines in a note field, then undo — undo removes ONE line / one typing burst at a time, NOT the whole field. One toolbar action (bold, list, heading, indent) undoes in exactly one step. On desktop, the Undo button and Ctrl/Cmd+Z produce the SAME result; Ctrl/Cmd+Shift+Z redoes. On the iPhone the buttons are the only undo affordance and work. Confirm autoGrow/snippets/preview still react after undo/redo. Check on BOTH a note field and the export editor.
    2. Preview toggle label + icon (Gap 4): on a note field AND the export editor, the toggle shows eye + "Preview" while editing and switches to pencil + "Edit" while previewing (target state), in all four languages.
    3. Snippet-accept in a list (Gap 8): inside a bullet or numbered list, type a snippet trigger to open the suggestion, press Enter to accept — the snippet expands and NO new list item is created; you can keep typing in the same bullet. With no suggestion open, plain Enter still continues the list. Re-check on the iPhone.
    4. Emotions before/after opt-out (Gap 7): in the export, the emotions before/after ratings section is pre-selected (included by default). Uncheck it — the PDF omits the before/after severity block and the copied markdown omits the Issues section. Re-open the export: the choice is remembered. The default (checked) still produces the same PDF as before.
    5. Heart-Wall export wording (Gap 9): export a session flagged as Heart-Wall but NOT released — the Heart-Wall section reads as the chosen explanatory copy (e.g. "identified — not yet released"), never a bare "No". Export a released one — it reads as the chosen released copy. Confirm in the copied markdown AND the opened PDF, including Hebrew.

    DEFERRED 46-08 MATRIX (finish now):
    6. iPhone PWA pass of the core toolbar (markers, shortcuts absent on touch, auto-format, nested lists, live preview) — the items 46-08 deferred.
    7. Real opened PDF: export a session with italic Hebrew AND Latin, bold, and a nested list — italic renders per the shipped 46-02 outcome, bold + nested lists render, Hebrew RTL/bidi correct.
    8. Snippets + autogrow unchanged (RTXT-09) in an enhanced note field on a real browser.
    9. Lists re-test: the list toggle/switch semantics and auto-renumber (delete item 3, paste at end) feel right with caret + undo intact.
    10. RTL: switch to Hebrew — toolbar, heading dropdown, preview, and the export chrome mirror correctly.

    IN-GATE ROUND-1 FIXES (re-verify now they ship together):
    11. Export Step 2 is a single full-width editor (no two-pane), the export toolbar is always visible, the formatting-tips accordion is gone, and the new modules are delivered via the SW precache (no stale-cache).
  </how-to-verify>
  <resume-signal>Reply "phase verified" if all pass, or list the failures (which browser/device, which item) for a further gap-closure round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built code -> real user devices | The gate is the last line catching device-only defects (WebKit editing/undo quirks, RTL mirroring, PDF appearance) jsdom cannot see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-14a | Spoofing | false-GREEN jsdom coverage | medium | mitigate | This gate exists because jsdom cannot see undo/caret/PDF/RTL; the checklist drives the real surfaces on real devices |
| T-46-14b | Tampering | preview/export render on real WebKit | high | mitigate | Items 2, 4, 5, 7 confirm the escape-first render + export output on real Safari; no raw-value innerHTML reaches users |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No installs (verification only) |
</threat_model>

<verification>
- Human sign-off across the browser/device matrix on all 11 checklist items.
- The five gap-round fixes, the deferred 46-08 matrix, and the round-1 in-gate fixes all confirmed on real devices + a real opened PDF.
</verification>

<success_criteria>
Every Phase 46 gap-round fix and every deferred/round-1 item is confirmed on real installed Safari (desktop + iPhone PWA), Chrome, Firefox, and a real opened PDF. Any failure routes to a further gap-closure round before the phase closes.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-14-SUMMARY.md` when done — record pass/fail per checklist item per browser/device, the real-PDF result, and any remaining gaps.
</output>
