---
phase: 46-rich-text-toolbar-editor
plan: 04
type: execute
wave: 3
depends_on: [46-03]
files_modified:
  - assets/rich-toolbar.js
  - assets/app.css
autonomous: true
requirements: [RTXT-03, RTXT-04, RTXT-05]
must_haves:
  truths:
    - "Typing a list marker starts a list, Enter continues it, Enter on an empty top-level item exits, and Enter on an empty nested item outdents one level (RTXT-03, D-10)"
    - "Tab/Shift+Tab indent/outdent the caret's list line AND the indent/outdent buttons do the same; on ordinary text Tab keeps native focus-move (no keyboard trap) (RTXT-05, D-09)"
    - "Ordered lists auto-renumber so raw text always reads 1..N — delete an item and the rest renumber; caret stays stable and native undo survives (D-11)"
    - "A per-field live preview pane below the textarea re-renders the formatted result while typing, via MdRender only, and resets when leaving the field (RTXT-04, D-05/06/07)"
  artifacts:
    - assets/rich-toolbar.js
    - assets/app.css
  key_links:
    - "Enter/Tab handling is anchored on keydown (not beforeinput — iOS Safari inconsistent) and applies TextEdit.autoFormatEnter / indentLine via editInsert so undo + the input event survive"
    - "The preview pane routes 100% through window.MdRender.render into a .note-rendered pane — never raw innerHTML of the raw value (V5 escape-first control)"
    - "The renumber path uses TextEdit.renumberOrderedBlock and restores the caret via setSelectionRange (Pitfall 4)"
---

<objective>
Extend `assets/rich-toolbar.js` with the list-editing mechanics and the live preview pane — the remaining toolbar behaviors. This delivers auto-format while typing (RTXT-03: continue/exit/outdent), indent/outdent via Tab/Shift+Tab and buttons (RTXT-05, D-09), full ordered-list auto-renumber (D-11), and the per-field live preview toggle (RTXT-04, D-05/06/07). All edits apply through the tested `window.TextEdit` transforms from 46-01; the preview reuses `window.MdRender` and the Phase-45 `.note-rendered` styling (zero new render code, escape-first).

D-12 ("live like Word") is satisfied here by the combination of live TEXT mechanics in the textarea (numbering/continuation) + live STYLING in the preview pane — WYSIWYG stays locked out. Heading lines render flush-left in the preview (D-15: no CSS indent — reuse .note-rendered as-is).

Purpose: complete the toolbar's editing behaviors so 46-05/46-06 only have to MOUNT it. This edits the same file as 46-03, so it is a strictly-later wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-rich-text-toolbar-editor/46-CONTEXT.md
@.planning/phases/46-rich-text-toolbar-editor/46-RESEARCH.md
@.planning/phases/46-rich-text-toolbar-editor/46-PATTERNS.md
@.planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md
@.planning/phases/46-rich-text-toolbar-editor/46-01-SUMMARY.md
@.planning/phases/46-rich-text-toolbar-editor/46-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift/source-grounding checks):
- New `window.RichToolbar` methods (added to 46-03's module): keydown list handler, indent/outdent button handlers, a renumber wiring, `togglePreview()` / preview-pane builder + debounced render.
- New CSS: preview-pane wrapper rules (reusing `.note-rendered`), the empty-state block, and the preview-toggle active styling — added to app.css.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Auto-format, indent/outdent, and auto-renumber wiring (RTXT-03, RTXT-05, D-09/D-10/D-11)</name>
  <files>assets/rich-toolbar.js</files>
  <read_first>
    - assets/rich-toolbar.js (46-03 output — extend the same module)
    - .planning/phases/46-rich-text-toolbar-editor/46-01-SUMMARY.md (TextEdit.autoFormatEnter / indentLine / outdentLine / renumberOrderedBlock signatures + the {value,selStart,selEnd,replacement} shape)
    - .planning/phases/46-rich-text-toolbar-editor/46-RESEARCH.md (Pattern 2 keydown-anchored auto-format; Pattern 3 indent no-trap; Pitfall 2 iOS keydown-not-beforeinput; Pitfall 4 renumber caret)
    - .planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md (Interaction Contracts 2/3/4/5)
    - assets/add-session.js (header lines 33-41 autoGrow composition contract — the input event from editInsert must still reach autoGrow + snippets)
  </read_first>
  <action>
    Add a keydown handler on the mounted textareas (anchored on keydown, NOT beforeinput — iOS Safari inconsistent, RESEARCH Pitfall 2): on Enter, call TextEdit.autoFormatEnter(value, caret); if it returns a transform (list line), preventDefault the native Enter and apply it via editInsert + setSelectionRange (continue / top-level exit / nested single-level outdent per D-10); if it returns null (ordinary line), do nothing (native Enter proceeds). On Tab/Shift+Tab: if the caret's line is a list line, preventDefault and apply TextEdit.indentLine/outdentLine (2-space unit); on ordinary text do NOT preventDefault (Tab keeps native focus-move — no keyboard trap, D-09). Wire the indent/outdent toolbar BUTTONS to the same indentLine/outdentLine path (touch + a11y baseline). Wire auto-renumber (D-11): after any edit that changes an ordered-list block (list-marker insert, Enter continuation, delete, indent/outdent), call TextEdit.renumberOrderedBlock and apply it via editInsert, restoring the caret with setSelectionRange using the returned offset (Pitfall 4 — caret stability). Keep every mutation flowing through editInsert so the real input event still reaches autoGrow + snippets (RTXT-09). Do NOT indent heading lines (D-15 flush-left).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -3; grep -c "autoFormatEnter\|renumberOrderedBlock\|indentLine" assets/rich-toolbar.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "autoFormatEnter" assets/rich-toolbar.js` returns >= 1 AND `grep -c "renumberOrderedBlock" assets/rich-toolbar.js` returns >= 1.
    - `grep -c "beforeinput" assets/rich-toolbar.js` returns 0 for Enter/Tab handling (keydown-anchored per Pitfall 2) — or if present, only for a non-Enter/Tab purpose documented in a comment.
    - `grep -c "setRangeText\|\.value =" assets/rich-toolbar.js` returns 0 (edits via TextEdit only).
    - `npm test` exits 0 (the pure-transform tests from 46-01 back this wiring; no regression).
  </acceptance_criteria>
  <done>List auto-format (continue/exit/outdent), Tab/Shift+Tab + button indent/outdent (no keyboard trap on ordinary text), and ordered auto-renumber (caret-stable, undo-safe) are wired through TextEdit; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-field live preview pane (RTXT-04, D-05/06/07)</name>
  <files>assets/rich-toolbar.js, assets/app.css</files>
  <read_first>
    - assets/rich-toolbar.js (Task 1 output)
    - assets/add-session.js (lines 278-322 renderReadModeNotes — the sanctioned MdRender.render-into-.note-rendered pattern to mirror for the preview pane; the escape-first, never-raw-innerHTML rule)
    - assets/md-render.js (window.MdRender.render surface)
    - assets/app.css (line ~3597 `.note-rendered` block to REUSE; do not re-spec heading styles — D-15 flush-left)
    - .planning/phases/46-rich-text-toolbar-editor/46-UI-SPEC.md (Live preview pane spec; empty-state copy "Nothing to preview yet" / "Start typing to see the formatted result."; D-07 resets on blur)
  </read_first>
  <action>
    Add a preview-toggle behavior to the toolbar eye button: `togglePreview()` builds (once, reused) a `.note-rendered` preview pane directly BELOW the focused textarea (per-field, D-05) and re-renders it debounced (debounce value is discretion, e.g. ~120ms) on input via window.MdRender.render(textarea.value) — route 100% through MdRender (escape-first; NEVER assign raw innerHTML of the raw value — mirror add-session.js renderReadModeNotes exactly). When MdRender is unavailable, fall back to textContent (literal), never raw innerHTML. Show the empty-state copy (UI-SPEC strings) when the field is empty. Preview is on-demand and per-field; reset the toggle state when focus leaves the field (D-07 — no stickiness). The eye button shows the accent active state while preview is open (its tooltip flips to Hide preview). Add preview-pane CSS in app.css that REUSES `.note-rendered` for rendered styling and adds only the wrapper/spacing + empty-state block (existing tokens; RTL-safe logical properties for layout).
  </action>
  <verify>
    <automated>grep -c "MdRender.render" assets/rich-toolbar.js; grep -c "note-rendered" assets/rich-toolbar.js assets/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MdRender.render" assets/rich-toolbar.js` returns >= 1 (preview routes through the escape-first renderer).
    - `grep -c "innerHTML" assets/rich-toolbar.js` — every match is a `MdRender.render(...)` assignment (no raw-value innerHTML); confirm by reading each match.
    - The preview pane reuses `.note-rendered` (grep the class in rich-toolbar.js pane creation) and app.css adds preview wrapper/empty-state rules without re-specifying heading sizes.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>An on-demand, per-field, debounced live preview pane renders below the focused field through MdRender into a reused .note-rendered surface, with an empty state and blur-reset; no raw-value innerHTML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note markdown -> preview DOM | User note text is rendered to HTML in the live preview — the primary XSS surface of the phase |
| keystroke -> textarea value | Auto-format/renumber rewrite text the user did not type; must stay undoable and caret-stable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04a | Tampering / Elevation | live preview pane rendering | high | mitigate | Route 100% of preview rendering through window.MdRender.render (escape-first, XSS-safe); NO raw `innerHTML = rawValue` path — grep-verified; fall back to textContent when MdRender absent. This is the V5 output-encoding control. |
| T-46-04b | Tampering | auto-renumber rewriting untouched text | medium | mitigate | Renumber scoped to the contiguous ordered block via the tested TextEdit.renumberOrderedBlock; applied through editInsert so native undo can revert it in one step; caret restored per Pitfall 4 |
| T-46-04c | Denial of Service | debounced preview re-render on large notes | low | accept | Preview render is debounced and per-field; MdRender is already device-verified on real note sizes |
| T-46-SC | Tampering | npm/pip installs | low | accept | No package installs (module + CSS edits). Package Legitimacy Audit verdict: N/A (RESEARCH) |
</threat_model>

<verification>
- `npm test` green (the 46-01 pure-transform tests back the list wiring).
- Source assertions: MdRender.render is the only preview render path (no raw-value innerHTML); edits via TextEdit (no direct .value/setRangeText); keydown-anchored Enter/Tab.
- Live caret/undo/renumber/preview behavior is verified on real devices at the phase gate (46-08).
</verification>

<success_criteria>
The toolbar's list mechanics (auto-continue, exit, nested outdent, Tab/Shift+Tab + button indent/outdent with no keyboard trap, and caret-stable undo-safe auto-renumber) and the on-demand per-field live preview pane (MdRender-only, reusing .note-rendered, blur-reset) are complete — all through the tested TextEdit + MdRender seams. RTXT-03/04/05 behaviors exist; D-05..D-12 satisfied.
</success_criteria>

<output>
Create `.planning/phases/46-rich-text-toolbar-editor/46-04-SUMMARY.md` when done — record the final preview-pane API (togglePreview, pane-creation contract) and any renumber-trigger points so the mount plans (46-05/46-06) know the complete RichToolbar surface.
</output>
