---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 10
type: execute
wave: 5
depends_on: [37-08]
files_modified:
  - tests/37-overview-filters.test.js
  - tests/37-sessions-filters.test.js
  - tests/37-overview-sort.test.js
  - tests/_helpers/app-stub.js
autonomous: true
requirements: [FILT-04]
must_haves:
  truths:
    - "Three new falsifiable test files fail RED before implementation and each turns GREEN independently in its owning wave (37-13, 37-14, 37-15)"
    - "They pin the exact DOM/selector contract for the Session Format multi-select, the Heart-Wall toggle, and the Overview sortable headers"
    - "Session Format predicate is proven for custom types, legacy-undefined->clinic, and multi-selection union on BOTH screens"
    - "Heart-Wall predicate is proven to match isHeartShield===true regardless of shieldRemoved, and exclude false/absent"
    - "Overview header-sort <-> #clientSortSelect stay in two-way sync"
    - "A custom-label XSS-as-literal-text guard is present for the Session Format checkbox list"
  artifacts:
    - "tests/37-overview-filters.test.js (green by 37-13)"
    - "tests/37-sessions-filters.test.js (green by 37-14)"
    - "tests/37-overview-sort.test.js (green by 37-15)"
  key_links:
    - "tests drive the REAL rendered table via jsdom (mock PortfolioDB seed -> rendered rows), not an internal predicate seam"
    - "the DOM ids/attributes asserted here are the contract Plans 37-13/37-14/37-15 implement verbatim"
---

<objective>
Author the falsifiable RED behavior tests for the three genuinely-new logics of this work package — BEFORE any implementation — split into three files so each implementation plan can turn its own file GREEN independently across waves (Overview filters -> 37-13, Sessions filters -> 37-14, Overview sort -> 37-15).

The tests MUST genuinely fail now (the controls do not exist) and MUST pass unchanged once the implementation lands — never weaken them to green. They also pin the DOM/selector contract every implementation plan consumes.
Purpose: lock behavior + selectors so the two screens compose identically and the sort stays in sync.
Output: three RED test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md
@assets/overview.js
@assets/sessions.js
@assets/app.js
@tests/37-personalization.test.js
</context>

<dom_contract>
Pinned selectors the implementation plans (37-13/37-14/37-15) must match exactly:
- Overview Session Format multi-select: container #clientFormatFilter (class multi-select), pill #clientFormatFilterToggle, panel #clientFormatFilterPanel, options `input[type="checkbox"][data-format-key]`.
- Overview Heart-Wall toggle: #clientHeartWallToggle inside `label.toggle-switch` (with `span.toggle-slider`).
- Sessions Session Format multi-select: #sessionFormatFilter / #sessionFormatFilterToggle / #sessionFormatFilterPanel / same option selector.
- Sessions Heart-Wall toggle: #sessionHeartWallToggle inside `label.toggle-switch`.
- Overview sortable headers: `th.sortable[data-sort-key="name"|"sessions"|"lastSession"]` with an `aria-sort` attribute (none|ascending|descending) and a child `span.sort-arrow`. The Type (Client Type) and Actions columns are NOT sortable. #clientSortSelect stays present.
Predicate rules: resolved format key = session.sessionType || "clinic"; Session Format empty selection = no filter; Heart-Wall ON = isHeartShield===true regardless of shieldRemoved.
Pill summary contract: the summary TEXT NODE carries NO data-i18n (applyTranslations would clobber it); zero checked -> t('filter.sessionFormat.all'); N checked -> t('filter.sessionFormat.count').replace('{count}', String(N)) — applyTranslations does NO interpolation (see add-session.js:1334 for the established caller-side .replace pattern), so the rendered summary must be asserted to contain the NUMBER and to NOT contain the literal token "{count}".
Sort default directions (pinned): first click on name -> ascending; first click on sessions or lastSession -> descending (most first — matches the shipped dropdown behavior); repeat click flips.
</dom_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests/37-overview-filters.test.js (Session Format + Heart-Wall, Overview)</name>
  <files>tests/37-overview-filters.test.js, tests/_helpers/app-stub.js</files>
  <read_first>
    - tests/37-personalization.test.js (the jsdom + localStorage['portfolioSessionTypes'] {overrides,custom} seed harness + App.getSessionTypes stub shape)
    - tests/31-sessions-render-hardening.test.js (existing full-page jsdom harness booting a render module against a mock PortfolioDB and asserting rendered rows)
    - tests/_helpers/mock-portfolio-db.js and tests/_helpers/app-stub.js
    - assets/overview.js lines 174-291 (filter/sort closure) and 364-530 (renderClientRows)
    - assets/app.js lines 1258-1345 (SESSION_TYPE_ORDER, formatSessionType, getSessionTypes)
    - the dom_contract block in THIS plan
  </read_first>
  <action>
    Create tests/37-overview-filters.test.js following the repo's zero-dep self-executing convention (Node built-in assert + jsdom, process.exit(failed?1:0), no watch flags, no pipes). Boot the REAL index.html markup + real overview.js against a mock PortfolioDB (mirror tests/31-sessions-render-hardening.test.js) and seed localStorage['portfolioSessionTypes'] with one custom type so getSessionTypes() yields it.

    STUB EXTENSION (required first): tests/_helpers/app-stub.js currently has NO getSessionTypes and a non-localizing formatSessionType pass-through. Add to the stub a default getSessionTypes() that derives [{key,label,locked}] from the seeded localStorage['portfolioSessionTypes'] (5 defaults + overrides + custom — mirror the real app.js:1329-1340 shape) so the option-build assertions are NOT vacuous. Additive only: existing tests that override getSessionTypes (e.g. tests/37-personalization.test.js sets it to ()=>[]) keep working; run the full suite to prove no regression.

    Seed sessions spanning sessionType 'clinic','online','remote','proxy', the custom key (e.g. 'custom.900'), and ONE legacy session with sessionType absent; plus isHeartShield combos — one active (isHeartShield:true, shieldRemoved falsey), one released (isHeartShield:true, shieldRemoved:true), and non-heart. Spread across >=2 clients so the client-level predicate is exercised.

    Assert (all FAIL RED now):
    - #clientFormatFilter / #clientFormatFilterToggle / #clientFormatFilterPanel exist; the set of `input[type="checkbox"][data-format-key]` values equals the getSessionTypes() keys (clinic/online/remote/proxy/other + custom). Zero checked -> all clients shown; check online+remote -> only clients with >=1 session whose resolved key is in {online,remote}; the legacy-undefined session counts as clinic.
    - #clientHeartWallToggle exists inside label.toggle-switch with span.toggle-slider. OFF -> all clients; ON -> only clients with >=1 isHeartShield===true session (the released one still counts); a client whose only heart session is released still appears; a non-heart client disappears. The old #clientHeartShieldFilter dropdown is ABSENT.
    - Pill summary (per the dom_contract): with 0 checked the toggle's summary text equals the filter.sessionFormat.all value; after checking 2 boxes it CONTAINS "2" and does NOT contain the literal "{count}"; the summary text node carries no data-i18n attribute. Panel behavior: it opens on pill click, closes on Escape, and closes on a click outside the control.
    - Security (T-37-10-SEC): seed a custom type whose stored label is a literal XSS string (e.g. an img-onerror string); after the panel renders, assert the option node exposes that string via textContent and the panel contains NO injected element (rendered as literal text).
    Each assertion carries a message. Drive the real controls; read the real rendered rows. Do NOT add the controls.
  </action>
  <verify>
    <automated>! node tests/37-overview-filters.test.js  # inverted RED gate: exits 0 ONLY while the test exits non-zero (no exit-swallowing echo)</automated>
  </verify>
  <acceptance_criteria>
    - The file exits NON-zero (RED) with messages naming the missing #clientFormatFilter / #clientHeartWallToggle controls; it fails on missing DOM, not a harness crash
    - References the pinned selectors from the dom_contract block
    - Seed exercises custom types, legacy-undefined->clinic, multi-selection union, shieldRemoved-agnostic heart matching, and the XSS-as-text guard
    - Plain `node tests/...`; no pipe that swallows the exit code
  </acceptance_criteria>
  <done>Overview filter RED file exists and fails as designed on missing controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — tests/37-sessions-filters.test.js (Session Format + Heart-Wall, Sessions)</name>
  <files>tests/37-sessions-filters.test.js</files>
  <read_first>
    - tests/31-sessions-render-hardening.test.js (the sessions.js full-page jsdom harness)
    - assets/sessions.js lines 15-211 (filter closure + renderSessions + table render)
    - assets/app.js getSessionTypes/formatSessionType (as Task 1)
    - the dom_contract block in THIS plan
  </read_first>
  <action>
    Create tests/37-sessions-filters.test.js mirroring Task 1's harness but for sessions.html + sessions.js. Seed the same variety of sessions (formats incl. custom + a legacy-undefined; isHeartShield active/released/none).

    Assert (all FAIL RED now):
    - #sessionFormatFilter / #sessionFormatFilterToggle / #sessionFormatFilterPanel exist; option `input[type="checkbox"][data-format-key]` set equals getSessionTypes() keys. Zero checked -> all sessions; check clinic -> the legacy-undefined session IS included (resolves to clinic) and non-clinic sessions excluded; check two formats -> union.
    - #sessionHeartWallToggle exists inside label.toggle-switch. ON -> both the active AND the released heart sessions shown; non-heart excluded. The old #sessionTypeFilter dropdown is ABSENT.
    - Pill summary (per the dom_contract): 0 checked -> summary equals filter.sessionFormat.all value; 2 checked -> contains "2", never the literal "{count}"; no data-i18n on the summary text node. Panel opens on pill click, closes on Escape + outside-click.
    - Security (T-37-10-SEC): same XSS-as-literal-text guard on the Sessions panel.
    Drive real controls; read real rendered rows. Do NOT add the controls.
  </action>
  <verify>
    <automated>! node tests/37-sessions-filters.test.js  # inverted RED gate: exits 0 ONLY while the test exits non-zero (no exit-swallowing echo)</automated>
  </verify>
  <acceptance_criteria>
    - Exits NON-zero (RED) naming missing #sessionFormatFilter / #sessionHeartWallToggle; fails on missing DOM not a crash
    - Covers legacy-undefined->clinic (session-level), union, shieldRemoved-agnostic heart, XSS-as-text; asserts #sessionTypeFilter absent
    - Plain `node tests/...`; no exit-swallowing pipe
  </acceptance_criteria>
  <done>Sessions filter RED file exists and fails as designed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — tests/37-overview-sort.test.js (header-sort <-> dropdown two-way sync)</name>
  <files>tests/37-overview-sort.test.js</files>
  <read_first>
    - assets/overview.js lines 180-291 (applyFiltersAndSort; sortVal from #clientSortSelect; name/lastSession/sessions branches) and 344-530 (render)
    - index.html lines 158-172 (the real table: thead th name/type/sessions/lastSession/actions, tbody #clientTableBody)
    - the dom_contract block in THIS plan; DECISIONS D2b
  </read_first>
  <action>
    Create tests/37-overview-sort.test.js booting index.html + overview.js with a mock PortfolioDB seeded so clients differ in name, session count, and last-session date (distinct orderings per key). Assert two-way sync (all FAIL RED now — headers not clickable, no data-sort-key/aria-sort):
    - Clicking `th[data-sort-key="sessions"]` sets #clientSortSelect.value='sessions', sets that th aria-sort to a directional value, resets the other sortable headers' aria-sort to 'none', and reorders #clientTableBody rows by session count (assert the top row is the client with the most sessions).
    - Clicking the SAME header again flips direction (ascending<->descending) and reverses row order.
    - Setting #clientSortSelect.value='lastSession' + dispatching change updates the Last Session header aria-sort to directional, resets others to 'none', reorders rows by most-recent date — proving the dropdown drives the SAME state.
    - Clicking the Name header sorts by display name and sets #clientSortSelect.value='name'.
    - The Type and Actions columns carry NO data-sort-key.
    Read real rendered rows + real control values. Do NOT implement the headers.
  </action>
  <verify>
    <automated>! node tests/37-overview-sort.test.js  # inverted RED gate: exits 0 ONLY while the test exits non-zero (no exit-swallowing echo)</automated>
  </verify>
  <acceptance_criteria>
    - Exits NON-zero (RED); failures on the missing sortable-header contract, not a harness error
    - Covers both sync directions, direction-toggle on repeat click, and Type/Actions non-sortable
    - References th[data-sort-key], aria-sort, span.sort-arrow, #clientSortSelect
    - Plain `node tests/...`; no exit-swallowing pipe
  </acceptance_criteria>
  <done>Overview sort-sync RED file exists and fails as designed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored custom session-type label -> filter option render | user-authored text (localStorage['portfolioSessionTypes'].custom[].label) crosses into the Session Format checkbox list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-10-SEC | Tampering (XSS) | Session Format checkbox-list option label | high | mitigate | RED tests seed an img-onerror-style custom label and assert literal-textContent render with no injected element — the guard 37-13/37-14 must satisfy |
| T-37-10-INERT | Tampering | the RED suite itself | medium | mitigate | tests drive REAL rendered rows (no stubbed render), fail on missing controls not on harness error, must never be weakened to green |
</threat_model>

<verification>
- Each of the three files runs, executes assertions, and exits NON-zero (RED) — falsifiable on missing controls, not an early crash
- All three are picked up by `npm test` (tests/run-all.js globs tests/*.test.js); the suite is expected RED on these until Waves 7-8 (as with the already-executed 37 RED plans)
</verification>

<success_criteria>
- Three RED files exist, each independently green-able by its owning plan, together pinning every selector/attribute the implementation consumes
- Session Format predicate (custom + legacy->clinic + union), Heart-Wall predicate (shieldRemoved-agnostic), XSS-as-text, and sort two-way sync all covered
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-10-SUMMARY.md` when done.
</output>
