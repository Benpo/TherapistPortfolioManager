---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 05
type: execute
wave: 2
depends_on: ["37-02", "37-03"]
files_modified:
  - assets/overview.js
  - add-client.html
  - assets/add-client.js
  - assets/backup.js
  - assets/settings-snippets.js
autonomous: true
requirements: [DATE-02, DATE-06, PERS-05, PERS-06]
must_haves:
  truths:
    - "countSessionsThisMonth counts by LOCAL month boundary via DateFormat.parseLocal (overview.js:579) — the real dashboard miscount is fixed (DATE-06/D-03)"
    - "overview.js age math (:631) and averageDaysBetween (:734) route through DateFormat.parseLocal; sort tie-breaks use string compare or parseLocal (D-03)"
    - "The add-client birthdate entry is a single native <input type='date'> (value YYYY-MM-DD, no data migration) replacing the 3-dropdown picker; populate-on-edit sets .value and read-on-save reads .value; age math parses .value locally via DateFormat.parseLocal (PERS-06/D-12)"
    - "backup.js export/restore carries BOTH localStorage['portfolioDateFormat'] AND localStorage['portfolioSessionTypes'] as additive-optional scalars in the localStorage settings block (guarded restores, mirroring snippetPrefix); the three UTC filename stamps (backup.js:731/:827 and settings-snippets.js:1019) become DateFormat.todayLocalISO() to match the already-local B1/B4 stamps (PERS-05/D-09/D-03/D-17)"
    - "The session-type list round-trips through backup via the localStorage['portfolioSessionTypes'] scalar added to the settings block — NOT via IndexedDB therapistSettings (VERIFIED: backup.js:1169 drops non-whitelisted sectionKeys and :1173-1177 coerces rows to {sectionKey,customLabel,enabled}, so an IDB sessionTypes row would lose all overrides/custom on restore — 37-PATTERNS.md A2 CORRECTED). The therapistSettings whitelist is left UNTOUCHED (D-17)"
  artifacts:
    - "assets/overview.js (local-parse sweep)"
    - "add-client.html (repo ROOT) + assets/add-client.js (native birthdate input)"
    - "assets/backup.js (dateFormat + sessionTypes scalars + local filename stamps)"
    - "assets/settings-snippets.js (UTC snippet-export filename stamp → todayLocalISO)"
  key_links:
    - "add-client.js must stop calling App.initBirthDatePicker but must NOT remove the app.js function (add-session.js still calls it until Plan 08) — removal is Plan 08's job"
  prohibitions:
    - "Do not reroute wall-clock timestamps (createdAt/exportedAt) — only calendar-date parses + the two UTC filename stamps change"
    - "No backup manifest version bump (additive-optional; older backups restore fine)"
---

<objective>
Land the overview UTC-parse sweep (including the real countSessionsThisMonth month-boundary bug), swap the add-client birthdate to a native date input, and wire backup persistence for BOTH `portfolioDateFormat` and `portfolioSessionTypes` as localStorage scalars in the settings block (the session-type list is stored in localStorage, not IndexedDB — the IDB path does not round-trip through backup restore; see 37-PATTERNS.md A2 CORRECTED).

Purpose: DATE-02/DATE-06 (overview sweep + local month boundary), PERS-06 (add-client birthdate), PERS-05 (backup of both new localStorage keys). All depend on the engine (Plan 03).
Output: local-parsed overview, native add-client birthdate, backup `dateFormat` + `sessionTypes` scalars + local filename stamps (backup.js + settings-snippets.js).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/overview.js
@assets/add-client.js
@assets/backup.js
</context>

<artifacts_produced>
- `assets/overview.js` — countSessionsThisMonth/age/averageDaysBetween/sort routed through DateFormat.parseLocal
- `add-client.html` (repo ROOT, not assets/) + `assets/add-client.js` — native `<input type="date">` birthdate
- `assets/backup.js` — `dateFormat` + `sessionTypes` scalars in the localStorage settings export/restore block + `todayLocalISO()` filename stamps (backup.js:731/:827)
- `assets/settings-snippets.js` — the UTC snippet-export filename stamp (:1019) → `DateFormat.todayLocalISO()`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: overview.js local-parse sweep (month boundary, age, averageDaysBetween, sort)</name>
  <files>assets/overview.js</files>
  <read_first>
    - 37-RESEARCH.md §"The Definitive UTC-Parse Sweep" rows 2/3/4 (overview.js:579 countSessionsThisMonth, :631 age, :734 averageDaysBetween) and the OUT-OF-SCOPE list (createdAt sort tie-breaks at :387-388 must NOT be rerouted)
    - assets/overview.js countSessionsThisMonth (~:573-582), age (~:631), averageDaysBetween (~:731-736)
  </read_first>
  <action>
    Route each calendar-date parse in overview.js through window.DateFormat.parseLocal: in countSessionsThisMonth replace the UTC parse of session.date with DateFormat.parseLocal(session.date) and early-return on a null parse (fixing the local month-boundary miscount); in the age calculation replace the UTC parse of client.birthDate with DateFormat.parseLocal; in averageDaysBetween map session.date through DateFormat.parseLocal and filter out nulls. Leave the createdAt/timestamp sort tie-breaks untouched (they are legitimate wall-clock reads). Confirm the display sites that call App.formatDate need no change (the wrapper already delegates).
  </action>
  <verify>
    <automated>node tests/37-date-format.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DateFormat.parseLocal" assets/overview.js` is at least 3 (month-boundary, age, averageDaysBetween)
    - No calendar-date UTC parse of `session.date`/`client.birthDate` remains in overview.js (createdAt timestamp reads are intentionally retained)
    - `node tests/37-date-format.test.js` green; full suite green
  </acceptance_criteria>
  <done>overview.js parses calendar dates locally; the countSessionsThisMonth month-boundary bug is fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Swap add-client birthdate to a native input; local-parse the age</name>
  <files>add-client.html, assets/add-client.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §7 (the exact add-client sites: add-client.html:63-65 container+hidden pair; add-client.js:38 init call, :140-141 populate-on-edit, :218-219 read+age)
    - 37-PATTERNS.md "Native date input" (analog: `#sessionDate` at add-session.html:94 — `<input class="input" id="…" type="date" />`)
    - 37-RESEARCH.md §"The Definitive UTC-Parse Sweep" row 11 (add-client.js:219 age via DateFormat.parseLocal)
  </read_first>
  <action>
    Replace the add-client.html birthdate container div + hidden input pair with a single native `<input class="input" id="clientBirthDate" type="date">` mirroring the #sessionDate field (value YYYY-MM-DD, no migration). In add-client.js, delete the App.initBirthDatePicker('birthDatePicker','clientBirthDate') call (do NOT touch the app.js function — Plan 08 removes it once add-session also migrates). Change populate-on-edit to set the input's .value directly to editingClient.birthDate || ''. The read-on-save already reads .value — leave it. Replace the age calculation's UTC parse of birthDate with DateFormat.parseLocal(birthDate).
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "birth" ; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'id="clientBirthDate" type="date"' add-client.html` matches a single native date input (add-client.html is at the repo ROOT, not under assets/)
    - `grep -c "initBirthDatePicker" assets/add-client.js` is 0 (the caller is removed)
    - the age math in add-client.js uses `DateFormat.parseLocal`
    - the birthdate portion of `tests/37-personalization.test.js` passes; full suite green
  </acceptance_criteria>
  <done>add-client uses a native date input; its age math parses locally; no data migration.</done>
</task>

<task type="auto">
  <name>Task 3: backup.js — dateFormat + sessionTypes scalars (export+restore) + local filename stamps (backup.js + settings-snippets.js)</name>
  <files>assets/backup.js, assets/settings-snippets.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §6 (exact export scalar block backup.js:602-613; restore scalar block :1210-1216 with the guarded snippetPrefix back-compat pattern; no version bump)
    - assets/backup.js (VERIFIED): the localStorage settings export object at :602-613 (language/theme/snippetPrefix) and the settings restore block at :1209-1229 (language/theme/snippetPrefix guarded restores) — this is the block that genuinely round-trips
    - 37-PATTERNS.md "assets/backup.js (MOD)" A2 CORRECTED: the session-type list is stored in localStorage['portfolioSessionTypes'] and rides the localStorage settings block, NOT the therapistSettings loop. VERIFIED the therapistSettings restore loop (backup.js:1169 whitelist + :1173-1177 coercion) DROPS a sessionTypes row's overrides/custom — so IDB does NOT round-trip; do NOT touch the therapistSettings whitelist
    - 37-RESEARCH.md §"BORDERLINE — backup/export filenames" (B2 backup.js:731 and B3 :827 are UTC toISOString().slice(0,10) → make local via todayLocalISO(); B1/B4 already local)
    - assets/settings-snippets.js:1019 (VERIFIED): the snippet-export filename `"snippets-" + new Date().toISOString().slice(0,10)` — the same UTC-stamp bug class (D-03)
  </read_first>
  <action>
    In the localStorage settings export object (backup.js:602-613, beside language/theme/snippetPrefix), add BOTH `dateFormat: localStorage.getItem("portfolioDateFormat")` and `sessionTypes: localStorage.getItem("portfolioSessionTypes")`. In the settings restore block (backup.js:1209-1229, right after the theme restore), add two guarded restores mirroring the snippetPrefix back-compat guard: if manifest.settings.dateFormat is present, localStorage.setItem the portfolioDateFormat key; if manifest.settings.sessionTypes is present, localStorage.setItem the portfolioSessionTypes key (older backups without these fields restore cleanly). Do NOT bump the manifest version (additive-optional). Do NOT touch the therapistSettings export/restore whitelist — the session-type list rides the localStorage settings block, NOT the therapistSettings loop (the IDB path silently strips overrides/custom on restore — 37-PATTERNS.md A2 CORRECTED).

    Change the two UTC filename stamps in backup.js (:731 and :827, the toISOString().slice(0,10) forms) to window.DateFormat.todayLocalISO() so a 20:00-local export gets today's local date, matching the already-local B1/B4 stamps. Also change the settings-snippets.js:1019 snippet-export filename stamp from `new Date().toISOString().slice(0,10)` to `window.DateFormat.todayLocalISO()` (date-format.js loads before this on settings.html per Plan 03) — the same UTC-stamp bug class (FIX 5/D-03).
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "backup\|round" ; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "portfolioDateFormat" assets/backup.js` is at least 2 (export + guarded restore)
    - `grep -c "portfolioSessionTypes" assets/backup.js` is at least 2 (export + guarded restore)
    - `grep -c "todayLocalISO" assets/backup.js` is at least 2 (the two former-UTC filename stamps)
    - `grep -c "todayLocalISO" assets/settings-snippets.js` is at least 1 (the former-UTC snippet-export filename stamp) and no `toISOString().slice(` remains on the snippet-export download filename line
    - the manifest version literal in backup.js is unchanged; the therapistSettings whitelist is unchanged
    - the backup round-trip portion of `tests/37-personalization.test.js` passes; full suite green
  </acceptance_criteria>
  <done>Backup carries portfolioDateFormat + portfolioSessionTypes (both guarded localStorage scalars in the settings block) and uses local filename stamps in backup.js + settings-snippets.js; the session-type list round-trips via localStorage (not IDB) with no schema/manifest bump and the therapistSettings whitelist untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend. User-entered data (birthdate, imported backup file) crosses into the app. The backup file is user-controlled input on restore.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-05-01 | Tampering | restored portfolioDateFormat value | low | mitigate | getPreference/format validate the key against VALID_KEYS and fall back to 'auto' for any unrecognized value, so a tampered backup cannot inject an invalid format path |
| T-37-05-02 | Denial of service | malformed backup on restore | low | accept | Existing restore guards + additive-optional handling treat missing/odd fields as "leave as-is"; no new parsing surface introduced |

No package installs — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/37-date-format.test.js` green (overview local-parse proof via unit boundary assertion)
- birthdate + backup-round-trip portions of `tests/37-personalization.test.js` green
- `npm test` full suite green
</verification>

<success_criteria>
overview.js parses calendar dates locally (month boundary fixed), add-client (repo-root add-client.html) uses a native birthdate input, and backup round-trips portfolioDateFormat + portfolioSessionTypes (both localStorage scalars in the settings block) with local filename stamps in backup.js + settings-snippets.js and no schema bump (DATE-02/DATE-06 partial, PERS-05, PERS-06 add-client half).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-05-SUMMARY.md` when done
</output>
