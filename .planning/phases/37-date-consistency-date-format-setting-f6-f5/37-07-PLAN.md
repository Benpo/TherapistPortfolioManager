---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 07
type: execute
wave: 3
depends_on: ["37-02", "37-06"]
files_modified:
  - assets/settings-session-types.js
  - assets/app.js
autonomous: true
requirements: [PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "A new self-booting IIFE assets/settings-session-types.js renders the two-tier editor into the Plan 06 container: the 5 locked defaults (clinic/online/remote/proxy/other, fixed order) show a rename input + lock icon and NO delete button; custom types show a rename input + a delete button; an add-new input + 'Add type' button sit at the bottom (PERS-03/D-13/D-15)"
    - "Renames are GLOBAL — a rename writes a single language-agnostic override string per key; the session-type list persists as ONE IndexedDB therapistSettings row {sectionKey:'sessionTypes', overrides, custom} storing only deviations, not materialized defaults (PERS-04/D-16/D-17, Pitfall 3)"
    - "App.formatSessionType resolves via a new _sessionTypeCache (loaded in initCommon beside _sectionLabelCache): override label if non-empty (D-16) → i18n default → RAW String(key) fallback for unknown/deleted keys (D-18); the 3 legacy keys clinic/online/other resolve forever (D-14); App.getSessionTypes() exposes the list (mirrors App.getSnippets)"
    - "Deleting a locked default is blocked TWO ways: no delete button is rendered for locked rows AND the delete handler early-returns on a locked/default key (Pitfall 4)"
    - "All user-entered labels render via .textContent / input.value, NEVER innerHTML; icons are built via createElementNS (reuse buildInfoIconSvg + TRASH_ICON_PATHS); the editor boots directly on DOMContentLoaded and does NOT call App.initCommon (Pitfall 1)"
  artifacts:
    - "assets/settings-session-types.js (NEW)"
    - "assets/app.js — formatSessionType rework + _sessionTypeCache + App.getSessionTypes + initCommon load"
  key_links:
    - "The editor reads/writes via App.getSessionTypes + PortfolioDB.setTherapistSetting/getAllTherapistSettings; re-render on app:language + a new app:session-types-changed event (BroadcastChannel sessions-garden-settings)"
    - "The IDB therapistSettings store already exists at DB_VERSION 6 — NO schema/version bump; backup already round-trips it (Plan 05 confirmed the generic restore loop)"
  prohibitions:
    - "Never render a user label via innerHTML (stored/DOM XSS) — textContent / input.value only"
    - "Do not persist materialized defaults — store only overrides + custom additions (Pitfall 3)"
    - "Do not call App.initCommon from the new IIFE (double-mount, Pitfall 1)"
    - "No DB_VERSION bump; no backup manifest version bump"
---

<objective>
Build the F4 two-tier session-type editor and the resolver/storage behind it. A new `settings-session-types.js` IIFE populates the Plan 06 container; `App.formatSessionType` is reworked into a cache-backed resolver with global renames (D-16) and a raw-string fallback (D-18); the list persists as one IndexedDB `therapistSettings` row (D-17).

Purpose: PERS-03 (two-tier editor), PERS-04 (resolver + durable storage + legacy-key resolution), PERS-05 (session-type half of the backup round-trip). Depends on Plan 06 (container markup + i18n keys).
Output: the editor IIFE + the reworked resolver + `App.getSessionTypes`; the F4 behavior tests go green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/settings-snippets.js
@assets/settings.js
@assets/app.js
@assets/db.js
</context>

<artifacts_produced>
- `assets/settings-session-types.js` (NEW) — two-tier editor IIFE (render/add/rename/delete/persist, locked-vs-custom divergence)
- `assets/app.js` — reworked `formatSessionType` (override → i18n → raw), new `_sessionTypeCache` (loaded in initCommon), `App.getSessionTypes()`
- IndexedDB `therapistSettings` row `{ sectionKey: "sessionTypes", overrides, custom }`
- `app:session-types-changed` CustomEvent + BroadcastChannel `sessions-garden-settings` message `type:"session-types-changed"`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rework App.formatSessionType into a cache-backed resolver + App.getSessionTypes</name>
  <files>assets/app.js</files>
  <read_first>
    - 37-PATTERNS.md "assets/app.js formatSessionType (MOD)" (the _sectionLabelCache / getSectionLabel analog at app.js:39, :58-60 to mirror) and 37-RESEARCH-personalization.md §4 (the reworked formatSessionType shape: override → DEFAULT_TYPE_I18N → raw String(key); the initCommon cache load beside _sectionLabelCache; expose getSessionTypes mirroring getSnippets)
    - assets/app.js formatSessionType (~:1207-1210), the _sectionLabelCache declaration + initCommon load (~:39, :665-673), and the App public-return object
    - assets/db.js therapistSettings store (getAllTherapistSettings / setTherapistSetting; keyPath sectionKey; DB_VERSION 6) — the storage seam
  </read_first>
  <action>
    Add a module-level _sessionTypeCache (Map) and a DEFAULT_TYPE_I18N map of the 5 locked keys (clinic/online/remote/proxy/other) to their session.type.* i18n keys, in fixed order. In initCommon, load the sessionTypes therapistSettings row once (beside the _sectionLabelCache load) and materialize the cache: defaults are implicit; only overrides + custom entries come from storage. Rework formatSessionType(type) to: look up the key in the cache and return a non-empty override label (D-16); else if the key is a known default return its i18n string; else return String(key) as the raw graceful fallback (D-18). The 3 legacy keys resolve forever via the default map (D-14). Expose App.getSessionTypes() returning a shallow copy of the ordered list (5 defaults with resolved labels + custom), mirroring App.getSnippets. Keep formatSessionType's signature and export unchanged (callers at overview.js/sessions.js/export-modal.js are untouched, all render via textContent).
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "formatSessionType\|resolver\|fallback\|getSessionTypes" ; npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `App.formatSessionType('nonexistent-key')` returns the raw string `nonexistent-key` (D-18), and returns the override for a renamed default (D-16)
    - `App.getSessionTypes` is exposed on the App public surface
    - the 3 legacy keys clinic/online/other still resolve to their labels
    - the resolver portion of `tests/37-personalization.test.js` passes; full suite green
  </acceptance_criteria>
  <done>formatSessionType is a cache-backed resolver with global renames + raw fallback, and getSessionTypes is exposed.</done>
</task>

<task type="auto">
  <name>Task 2: Build assets/settings-session-types.js (two-tier editor IIFE)</name>
  <files>assets/settings-session-types.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §3 (the full snippet-editor→session-type-editor mapping table; the data model; the locked-vs-custom divergence; afterTypeMutation + BroadcastChannel + app:session-types-changed) and §5 (the single therapistSettings row shape {sectionKey:"sessionTypes", overrides, custom} storing only deviations)
    - assets/settings-snippets.js (render list ~:427, buildListRow ~:471, trash icon ~:510/:527, self-boot ~:1318-1328 — do NOT call initCommon) — the CRUD skeleton to clone
    - assets/settings.js locked-row pattern (LOCKED_RENAME ~:35, buildInfoIconSvg ~:74) — the lock-icon divergence
    - 37-UI-SPEC.md "Component & Interaction Inventory" + "Copywriting Contract" (order In-person/Online/Remote/Proxy/Other then custom; confirmDialog tone=danger for custom delete; the exact i18n keys for labels/tooltip/aria/confirm)
  </read_first>
  <action>
    Create assets/settings-session-types.js as a self-booting window-IIFE modeled on settings-snippets.js (boot directly on DOMContentLoaded — never App.initCommon). renderTypeList reads App.getSessionTypes(), clears the Plan 06 container, and appends rows in fixed order (5 defaults, then custom). buildTypeRow: for a LOCKED default render a rename input prefilled with the resolved label + a lock icon (buildInfoIconSvg) with the locked tooltip, and NO delete button; for a CUSTOM type render a rename input + a delete button (TRASH_ICON_PATHS with the delete aria). commitRename(key, newLabel) writes the override string into the therapistSettings sessionTypes row (empty/blank clears the override back to the i18n default). addCustomType(label) validates (non-blank, not a duplicate; too-long uses the add.invalid / rename.tooLong copy) and appends a {key:'custom.<ts>', label} entry. handleDeleteCustom(key) shows a confirmDialog (tone danger) then removes the custom entry — and early-returns if the key is a locked default (two-layer guard). After any mutation, refresh App's session-type cache, post the BroadcastChannel sessions-garden-settings message, dispatch app:session-types-changed, and re-render. Re-render on app:language so un-renamed defaults re-translate while overrides stay fixed (D-16). Render every user label via textContent / input.value; build icons via createElementNS — never innerHTML for user text. Persist via PortfolioDB.setTherapistSetting; store only overrides + custom (no materialized defaults). No DB_VERSION bump.
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "editor\|locked\|delete\|rename\|add\|xss\|round" ; npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "innerHTML" assets/settings-session-types.js` counting only non-comment lines (`grep -v '^\s*//' assets/settings-session-types.js | grep -c "innerHTML"`) is 0 — no user label rendered via innerHTML
    - `grep -q "DOMContentLoaded" assets/settings-session-types.js` matches and there is no `initCommon` call in the file
    - the editor rows: 5 locked defaults render a lock icon + no delete button; custom rows render a delete button (asserted by `tests/37-personalization.test.js`)
    - the F4 editor + locked-guard + XSS-as-text portions of `tests/37-personalization.test.js` pass; full suite green
  </acceptance_criteria>
  <done>The two-tier editor persists to one therapistSettings row, blocks locked-default deletion two ways, renders all labels safely, and boots without double-mounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA (localStorage + IndexedDB, no backend). The primary in-scope boundary: user-entered session-type labels + rename values crossing into the DOM and into IndexedDB, then re-rendered across the app (session cards, session list, overview, PDF, export).

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-07-SEC | Tampering (stored/DOM XSS) | session-type label + rename value rendering | medium | mitigate | All user labels render via `.textContent` / `input.value`; icons via `createElementNS`; a `grep -v '^\s*//' \| grep -c "innerHTML"` gate returns 0 for user text, and `tests/37-personalization.test.js` renders an injection payload and asserts it stays literal text (T-37-02-SEC) |
| T-37-07-01 | Tampering (privilege) | delete a locked default | low | mitigate | Two-layer guard: no delete button rendered for locked rows AND the handler early-returns on a locked key (Pitfall 4), tested both ways |
| T-37-07-02 | Denial of service | oversized/duplicate label | low | mitigate | add/rename validate length + duplicates (add.invalid / rename.tooLong copy) before persisting |

No package installs — no supply-chain (T-SC) threat. ASVS L1; block threshold high; no high-severity threats in this local-first surface.
</threat_model>

<verification>
- F4 editor + resolver + locked-guard + XSS-as-text + session-type backup round-trip portions of `tests/37-personalization.test.js` green
- non-comment `innerHTML` grep on settings-session-types.js is 0
- `npm test` full suite green
</verification>

<success_criteria>
The session-type editor is a two-tier list with global renames + safe rendering, backed by one therapistSettings row (no schema bump) and a raw-fallback resolver; locked defaults cannot be deleted; the F4 behavior tests pass (PERS-03, PERS-04, PERS-05 session-type half).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-07-SUMMARY.md` when done
</output>
