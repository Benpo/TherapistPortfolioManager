---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 07
type: execute
wave: 3
depends_on: ["37-02", "37-06"]
files_modified:
  - assets/settings-session-types.js
  - assets/app.js
autonomous: true
requirements: [PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "A new self-booting IIFE assets/settings-session-types.js renders the two-tier editor into the Plan 06 container: the 5 locked defaults (clinic/online/remote/proxy/other, fixed order) show a rename input + lock icon and NO delete button; custom types show a rename input + a delete button; an add-new input + 'Add type' button sit at the bottom (PERS-03/D-13/D-15)"
    - "Renames are GLOBAL — a rename writes a single language-agnostic override string per key; the session-type list persists as ONE localStorage key `portfolioSessionTypes` holding JSON `{ overrides: { <lockedKey>: \"<label>\", ... }, custom: [ { key, label }, ... ] }` storing only deviations, not materialized defaults (PERS-04/D-16/D-17, Pitfall 3). Storage is localStorage — NOT IndexedDB (the IDB therapistSettings path does not round-trip through backup restore; see 37-PATTERNS.md A2 CORRECTED). Read/write mirrors portfolioDateFormat (try/catch + JSON.parse with default)"
    - "App.formatSessionType reads localStorage['portfolioSessionTypes'] SYNCHRONOUSLY on each call (no IDB, no async cache, no first-paint race): override label if non-empty (D-16) → i18n default → RAW String(key) fallback for unknown/deleted keys (D-18); the 3 legacy keys clinic/online/other resolve forever (D-14); App.getSessionTypes() reads the same key synchronously and exposes the ordered list (5 defaults + custom)"
    - "Deleting a locked default is blocked TWO ways: no delete button is rendered for locked rows AND the delete handler early-returns on a locked/default key (Pitfall 4)"
    - "After any add/rename/delete the editor writes localStorage['portfolioSessionTypes'], dispatches a within-tab `app:session-types-changed` CustomEvent, and re-renders its own list from the just-written value; App.initCommon adds a native `storage`-event listener that re-dispatches `app:session-types-changed` in peer tabs so formatSessionType displays there update (FIX 2)"
    - "All user-entered labels render via .textContent / input.value, NEVER innerHTML; icons are built via createElementNS — the lock/trash SVGs are re-implemented by COPYING the createElementNS SVG-building code from settings.js / settings-snippets.js (buildInfoIconSvg + TRASH_ICON_PATHS are IIFE-private, NOT on window/App — a literal call ReferenceErrors; FIX 8); the editor boots directly on DOMContentLoaded and does NOT call App.initCommon (Pitfall 1)"
  artifacts:
    - "assets/settings-session-types.js (NEW)"
    - "assets/app.js — formatSessionType rework (synchronous localStorage read) + App.getSessionTypes + a storage-event listener in initCommon"
  key_links:
    - "The editor reads/writes via App.getSessionTypes + direct localStorage['portfolioSessionTypes'] read/write (JSON try/catch); after a mutation it dispatches app:session-types-changed within-tab and re-renders from the just-written value; it also re-renders on app:language (un-renamed defaults re-translate, overrides stay fixed)"
    - "Cross-tab: a rename in the Settings tab reaches a peer sessions.html/overview tab via the native `storage` event → App.initCommon's storage-listener re-dispatches app:session-types-changed there (the storage event does NOT fire in the writing tab — hence the explicit within-tab dispatch). No IndexedDB; backup round-trips the localStorage key via Plan 05's settings-block scalar"
  prohibitions:
    - "Never render a user label via innerHTML (stored/DOM XSS) — textContent / input.value only"
    - "Do not persist materialized defaults — store only overrides + custom additions (Pitfall 3)"
    - "Do not call App.initCommon from the new IIFE (double-mount, Pitfall 1)"
    - "Do not call buildInfoIconSvg / TRASH_ICON_PATHS — they are IIFE-private (not on window/App); copy the createElementNS SVG-building code instead (FIX 8)"
    - "No IndexedDB use for the session-type list (localStorage['portfolioSessionTypes'] only); no backup manifest version bump"
---

<objective>
Build the F4 two-tier session-type editor and the resolver/storage behind it. A new `settings-session-types.js` IIFE populates the Plan 06 container; `App.formatSessionType` is reworked into a synchronous localStorage-backed resolver with global renames (D-16) and a raw-string fallback (D-18); the list persists as one localStorage key `portfolioSessionTypes` (D-17 — localStorage, NOT IndexedDB, because the IDB path does not round-trip through backup restore; see 37-PATTERNS.md A2 CORRECTED).

Purpose: PERS-03 (two-tier editor), PERS-04 (resolver + durable storage + legacy-key resolution), PERS-05 (session-type half of the backup round-trip). Depends on Plan 06 (container markup + i18n keys).
Output: the editor IIFE + the reworked resolver + `App.getSessionTypes`; the F4 behavior tests go green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/settings-snippets.js
@assets/settings.js
@assets/app.js
</context>

<artifacts_produced>
- `assets/settings-session-types.js` (NEW) — two-tier editor IIFE (render/add/rename/delete/persist, locked-vs-custom divergence)
- `assets/app.js` — reworked `formatSessionType` (override → i18n → raw) reading localStorage synchronously, `App.getSessionTypes()`, + a native `storage`-event listener in initCommon
- `localStorage['portfolioSessionTypes']` JSON `{ overrides: { <lockedKey>: "<label>", ... }, custom: [ { key, label }, ... ] }` (replaces the IDB therapistSettings row)
- `app:session-types-changed` CustomEvent — dispatched within-tab by the editor after a mutation; re-dispatched in peer tabs by the initCommon `storage`-event listener (the native localStorage `storage` event is the cross-tab mechanism)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rework App.formatSessionType into a synchronous localStorage-backed resolver + App.getSessionTypes + cross-tab storage listener</name>
  <files>assets/app.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §4 (the reworked formatSessionType shape: override → DEFAULT_TYPE_I18N → raw String(key); expose getSessionTypes mirroring getSnippets) — but note the STORAGE is now localStorage['portfolioSessionTypes'], not IDB (FIX 1 / 37-PATTERNS.md A2 CORRECTED), read synchronously with NO cache
    - assets/settings.js portfolioLang/portfolioTheme read/write pattern + the portfolioDateFormat key (Plan 06) — the localStorage scalar pattern to mirror (try/catch + JSON.parse with a `{overrides:{},custom:[]}` default)
    - assets/app.js formatSessionType (~:1207-1210) and the App public-return object (~:1450)
    - assets/app.js initCommon (~:687-711 — the existing BroadcastChannel block) and refreshSnippetCache's `document.dispatchEvent(new CustomEvent("app:snippets-changed"))` (~:108) — the event-dispatch + listener-registration analog for the new storage listener
  </read_first>
  <action>
    Add a DEFAULT_TYPE_I18N map of the 5 locked keys (clinic/online/remote/proxy/other) to their session.type.* i18n keys, in fixed order. Add a private `_readSessionTypes()` helper that reads localStorage['portfolioSessionTypes'] behind try/catch, JSON.parse's it, and returns a normalized `{ overrides: {...}, custom: [...] }` (defaulting each half so a missing/corrupt key yields empty overrides + empty custom) — no IDB, no module-level cache, no async. Rework formatSessionType(type) to read synchronously via _readSessionTypes(): return a non-empty override label for the key (D-16); else if the key is a custom entry return its label; else if the key is a known default return its i18n string; else return String(type) as the raw graceful fallback (D-18). The 3 legacy keys resolve forever via the default map (D-14). Expose App.getSessionTypes() returning a freshly-read ordered list (5 defaults with resolved labels + custom) on each call, mirroring App.getSnippets. Keep formatSessionType's signature and export unchanged (callers at overview.js/sessions.js/export-modal.js are untouched, all render via textContent).

    In initCommon, register a native `window.addEventListener("storage", ...)` handler: when the changed key is `portfolioSessionTypes`, dispatch a `document` `app:session-types-changed` CustomEvent so peer-tab display sites re-render (the native storage event fires ONLY in other tabs, which is exactly the cross-tab case). No cache to refresh — reads are synchronous.
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "formatSessionType\|resolver\|fallback\|getSessionTypes" ; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `App.formatSessionType('nonexistent-key')` returns the raw string `nonexistent-key` (D-18), and returns the override for a renamed default (D-16), reading localStorage['portfolioSessionTypes'] synchronously (no IDB, no cache)
    - `App.getSessionTypes` is exposed on the App public surface and reflects the current localStorage value on each call
    - the 3 legacy keys clinic/online/other still resolve to their labels
    - `grep -n "addEventListener(\"storage\"" assets/app.js` (or a `'storage'` variant) shows the initCommon storage listener that dispatches `app:session-types-changed` on a `portfolioSessionTypes` change
    - the resolver portion of `tests/37-personalization.test.js` passes; full suite green
  </acceptance_criteria>
  <done>formatSessionType is a synchronous localStorage-backed resolver with global renames + raw fallback, getSessionTypes is exposed, and a cross-tab storage listener re-dispatches app:session-types-changed in peer tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Build assets/settings-session-types.js (two-tier editor IIFE)</name>
  <files>assets/settings-session-types.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §3 (the full snippet-editor→session-type-editor mapping table; the data model; the locked-vs-custom divergence; afterTypeMutation) and §5 (the data shape) — but note STORAGE is now localStorage['portfolioSessionTypes'] JSON `{overrides,custom}`, NOT an IDB therapistSettings row (FIX 1 / 37-PATTERNS.md A2 CORRECTED), and the cross-tab mechanism is the native `storage` event (not BroadcastChannel)
    - assets/settings-snippets.js (render list ~:427, buildListRow ~:471, trash icon ~:510/:527, self-boot ~:1318-1328 — do NOT call initCommon) — the CRUD skeleton to clone; the trash-icon createElementNS code (TRASH_ICON_PATHS is IIFE-private here) is the SVG code to COPY, not call
    - assets/settings.js locked-row pattern (LOCKED_RENAME ~:35, buildInfoIconSvg ~:74) — buildInfoIconSvg is IIFE-private (not on window/App); COPY its createElementNS lock-icon building code into this file, do NOT call it (FIX 8)
    - 37-UI-SPEC.md "Component & Interaction Inventory" + "Copywriting Contract" (order In-person/Online/Remote/Proxy/Other then custom; confirmDialog tone=danger for custom delete; the exact i18n keys for labels/tooltip/aria/confirm)
  </read_first>
  <action>
    Create assets/settings-session-types.js as a self-booting window-IIFE modeled on settings-snippets.js (boot directly on DOMContentLoaded — never App.initCommon). renderTypeList reads App.getSessionTypes(), clears the Plan 06 container, and appends rows in fixed order (5 defaults, then custom). buildTypeRow: for a LOCKED default render a rename input prefilled with the resolved label + a lock icon with the locked tooltip, and NO delete button; for a CUSTOM type render a rename input + a delete button with the delete aria. Build BOTH icons with createElementNS by COPYING the SVG-building code from settings.js (buildInfoIconSvg) / settings-snippets.js (trash icon) — those symbols are IIFE-private and calling them by name ReferenceErrors (FIX 8).

    Persist to localStorage['portfolioSessionTypes'] (NOT IndexedDB): read the current `{overrides,custom}` via a JSON try/catch read (default `{overrides:{},custom:[]}`), mutate, and write back the JSON — store only deviations (overrides + custom), never materialized defaults (Pitfall 3). commitRename(key, newLabel) writes the override string (empty/blank clears the override back to the i18n default). addCustomType(label) validates (non-blank, not a duplicate; too-long uses the add.invalid / rename.tooLong copy) and appends a {key:'custom.<ts>', label} entry. handleDeleteCustom(key) shows a confirmDialog (tone danger) then removes the custom entry — and early-returns if the key is a locked default (two-layer guard).

    After ANY mutation (add/rename/delete): (1) write localStorage['portfolioSessionTypes'], (2) dispatch a within-tab `app:session-types-changed` CustomEvent on document (the native `storage` event does NOT fire in the writing tab, so this explicit dispatch is what updates same-tab displays), and (3) re-render the editor list from the just-written value. Re-render on app:language so un-renamed defaults re-translate while overrides stay fixed (D-16). Render every user label via textContent / input.value; build icons via createElementNS — never innerHTML for user text.
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "editor\|locked\|delete\|rename\|add\|xss\|round\|changed" ; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "innerHTML" assets/settings-session-types.js` counting only non-comment lines (`grep -v '^\s*//' assets/settings-session-types.js | grep -c "innerHTML"`) is 0 — no user label rendered via innerHTML
    - `grep -q "DOMContentLoaded" assets/settings-session-types.js` matches and there is no `initCommon` call in the file
    - `grep -c "portfolioSessionTypes" assets/settings-session-types.js` is at least 1 (persists to localStorage, not IDB) and there is no `setTherapistSetting` / `PortfolioDB` call in the file
    - `grep -q "app:session-types-changed" assets/settings-session-types.js` matches (the within-tab dispatch after a mutation) and the editor re-renders its own list from the just-written value
    - the editor rows: 5 locked defaults render a lock icon + no delete button; custom rows render a delete button (asserted by `tests/37-personalization.test.js`)
    - the F4 editor + locked-guard + XSS-as-text portions of `tests/37-personalization.test.js` pass; full suite green
  </acceptance_criteria>
  <done>The two-tier editor persists to localStorage['portfolioSessionTypes'], dispatches app:session-types-changed within-tab and re-renders from the written value, blocks locked-default deletion two ways, renders all labels safely (icons copied via createElementNS), and boots without double-mounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA (localStorage, no backend). The primary in-scope boundary: user-entered session-type labels + rename values crossing into the DOM and into localStorage['portfolioSessionTypes'], then re-rendered across the app (session cards, session list, overview, PDF, export).

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-07-SEC | Tampering (stored/DOM XSS) | session-type label + rename value rendering | medium | mitigate | All user labels render via `.textContent` / `input.value`; icons via `createElementNS`; a `grep -v '^\s*//' \| grep -c "innerHTML"` gate returns 0 for user text, and `tests/37-personalization.test.js` renders an injection payload and asserts it stays literal text (T-37-02-SEC) |
| T-37-07-01 | Tampering (privilege) | delete a locked default | low | mitigate | Two-layer guard: no delete button rendered for locked rows AND the handler early-returns on a locked key (Pitfall 4), tested both ways |
| T-37-07-02 | Denial of service | oversized/duplicate label | low | mitigate | add/rename validate length + duplicates (add.invalid / rename.tooLong copy) before persisting |

No package installs — no supply-chain (T-SC) threat. ASVS L1; block threshold high; no high-severity threats in this local-first surface.
</threat_model>

<verification>
- F4 editor + resolver + locked-guard + XSS-as-text + session-type backup round-trip portions of `tests/37-personalization.test.js` green
- non-comment `innerHTML` grep on settings-session-types.js is 0
- `npm test` full suite green
</verification>

<success_criteria>
The session-type editor is a two-tier list with global renames + safe rendering, backed by one localStorage key portfolioSessionTypes (no IDB, no schema bump) and a synchronous raw-fallback resolver; locked defaults cannot be deleted; a within-tab app:session-types-changed dispatch + a cross-tab storage listener keep displays live; the F4 behavior tests pass (PERS-03, PERS-04, PERS-05 session-type half).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-07-SUMMARY.md` when done
</output>
