---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 08
type: execute
wave: 4
depends_on: ["37-03", "37-07"]
files_modified:
  - add-session.html
  - assets/add-session.js
  - assets/app.js
autonomous: true
requirements: [DATE-02, DATE-06, PERS-04, PERS-06]
must_haves:
  truths:
    - "The add-session UTC-parse sweep is complete: the new-session date input defaults to LOCAL today via sessionDate.value = DateFormat.todayLocalISO() (add-session.js:516, the genuine input bug, DATE-06); the three age-math sites (:473/:1027/:1428) parse birthDate via DateFormat.parseLocal; the sort tie-break (:1369) uses string compare or parseLocal (D-03)"
    - "The session-type cards render data-driven from App.getSessionTypes() (5 defaults + custom) with labels via App.formatSessionType(key) and re-resolve on app:language; the first card is the checked default; setupToggleGroup + the checked-value read + edit-repopulate stay generic and survive dynamic cards (PERS-04/D-16)"
    - "The add-session birthdate entries (inline new-client + edit-client) are native <input type='date'> mirroring #sessionDate; populate/clear set .value, read-on-save reads .value; no data migration (PERS-06/D-12)"
    - "App.initBirthDatePicker (app.js:1251-1372) and its export are removed now that no caller remains (add-client migrated in Plan 05, add-session migrated here); the legacy JSON backup filename (app.js:1033) uses DateFormat.todayLocalISO() (D-03)"
    - "The phase-wide UTC-parse sweep is COMPLETE: an agent audit finds zero remaining new Date(<calendar-date>) parse of session.date / client.birthDate / birthDate app-wide outside assets/date-format.js, while wall-clock timestamps (createdAt/exportedAt/Date.now) are untouched (DATE-02)"
  artifacts:
    - "add-session.html (data-driven session-type cards container + 2 native birthdate inputs)"
    - "assets/add-session.js (sweep + data-driven cards + birthdate .value plumbing)"
    - "assets/app.js (initBirthDatePicker removed + legacy filename local + sweep-complete)"
  key_links:
    - "Cards depend on App.getSessionTypes / App.formatSessionType (Plan 07) and re-resolve on app:language"
    - "App.initBirthDatePicker can only be removed after BOTH add-client (Plan 05) and add-session (this plan) stop calling it"
  prohibitions:
    - "Do not reroute wall-clock timestamps (createdAt sort tie-breaks at add-session.js:1372-1373, exportedAt writes) — only calendar-date parses change"
    - "Render card labels via textContent (App.formatSessionType output) — never innerHTML for user labels"
---

<objective>
Complete the add-session vertical: finish the UTC-parse sweep in add-session.js, make the session-type cards data-driven from the managed list, swap the two add-session birthdate entries to native inputs, and remove the now-orphaned `App.initBirthDatePicker`. This plan closes the phase-wide DATE-02 sweep and the add-session halves of PERS-04/PERS-06.

Purpose: DATE-02 (final sweep + app-wide zero-remaining audit), DATE-06 (local input default), PERS-04 (data-driven cards), PERS-06 (add-session birthdate). Depends on the engine (Plan 03) + the resolver/getSessionTypes (Plan 07).
Output: swept + data-driven + native-birthdate add-session; app.js birthdate picker removed; app-wide sweep verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/add-session.js
@assets/app.js
</context>

<artifacts_produced>
- `add-session.html` — data-driven `#sessionTypeGroup` cards + 2 native birthdate inputs (`#inlineClientBirthDate`, `#editClientBirthDate`)
- `assets/add-session.js` — sweep (valueAsDate/age/sort) + card render from `App.getSessionTypes()` + birthdate `.value` plumbing
- `assets/app.js` — `App.initBirthDatePicker` removed; legacy JSON backup filename local; UTC-parse sweep complete
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: add-session.js UTC-parse sweep (local input default, age, sort) + app.js legacy filename</name>
  <files>assets/add-session.js, assets/app.js</files>
  <read_first>
    - 37-RESEARCH.md §"The Definitive UTC-Parse Sweep" rows 5/6/7/8/10 (add-session.js:473/:516/:1027/:1369/:1428) and the OUT-OF-SCOPE list (add-session.js:1372-1373 createdAt sort tie-breaks + the exportedAt writes must NOT be rerouted); B5 (app.js:1033 legacy JSON backup filename → todayLocalISO)
    - assets/add-session.js valueAsDate default (~:516), age sites (~:473, :1027, :1428), sort tie-break (~:1369)
    - assets/app.js legacy backup filename (~:1033)
  </read_first>
  <action>
    Change the new-session date input default from valueAsDate = new Date() to value = window.DateFormat.todayLocalISO() (add-session.js:516) — the genuine input bug that could default to tomorrow at night in a negative-UTC zone. Replace the three age-math UTC parses of birthDate/selectedClient.birthDate (:473, :1027, :1428) with window.DateFormat.parseLocal. Change the session-date sort tie-break (:1369) to a string compare of the ISO date values (matching overview.js/sessions.js) or DateFormat.parseLocal — leaving the adjacent createdAt tie-breaks (:1372-1373) untouched. In app.js, change the legacy JSON backup filename stamp (:1033) from the UTC toISOString().slice(0,10) form to window.DateFormat.todayLocalISO(). Do not touch any exportedAt/createdAt write.
  </action>
  <verify>
    <automated>node tests/37-date-format.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "todayLocalISO" assets/add-session.js` shows the input default uses the local-today helper (no `valueAsDate = new Date()` remains for the session-date default)
    - `grep -c "DateFormat.parseLocal" assets/add-session.js` is at least 3 (the three age sites)
    - `grep -n "todayLocalISO" assets/app.js` shows the legacy backup filename is local
    - `node tests/37-date-format.test.js` green; full suite green
  </acceptance_criteria>
  <done>The add-session date input defaults to local today, age/sort parse locally, and the legacy backup filename is local.</done>
</task>

<task type="auto">
  <name>Task 2: Data-driven session-type cards from App.getSessionTypes()</name>
  <files>add-session.html, assets/add-session.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §4 (render cards from App.getSessionTypes into #sessionTypeGroup; label via App.formatSessionType(key); keep first card checked; setupToggleGroup + the checked-value read at :729/:1104 + edit-repopulate loop at :1528-1531 are generic and survive dynamic cards; re-resolve on app:language)
    - 37-PATTERNS.md "Session-type cards" (analog: existing hardcoded .toggle-group#sessionTypeGroup at add-session.html:77-91)
    - assets/add-session.js setupToggleGroup call (~:520/:1315), the checked read (~:729/:1104), the edit-repopulate loop (~:1528-1531)
  </read_first>
  <action>
    Replace the 3 hardcoded .toggle-card radios in add-session.html #sessionTypeGroup with a render driven from App.getSessionTypes() at form init: for each entry build a `<label class="toggle-card" data-type="<key>">` with a radio `name="sessionType" value="<key>"` and a `<span>` whose textContent is App.formatSessionType(key); mark the first card active/checked as the default. Keep the wiring generic — do not change setupToggleGroup, the checked-value read, or the edit-repopulate loop (they operate on any .toggle-card set). On app:language, re-render the cards so default labels re-translate while overrides stay fixed. Render labels via textContent only. Do not add helper/"optional" hints under the cards.
  </action>
  <verify>
    <automated>node tests/37-personalization.test.js 2>&1 | grep -i "card\|sessionType" ; npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - the session-type cards are built from `App.getSessionTypes()` (the 3 hardcoded radios are gone; a custom type added in the editor appears as a card)
    - card labels render via `textContent` (App.formatSessionType output) — non-comment `innerHTML` count on the card render path is 0
    - selecting a card and saving stores the correct stored key; edit-repopulate re-checks the saved card
    - the card portion of `tests/37-personalization.test.js` passes; full suite green
  </acceptance_criteria>
  <done>Session-type cards render data-driven from the managed list, re-resolve on language change, and keep the generic selection/edit wiring intact.</done>
</task>

<task type="auto">
  <name>Task 3: Swap add-session birthdate inputs to native + remove App.initBirthDatePicker + verify app-wide sweep</name>
  <files>add-session.html, assets/add-session.js, assets/app.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §7 (the add-session sites: add-session.html:150-152 inline + :530-532 edit container+hidden pairs; add-session.js:148-149 init calls, :344-347 populate, :267-268 clear; App.initBirthDatePicker at app.js:1251-1372 + its export at ~:1438)
    - 37-PATTERNS.md "Birthdate <input type=date>" (analog #sessionDate at add-session.html:94) and "Native date input"
    - 37-RESEARCH.md §"The Definitive UTC-Parse Sweep" — the verification gate (grep for new Date( of a variable holding .date/.birthDate must return zero outside date-format.js)
  </read_first>
  <action>
    Replace the two add-session birthdate container+hidden pairs (inline new-client at :150-152, edit-client at :530-532) with single native `<input class="input" type="date">` elements (ids inlineClientBirthDate / editClientBirthDate) mirroring #sessionDate. Delete the two App.initBirthDatePicker calls (add-session.js:148-149). Change populate-on-edit (:344-347) and clear (:267-268) to set the input .value directly (YYYY-MM-DD or ''). Reads at :472/:1026 already read .value — leave them. Once no caller remains, remove App.initBirthDatePicker (app.js:1251-1372) and its export entry.

    Then run the phase-wide UTC-parse sweep audit: confirm there is no remaining calendar-date UTC parse (a new Date(...) of a variable holding session.date / client.birthDate / birthDate) anywhere outside assets/date-format.js, while wall-clock timestamp reads/writes (createdAt/exportedAt/Date.now) are intact. This closes DATE-02.
  </action>
  <verify>
    <automated>grep -rn "initBirthDatePicker" assets/ | grep -v "date-format.js" | wc -l | grep -qx 0 && node tests/37-date-format.test.js && node tests/37-personalization.test.js 2>&1 | grep -i "birth" ; npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "initBirthDatePicker" assets/` returns 0 matches (function + all callers removed)
    - `grep -n 'type="date"' add-session.html` shows the two new native birthdate inputs (plus the existing #sessionDate)
    - The sweep audit finds zero remaining `new Date(<calendar-date>)` parse of session.date / client.birthDate / birthDate outside `assets/date-format.js` (agent audit; wall-clock timestamps retained)
    - the birthdate portion of `tests/37-personalization.test.js` passes; `npm test` full suite green
  </acceptance_criteria>
  <done>add-session uses native birthdate inputs, App.initBirthDatePicker is gone, and the app-wide UTC-parse sweep is verified complete (DATE-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend. User-entered session-type selection + birthdate cross into the app; the session-type card labels are managed-list output (may include user renames) re-rendered here.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-08-SEC | Tampering (stored/DOM XSS) | session-type card label rendering | medium | mitigate | Card `<span>` labels render via `textContent` (App.formatSessionType output); no user label is injected via innerHTML; covered by the XSS-as-text guard in `tests/37-personalization.test.js` |
| T-37-08-01 | Tampering | native birthdate `.value` | low | accept | Native `<input type="date">` yields a validated YYYY-MM-DD string; age math parses it via DateFormat.parseLocal which returns null on a bad value |

No package installs — no supply-chain (T-SC) threat. ASVS L1; block threshold high; no high-severity threats.
</threat_model>

<verification>
- `node tests/37-date-format.test.js` green (local input default + parse proofs)
- card + birthdate portions of `tests/37-personalization.test.js` green
- `grep -rn "initBirthDatePicker" assets/` returns 0
- app-wide UTC-parse sweep audit: zero remaining calendar-date UTC parse outside date-format.js
- `npm test` full suite green
</verification>

<success_criteria>
add-session is fully swept (local input default, local age/sort), its session-type cards are data-driven from the managed list, its birthdate entries are native inputs, App.initBirthDatePicker is removed, and the phase-wide UTC-parse sweep is verified complete (DATE-02, DATE-06, PERS-04 cards, PERS-06 add-session half).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-08-SUMMARY.md` when done
</output>