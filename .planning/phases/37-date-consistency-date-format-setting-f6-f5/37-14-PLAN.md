---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 14
type: execute
wave: 7
depends_on: [37-10, 37-11]
files_modified:
  - assets/sessions.js
  - sessions.html
autonomous: true
requirements: [FILT-01, FILT-02]
must_haves:
  truths:
    - "Sessions has a Session Format multi-select (pill 'All formats' opens a checkbox list) that filters SESSIONS by resolved format"
    - "The checkbox options are the getSessionTypes() list (5 defaults + custom, dynamic); legacy sessions with no sessionType count as clinic; zero checked = no filter"
    - "The old #sessionTypeFilter dropdown is gone; a Heart-Wall .toggle-switch replaces it and shows sessions where isHeartShield===true regardless of shieldRemoved"
    - "All new DOM is built via DOM APIs + textContent (custom labels never innerHTML)"
  artifacts:
    - "sessions.html: #sessionFormatFilter multi-select + #sessionHeartWallToggle (replacing #sessionTypeFilter)"
    - "sessions.js: format multi-select build + predicate + Heart-Wall toggle predicate, wired into renderSessions"
  key_links:
    - "uses the i18n keys (filter.sessionFormat*, sessions.filter.type repurposed) + CSS classes from 37-11"
    - "must satisfy the pinned selectors from tests/37-sessions-filters.test.js (37-10) — that file turns GREEN here"
---

<objective>
Replace the Sessions filter bar's mislabeled heart dropdown (#sessionTypeFilter, which actually filtered Heart-Wall status) with the two decided controls (DECISIONS D2/D2a): a Session Format multi-select that filters SESSIONS by resolved format, and a Heart-Wall toggle that shows sessions where the Heart-Wall was handled (isHeartShield===true, released or not).

This turns the 37-10 RED Sessions filter file GREEN. Reuse the i18n keys + CSS classes 37-11 front-loaded; touch NO i18n bundle and NO app.css here. Build the same checkbox-dropdown DOM structure + classes as Overview (37-13) so both screens render identically.
Purpose: disambiguated, useful filters on the Sessions list.
Output: sessions.js + sessions.html changes only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-TERMINOLOGY-FILTERS-DECISIONS.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@assets/sessions.js
@sessions.html
@tests/37-sessions-filters.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session Format multi-select filter on Sessions (build + predicate)</name>
  <files>sessions.html, assets/sessions.js</files>
  <read_first>
    - tests/37-sessions-filters.test.js (pinned selectors + expected behavior for #sessionFormatFilter and the union/legacy/custom cases)
    - sessions.html lines 52-74 (the filter-bar markup; the Client / date-range .filter-field siblings)
    - assets/sessions.js lines 15-78 (filter closure + renderSessions predicate) and 101-211 (row render + filter wiring)
    - assets/app.js lines 1324-1345 (App.getSessionTypes) + formatSessionType
    - 37-11 artifacts: CSS classes .multi-select/.multi-select-toggle/.multi-select-panel/.multi-select-option and i18n keys filter.sessionFormat / .all / .count
    - 37-13's Overview build (compose the SAME checkbox-dropdown DOM structure + classes so both screens match)
  </read_first>
  <action>
    In sessions.html, add a new .filter-field to the filter bar containing the Session Format multi-select: container #sessionFormatFilter.multi-select with pill button #sessionFormatFilterToggle (label via data-i18n filter.sessionFormat, summary via filter.sessionFormat.all) and panel #sessionFormatFilterPanel (.is-hidden). Do not hand-author option rows.

    In sessions.js, build the checkbox options at load (and refresh on app:language) from App.getSessionTypes(): one .multi-select-option per {key,label} with input[type=checkbox][data-format-key=key] and a span whose textContent is the resolved label (NEVER innerHTML). Open/close the panel on the pill; close on outside-click + Escape; update the pill summary (all vs count). Maintain a selected-set.

    Add the predicate to renderSessions: when the selected-set is non-empty, a session passes only if its resolved format key (session.sessionType || "clinic") is in the set. Empty set = no format filtering. Wire the control into the existing change-listener path. Keep the row render (including the modality Type column) unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');var h=fs.readFileSync('sessions.html','utf8');var s=fs.readFileSync('assets/sessions.js','utf8');if(h.indexOf('sessionFormatFilter')===-1){console.error('missing #sessionFormatFilter in sessions.html');process.exit(1);}if(s.indexOf('getSessionTypes')===-1||s.indexOf('data-format-key')===-1){console.error('sessions.js must build options from getSessionTypes via data-format-key');process.exit(1);}console.log('OK: Sessions Session Format control wired')"</automated>
  </verify>
  <acceptance_criteria>
    - sessions.html has #sessionFormatFilter (.multi-select) with #sessionFormatFilterToggle + #sessionFormatFilterPanel; no hand-authored option rows
    - sessions.js builds one input[type=checkbox][data-format-key] per getSessionTypes() entry, label via textContent only; a custom HTML label renders as literal text
    - Predicate: check clinic -> legacy-undefined sessions included; union across two checked formats; zero checked = all sessions
    - After Task 2, the Session Format cases in tests/37-sessions-filters.test.js pass
    - No edit to any i18n bundle or app.css; the modality Type column render unchanged
  </acceptance_criteria>
  <done>Sessions Session Format multi-select filters sessions by resolved format, custom + legacy handled, XSS-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heart-Wall toggle replaces the old #sessionTypeFilter dropdown</name>
  <files>sessions.html, assets/sessions.js</files>
  <read_first>
    - tests/37-sessions-filters.test.js (#sessionHeartWallToggle contract + shieldRemoved-agnostic cases)
    - sessions.html lines 67-74 (the #sessionTypeFilter select to REMOVE)
    - assets/sessions.js lines 21, 70, 75-76, 200-202 (the typeFilter lookup, selectedType read, the heartShield/regular predicate, and the change wiring to REMOVE/replace) and 137-151 (the Heart-Wall badge cell — unchanged)
    - assets/app.css lines 1951-1996 (.toggle-switch/.toggle-slider reused) + the 37-11 .filter-toggle class
  </read_first>
  <action>
    In sessions.html, remove the #sessionTypeFilter select and its .filter-field, and add a .filter-field.filter-toggle with a label (data-i18n sessions.filter.type — repurposed by 37-11 to read "Heart-Wall") and a label.toggle-switch wrapping input[type=checkbox]#sessionHeartWallToggle + span.toggle-slider (mirror the settings.js toggle build). Unchecked by default.

    In sessions.js, delete the typeFilter lookup, the selectedType read, and the "heartShield"/"regular" predicate branch; add a #sessionHeartWallToggle lookup and, when checked, keep only sessions where isHeartShield===true (regardless of shieldRemoved — D2a); unchecked = no heart filtering. Wire the toggle's change event into renderSessions. Leave the Heart-Wall badge cell rendering unchanged.
  </action>
  <verify>
    <automated>node tests/37-sessions-filters.test.js; echo "exit=$? (expect 0 GREEN after both tasks)"</automated>
  </verify>
  <acceptance_criteria>
    - sessions.html no longer contains #sessionTypeFilter; it contains #sessionHeartWallToggle inside label.toggle-switch (+ span.toggle-slider) in a .filter-toggle field
    - sessions.js has no remaining typeFilter/selectedType reference; the toggle keeps isHeartShield===true sessions regardless of shieldRemoved
    - Toggle ON shows both the active AND the released heart sessions; non-heart excluded
    - `node tests/37-sessions-filters.test.js` exits 0 (both Session Format and Heart-Wall Sessions cases GREEN)
    - No edit to any i18n bundle or app.css
  </acceptance_criteria>
  <done>The Sessions heart dropdown is gone; a Heart-Wall toggle filters shieldRemoved-agnostically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored custom session-type label -> Session Format option render | user-authored text crosses into the checkbox list DOM |
| stored session records -> filter predicate | untrusted session.sessionType / isHeartShield values drive session visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-14-SEC | Tampering (XSS) | #sessionFormatFilterPanel option label | high | mitigate | option labels via textContent only; icons via createElementNS; guarded by the 37-10 XSS-as-literal-text test |
| T-37-14-01 | Tampering | filter predicate for legacy/custom keys | medium | mitigate | resolved key = sessionType || 'clinic'; options from getSessionTypes(); covered by union/legacy cases in tests/37-sessions-filters.test.js |
| T-37-14-SC | Tampering | npm/pip/cargo installs | low | mitigate | no package installs; hooks on; never --no-verify |
</threat_model>

<verification>
- `node tests/37-sessions-filters.test.js` exits 0 (Sessions Session Format + Heart-Wall cases GREEN); the Overview files are green-able independently by 37-13/37-15
- No i18n bundle or app.css modified; #sessionTypeFilter fully removed; the modality Type column + Heart-Wall badge cell unchanged
</verification>

<success_criteria>
- Sessions shows a Session Format multi-select + a Heart-Wall toggle, both wired into filtering
- tests/37-sessions-filters.test.js is GREEN; XSS-safe; no i18n/CSS churn here; both screens render the checkbox-dropdown identically
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-14-SUMMARY.md` when done.
</output>
