---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 06
type: execute
wave: 2
depends_on: ["37-02", "37-03"]
files_modified:
  - settings.html
  - assets/settings.js
  - sw.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [PERS-01, PERS-02, PERS-07]
must_haves:
  truths:
    - "A 5th 'Personalization' Settings tab (button + panel) is added and `?tab=personalize` deep-links via a single new whitelist token at settings.js:729; invalid ?tab= still falls back to the first tab (PERS-01/D-10/D-11)"
    - "The Personalization panel hosts the F5 date-format <select> FIRST (6 options carrying the exact seam keys auto/month-day-year/day-month-year/mm/dd/yyyy/dd/mm/yyyy/yyyy-mm-dd) and, below it, the empty F4 session-type editor container (list + add-input) that Plan 07's IIFE populates (D-10)"
    - "The 5 non-auto option LABELS are filled at wire-time by calling window.DateFormat on a fixed reference date — never hand-typed (SEAM); only the auto option uses a static i18n label"
    - "On change the picker persists localStorage['portfolioDateFormat'], dispatches an app:dateformat CustomEvent, and shows the saved toast — live re-render, NEVER a page reload (PERS-02/D-09); default reads 'auto' when unset"
    - "All new i18n keys are added across en/he/de/cs (PERS-07/D-11/D-13): settings.tab.personalize, settings.dateFormat.{label,auto,savedToast}, settings.sessionTypes.{heading,helper,add.label,add.placeholder,add.invalid,locked.tooltip,rename.aria,delete.aria,savedToast,empty,confirm.delete.title,confirm.delete.body,confirm.delete.confirm}, session.type.remote, session.type.proxy"
    - "Plan 07's F4 editor script is WIRED into the page here: a <script src=./assets/settings-session-types.js> tag is inserted into settings.html immediately AFTER the existing settings.js tag, and /assets/settings-session-types.js is added to sw.js PRECACHE_URLS — without this the F4 editor IIFE never loads and the editor is silently inert (PERS-03 enabler/D-15)"
  artifacts:
    - "settings.html (Personalization tab button + panel with F5 picker + empty F4 container + the settings-session-types.js script tag)"
    - "assets/settings.js (whitelist token + F5 picker persistence/label-fill/event)"
    - "sw.js (PRECACHE_URLS entry for /assets/settings-session-types.js)"
    - "4 i18n bundles (all new keys)"
  key_links:
    - "The picker calls window.DateFormat to render option labels — date-format.js loads before settings.js via the Plan 03 script tag on settings.html"
    - "assets/settings-session-types.js (created by Plan 07) loads only because the <script> tag is added here after settings.js; the file does not yet exist at Plan 06 execution time — the tag is a forward-declared load hook, precached so it is offline-available"
    - "he/de/cs translations are [ASSUMED — pending Sapir/Ben native review]; ship the keys, flag them in the SUMMARY"
  prohibitions:
    - "No page reload on format change — dispatch app:dateformat and let display sites re-render (Pitfall 2)"
    - "No hand-typed date example strings in the option labels (must come from the engine)"
    - "No 'optional' hints under the picker (MEMORY feedback-optional-fields-no-hints)"
---

<objective>
Add the Personalization Settings tab and the F5 date-format picker, and front-load every new i18n key (F5 + F4) across all 4 languages. The F4 editor container is added here (empty); Plan 07's IIFE populates it. Owning all of settings.html + settings.js + i18n in one plan keeps Plan 07 free of those shared files.

Purpose: PERS-01 (tab), PERS-02 (picker persistence + re-render event), PERS-07 (all i18n keys). Depends on the engine (Plan 03) for the option-label rendering.
Output: Personalization tab + panel, F5 `<select>` with engine-sourced labels + persistence + `app:dateformat` event, empty F4 container, the `settings-session-types.js` script tag + `sw.js` precache entry that load Plan 07's F4 editor, and the complete i18n key set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH-personalization.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-UI-SPEC.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/settings.js
@settings.html
</context>

<artifacts_produced>
- `settings.html` — `settingsTabPersonalizeBtn` tab button + `settingsTabPersonalize` panel containing the F5 `#dateFormatSelect` and the empty F4 container (`#sessionTypeList` + add-input), plus a `<script src="./assets/settings-session-types.js">` tag (added right after the `settings.js` tag) that loads Plan 07's F4 editor IIFE
- `assets/settings.js` — `personalize` whitelist token + F5 picker JS (persist, engine-sourced labels, `app:dateformat` dispatch, saved toast)
- `sw.js` — `/assets/settings-session-types.js` added to PRECACHE_URLS (offline availability of the F4 editor script)
- `assets/i18n-en.js` / `i18n-he.js` / `i18n-de.js` / `i18n-cs.js` — all new keys
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Personalization tab + F5 picker markup, whitelist, picker JS, and F4-editor script wiring</name>
  <files>settings.html, assets/settings.js, sw.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §1 (exact tab button + panel markup; whitelist add at settings.js:729; the IIFE reads tabs dynamically), §2 (the F5 <select> markup with data-df-example options + the persistence/label-fill/event JS + the app:dateformat propagation), §3 intro (the empty F4 container to place below the picker)
    - 37-UI-SPEC.md "Component & Interaction Inventory" (the picker is the panel focal point, first in reading order; auto-saves on change, no Save button; the F4 editor is the secondary block below)
    - assets/settings.js readUrlTab (lines 724-732 — add `|| t === "personalize"` to the whitelist) and the tab-nav IIFE (721-799)
    - settings.html tablist (~:58-63) and the sibling tabpanel divs (~:65/118/165/209)
    - settings.html script block (~:341-344 — the sibling settings-snippets.js/settings-photos.js/settings.js tags; insert the settings-session-types.js tag immediately AFTER the `./assets/settings.js` tag)
    - sw.js PRECACHE_URLS (~:75-77 — the `/assets/settings.js` / `/assets/settings-snippets.js` / `/assets/settings-photos.js` entries; add `/assets/settings-session-types.js` beside them)
    - 37-03-PLAN.md Task 2 (the exact analog: how date-format.js is added as a page `<script>` tag AND to sw.js PRECACHE_URLS — mirror that wiring here for settings-session-types.js)
  </read_first>
  <action>
    In settings.html, add a 5th `<button role="tab" id="settingsTabPersonalizeBtn" class="settings-tab" aria-selected="false" aria-controls="settingsTabPersonalize" data-tab="personalize" tabindex="-1" data-i18n="settings.tab.personalize">` to the tablist (insert right after the fields tab), and add a matching `<div id="settingsTabPersonalize" role="tabpanel" class="settings-tabpanel" aria-labelledby="settingsTabPersonalizeBtn" hidden>` panel. Inside the panel, first a `.form-field` with a labelled `<select id="dateFormatSelect" class="input select-field">` carrying 6 options whose `value` attributes are the exact seam keys; the auto option uses `data-i18n="settings.dateFormat.auto"`, the other 5 carry a `data-df-example` marker (label filled by JS). Below the picker, an `<h3>` session-types heading + helper + an empty container for the F4 editor (a list element + an add-new input + an "Add type" button) that Plan 07's IIFE will populate — leave it empty of rows here.

    In settings.js: add `|| t === "personalize"` to the readUrlTab whitelist. Add F5 picker JS (self-booting within the settings page): on init set the select value from localStorage['portfolioDateFormat'] || 'auto'; fill each data-df-example option's textContent by calling window.DateFormat.format on a fixed reference date ('2026-07-02') with the option's key and the current UI language; on `change` persist the value to localStorage, dispatch a new CustomEvent('app:dateformat', {detail:{format}}) on document, and show the saved toast via App.showToast with the settings.dateFormat.savedToast key. Re-fill the example labels on `app:language` so they re-localize. Never reload the page.

    Wire Plan 07's F4 editor script into the page (without this the F4 editor IIFE never loads and the editor is silently inert): insert `<script src="./assets/settings-session-types.js"></script>` into settings.html immediately AFTER the existing `./assets/settings.js` tag (mirroring the sibling settings-snippets.js/settings-photos.js ordering — matching Plan 03 Task 2's date-format.js wiring analog). The file itself is created by Plan 07; the tag is a forward-declared load hook added here. Also add `/assets/settings-session-types.js` to sw.js PRECACHE_URLS alongside the existing `/assets/settings.js` / `/assets/settings-snippets.js` entries so the editor script is offline-available.
  </action>
  <verify>
    <automated>grep -q 'settings-session-types.js' settings.html && grep -q 'settings-session-types.js' sw.js && node tests/30-settings-tabnav.test.js && node tests/37-personalization.test.js 2>&1 | grep -i "picker\|dateformat\|tab"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tab="personalize"' settings.html` is 1 and a matching `#settingsTabPersonalize` panel exists
    - `grep -n 'personalize' assets/settings.js` shows the whitelist token
    - `grep -n 'app:dateformat' assets/settings.js` shows the CustomEvent dispatch (no location.reload in the picker path)
    - the 6 `<option value>` are exactly auto/month-day-year/day-month-year/mm/dd/yyyy/dd/mm/yyyy/yyyy-mm-dd
    - `grep -q 'settings-session-types.js' settings.html` matches (the F4-editor `<script>` tag is added after the settings.js tag)
    - `grep -q 'settings-session-types.js' sw.js` matches (the F4-editor script is in PRECACHE_URLS)
    - `node tests/30-settings-tabnav.test.js` green; the picker + tab portions of `tests/37-personalization.test.js` green
  </acceptance_criteria>
  <done>The Personalization tab + F5 picker exist; the picker sources labels from the engine, persists on change, fires app:dateformat, and never reloads; and the settings-session-types.js script is wired into settings.html + sw.js PRECACHE so Plan 07's F4 editor actually loads.</done>
</task>

<task type="auto">
  <name>Task 2: Add all new i18n keys across en/he/de/cs</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - 37-RESEARCH-personalization.md §8 (the full key list + the English proposed values table; the note that chrome strings are i18n but date example strings are engine output)
    - 37-UI-SPEC.md "Copywriting Contract" (the canonical EN copy for each key, incl. the confirm-dialog title/body/confirm and the empty/error states; reuse the shared confirm.cancel key — do NOT add a new cancel)
    - assets/i18n-en.js settings.* family (~:418-457) and session.type.* (~:272-274) to match placement + existing key style
  </read_first>
  <action>
    Add the full new key set to all 4 bundles following the existing settings.* / session.type.* placement and style: settings.tab.personalize; settings.dateFormat.label / .auto / .savedToast; settings.sessionTypes.heading / .helper / .add.label / .add.placeholder / .add.invalid / .locked.tooltip / .rename.aria / .delete.aria / .savedToast / .empty / .confirm.delete.title / .confirm.delete.body / .confirm.delete.confirm; session.type.remote; session.type.proxy. Use the exact English copy from the UI-SPEC Copywriting Contract. Reuse the shared confirm.cancel key for the delete dialog's cancel (do not add a new one). Provide he/de/cs translations; where a native-quality translation is uncertain, ship a best-effort value and mark it [ASSUMED] in the SUMMARY for Sapir/Ben review. Do not add any "optional" helper hints.
  </action>
  <verify>
    <automated>for F in en he de cs; do grep -q "settings.tab.personalize" assets/i18n-$F.js && grep -q "session.type.remote" assets/i18n-$F.js && grep -q "session.type.proxy" assets/i18n-$F.js || { echo "MISSING keys in i18n-$F.js"; exit 1; }; done; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 i18n bundles contains `settings.tab.personalize`, `settings.dateFormat.label`, `settings.sessionTypes.heading`, `settings.sessionTypes.confirm.delete.title`, `session.type.remote`, and `session.type.proxy`
    - The delete dialog reuses the shared `confirm.cancel` key (no new cancel key added)
    - No `(optional)` helper strings are added
    - `npm test` full suite stays green
  </acceptance_criteria>
  <done>All new F5 + F4 i18n keys exist across en/he/de/cs using the UI-SPEC canonical copy; uncertain he/de/cs values are flagged [ASSUMED] in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend. The picker writes a scalar preference; option labels are engine output, not user free text. No user-entered free text is rendered in this plan (the F4 container is empty here).

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-06-01 | Tampering | persisted portfolioDateFormat value | low | mitigate | The stored value is only ever consumed by window.DateFormat, which validates against VALID_KEYS and falls back to 'auto' — an out-of-range value cannot alter control flow |
| T-37-06-02 | Spoofing (bidi) | i18n chrome strings incl. Hebrew | low | accept | Chrome strings are plain text handled by the existing settings tab i18n path; no numeric-date LTR wrap needed here (that is the engine's concern) |

No package installs — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/30-settings-tabnav.test.js` green (Personalization deep-link + count guard)
- picker + tab portions of `tests/37-personalization.test.js` green
- `grep -q 'settings-session-types.js' settings.html` AND `grep -q 'settings-session-types.js' sw.js` both match (F4-editor script wired + precached)
- all 4 i18n bundles carry the new keys
- `npm test` full suite green
</verification>

<success_criteria>
The Personalization tab exists and deep-links, the F5 picker sources labels from the engine and persists + fires app:dateformat on change (no reload), and every new i18n key ships across en/he/de/cs (PERS-01, PERS-02, PERS-07).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-06-SUMMARY.md` when done
</output>