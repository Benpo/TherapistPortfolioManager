---
phase: 37-date-consistency-date-format-setting-f6-f5
plan: 03
type: execute
wave: 1
depends_on: ["37-01"]
files_modified:
  - assets/date-format.js
  - assets/app.js
  - index.html
  - add-client.html
  - add-session.html
  - sessions.html
  - settings.html
  - report.html
  - reporting.html
  - demo.html
  - sw.js
  - tests/_helpers/jsdom-pdf-env.js
autonomous: true
requirements: [DATE-01, DATE-02, DATE-03, DATE-04]
must_haves:
  truths:
    - "One canonical zero-dependency window.DateFormat IIFE exists in assets/date-format.js that regex-extracts a leading YYYY-MM-DD and constructs a LOCAL Date(y,m-1,d) — never new Date('YYYY-MM-DD') UTC-midnight (D-01/D-02); exposes format(input,formatKey,lang), parseLocal(input), todayLocalISO(), getPreference()"
    - "App.formatDate delegates its whole body to window.DateFormat.format(dateString, window.DateFormat.getPreference(), currentLang) so the ~10 App.formatDate callers are fixed at once; the D-04 'auto' rule (en→en-US, de/cs long, en/he short) is reproduced byte-for-byte inside date-format.js (D-03/D-04)"
    - "The 6 D-05 format options render per spec with D-06 separators (/ for slash formats, - for yyyy-mm-dd); Hebrew numeric formats are wrapped in Unicode isolates U+2066…U+2069 as a bare string (D-07/DATE-04), named-month Hebrew is NOT wrapped"
    - "date-format.js loads via a <script> tag BEFORE app.js and pdf-export.js on all 8 app.js pages, is added to sw.js PRECACHE_URLS, and is injected into tests/_helpers/jsdom-pdf-env.js before pdf-export.js (D-21)"
    - "tests/37-date-format.test.js spine + 6-format + Hebrew-isolate + parseLocal boundary/age assertions go GREEN"
  artifacts:
    - "assets/date-format.js (NEW)"
    - "assets/app.js formatDate (delegating wrapper)"
    - "date-format.js <script> on 8 pages + sw.js PRECACHE + jsdom-pdf-env injection"
  key_links:
    - "Load order: date-format.js must precede app.js AND pdf-export.js on every page (those modules call window.DateFormat)"
    - "jsdom-pdf-env.js must eval date-format.js BEFORE pdf-export.js — and this edit MUST land together with the new module (editing it earlier would break every existing PDF test)"
  prohibitions:
    - "No date library (dayjs/luxon) — zero-dependency vanilla IIFE only"
    - "The helper returns a BARE string with Unicode isolates — never <bdo> markup (call sites use .textContent)"
    - "Do not reroute wall-clock timestamps (createdAt/exportedAt/Date.now) — this plan touches only the helper + formatDate wrapper + wiring"
---

<objective>
Build the date-correctness engine spine: the canonical `window.DateFormat` helper, the `App.formatDate` delegating wrapper, and the load-order + precache + test-env wiring so every page and the PDF test env sees the helper. This is the foundation every date surface, the UTC sweep, the PDF path, and the F5 picker depend on.

Purpose: Centralize the single distinction `new Date(y,m-1,d)` (local) vs `new Date("YYYY-MM-DD")` (UTC) in one place, killing the off-by-one bug class at the root (D-01/D-02).
Output: `assets/date-format.js` + a delegating `App.formatDate` + the script-tag/precache/test-env wiring; `tests/37-date-format.test.js` unit + spine assertions turn green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-RESEARCH.md
@.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-PATTERNS.md
@assets/crashlog.js
@assets/version.js
@assets/app.js
@tests/_helpers/jsdom-pdf-env.js
</context>

<artifacts_produced>
This section is the phase-wide catalog of every new/changed symbol Phase 37 produces (referenced by all downstream plans):

**Engine (this plan):**
- `assets/date-format.js` (NEW) → `window.DateFormat` with public methods `format(input, formatKey, lang)`, `parseLocal(input)`, `todayLocalISO()`, `getPreference()`
- `assets/app.js` `App.formatDate` reworked to delegate to `window.DateFormat`

**Personalization surface (Plans 05/06/07/08):**
- `localStorage['portfolioDateFormat']` scalar key (values `auto` / `month-day-year` / `day-month-year` / `mm/dd/yyyy` / `dd/mm/yyyy` / `yyyy-mm-dd`; default `auto`)
- `app:dateformat` CustomEvent (fired by the picker; forward-compat hook — the picker page has no date surfaces to re-render, and cross-page date correctness relies on page navigation, each page reading `getPreference()` at render; see Plan 06 note)
- `?tab=personalize` deep-link token (settings.js whitelist)
- `assets/settings-session-types.js` (NEW) — two-tier session-type editor IIFE
- `App.getSessionTypes()` + reworked `App.formatSessionType()` — both read `localStorage['portfolioSessionTypes']` **synchronously** (no IDB, no async cache, no first-paint race)
- `localStorage['portfolioSessionTypes']` JSON key `{ overrides: { <lockedKey>: "<label>", ... }, custom: [ { key, label }, ... ] }` (read/write mirrors `portfolioDateFormat`: try/catch + JSON.parse with default) — session-type storage is localStorage, NOT IndexedDB (the IDB therapistSettings path does not round-trip through backup restore — see 37-PATTERNS.md A2 CORRECTED)
- `app:session-types-changed` CustomEvent (fired within-tab by the editor after a mutation; a `storage`-event listener in `initCommon` re-dispatches it in peer tabs) — date/type display sites re-render on it
- new session-type keys `remote`, `proxy`
- new i18n keys: `settings.tab.personalize`, `settings.dateFormat.*` (`label`/`auto`/`savedToast`), `settings.sessionTypes.*` (`heading`/`helper`/`add.label`/`add.placeholder`/`locked.tooltip`/`rename.aria`/`delete.aria`/`savedToast`/`empty`/`confirm.delete.{title,body,confirm}`/`add.invalid`), `session.type.remote`, `session.type.proxy`
- new file paths: `assets/date-format.js`, `assets/settings-session-types.js`, `tests/37-date-format.test.js`, `tests/37-personalization.test.js`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assets/date-format.js (window.DateFormat) + rework App.formatDate to delegate</name>
  <files>assets/date-format.js, assets/app.js</files>
  <read_first>
    - 37-RESEARCH.md §"Module API — assets/date-format.js" (the full reference implementation, lines 83-167 — copy it) and §"The 6 Format Options × 4 Locales" + §"Hebrew Numeric LTR Wrapping"
    - assets/crashlog.js (line ~24) + assets/version.js (line ~22) — the window-IIFE shape to follow (register on window)
    - assets/app.js formatDate (lines 941-958 — the current body with the localeMap + long/short month rule to reproduce inside autoFormat) and the App public-return object where formatDate is exposed
  </read_first>
  <behavior>
    - parseLocal('2026-07-02').getDate() === 2 (local, not UTC's 1 in a negative-UTC TZ)
    - format('2026-07-02','auto','en') === 'Jul 2, 2026' (en-US, D-04); de/cs use long month, en/he short (reproduces current app.js)
    - the 6 option outputs per the RESEARCH table; numeric formats use ASCII digits with `/` (slash formats) and `-` (yyyy-mm-dd), D-06
    - format('2026-07-02','dd/mm/yyyy','he') is wrapped in U+2066…U+2069; stripped === '02/07/2026'; named-month he is NOT wrapped
    - todayLocalISO() === local YYYY-MM-DD; getPreference() reads localStorage['portfolioDateFormat'] || 'auto' behind try/catch
  </behavior>
  <action>
    Create assets/date-format.js as window.DateFormat = (function(){ 'use strict'; … })() following the RESEARCH reference implementation exactly. Core parse: regex-extract the leading `\d{4}-\d{2}-\d{2}` from the input (handles bare and legacy full-ISO values) then construct a LOCAL Date via the (year, month-1, day) constructor. Implement format() switching on the 6 seam keys; `auto` uses Intl.DateTimeFormat reproducing the current app.js rule (long month for de/cs, short for en/he, locale en-US for en per D-04); the named-month options hand-assemble using an Intl {month:'short'} token so the order is stable across locales; the numeric options hand-assemble ASCII digits with D-06 separators. Wrap Hebrew numeric output in the Unicode LEFT-TO-RIGHT ISOLATE / POP DIRECTIONAL ISOLATE code points (U+2066 / U+2069) as a bare string; do not wrap named-month Hebrew. Expose format/parseLocal/todayLocalISO/getPreference.

    Rework App.formatDate (app.js) so its whole body becomes a single delegation returning window.DateFormat.format(dateString, window.DateFormat.getPreference(), currentLang). Remove the now-duplicated localeMap/month logic from formatDate (it now lives in date-format.js autoFormat). Keep the exported symbol and signature unchanged so all ~10 callers are untouched. Preserve the empty-input and unparseable-input pass-through behavior.
  </action>
  <verify>
    <automated>node tests/37-date-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "window.DateFormat = (function" assets/date-format.js` matches
    - `node tests/37-date-format.test.js` exits 0 (the spine + 6-format + Hebrew-isolate + parseLocal boundary/age assertions all pass)
    - `formatDate('2026-07-02')` returns `Jul 2, 2026` under TZ=America/New_York (was `Jul 1, 2026`)
    - `assets/date-format.js` has zero import/require statements (zero-dependency)
  </acceptance_criteria>
  <done>The canonical helper exists and App.formatDate delegates to it; the TZ-pinned engine spec is green for the unit + spine layer.</done>
</task>

<task type="auto">
  <name>Task 2: Wire date-format.js on all 8 pages, sw.js precache, and the PDF test env (D-02/D-21)</name>
  <files>index.html, add-client.html, add-session.html, sessions.html, settings.html, report.html, reporting.html, demo.html, sw.js, tests/_helpers/jsdom-pdf-env.js</files>
  <read_first>
    - 37-RESEARCH.md §"Script Load-Order Map" (the exact per-page insert line for each of the 8 pages; the note that landing/license/legal pages do NOT load app.js and need no insert; the runtime-call safety note for backup.js on index.html)
    - sw.js (PRECACHE_URLS at ~line 33, right after '/assets/version.js' and '/assets/crashlog.js')
    - tests/_helpers/jsdom-pdf-env.js (the sequential win.eval(readAsset(...)) block ~lines 90-125 — inject date-format.js before the pdf-export.js eval)
  </read_first>
  <action>
    Add a `<script src="assets/date-format.js"></script>` tag on each of the 8 pages that load app.js (index, add-client, add-session, sessions, settings, report, reporting, demo), positioned BEFORE the app.js tag (and therefore before pdf-export.js where present). Do NOT add it to landing/license/legal pages (they do not load app.js). Add `/assets/date-format.js` to sw.js PRECACHE_URLS alongside the existing version.js/crashlog.js entries so it is offline-available. In tests/_helpers/jsdom-pdf-env.js, add a win.eval(readAsset('assets/date-format.js')) call immediately BEFORE the pdf-export.js eval so PDF tests exercise the real helper (D-21). This test-env edit lands together with the new module — do not split it earlier.
  </action>
  <verify>
    <automated>grep -l 'assets/date-format.js' index.html add-client.html add-session.html sessions.html settings.html report.html reporting.html demo.html | wc -l | grep -qx 8 && grep -q '/assets/date-format.js' sw.js && grep -q "date-format.js" tests/_helpers/jsdom-pdf-env.js && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - All 8 app.js pages contain a `assets/date-format.js` script tag positioned before `assets/app.js`
    - `sw.js` PRECACHE_URLS includes `/assets/date-format.js`
    - `tests/_helpers/jsdom-pdf-env.js` evals `assets/date-format.js` before `assets/pdf-export.js`
    - `npm test` full suite stays green (no existing PDF test broken by the env change)
  </acceptance_criteria>
  <done>The helper is loaded before app.js/pdf-export.js everywhere, precached, and available in the PDF test env; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Local-first PWA, no backend. This plan adds a pure transform utility + script wiring; no user-entered free text is rendered here.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03-01 | Tampering (bidi spoofing) | Hebrew numeric date string in DOM/title/PDF | low | mitigate | Only the two directional-isolate code points (U+2066/U+2069) are inserted around numeric dates; no markup is emitted, so no HTML-injection surface is created by the wrap |
| T-37-03-02 | Information disclosure | localStorage read in getPreference | low | accept | getPreference reads only the non-secret portfolioDateFormat key behind try/catch; no sensitive data crosses a boundary |
| T-37-03-03 | Spoofing (bidi in browser chrome) | Hebrew numeric date carrying U+2066/U+2069 lands in `document.title` (add-session.js:1494-1498) | low | accept | Accepted, title-bar-only, rare (occurs only for he UI + a non-default numeric format). The isolate code points are inert control chars in the tab title; no markup, no HTML surface. No mitigation beyond the existing bare-string wrap — the browser renders the title LTR-isolated as intended |

No package installs — no supply-chain (T-SC) threat.
</threat_model>

<verification>
- `node tests/37-date-format.test.js` green (unit + spine + Hebrew isolate + boundary/age)
- All 8 pages + sw.js + jsdom-pdf-env wired (grep gate above)
- `npm test` full suite green
</verification>

<success_criteria>
The canonical date engine exists, App.formatDate delegates to it, it is loaded before app.js/pdf-export.js on every page and in the PDF test env, and the TZ-pinned engine spec's unit/spine layer is green (DATE-01/DATE-03/DATE-04 helper half; DATE-02 enabler).
</success_criteria>

<output>
Create `.planning/phases/37-date-consistency-date-format-setting-f6-f5/37-03-SUMMARY.md` when done
</output>
