---
phase: 44-tech-debt-guardrails-pre-prod-environment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cf-await-promotion.sh
  - tests/cf-await-promotion.test.js
autonomous: true
requirements: [DEBT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "scripts/cf-await-promotion.sh polls the live origin (default https://sessionsgarden.app/assets/version.js, served no-cache) with a cache-buster until the body contains the new short-SHA BUILD_TOKEN, then purges the Cloudflare zone cache"
    - "On poll timeout the script exits 1 with a loud stderr message and does NOT purge (a uniformly-stale cache is safe; a mixed one is not)"
    - "After confirmed promotion, a failed purge exits 1 loudly (confirmed-new origin + un-purged edge IS the mixed-cache bug)"
    - "The token match is the 7-hex short SHA (first 7 chars of GITHUB_SHA), never the full SHA (Pitfall 1)"
    - "node tests/cf-await-promotion.test.js exits 0, driving token-present-first-poll→exit 0 fast; never-present→exit 1 after timeout with no purge; present-on-Nth-poll→exit 0; purge-fails→exit 1"
    - "The test source-audits that _headers still declares /assets/version.js no-cache (sentinel precondition, Pitfall 4)"
  artifacts:
    - "scripts/cf-await-promotion.sh"
    - "tests/cf-await-promotion.test.js"
  key_links:
    - "cf-await-promotion.sh is the single tested unit deploy.yml calls in Wave 2 (Plan 04) to replace the old immediate non-blocking purge"
  prohibitions:
    - "MUST NOT purge on a fixed sleep or before token confirmation (the exact v1.3.0 failure mode)"
    - "MUST NOT poll for the full GITHUB_SHA (Pitfall 1 — always the 7-hex short SHA)"
    - "MUST NOT introduce any new secret (content-polling uses a public URL; purge reuses CF_ZONE_ID/CF_PURGE_TOKEN)"
    - "MUST NOT use bash-only substring ${GITHUB_SHA::7} (the script runs under sh; use POSIX printf '%.7s')"
---

<objective>
Close the v1.3.0 mixed-cache incident class (DEBT-02) by extracting the purge into a fail-closed, offline-testable script that purges the Cloudflare zone cache ONLY after the live production origin is confirmed serving the new deploy. `scripts/cf-await-promotion.sh` polls the no-cache `assets/version.js` token sentinel, then purges; either a promotion timeout or a purge failure fails the run loudly.

Purpose: the incident was an Integrity/Availability failure — edges re-cached OLD assets before Pages promoted, poisoning installed PWA precaches. Sentinel-then-purge sequencing is the mitigation.
Output: `scripts/cf-await-promotion.sh` + `tests/cf-await-promotion.test.js` (stub-curl behavior spec).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md
@scripts/ci-resolve-docs-range.sh
@scripts/cf-purge-cache.sh
@tests/ci-resolve-docs-range.test.js
@_headers
</context>

## Artifacts this phase produces (this plan's share)

- `scripts/cf-await-promotion.sh` (new — POSIX-sh sentinel-then-blocking-purge)
- `tests/cf-await-promotion.test.js` (new — stub-curl offline behavior spec + _headers no-cache source-audit)
- Env-var contract consumed by deploy.yml in Wave 2: `GITHUB_SHA`, `CF_ZONE_ID`, `CF_PURGE_TOKEN`, optional `POLL_URL`, `SENTINEL_INTERVAL`, `SENTINEL_TIMEOUT`

<tasks>

<task type="auto">
  <name>Task 1: Author the sentinel-then-purge behavior spec with a stub curl (RED)</name>
  <files>tests/cf-await-promotion.test.js</files>
  <read_first>
    - tests/ci-resolve-docs-range.test.js (the stub-binary harness: mkdtemp temp dir, stub executable on a bin/ PATH, spawnSync('sh',[SCRIPT],{env}) capturing {code,stdout,stderr}, self-clean in finally — lines ~45-109 and ~166-168)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Pattern 1, § Pattern 2, § Validation Architecture → Test Map)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md (tests/cf-await-promotion.test.js entry)
    - _headers (lines 22-23 — the /assets/version.js no-cache rule the sentinel depends on)
    - scripts/cf-purge-cache.sh (the curl POST purge idiom + `"success":true` grep)
  </read_first>
  <action>
    Create `tests/cf-await-promotion.test.js` (NO phase-number prefix, per D-04) copying the ci-resolve-docs-range.test.js scaffolding, but stub `curl` (not `git`): a `#!/bin/sh` script on a temp bin/ dir prepended to PATH. Drive the stub via `STUB_*` env plus a temp counter file so it returns an OLD token for the first N GET polls then the NEW token, and returns a chosen JSON for the purge POST; the stub distinguishes a GET of the version URL from a POST to `purge_cache` by inspecting its arguments. Invoke the real script with `spawnSync('sh', [SCRIPT], { env })` capturing `{ code, stdout, stderr }`, and set `SENTINEL_INTERVAL`/`SENTINEL_TIMEOUT` small (e.g. 1 and 2) so the timeout case finishes in ~seconds. Cases: (1) new token on the first poll → purge JSON `"success":true` → exit 0, and assert only one poll happened; (2) new token never appears → exit 1 after the shortened timeout, stderr names the timeout AND "not purging"/"failing closed", and assert NO purge POST was issued; (3) new token first appears on the 3rd poll → exit 0; (4) new token present but purge POST returns `"success":false` → exit 1 with loud stderr. Fold in a source-audit that reads the real `_headers` and asserts it still contains the `/assets/version.js` no-cache rule (Pitfall 4). Self-clean the temp dir in finally; `process.exit(failed === 0 ? 0 : 1)`. RED is expected now (the script is absent) — the harness self-defends like the ci-resolve precedent.
  </action>
  <verify>
    <automated>node tests/cf-await-promotion.test.js</automated>
  </verify>
  <acceptance_criteria>
    `tests/cf-await-promotion.test.js` runs under node and exits NON-ZERO (RED) because `scripts/cf-await-promotion.sh` does not exist yet; filename matches `^cf-await-promotion\.test\.js$`; `node tests/run-all.js` discovers it.
  </acceptance_criteria>
  <done>A falsifiable RED spec covering all four exit-code cases + the no-cache precondition exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scripts/cf-await-promotion.sh (poll-then-blocking-purge) (GREEN)</name>
  <files>scripts/cf-await-promotion.sh</files>
  <read_first>
    - scripts/ci-resolve-docs-range.sh (fail-closed shape: `set -eu`, WHAT/WHY/CONTRACT doc-header, loud stderr + `exit 1`, stdout quiet)
    - scripts/cf-purge-cache.sh (curl POST to `.../zones/$CF_ZONE_ID/purge_cache`, `--data '{"purge_everything":true}'`, `"success":true` grep)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Pattern 1 poll loop + § Pattern 2 blocking purge + § Pitfall 1)
    - assets/version.js (line ~32 — the `var BUILD_TOKEN = '<7hex>';` literal shape the poll greps for)
    - _headers (lines 22-23)
    - tests/cf-await-promotion.test.js (the contract this must satisfy)
  </read_first>
  <action>
    Create `scripts/cf-await-promotion.sh` as `#!/bin/sh` with `set -eu` and a WHAT/WHY/CONTRACT doc-header matching ci-resolve-docs-range.sh (stdout quiet, all banners to stderr, exit 1 fail-closed). Compute the short SHA as the first 7 chars of GITHUB_SHA using POSIX substring `SHORT=$(printf '%.7s' "$GITHUB_SHA")` — NOT bash `${GITHUB_SHA::7}` — so it runs under `sh`. Read `POLL_URL` from env (default `https://sessionsgarden.app/assets/version.js`), `SENTINEL_INTERVAL` (default 10), `SENTINEL_TIMEOUT` (default 300). Poll loop: `curl -fsS -H 'Cache-Control: no-cache' "$POLL_URL?cb=$(date +%s)"` and grep the body for `BUILD_TOKEN = '$SHORT'`; on match, break to the purge; if the elapsed time reaches the deadline, echo a loud "timed out waiting for promotion of $SHORT; NOT purging (a uniformly-stale cache is safe, a mixed one is not)" to `>&2` and `exit 1` (no purge); otherwise `sleep "$SENTINEL_INTERVAL"` and continue. Purge stage (only after a confirmed match): POST `https://api.cloudflare.com/client/v4/zones/$CF_ZONE_ID/purge_cache` with `Authorization: Bearer $CF_PURGE_TOKEN`, `Content-Type: application/json`, `--data '{"purge_everything":true}'`; if the response contains `"success":true` echo "Cache purged." and `exit 0`, else echo a loud "purge failed AFTER confirmed promotion — edge may serve a mixed cache; re-run this job." to `>&2`, print the response to `>&2`, and `exit 1`. Keep it POSIX-sh clean so the stub-curl test drives every branch offline.
  </action>
  <verify>
    <automated>node tests/cf-await-promotion.test.js</automated>
    <automated>node tests/run-all.js</automated>
    <automated>sh -n scripts/cf-await-promotion.sh</automated>
  </verify>
  <acceptance_criteria>
    `node tests/cf-await-promotion.test.js` exits 0 (all four cases + the no-cache precondition GREEN); `node tests/run-all.js` green; `sh -n scripts/cf-await-promotion.sh` parses clean; the script matches the 7-hex short SHA (never the full SHA) and never purges before a confirmed match.
  </acceptance_criteria>
  <done>A POSIX-sh sentinel-then-blocking-purge script exists, fully covered offline; ready for deploy.yml to call in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → live CF origin | the runner reads deploy truth from the public no-cache version.js |
| CI runner → CF zone purge API | the runner invalidates the production edge cache with a scoped token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-03 | Integrity / Availability | deploy purge sequencing | high | mitigate | Sentinel confirms the live origin serves the new short-SHA token before purging; poll timeout and purge failure both `exit 1` (fail-closed); driven by tests/cf-await-promotion.test.js |
| T-44-04 | Information Disclosure | CF secrets in the runner | low | accept | Reuses existing CF_ZONE_ID/CF_PURGE_TOKEN; content-polling a public URL adds no new secret |
| T-44-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node tests/cf-await-promotion.test.js` exits 0.
- `node tests/run-all.js` green; `sh -n scripts/cf-await-promotion.sh` parses clean.
- Manual/live (VALIDATION.md): the real poll against `sessionsgarden.app` and the real zone purge are only verifiable on a live CI run (documented as manual-only).
</verification>

<success_criteria>
A fail-closed, offline-tested sentinel-then-purge script exists that purges only after the live origin serves the new token — the DEBT-02 mixed-cache class can no longer occur once deploy.yml calls it (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-02-SUMMARY.md` when done.
</output>
