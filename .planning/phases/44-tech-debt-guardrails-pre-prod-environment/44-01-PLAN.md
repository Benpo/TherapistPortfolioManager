---
phase: 44-tech-debt-guardrails-pre-prod-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - assets/add-client.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-07-10-comment-hygiene-retrofit-and-forward-gate.md
  - tests/conventions-hygiene.test.js
autonomous: true
requirements: [DEBT-01]
nyquist_compliant: true

must_haves:
  truths:
    - "CONVENTIONS.md §Comments no longer mandates phase/plan-ID citations; it carries the strip-all-planning-IDs rule with BOTH rationales (archived-.planning dangling refs AND assets/** customer exposure) per D-02"
    - "CONVENTIONS.md §Comments folds in the 4-slot banner shape (OWNS · PUBLIC SURFACE · DEPENDENCIES · CONSTRAINTS) per D-03 and the {slug}.test.js no-phase-number test-naming rule per D-04"
    - "assets/add-client.js line ~89 console.warn keeps the no-hard-cap rationale in plain prose and no longer prints a planning-ID token (D-05)"
    - "REQUIREMENTS.md DEBT-01 and ROADMAP.md Phase 44 criterion 1 describe only the CONVENTIONS fix + console.warn reword; the forward gate is recorded as a v1.5 deferred item in both docs (D-06)"
    - "No enforcement/test gate over shipped comments ships this phase (D-01)"
    - "node tests/conventions-hygiene.test.js exits 0"
  artifacts:
    - "tests/conventions-hygiene.test.js"
  key_links:
    - "The push carrying assets/add-client.js must carry the two tip-commit docs-gate trailers (Changelog-Unaffected + Help-Unaffected) or the docs-gate blocks it"
  prohibitions:
    - "MUST NOT ship any grep/test gate enforcing comment hygiene over shipped code (D-01 — deferred to v1.5)"
    - "MUST NOT rename existing phase-numbered test files (the D-04 rule is forward-looking; the retrofit is v1.5)"
    - "MUST NOT promote or move 36-COMMENT-STYLE-GUIDE.md out of the archive (D-03)"
    - "MUST NOT touch any shipped comment other than the single add-client.js console.warn (D-05)"
---

<objective>
Fix the root cause of planning-ref leakage into shipped code and remove the one runtime leak — WITHOUT shipping an enforcement gate this milestone (D-01). Rewrite `.planning/codebase/CONVENTIONS.md` §Comments to adopt the Phase 36 D-07 rule (D-02/D-03/D-04), reword the single `assets/add-client.js` `console.warn` that prints a decision ID into customer DevTools (D-05), and align REQUIREMENTS.md + ROADMAP.md to the re-cut (D-06). A Wave-0 source-audit test pins the two content changes.

Purpose: v1.4's feature phases must be protected by corrected instructions (the enforcement layer + ~680-line retrofit travel together in v1.5). This "stops the bleeding."
Output: rewritten §Comments, a de-IDed console.warn, aligned planning docs, and `tests/conventions-hygiene.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-CONTEXT.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@tests/ci-resolve-docs-range.test.js
</context>

<!-- planner-discipline-allow: do not omit -->
<!-- planner-discipline-allow: D-23 -->

## Out of scope this phase (NOT-list — confirmed, do NOT plan or implement)

- The ~680-line / ~43-file comment retrofit AND the forward grep-gate — both deferred to v1.5 (they travel together; D-01 locked). This phase ships NO enforcement gate.
- Renaming existing phase-numbered test files — the D-04 rule is forward-looking only; the legacy corpus stays until the v1.5 retrofit.
- Promoting / moving `36-COMMENT-STYLE-GUIDE.md` out of the archive (D-03 — it stays as history).
- The `max-age=86400` rethink (purge-race direction 3) — note as a future pipeline consideration only; not needed once purge is correctly sequenced.
- Checking in the un-committed Cloudflare-faithful local server from Phase 42 UAT — Ben declined; the pre-prod CF Pages project IS the real fix.

## Artifacts this phase produces (this plan's share)

- `tests/conventions-hygiene.test.js` (new — offline source-audit)
- Rewritten `.planning/codebase/CONVENTIONS.md` §Comments (modified)
- De-IDed `assets/add-client.js` console.warn (modified, one line)
- Aligned `.planning/REQUIREMENTS.md` DEBT-01 + `.planning/ROADMAP.md` criterion 1 (modified)

<tasks>

<task type="auto">
  <name>Task 1: Author the DEBT-01 hygiene source-audit test (RED)</name>
  <files>tests/conventions-hygiene.test.js</files>
  <read_first>
    - tests/ci-resolve-docs-range.test.js (harness template: test()/assert() mini-runner, readFileSync, self-clean, exit-0/1 contract)
    - .planning/codebase/CONVENTIONS.md (§Comments, lines ~149-157 — the citation-mandate text to be removed)
    - assets/add-client.js (line ~89 — the console.warn carrying a decision-ID token)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Validation Architecture → Phase Requirements → Test Map)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md (tests/conventions-hygiene.test.js entry)
  </read_first>
  <action>
    Create `tests/conventions-hygiene.test.js` (NO phase-number prefix, per D-04) as a pure offline source-audit reusing the ci-resolve-docs-range.test.js harness shell (test()/assert()/readFileSync — no stub binary). Assert three things: (1) `.planning/codebase/CONVENTIONS.md` §Comments no longer contains the old citation-mandate sentence (the traceability instruction ending in the do-not-omit directive) AND now contains the strip-all-planning-IDs rule (a phrase asserting shipped code carries no planning IDs / plain prose); (2) `assets/add-client.js` contains no decision-ID token on its large-photo console.warn line — match the pattern `/\bD-\d+\b/` and assert absent; (3) keep the CONVENTIONS.md assertion SCOPED — do NOT blanket-grep the whole file for `D-` because the rewritten Don't-list legitimately shows ID examples; assert only against the removed mandate sentence and the presence of the new rule. Follow the harness's `process.exit(failed === 0 ? 0 : 1)` contract. RED is expected now — the edits happen in Task 2.
  </action>
  <verify>
    <automated>node tests/conventions-hygiene.test.js</automated>
  </verify>
  <acceptance_criteria>
    `tests/conventions-hygiene.test.js` runs under node and exits NON-ZERO (RED) because the CONVENTIONS.md rewrite and add-client.js reword have not happened yet; the filename matches `^conventions-hygiene\.test\.js$` (no `44-` prefix); `node tests/run-all.js` discovers it.
  </acceptance_criteria>
  <done>A falsifiable RED source-audit exists, failing for the right reason (edits pending).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CONVENTIONS.md §Comments and reword the add-client.js console.warn (GREEN)</name>
  <files>.planning/codebase/CONVENTIONS.md, assets/add-client.js</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (§Comments, lines ~149-157)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-CONTEXT.md (D-02, D-03, D-04, D-05)
    - .planning/milestones/v1.2-phases/36-code-comments-batch-2/36-CONTEXT.md (line ~40 — the D-07 source rule being adopted)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ "Source rules to fold into CONVENTIONS.md §Comments")
    - assets/add-client.js (lines 82-95 — the large-photo warn in context)
  </read_first>
  <action>
    In CONVENTIONS.md §Comments (currently lines ~149-157), replace the phase/plan-citation traceability mandate with the Phase 36 D-07 rule: shipped code carries NO planning IDs — requirement IDs (`REQ-`/`OBS-`/`VER-`/`RFCT-`/`DOCS-`/`DEMO-`/`I18N-`/`TEST-` etc.), decision IDs, code-review IDs, task IDs, and process framing (UAT / architect-gate / "the executor" / `.planning/` paths) all become plain prose; only the ID leaves, the WHY/constraint stays. State explicitly that REAL technical tokens are NOT planning IDs and stay untouched: `AES-256`, `SHA-256`, schema `v1–v6`, `IDBDatabase`. Record BOTH rationales per D-02: (a) `.planning/` is archived per-milestone so an ID in shipped code becomes a dangling reference (git blame is the durable trace); (b) `assets/**` ships its comments, so an internal ID is customer DevTools exposure. Fold in the D-03 4-slot banner shape (`OWNS · PUBLIC SURFACE · DEPENDENCIES · CONSTRAINTS`), pointing to the archived `36-COMMENT-STYLE-GUIDE.md` for Do/Don't wording — do NOT promote or move that guide (D-03). Fold in the D-04 test-naming rule: new test files are `{slug}.test.js`, no phase numbers, provenance lives in git; note it is forward-looking (legacy phase-numbered tests are not renamed here). In `assets/add-client.js` (line ~89), reword the large-photo `console.warn` so it keeps the soft-cap / no-hard-cap rationale in plain prose and drops the decision-ID tag (D-05) — this is the ONLY shipped string touched this phase; do not alter any other comment.
  </action>
  <verify>
    <automated>node tests/conventions-hygiene.test.js</automated>
    <automated>node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    `node tests/conventions-hygiene.test.js` exits 0 (GREEN); `node tests/run-all.js` stays green; §Comments carries the strip-all rule + both rationales + the 4-slot banner shape + the {slug}.test.js rule; the add-client.js warn retains its no-hard-cap rationale with no decision-ID token; `git diff` shows exactly one changed line in assets/add-client.js.
  </acceptance_criteria>
  <done>Root-cause instruction fixed and the one runtime leak removed; the hygiene test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Align REQUIREMENTS/ROADMAP to the re-cut and record the docs-gate trailers</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/todos/pending/2026-07-10-comment-hygiene-retrofit-and-forward-gate.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DEBT-01 at line ~46; Deferred / Future at lines ~57-62)
    - .planning/ROADMAP.md (Phase 44 success criterion 1 at line ~113; Deferred section at line ~179)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-CONTEXT.md (D-06 + § Deferred/Reviewed Todos)
    - .planning/todos/pending/2026-07-10-comment-hygiene-retrofit-and-forward-gate.md
    - ./CLAUDE.md (Definition of Done — docs must not rot; the changelog-only tier + trailer rules)
    - scripts/lib/role-table.js (CHANGELOG_ONLY set — confirm add-client.js is NOT a member)
  </read_first>
  <action>
    Per D-06, edit REQUIREMENTS.md DEBT-01 (line ~46) so it describes ONLY the CONVENTIONS.md §Comments root-cause fix + the single add-client.js console.warn reword — remove the forward grep-gate clause; adjust the Deferred / Future entry so the forward gate is explicitly a v1.5 item traveling with the ~680-line retrofit. Edit ROADMAP.md Phase 44 success criterion 1 (line ~113) the same way (drop the "baseline-aware test gate blocks NEW internal planning reference" language; keep the CONVENTIONS fix + console.warn reword), and update the ROADMAP Deferred entry (line ~179) so it no longer claims v1.4 ships a forward gate — the gate defers to v1.5 with the retrofit. In the comment-hygiene retrofit todo, add a dated note recording that Phase 44 handled its two carve-out items (the CONVENTIONS.md §Comments rewrite and the add-client.js console.warn reword); the retrofit + forward gate remain pending for v1.5. Finally, record the docs-gate satisfier for this phase's one watched change (`assets/add-client.js`): the push carrying it MUST include, on the TIP commit, the two trailer lines `Changelog-Unaffected: — DevTools-only console.warn reword, not user-facing` and `Help-Unaffected: assets/add-client.js — internal log-string reword, no help surface`. add-client.js is NOT in the changelog-only tier, so BOTH demands are real and must be waived. Capture both trailer lines verbatim in this plan's SUMMARY so whoever pushes applies them on the tip commit.
  </action>
  <verify>
    <automated>node tests/run-all.js</automated>
    <human-check>REQUIREMENTS.md DEBT-01 and ROADMAP.md criterion 1 no longer describe a forward/baseline test gate; both docs' deferred sections name the gate as a v1.5 item; the retrofit todo carries a dated Phase-44-handled note; the SUMMARY records both docs-gate trailer lines verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    `.planning/`-only edits carry no docs-gate demand; the SUMMARY contains the two verbatim tip-commit trailer lines; `node tests/run-all.js` stays green.
  </acceptance_criteria>
  <done>Planning docs match the re-cut, the pusher has the exact trailers, and no test gate shipped (D-01 honored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped `assets/**` comments/strings → customer DevTools | internal planning text is visible to practitioners with DevTools open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Information Disclosure | assets/add-client.js console.warn | low | mitigate | Reword to drop the decision-ID token (D-05); tests/conventions-hygiene.test.js asserts no `D-\d+` remains on that line |
| T-44-02 | Repudiation | CONVENTIONS.md §Comments instruction | low | mitigate | Replace the citation mandate with the strip-all-planning-IDs rule so future shipped comments carry no planning IDs (D-02) |
| T-44-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node tests/conventions-hygiene.test.js` exits 0.
- `node tests/run-all.js` green.
- Manual read: §Comments carries the D-02/D-03/D-04 rules; REQUIREMENTS/ROADMAP match the re-cut; SUMMARY records the two docs-gate trailers.
</verification>

<success_criteria>
CONVENTIONS.md §Comments no longer instructs agents to cite phase/plan IDs; the sole runtime leak is removed; planning docs describe only the shipped scope; the forward gate is a documented v1.5 deferral; no enforcement gate shipped.
</success_criteria>

<output>
Create `.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-01-SUMMARY.md` when done. Record the two tip-commit docs-gate trailer lines verbatim.
</output>
