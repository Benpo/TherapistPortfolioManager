---
phase: 44-tech-debt-guardrails-pre-prod-environment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-staging.sh
  - tests/build-staging.test.js
autonomous: true
requirements: [DEBT-03]
nyquist_compliant: true

must_haves:
  truths:
    - "scripts/build-staging.sh <target-dir> [--noindex] reproduces the prod staging transform exactly: whitelist cp (_headers, _redirects, LICENSE, *.html, assets/, manifest.json, sw.js) + sed-stamp '__BUILD_TOKEN__' -> the 7-hex short SHA in <target>/assets/version.js"
    - "--noindex appends a second /* block carrying X-Robots-Tag: noindex to the STAGED _headers only, leaving the base CSP /* block byte-identical (D-09)"
    - "The staged tree never contains .planning, .claude, CLAUDE.md, or .env (no-leak invariant)"
    - "node tests/build-staging.test.js exits 0 (whitelist completeness, token stamp, no-leak, noindex divergence with the base CSP block unchanged)"
    - "The committed _headers and assets/version.js are never mutated by the script (it writes only into <target-dir>)"
  artifacts:
    - "scripts/build-staging.sh"
    - "tests/build-staging.test.js"
  key_links:
    - "build-staging.sh is the single source of truth both deploy.yml (Wave 2 Plan 04) and deploy-preprod.yml (Wave 2 Plan 05) call — killing prod<->pre-prod transform drift (D-07)"
  prohibitions:
    - "MUST NOT edit the committed _headers to add noindex (Pitfall 2 — inject into the staged copy only)"
    - "MUST NOT use bash-only substring ${GITHUB_SHA::7} (the script is invoked with sh; use POSIX printf '%.7s')"
    - "MUST NOT copy any path outside the explicit whitelist (the no-leak guard depends on the whitelist)"
---

<objective>
Extract the prod staging transform into a single parameterized, offline-testable script `scripts/build-staging.sh <target-dir> [--noindex]` (DEBT-03 shared-script approach). It is byte-faithful to the current inline `deploy.yml` transform (whitelist cp + `__BUILD_TOKEN__` short-SHA stamp) and adds ONE deliberate pre-prod-only divergence behind `--noindex`: appending an `X-Robots-Tag: noindex` block to the STAGED `_headers` only (D-09). A tmp-dir behavior test pins whitelist completeness, token stamp, the no-leak invariant, and the noindex divergence.

Purpose: one transform, two callers — prod and pre-prod can never drift, and the "Verify no sensitive files" invariant becomes a real, falsifiable test.
Output: `scripts/build-staging.sh` + `tests/build-staging.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md
@.github/workflows/deploy.yml
@tests/ci-resolve-docs-range.test.js
@_headers
</context>

## Artifacts this phase produces (this plan's share)

- `scripts/build-staging.sh` (new — POSIX-sh parameterized shared transform, `<target-dir> [--noindex]`)
- `tests/build-staging.test.js` (new — tmp-dir offline fidelity + noindex-divergence spec)

<tasks>

<task type="auto">
  <name>Task 1: Author the staging-transform fidelity + noindex-divergence test (RED)</name>
  <files>tests/build-staging.test.js</files>
  <read_first>
    - tests/ci-resolve-docs-range.test.js (harness: mkdtemp, spawnSync capture, self-clean in finally)
    - .github/workflows/deploy.yml (lines 42-59 inline transform being extracted; lines 61-73 the "Verify no sensitive files" guard being turned into a real assertion)
    - _headers (lines 1-6 — the base CSP /* block; lines 22-23 — version.js no-cache)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Validation Architecture + § Pattern 4)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md (tests/build-staging.test.js entry)
  </read_first>
  <action>
    Create `tests/build-staging.test.js` (NO phase-number prefix, per D-04) using the ci-resolve harness shell. In each case run `spawnSync('sh', ['scripts/build-staging.sh', <tmp target dir>], { cwd: REPO_ROOT, env: { ...process.env, GITHUB_SHA: <a fixed 40-hex fixture> } })` (add `--noindex` as a third arg for the noindex case), then assert on the staged tree: (1) whitelist complete — `_headers`, `_redirects`, `LICENSE`, at least one `*.html`, an `assets/` directory, `manifest.json`, and `sw.js` are all present; (2) token stamped — staged `assets/version.js` contains the first-7-hex of the fixture SHA as `'<7hex>'` and no longer contains the `__BUILD_TOKEN__` placeholder; (3) no-leak — the staged tree contains NO `.planning`, `.claude`, `CLAUDE.md`, or `.env` (turning the deploy.yml verify step into a real assertion); (4) noindex divergence — a `--noindex` run has `X-Robots-Tag: noindex` in the staged `_headers`, a run without it does NOT, and the base CSP block (the first `/*` block) is byte-identical between the two staged copies. Run into two separate tmp dirs for the divergence comparison; self-clean all tmp dirs in finally. RED is expected now (the script is absent).
  </action>
  <verify>
    <automated>node tests/build-staging.test.js</automated>
  </verify>
  <acceptance_criteria>
    `tests/build-staging.test.js` runs under node and exits NON-ZERO (RED) because `scripts/build-staging.sh` is absent; filename matches `^build-staging\.test\.js$`; `node tests/run-all.js` discovers it.
  </acceptance_criteria>
  <done>A falsifiable RED spec pinning whitelist completeness, token stamp, no-leak, and noindex divergence exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scripts/build-staging.sh (parameterized shared transform) (GREEN)</name>
  <files>scripts/build-staging.sh</files>
  <read_first>
    - .github/workflows/deploy.yml (lines 42-59 — the exact inline transform to reproduce verbatim: `mkdir -p`, the seven `cp` lines, the `sed` token stamp)
    - _headers (lines 1-6 — base CSP block that must stay byte-identical)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Pattern 4 noindex append + § Pattern 6 shared script)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md (scripts/build-staging.sh entry + § "Whitelist cp recipe" / § "Token stamp")
    - tests/build-staging.test.js (the contract this must satisfy)
  </read_first>
  <action>
    Create `scripts/build-staging.sh` as `#!/bin/sh` with `set -eu` and a WHAT/WHY/CONTRACT doc-header. Signature: `build-staging.sh <target-dir> [--noindex]`. Operate on the current working directory as the source repo root. `mkdir -p "$1"`; copy the exact prod whitelist into it, byte-faithful to deploy.yml:44-52 — `_headers`, `_redirects`, `LICENSE`, `*.html`, `assets/` (recursively via `cp -r`), `manifest.json`, `sw.js`. Stamp the token in the STAGED version.js only. Compute the short SHA with POSIX substring `SHORT=$(printf '%.7s' "$GITHUB_SHA")` — NOT bash `${GITHUB_SHA::7}`. Do the substitution PORTABLY without `sed -i`: `sed "s/'__BUILD_TOKEN__'/'$SHORT'/" "$1/assets/version.js" > "$1/assets/version.js.tmp" && mv "$1/assets/version.js.tmp" "$1/assets/version.js"`. This matters because this script's TEST runs on the developer's macOS (BSD sed, where `sed -i` needs a backup-suffix arg) as well as on ubuntu CI (GNU sed); the redirect+mv form behaves identically on both, whereas GNU-style `sed -i "s/..."` (deploy.yml:59, ubuntu-only) breaks the macOS test run. If the second argument is `--noindex`, append a second `/*` block to the staged headers ONLY via `printf '\n/*\n  X-Robots-Tag: noindex\n' >> "$1/_headers"` — append, do not `sed` into the existing block, so the base CSP line is untouched (Pitfall 2 / D-09); do not add the block when the flag is absent. Never write to the committed `_headers` or `assets/version.js` — only into `$1`. Keep it POSIX-sh clean so the tmp-dir test drives it offline on both macOS and ubuntu.
  </action>
  <verify>
    <automated>node tests/build-staging.test.js</automated>
    <automated>node tests/run-all.js</automated>
    <automated>sh -n scripts/build-staging.sh</automated>
  </verify>
  <acceptance_criteria>
    `node tests/build-staging.test.js` exits 0 (whitelist, token stamp, no-leak, noindex divergence all GREEN); `node tests/run-all.js` green; `sh -n scripts/build-staging.sh` parses clean; after running the script, `git status --porcelain _headers assets/version.js` is empty (committed files untouched).
  </acceptance_criteria>
  <done>A single tested shared transform script exists; both workflows can call it in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → staged deploy tree | the staged tree is force-pushed to a public host; anything copied in is shipped |
| committed `_headers` → staged `_headers` | the noindex divergence must live only in the staged copy, never the committed one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-05 | Information Disclosure | staged deploy tree | high | mitigate | Explicit whitelist cp; tests/build-staging.test.js asserts NO .planning/.claude/CLAUDE.md/.env in the staged tree; the workflows retain the verify-no-sensitive-files guard on the output |
| T-44-06 | Tampering | committed _headers CSP block | medium | mitigate | noindex is appended to the staged copy only; the test asserts the base CSP /* block is byte-identical between staged copies and `git status` stays clean (Pitfall 2) |
| T-44-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node tests/build-staging.test.js` exits 0.
- `node tests/run-all.js` green; `sh -n scripts/build-staging.sh` parses clean; committed `_headers`/`version.js` untouched after a run.
</verification>

<success_criteria>
One tested, byte-faithful shared transform exists with a pre-prod-only noindex flag; the no-leak invariant is now a real test; prod and pre-prod can never drift.
</success_criteria>

<output>
Create `.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-03-SUMMARY.md` when done.
</output>
