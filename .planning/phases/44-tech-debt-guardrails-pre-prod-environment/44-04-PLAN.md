---
phase: 44-tech-debt-guardrails-pre-prod-environment
plan: 04
type: execute
wave: 2
depends_on: [44-02, 44-03]
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements: [DEBT-02, DEBT-03]
nyquist_compliant: true

must_haves:
  truths:
    - "deploy.yml's Prepare-deploy-directory step calls `sh scripts/build-staging.sh deploy-staging` instead of an inline cp/sed transform — behavior-preserving extraction pinned by tests/build-staging.test.js (D-07 shared-script)"
    - "deploy.yml runs `sh scripts/cf-await-promotion.sh` AFTER the deploy-branch push; that step both awaits promotion and purges, replacing the old immediate non-blocking purge (DEBT-02)"
    - "The purge now happens ONLY after the live origin serves the new token, and a purge failure fails the run (blocking)"
    - "The Verify-no-sensitive-files defense-in-depth step is retained, operating on the staged output"
    - "deploy.yml still triggers only on push to main and still force-pushes only the deploy branch — prod topology otherwise unchanged"
  artifacts:
    - "Reworked .github/workflows/deploy.yml (modification only — the hardened prod pipeline)"
  key_links:
    - "deploy.yml -> scripts/build-staging.sh (shared transform) and -> scripts/cf-await-promotion.sh (sentinel-then-purge); both must exist and be GREEN (Wave 1) before this wiring"
  prohibitions:
    - "MUST NOT change the prod trigger (push: branches:[main]) or the deploy branch target"
    - "MUST NOT leave the purge non-blocking or running before promotion is confirmed"
    - "MUST NOT alter the docs-gate step or the deploy branch's `Deploy from <sha>` anchor subject"
---

<objective>
Wire the two Wave-1 scripts into the single most blast-radius-sensitive file in the repo, `.github/workflows/deploy.yml`. Two edits, one file, kept in one plan so they can never produce conflicting overlapping diffs: (1) replace the inline staging transform with a `build-staging.sh` call (DEBT-03 shared-script extraction, behavior-preserving); (2) replace the immediate non-blocking purge with a `cf-await-promotion.sh` call that awaits promotion then blocks on purge (DEBT-02). Everything else in the prod pipeline — trigger, docs-gate, deploy-branch push, concurrency, the sensitive-files guard — is left untouched.

Purpose: land both hardening changes to deploy.yml atomically and conservatively.
Output: a reworked deploy.yml that stages via the shared script and purges only after confirmed promotion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md
@.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md
@.github/workflows/deploy.yml
@scripts/build-staging.sh
@scripts/cf-await-promotion.sh
</context>

## Artifacts this phase produces (this plan's share)

- Modified `.github/workflows/deploy.yml` — inline transform → `build-staging.sh deploy-staging`; immediate non-blocking purge → `cf-await-promotion.sh` (sentinel-then-blocking-purge). No new files.

<tasks>

<task type="auto">
  <name>Task 1: Replace deploy.yml's inline staging transform with a build-staging.sh call</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the "Prepare deploy directory" step, lines ~42-59; the "Verify no sensitive files" step, lines ~61-73)
    - scripts/build-staging.sh (the shared transform it will now call)
    - tests/build-staging.test.js (proves the extraction is byte-faithful)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Pattern 6)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-PATTERNS.md (.github/workflows/deploy.yml MODIFIED entry)
  </read_first>
  <action>
    In `.github/workflows/deploy.yml`, replace the body of the "Prepare deploy directory" step (the inline `mkdir -p deploy-staging`, the seven `cp` whitelist lines, and the `sed` token-stamp line, ~42-59) with a single command: `sh scripts/build-staging.sh deploy-staging` (no `--noindex` for prod). `GITHUB_SHA` is already in the Actions environment and the script reads it. Leave the "Verify no sensitive files" step (~61-73) exactly as-is — it is the defense-in-depth guard on the staged output and stays. Do not touch the checkout, docs-gate, push, or concurrency steps. This is a behavior-preserving extraction: the staged tree must be byte-identical to today's inline transform, which tests/build-staging.test.js pins.
  </action>
  <verify>
    <automated>node tests/build-staging.test.js</automated>
    <automated>node tests/run-all.js</automated>
    <human-check>Diff review: the "Prepare deploy directory" step now contains only the build-staging.sh call; the "Verify no sensitive files" step is unchanged; no other step moved.</human-check>
  </verify>
  <acceptance_criteria>
    The "Prepare deploy directory" step invokes `sh scripts/build-staging.sh deploy-staging`; the "Verify no sensitive files" step is still present; the staging behavior is unchanged (tests/build-staging.test.js exits 0); `node tests/run-all.js` green.
  </acceptance_criteria>
  <done>The prod pipeline stages via the shared script; behavior unchanged; the no-leak guard intact.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the sentinel-then-purge step and remove the immediate non-blocking purge</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the "Push to deploy branch" step, lines ~75-86; the "Purge Cloudflare cache" step, lines ~88-96 — the immediate `if: success()` non-blocking purge)
    - scripts/cf-await-promotion.sh (the sentinel-then-blocking-purge script it will call)
    - tests/cf-await-promotion.test.js (the behavior contract)
    - .planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-RESEARCH.md (§ Pattern 1 + § Pattern 2 + the new failure-semantics table)
  </read_first>
  <action>
    In `.github/workflows/deploy.yml`, replace the existing "Purge Cloudflare cache" step (the immediate, non-blocking `curl … | grep … || echo "…non-blocking"`, ~88-96) with a single step placed AFTER "Push to deploy branch" that runs `sh scripts/cf-await-promotion.sh`, passing the environment it needs via an `env:` block on the step: `GITHUB_SHA` (already present in the Actions context), `CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}`, and `CF_PURGE_TOKEN: ${{ secrets.CF_PURGE_TOKEN }}`. The script awaits the live origin serving the new short-SHA token, THEN purges, and exits non-zero (failing the run) on either a promotion timeout or a purge failure — so remove the old `if: success()` swallow-the-failure form entirely; the new step blocks by virtue of the script's exit codes. Do not add a purge to any other branch or step.
  </action>
  <verify>
    <automated>node tests/cf-await-promotion.test.js</automated>
    <automated>node tests/run-all.js</automated>
    <human-check>Diff review: exactly one Cloudflare purge path remains and it is delegated to cf-await-promotion.sh, positioned after "Push to deploy branch"; the old inline non-blocking purge block is gone; the deploy trigger, docs-gate, and deploy-branch push/anchor are unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    deploy.yml contains a step running `sh scripts/cf-await-promotion.sh` after "Push to deploy branch" with the three env values wired; the purge is now blocking (failure fails the run); `node tests/cf-await-promotion.test.js` exits 0; `node tests/run-all.js` green.
  </acceptance_criteria>
  <done>Purge runs only after confirmed promotion and blocks on failure; the DEBT-02 mixed-cache class is closed in the prod pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yml → shared scripts | the prod pipeline now delegates its transform + purge to versioned, tested scripts |
| deploy.yml → deploy branch anchor | the docs-gate anchor (`Deploy from <sha>`) must remain intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-07 | Integrity | prod pipeline behavior drift from the transform extraction | medium | mitigate | Behavior-preserving extraction pinned by tests/build-staging.test.js; the Verify-no-sensitive-files guard is retained on the staged output |
| T-44-03 | Integrity / Availability | purge sequencing | high | mitigate | Purge delegated to cf-await-promotion.sh (sentinel-then-blocking-purge); no purge before confirmed promotion |
| T-44-10 | Tampering | accidental change to the deploy trigger / branch / anchor | high | mitigate | Only the transform-call and the purge step change; trigger (main), docs-gate, deploy-branch push, and the `Deploy from <sha>` anchor are left untouched (human diff-check in verify) |
| T-44-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node tests/build-staging.test.js` and `node tests/cf-await-promotion.test.js` both exit 0.
- `node tests/run-all.js` green.
- Diff review confirms only the transform-call and the purge step changed; trigger/docs-gate/anchor untouched.
- Manual/live (VALIDATION.md): the real promotion poll + zone purge are only verifiable on a live prod deploy run.
</verification>

<success_criteria>
deploy.yml stages via the shared script and purges only after confirmed promotion (blocking) — both DEBT-02 and the DEBT-03 shared-transform extraction land atomically in the one blast-radius file, prod topology otherwise unchanged.
</success_criteria>

<output>
Create `.planning/phases/44-tech-debt-guardrails-pre-prod-environment/44-04-SUMMARY.md` when done.
</output>
