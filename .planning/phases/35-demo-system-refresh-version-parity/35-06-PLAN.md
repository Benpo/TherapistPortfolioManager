---
phase: 35-demo-system-refresh-version-parity
plan: 06
type: execute
wave: 3
depends_on: [35-02, 35-03, 35-04, 35-05]
files_modified:
  - assets/app.js
  - assets/license.js
  - assets/backup-modal.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: false
requirements: [DEMO-11, DEMO-10]
nyquist_compliant: true

must_haves:
  truths:
    - "In demo mode (window.name==='demo-mode') the Backup cloud button is not mounted, the overview Export/Import controls are hidden, and the license activate/deactivate controls are disabled/hidden (D-09 / DEMO-11)."
    - "In demo mode the backup Export flow (`openExportFlow`) is blocked (toast + early-return, mirroring the existing import guard), so neither export nor import runs in demo (D-09 / DEMO-11)."
    - "All of those controls remain present and functional in the real (non-demo) app — the guard is strictly demo-scoped (no regression)."
    - "The landing demo entry point still works end-to-end: iframe loads, gates bypass, language sync both ways, navigate demo→sessions→home reseed — after all Phase 35 changes (DEMO-10)."
  artifacts:
    - "assets/app.js (modified) — mountBackupCloudButton early-returns in demo mode; a demo-mode hide pass for #exportBtn + the import control"
    - "assets/license.js (modified) — demo-mode guard disabling/hiding #license-activate-btn + #license-deactivate-btn"
  key_links:
    - "mountBackupCloudButton + initDemoMode both already key on window.name==='demo-mode' (the established seam); license.js runs on its own DOMContentLoaded (license.html does not load app.js)."
    - "Export/Import are hidden via a JS-observable signal (el.hidden / disabled), not a CSS-only display:none, so the 35-02 jsdom exposure gate can assert it."
  prohibitions:
    - "Do NOT disable these controls outside demo mode — the real app must keep working (the 35-02 normal-mode half guards this)."
    - "Do NOT treat control-hiding as the data boundary — it is UX-level exposure reduction; the real isolation remains the `demo_portfolio` DB-name split (T-DBLEAK)."
    - "Settings lock-down and broader hardening stay DEFERRED — scope is ONLY backup/export/license (D-09)."
    - "Settings→Backups tab (schedule/folder-picker) is NOT guarded this phase — it stays operable in demo by design (deferred Settings lock-down); do not silently claim 'no backup anywhere in demo.'"
---

<objective>
Hide/disable the **Backup cloud button, Export/Import, and license activate/deactivate** controls when running in demo mode (D-09 / DEMO-11) using the existing `window.name==='demo-mode'` seam — a focused vanilla-JS guard, not a redesign — then run the full-demo **DEMO-10 regression** in a real browser now that chrome (35-03), seed (35-04), and demo-hints removal (35-05) are all live.

Purpose: Ben does not want backup/export/license operable inside the sales demo. This is UX-level exposure reduction layered on top of the real `demo_portfolio` DB-name isolation. Settings + broader hardening stay deferred.
Output: Demo controls hidden (35-02 exposure gate GREEN), real app untouched, and a human-verified end-to-end demo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-demo-system-refresh-version-parity/35-RESEARCH.md
@.planning/phases/35-demo-system-refresh-version-parity/35-CONTEXT.md
@.planning/phases/35-demo-system-refresh-version-parity/35-VALIDATION.md

# Files being changed + the seams:
@assets/app.js
@assets/license.js
@assets/backup-modal.js
@demo.html
@license.html
</context>

<artifacts_produced>
- `assets/app.js` — mountBackupCloudButton demo-mode early-return; demo-mode hide for #exportBtn + import control (a small `hideDemoExposedControls()` helper called from initDemoMode, or inline guards).
- `assets/license.js` — demo-mode guard hiding/disabling #license-activate-btn + #license-deactivate-btn.
- `assets/backup-modal.js` — demo-mode early guard in `openExportFlow` (toast + early-return) mirroring the existing `openImportFlow` guard.
- `assets/i18n-{en,he,de,cs}.js` — new `toast.exportDisabledDemo` key in all four dicts (UI chrome copy, mirroring `toast.importDisabledDemo`).
- New symbol (if used): `hideDemoExposedControls` in app.js (internal).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Hide backup + export/import in demo mode (app.js — DEMO-11)</name>
  <files>assets/app.js</files>
  <read_first>
    - assets/app.js (mountBackupCloudButton ~L425 → mounts #backupCloudBtn; initDemoMode ~L263 — runs FIRST in initCommon; initCommon ~L584)
    - demo.html (overview body controls: #exportBtn L101, `.import-label` + #importInput L102-105)
    - assets/backup-modal.js (existing demo-mode guard ~L295 — the established pattern to mirror)
    - tests/35-demo-exposure.test.js (the contract: #backupCloudBtn absent, #exportBtn hidden, import hidden/disabled in demo; present in normal)
  </read_first>
  <action>
    Backup cloud button: add an early guard at the top of `mountBackupCloudButton()` — `if (typeof window !== 'undefined' && window.name === 'demo-mode') return;` — so the cloud entry point is never mounted in demo mode. (initCommon already calls updateBackupCloudState on a possibly-absent #backupCloudBtn defensively, so no follow-on breakage.)

    Export/Import: add a small `hideDemoExposedControls()` helper (or inline guard) invoked from `initDemoMode()` (which already runs in demo mode at the start of initCommon). It must, when present, set a JS-observable hidden state on the overview Export/Import controls — `#exportBtn` → `el.hidden = true`; the import `<label class="import-label">` (and/or `#importInput`) → hidden and/or its input `disabled = true`. Guard every lookup for null (these exist only on the overview/home page; initDemoMode runs on every demo page). Use the JS `hidden`/`disabled` properties (not CSS-only display:none) so the jsdom gate can assert it. Do NOT alter these controls when window.name is not 'demo-mode'.
  </action>
  <verify>
    <automated>node tests/35-demo-exposure.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - 35-demo-exposure home-half GREEN: in demo mode #backupCloudBtn absent + #exportBtn hidden + import hidden/disabled; in normal mode (index.html) #backupCloudBtn present + #exportBtn visible.
    - No new failures elsewhere in `node tests/run-all.js`.
  </acceptance_criteria>
  <done>Backup cloud + Export/Import are demo-scoped-hidden via the window.name seam; real app unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Guard license activate/deactivate in demo mode (license.js — DEMO-11)</name>
  <files>assets/license.js</files>
  <read_first>
    - assets/license.js (DOMContentLoaded init ~L339; binds #license-activate-btn click ~L417 handleActivate; #license-deactivate-btn click ~L466)
    - license.html (#license-activate-btn L428, #license-deactivate-btn L455; loads shared-chrome.js + license.js, NOT app.js)
    - tests/35-demo-exposure.test.js (license-half contract: both buttons disabled/hidden in demo; activate enabled/visible in normal)
  </read_first>
  <action>
    In license.js's DOMContentLoaded init, add a demo-mode guard: when `window.name === 'demo-mode'`, disable and/or hide `#license-activate-btn` and `#license-deactivate-btn` (set `.disabled = true` and optionally `.hidden = true`) so a demo visitor cannot trigger a real Lemon Squeezy activation/deactivation against the demo. Apply the guard AFTER the elements are resolved but such that it short-circuits/neutralizes the activate + deactivate handlers in demo mode (disabling the buttons is sufficient since handlers fire on click). Locate the activate/deactivate wiring by content. Do nothing when window.name is not 'demo-mode' (normal license flow unchanged). Optionally surface a brief inline notice that licensing is unavailable in the demo, but keep it minimal (no redesign).
  </action>
  <verify>
    <automated>node tests/35-demo-exposure.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 35-demo-exposure license-half GREEN: demo mode → #license-activate-btn and #license-deactivate-btn disabled/hidden; normal mode → activate enabled/visible.
    - Normal license activation/deactivation logic is untouched (no behavior change off demo-mode).
  </acceptance_criteria>
  <done>License activate/deactivate is neutralized in demo mode, real licensing unchanged.</done>
</task>

<task type="auto">
  <name>Task 2.5: Guard the backup Export flow in demo mode (backup-modal.js — DEMO-11)</name>
  <files>assets/backup-modal.js, assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/backup-modal.js (openExportFlow ~L236 — currently has NO demo guard; openImportFlow demo guard ~L295 is the established pattern: after the App/BM resolution it checks `window.name === 'demo-mode'`, shows a toast, and early-returns a resolved Promise)
    - assets/i18n-en.js (toast.importDisabledDemo ~L213 — read it first; mirror its wording/shape for the export key)
    - tests/35-demo-exposure.test.js (the backup-export contract from 35-02: in demo, `openExportFlow` must NOT call the export primitive and should request the disabled toast; in normal it reaches the primitive)
  </read_first>
  <action>
    In `openExportFlow` (backup-modal.js ~L236), add an early demo guard mirroring the existing `openImportFlow` guard at L295. Place it AFTER the `App`/`BM` resolution (after the `if (!BM || !App) return Promise.resolve(null);` line) and BEFORE the `BM.exportEncryptedBackup()` call: when `typeof window !== 'undefined' && window.name === 'demo-mode'`, call `App.showToast('', 'toast.exportDisabledDemo')` (guarded by `typeof App.showToast === 'function'`) and `return Promise.resolve(null);`. Do nothing when `window.name` is not 'demo-mode' — the normal export path stays untouched.

    Add a new i18n key `toast.exportDisabledDemo` to ALL four dicts (assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js), placed adjacent to the existing `toast.importDisabledDemo` key (~L213) and mirroring its wording/shape (e.g. EN: "Export is not available in demo mode"). This is UI chrome copy — not seed clinical content — so the seed English-only rule does NOT apply here: it SHOULD be localized in all four languages.
  </action>
  <verify>
    <automated>node tests/35-demo-exposure.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - In demo mode, `openExportFlow` returns early WITHOUT calling `BM.exportEncryptedBackup`/`BM.exportBackup` and shows the disabled toast.
    - In normal mode, `openExportFlow` reaches the export primitive (no regression).
    - `toast.exportDisabledDemo` resolves in all four dicts (en/he/de/cs).
    - The 35-02 backup-export assertion goes GREEN.
  </acceptance_criteria>
  <done>The backup Export flow is demo-guarded (toast + early-return) mirroring the import guard; export is blocked in demo, untouched in normal; the new toast key resolves in all 4 languages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DEMO-10 full-demo regression + DEMO-11 visual check (real browser)</name>
  <files>demo.html, landing.html</files>
  <action>
    Pause for a BLOCKING human verification of the complete demo flow in a real browser (DEMO-10 + the DEMO-11 visual confirmation), after Tasks 1-2 and all prior Phase 35 plans (35-03/04/05) are merged and every automated gate is GREEN. Walk the steps below on Ben's machine; only mark complete on approval, otherwise capture the specific regression(s) and route them back to the owning task/plan.
  </action>
  <what-built>
    The complete Phase 35 demo: single-sourced chrome + version footer (35-03), refreshed Heart-Shield seed with self-freshening dates (35-04), demo-hints removed (35-05), and backup/export/license hidden in demo mode (this plan).
  </what-built>
  <how-to-verify>
    On Ben's machine in a real browser, open `landing.html` and scroll to the `#demo` section, then verify (35-VALIDATION DEMO-10 + DEMO-11):
    1. The demo loads in the iframe with NO redirect to landing/license (the per-page demo-mode gate short-circuits).
    2. Switch language in the parent landing page → the demo iframe re-translates (postMessage path); switch language via the in-demo globe picker → it also re-translates (both reach App.setLanguage and coexist).
    3. The demo home shows the footer with the current version, the live-demo banner, exactly one language picker, and ≥1 "Session This Month".
    4. The Heart-Shield client shows the removed (✅) badge; open it and confirm the active→removed session arc.
    5. Navigate demo → a client's sessions → back to the demo home: returning to home reseeds (in-demo edits reset) — expected behavior.
    6. DEMO-11: inside the demo — the Backup cloud icon is absent, the overview Export/Import buttons are hidden, the backup modal's Export is blocked (if you reach it, it shows a "not available in demo" toast and does nothing — no file is produced), and on the license page (footer License link) the Activate/Deactivate controls are disabled/hidden — all only inside the demo. KNOWN RESIDUAL (do NOT flag as a regression): Settings → Backups (the gear → Backups tab, schedule + OS folder picker) is still reachable and operable in the demo this phase BY DESIGN — Settings lock-down is explicitly deferred (D-09 refinement). So "backup is fully gone from the demo" is NOT the claim; the claim is home backup entries hidden + restore blocked + modal export blocked, with Settings→Backups left for the future Settings phase.
    7. Console: no 404 for demo-hints.js, no SW install error.
  </how-to-verify>
  <resume-signal>Type "approved" if the full demo flow + control-hiding are correct, or describe the specific regression(s) observed.</resume-signal>
  <verify>
    <human-check>In a real browser, the landing → #demo iframe loads with no redirect, language sync works both ways, the Heart-Shield seed + version footer render, reseed-on-home works, and backup/export/license controls are hidden in demo — with no console 404/SW errors.</human-check>
  </verify>
  <done>The full Phase 35 demo passes the DEMO-10 regression and DEMO-11 visual check in a real browser; Ben has approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo visitor → backup/export/license controls | Demo must not let a visitor run backups, import/export data, or hit the Lemon Squeezy activation API |
| demo context → real DB | `window.name` keeps the demo on `demo_portfolio`; control-hiding is UX-level on top of it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-DBLEAK | Tampering | demo control exposure | low | mitigate | Hide the backup cloud button + overview Export/Import in demo, AND add a demo guard to `openExportFlow` (Task 2.5) so the backup modal's Export path is also blocked (toast + early-return) — closing the last demo export route; the real boundary remains the `demo_portfolio` DB-name isolation, unchanged |
| T-LICENSE | Spoofing/Tampering | license.js activate in demo | medium | mitigate | Disable activate/deactivate in demo so a visitor cannot fire a real LS activation against the demo |
| T-SETTINGS-RESID | Tampering | Settings→Backups tab in demo | low | accept | Settings→Backups (schedule + OS folder picker) stays operable in demo this phase by design — explicitly DEFERRED to the future Settings exposure phase (D-09 refinement). Documented residual, not a silent gap; the data boundary remains the `demo_portfolio` DB-name isolation |
| T-IFRAME | Elevation | landing iframe | low | accept | Sandbox unchanged; guard is in-page JS keyed on window.name |
| T-35-SC | Tampering (supply chain) | installs | low | accept | No new packages (zero-dependency rule) |
</threat_model>

<verification>
- 35-02 exposure gate (DEMO-11) GREEN in both demo + normal modes; `node tests/run-all.js` fully green (all Phase 35 gates now GREEN).
- DEMO-10 blocking human-verify approved in a real browser (manual, per 35-VALIDATION Manual-Only Verifications).
</verification>

<success_criteria>
Backup/export/license controls are hidden in demo mode and present in the real app (D-09 / DEMO-11), and the full demo flow passes the DEMO-10 regression in a real browser with no console errors.
</success_criteria>

<output>
Create `.planning/phases/35-demo-system-refresh-version-parity/35-06-SUMMARY.md` when done.
</output>
