---
phase: 43-docs-maintenance-hard-gate
plan: 04
type: execute
wave: 2
depends_on: [43-02]
files_modified:
  - assets/help-content-he.js
  - assets/help-content-de.js
  - assets/help-content-cs.js
  - tests/help-integrity-locale.test.js
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "D-20: covers[] becomes EN-only — it is stripped from the three locale help files (he/de/cs); covers[] is repo metadata, EN-canonical, never translatable content."
    - "D-20: the locale parity test's covers-comparison block is deleted so the suite stays green after the strip; the rest of the parity assertions (ids, group, featured, priority, {ui:} tokens) remain."
    - "Runtime-safe (verified): assets/help.js has zero reads of covers[], so stripping it from locale files changes no runtime behavior."
  artifacts:
    - assets/help-content-he.js
    - assets/help-content-de.js
    - assets/help-content-cs.js
  key_links:
    - "The parity-test edit targets the POST-RENAME file tests/help-integrity-locale.test.js (renamed in 43-02) — that is why this plan depends on 43-02 and lands in a later wave."
    - "The exact block to delete is the enCovers/lcCovers JSON.stringify comparison (formerly tests/42_1-help-integrity.test.js:244-248)."
---

<objective>
Make covers[] EN-only (D-20): strip it from the three locale help files and delete the one
locale parity assertion that would otherwise go red.

Purpose: covers[] is gate metadata, not translatable content. Duplicating it ×4 was a design
flaw; the gate reads only the EN canonical. Fix it now while the phase touches these files.

Output:
- covers[] removed from `assets/help-content-{he,de,cs}.js`.
- The covers-parity block removed from `tests/help-integrity-locale.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-docs-maintenance-hard-gate/43-CONTEXT.md
@.planning/phases/43-docs-maintenance-hard-gate/43-RESEARCH.md
@tests/42_1-help-integrity.test.js

Runtime safety (VERIFIED, RESEARCH §Q-G): `grep -n covers assets/help.js` → NONE. The only other
`covers` hits in assets/*.js are unrelated prose comments. Stripping covers[] from locale files is
runtime-safe. The ONLY assertion that goes RED is the enCovers/lcCovers JSON.stringify comparison
(formerly lines 244-248 of the locale help test): when a locale topic loses covers[], `lcTp.covers`
is undefined and JSON.stringify(undefined) !== the EN JSON string → throws for all he/de/cs. Delete
that block; nothing else in the test references covers.

NOTE: this plan edits the POST-RENAME filename `tests/help-integrity-locale.test.js` (43-02 renamed
`42_1-help-integrity.test.js`). If the rename has not run, STOP — 43-02 is a prerequisite.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip covers[] from the three locale help files (D-20)</name>
  <files>assets/help-content-he.js, assets/help-content-de.js, assets/help-content-cs.js</files>
  <action>
    Remove the `covers` property from every topic object in `assets/help-content-he.js`,
    `assets/help-content-de.js`, and `assets/help-content-cs.js`. Remove ONLY the covers[] metadata —
    leave every other field (id, priority, title, body, {ui:} tokens) and all translated prose exactly
    as-is. Do not touch `assets/help-content-en.js` (EN keeps covers[] — it is the gate's canonical
    source, backfilled in 43-03). Comments/headers: do not add or edit phase/decision-ID citations.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),vm=require('vm');for(const loc of ['he','de','cs']){const sb={window:{I18N:{},QUOTES:{}},console:{log(){},warn(){},error(){}}};vm.createContext(sb);for(const f of ['i18n-'+loc+'.js','help-content-'+loc+'.js']){vm.runInContext(fs.readFileSync('assets/'+f,'utf8'),sb,{filename:f})}const S=sb.window['HELP_CONTENT_'+loc.toUpperCase()];let n=0;(S||[]).forEach(x=>(x.topics||[]).forEach(t=>{if('covers' in t)n++}));if(n){console.error('FAIL '+loc+' still has covers on '+n+' topics');process.exit(1)}console.error(loc+' clean')}console.log('OK all locales covers-free')"</automated>
  </verify>
  <done>
    No topic in any of the three locale help files carries a covers property; all translated prose,
    ids, priorities and {ui:} tokens are unchanged; EN is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the covers-parity block from the locale integrity test (D-20)</name>
  <files>tests/help-integrity-locale.test.js</files>
  <action>
    In `tests/help-integrity-locale.test.js` (the file 43-02 renamed from `42_1-help-integrity.test.js`),
    delete the covers-parity assertion — the `enCovers`/`lcCovers` JSON.stringify comparison block that
    throws when a locale topic's covers[] differs from EN's. Remove ONLY that block. Keep every other
    parity assertion in the same test (section ids, group, featured flag, topic ids, priority, {ui:}
    tokens) — those stay valid and must still run. Do not edit phase/decision-ID citations in comments.
  </action>
  <verify>
    <automated>node tests/help-integrity-locale.test.js && ! grep -q 'lcCovers' tests/help-integrity-locale.test.js && echo LOCALE-PARITY-OK</automated>
  </verify>
  <done>
    The locale help integrity test passes with the covers-parity block removed; no other parity
    assertion was touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale content → runtime | help.js renders locale content; the strip must not remove anything help.js reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-07 | Denial of function | stripping covers[] breaks a runtime read in help.js | low | mitigate | VERIFIED help.js has zero covers reads (RESEARCH Q-G); the strip removes only metadata; locale integrity test still runs. |
| T-43-08 | Tampering | over-broad edit removes translated prose or ids | low | mitigate | Task 1 verify loads each locale and asserts only covers is gone; ids/priority/{ui:} parity still asserted by the locale test. |
| T-43-SC | Tampering | supply chain | low | accept | No packages installed; verify uses node built-ins only. |
</threat_model>

<verification>
- All three locale help files load and no topic carries a covers property (Task 1 verify).
- `node tests/help-integrity-locale.test.js` passes with the covers-parity block removed (Task 2 verify).
- `node tests/help-integrity.test.js` stays green (EN covers[] untouched here).
</verification>

<success_criteria>
- covers[] is EN-only: absent from he/de/cs, present on EN.
- The one locale parity assertion that would go red is removed; all other parity checks remain.
- No runtime behavior changes; no translated prose altered.
</success_criteria>

<output>
Create `.planning/phases/43-docs-maintenance-hard-gate/43-04-SUMMARY.md` when done. Note the number of
locale topics stripped per file and confirm the locale parity test still asserts id/group/priority/{ui:} parity.
</output>