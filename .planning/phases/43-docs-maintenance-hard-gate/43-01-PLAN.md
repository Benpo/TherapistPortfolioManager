---
phase: 43-docs-maintenance-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/docs-gate.test.js
  - tests/docs-gate-role-table.test.js
autonomous: true
requirements: [GATE-01, GATE-03, GATE-04]
must_haves:
  truths:
    - "D-21: an automated RED/GREEN behavior test exists and is authored BEFORE scripts/docs-gate.js — it fails RED (exits non-zero) until the gate lands, and that redness is the proof the test is falsifiable."
    - "GATE-01: the behavior test asserts a staged watched-code change with no changelog entry BLOCKS (exit non-zero, message names the file), and adding the entry flips it to PASS (exit 0)."
    - "GATE-03: the behavior test asserts each of the three trailers (Help-Unaffected, Changelog-Unaffected, Docs-Emergency-Skip) flips a BLOCK to a PASS; a separate role-table test asserts the D-17 #4 self-consistency invariant."
    - "GATE-04: the behavior test asserts an APP_VERSION bump with a matching non-empty-highlights entry PASSES, a missing entry BLOCKS, and an origin:true entry is tolerated."
    - "D-23: both new test files are named for the behavior they guard with no phase-number prefix (docs-gate.test.js, docs-gate-role-table.test.js)."
  artifacts:
    - tests/docs-gate.test.js
    - tests/docs-gate-role-table.test.js
  key_links:
    - "The behavior test invokes the REAL scripts/docs-gate.js (built in plan 43-06) with cwd set to a throwaway git repo — the gate must resolve its assets from the repo root (cwd), not its own install directory (see 43-02 / 43-06)."
    - "The role-table test requires scripts/lib/role-table.js + checkRoleTable() (built in 43-05) — RED until Wave 3."
---

<objective>
Author the two Wave-1 RED-first tests that specify the gate's behavior before any gate
code exists (D-21, the project's standing "behavior tests before implementation" rule).

Purpose: These tests ARE the executable specification of GATE-01/03/04. A gate that
always passes also ships green — only a test that can go RED proves the gate can block.
Both tests are EXPECTED to fail RED at this plan's commit boundary; that is the deliverable.

Output:
- `tests/docs-gate.test.js` — throwaway-git-repo RED/GREEN harness exercising the real gate.
- `tests/docs-gate-role-table.test.js` — the D-17 invariant #4 (role-table self-consistency).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-docs-maintenance-hard-gate/43-CONTEXT.md
@.planning/phases/43-docs-maintenance-hard-gate/43-RESEARCH.md
@.planning/phases/43-docs-maintenance-hard-gate/43-PATTERNS.md
@tests/37-date-format.test.js
@tests/run-all.js

RED-first precedent (header prose): `tests/42-changelog-integrity.test.js` — "Authored
BEFORE ... fails RED for the right reason (module absent)."

Verified test recipe: RESEARCH §Q-H (throwaway repo, bare origin, GIT_CONFIG_GLOBAL=/dev/null,
execFileSync exit+stderr capture, synthesize a MINIMAL fixture corpus — do NOT copy the real
help-content-en.js, per the phase-31 test-shape-coupling learning).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED/GREEN behavior test for the docs gate</name>
  <files>tests/docs-gate.test.js</files>
  <action>
    Create the throwaway-git-repo behavior test per RESEARCH §Q-H. Build a temp dir with
    `fs.mkdtempSync(path.join(os.tmpdir(),'docsgate-'))`; init a `--bare` origin, clone it to a
    work repo, set per-repo `user.email`/`user.name`, and isolate identity/config with
    `GIT_CONFIG_GLOBAL=/dev/null` and `GIT_CONFIG_SYSTEM=/dev/null` on every git call. Clean up
    with `fs.rmSync(tmp,{recursive:true,force:true})` in a `finally`.

    SYNTHESIZE a minimal fixture corpus inside the work repo's `assets/` — a tiny
    `help-content-en.js` (about 2 sections / 3 topics, a few covers[] paths pointing at fixture
    files that exist in the temp repo), `changelog-content-en.js` (a current-version entry plus
    one origin:true entry with no highlights), `version.js` (an APP_VERSION), plus `i18n-en.js`
    if the loader needs `window.I18N.en`. Do NOT copy the real corpus — a minimal synthetic one
    makes each RED cause unambiguous and stays decoupled from the D-19 backfill.

    Commit a baseline on `main` and push to the bare origin so `origin/main` resolves. Then
    exercise the REAL gate via `execFileSync(process.execPath, [GATE, '--range', <range>], {cwd:
    workRepo, env})`, catching non-zero via `e.status`/`e.stderr`. GATE path = the real
    `scripts/docs-gate.js` at the outer repo root. Assert these cases:
      - CHANGELOG BLOCK/PASS (GATE-01): stage a watched code edit (e.g. a fixture `assets/app.js`)
        with no changelog edit and no trailer → exit non-zero, stderr mentions changelog and names
        the file; then add a changelog bullet → exit 0.
      - HELP BLOCK/PASS (GATE-01, D-11): edit a covered fixture file without touching its claiming
        topic → block, stderr names the topic id+title; then add a `Help-Unaffected: <file> — reason`
        trailer → exit 0.
      - UNCOVERED BLOCK (D-12): edit a watched fixture file that no topic covers → block with the
        "add it to a covers[] or declare Help-Unaffected" guidance.
      - TRAILERS (GATE-03, D-14): each of `Help-Unaffected:`, `Changelog-Unaffected:`, and
        `Docs-Emergency-Skip:` flips its respective BLOCK to PASS; assert `Docs-Emergency-Skip:`
        prints a loud banner naming the commit + reason + skipped files.
      - RELEASE MOMENT (GATE-04, D-08): bump the fixture APP_VERSION with a matching entry whose
        highlights is non-empty → pass; remove that entry → block; the origin:true entry with no
        highlights must NOT trip the gate.
    The gate binary does not exist yet, so `execFileSync` throws ENOENT — the test fails RED for
    the right reason. Structure it so that once `scripts/docs-gate.js` lands (43-06) every case
    passes. Follow the tests/run-all.js exit contract (exit 0 all-pass, non-zero on any failure),
    `'use strict'`, and behavior-only comments (no phase/decision-ID citations — that is Phase 44).
  </action>
  <verify>
    <automated>node -c tests/docs-gate.test.js && echo "SYNTAX-OK (RED expected at runtime until 43-06)"</automated>
  </verify>
  <done>
    File is valid JS. Running `node tests/docs-gate.test.js` exits NON-ZERO (RED) because
    scripts/docs-gate.js is absent — this RED is the intended deliverable state; do NOT stub the
    gate to make it pass. It is designed to go GREEN when 43-06 lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Role-table self-consistency test (D-17 invariant #4)</name>
  <files>tests/docs-gate-role-table.test.js</files>
  <action>
    Create the invariant #4 test. It requires the role-table definition built in 43-05
    (`require('../scripts/lib/role-table.js')`, or `checkRoleTable()` from
    `../scripts/lib/invariants.js` — match whichever surface 43-05 exposes; state the expectation
    in a header so 43-05 satisfies it). Assert the role table is internally self-consistent:
      - No single path is BOTH denylisted AND a watched trigger.
      - No satisfier file (help-content-*.js, changelog-content-*.js) is also treated as a trigger
        (D-07 — satisfiers never raise their own demand).
      - The watched-extension set is exactly {.js, .css, .html} plus the explicitly-named
        manifest.json and sw.js (OD-1 WATCH-CODE-ONLY), and nothing else (no .png/.woff2/.json/.txt).
      - Every denylisted path is a real shipped file (no dangling denylist entries).
    Follow the exit-0/1 contract, `'use strict'`, behavior-only comments.
  </action>
  <verify>
    <automated>node -c tests/docs-gate-role-table.test.js && echo "SYNTAX-OK (RED expected until 43-05)"</automated>
  </verify>
  <done>
    File is valid JS. Running it exits NON-ZERO (RED) because scripts/lib/role-table.js is absent —
    intended until 43-05 lands. Do NOT create a stub role table here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit author → gate verdict | Author-supplied commit messages / trailers are untrusted input the gate parses; the test must prove the gate reads them via git's trailer parser, not a body regex. |
| test harness → host git config | The throwaway repo must not read or mutate Ben's real gitconfig or the real repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | trailer-looking text pasted into a commit body (code fence) | low | mitigate | Test includes a decoy: a `Help-Unaffected:`-looking line inside a fenced block that must NOT satisfy the gate (git %(trailers) scopes to the trailer block; RESEARCH Q-C). |
| T-43-02 | Denial of function | temp-repo harness leaks dirs or reads host gitconfig | low | mitigate | `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM`=/dev/null on every git call; `fs.rmSync` cleanup in `finally`. |
| T-43-SC | Tampering | supply chain | low | accept | This plan installs no packages; uses only node built-ins (fs/os/path/child_process). |
</threat_model>

<verification>
- `node -c` passes on both files (valid JS, auto-discovered by tests/run-all.js glob).
- Both tests fail RED at this commit (implementations absent) — this is expected and correct.
- The behavior test's cleanup leaves no temp dirs (`ls $TMPDIR/docsgate-* 2>/dev/null` empty after a run).
</verification>

<success_criteria>
- Two RED-first test files exist, both syntactically valid, both failing RED for the right reason.
- The behavior test covers all five case-families (changelog block/pass, help block/pass, uncovered
  block, three trailers, release moment) against a synthetic fixture corpus.
- Names follow D-23 (no phase-number prefix).
</success_criteria>

<output>
Create `.planning/phases/43-docs-maintenance-hard-gate/43-01-SUMMARY.md` when done. Note explicitly
that both tests are RED by design and name the plans that turn them GREEN (43-05 role-table, 43-06 gate).
</output>
