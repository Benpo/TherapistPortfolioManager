---
phase: 43-docs-maintenance-hard-gate
plan: 05
type: execute
wave: 3
depends_on: [43-02, 43-03]
files_modified:
  - scripts/lib/role-table.js
  - scripts/lib/invariants.js
  - tests/help-integrity.test.js
  - tests/changelog-integrity.test.js
autonomous: true
requirements: [GATE-01, GATE-03]
must_haves:
  truths:
    - "D-05/D-06/D-07 + OD-1: role-table.js is the written, checkable definition of a user-facing change — WATCH-CODE-ONLY (.js/.css/.html + manifest.json + sw.js), a legal/marketing denylist (pages + scripts + the landing/demo stylesheets), and satisfiers (help-content/changelog-content) that never raise their own demand."
    - "D-17: the four gate invariants live in ONE shared module (scripts/lib/invariants.js) — HELP-MAP freshness (#1, reusing 43-03's check), covers-paths-exist (#2), changelog schema (#3, reusing the changelog test's existing logic), role-table self-consistency (#4)."
    - "D-17: the renamed integrity tests call the shared invariants so npm test and gsd-verifier still catch drift — one implementation, two callers."
  artifacts:
    - scripts/lib/role-table.js
    - scripts/lib/invariants.js
  key_links:
    - "invariant #1 (HELP-MAP freshness) requires the generator + committed map from 43-03; invariant #4 requires role-table.js created here — both are why this plan is Wave 3."
    - "tests/docs-gate-role-table.test.js (43-01, RED) goes GREEN when role-table.js + checkRoleTable() land here."
    - "docs-gate.js (43-06) requires this module and runs all four invariants FIRST, before the push-range rule (D-17 order)."
---

<objective>
Build the shared role-table (the written GATE-03 definition) and the shared invariants module
(D-17's four file-on-disk checks), and wire the renamed integrity tests to call them.

Purpose: The role table operationalizes D-05/06/07 under OD-1 WATCH-CODE-ONLY into one checkable
place. The invariants module is the "one implementation" both the gate and the test suite run, so
CI gets the invariants for free and npm test still catches drift.

Output:
- `scripts/lib/role-table.js` (watched types + denylist + satisfiers + the written definition).
- `scripts/lib/invariants.js` (four invariant functions), called by the two renamed integrity tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-docs-maintenance-hard-gate/43-CONTEXT.md
@.planning/phases/43-docs-maintenance-hard-gate/43-PATTERNS.md
@tests/changelog-integrity.test.js
@scripts/lib/help-loader.js
@scripts/gen-help-map.js

Role table (OD-1 WATCH-CODE-ONLY, orchestrator override of RESEARCH Q-F):
- Watched types (a change here can demand a changelog/help update): file extensions `.js`, `.css`,
  `.html`, PLUS the explicitly-named `manifest.json` and `sw.js`. Nothing else — `.png/.jpg/.ico/.woff2/
  .txt/.json` (incl. assets/demo-seed-data.json) are ignored entirely (neither trigger nor satisfier).
- Denylist (D-06, legal + marketing surfaces — pages AND their scripts): impressum*/datenschutz*/
  disclaimer* (×4 each), landing.html, demo.html; assets/landing.js, assets/demo.js, assets/demo-seed.js,
  assets/disclaimer.js, assets/i18n-disclaimer.js; PLUS the D-06 gap extension → assets/landing.css,
  assets/demo.css (see Deviations — needs Ben's ack).
- Vendor bundles jspdf.min.js / jszip.min.js / bidi.min.js are NOT denylisted (watched, D-06).
- Satisfiers (D-07, never triggers): assets/help-content-{en,he,de,cs}.js satisfy help;
  assets/changelog-content-{en,he,de,cs}.js satisfy changelog.

Changelog schema logic to reuse (invariant #3): `tests/changelog-integrity.test.js` already asserts
unique semver versions, reverse-chronological order, highlights length 2–4 on non-origin entries, and
the origin:true special-case (no highlights). Do NOT duplicate — expose it as the shared check.
</context>

<tasks>

<task type="auto">
  <name>Task 1: role-table.js — the written, checkable "user-facing change" definition</name>
  <files>scripts/lib/role-table.js</files>
  <action>
    Create `scripts/lib/role-table.js` exporting the role classification: the watched extension set
    (`.js`, `.css`, `.html`) plus the named singletons (`manifest.json`, `sw.js`); the denylist array
    (legal + marketing pages, their scripts, and the landing/demo stylesheets per the D-06 gap
    extension); the satisfier matcher (help-content-*.js / changelog-content-*.js). Provide helper
    predicates: `isWatched(path)`, `isDenylisted(path)`, `isSatisfier(path)`, and a `classify(path)`
    returning trigger | satisfier | denylisted | ignored. The module's HEADER comment is the WRITTEN
    GATE-03 definition — a short, checkable statement of what counts as user-facing, including the
    OD-1 accepted limitation stated plainly: an image/font/JSON change (e.g. a new logo or hero
    illustration) ships with NO changelog demand and the gate stays silent. Behavior-only comments,
    no phase/decision-ID citations.
  </action>
  <verify>
    <automated>node -e "const r=require('./scripts/lib/role-table.js');const a=(c,p)=>{if(r.classify(p)!==c){console.error('FAIL',p,'=>',r.classify(p),'want',c);process.exit(1)}};a('trigger','assets/app.js');a('trigger','assets/app.css');a('trigger','index.html');a('trigger','assets/jspdf.min.js');a('denylisted','assets/landing.js');a('denylisted','landing.html');a('denylisted','assets/landing.css');a('denylisted','assets/demo.css');a('satisfier','assets/help-content-en.js');a('satisfier','assets/changelog-content-de.js');a('ignored','assets/branding/logo.png');a('ignored','assets/demo-seed-data.json');console.log('OK role-table')"</automated>
  </verify>
  <done>
    role-table.js classifies watched code (.js/.css/.html + manifest/sw) as triggers, legal/marketing
    surfaces (incl. landing/demo .css) as denylisted, help/changelog content as satisfiers, and non-code
    assets as ignored. Its header states the written definition and the OD-1 accepted limitation.
  </done>
</task>

<task type="auto">
  <name>Task 2: invariants.js — the four D-17 gate invariants, one shared module</name>
  <files>scripts/lib/invariants.js</files>
  <action>
    Create `scripts/lib/invariants.js` exporting four functions, each returning a result the caller can
    act on (throw or `{ok, errors[]}` — pick one shape and use it consistently):
      - `checkHelpMapFresh(repoRoot)` — reuse `scripts/gen-help-map.js`'s check mode (require it; do NOT
        re-implement the map generation) to assert the committed HELP-MAP.md byte-matches a fresh regen.
      - `checkCoversExist(assetsDir)` — every path named in any EN topic's covers[] exists on disk.
      - `checkChangelogSchema(assetsDir)` — reuse the changelog schema logic (unique versions, reverse
        chronological, highlights 2–4 on non-origin, origin:true tolerated); expose it as the single impl.
      - `checkRoleTable()` — require role-table.js and assert self-consistency: no path both denylisted
        and a watched trigger; no satisfier is also a trigger; watched set is exactly the OD-1 types; every
        denylist entry is a real shipped file.
    Resolve the assets dir from an argument (default repo-root relative), matching help-loader's contract,
    so the gate can point it at a target repo. Fail closed on any error (D-04). Behavior-only comments.
  </action>
  <verify>
    <automated>node -e "const i=require('./scripts/lib/invariants.js');['checkHelpMapFresh','checkCoversExist','checkChangelogSchema','checkRoleTable'].forEach(f=>{if(typeof i[f]!=='function'){console.error('FAIL missing',f);process.exit(1)}});i.checkRoleTable();i.checkCoversExist();i.checkChangelogSchema();i.checkHelpMapFresh(process.cwd());console.log('OK all four invariants pass on live repo')"</automated>
  </verify>
  <done>
    invariants.js exports the four checks; all four pass on the live repo; #1 reuses gen-help-map's
    check mode and #4 requires role-table.js (no logic duplicated).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the renamed integrity tests + turn the role-table test GREEN</name>
  <files>tests/help-integrity.test.js, tests/changelog-integrity.test.js</files>
  <action>
    Wire the shared module into the renamed integrity tests so npm test exercises the same code the gate
    runs. In `tests/help-integrity.test.js` add calls to `checkHelpMapFresh()` and `checkCoversExist()`
    (the two new file-on-disk invariants). In `tests/changelog-integrity.test.js` add a call to
    `checkChangelogSchema()`. Keep the existing inline assertions in both tests intact (do NOT rip out
    working schema/content assertions — the phase-31 test-shape-coupling learning warns that
    behavior-preserving extractions silently weaken coverage; additive wiring is the safe path). This
    also confirms the tests/docs-gate-role-table.test.js from 43-01 now passes, since role-table.js +
    checkRoleTable() exist. Comments: behavior-only, do not edit phase/decision-ID citations that already
    live in these files' headers (Phase 44 owns that).
  </action>
  <verify>
    <automated>node tests/help-integrity.test.js && node tests/changelog-integrity.test.js && node tests/docs-gate-role-table.test.js && echo INTEGRITY-WIRED-OK</automated>
  </verify>
  <done>
    Both renamed integrity tests pass while now calling the shared invariants; the previously-RED
    tests/docs-gate-role-table.test.js is GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role table → gate decision | The role table is the single source of "what is user-facing"; an inconsistent table would let a real change through or block a non-change. |
| shared invariants → CI | The gate runs these invariants in CI (43-06/07); a weakened invariant weakens the whole gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-09 | Tampering | role table has an internal contradiction (path both denylisted and trigger) | medium | mitigate | checkRoleTable() self-consistency invariant (#4) + tests/docs-gate-role-table.test.js run in the suite and in the gate. |
| T-43-10 | Repudiation | invariant logic drifts between module and tests | low | mitigate | One implementation in invariants.js; tests call it rather than fork it (D-17). |
| T-43-11 | Information disclosure | OD-1 accepted gap — image/font change ships silently | low | accept | Stated plainly in role-table.js's header (the written definition); surfaced in Deviations for Ben's ack. |
| T-43-SC | Tampering | supply chain | low | accept | No packages installed; module uses node built-ins + in-repo siblings only. |
</threat_model>

<verification>
- role-table.js classifies triggers/denylist/satisfiers/ignored correctly (Task 1).
- All four invariants pass on the live repo (Task 2).
- Both renamed integrity tests pass while calling the shared module; role-table test is GREEN (Task 3).
- `npm test` stays green.
</verification>

<success_criteria>
- The written GATE-03 definition exists as a checkable role table with self-consistency enforced.
- The four D-17 invariants live in one module, reused (not duplicated) by the gate and the tests.
- 43-01's role-table test flips RED → GREEN.
</success_criteria>

<deviations>
## Open Questions / Deviations — read at plan approval

- **D-06 denylist gap (needs Ben's ack):** D-06 denylists `landing.html`+`assets/landing.js` and
  `demo.html`+`assets/demo.js` under "a page and its script are one surface," but never names
  `assets/landing.css` / `assets/demo.css`. Under OD-1 WATCH-CODE-ONLY those stylesheets are now
  watched-and-uncovered and would permanently BLOCK. By D-06's own rationale they belong on the denylist
  (page + script + stylesheet = one surface). This plan ADDS them to the denylist. If Ben rejects the
  extension, remove those two entries and accept that touching landing/demo CSS costs a trailer.
- **OD-1 accepted cost (already agreed, stated for the record):** image/font/JSON changes (a logo swap,
  a new hero illustration) ship with NO changelog demand — the gate stays silent by design. Written into
  role-table.js's header, not hidden.
</deviations>

<output>
Create `.planning/phases/43-docs-maintenance-hard-gate/43-05-SUMMARY.md` when done. Restate the D-06 CSS
denylist decision and confirm whether Ben acked it.
</output>