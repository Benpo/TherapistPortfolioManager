---
phase: 43-docs-maintenance-hard-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/help-loader.js
  - tests/39-help-integrity.test.js
  - tests/42-changelog-integrity.test.js
  - tests/42_1-help-integrity.test.js
  - tests/42_1-changelog-integrity-locale.test.js
  - tests/28-04-integrity-state.test.js
  - tests/40-i18n-parity.test.js
  - tests/29-01-crashlog-capture.test.js
  - assets/help-content-en.js
  - assets/help-content-he.js
  - assets/help-content-de.js
  - assets/help-content-cs.js
  - assets/changelog-content-en.js
  - assets/changelog-content-he.js
  - assets/changelog-content-de.js
  - assets/changelog-content-cs.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "D-17: the vm-sandbox content loader lives in ONE shared module (scripts/lib/help-loader.js) that both the gate and the renamed integrity tests will require — one implementation, two callers."
    - "D-22: the five standing integrity guards are renamed to drop phase-number prefixes; replace-all runs on LIVE FILES ONLY (allowlist); the before/after grep post-condition proves no historical artifact was clobbered and no live filename reference was missed."
    - "D-22: the rename map is recorded in .planning/codebase/TESTING.md and its stale test-file count (says 106) is corrected to the live figure (164)."
    - "D-23: the new names follow the {slug}.test.js convention (no phase number) even though the rule itself is authored in Phase 44."
  artifacts:
    - scripts/lib/help-loader.js
    - tests/help-integrity.test.js
    - tests/changelog-integrity.test.js
    - tests/help-integrity-locale.test.js
    - tests/changelog-integrity-locale.test.js
    - tests/update-integrity-state.test.js
    - .planning/codebase/TESTING.md
  key_links:
    - "help-loader.js parameterizes the assets directory so the gate can point it at a throwaway fixture repo (43-01) and at a TARGET repo root (43-06) — NOT its own __dirname."
    - "tests/run-all.js discovers tests/*.test.js by glob, so renames need no wiring; a require of ../scripts/lib/help-loader.js is invisible to the glob (it only spawns .test.js files)."
---

<objective>
Lay the Wave-1 foundation: extract the shared vm-sandbox content loader (D-17) and execute
the D-22 test-rename pass with its high-conviction before/after grep post-condition.

Purpose: The loader is the single parsing substrate the gate, the map generator, and the
integrity tests all share. The rename disambiguates the "integrity" family and drops phase
prefixes Phase 39 should never have carried — done now while the phase touches these files anyway.

Output:
- `scripts/lib/help-loader.js` — parameterized vm loader for help/changelog content files.
- Five renamed test files + a rename map in TESTING.md + a corrected test-file count.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-docs-maintenance-hard-gate/43-CONTEXT.md
@.planning/phases/43-docs-maintenance-hard-gate/43-PATTERNS.md
@tests/39-help-integrity.test.js
@tests/_helpers/base64-codec.js
@.planning/codebase/TESTING.md

Loader source to extract: `tests/39-help-integrity.test.js:33-52` (fs/path/vm sandbox, fake
window, `sandbox.window.HELP_CONTENT_EN`). Helper-module shape to mirror:
`tests/_helpers/base64-codec.js` (plain CommonJS, `'use strict'`, `module.exports`).

Rename table (D-22) — FULL OLD token → NEW token (anchor replace on the full token, longest first,
to avoid the help-integrity ⊂ help-integrity-locale substring collision, RESEARCH Pitfall 2):
- `42_1-changelog-integrity-locale.test.js` → `changelog-integrity-locale.test.js`
- `42_1-help-integrity.test.js` → `help-integrity-locale.test.js`
- `42-changelog-integrity.test.js` → `changelog-integrity.test.js`
- `39-help-integrity.test.js` → `help-integrity.test.js`
- `28-04-integrity-state.test.js` → `update-integrity-state.test.js`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared vm content loader to scripts/lib/help-loader.js</name>
  <files>scripts/lib/help-loader.js</files>
  <action>
    Create `scripts/lib/help-loader.js` (create the `scripts/lib/` dir) as a plain CommonJS module
    extracting the vm-sandbox loader from `tests/39-help-integrity.test.js:33-52`. Export functions
    that load a content file into a fresh vm sandbox (fake `window`, silenced console,
    `window.I18N={}`) and return the registered global — at minimum `loadHelpContentEN(assetsDir)`
    returning `window.HELP_CONTENT_EN`, and `loadChangelogEN(assetsDir)` returning the changelog
    array; also expose the loaded i18n dictionary for callers that resolve `{ui:}` keys.

    CRITICAL — parameterize the assets directory. Accept `assetsDir` as an argument, defaulting to
    the repo-root `assets/` resolved relative to the module (`path.join(__dirname,'..','..','assets')`).
    This lets the gate (43-06) point the loader at a TARGET repo's assets (resolved from the git repo
    root / cwd, NOT the gate's own install dir) and lets the RED/GREEN test (43-01) point it at a
    throwaway fixture dir. Do NOT hardcode a `__dirname`-relative assets path as the only option.

    Fail closed: on a load throw, surface a clear error (throw or a FATAL string the caller acts on)
    — never return a half-populated object silently (D-04 posture). Node built-ins fs/path/vm only
    (no jsdom). Header comment describes BEHAVIOR only — no phase/plan/decision-ID citations (new
    tooling files start clean; Phase 44 owns comment hygiene).
  </action>
  <verify>
    <automated>node -e "const l=require('./scripts/lib/help-loader.js'); const s=l.loadHelpContentEN(); if(!Array.isArray(s)||!s.length){console.error('FAIL sections');process.exit(1)} const c=l.loadChangelogEN(); if(!Array.isArray(c)||!c.length){console.error('FAIL changelog');process.exit(1)} console.log('OK',s.length,'sections',c.length,'entries')"</automated>
  </verify>
  <done>
    Requiring the module and calling both loaders against the real repo returns the sections array
    and the changelog array. The loader accepts an explicit assetsDir argument.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename the five integrity guards + update live filename references</name>
  <files>tests/39-help-integrity.test.js, tests/42-changelog-integrity.test.js, tests/42_1-help-integrity.test.js, tests/42_1-changelog-integrity-locale.test.js, tests/28-04-integrity-state.test.js, tests/40-i18n-parity.test.js, tests/29-01-crashlog-capture.test.js, assets/help-content-en.js, assets/help-content-he.js, assets/help-content-de.js, assets/help-content-cs.js, assets/changelog-content-en.js, assets/changelog-content-he.js, assets/changelog-content-de.js, assets/changelog-content-cs.js, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    FIRST capture the before-set: run `git grep -lE '39-help-integrity|42-changelog-integrity|42_1-help-integrity|42_1-changelog-integrity-locale|28-04-integrity-state'` and save the list. Expect ~72 files total (about 22 live-allowlist + ~50 historical). Record it in the SUMMARY.

    Then `git mv` each of the five test files to its new name (rename table in Context). Within each
    RENAMED file, update ONLY its self-referential filename tokens — the `Run: node tests/<old>` header
    line and the `console.error` hint string that prints the file's own name — to the new filename. Do
    NOT touch any phase/plan/decision-ID citation in those headers (that is Phase 44).

    Then replace-all the FULL old tokens → new tokens across the LIVE-ONLY allowlist: the two other
    tests that reference these names (`tests/40-i18n-parity.test.js`, `tests/29-01-crashlog-capture.test.js`),
    the eight content files (`assets/{help,changelog}-content-{en,he,de,cs}.js` — references are inside
    comments / hint strings naming the guard file), and `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`.
    Anchor every replacement on the FULL old token (longest first), never the bare stem `help-integrity`.
    These are mechanical FILENAME-token updates, NOT hygiene edits — do not opportunistically clean any
    phase/decision-ID citation you pass over.

    The 51-ish historical artifacts under `.planning/phases/{≠43}` and `.planning/milestones/` MUST stay
    untouched — they make true statements about past events. TESTING.md's rename map and this phase's own
    43-* planning docs (which narrate the old→new mapping) legitimately retain old tokens by design; do
    NOT blind-replace inside them (that would corrupt "X → Y" into "Y → Y").
  </action>
  <verify>
    <automated>test -f tests/help-integrity.test.js && test -f tests/changelog-integrity.test.js && test -f tests/help-integrity-locale.test.js && test -f tests/changelog-integrity-locale.test.js && test -f tests/update-integrity-state.test.js && ! test -f tests/39-help-integrity.test.js && node tests/help-integrity.test.js && node tests/changelog-integrity.test.js && node tests/update-integrity-state.test.js && echo RENAME-OK</automated>
  </verify>
  <done>
    All five files renamed via `git mv`; the three still-green integrity tests pass under their new
    names; no old test filename survives as a live filename reference in assets/** or tests/**.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rename map in TESTING.md + fix the stale count + assert the D-22 post-condition</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
    Add a rename map (a small old→new table) to `.planning/codebase/TESTING.md` so anyone landing on
    an old SUMMARY can follow the name forward — this table deliberately CONTAINS the old tokens (its
    whole purpose). Correct the stale "Run all 106 test files" figure to the live count (164 — verify
    with `ls tests/*.test.js | wc -l` at execute time and use whatever it reports). Do NOT write the
    {slug}.test.js naming RULE here — that is Phase 44's (D-23); this is only the map + count fix.

    Then assert the D-22 post-condition (this is the conviction Ben asked for, made executable).
    It has TWO directions and BOTH must be machine-checked. A one-directional grep is the
    "promise, not a mechanism" failure D-22 exists to prevent.

    The before-set, captured empirically, is **75 files**: 50 historical + 8 of this phase's own
    `43-*` planning docs + **17 live targets**. The 17 live targets are:
      tests/  (7): 39-help-integrity, 42-changelog-integrity, 42_1-help-integrity,
                   42_1-changelog-integrity-locale, 28-04-integrity-state (the five renamed),
                   plus 40-i18n-parity.test.js and 29-01-crashlog-capture.test.js (which merely
                   *reference* old tokens and are NOT renamed — update their references only)
      assets/ (8): help-content-{en,he,de,cs}.js, changelog-content-{en,he,de,cs}.js
      .planning/ (2): REQUIREMENTS.md, ROADMAP.md
    Note the last two sit OUTSIDE `assets/**` and `tests/**`. Any post-condition scoped to those two
    globs silently misses them. Scope the check to the WHOLE REPO.

    **Direction (i) — no live file was missed.** After the rename, no file in the 17-file live
    allowlist may still contain any of the 5 old tokens.

    **Direction (ii) — no historical file was clobbered.** No file under `.planning/phases/` (excluding
    `43-*`) or `.planning/milestones/` may have been MODIFIED AT ALL by this plan's commits. Assert this
    with `git diff --name-only`, not with a token grep: a clobber that happens to leave the token intact
    would slip past a grep. Ben's concern was that "a SUMMARY saying *created tests/42_1-help-integrity.test.js*
    is a true statement about a past event; rewriting it makes the record claim an executor did something
    it did not do." Only a content-diff check catches that.

    Expected after-set = 50 historical + 8 `43-*` docs + TESTING.md's new map (which deliberately
    contains old tokens — that is its purpose) = **59 files**. Record before-set, after-set, and the
    diff in the SUMMARY. If either direction fails, STOP and report — do not proceed.
  </action>
  <verify>
    <automated>bash .planning/phases/43-docs-maintenance-hard-gate/assert-rename-postcondition.sh</automated>
    <manual>The script requires `GSD_PLAN_BASE_SHA` — the SHA of the commit immediately preceding this plan's FIRST commit. Capture it in Task 1 (`git rev-parse HEAD` before any rename commit) and export it. The script FAILS CLOSED if it is unset: `git diff ..HEAD` with an empty left side is a silent no-op, which would make direction (ii) always pass. That fail-open is exactly the class of bug D-04 forbids.</manual>
  </verify>
  <done>
    TESTING.md carries the rename map and the corrected count. No `assets/**` or `tests/**` file
    references an old test token. The before/after grep diff is recorded and equals exactly the
    historical artifacts plus the documented carve-outs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rename replace-all → historical record | The replace-all must not rewrite historical SUMMARY/PLAN files (they assert past events); the allowlist is the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-03 | Tampering | replace-all clobbers a historical artifact or misses a live reference | medium | mitigate | Full-token anchored replace (longest-first, no bare stem); before/after grep post-condition diffed and recorded (D-22). |
| T-43-04 | Repudiation | a renamed file's self-reference (Run: line / console hint) left stale | low | mitigate | Task 2 updates each renamed file's own filename tokens; the three green tests are re-run under new names. |
| T-43-SC | Tampering | supply chain | low | accept | No packages installed; scripts/lib/help-loader.js uses node built-ins only. |
</threat_model>

<verification>
- `scripts/lib/help-loader.js` loads the real corpus (Task 1 verify).
- Five files renamed; three still-green integrity tests pass under new names; `npm test` stays green
  (the two renamed locale/help tests that gain adjustments are handled in 43-04/43-05).
- D-22 post-condition: `git grep` for old tokens returns exactly historical artifacts + documented
  carve-outs; no live `assets/**` / `tests/**` reference survives.
- TESTING.md count corrected to the live figure.
</verification>

<success_criteria>
- One shared, parameterized vm loader exists in `scripts/lib/`.
- The five guards are renamed (phase prefixes dropped, D-23 shape), references updated on live files only,
  and the high-conviction before/after grep proves no historical clobber and no missed live reference.
- TESTING.md documents the rename map and the corrected count.
</success_criteria>

<output>
Create `.planning/phases/43-docs-maintenance-hard-gate/43-02-SUMMARY.md` when done. Record the exact
before-set and after-set of the D-22 grep and their diff — this is the auditable conviction Ben required.
</output>