---
phase: 43-docs-maintenance-hard-gate
plan: 03
type: execute
wave: 2
depends_on: [43-02]
files_modified:
  - assets/help-content-en.js
  - scripts/gen-help-map.js
  - HELP-MAP.md
autonomous: true
requirements: [GATE-01, GATE-03]
must_haves:
  truths:
    - "D-19: existing help topics' covers[] are extended (metadata only, no new prose) to name the scripts behind already-covered pages and obvious module owners — shrinking the uncovered-watched set the gate would block on."
    - "D-13: a generated HELP-MAP.md (section → topic → title → covers[]) is committed so an agent can locate where a file belongs in help cold, without running anything."
    - "D-17 #1 (coherence): the committed HELP-MAP.md byte-matches a fresh regeneration; the covers[] edit and the regenerated map land in the SAME commit so the repo is never transiently red."
    - "D-20 boundary: only assets/help-content-en.js is edited here — EN is the canonical covers[] source the gate reads."
  artifacts:
    - scripts/gen-help-map.js
    - HELP-MAP.md
  key_links:
    - "gen-help-map.js reuses scripts/lib/help-loader.js (43-02) for input; it is the SAME code the D-17 #1 freshness invariant (43-05) calls in check mode — one implementation, write + check modes."
    - "HELP-MAP.md is repo-root and does NOT ship (deploy.yml copies only *.html/assets/ etc.); it is a live tooling artifact, not planning history."
---

<objective>
Backfill help topic covers[] (D-19) and generate the freshness-checked HELP-MAP.md (D-13),
committed coherently in one commit so the D-17 #1 invariant holds.

Purpose: A leaky reverse index makes the gate either miss real changes or block on files no
topic will ever name. Backfilling the obvious owners shrinks the trailer-cost surface; the map
gives agents a cold-readable "where does this belong in help?" lookup.

Output:
- Extended covers[] in `assets/help-content-en.js`.
- `scripts/gen-help-map.js` (write + check modes) and the generated `HELP-MAP.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-docs-maintenance-hard-gate/43-CONTEXT.md
@.planning/phases/43-docs-maintenance-hard-gate/43-RESEARCH.md
@assets/help-content-en.js
@scripts/lib/help-loader.js

Map shape + canonicalization: RESEARCH §Q-I — one row per topic `| Section | Topic | Title | Covers |`;
emit LF endings, exactly one trailing `\n`, covers[] SORTED within each row (do NOT re-sort sections/
topics — document order is meaningful). Repo-root location. Freshness = regenerate-to-string then `===`.

D-19 backfill targets (page scripts of already-covered pages + obvious module owners + vendor bundles),
verified from live source under OD-1 WATCH-CODE-ONLY:
- `add-client.html` topic → add `assets/add-client.js`
- `add-session.html` topic → add `assets/add-session.js`; `sessions.html` topic → `assets/sessions.js`
- `report.html` topic → `assets/report.js`; settings topics → `assets/settings-photos.js`, `assets/settings-session-types.js`
- date-format topic → `assets/date-format.js`
- pdf-export topic → `assets/jspdf.min.js`, `assets/bidi.min.js`; backup topic → `assets/jszip.min.js`
Assign each to the topic that already covers the corresponding page/module. Do NOT invent new topics
or author prose. Files with no obvious owner (app.js, i18n-*.js, shared-chrome.js, tour.js, whats-new.js,
changelog.js, reporting.js, help.js, css files) stay uncovered BY DESIGN — they cost one trailer per
edit (D-12 accepted cost); do NOT force them into an unrelated topic.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill covers[] on existing EN help topics (D-19, metadata only)</name>
  <files>assets/help-content-en.js</files>
  <action>
    Extend the `covers[]` arrays of EXISTING topics in `assets/help-content-en.js` per the backfill
    targets in Context. Add the script(s) behind each already-covered page to that page's topic, and
    the obvious module owners (date-format.js → the date-format topic; jspdf.min.js + bidi.min.js → the
    pdf-export topic; jszip.min.js → the backup topic). Every added path MUST be a real file that exists
    on disk (the 43-05 invariant #2 will enforce this). Metadata only — add no topic, edit no body prose,
    change no {ui:} tokens. Do NOT touch he/de/cs files (that is 43-04). Leave `landing.html`'s existing
    covers[] entry alone (D-06 note). Header/comments: no phase/decision-ID citations.
  </action>
  <verify>
    <automated>node -e "const l=require('./scripts/lib/help-loader.js');const fs=require('fs');const s=l.loadHelpContentEN();const cov=new Set();s.forEach(x=>(x.topics||[]).forEach(t=>(t.covers||[]).forEach(c=>cov.add(c))));const missing=[...cov].filter(p=>!fs.existsSync(p));if(missing.length){console.error('FAIL covers point at missing files:',missing);process.exit(1)}['assets/add-session.js','assets/sessions.js','assets/date-format.js'].forEach(p=>{if(!cov.has(p)){console.error('FAIL not backfilled:',p);process.exit(1)}});console.log('OK covers',cov.size)"</automated>
  </verify>
  <done>
    covers[] on existing EN topics now name the page scripts + obvious module owners; every covered
    path exists on disk; no new topic or prose was added; he/de/cs untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: gen-help-map.js (write+check) + generate HELP-MAP.md coherently</name>
  <files>scripts/gen-help-map.js, HELP-MAP.md</files>
  <action>
    Create `scripts/gen-help-map.js` reusing `scripts/lib/help-loader.js` for input. It builds the map
    string — one row per topic, `| Section | Topic | Title | Covers |`, sections/topics in document
    order, covers[] SORTED within each row, LF endings, exactly one trailing newline (RESEARCH §Q-I
    canonicalization). Support two modes: WRITE (default — write HELP-MAP.md) and CHECK (`--check` —
    regenerate to a string and `===` compare against the committed file; on mismatch print a message
    telling the reader to run the generator and commit, then exit non-zero). The CHECK path is the
    single implementation the D-17 #1 freshness invariant (43-05) will call — expose the compare as a
    reusable export so 43-05 requires it rather than re-implementing.

    Then run it in WRITE mode to generate `HELP-MAP.md` FROM THE POST-BACKFILL covers[] (Task 1). Commit
    the covers[] edit (Task 1) and this generated map in the SAME commit so D-17 #1 holds and the repo is
    never transiently red. Add `HELP-MAP.md text eol=lf` to `.gitattributes` if one exists (cheap CRLF
    insurance); if no .gitattributes exists, note it in the SUMMARY rather than creating one.
    Comments: behavior-only, no phase/decision-ID citations.
  </action>
  <verify>
    <automated>node scripts/gen-help-map.js && node scripts/gen-help-map.js --check && test "$(tail -c1 HELP-MAP.md | wc -l)" -eq 1 && echo MAP-FRESH-OK</automated>
  </verify>
  <done>
    HELP-MAP.md exists at repo root, matches a fresh regeneration (`--check` exits 0), ends in exactly
    one newline, and reflects the post-backfill covers[]. The generator exposes a reusable check function.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| covers[] source → gate reverse index | The gate trusts covers[] as its only help-routing input; a covered path that does not exist would make the gate point at a missing file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05 | Tampering | a covers[] entry names a file that does not exist | low | mitigate | Task 1 verify asserts every covered path exists on disk; 43-05 invariant #2 enforces it in the gate. |
| T-43-06 | Denial of function | HELP-MAP.md drifts from covers[] (cosmetic diff churn trains people to ignore the freshness gate) | low | mitigate | Canonicalized generation (LF, single trailing newline, sorted covers[]); covers edit + map regen in one commit. |
| T-43-SC | Tampering | supply chain | low | accept | No packages installed; generator uses node built-ins + the shared loader only. |
</threat_model>

<verification>
- Every covered path exists on disk (Task 1 verify).
- Named backfill targets (page scripts, date-format, vendor bundles) are present in some topic's covers[].
- `HELP-MAP.md --check` exits 0 (committed map == fresh regen); file ends in one newline.
- `node tests/help-integrity.test.js` stays green (EN covers[] still non-empty per topic).
</verification>

<success_criteria>
- covers[] backfilled (metadata only) on EN topics; uncovered-watched set shrunk to obvious-owner residual.
- Freshness-checked HELP-MAP.md generated coherently in the same commit as the covers[] edit.
- The generator's check mode is reusable by the 43-05 freshness invariant (one implementation).
</success_criteria>

<output>
Create `.planning/phases/43-docs-maintenance-hard-gate/43-03-SUMMARY.md` when done. List which paths were
backfilled onto which topics, and which watched code files remain uncovered by design (trailer-cost).
</output>