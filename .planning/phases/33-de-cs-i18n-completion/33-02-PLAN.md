---
phase: 33-de-cs-i18n-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-cs.js
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "Czech users opening the export modal see all 13 formerly-English keys in native Czech — no English fallback remains (I18N-02)"
    - "The placeholder translate-to-Czech comment lines are gone from assets/i18n-cs.js (I18N-02)"
    - "The 13 Czech values are produced by the D-01 native panel: 3 independent native-speaker perspectives per key, cross-checked for the most natural phrasing, then synthesized into one authoritative value"
    - "Czech terminology matches the already-shipped export-modal keys in i18n-cs.js — Upravit (edit), Náhled (preview), Další (next), Nastavení uloženo (settings saved) (D-03)"
    - "The 3 export.stepN.helper values are faithful-but-natural Czech: every piece of guidance in the expanded EN copy is preserved but phrased as a native Czech speaker would write it; CS users get the SAME amount of help as EN users (D-04)"
    - "In the 4 export.format.help.* values the literal Markdown syntax characters stay ASCII and untranslated (** * # ## dash+space); only the surrounding prose is Czech, and the prose still describes those exact ASCII characters (D-05)"
  artifacts:
    - "assets/i18n-cs.js — 13 translated string VALUES only (no new keys, no renames, no key deletions)"
  key_links:
    - "The 13 keys are resolved by the App i18n lookup that the export modal already calls — correct values are the only missing piece (no DOM/JS change)"
    - "export.format.help.heading must keep the literal # and ## that assets/md-render.js parses as ASCII heading syntax"
---

<objective>
Translate the 13 English-fallback i18n keys in `assets/i18n-cs.js` to native-quality Czech and remove their placeholder translate-to-Czech comment lines. These 13 keys fell back to English during the Phase 31 export-modal extraction; the rest of the export modal is already fully localized in Czech.

Purpose: Czech users must see the export-modal stepper labels, step helpers, and Markdown formatting tips in Czech rather than English (I18N-02).
Output: `assets/i18n-cs.js` with 13 translated VALUES (no new keys, no code, no behavior change).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-de-cs-i18n-completion/33-CONTEXT.md
</context>

<translation_panel_note>
The D-01 quality mechanism is a native-translation PANEL, realized either way:
- **Inline realization (default — gsd-executor has no Task/sub-agent tool):** for EACH of the 13 keys, generate three distinct native-Czech candidate renderings from three perspectives, cross-check them for the most natural phrasing, then commit ONE synthesized authoritative value.
- **Multi-agent realization (only if the execute runtime supports fan-out):** a translation-panel Workflow MAY produce the strings.

Acceptance is about the RESULT (native-quality, terminology-consistent with the shipped Czech export keys, faithful-but-natural helpers, D-05 ASCII syntax preserved) plus the deterministic checks below — NOT about how many agents were spawned. Do not treat the strings below as pre-written translations; the EN strings are the source of MEANING and the CS anchors are the terminology reference — the panel produces the Czech.
</translation_panel_note>

<tasks>

<task type="auto">
  <name>Task 1: Translate the 13 export-modal keys to Czech via the D-01 native panel</name>
  <files>assets/i18n-cs.js</files>

  <read_first>
    - `assets/i18n-cs.js` — the file being modified; locate the 13 keys BY NAME (they are contiguous but line numbers drift, do not trust any line-number annotation). Also read the already-localized export keys in this same file for terminology/tone: `export.next1` = "Další: Upravit dokument", `export.next2` = "Další: Získat dokument", `export.tab.edit` / `export.step2.label.edit` = "Upravit", `export.tab.preview` / `export.step2.label.preview` = "Náhled", `export.done` = "Hotovo", `export.back` = "Zpět", `settings.saved.toast` = "Nastavení uloženo". These set the voice the panel MUST match (D-03).
    - `assets/i18n-en.js` — authoritative EN source of meaning for the 13 keys (locate by name).
    - `assets/md-render.js` — the ASCII-only Markdown parser; it only recognizes ASCII `*`, `#`, and `-`+space. This is WHY D-05 requires the syntax characters in `export.format.help.*` to stay literal ASCII.
    - `.planning/milestones/v1.1-phases/15-architecture-and-ui-audit/15-03-REPORT-i18n-translations.md` — prior DE/CS terminology decisions (established register/vocabulary).
  </read_first>

  <action>
Replace the English fallback VALUE of each of the 13 keys below in `assets/i18n-cs.js` with a synthesized native-Czech value from the D-01 panel, and delete the placeholder translate-to-Czech comment line that currently annotates each key (the marker check in verify asserts none remain). Change VALUES only — do not add, rename, reorder, or remove any key; the surrounding keys and file structure stay byte-stable.

Write native Czech characters directly as raw UTF-8 (the file's dominant convention for prose — it already contains many raw diacritics); the few pre-existing `\uXXXX`-escaped values elsewhere in the file are equivalent and may be left untouched. The file MUST remain valid JavaScript that loads in a vm sandbox (the verify check loads it).

The 13 keys and their EN source strings (translate from MEANING, per the rules noted on each):

- `settings.saved.notice` — EN "Settings saved" → align with the shipped `settings.saved.toast` "Nastavení uloženo" (D-03).
- `settings.saved.dismiss` — EN "Dismiss" → a natural Czech dismiss action (e.g. Zavřít / Zrušit — the panel decides).
- `export.stepper.label.1` — EN "Choose" → short stepper chip label; keep it short to fit the chip.
- `export.stepper.label.2` — EN "Edit" → align with the shipped "Upravit" (D-03).
- `export.stepper.label.3` — EN "Export" → short stepper chip label.
- `export.step1.helper` — EN "Step 1 of 3 — Choose which session sections to include in the export. Your selection here decides what shows up in the editable preview on the next step." → faithful-but-natural Czech preserving BOTH the what-to-do clause AND the why-it-matters-next clause; keep the "Step 1 of 3 —" progress cue; restructure for Czech sentence flow (D-04).
- `export.step2.helper` — EN "Step 2 of 3 — Edit your document on the left; the live preview on the right shows how it will look. Formatting tips appear below if you need them." → faithful-but-natural Czech, preserve every guidance clause (D-04).
- `export.step3.helper` — EN "Step 3 of 3 — Choose how to deliver the document. PDF is best for printing or email; the text file works for any text editor; Share opens your device's share sheet (if supported)." → faithful-but-natural Czech, preserve every delivery-option clause including the "if supported" caveat (D-04).
- `export.format.help.title` — EN "Formatting tips (click to expand)" → Czech prose.
- `export.format.help.bold` — EN "surround text with two stars to make it bold" → Czech prose that STILL describes surrounding text with two star characters (the `*` ASCII char) (D-05).
- `export.format.help.italic` — EN "surround text with one star to make it italic" → Czech prose that STILL describes one star character (D-05).
- `export.format.help.heading` — EN "start a line with # for a heading; ## for a smaller heading" → Czech prose, but the literal `#` and `##` characters stay ASCII and untranslated; only the words around them are Czech (D-05). md-render.js parses these exact ASCII characters.
- `export.format.help.list` — EN "start a line with a dash and a space to create a bullet list" → Czech prose that STILL describes a dash character followed by a space (D-05).
  </action>

  <verify>
    <automated>node -e "const vm=require('vm'),fs=require('fs');const s={window:{I18N:{}},console:{log(){},warn(){},error(){}}};vm.createContext(s);['i18n-en.js','i18n-cs.js'].forEach(f=>vm.runInContext(fs.readFileSync('assets/'+f,'utf8'),s,{filename:f}));const src=fs.readFileSync('assets/i18n-cs.js','utf8');if(/TODO i18n/.test(src)){console.error('FAIL: placeholder markers remain in i18n-cs.js');process.exit(1)}const K=['settings.saved.notice','settings.saved.dismiss','export.stepper.label.1','export.stepper.label.2','export.stepper.label.3','export.step1.helper','export.step2.helper','export.step3.helper','export.format.help.title','export.format.help.bold','export.format.help.italic','export.format.help.heading','export.format.help.list'];const en=s.window.I18N.en,cs=s.window.I18N.cs;const miss=K.filter(k=>!cs[k]||typeof cs[k]!=='string');if(miss.length){console.error('FAIL: missing CS keys: '+miss.join(', '));process.exit(1)}const H=['export.step1.helper','export.step2.helper','export.step3.helper'];const same=H.filter(k=>cs[k]===en[k]);if(same.length){console.error('FAIL: CS helper still English: '+same.join(', '));process.exit(1)}if(!cs['export.format.help.heading'].includes('#')){console.error('FAIL: heading key lost the literal # syntax char (D-05)');process.exit(1)}console.log('OK: markers gone; 13 keys present; helpers translated; heading # preserved')"</automated>
  </verify>

  <acceptance_criteria>
    - All 13 keys still present in `assets/i18n-cs.js` with non-empty Czech string values; file loads as valid JS in the vm sandbox.
    - Zero placeholder translate-to-Czech comment lines remain (the `// TODO i18n` marker count in the file is 0).
    - The 3 `export.stepN.helper` values differ from their EN source (proving real translation of the substantive copy) and preserve the full guidance (D-04).
    - `export.format.help.heading` still contains the literal `#` / `##` ASCII characters; the other 3 format keys describe the same ASCII syntax characters in Czech prose (D-05).
    - No key added, renamed, reordered, or removed; only the 13 VALUES changed.
  </acceptance_criteria>
  <done>All 13 export-modal keys in `assets/i18n-cs.js` carry native-Czech values, placeholder markers removed, file loads as valid JS, and the Task 1 automated check passes.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

- **No new symbols, no new files, no new keys.** This plan changes 13 string VALUES in `assets/i18n-cs.js` only.
- The 13 keys whose values change: `settings.saved.notice`, `settings.saved.dismiss`, `export.stepper.label.1`, `export.stepper.label.2`, `export.stepper.label.3`, `export.step1.helper`, `export.step2.helper`, `export.step3.helper`, `export.format.help.title`, `export.format.help.bold`, `export.format.help.italic`, `export.format.help.heading`, `export.format.help.list`.
- Drift verification should EXCLUDE these — they are expected value changes, not new API surface.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a static-string DATA change. No new user-input path, no new DOM sink, no network surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-02 | Tampering / Injection | Translated static values in `assets/i18n-cs.js` rendered in the export modal | low | accept | Values are static developer-authored content, NOT user input; the injection surface is identical to the pre-existing English fallbacks. No new user-input path, DOM sink, or network call is added. `md-render.js` HTML-escapes all input before `innerHTML`; stepper/helper labels render as text. |

No package-manager installs occur, so no supply-chain (`T-33-SC`) checkpoint is required.
</threat_model>

<verification>
- Automated: the Task 1 node check (markers gone, 13 keys present, helpers translated, heading `#` preserved). The comprehensive bidirectional parity + no-TODO gate across BOTH files runs in Plan 33-03 via `npm test`.
- Manual (D-06 part 2, tracked at phase level in Plan 33-03): switch the app locale to Czech and screenshot export-modal steps 1–3 to confirm strings render and fit — no overflow on the stepper-label chips, no clipped helpers.
</verification>

<success_criteria>
- The 13 export-modal keys in `assets/i18n-cs.js` read as native Czech that matches the already-shipped export-modal terminology; no English fallback and no placeholder markers remain (I18N-02).
</success_criteria>

<output>
Create `.planning/phases/33-de-cs-i18n-completion/33-02-SUMMARY.md` when done.
</output>
