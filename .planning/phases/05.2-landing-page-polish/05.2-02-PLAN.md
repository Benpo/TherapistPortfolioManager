---
phase: 05.2-landing-page-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05.2-01
files_modified:
  - landing.html
  - assets/landing.css
  - assets/landing.js
autonomous: true
requirements:
  - DIST-05

must_haves:
  truths:
    - "A screenshots section exists showing 3 real app screenshots (homepage, session, client views)"
    - "Screenshots section is positioned after 'Sound Familiar?' and before 'Why I Built This'"
    - "A contact section exists before the legal sections with a placeholder email address"
    - "Impressum and Privacy Policy sections are collapsed inside an accordion/details element in the footer area"
    - "The Hebrew 'Buy' button text ('לרכישה — €119') correctly appears when Hebrew language is selected"
    - "Content boxes (who-for, why-built, pricing) have softer rounded corners and gentle glow"
    - "Visual separation exists between 'Why I Built This' and Pricing sections"
    - "All new section text translates correctly across all 4 languages (en, he, de, cs)"
    - "Hebrew RTL layout renders correctly for all new sections"
  artifacts:
    - path: "landing.html"
      provides: "Screenshots section, contact section, accordion legal sections, RTL-safe HTML for new sections"
      contains: "screenshots-section, contact-section, details, summary"
    - path: "assets/landing.css"
      provides: "Screenshot tile styles, contact section styles, accordion CSS, glow effects on content boxes"
      contains: "screenshot-tile, contact-section, details[open]"
    - path: "assets/landing.js"
      provides: "i18n strings for screenshots and contact sections in en/he/de/cs, applyLang renderer updates, Hebrew pricingCta fix"
      contains: "screenshotsTitle, contactTitle, pricingCta"
  key_links:
    - from: "assets/landing.js applyLang()"
      to: "landing.html #screenshots-title, .screenshot-label"
      via: "setText() and querySelectorAll('.screenshot-label') in applyLang()"
      pattern: "screenshot"
    - from: "assets/landing.js applyLang()"
      to: "landing.html #contact-title"
      via: "setText('contact-title', t.contactTitle) in applyLang()"
      pattern: "contactTitle"
    - from: "assets/landing.js LANDING_I18N.he.pricingCta"
      to: "landing.html #pricing-cta"
      via: "setText2('pricing-cta', t.pricingCta) in applyLang()"
      pattern: "pricingCta"
---

<objective>
Add the screenshots section (real app screenshots), contact section, fix the Hebrew buy button, add soft glow to content boxes, improve visual separation between sections, and collapse Impressum/Privacy into an accordion.

Purpose: Complete the landing page's content sections and fix the known translation bug. These additions are content-layer changes that build on the visual foundation established in Plan 01.

Output: landing.html with 2 new sections + accordion legal + CSS glow effects, landing.js with i18n strings for all new content, Hebrew buy button translation fixed.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-landing-page-polish/05.2-CONTEXT.md
@.planning/phases/05.2-landing-page-polish/05.2-RESEARCH.md
@.planning/phases/05.2-landing-page-polish/05.2-01-SUMMARY.md

<interfaces>
<!-- Patterns from existing code executor must follow exactly. -->

From assets/landing.js — how new sections must be added to applyLang():
```javascript
// Pattern (existing who-for section):
setText('whofor-title', t.whoForTitle);
setText('whofor-subtitle', t.whoForSubtitle);
var whoForItems = document.querySelectorAll('.who-for-list li');
t.whoForItems.forEach(function(item, i) {
  if (whoForItems[i]) whoForItems[i].textContent = item;
});

// New screenshots pattern to add:
setText('screenshots-title', t.screenshotsTitle);
setText('screenshots-subtitle', t.screenshotsSubtitle);
var screenshotLabels = document.querySelectorAll('.screenshot-label');
t.screenshotLabels.forEach(function(label, i) {
  if (screenshotLabels[i]) screenshotLabels[i].textContent = label;
});

// New contact pattern to add:
setText('contact-title', t.contactTitle);
setText('contact-text', t.contactText);
```

From assets/landing.js — known Hebrew pricingCta bug:
```javascript
// In LANDING_I18N.he, the current value is:
pricingCta: 'לרכישה — €119',
// BUT the HTML button has id="pricing-cta" and applyLang() calls:
setText2('pricing-cta', t.pricingCta);
// Check: does the button text actually change? The CONTEXT.md flags it as broken.
// Likely cause: the Hebrew pricing-cta button has class="landing-cta" and uses
// setText2() which sets textContent (clears children). Verify the id="pricing-cta"
// exists in landing.html and the Hebrew pricingCta key value is correct.
// Fix: if the id is missing or mismatched in landing.html, add/fix it.
```

From landing.html — current section order (Plan 02 must insert screenshots between Sound Familiar and Why I Built This):
```
1. HEADER
2. HERO
3. FEATURES
4. WHO IS IT FOR
5. botanical-divider-alt
6. SOUND FAMILIAR   ← insert screenshots AFTER this
7. botanical-divider-alt
8. WHY I BUILT THIS ← screenshots go before this
9. PRICING
10. FAQ
11. IMPRESSUM       ← move into accordion
12. DATENSCHUTZ     ← move into accordion
13. FOOTER
```

Real screenshot paths:
- ./assets/screenshots/homepage pic.png  → Dashboard
- ./assets/screenshots/patirnt session_small.png → Session view
- ./assets/screenshots/addnewclient pic.png → Client view

Note: filenames contain spaces — use URL encoding or quote carefully. In HTML src attributes, spaces in filenames must be percent-encoded: `homepage%20pic.png`

i18n placeholder email: `contact@sessions.garden` (clearly marked as TODO in landing.js comment)

Contact section position: between FAQ section and the legal sections (Impressum, Datenschutz).

LANDING_I18N currently has en/he/de/cs blocks — ALL 4 must get new keys.

CSS logical properties ONLY — never use left/right/top/bottom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screenshots section + content box glow + section separation</name>
  <files>landing.html, assets/landing.css</files>
  <action>
**Screenshots section HTML:**

In `landing.html`, locate the botanical divider `<div>` that currently sits BETWEEN "Sound Familiar?" and "Why I Built This". Insert the screenshots section BEFORE that divider (i.e., directly after the closing `</section>` of the Sound Familiar section):

```html
<!-- =========================================================
     SCREENSHOTS
     ========================================================= -->
<section id="screenshots" class="landing-section screenshots-section" aria-labelledby="screenshots-title">
  <h2 id="screenshots-title" class="landing-section-title">See it in action</h2>
  <p id="screenshots-subtitle" class="landing-section-subtitle">A closer look at what's inside</p>
  <div class="screenshots-grid">
    <div class="screenshot-tile">
      <img src="./assets/screenshots/homepage%20pic.png" alt="Dashboard view" class="screenshot-img" loading="lazy" />
      <p class="screenshot-label">Dashboard</p>
    </div>
    <div class="screenshot-tile">
      <img src="./assets/screenshots/patirnt%20session_small.png" alt="Session view" class="screenshot-img" loading="lazy" />
      <p class="screenshot-label">Session view</p>
    </div>
    <div class="screenshot-tile">
      <img src="./assets/screenshots/addnewclient%20pic.png" alt="Client view" class="screenshot-img" loading="lazy" />
      <p class="screenshot-label">Client view</p>
    </div>
  </div>
</section>
```

**Visual separation between "Why I Built This" and Pricing:**

The "Why I Built This" section uses `class="landing-section landing-section-alt"` and the Pricing section uses `class="landing-pricing"`. They currently merge visually. Add a botanical divider between them. In `landing.html`, insert between `</section>` (why-built) and `<section class="landing-pricing"`:

```html
<div class="section-separator" aria-hidden="true"></div>
```

In `landing.css`:
```css
.section-separator {
  block-size: 3rem;
  background: linear-gradient(
    to bottom,
    var(--color-surface-alt),
    var(--color-background)
  );
}
```

**Soft glow on content boxes (who-for, why-built, pricing):**

In `landing.css`, add/update the following. Use existing class names from landing.html:

```css
/* Who-for section — currently a dark green rounded box — soften it */
.landing-section-alt {
  border-radius: 1.5rem;
  box-shadow: 0 0 32px color-mix(in srgb, var(--color-primary) 10%, transparent);
  transition: box-shadow 0.3s;
}

.landing-section-alt:hover {
  box-shadow: 0 0 48px color-mix(in srgb, var(--color-primary) 18%, transparent);
}

/* Why I built this blockquote — already has botanical accents */
.why-built-quote {
  border-radius: 1.25rem;
  box-shadow: 0 0 24px color-mix(in srgb, var(--color-primary) 12%, transparent);
}

/* Pricing card */
.landing-pricing-inner {
  border-radius: 1.5rem;
  box-shadow: 0 0 40px color-mix(in srgb, var(--color-primary) 14%, transparent);
  transition: box-shadow 0.3s;
}
```

**Screenshots CSS:**

```css
/* Screenshots section */
.screenshots-section {
  padding-block: 5rem;
  padding-inline: 2rem;
  max-inline-size: 1100px;
  margin-inline: auto;
}

.screenshots-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  margin-block-start: 2.5rem;
}

@media (max-width: 820px) {
  .screenshots-grid {
    grid-template-columns: 1fr;
    max-inline-size: 480px;
    margin-inline: auto;
  }
}

.screenshot-tile {
  border-radius: 1rem;
  border: 1.5px solid color-mix(in srgb, var(--color-primary) 25%, transparent);
  background: color-mix(in srgb, var(--color-primary) 5%, var(--color-surface));
  overflow: hidden;
  display: flex;
  flex-direction: column;
  transition: box-shadow 0.25s, transform 0.25s;
}

.screenshot-tile:hover {
  box-shadow: 0 4px 24px color-mix(in srgb, var(--color-primary) 20%, transparent);
  transform: translateY(-2px);
}

.screenshot-img {
  display: block;
  inline-size: 100%;
  block-size: auto;
  aspect-ratio: 16 / 10;
  object-fit: cover;
  object-position: top;
}

.screenshot-label {
  padding: 0.75rem 1rem;
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--color-primary);
  text-align: center;
}
```

Use logical properties only — no left/right/top/bottom in CSS.
  </action>
  <verify>
    <automated>grep -c "screenshots-section\|screenshot-tile\|screenshot-img\|section-separator" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/landing.html /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/landing.css 2>/dev/null</automated>
  </verify>
  <done>
    - Screenshots section exists in landing.html with id="screenshots", 3 `.screenshot-tile` children each containing an `<img>` and `.screenshot-label`
    - Screenshots section is positioned after Sound Familiar section and before Why I Built This section
    - `.section-separator` div exists between why-built and pricing sections
    - `.screenshots-grid`, `.screenshot-tile`, `.screenshot-img`, `.screenshot-label` CSS rules exist in landing.css
    - Glow box-shadow added to `.landing-section-alt`, `.why-built-quote`, `.landing-pricing-inner`
  </done>
</task>

<task type="auto">
  <name>Task 2: Contact section + accordion legal + Hebrew buy button fix + i18n strings</name>
  <files>landing.html, assets/landing.css, assets/landing.js</files>
  <action>
**Contact section HTML:**

In `landing.html`, add the contact section between the FAQ closing tag and the legal divider/Impressum section:

```html
<!-- =========================================================
     CONTACT
     ========================================================= -->
<section id="contact" class="landing-section contact-section" aria-labelledby="contact-title">
  <h2 id="contact-title" class="landing-section-title">Get in touch</h2>
  <p id="contact-text" class="landing-section-subtitle">
    Have questions or need help? We'd love to hear from you.
  </p>
  <!-- TODO: Replace placeholder email with real contact address before launch -->
  <a id="contact-email" href="mailto:contact@sessions.garden" class="contact-email-link">contact@sessions.garden</a>
</section>
```

**Accordion for Impressum and Privacy Policy:**

In `landing.html`, wrap each legal section in a `<details>` / `<summary>` accordion pattern. Replace the current `<hr class="legal-divider" /><section id="impressum" ...>` blocks:

```html
<!-- =========================================================
     LEGAL ACCORDION
     ========================================================= -->
<div class="legal-accordion">

  <details class="legal-details">
    <summary class="legal-summary" id="footer-impressum-toggle">
      <span id="impressum-title">Impressum</span>
    </summary>
    <div class="legal-details-content">
      <h3 id="impressum-angaben">Angaben gemäß DDG § 5</h3>
      <p id="impressum-note" class="legal-placeholder" style="white-space:pre-line;">TODO: Generate your Impressum at https://www.e-recht24.de/impressum-generator.html and replace the placeholder below.</p>
      <div class="legal-placeholder" aria-label="Impressum placeholder">
        <pre id="impressum-placeholder" style="font-family:inherit;margin:0;white-space:pre-wrap;">[Full Legal Name]
[Street Address]
[City, Postal Code, Country]
Email: [Email Address]
Phone: [Phone Number — optional]</pre>
      </div>
    </div>
  </details>

  <details class="legal-details">
    <summary class="legal-summary" id="footer-privacy-toggle">
      <span id="datenschutz-title">Privacy Policy / Datenschutzerklärung</span>
    </summary>
    <div class="legal-details-content">
      <h3 id="datenschutz-art13-title">1. Data Processing (GDPR Art. 13/14)</h3>
      <p id="datenschutz-art13">Sessions Garden does not collect, store, or transmit any personal data. All session and client data is stored exclusively on your device using IndexedDB browser technology. The development team has zero access to your data.</p>
      <h3 id="datenschutz-hosting-title">2. Hosting</h3>
      <p id="datenschutz-hosting">This website is hosted on Cloudflare Pages (Cloudflare, Inc., 101 Townsend St, San Francisco, CA 94107, USA). Cloudflare may process access logs (IP address, browser type, timestamp) as part of standard web hosting operations. For details, see Cloudflare's privacy policy: <a href="https://www.cloudflare.com/privacypolicy/" target="_blank" rel="noopener noreferrer">https://www.cloudflare.com/privacypolicy/</a></p>
      <h3 id="datenschutz-payment-title">3. Payment Processing</h3>
      <p id="datenschutz-payment">Payments are processed by Lemon Squeezy (Lemon Squeezy, LLC), who acts as Merchant of Record. Purchase data (name, email, payment information) is handled by Lemon Squeezy. For details, see Lemon Squeezy's privacy policy: <a href="https://www.lemonsqueezy.com/privacy" target="_blank" rel="noopener noreferrer">https://www.lemonsqueezy.com/privacy</a></p>
      <h3 id="datenschutz-storage-title">4. Local Storage</h3>
      <p id="datenschutz-storage">This application uses localStorage for preferences (language, theme) and license activation status. No tracking cookies are used. All clinical data is stored in your browser's IndexedDB, accessible only on your device.</p>
      <h3 id="datenschutz-rights-title">5. Your Rights (GDPR Art. 15–21)</h3>
      <p id="datenschutz-rights">Right to access: your data is already on your device — open the app to see it. Right to deletion: clear your browser's site data to remove all stored information. Right to data portability: use the Export function in the app to download your data as a JSON file.</p>
      <p id="datenschutz-note" class="legal-placeholder">TODO: Run the full Datenschutzerklärung through https://www.adsimple.de/datenschutz-generator/ or https://www.e-recht24.de before launch for a finalized, lawyer-reviewed version.</p>
    </div>
  </details>

</div>
```

Remove the old `<hr class="legal-divider" />` separators — the accordion provides its own visual separation.

Update footer links to point to `#footer-impressum-toggle` and `#footer-privacy-toggle` (or leave as anchor links to the toggle summary elements — details/summary is natively accessible).

**Accordion CSS:**

In `assets/landing.css`, add:

```css
/* Legal accordion */
.legal-accordion {
  max-inline-size: 800px;
  margin-inline: auto;
  padding-inline: 2rem;
  padding-block: 3rem;
}

.legal-details {
  border: 1px solid var(--color-border);
  border-radius: 0.75rem;
  margin-block-end: 1rem;
  overflow: hidden;
}

.legal-summary {
  padding: 1.25rem 1.5rem;
  cursor: pointer;
  font-weight: 600;
  font-size: 1rem;
  list-style: none;
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: var(--color-primary);
  background: var(--color-surface);
  user-select: none;
}

.legal-summary::-webkit-details-marker { display: none; }

.legal-summary::after {
  content: '+';
  font-size: 1.25rem;
  font-weight: 400;
  transition: transform 0.2s;
}

.legal-details[open] .legal-summary::after {
  transform: rotate(45deg);
}

.legal-details-content {
  padding: 1.5rem;
  border-block-start: 1px solid var(--color-border);
  background: var(--color-background);
}

/* Contact section */
.contact-section {
  padding-block: 4rem;
  padding-inline: 2rem;
  max-inline-size: 800px;
  margin-inline: auto;
  text-align: center;
}

.contact-email-link {
  display: inline-block;
  margin-block-start: 1.5rem;
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--color-primary);
  text-decoration: underline;
  text-underline-offset: 3px;
}

.contact-email-link:hover {
  opacity: 0.8;
}
```

**i18n strings in landing.js — add to all 4 language objects:**

In `assets/landing.js`, add new keys to `LANDING_I18N.en`, `LANDING_I18N.he`, `LANDING_I18N.de`, and `LANDING_I18N.cs`:

For `en`:
```javascript
screenshotsTitle: 'See it in action',
screenshotsSubtitle: 'A closer look at what\'s inside',
screenshotLabels: ['Dashboard', 'Session view', 'Client view'],
contactTitle: 'Get in touch',
contactText: 'Have questions or need help? We\'d love to hear from you.',
```

For `he`:
```javascript
screenshotsTitle: 'ראו את זה בפעולה',
screenshotsSubtitle: 'מבט מקרוב על מה שבפנים',
screenshotLabels: ['דף הבית', 'תצוגת פגישה', 'תצוגת לקוח'],
contactTitle: 'צרו קשר',
contactText: 'יש לכם שאלות או צריכים עזרה? נשמח לשמוע מכם.',
```

For `de`:
```javascript
screenshotsTitle: 'Sieh es in Aktion',
screenshotsSubtitle: 'Ein genauerer Blick auf das Innere',
screenshotLabels: ['Dashboard', 'Sitzungsansicht', 'Klientenansicht'],
contactTitle: 'Kontakt',
contactText: 'Hast du Fragen oder benötigst Hilfe? Wir freuen uns von dir zu hören.',
```

For `cs`:
```javascript
screenshotsTitle: 'Podívejte se na to v akci',
screenshotsSubtitle: 'Bližší pohled na to, co je uvnitř',
screenshotLabels: ['Přehled', 'Zobrazení sezení', 'Zobrazení klienta'],
contactTitle: 'Kontaktujte nás',
contactText: 'Máte otázky nebo potřebujete pomoc? Rádi vás uslyšíme.',
```

**applyLang() renderer — add new sections:**

In `applyLang()`, add after the FAQ block (before Impressum):

```javascript
// Screenshots
setText('screenshots-title', t.screenshotsTitle);
setText('screenshots-subtitle', t.screenshotsSubtitle);
var screenshotLabels = document.querySelectorAll('.screenshot-label');
if (t.screenshotLabels) {
  t.screenshotLabels.forEach(function(label, i) {
    if (screenshotLabels[i]) screenshotLabels[i].textContent = label;
  });
}

// Contact
setText('contact-title', t.contactTitle);
setText('contact-text', t.contactText);
```

**Hebrew pricingCta bug fix:**

In `landing.html`, find the pricing CTA button. Verify it has `id="pricing-cta"`. If the id is on an `<a>` that has a child `<span>` or nested elements, `setText2` (which sets textContent) will work. If the pricing CTA button is missing the id or has a different id, add/correct it.

Also verify that `LANDING_I18N.he.pricingCta` has the correct Hebrew value. The current value appears correct (`'לרכישה — €119'`) but applyLang() calls `setText2('pricing-cta', t.pricingCta)`. Check if the `id="pricing-cta"` is actually present on the element in `landing.html`. If missing or on a wrong element, fix the HTML — add `id="pricing-cta"` to the purchase button in the pricing section.

Also update the accordion `<summary>` toggle elements. The existing footer links target `#impressum` and `#datenschutz` — update them in the footer nav to target `#footer-impressum-toggle` and `#footer-privacy-toggle` respectively (the `<summary>` elements). Update in landing.html footer:
```html
<a id="footer-impressum-link" href="#footer-impressum-toggle">Impressum</a>
<a id="footer-privacy-link" href="#footer-privacy-toggle">Privacy Policy</a>
```

Also update `applyLang()` to translate the accordion summary labels — the `setText()` calls for `impressum-title` and `datenschutz-title` already exist and will work since those ids are on `<span>` elements inside the `<summary>`.
  </action>
  <verify>
    <automated>grep -c "contact-section\|screenshots-section\|legal-details\|screenshotsTitle\|contactTitle" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/landing.html /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/landing.js /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/landing.css 2>/dev/null</automated>
  </verify>
  <done>
    - Contact section exists in landing.html with id="contact", position between FAQ and legal accordion
    - Impressum and Privacy Policy wrapped in `<details class="legal-details">` elements inside `.legal-accordion`
    - `LANDING_I18N.en`, `.he`, `.de`, `.cs` all have `screenshotsTitle`, `screenshotsSubtitle`, `screenshotLabels`, `contactTitle`, `contactText` keys
    - `applyLang()` in landing.js has renderer blocks for screenshots and contact sections
    - `id="pricing-cta"` present on the pricing CTA button in landing.html
    - `.legal-summary`, `.legal-details`, `.legal-details-content`, `.contact-section`, `.contact-email-link` CSS rules exist in landing.css
  </done>
</task>

</tasks>

<verification>
Open `landing.html` in browser and verify:
1. Scroll to screenshots section — 3 real app screenshots visible between "Sound Familiar?" and "Why I Built This"
2. Scroll to bottom — Impressum and Privacy Policy are collapsed accordion items (not fully expanded walls of text)
3. Click accordion to expand — legal content appears
4. Contact section visible before accordion with email link
5. Switch to Hebrew — all new sections render translated text RTL
6. Switch to German and Czech — new section headings appear in correct language
7. In Hebrew mode, click the buy/pricing CTA button — text shows Hebrew (לרכישה — €119) not English
8. Content boxes (who-for, why-built, pricing) have visible rounded corners and subtle glow
9. Visual gap/separator exists between "Why I Built This" and Pricing sections
</verification>

<success_criteria>
- 3 real screenshot images visible in the screenshots section
- Screenshot labels translate in all 4 languages
- Contact section renders with placeholder email
- Impressum and Privacy Policy are accordion-collapsed by default
- Hebrew pricingCta renders correctly in Hebrew mode
- All new i18n keys exist in all 4 language objects (en/he/de/cs)
- No broken layout in RTL (Hebrew) mode for new sections
- Mobile viewport (< 820px): screenshots grid collapses to single column
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-landing-page-polish/05.2-02-SUMMARY.md`
</output>
