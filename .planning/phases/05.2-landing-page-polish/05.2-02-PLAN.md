---
phase: 05.2-landing-page-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - landing.html
  - assets/landing.css
  - assets/landing.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "A screenshots section exists with 3 placeholder tiles labeled Dashboard, Session view, Client view"
    - "Screenshot placeholders look intentional — not broken or empty"
    - "A contact section exists with a placeholder email address"
    - "Both new sections translate correctly across all 4 languages (en/he/de/cs)"
    - "Hebrew/RTL layout is correct in both new sections"
  artifacts:
    - path: landing.html
      provides: "Screenshots section HTML + Contact section HTML at correct positions"
      contains: "screenshots-grid"
    - path: assets/landing.css
      provides: "CSS for .screenshot-tile, .screenshots-section, .contact-section"
      contains: "screenshot-tile"
    - path: assets/landing.js
      provides: "screenshotsTitle/screenshotsSubtitle/screenshotLabels + contactTitle/contactText in all 4 language objects; applyLang() wiring for both sections"
      contains: "screenshotsTitle"
  key_links:
    - from: assets/landing.js
      to: landing.html
      via: "applyLang() setText calls targeting screenshots-title, screenshots-subtitle, screenshot-label elements and contact-title, contact-text"
      pattern: "setText.*screenshots"
    - from: landing.html
      to: assets/landing.css
      via: ".screenshot-tile class applied to 3 placeholder divs"
      pattern: "screenshot-tile"
---

<objective>
Add two missing sections to the landing page: a screenshots section with 3 placeholder tiles, and a contact section with a placeholder email.

Purpose: Screenshots provide social proof and let prospects see the UI before buying. A contact section is a standard expectation for any product landing page.
Output: Two new HTML sections in landing.html, CSS styles in landing.css, i18n strings added to all 4 language objects in landing.js, applyLang() wiring for both sections.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Interface context extracted from source files -->
<interfaces>
From landing.html — current section order (for correct insertion points):

```
1. <header class="landing-header">
2. <section class="landing-hero">
3. <div class="botanical-divider">
4. <section class="landing-section"> (Features)
5. <section class="landing-section landing-section-alt"> (Who is it for)
6. <section class="landing-section"> (Sound familiar?)
   ← INSERT SCREENSHOTS SECTION HERE (after "Sound familiar", before "Why I built this")
7. <section class="landing-section landing-section-alt"> (Why I built this)
8. <section class="landing-pricing"> (Pricing)
9. <hr class="legal-divider">
10. <section id="faq"> (FAQ)
    ← INSERT CONTACT SECTION HERE (after FAQ, before legal sections)
11. <hr class="legal-divider">
12. <section id="impressum"> (Impressum)
13. <hr class="legal-divider">
14. <section id="datenschutz"> (Datenschutz)
15. <footer>
```

From assets/landing.js — applyLang() rendering patterns to follow:

```javascript
// Pattern from existing "who is it for" section:
setText('whofor-title', t.whoForTitle);
setText('whofor-subtitle', t.whoForSubtitle);
var whoForItems = document.querySelectorAll('.who-for-list li');
t.whoForItems.forEach(function(item, i) {
  if (whoForItems[i]) whoForItems[i].textContent = item;
});

// Screenshots section renderer to add — add after FAQ rendering block:
setText('screenshots-title', t.screenshotsTitle);
setText('screenshots-subtitle', t.screenshotsSubtitle);
var screenshotLabels = document.querySelectorAll('.screenshot-label');
t.screenshotLabels.forEach(function(label, i) {
  if (screenshotLabels[i]) screenshotLabels[i].textContent = label;
});

// Contact section renderer to add — add after screenshots renderer:
setText('contact-title', t.contactTitle);
setText('contact-text', t.contactText);
```

From assets/landing.css — design token patterns for new section CSS:

```css
/* Section padding pattern (follow exactly): */
.landing-section {
  padding: 5rem 2rem;
  max-inline-size: 1100px;
  margin-inline: auto;
}

/* Alt-background pattern: */
.landing-section-alt {
  background: color-mix(in srgb, var(--color-surface-alt) 85%, var(--color-primary) 15%);
}

/* Available tokens: */
--color-primary       /* teal/garden green */
--color-surface       /* page background */
--color-surface-alt   /* slightly different background */
--color-text          /* main text */
--color-text-muted    /* secondary text */
--color-border        /* border color */
```

From assets/landing.css — responsive pattern (use logical properties, never left/right/top/bottom):
```css
/* Use margin-inline, padding-inline, padding-block, inset-inline-start, etc. */
/* RTL is handled by document dir="rtl" on html element — no extra CSS needed */
```

i18n keys to add to all 4 language objects (en/he/de/cs) in LANDING_I18N:

For screenshots section:
- screenshotsTitle: string
- screenshotsSubtitle: string
- screenshotLabels: array of 3 strings ['Dashboard', 'Session view', 'Client view'] (labels translated per language)

For contact section:
- contactTitle: string
- contactText: string (includes placeholder email — use [contact@sessions.garden])
- No separate contactEmail key needed; embed in contactText or use a data attribute approach

Languages to update: en (lines ~27–123), he (lines ~125–221), de (lines ~223–), cs (the 4th language object after de)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add screenshots section to HTML, CSS, and i18n</name>
  <files>landing.html, assets/landing.css, assets/landing.js</files>
  <action>
    STEP 1 — landing.html: Insert the screenshots section after the "Sound familiar?" section (after its closing `</section>` tag, around line 365) and before the "Why I built this" section:

    ```html
    <!-- =========================================================
         SCREENSHOTS
         ========================================================= -->
    <section id="screenshots" class="landing-section landing-section-screenshots" aria-labelledby="screenshots-title">
      <h2 id="screenshots-title" class="landing-section-title">See it in action</h2>
      <p id="screenshots-subtitle" class="landing-section-subtitle">A closer look at what's inside</p>
      <div class="screenshots-grid">
        <div class="screenshot-tile">
          <div class="screenshot-placeholder-img" aria-hidden="true">
            <!-- TODO: Replace with <img src="assets/screenshots/dashboard.png" alt="Dashboard"> when ready -->
          </div>
          <p class="screenshot-label">Dashboard</p>
        </div>
        <div class="screenshot-tile">
          <div class="screenshot-placeholder-img" aria-hidden="true">
            <!-- TODO: Replace with <img src="assets/screenshots/session.png" alt="Session view"> when ready -->
          </div>
          <p class="screenshot-label">Session view</p>
        </div>
        <div class="screenshot-tile">
          <div class="screenshot-placeholder-img" aria-hidden="true">
            <!-- TODO: Replace with <img src="assets/screenshots/client.png" alt="Client view"> when ready -->
          </div>
          <p class="screenshot-label">Client view</p>
        </div>
      </div>
    </section>
    ```

    STEP 2 — assets/landing.css: Add new CSS at the bottom of the file (after the last existing rule):

    ```css
    /* =========================================================
       SCREENSHOTS SECTION
       ========================================================= */
    .landing-section-screenshots {
      text-align: center;
    }

    .screenshots-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-block-start: 2.5rem;
    }

    @media (max-width: 820px) {
      .screenshots-grid {
        grid-template-columns: 1fr;
        max-inline-size: 480px;
        margin-inline: auto;
      }
    }

    .screenshot-tile {
      border-radius: 1rem;
      border: 1.5px solid color-mix(in srgb, var(--color-primary) 25%, transparent);
      background: color-mix(in srgb, var(--color-primary) 6%, var(--color-surface));
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    [data-theme="dark"] .screenshot-tile {
      background: color-mix(in srgb, var(--color-primary) 8%, var(--color-surface));
      border-color: color-mix(in srgb, var(--color-primary) 30%, transparent);
    }

    .screenshot-placeholder-img {
      inline-size: 100%;
      aspect-ratio: 16 / 10;
      background: color-mix(in srgb, var(--color-primary) 10%, var(--color-surface));
      display: flex;
      align-items: center;
      justify-content: center;
    }

    [data-theme="dark"] .screenshot-placeholder-img {
      background: color-mix(in srgb, var(--color-primary) 12%, var(--color-surface));
    }

    .screenshot-label {
      padding-block: 0.75rem;
      padding-inline: 1rem;
      font-size: 0.9rem;
      color: var(--color-text-muted);
      font-weight: 600;
      letter-spacing: 0.02em;
    }

    /* When real images arrive: swap .screenshot-placeholder-img with <img> */
    .screenshot-tile img {
      inline-size: 100%;
      aspect-ratio: 16 / 10;
      object-fit: cover;
      display: block;
    }

    /* =========================================================
       CONTACT SECTION
       ========================================================= */
    .landing-contact {
      padding-block: 4rem;
      padding-inline: 2rem;
      background: color-mix(in srgb, var(--color-surface-alt) 85%, var(--color-primary) 15%);
      text-align: center;
    }

    [data-theme="dark"] .landing-contact {
      background: var(--color-surface-alt);
    }

    .landing-contact-inner {
      max-inline-size: 640px;
      margin-inline: auto;
    }

    .landing-contact h2 {
      font-size: 1.6rem;
      margin-block-end: 0.75rem;
      color: var(--color-text);
    }

    .landing-contact p {
      color: var(--color-text-muted);
      font-size: 1.05rem;
      line-height: 1.6;
    }

    .landing-contact a {
      color: var(--color-primary);
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .landing-contact a:hover {
      opacity: 0.8;
    }
    ```

    STEP 3 — assets/landing.js: Add i18n strings for screenshots section to all 4 language objects (en, he, de, cs).

    Add these keys to LANDING_I18N.en (before the closing brace of the en object, after footerTagline):
    ```javascript
    screenshotsTitle: 'See it in action',
    screenshotsSubtitle: 'A closer look at what\'s inside',
    screenshotLabels: ['Dashboard', 'Session view', 'Client view'],
    ```

    Add equivalent translated keys to LANDING_I18N.he, LANDING_I18N.de, and LANDING_I18N.cs.

    Hebrew (he):
    ```javascript
    screenshotsTitle: 'ראו מה יש בפנים',
    screenshotsSubtitle: 'מבט מקרוב על האפליקציה',
    screenshotLabels: ['לוח בקרה', 'תצוגת פגישה', 'תצוגת לקוח'],
    ```

    German (de):
    ```javascript
    screenshotsTitle: 'Sieh es in Aktion',
    screenshotsSubtitle: 'Ein genauer Blick ins Innere',
    screenshotLabels: ['Dashboard', 'Sitzungsansicht', 'Klientenansicht'],
    ```

    Czech (cs) — add to the cs language object:
    ```javascript
    screenshotsTitle: 'Podívejte se na to v akci',
    screenshotsSubtitle: 'Bližší pohled na aplikaci',
    screenshotLabels: ['Přehled', 'Pohled na sezení', 'Pohled na klienta'],
    ```

    Then add the applyLang() rendering block for the screenshots section. Find the end of the FAQ rendering block (after the faqDds loop) and add:

    ```javascript
    // Screenshots
    setText('screenshots-title', t.screenshotsTitle);
    setText('screenshots-subtitle', t.screenshotsSubtitle);
    var screenshotLabels = document.querySelectorAll('.screenshot-label');
    if (t.screenshotLabels) {
      t.screenshotLabels.forEach(function(label, i) {
        if (screenshotLabels[i]) screenshotLabels[i].textContent = label;
      });
    }
    ```

    Note: Add the screenshotsTitle/screenshotsSubtitle/screenshotLabels keys to each language object by finding the `footerTagline:` line in each language block and adding after it (before the closing `},` of the language object). Each language object ends with footerTagline, then `},`.
  </action>
  <verify>
    <automated>grep -c "screenshot-tile" landing.html && grep -c "screenshotsTitle" assets/landing.js && grep -c "screenshot-tile" assets/landing.css && open landing.html</automated>
  </verify>
  <done>
    Screenshots section exists in landing.html with 3 `.screenshot-tile` divs. CSS in landing.css has `.screenshot-tile` and `.screenshots-grid` rules. All 4 language objects in landing.js have screenshotsTitle, screenshotsSubtitle, screenshotLabels. applyLang() wires the text. Page opens and shows 3 placeholder tiles with labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contact section to HTML, i18n, and applyLang()</name>
  <files>landing.html, assets/landing.js</files>
  <action>
    STEP 1 — landing.html: Insert the contact section after the FAQ section and before the first `<hr class="legal-divider">` that precedes the Impressum section. The insertion point is after the `</section>` closing tag of the FAQ section (around line 549):

    ```html
    <!-- =========================================================
         CONTACT
         ========================================================= -->
    <section id="contact" class="landing-contact" aria-labelledby="contact-title">
      <div class="landing-contact-inner">
        <h2 id="contact-title">Have questions?</h2>
        <p id="contact-text">We'd love to hear from you. Reach us at <a href="mailto:contact@sessions.garden">contact@sessions.garden</a></p>
        <!-- TODO: Replace contact@sessions.garden with Sapir's real email before launch -->
      </div>
    </section>
    ```

    STEP 2 — assets/landing.js: Add contact section i18n strings to all 4 language objects.

    The contact text must include the email as a plain string with a mailto link hint. Since the email address is in the HTML as a real `<a href="mailto:...">` element and the renderer uses `innerHTML` for the text node, we need to handle this carefully.

    Use a simpler approach: store the surrounding text and the email separately, OR use innerHTML for the contact text paragraph. Looking at the existing code, `setText` uses textContent. For the contact section, the email link needs to be kept as HTML. Therefore, add a `setInner` call for contact-text.

    Add a helper if not present (but check first — the existing code uses setText and setText2 which set textContent):
    The applyLang function already has `icon.innerHTML = f.icon` for icons. For contact-text, use direct innerHTML assignment in applyLang():

    In applyLang(), add after the screenshots block:
    ```javascript
    // Contact
    setText('contact-title', t.contactTitle);
    var contactTextEl = document.getElementById('contact-text');
    if (contactTextEl && t.contactText) contactTextEl.innerHTML = t.contactText;
    ```

    Add to each language object (after screenshotLabels, before closing brace):

    English (en):
    ```javascript
    contactTitle: 'Have questions?',
    contactText: 'We\'d love to hear from you. Reach us at <a href="mailto:contact@sessions.garden">contact@sessions.garden</a>',
    ```

    Hebrew (he):
    ```javascript
    contactTitle: 'יש לכם שאלות?',
    contactText: 'נשמח לשמוע מכם. פנו אלינו בכתובת <a href="mailto:contact@sessions.garden">contact@sessions.garden</a>',
    ```

    German (de):
    ```javascript
    contactTitle: 'Hast du Fragen?',
    contactText: 'Wir freuen uns von dir zu hören. Schreib uns an <a href="mailto:contact@sessions.garden">contact@sessions.garden</a>',
    ```

    Czech (cs):
    ```javascript
    contactTitle: 'Máte otázky?',
    contactText: 'Rádi od vás uslyšíme. Napište nám na <a href="mailto:contact@sessions.garden">contact@sessions.garden</a>',
    ```

    CSS for the contact section was already added in Task 1 (`.landing-contact`, `.landing-contact-inner`, etc.). No additional CSS needed in this task.
  </action>
  <verify>
    <automated>grep -c "id=\"contact\"" landing.html && grep -c "contactTitle" assets/landing.js && open landing.html</automated>
  </verify>
  <done>
    Contact section exists in landing.html with id="contact". All 4 language objects in landing.js have contactTitle and contactText. applyLang() sets contact-title and uses innerHTML for contact-text (preserving the mailto link). Contact section renders correctly at the bottom of the page above the legal sections.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "screenshot-tile" landing.html` returns 3 (one per tile)
2. `grep -c "screenshotsTitle" assets/landing.js` returns 4 (one per language)
3. `grep -c "contactTitle" assets/landing.js` returns 4 (one per language)
4. `grep -c "contact-title" assets/landing.js` returns 1 (the applyLang call)
5. Open landing.html — screenshots section visible between "Sound familiar" and "Why I built this"
6. Open landing.html — contact section visible after FAQ, before Impressum
7. Switch to Hebrew — both sections render RTL correctly with translated text
8. Switch to German — both sections show German text
9. Switch to Czech — both sections show Czech text
10. Toggle dark mode — both sections look correct
11. Resize to mobile (< 820px) — screenshots stack vertically, contact section readable
</verification>

<success_criteria>
- Screenshots section exists with 3 tasteful placeholder tiles at the correct position in the page flow
- Contact section exists at the bottom of the main content area with a placeholder email
- All new section text translates correctly in en/he/de/cs
- Hebrew RTL layout is correct in both new sections
- Page flow: features → who it's for → pain points → screenshots → founder story → pricing → FAQ → contact → legal
- No JavaScript console errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-landing-page-polish/05.2-02-SUMMARY.md`
</output>
