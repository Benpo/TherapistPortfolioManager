---
phase: 05.2-landing-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/landing.css
  - assets/landing.js
  - landing.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hero botanical illustrations feel delicate and subtle — not dominant or heavy"
    - "Section dividers have consistent, lighter botanical visual weight"
    - "Feature card icons look beautiful and cohesive with the hand-drawn garden aesthetic"
  artifacts:
    - path: assets/landing.css
      provides: "Reduced opacity rules for all 5 botanical CSS contexts"
      contains: "opacity: 0.22"
    - path: landing.html
      provides: "Reduced stroke-widths on hero botanical SVG paths"
      contains: "stroke-width=\"1.4\""
    - path: assets/landing.js
      provides: "6 new DOODLE_ICONS SVG strings with organic hand-drawn style"
      contains: "DOODLE_ICONS"
  key_links:
    - from: assets/landing.css
      to: ".hero-botanical"
      via: "opacity property"
      pattern: "\\.hero-botanical"
    - from: assets/landing.js
      to: "feature cards"
      via: "DOODLE_ICONS keys referenced by features arrays in all 4 language objects"
      pattern: "DOODLE_ICONS\\."
---

<objective>
Polish the existing botanical illustrations and feature card icons to match Sapir's visual intent — more delicate, beautiful, and cohesive with the garden theme.

Purpose: The botanical illustrations are too heavy at current opacity; the feature card icons already have a good botanical hand-drawn style from Phase 05.1 but need refinement to be truly beautiful. This is a visual polish pass with no structural changes.
Output: Updated landing.css with lighter botanical opacities, updated landing.html with thinner SVG stroke-widths on hero botanicals, updated landing.js with 6 new beautiful DOODLE_ICONS SVGs.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Interface context extracted from source files -->
<interfaces>
From assets/landing.css — ALL botanical CSS contexts that need opacity reduction:

```css
/* Line 88–94: Hero botanicals — currently opacity: 0.4 */
.hero-botanical {
  position: absolute;
  top: -10px;
  color: var(--color-primary);
  opacity: 0.4;     /* CHANGE TO: 0.22 */
  pointer-events: none;
}

/* Line 106–109: Responsive (1100px breakpoint) */
@media (max-width: 1100px) {
  .hero-botanical {
    opacity: 0.28;  /* CHANGE TO: 0.18 */
  }
}

/* Line 129–136: Botanical section divider — currently opacity: 0.55 */
.botanical-divider {
  opacity: 0.55;  /* CHANGE TO: 0.38 */
}

/* Line 776–781: Positioned botanical decorations — currently opacity: 0.3 */
.botanical-positioned {
  opacity: 0.3;   /* CHANGE TO: 0.20 */
}

/* Line 793–801: Botanical divider alt — currently opacity: 0.55 */
.botanical-divider-alt {
  opacity: 0.55;  /* CHANGE TO: 0.38 */
}

/* Dark mode (lines 276–282): hero-botanical and botanical-divider dark mode
   use color override only — no opacity set separately, inherits from above */
```

From landing.html — hero botanical SVG stroke-widths (lines 55–151):

Left botanical (lines 55–98):
- Main stem: stroke-width="2.3"  → reduce to "1.6"
- Root marks: stroke-width="1.1" → reduce to "0.8"
- Leaf outlines: stroke-width="1.7" / "1.6" / "1.4" / "1.2" → reduce each by ~0.3
- Leaf veins: stroke-width="0.85" / "0.8" / "0.75" / "0.7" → reduce each by ~0.15
- Berry circles: stroke-width="1.2" / "1.1" → reduce to "0.9" / "0.8"
- Bud/small accents: stroke-width="1.1" / "0.9" → reduce to "0.8" / "0.7"
- Flower petals: stroke-width="1.5" / "1.4" / "1.3" → reduce to "1.2" / "1.1" / "1.0"
- Flower center circle: stroke-width="1.2" → reduce to "0.9"

Right botanical (lines 102–151):
- Central stem: stroke-width="2.1" → reduce to "1.5"
- Branches: stroke-width="1.6" / "1.5" / "1.4" → reduce each by ~0.3
- Leaf outlines: stroke-width="1.4" / "1.3" / "1.2" → reduce each by ~0.3
- Leaf veins: stroke-width="0.8" / "0.75" / "0.7" → reduce each by ~0.15
- Berry circles: stroke-width="1.4" / "1.3" / "1.2" / "1.1" → reduce each by ~0.25
- Flower petals: stroke-width="1.5" → reduce to "1.1"
- Side petals, bud center: stroke-width="1.1" → reduce to "0.8"

Note: The botanical divider SVG in landing.html (lines 188–247) has its own inline stroke-widths.
Reduce main stem from stroke-width="2.2" → "1.6", other elements proportionally (~0.3 reduction).

From assets/landing.js — DOODLE_ICONS keys (lines 11–23):

Current keys: folder, lock, offline, sparkle, coin, notebook
These 6 keys are referenced directly in ALL 4 language features arrays:
  DOODLE_ICONS.folder, DOODLE_ICONS.lock, DOODLE_ICONS.offline,
  DOODLE_ICONS.sparkle, DOODLE_ICONS.coin, DOODLE_ICONS.notebook

SVG requirements for replacement icons:
- width="36" height="36" viewBox="0 0 36 36"
- fill="none" stroke="currentColor"
- stroke-linecap="round" stroke-linejoin="round"
- Organic/hand-drawn bezier curves (not straight lines or perfect arcs)
- Varied stroke-widths (range 0.8–1.7) — not uniform
- Each icon should have at least ONE botanical accent: small leaf, vine tendril, berry, or bud
- Style reference: existing DOODLE_ICONS SVGs use this pattern successfully
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce botanical illustration opacity and stroke-widths</name>
  <files>assets/landing.css, landing.html</files>
  <action>
    In assets/landing.css, reduce opacity on all 5 botanical CSS contexts:
    - `.hero-botanical` base rule: opacity 0.4 → 0.22
    - `.hero-botanical` inside `@media (max-width: 1100px)`: opacity 0.28 → 0.18
    - `.botanical-divider`: opacity 0.55 → 0.38
    - `.botanical-positioned`: opacity 0.3 → 0.20
    - `.botanical-divider-alt`: opacity 0.55 → 0.38

    In landing.html, reduce stroke-widths on the hero botanical SVGs to make them feel more delicate:

    Left botanical (first SVG, viewBox="0 0 210 370"):
    - Main stem path: 2.3 → 1.6
    - Root mark paths: 1.1 → 0.8, 0.9 → 0.65
    - Leaf outline paths (the closed Z paths): 1.7 → 1.3, 1.6 → 1.2, 1.4 → 1.1, 1.2 → 0.9
    - Leaf vein paths: 0.85 → 0.65, 0.8 → 0.6, 0.75 → 0.55, 0.7 → 0.55
    - Berry circles: 1.2 → 0.9, 1.1 → 0.8
    - Bud/small accents: 1.1 → 0.8
    - Flower petals: 1.5 → 1.1, 1.4 → 1.0, 1.3 → 0.95
    - Flower center circle: 1.2 → 0.9

    Right botanical (second SVG, viewBox="0 0 195 318"):
    - Central stem path: 2.1 → 1.5
    - Branch paths: 1.6 → 1.2, 1.5 → 1.1, 1.4 → 1.0
    - Leaf outlines: 1.4 → 1.0, 1.3 → 0.95, 1.2 → 0.9
    - Leaf veins: 0.8 → 0.6, 0.75 → 0.55, 0.7 → 0.55
    - Berry circles: 1.4 → 1.0, 1.3 → 0.95, 1.2 → 0.9, 1.1 → 0.8
    - Flower petals: 1.5 → 1.1
    - Side petals, bud center: 1.1 → 0.8

    Botanical divider SVG (inside `.botanical-divider` div, lines ~188–247):
    - Central branch path: 2.2 → 1.6
    - Left/right twig stroke-widths: 1.8 → 1.3
    - Leaf outline paths (Z closed): 1.7 → 1.2
    - Leaf vein paths: 1.0 → 0.7
    - Flower stem: 2.0 → 1.4
    - Flower petals: 1.7 → 1.2, 1.6 → 1.1, 1.5 → 1.0
    - Flower center circle: 1.6 → 1.1
    - Small buds: 1.3 → 0.9, 1.4 → 1.0
    - Decorative vine opacity lines: leave as-is (they are already subtle)
    - Berry circles: 1.5 → 1.0
  </action>
  <verify>
    <automated>grep -c "opacity: 0.22" assets/landing.css && grep -c "opacity: 0.18" assets/landing.css && grep -c "opacity: 0.38" assets/landing.css && open landing.html</automated>
  </verify>
  <done>
    All 5 botanical opacity rules updated in landing.css. Hero botanical SVG paths have reduced stroke-widths throughout landing.html. Page opens and botanicals appear visibly lighter and more delicate than before.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace DOODLE_ICONS with beautiful hand-drawn icons</name>
  <files>assets/landing.js</files>
  <action>
    Replace the SVG string values for all 6 keys in the DOODLE_ICONS object in assets/landing.js (lines 11–23).
    Keep the same 6 key names: folder, lock, offline, sparkle, coin, notebook.
    Do NOT change the keys — the language arrays reference them directly.

    Each replacement SVG must:
    - Use width="36" height="36" viewBox="0 0 36 36" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"
    - Have varied stroke-widths (mix of 0.9, 1.1, 1.3, 1.5, 1.7)
    - Use organic bezier curves (C or Q path commands), not straight lines
    - Include at least one botanical accent per icon (tiny leaf, vine curl, small bud, or berry)
    - Feel cohesive with the existing botanical garden aesthetic

    Design intent per icon:
    - folder: An open folder with a small vine/leaf curling from the top corner
    - lock: A padlock body with a delicate floral/leaf accent on one side
    - offline: A device/laptop outline with a small plant sprig growing from one corner
    - sparkle: A 4-point star/sparkle with small leaf tips extending from the star points
    - coin: A coin/circle with a currency symbol and tiny leaf or bud embellishment
    - notebook: An open journal/notebook with lines inside and a botanical bookmark/sprig

    The botanical accents should be small (4–8 units in the 36x36 space) and use stroke-widths of 0.9–1.1.
    Main icon shapes use stroke-widths of 1.5–1.7.
    Secondary shapes (inner details) use stroke-widths of 1.1–1.3.

    After writing the SVGs, verify the JavaScript syntax is valid ES5: no template literals,
    all SVG attribute values in double quotes inside single-quoted JS strings, proper escaping.
  </action>
  <verify>
    <automated>node -e "var DOODLE_ICONS = {}; eval(require('fs').readFileSync('assets/landing.js','utf8').match(/var DOODLE_ICONS = \{[\s\S]*?\};/)[0]); console.log(Object.keys(DOODLE_ICONS).join(', '))"</automated>
  </verify>
  <done>
    All 6 DOODLE_ICONS keys contain new SVG strings. `node` eval confirms the DOODLE_ICONS object parses without errors and all 6 keys are present. Feature card icons in the browser show beautiful hand-drawn botanical-accented icons.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "opacity: 0.22" assets/landing.css` returns a match
2. `grep "opacity: 0.38" assets/landing.css` returns at least 2 matches (divider + divider-alt)
3. `node -e "..."` confirms DOODLE_ICONS parses with all 6 keys present
4. Open landing.html in browser — hero botanicals are visibly lighter/more delicate
5. Toggle to dark mode — botanicals still visible and coherent
6. Scroll to features section — all 6 icons render with new designs
7. Switch to Hebrew — all 6 icons still render (they're shared via DOODLE_ICONS keys)
</verification>

<success_criteria>
- Hero botanical SVGs appear delicate and subtle — supporting the layout rather than competing with the text
- Section dividers have proportionally lighter visual weight
- All 6 feature card icons display beautiful organic hand-drawn designs with botanical accents
- All changes work in light mode, dark mode, and all 4 languages (en/he/de/cs)
- JavaScript syntax is valid ES5 (no syntax errors in browser console)
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-landing-page-polish/05.2-01-SUMMARY.md`
</output>
