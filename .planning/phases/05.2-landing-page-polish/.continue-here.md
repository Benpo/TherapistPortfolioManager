---
phase: 05.2-landing-page-polish
task: visual-review-in-progress
status: in_progress
last_updated: 2026-03-16T13:11:46.068Z
---

<current_state>
Phase 05.2 execution is complete (plans 01 and 02 done, committed). Plan 03 is the visual review checkpoint — Sapir reviewed and gave detailed feedback. A first round of fixes was committed (63b5a82), but several issues remain. The phase is NOT yet marked complete. Verification has not run yet.

The correct next step is `/gsd:verify-work 05.2` in a fresh conversation to handle remaining visual fixes + formal verification.
</current_state>

<completed_work>

**Plans executed (waves 1–2):**
- Plan 05.2-01: Aurora hero animation, real PNG botanical images (hero, dividers, footer), PNG feature card icons, spotlight glow on hover
- Plan 05.2-02: Screenshots section (3 app screens), contact section, accordion legal, content box glow, section separator, full i18n (4 languages)

**Round 1 visual fixes (commit 63b5a82):**
- Hero gradient darkened to forest green (was blinding mint)
- `mix-blend-mode: multiply` on botanical dividers + feature icons (no sticker effect)
- Hero botanicals: `filter: invert(1)` + `mix-blend-mode: screen` (dark bg compatible)
- Feature icons centered (`justify-content: center`)
- Features CTA button: added `id="features-cta"` + i18n translation
- Screenshots: 2-column grid, `object-fit: contain`, 4 screenshots (added `edit session.png`, `add new session pic.png`; removed blurry `patirnt session_small.png`)
- Pricing SVG stem decoration replaced with `גינה 2.png`
- Botanical divider spacing reduced
</completed_work>

<remaining_work>

**Visual issues still open (from Sapir's second review):**

1. **Botanical illustration background** — PNG backgrounds are NOT white (they have a tinted/off-white background that doesn't match page). `mix-blend-mode: multiply` didn't fully solve it. Need exact color matching OR a different blend approach.

2. **Botanical divider spacing** — spacing between wide botanical images and the text below is still too large. Persists everywhere. Need further reduction.

3. **Screenshots** — Current state is bad:
   - Short screenshots leave large grey background fill that looks ugly
   - `patirnt session_small.png` (the "real therapy session with filled fields") is the MOST important screenshot and it's missing — Sapir wants it included even if small resolution
   - Top two screenshots (Dashboard + Session notes / edit session) look too similar
   - Need to decide which 3-4 to keep that tell a clear story: "here's what you'll actually use"
   - Consider: homepage, patirnt_session_small (real therapy), addnewclient — or a curated set
   - Maybe use `aspect-ratio: auto` with padding instead of grey fill

4. **Hero contrast** — Better than before but still slightly too aggressive contrast. Sapir wants:
   - Slightly lighter green (not as dark)
   - Text in a slightly darker shade (currently pure white, could be soft cream/warm white)

5. **Additional bugs** — Sapir mentioned more bugs she hasn't listed yet. Need to surface in verify-work.
</remaining_work>

<decisions_made>

- **Aurora colors**: Shifted from `#e8f5e4` (mint) to `#1e4831`/`#2d6445` (forest green) — too dark per Sapir's round 2 feedback, needs slight lightening
- **Botanical blending**: Using CSS blend modes (multiply/screen) rather than re-exporting PNGs — quick fix approach, may need refinement
- **Screenshots layout**: 2-column grid chosen over 3-column; `object-fit: contain` chosen over `cover` to show full screenshots
- **Pricing decoration**: Replaced inline SVG stem with `גינה 2.png` at reduced size/opacity
- **Feature icons**: Centered in card (`justify-content: center`)
- **Features CTA**: Added `id="features-cta"` and reuses `pricingCta` i18n key
- **המשפך בסוף** (footer funnel decoration) — Sapir loves it, considering it for the app logo
</decisions_made>

<blockers>

- None blocking. All changes are CSS/HTML/JS — easily adjustable.
- Screenshots selection needs Sapir's final curation decision (which 3-4 tell the best story)
</blockers>

<context>
This is a UAT/visual-polish phase. The design language is: soft botanicals, forest green, cream/warm whites, elegant simplicity. The target audience is energy healers and holistic therapists.

Key asset files:
- `./assets/גינה.png`, `./assets/גינה 2.png` — wide botanical meadow images (section dividers)
- `./assets/משפך.png` — funnel/spout decoration (Sapir's favorite, footer)
- `./assets/icons/` — alim, ale, atsitz, tulip, vassa, merizza PNGs (feature card icons)
- `./assets/screenshots/` — homepage pic.png, edit session.png, addnewclient pic.png, add new session pic.png, patirnt session_small.png

The conversation got long mid-execute because visual review surfaced many issues. Continue in verify-work for proper UAT loop.
</context>

<next_action>
Run `/gsd:verify-work 05.2` in a fresh conversation.
Sapir will continue describing visual bugs and you apply fixes iteratively.
Start by reading: landing.html, assets/landing.css, this file, and .planning/STATE.md.
Then ask Sapir to describe remaining bugs OR open the page and start reviewing.
</next_action>
