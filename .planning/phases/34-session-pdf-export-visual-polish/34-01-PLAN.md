---
phase: 34-session-pdf-export-visual-polish
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/branding/icon-512-base64.js
  - sw.js
autonomous: true
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-05/FN-3: the session-export logo is an embedded base64 PNG — no runtime fetch, export works fully offline."
    - "The base64 module is in the SW precache so an installed PWA can render the logo with zero network."
  artifacts:
    - "assets/branding/icon-512-base64.js exposing window.IconLogoBase64 (raw base64 of icon-512.png), mirroring assets/fonts/heebo-base64.js (D-05)."
    - "sw.js PRECACHE_URLS contains the new module; CACHE token bumped so installed clients pick it up."
  key_links:
    - "icon-512.png bytes → base64 module → consumed by pdf-export.js drawHeaderBand via doc.addImage (wired in 34-06) (D-05)."
    - "byte-identical fallback: precached /assets/branding/logo-512.png already in PRECACHE_URLS (D-05 fallback)."
---

<objective>
Produce the offline-safe logo asset for the redesigned PDF (D-05 / FN-3). Encode `assets/branding/icon-512.png` as a base64 JS module loaded in the existing lazy-load chain (zero fetch, deterministic for tests), and register it in the service worker precache.

Purpose: The header band (34-06) embeds the app icon via `doc.addImage`. Per D-05 the logo must never be fetched — it is embedded so export works in an installed PWA with no network. This plan creates the embeddable bytes and closes the offline gap (Pitfall 4: `icon-512.png` is NOT currently in PRECACHE_URLS).
Output: `assets/branding/icon-512-base64.js` (`window.IconLogoBase64`); updated `sw.js` precache + CACHE token.

## Artifacts this phase produces (★ = owned by this plan)
- ★ `assets/branding/icon-512-base64.js` (`window.IconLogoBase64`)
- ★ sw.js PRECACHE_URLS entry + CACHE token bump
- pdf-export.js render fns: drawHeaderBand, drawClientCard, drawSeverityBlock, drawFooterBand (34-06/07/09)
- export-modal.js: deriveSessionOrdinal + buildSessionPDF input contract (34-05)
- add-session.js: extracted reusable saveSessionForm() (34-08)
- tests/34-{logo-embed,pill-localized,rtl-newblocks,severity-bars,session-ordinal,save-before-export}.test.js (34-03/04)
- regenerated `.planning/fixtures/phase-23/*.sha256` baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode icon-512.png as a base64 JS module</name>
  <read_first>
    - assets/fonts/heebo-base64.js (the analog: provenance header comment + `window.Heebo = "<base64>";` shape)
    - assets/pdf-export.js lines 91-114 (ensureDeps lazy-load chain — where this module will be slotted in 34-06)
    - 34-RESEARCH.md § Common Pitfalls (Pitfall 4) + § Architectural Responsibility Map (logo tier)
  </read_first>
  <files>assets/branding/icon-512-base64.js</files>
  <action>
    Create `assets/branding/icon-512-base64.js` exposing `window.IconLogoBase64` as the RAW base64 string of `assets/branding/icon-512.png` (no `data:` prefix — mirror how `window.Heebo` holds raw base64). Generate the literal by reading the PNG bytes (e.g. `node -e "process.stdout.write(require('fs').readFileSync('assets/branding/icon-512.png').toString('base64'))"`) and writing the result into the module. Prepend a provenance header comment block mirroring `heebo-base64.js`: source asset path, that it is byte-identical to the precached `logo-512.png` (md5 f78c482f4d141b487fa68ce26f7a9558), the consumer (`pdf-export.js drawHeaderBand → doc.addImage(window.IconLogoBase64, 'PNG', ...)`), and the D-05 rationale (embedded, never fetched). Do NOT add a build step — this is a committed vendored asset like the Heebo modules.
  </action>
  <verify>
    <automated>node -e "global.window={}; require('./assets/branding/icon-512-base64.js'); const b=Buffer.from(window.IconLogoBase64,'base64'); if(!(b.length>1000 && b[0]===0x89 && b[1]===0x50 && b[2]===0x4e && b[3]===0x47)){process.exit(1)} console.log('PNG magic ok, bytes=',b.length)"</automated>
  </verify>
  <done>
    - `window.IconLogoBase64` is a non-empty base64 string that decodes to a valid PNG (magic bytes 89 50 4E 47) of meaningful size (>1KB).
    - Module assigns to `window.IconLogoBase64` and self-contains (no imports, no fetch), matching the `heebo-base64.js` global-assignment pattern.
    - A provenance header comment names the source asset, the byte-identical logo-512.png fallback, and the D-05 offline rationale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the module to the SW precache and bump the CACHE token</name>
  <read_first>
    - sw.js lines 16-19 (CACHE_NAME derived from AppVersion.INTEGRITY_TOKEN), 26-80 (PRECACHE_URLS — note logo-512.png at :56, heebo modules at the tail)
    - $HOME memory reference-pre-commit-sw-bump (the pre-commit hook SKIPS the CACHE_NAME bump when sw.js is in the diff — a manual chore commit may be needed)
    - .planning/STATE.md (Phase 19/28 SW CACHE bump precedents)
  </read_first>
  <files>sw.js</files>
  <action>
    Add `'/assets/branding/icon-512-base64.js'` to `PRECACHE_URLS` (place it beside the other `/assets/branding/*` and font modules so the logo is available offline). Then bump the SW CACHE token following the existing version-bump convention (CACHE_NAME is derived from `AppVersion.INTEGRITY_TOKEN` in `assets/version.js`; bump the app version footer per `project-version-bump-convention` so the integrity-derived token changes and installed PWAs re-precache). Per `reference-pre-commit-sw-bump`: because sw.js is in this diff the pre-commit hook will NOT auto-bump — verify the token actually changed after commit and, if not, follow up with the documented manual chore. Do NOT remove the existing `/assets/branding/logo-512.png` entry (D-05 byte-identical fallback stays precached).
  </action>
  <verify>
    <automated>grep -q "assets/branding/icon-512-base64.js" sw.js && grep -q "assets/branding/logo-512.png" sw.js && echo "precache entries present"</automated>
  </verify>
  <done>
    - `PRECACHE_URLS` lists `/assets/branding/icon-512-base64.js` AND still lists `/assets/branding/logo-512.png`.
    - The SW CACHE token / app version is bumped so installed clients invalidate and re-precache (verified post-commit per the pre-commit-sw-bump note).
    - No other PRECACHE_URLS entries are removed or reordered destructively.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's IndexedDB → local jsPDF render → Blob download | First-party data only; the logo is a static bundled asset; no network egress and no untrusted input cross any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | bundled logo asset | low | accept | Asset is a committed first-party PNG re-encoded verbatim; no registry interaction, no remote fetch (D-05). |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — all deps vendored/locked (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `assets/branding/icon-512-base64.js` decodes to a valid PNG and assigns `window.IconLogoBase64`.
- sw.js precache contains the module + the logo-512.png fallback; CACHE token bumped.
- No production behavior change yet (the module is consumed by 34-06).
</verification>

<success_criteria>
The offline logo bytes exist as an embeddable base64 module and are precached, unblocking the header-band `addImage` in 34-06 with zero-network guarantee (D-05 / FN-3).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-01-SUMMARY.md` when done.
</output>
