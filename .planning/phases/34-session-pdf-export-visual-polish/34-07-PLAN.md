---
phase: 34-session-pdf-export-visual-polish
plan: 7
type: execute
wave: 3
depends_on: [34-06, 34-03]
files_modified:
  - assets/pdf-export.js
autonomous: true
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-06: section headings render with a #456b42 16pt bold label, a #7da877 rotated-square 'leaf-diamond' bullet (two triangle() calls) and a #bfe0b0 rule beneath, top margin 24pt / bottom 8pt."
    - "D-07: the trapped-emotions / summary body stays free-text paragraphs via the existing splitTextToSize + parseInlineBold path (unchanged), at 11.5pt normal #2f2d38, line-height ≈1.65."
    - "D-09: every page ends with a full-bleed footer band — top rule, three zones (made-with brand-as-tool mark + small logo / Page X of Y / 'Exported on' + date)."
    - "D-10: the leaf-diamond is symmetric (no RTL mirroring needed); footer zones mirror under RTL; all footer text anchors by docDir with isInputVisual:false."
    - "D-12: sizes/spacing/palette match 34-UI-SPEC + FINAL mockup; Heebo normal+bold only."
  artifacts:
    - "Section-heading branch extended with the leaf-diamond bullet + vein rule + margins; body typography constants bumped (BODY_SIZE→11.5, body line-height ≈19); drawFooterBand() extending the footer pass (1158-1182)."
  key_links:
    - "leaf-diamond via two triangle() calls about a centre (34-RESEARCH Pattern 3); footer reuses window.IconLogoBase64 (34-01) for the small mark and shapeForJsPdf for text; pagination keeps the existing per-locale label (D-06/D-09/D-10)."
---

<objective>
Style the mid-document chrome to the locked design: leaf-diamond section headings with a vein rule (D-06), the airier free-text body typography (D-07/D-12), and a full-bleed three-zone footer band on every page (D-09). The body block loop itself stays the existing free-text path — only its heading styling and type scale change; severity is handled separately in 34-09.

Purpose: Give sections a branded, scannable rhythm and a consistent brand-as-tool footer without disturbing the verified RTL body pipeline.
Output: extended heading branch + bumped body type constants + `drawFooterBand()` in `assets/pdf-export.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ leaf-diamond section heading + vein rule (heading branch); ★ body typography bump; ★ drawFooterBand()
- drawHeaderBand/drawClientCard (34-06); drawSeverityBlock (34-09); logo (34-01); i18n footer keys (34-02); tests (34-03/04); baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-UI-SPEC.md
@.planning/phases/34-session-pdf-export-visual-polish/design-mockups/FINAL-mockup.html
@assets/pdf-export.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leaf-diamond section heading + vein rule; airier free-text body typography</name>
  <read_first>
    - assets/pdf-export.js lines 699-722 (constants: BODY_SIZE=11, HEADING_SIZE=16, LINE_HEIGHT_BODY=16), 849-872 (drawTextLine), 961-980 (heading block in the render loop), 982-1155 (list/paragraph body path — leave structurally unchanged, D-07)
    - 34-RESEARCH.md § Pattern 3 (leaf-diamond via two triangle() calls) + § State of the Art (type-scale bump)
    - 34-UI-SPEC.md § Component Inventory item 3 + item 4 + § Typography (body 11.5pt, line-height 1.65) + § Spacing; 34-CONTEXT.md D-06, D-07, D-12; FINAL-mockup.html
  </read_first>
  <files>assets/pdf-export.js</files>
  <action>
    In the heading branch of the render loop: before drawing the label, draw a leaf-diamond bullet at the START edge — a #7da877 45° square ≈9pt made of two triangle() calls about a centre (cx,cy,d): triangle(cx,cy-d, cx+d,cy, cx,cy+d,'F') and triangle(cx,cy-d, cx-d,cy, cx,cy+d,'F') (symmetric — identical LTR/RTL, no mirroring; 34-RESEARCH Pattern 3). Draw the label 16pt bold #456b42 4pt after the bullet (start-anchored via drawTextLine), then a #bfe0b0 rule (~1.5pt) spanning the content width ~4pt beneath the label baseline. Apply top margin 24pt / bottom 8pt. Under RTL the bullet sits at the right with the label to its left; the rule spans full content width either way. For the body: bump BODY_SIZE 11→11.5 and the body line-height to ≈19 (1.65) so the free-text paragraphs match the mockup's rhythm; keep ink colour #2f2d38. Do NOT change the splitTextToSize/parseInlineBold paragraph + list structure (D-07) — only sizes/leading/heading-chrome. Keep Heebo normal/bold only.
  </action>
  <verify>
    <automated>node tests/pdf-digit-order.test.js && node tests/pdf-glyph-coverage.test.js && node tests/pdf-bold-rendering.test.js && node tests/quick-260608-c8x-fix-pdf-list-rendering.test.js 2>/dev/null || node tests/run-all.js</automated>
  </verify>
  <done>
    - Section headings show the leaf-diamond bullet (two triangles) + #bfe0b0 rule with the 24/8pt margins (D-06); body renders free-text at 11.5pt / ~1.65 leading (D-07/D-12).
    - The free-text body structure (paragraphs/lists/inline-bold) is unchanged; the RTL/order floor tests stay green — pdf-digit-order, pdf-glyph-coverage, pdf-bold-rendering, and the quick-* list tests pass (anchor/order robust to the size bump) (D-10).
  </done>
</task>

<task type="auto">
  <name>Task 2: drawFooterBand — full-bleed three-zone footer on every page</name>
  <read_first>
    - assets/pdf-export.js lines 1158-1182 (the footer pass loop over pages — the per-locale "Page X of Y" label already built here; extend it), 723 (FOOTER_BASELINE_Y), 849-872 (drawTextLine)
    - 34-UI-SPEC.md § Component Inventory item 6 (Footer band, three zones) + § Color (top rule #bfe0b0, muted #5f5c72); 34-CONTEXT.md D-09 ('Exported on' relabel); FINAL-mockup.html
    - assets/branding/icon-512-base64.js (small footer logo) + i18n keys pdf.footer.madeWith / pdf.footer.exportedOn (34-02)
  </read_first>
  <files>assets/pdf-export.js</files>
  <action>
    Extend the existing footer pass (the `for (pn …)` loop, 1158-1182) into a full-bleed footer band on EVERY page. Add `function drawFooterBand(pn,totalPages)` (or inline within the loop): a #bfe0b0 top rule (~1pt) spanning the content width; then three zones along the footer baseline — START: a small ~15pt logo (addImage window.IconLogoBase64) + pdf.footer.madeWith ("Made with Sessions Garden · sessionsgarden.app", #456b42 bold — brand-as-tool, not a letterhead); CENTER: the existing per-locale "Page X of Y" label (keep the current inline switch); END: pdf.footer.exportedOn + the exportedOn date (muted #5f5c72) — relabelled per D-09 to disambiguate from the card's session date. Under RTL the zones mirror (made-with on the right, exported-on on the left); every doc.text passes isInputVisual:false and anchors by docDir; numerals keep visual order. Source exportedOn from sessionData.exportedOn (34-05). Keep Heebo normal/bold only.
  </action>
  <verify>
    <automated>node tests/34-rtl-newblocks.test.js; node tests/pdf-digit-order.test.js && node tests/pdf-glyph-coverage.test.js</automated>
  </verify>
  <done>
    - Every page carries the full-bleed footer band: top rule + made-with mark (with small logo) + Page X of Y + 'Exported on' date (D-09).
    - Footer zones mirror correctly under RTL and digits keep visual order; pdf-digit-order + pdf-glyph-coverage stay green; 34-rtl-newblocks advances for the footer block (D-10).
    - Pagination label still localized; brand appears as a subtle tool mark, never a letterhead (D-09).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's IndexedDB data → local jsPDF render → Blob download | First-party data only; footer logo is a bundled asset; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-07 | Tampering | session text → heading/body/footer | low | mitigate | All text flows through the existing shapeForJsPdf/splitTextToSize path; no new injection surface; first-party content only. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Section headings show the leaf-diamond + vein rule; body free-text at the new scale; footer band on every page.
- RTL/order floor tests (pdf-digit-order, pdf-glyph-coverage, pdf-bold-rendering, quick-* list) stay green.
</verification>

<success_criteria>
The document's section rhythm and footer match the locked design (D-06/D-07/D-09/D-12) with the RTL body pipeline and content-stream floor tests intact (D-10).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-07-SUMMARY.md` when done.
</output>
