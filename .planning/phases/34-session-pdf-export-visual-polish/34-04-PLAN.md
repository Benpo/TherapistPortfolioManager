---
phase: 34-session-pdf-export-visual-polish
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/34-severity-bars.test.js
  - tests/34-session-ordinal.test.js
  - tests/34-save-before-export.test.js
autonomous: true
requirements: [PDFX-01, PDFX-02, PDFX-03]
must_haves:
  truths:
    - "D-03/FN-1: the Ben-flagged MUST-test — 3 dated sessions export ordinals 1/2/3; delete the middle → remaining renumber to 1/2 (no gap). Falsifiable: swapping derivation to session.id makes the renumber case fail."
    - "D-08: severity bar fill widths are proportional (value/10 × track) and the before fill uses a flat hex with NO opacity graphics-state op in the content stream."
    - "D-13/PDFX-03: a dirty/never-saved export shows the prompt; 'Save & export' persists (new session gains an id) then exports with the correct ordinal; 'Keep editing' dismisses; save-validation failure aborts the export (no PDF); the extracted save stays behavior-preserving for the save button."
  artifacts:
    - "tests/34-severity-bars.test.js, tests/34-session-ordinal.test.js, tests/34-save-before-export.test.js — authored RED, gating 34-05/08/09."
  key_links:
    - "ordinal test mocks PortfolioDB.getSessionsByClient (mock-portfolio-db.js) and pins the date-sort/tie-break derivation (D-03); severity test walks the content stream for fill geometry + absence of a GState op (D-08); save-before-export test drives the jsdom export trigger (D-13)."
---

<objective>
Author the three behavior/data Wave-0 tests BEFORE their implementations (project hard rule `feedback-behavior-verification`). The FN-1 ordinal test is the falsifiable spine of the phase (swap to `session.id` → the renumber case fails). All three are RED now and become the GREEN gate for 34-05 (deriveSessionOrdinal + input contract), 34-09 (severity bars), and 34-08 (save-before-export).

Purpose: Make the correctness contracts (renumber-on-delete, proportional bars without GState, no-stale-export guard) executable and falsifiable before any implementation.
Output: `tests/34-severity-bars.test.js`, `tests/34-session-ordinal.test.js`, `tests/34-save-before-export.test.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ tests/34-severity-bars.test.js, ★ tests/34-session-ordinal.test.js, ★ tests/34-save-before-export.test.js
- tests/34-{logo-embed,pill-localized,rtl-newblocks}.test.js (34-03); deriveSessionOrdinal + buildSessionPDF contract (34-05); drawSeverityBlock + markdown-drop (34-09); saveSessionForm() + prompt wiring (34-08); logo module (34-01); i18n (34-02); regenerated baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-VALIDATION.md
@.planning/phases/34-session-pdf-export-visual-polish/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 34-session-ordinal.test.js — derived chronological ordinal renumbers on delete (FN-1, the MUST-test)</name>
  <read_first>
    - 34-RESEARCH.md § Code Examples (deriveSessionOrdinal reference) + § Common Pitfalls (Pitfall 2) + Assumptions A1/A2
    - assets/db.js lines 976-986 (getSessionsByClient — unsorted index.getAll, the data source to mock) + 225 (sessions keyPath/autoIncrement)
    - tests/_helpers/mock-portfolio-db.js (the mock DB seam) + an existing data-behavior test for the self-exit pattern
    - assets/export-modal.js lines 27-31, 289-296 (ctx + data-assembly tier where deriveSessionOrdinal will live)
  </read_first>
  <files>tests/34-session-ordinal.test.js</files>
  <action>
    Author a behavior test that targets the ordinal-derivation contract in the data-assembly tier (the function 34-05 will add, e.g. `deriveSessionOrdinal(clientId, thisSessionId)`). Seed (via the mock PortfolioDB.getSessionsByClient) one client with 3 sessions on ISO dates d1<d2<d3 with autoIncrement ids that are NOT in date order (e.g. ids 7,3,9 to prove id-order ≠ date-order). Assert each session derives ordinal 1/2/3 by date. Then delete the middle (d2) session from the mock set, re-derive, and assert the former d3 session now derives #2 (renumbered, no gap). Add a same-date tie-break case (two sessions share a date → deterministic by numeric id). Make the falsifiability explicit in a header comment and structurally: if derivation used `session.id` (or unsorted getAll order) instead of the date sort, the renumber and tie-break assertions FAIL. RED now (function absent — gates 34-05).
  </action>
  <verify>
    <automated>node tests/34-session-ordinal.test.js; test $? -ne 0 && echo "RED as expected (deriveSessionOrdinal not implemented)"</automated>
  </verify>
  <done>
    - Seeds id-order ≠ date-order, asserts 1/2/3 by date, asserts renumber-to-1/2 after deleting the middle, asserts same-date tie-break by id.
    - Falsifiable: an id-based or unsorted derivation fails the renumber and tie-break assertions (the FN-1 spine).
    - RED now; the GREEN gate for 34-05.
  </done>
</task>

<task type="auto">
  <name>Task 2: 34-severity-bars.test.js — proportional fills, flat hex, no GState op (D-08)</name>
  <read_first>
    - 34-RESEARCH.md § Pattern 2 (bar fill from start edge) + § Anti-Patterns (flat hex, not opacity graphics-state) + § Validation Architecture (Severity row)
    - tests/30-issue-delta.test.js (the {name,before,after} payload contract this consumes — MUST stay green)
    - 34-UI-SPEC.md § Component Inventory item 5 (track 118×8pt, fill = value/10 × track, before #ee6a6a flat, after #2fb37d)
    - tests/pdf-glyph-coverage.test.js (content-stream walking)
  </read_first>
  <files>tests/34-severity-bars.test.js</files>
  <action>
    Author a content-stream test that passes a structured `issues[]` (e.g. [{name,before:8,after:3}]) directly into buildSessionPDF and asserts the two-bar geometry: the before fill width is proportional to before/10 of the track and the after fill width to after/10 (assert the ratio/relative widths from the drawn rectangle ops, robust to exact pt — e.g. before-fill-width > after-fill-width for 8 vs 3, and each ≈ value/10 × track within tolerance). Assert the before bar uses a flat fill colour (the pre-lightened red) and that the content stream contains NO opacity graphics-state operator (i.e. the deterministic-renderer rule — no `/GS`/`gs` ExtGState alpha op introduced by the severity draw). Assert the track background and both fills are emitted. RED now (drawSeverityBlock absent — gates 34-09). Keep colour assertions tolerant to the exact locked hex but strict on "flat fill, no alpha op".
  </action>
  <verify>
    <automated>node tests/34-severity-bars.test.js; test $? -ne 0 && echo "RED as expected (severity bars not drawn yet)"</automated>
  </verify>
  <done>
    - Asserts proportional before/after fill widths (value/10 × track) and that before-fill > after-fill for a higher before value.
    - Asserts a flat fill and the ABSENCE of an opacity/ExtGState op in the content stream (D-08 determinism).
    - RED now; the GREEN gate for 34-09.
  </done>
</task>

<task type="auto">
  <name>Task 3: 34-save-before-export.test.js — non-blocking guard, behavior-preserving extraction (PDFX-03)</name>
  <read_first>
    - assets/add-session.js lines 1066-1153 (the inline submit handler to extract: validation 1069-1096, persist 1108-1148, the 600ms redirect 1149-1152), 45-46/89-90 (formDirty/formSaving + PortfolioFormDirty), 970-979 (__exportModalInit ctx)
    - assets/export-modal.js lines 667-696 (openExportDialog), 778-795 (exportSessionBtn trigger), 551-583 (exportHandleDownloadPdf)
    - tests/30-export-stepper.test.js (export modal jsdom harness pattern — reuse its environment approach) + 34-CONTEXT.md § Specifics (PDFX-03 extraction risk)
  </read_first>
  <files>tests/34-save-before-export.test.js</files>
  <action>
    Author a jsdom behavior test for the PDFX-03 contract (the wiring 34-08 will add). Cover: (a) export fired with a dirty form (PortfolioFormDirty()===true) OR a never-saved new session surfaces the non-blocking prompt rather than exporting immediately or hard-blocking; (b) choosing "Save & export" calls the extracted reusable save (validate→persist, returning {savedId,isNew}) WITHOUT the 600ms location redirect, and then proceeds to export so a brand-new session gains an id and the export derives the correct ordinal for savedId; (c) choosing "Keep editing" dismisses and does NOT export; (d) a save-validation failure (e.g. missing required field) aborts the export entirely (no PDF) and keeps the user editing; (e) behavior-preservation: invoking the save path the SAVE BUTTON uses still validates+persists (the extraction did not change save-button semantics). Make the assertions observable (spies on persist + export + prompt), not internal-name-coupled where avoidable. RED now (prompt + extraction absent — gates 34-08).
  </action>
  <verify>
    <automated>node tests/34-save-before-export.test.js; test $? -ne 0 && echo "RED as expected (save-before-export not wired yet)"</automated>
  </verify>
  <done>
    - Asserts all five behaviors (prompt-on-dirty, Save&export persists-no-redirect-then-exports-with-correct-ordinal, Keep-editing dismisses, validation-failure aborts export, save-button behavior preserved).
    - Falsifiable: a hard-block, a silent stale export, or a redirect-inside-the-extracted-save fails the test.
    - RED now; the GREEN gate for 34-08.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked PortfolioDB + jsdom | Tests use a mock DB and isolated jsdom; no real IndexedDB, no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-04 | Tampering | mock DB seam | low | accept | Tests drive a first-party mock (mock-portfolio-db.js); no production data path touched. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- All three tests run under `node` and FAIL RED for the feature-absent reason now.
- The ordinal test is structurally falsifiable on an id-based derivation (the FN-1 spine).
</verification>

<success_criteria>
The FN-1 ordinal, two-bar severity, and save-before-export contracts (PDFX-02/PDFX-01/PDFX-03) are pinned by falsifiable RED-now tests — the executable gate for 34-05/08/09.
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-04-SUMMARY.md` when done.
</output>
