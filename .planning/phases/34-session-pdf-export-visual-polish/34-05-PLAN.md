---
phase: 34-session-pdf-export-visual-polish
plan: 5
type: execute
wave: 2
depends_on: [34-04]
files_modified:
  - assets/export-modal.js
autonomous: true
requirements: [PDFX-02, PDFX-01]
must_haves:
  truths:
    - "D-03/FN-1: the card's Session #N is a derived chronological ordinal computed at export time (1-based position among the client's sessions sorted ascending by ISO date, tie-break id), NEVER the autoIncrement key — so deleting a middle session renumbers the rest."
    - "PDFX-01: buildSessionPDF receives a structured issues[] array and an exportedOn date in addition to the existing fields, so the renderer is a pure function of its inputs."
    - "D-04/FN-2: the existing localized sessionType label continues to flow through as the pill value (no new field, no hardcoded label)."
  artifacts:
    - "deriveSessionOrdinal(clientId, thisSessionId) in export-modal.js (sort asc by date via localeCompare, tie-break numeric id, findIndex+1, unsaved → length+1) (D-03)."
    - "Extended buildSessionPDF call passing sessionNumber, issues[] (structured {name,before,after} from getIssuesPayload), and exportedOn (D-03/PDFX-01)."
  key_links:
    - "deriveSessionOrdinal reads PortfolioDB.getSessionsByClient(ctx.getEditingSession().clientId) and locates ctx.getEditingSession().id; issues forwarded from ctx.getIssuesPayload(); markdown body is UNCHANGED here (severity is dropped from markdown later, in 34-09) (D-03/D-08)."
---

<objective>
Extend the export-modal data-assembly tier so the renderer receives everything it needs as explicit inputs (FN-1 ordinal, structured issues[], exportedOn) while keeping the renderer a deterministic pure function of its inputs. This plan adds the FN-1 derivation (PDFX-02) and the widened `buildSessionPDF` input contract; it does NOT yet drop severity from the markdown body (that lands atomically with the severity bars in 34-09 to avoid a transient missing-severity state).

Purpose: Compute the honest, renumber-safe session ordinal at export time (D-03) and forward the structured severity payload + exportedOn so the render tier can draw the card number and the two-bar severity block from data, not text.
Output: `deriveSessionOrdinal()` + extended `buildSessionPDF({ …, sessionNumber, issues, exportedOn })` in `assets/export-modal.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ deriveSessionOrdinal(clientId, thisSessionId) (export-modal.js)
- ★ buildSessionPDF input contract extension: sessionNumber, issues[], exportedOn
- logo module (34-01); i18n (34-02); 6 tests (34-03/04); render fns drawHeaderBand/drawClientCard/drawSeverityBlock/drawFooterBand (34-06/07/09); saveSessionForm() (34-08); regenerated baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-CONTEXT.md
@assets/export-modal.js
@assets/db.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deriveSessionOrdinal (FN-1 chronological ordinal)</name>
  <read_first>
    - tests/34-session-ordinal.test.js (the RED contract from 34-04 — implement to GREEN)
    - 34-RESEARCH.md § Code Examples (deriveSessionOrdinal reference) + § Common Pitfalls (Pitfall 2, ISO lexical sort == chronological, no new Date()) + Assumptions A1/A2
    - assets/db.js lines 976-986 (getSessionsByClient unsorted getAll) + 225 (autoIncrement)
    - assets/export-modal.js lines 27-31 (ctx accessors), 289-296 (getCurrentSessionDataForExport)
  </read_first>
  <files>assets/export-modal.js</files>
  <behavior>
    - 3 sessions d1<d2<d3 (ids NOT in date order) → ordinals 1/2/3 by date.
    - delete middle (d2) → former d3 derives #2 (renumber, no gap).
    - same-date pair → deterministic tie-break by numeric id.
    - thisSessionId not found (unsaved) → returns length+1 (the next ordinal it will become on save).
  </behavior>
  <action>
    Add `deriveSessionOrdinal(clientId, thisSessionId)` to export-modal.js. It awaits `PortfolioDB.getSessionsByClient(clientId)`, sorts a COPY ascending by ISO `date` (string compare via localeCompare — dates are ISO YYYY-MM-DD so lexical == chronological; do NOT use new Date()/TZ parsing per Pitfall 2) with a numeric `id` tie-break, then returns `findIndex(s => s.id === thisSessionId) + 1`, or `length + 1` when not found (unsaved). Do NOT read the ordinal from any session.id-as-number shortcut (D-03 / Pitfall: id is autoIncrement and never renumbers). Source clientId/thisSessionId from `ctx.getEditingSession()` (.clientId/.id) at the call site.
  </action>
  <verify>
    <automated>node tests/34-session-ordinal.test.js</automated>
  </verify>
  <done>
    - `node tests/34-session-ordinal.test.js` exits 0 (GREEN): 1/2/3 by date, renumber-to-1/2 on delete, same-date tie-break by id, unsaved → length+1.
    - Sort is on ISO date string + numeric id tie-break (no Date parsing); ordinal is position-based, not id-based (the FN-1 spine stays falsifiable).
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen the buildSessionPDF input contract (sessionNumber, issues[], exportedOn)</name>
  <read_first>
    - assets/export-modal.js lines 289-296 (getCurrentSessionDataForExport), 531-549 (exportCloseDialog dirty plumbing — leave intact), 551-583 (exportHandleDownloadPdf — the buildSessionPDF call at 563-575), 667-696 (openExportDialog)
    - assets/export-modal.js line 31 (ctx.getIssuesPayload) + assets/add-session.js lines 702-711 (getIssuesPayload → {name,before,after})
    - assets/pdf-export.js lines 640-671 (buildSessionPDF JSDoc + signature — the contract the renderer reads)
  </read_first>
  <files>assets/export-modal.js</files>
  <action>
    Extend the data passed to `window.PDFExport.buildSessionPDF(...)` in exportHandleDownloadPdf so it ALSO includes: `sessionNumber` (await deriveSessionOrdinal(editingSession.clientId, editingSession.id) — guard when there is no editing session by using its clientId count + 1, or omit gracefully); `issues` (the structured array from `getIssuesPayload()` — pass the {name,before,after} objects verbatim, NOT markdown); and `exportedOn` (today's date, formatted via App.formatDate to match the locale, distinct from the card's session date per D-09). Keep `clientName`, `sessionDate`, `sessionType` (the localized pill value via formatSessionType — D-04) exactly as today. Do NOT change buildFilteredSessionMarkdown's issues emission in this plan (severity stays in markdown until 34-09 swaps it atomically). Surface the new fields through `getCurrentSessionDataForExport`/`_exportState.sessionData` if that is where the call reads from, so both the direct-build and stepper paths carry them.
  </action>
  <verify>
    <automated>node tests/30-export-stepper.test.js && grep -nE "sessionNumber|issues:|exportedOn" assets/export-modal.js | grep -v '^#'</automated>
  </verify>
  <done>
    - The buildSessionPDF call passes sessionNumber, issues[] (structured), and exportedOn alongside the existing fields.
    - issues[] is forwarded as structured objects (not stringified markdown); the localized sessionType pill value is unchanged (D-04).
    - `node tests/30-export-stepper.test.js` stays green (data-assembly wiring did not break the stepper).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's IndexedDB (getSessionsByClient) → export-modal data assembly → buildSessionPDF | First-party data only; the ordinal is derived in-memory and never stored; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Information disclosure | exported session content | low | accept | Export contains only the user's own first-party session data, downloaded locally via Blob URL; no new egress path. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node tests/34-session-ordinal.test.js` GREEN; `node tests/30-export-stepper.test.js` green.
- buildSessionPDF call carries sessionNumber, issues[], exportedOn; markdown body unchanged (severity still emitted until 34-09).
</verification>

<success_criteria>
The renderer has explicit, deterministic inputs for the FN-1 ordinal (PDFX-02), structured severity, and exportedOn — with the localized pill value preserved (D-04) and the ordinal renumber-safe (D-03).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-05-SUMMARY.md` when done.
</output>
