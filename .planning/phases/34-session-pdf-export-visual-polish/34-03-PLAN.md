---
phase: 34-session-pdf-export-visual-polish
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/34-logo-embed.test.js
  - tests/34-pill-localized.test.js
  - tests/34-rtl-newblocks.test.js
autonomous: true
requirements: [PDFX-01]
must_haves:
  truths:
    - "D-11: render-tier behaviors are pinned by falsifiable content-stream tests authored BEFORE implementation (feedback-behavior-verification)."
    - "D-05/FN-3: a test asserts the embedded logo emits an image XObject and the build succeeds with no network."
    - "D-04/FN-2: a test asserts the pill renders the localized sessionType label verbatim across clinic/online/other × en/he/de/cs."
    - "D-10: a test asserts every new block (header band/card/pill/severity/footer) anchors to the start edge and digits keep visual order under uiLang:'he' (RTL non-regression spine)."
  artifacts:
    - "tests/34-logo-embed.test.js, tests/34-pill-localized.test.js, tests/34-rtl-newblocks.test.js — authored RED (feature absent), gating 34-06/07/09."
  key_links:
    - "tests use tests/_helpers/jsdom-pdf-env.js buildJsdomEnv() → win.PDFExport.buildSessionPDF(...) and walk the content stream, mirroring pdf-digit-order/pdf-glyph-coverage (D-10/D-11)."
---

<objective>
Author the three render-tier Wave-0 tests BEFORE the render implementation lands (project hard rule `feedback-behavior-verification`). Each asserts observable PDF content-stream behavior (image XObject, localized glyphs, start-edge anchoring + digit order) so it is implementation-agnostic and falsifiable. All three are expected RED now (the new blocks do not exist yet) and become the GREEN gate for 34-06 (header+card+logo), 34-09 (severity), and 34-07 (footer/headings).

Purpose: Lock the load-bearing RTL invariant (D-10) and the FN-2/FN-3 contracts with executable evidence before any pixel moves.
Output: `tests/34-logo-embed.test.js`, `tests/34-pill-localized.test.js`, `tests/34-rtl-newblocks.test.js`.

## Artifacts this phase produces (★ = owned by this plan)
- ★ tests/34-logo-embed.test.js, ★ tests/34-pill-localized.test.js, ★ tests/34-rtl-newblocks.test.js
- tests/34-{severity-bars,session-ordinal,save-before-export}.test.js (34-04); render fns (34-06/07/09); deriveSessionOrdinal + buildSessionPDF contract (34-05); saveSessionForm() (34-08); logo module + sw precache (34-01); i18n keys (34-02); regenerated baselines (34-10)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-session-pdf-export-visual-polish/34-RESEARCH.md
@.planning/phases/34-session-pdf-export-visual-polish/34-VALIDATION.md
@.planning/phases/34-session-pdf-export-visual-polish/34-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 34-logo-embed.test.js — image XObject present, offline build succeeds (FN-3)</name>
  <read_first>
    - tests/_helpers/jsdom-pdf-env.js (buildJsdomEnv() API + onJsPDF hook + deterministic pins)
    - tests/pdf-glyph-coverage.test.js (content-stream walking pattern + self-exit 0/1 convention)
    - 34-RESEARCH.md § Validation Architecture (FN-3 row) + § Common Pitfalls (Pitfall 4)
    - assets/branding/icon-512-base64.js (the asset 34-01 created — the test should eval/inject it alongside pdf-export.js)
  </read_first>
  <files>tests/34-logo-embed.test.js</files>
  <action>
    Author a Node self-exiting test (plain `node tests/34-logo-embed.test.js`, exit 0/1) using buildJsdomEnv(). Inject `window.IconLogoBase64` (eval assets/branding/icon-512-base64.js into the jsdom window before building) and call `win.PDFExport.buildSessionPDF(sessionData, { uiLang:'en' })`. Assert: (a) the build resolves to a Blob with no network access (jsdom has no fetch backend — any fetch attempt would reject); (b) the produced PDF contains an embedded image XObject (walk the PDF bytes/object table for an `/XObject` `/Subtype /Image` entry — the addImage output). Make it falsifiable: with the logo draw absent (current code) the image-XObject assertion FAILS (RED now). Keep it robust to exact placement (assert presence, not coordinates).
  </action>
  <verify>
    <automated>node tests/34-logo-embed.test.js; test $? -ne 0 && echo "RED as expected (logo not embedded yet)"</automated>
  </verify>
  <done>
    - Test runs without syntax/harness error and FAILS now (RED) on the missing image-XObject assertion (feature absent — gates 34-06).
    - Assertion targets an `/XObject /Image` in the content stream and a successful offline (fetch-free) build — not pixel coordinates.
    - Falsifiability documented in a header comment: removing the addImage call keeps it RED; adding it turns it GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: 34-pill-localized.test.js — localized sessionType verbatim, 4 locales (FN-2)</name>
  <read_first>
    - assets/app.js lines 1145-1154 (formatSessionType — the localized label source)
    - assets/i18n-en.js / he / de / cs lines 265-267 (session.type.{clinic,online,other})
    - tests/pdf-glyph-coverage.test.js (glyph/CID assertion approach)
    - 34-RESEARCH.md § Validation Architecture (FN-2 row) + § Reading the localized pill value
  </read_first>
  <files>tests/34-pill-localized.test.js</files>
  <action>
    Author a content-stream test that builds the PDF for each (sessionType ∈ {clinic,online,other}) × (uiLang ∈ {en,he,de,cs}) and asserts the pill renders the localized label verbatim — i.e. the glyphs of `App.formatSessionType(type)` for that locale appear in the rendered output (assert the localized string is emitted; for HE assert the shaped Hebrew label appears, not a hardcoded English token). Drive the localized value the same way export-modal does (the label resolved via formatSessionType), so the test fails if anyone hardcodes a pill label (D-04). RED now (no pill is drawn yet — gates 34-06).
  </action>
  <verify>
    <automated>node tests/34-pill-localized.test.js; test $? -ne 0 && echo "RED as expected (pill not drawn yet)"</automated>
  </verify>
  <done>
    - Test exercises all 12 (type × locale) combinations and asserts the verbatim localized label is emitted.
    - Falsifiable against a hardcoded label: replacing the rendered value with a constant string would fail the HE/DE/CS cases.
    - RED now (no pill rendered); becomes GREEN under 34-06.
  </done>
</task>

<task type="auto">
  <name>Task 3: 34-rtl-newblocks.test.js — start-edge anchoring + digit order for new blocks (D-10)</name>
  <read_first>
    - assets/pdf-export.js lines 829-872 (drawSegmentedLine RTL anchor + drawTextLine align:'right' / isInputVisual:false), 682 (docDir)
    - tests/pdf-digit-order.test.js (digit-GID order assertion under RTL) + tests/pdf-glyph-coverage.test.js
    - 34-RESEARCH.md § Pattern 1 + Pattern 2 + § Validation Architecture (RTL row); 34-CONTEXT.md D-10
  </read_first>
  <files>tests/34-rtl-newblocks.test.js</files>
  <action>
    Author a content-stream test that builds the PDF under `uiLang:'he'` with sessionData carrying clientName (Hebrew), sessionNumber, a localized pill, and a structured issues[] (with numeric before/after). Assert the RTL non-regression invariant for the NEW blocks: (a) the client-card meta numerals (Session #N and the severity values) keep correct visual digit order (reuse the digit-GID run technique from pdf-digit-order — e.g. "10"/"2026" do NOT appear reversed); (b) new text is start-edge anchored under RTL (assert no new draw places its anchor at the LTR left margin when docDir==='rtl' — mirror the existing right-anchor expectation). Keep assertions about ORDER and ANCHOR-SIDE, not absolute y, so they survive layout tuning. RED now (new blocks absent); grows to GREEN across 34-06/07/09. Add a header comment listing which blocks each assertion covers (header band, card/pill, severity bars, footer).
  </action>
  <verify>
    <automated>node tests/34-rtl-newblocks.test.js; test $? -ne 0 && echo "RED as expected (new RTL blocks not built yet)"</automated>
  </verify>
  <done>
    - Asserts digit visual-order and start-edge anchoring for the new blocks under uiLang:'he' (D-10), using order/anchor-side checks (not absolute coordinates).
    - Falsifiable: a left-anchored or digit-reversed new block fails the test.
    - RED now; the gate that 34-06/07/09 must turn fully GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → jsdom-evaluated pdf-export.js | Tests only READ/EVAL assets into an isolated jsdom window; they never write production files. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-03 | Tampering | test harness eval of assets | low | accept | Read-only eval into an isolated jsdom window (jsdom-pdf-env.js); no writes to assets/*. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (34-RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- All three tests execute under `node` and FAIL RED for the right (feature-absent) reason now.
- Each documents its falsifiability and the blocks it gates.
</verification>

<success_criteria>
The render-tier RTL/FN-2/FN-3 contracts are pinned by falsifiable, RED-now content-stream tests — the executable gate for the render plans (D-04/D-05/D-10/D-11).
</success_criteria>

<output>
Create `.planning/phases/34-session-pdf-export-visual-polish/34-03-SUMMARY.md` when done.
</output>
