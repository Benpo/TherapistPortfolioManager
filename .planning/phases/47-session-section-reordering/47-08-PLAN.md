---
phase: 47-session-section-reordering
plan: 08
type: execute
wave: 4
depends_on: ["47-03", "47-04", "47-05", "47-07"]
files_modified:
  - assets/help-content-en.js
  - assets/changelog-content-en.js
  - assets/help-content-he.js
  - assets/help-content-de.js
  - assets/help-content-cs.js
  - assets/changelog-content-he.js
  - assets/changelog-content-de.js
  - assets/changelog-content-cs.js
  - HELP-MAP.md
autonomous: false
requirements: [ORDR-06, ORDR-07, ORDR-08]
user_setup: []
must_haves:
  truths:
    - "The docs-gate is satisfied: an EN changelog entry AND EN help-topic updates ship with the phase (EN is the corpus of record)."
    - "The headline reorder feature has its own help topic (drag + arrows, groups, Reset order/names, order mirrored in export); the mandated 'how to turn severity ratings off' help entry exists (D-08)."
    - "The changelog carries the 4-part D-18 user story (reordering; app-level severity switch; the — skip value; skipped severity excluded from export + the export topics/severity split)."
    - "All new/updated help + changelog strings exist in EN/DE/HE/CS; Ben has reviewed the drafted copy before it lands."
  artifacts:
    - "assets/help-content-en.js — new topic-reordering + new severity-off topic; updated topic-sections-on-off, topic-renaming, topic-before-after (incl. the re-pointed start-rating label ref), topic-single-export, topic-export-formats, topic-multiple-issues (before/after wording corrected for switch/skip), topic-backup-restore (restores carry section order) (G-14)."
    - "assets/changelog-content-en.js — the 4-part Phase-47 user-story entry."
    - "he/de/cs help + changelog mirrors; HELP-MAP.md updated with the new topics."
  key_links:
    - "The docs-gate (scripts/docs-gate.js) is satisfied by an EN help edit + an EN changelog edit in the pushed range; locale-only edits do NOT satisfy it (CLAUDE.md Definition of Done)."
    - "This plan is the planner-owned docs pass (D-18) drafted as its own content pass Ben reviews — never bundled into the code tasks."
---

<objective>
Ship the planner-owned documentation pass for Phase 47 (D-18): the help topics and the changelog/what's-new entry that the docs hard-gate demands for a heavily user-facing change. Content is drafted here as its own pass Ben reviews (repo memory feedback-docs-content-passes-separate-from-code), covering the exact list the planner derived against D-01..D-16 and HELP-MAP.md — including the mandated 'how to turn severity ratings off' help entry (D-08).

Purpose: The docs hard-gate fails the push without EN help + EN changelog edits (CLAUDE.md Definition of Done). This plan provides them, in all four locales, and gates on Ben's copy review.

Output: EN + locale help/changelog edits; HELP-MAP.md updates; a copy-review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-03-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-04-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-05-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-07-SUMMARY.md
@HELP-MAP.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols/artifacts created by THIS plan (exclude from drift checks):
- New help topic `make-it-yours / topic-reordering` (the headline reorder feature) in help-content-en.js + locale mirrors + a HELP-MAP.md row
- New help topic `severity / topic-turn-off` ("how to turn severity ratings off", mandated by D-08) in help-content-en.js + locale mirrors + a HELP-MAP.md row
- A new changelog entry (the 4-part Phase-47 user story) in changelog-content-en.js + locale mirrors
- Updated existing topics: topic-sections-on-off, topic-renaming, topic-before-after, topic-single-export, topic-export-formats, topic-multiple-issues, topic-backup-restore (G-14)
- Explicit unaffected declarations (reviewed, no edit): the remaining add-session.html-owning topics — the heart-wall topics and topic-new-session (G-14)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EN help topics (new + updated) + EN changelog entry + HELP-MAP.md</name>
  <files>assets/help-content-en.js, assets/changelog-content-en.js, HELP-MAP.md</files>
  <read_first>
    - assets/help-content-en.js — the existing make-it-yours (topic-sections-on-off, topic-renaming), severity (topic-before-after), and review-export (topic-single-export, topic-export-formats) entries, to match structure and voice
    - assets/changelog-content-en.js — the existing entry structure (version/date/items) to append a new entry consistently
    - HELP-MAP.md — the topic index (add the two new topic rows: make-it-yours/topic-reordering and severity/topic-turn-off)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md `## Docs Coverage` (the exact affected-topic table) + CONTEXT D-18 (the 4-part changelog user story + the explicit NOT-in-changelog list)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Copywriting (the ⓘ explainer text seeds the severity-off help entry) + the finalized strings from the Wave-2/3 SUMMARYs
    - .planning/reference (repo memory feedback-docs-content-passes-separate-from-code) — copy tight + practical, no intro blah-blah
  </read_first>
  <action>
    In help-content-en.js: ADD a new make-it-yours topic `topic-reordering` (the headline feature) — how to reorder sections by dragging the handle or using the up/down arrows, that sections live in groups you can rename, that turning a section off keeps its slot, and that Reset order / Reset names restore defaults, and that the order you set is mirrored in the form and in exports. ADD a new severity topic `topic-turn-off` ("how to turn severity ratings off") seeded from the ⓘ explainer copy: the Issue severity row's toggle is one switch for all severity ratings (off hides the end-of-session section and the per-topic start rating; the topics themselves stay), and its drag position controls where the end-of-session ratings appear. UPDATE topic-sections-on-off (toggles now live in the reorder list; rows keep their slot), topic-renaming (group renames + Reset names), topic-before-after (the app-level on/off switch + the new '—' skip value that leaves a rating untracked; ALSO re-point its {ui:session.form.beforeSeverity} label reference to the new start-rating label key session.form.severityAtStart so the topic names the label the form actually shows), topic-single-export + topic-export-formats (the topics/severity Step-1 split — Session topics with a dependent 'Include severity' option — and that export order mirrors your saved order), topic-multiple-issues (severity section: its "each with its own before-and-after ratings" wording becomes wrong once ratings are switchable/skippable — rephrase so ratings are described as optional per the switch and the '—' skip), and topic-backup-restore (backups section: restores now bring back your section order and group names along with your data — one added line). Explicitly DECLARE the remaining add-session.html-owning topics unaffected after review: the heart-wall topics and topic-new-session (the restructure moves their sections but changes no behavior they document) — record the declaration in the SUMMARY so the coverage audit is honest. In changelog-content-en.js: append ONE new entry with the 4-part D-18 user story (1: section reordering — drag + arrows, renamable groups, enable toggles that keep their slot, Reset order / Reset names, order mirrored in export; 2: an app-level severity switch that turns all ratings on/off; 3: the '—' skip value to leave a session/topic untracked; 4: skipped severity is left out of exports, and export now lets you include topics with or without the severity change). Do NOT changelog the internal behaviors D-18 lists as out (the topics<severity clamp, saved-order sanitization/migration, tab-order/pointer-drag internals, RTL drag math, label micro-renames). Place the entry under the next app version per the project version-bump convention (Ben confirms the exact number at the review checkpoint in Task 3). Update HELP-MAP.md with the two new topic rows (Covers columns: topic-reordering → assets/settings.js, settings.html, assets/add-session.js; topic-turn-off → add-session.html, assets/settings.js). Keep copy tight and practical (no intro filler).
  </action>
  <verify>
    <automated>node --check assets/help-content-en.js && node --check assets/changelog-content-en.js && grep -q 'topic-reordering' HELP-MAP.md && grep -q 'topic-turn-off' HELP-MAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/help-content-en.js` and `node --check assets/changelog-content-en.js` succeed.
    - help-content-en.js contains a topic-reordering entry and a topic-turn-off entry (grep-assert both keys/titles).
    - topic-multiple-issues no longer asserts every issue has its own before-and-after ratings unconditionally; topic-backup-restore mentions section order coming back with a restore; topic-before-after references the start-rating label via session.form.severityAtStart, not session.form.beforeSeverity (grep-assert the ref swap) (G-14).
    - changelog-content-en.js has one new entry covering all four D-18 story parts (grep-assert phrases for reordering, severity switch, skip/untracked, export split).
    - HELP-MAP.md lists both new topics with Covers columns.
    - The changelog entry does NOT mention the clamp, sanitization/migration, drag internals, or RTL drag math (out-of-changelog list).
  </acceptance_criteria>
  <done>The EN corpus carries the new reordering + severity-off help topics, the updated existing topics, and the 4-part changelog entry; HELP-MAP.md indexes the new topics. The docs-gate's EN demand is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: DE / HE / CS locale mirrors of the new + updated help and changelog copy</name>
  <files>assets/help-content-he.js, assets/help-content-de.js, assets/help-content-cs.js, assets/changelog-content-he.js, assets/changelog-content-de.js, assets/changelog-content-cs.js</files>
  <read_first>
    - assets/help-content-en.js + assets/changelog-content-en.js — the finalized EN content from Task 1 (the source to translate)
    - assets/help-content-he.js / de / cs and assets/changelog-content-he.js / de / cs — the parallel locale files and their existing entry structure/voice; HE renders RTL (keep דרגת חומרה severity terminology)
  </read_first>
  <action>
    Mirror the Task-1 additions/updates into each locale: the new topic-reordering + topic-turn-off help topics, the updated topic-sections-on-off / topic-renaming / topic-before-after / topic-single-export / topic-export-formats / topic-multiple-issues / topic-backup-restore entries, and the new changelog entry — translated naturally into German, Hebrew, and Czech. Keep the same entry keys/ids/version as EN so each locale resolves the same lookups. HE keeps its existing severity vocabulary (דרגת חומרה) and reads correctly RTL. This locale pass does NOT by itself satisfy the docs-gate (EN is the corpus of record) — it is the follow-through so all four locales carry the content.
  </action>
  <verify>
    <automated>node --check assets/help-content-he.js && node --check assets/help-content-de.js && node --check assets/help-content-cs.js && node --check assets/changelog-content-he.js && node --check assets/changelog-content-de.js && node --check assets/changelog-content-cs.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six locale files pass `node --check`.
    - Each locale help file contains topic-reordering and topic-turn-off entries (grep-assert per file).
    - Each locale changelog file has the new entry with the same version/id as EN.
    - HE severity copy uses דרגת חומרה (grep-assert) and no EN placeholder text remains in the locale files.
  </acceptance_criteria>
  <done>DE/HE/CS carry translated mirrors of the new + updated help topics and the changelog entry, keyed identically to EN, so all four locales are complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ben reviews the drafted docs copy + confirms the release version</name>
  <files>assets/help-content-en.js, assets/changelog-content-en.js</files>
  <action>Pause for Ben's review of the drafted EN help + changelog copy (the planner-owned content pass, D-18). Do not edit further until Ben approves or supplies edits; on approval, set the confirmed version number on the changelog entry.</action>
  <what-built>The planner-owned docs pass (D-18): a new 'Reordering sections' help topic and a new 'How to turn severity ratings off' help topic, updates to the sections-on-off / renaming / before-after / export / multiple-issues / backup-restore help topics (with the heart-wall + new-session topics reviewed and declared unaffected), and a 4-part changelog entry — drafted in EN (corpus of record) and mirrored in DE/HE/CS, with HELP-MAP.md updated.</what-built>
  <how-to-verify>
    1. Read the new EN help copy in assets/help-content-en.js: the topic-reordering entry and the topic-turn-off ("how to turn severity ratings off") entry — confirm they are tight, practical, and match the shipped UX (drag + arrows, groups/rename, Reset order/names, the severity switch, the '—' skip value, order mirrored in export).
    2. Read the new changelog entry in assets/changelog-content-en.js — confirm the 4-part story reads well for therapists and confirm the VERSION NUMBER for this release (per the version-bump convention).
    3. Skim the HE copy (Sapir's review lane) for tone; DE/CS for obvious errors.
    4. Reply with the confirmed version number and any copy edits, or "approved".
  </how-to-verify>
  <resume-signal>Type "approved" (with the version number) or list copy edits to apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation content only — no user input, no persistence, no new runtime code path. Help/changelog strings render via the existing escape-first help renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-13 | Information Disclosure | Docs describing a behavior the code does not actually ship (doc/behavior drift) | low | mitigate | Content is drafted from the finalized Wave-2/3 SUMMARYs and reviewed by Ben at the checkpoint before it lands. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- All help/changelog files pass `node --check`; `npm test` full suite green (any help/changelog structure tests still pass).
- Docs-gate dry expectation: the pushed range contains an EN help edit (assets/help-content-en.js) AND an EN changelog edit (assets/changelog-content-en.js) — the two the gate checks.
- Comment hygiene: no planning IDs in any content file.
</verification>

<success_criteria>
The docs-gate is satisfied (EN help + EN changelog edits present), the headline reorder feature and the mandated severity-off entry have help topics, the 4-part changelog story ships in all four locales, HELP-MAP.md indexes the new topics, and Ben has approved the copy + version number.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-08-SUMMARY.md` when done. Record the confirmed release version number, the final help topic ids, and the explicit unaffected-after-review declarations (heart-wall topics, topic-new-session) so the docs index and coverage audit stay accurate.
</output>
