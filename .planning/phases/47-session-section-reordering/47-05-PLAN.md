---
phase: 47-session-section-reordering
plan: 05
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - assets/export-modal.js
  - tests/30-export-markdown.test.js
  - tests/47-severity-unrated.test.js
autonomous: true
requirements: [ORDR-04, ORDR-07]
must_haves:
  truths:
    - "The export Step-1 checkbox list, the filtered markdown builder (buildFilteredSessionMarkdown), and the document-section labels ALL read section order from App.getSectionOrder() — one source (D-13); on add-session.html that is the page-pinned snapshot the form captured at open, so a peer-tab order change mid-edit can never make form and export diverge (G-5); the 260615 three-way invariant (form == Step-1 == export) holds against a MUTATED saved order. (The clipboard buildSessionMarkdown is repointed to the same source in 47-09.)"
    - "Export Step-1 shows 'Session topics' as a main checkbox named identically to the in-session section title, with 'Include severity before/after' as a dependent, indented sub-option enabled only when topics is checked; the sub-option defaults checked when issue data exists, RE-checking topics restores that default, the topics checkbox keeps today's pre-selected opt-out default (checked iff issue data exists), and both reset per export (D-14/D-15/G-12)."
    - "With topics selected but severity excluded, the filtered markdown emits a 'Session topics' section (via getSectionLabel('issues',…)) listing the topic names at the issues slot; numeric ratings stay PDF-structural (bars), never text lines in the filtered markdown; the afterSeverity slot emits NOTHING in markdown (the PDF renders the bars there) (G-2)."
    - "When App.isSectionEnabled('afterSeverity') is false, the 'Include severity before/after' sub-option is hidden/disabled and the filtered builder + PDF payload exclude severity entirely — no PDF bars even for old sessions that carry ratings (D-08/G-3); the clipboard builder honors the same gate in 47-09."
    - "The issues[] payload forwarded to the PDF severity block is filtered to topics with at least one numeric rating (before OR after is a Number); a fully-unrated topic (both sides null) contributes no bar row, and when zero topics qualify the block is omitted entirely via the existing empty-array early-return — no empty bars, no dash, no literal placeholder (D-21). A partially-rated topic keeps its row (one filled bar is real information)."
  artifacts:
    - "assets/export-modal.js — order-driven emission (EXPORT_SECTION_ORDER removed) across the filtered builder + Step-1 + labels, topics/severity split with the pinned Session-topics emission (G-2), App.isSectionEnabled('afterSeverity') switch wiring (G-3), unrated-omission via the PDF-input filter at buildRenderInputs (≥1 numeric rating, D-21), pinned Step-1 defaults (G-12). (Clipboard buildSessionMarkdown + edit-aware deriveSeverityAfterSections + pdf-export.js placement move to 47-09.)"
    - "tests/30-export-markdown.test.js (rewritten three-way invariant), tests/47-severity-unrated.test.js (topics/sub-option/switch gating + PDF-input filter: fully-unrated → no row, all-unrated → block absent, partial → row present, plus the G-12 default cases)."
  key_links:
    - "All order reads go through App.flattenOrderKeys(App.getSectionOrder()) (47-01) — the SAME source the form (47-04) reads; on add-session.html the form pins the snapshot at open (App.pinSectionOrder) and export reads that pin, so both consumers see one identical order per page load (G-5)."
    - "App.isSectionEnabled('afterSeverity') (toggle owned by 47-03) gates the sub-option AND the filtered builder + PDF payload severity output (G-3); the clipboard builder honors the same gate in 47-09."
    - "Unrated omission is by numeric-type filter — topics with no numeric before/after are dropped from the PDF payload at the buildRenderInputs issues assembly (D-21); export never renders a literal '—'/'N/A'/dash placeholder for an unrated topic. The severity-block PLACEMENT ordinal (deriveSeverityAfterSections) and the clipboard builder are 47-09."
---

<objective>
Repoint the FILTERED export builder to the saved order and fix the topics/severity selection. Today export-modal.js has three order consumers (EXPORT_SECTION_ORDER, buildDocumentSectionLabels, the Step-1 renderer) plus buildFilteredSessionMarkdown (L379-475) with a hardcoded in-line emission order — the exact fragmentation that produced the 260615 bug. All must read App.getSectionOrder() (the page-pinned snapshot on add-session.html, G-5). Then implement the D-14/D-15 topics/severity split (topics main checkbox + dependent severity sub-option with pinned defaults, G-12), the pinned topics-without-severity document representation (G-2), the app-level severity-switch wiring (App.isSectionEnabled('afterSeverity') gates the sub-option and the filtered builder + PDF payload, G-3), and the D-21 unrated omission: the issues[] forwarded to the PDF severity block is filtered (at buildRenderInputs) to topics with ≥1 numeric rating so a fully-unrated topic gets no bar row and an all-unrated set omits the block via the existing empty-array early-return — the drawSeverityBlock render loop stays untouched.

Purpose: ORDR-04 (atomic 260615 rewrite) + the folded 46.1 export bug (D-14) + D-21 (unrated omitted everywhere). The three-way invariant (form == Step-1 == export) must hold against a mutated order. The clipboard-copy builder (buildSessionMarkdown) and the edit-aware severity-block PLACEMENT ordinal (deriveSeverityAfterSections + pdf-export.js) are split into 47-09 (wave 3) — Ben's context-quality directive — so the two export-modal.js executors never run concurrently and each gets a fresh 2-task context.

Output: order-driven buildFilteredSessionMarkdown + Step-1 topics/severity split + the buildRenderInputs PDF-payload unrated-filter in export-modal.js; a rewritten three-way guard test + a new unrated-omission behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/CONTEXT-SEED.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-01-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from drift checks):
- Topics main-checkbox + dependent severity sub-option controls in the Step-1 renderer
- The pinned Session-topics markdown emission (topic-name list at the issues slot via getSectionLabel) in buildFilteredSessionMarkdown (G-2)
- The PDF-payload unrated filter (keep only topics whose before OR after is a Number) at the buildRenderInputs issues[] assembly site (D-21)
- Removal of `EXPORT_SECTION_ORDER` (export-modal.js L343) — replaced by App.flattenOrderKeys(App.getSectionOrder())
- tests/30-export-markdown.test.js (rewritten), tests/47-severity-unrated.test.js
- Moved to 47-09 (wave 3, sequential on export-modal.js): the clipboard buildSessionMarkdown rewrite, deriveSeverityAfterSections, and the pdf-export.js severity-block placement.
- Consumes (does NOT create): App.getSectionOrder / pinSectionOrder pin semantics / flattenOrderKeys (47-01), App.isSectionEnabled('afterSeverity') (47-01/47-03), export.section.topics / export.suboption.includeSeverity (47-02), afterSeverity key (47-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order-driven emission — all export order consumers read App.getSectionOrder() (three-way invariant)</name>
  <files>assets/export-modal.js, tests/30-export-markdown.test.js</files>
  <read_first>
    - assets/export-modal.js L325-360 (EXPORT_DEFAULT_CHECKED, EXPORT_SECTION_ORDER, exportDefaultI18nKey) and L379-475 (buildFilteredSessionMarkdown — the HARDCODED in-line emission order L434-470) and L485-540 (buildDocumentSectionLabels, emotionsBlockIncluded, exportRenderStep1Rows)
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.flattenOrderKeys(App.getSectionOrder()) / flattenOrderKeys — the ordered form-only key list, groups flattened, no group names per D-03)
    - tests/30-export-markdown.test.js — the existing 260615 guard suite to REWRITE (must assert against a MUTATED saved order, not static DOM)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pitfall 1 + `## Code Examples` (order-driven markdown emission replacing L434-470)
  </read_first>
  <behavior>
    - buildFilteredSessionMarkdown emits ## sections in App.flattenOrderKeys(App.getSectionOrder()) order (only selected + non-empty keys), not a hardcoded sequence; group NAMES never appear (D-03); on add-session.html this order is the page-pinned snapshot the form captured at open (G-5).
    - The "issues" key emits the Session-topics section at its slot (representation pinned in Task 2); the "afterSeverity" key emits NO markdown text section (its content is the PDF-structural severity block).
    - buildDocumentSectionLabels returns labels in saved order (plus the document title, preserved for the PDF chrome classification).
    - exportRenderStep1Rows renders rows in saved order.
    - For a MUTATED saved order, the Step-1 row order, the markdown ## heading order, and buildDocumentSectionLabels order are identical (three-way invariant).
  </behavior>
  <action>
    Remove EXPORT_SECTION_ORDER (L343) and replace every consumer with App.flattenOrderKeys(App.getSectionOrder()) (the flattened, form-only ordered key list from 47-01). Rewrite buildFilteredSessionMarkdown so instead of the hardcoded L434-470 sequence it iterates the ordered key list: for each key, if selected and its per-key value is non-empty, push `""` then `## ${stripRequired(App.getSectionLabel(key, exportDefaultI18nKey(key)))}` then the value — reusing the EXISTING per-key readers (heartShield yes/no line, heartShieldEmotions, trapped, insights, limitingBeliefs, additionalTech, comments, nextSession note+date) PLUS the "issues" key, which emits the pinned Session-topics representation defined in Task 2 (topic-name list under the getSectionLabel("issues",…) heading); the "afterSeverity" key emits NO markdown section — severity stays PDF-structural (G-2). Keep the heartShield yes/no export wording and the nextSession note-OR-date gate exactly as today; only the ORDER of emission changes. Do NOT add a separate order read for export: App.getSectionOrder() already returns the page-pinned snapshot on add-session.html (the form pins at open, 47-04/47-01) — export must not call refreshSectionOrderCache or otherwise bypass the pin (G-5). Rewrite buildDocumentSectionLabels to map App.flattenOrderKeys(App.getSectionOrder()) to labels (then push the document title — it MUST stay in the set so the PDF chrome branch does not demote the title, per the L485-495 WARNING). Rewrite exportRenderStep1Rows to iterate the saved order. Add exportDefaultI18nKey cases for any key not already present (afterSeverity is handled via the split in Task 2, not as a normal content section). Do NOT emit group names anywhere (D-03). When removing the old static array, do not leave a tombstone comment that names the removed identifier (keep comments clean). Rewrite tests/30-export-markdown.test.js to assert the three-way invariant against a MUTATED saved order: stub App.getSectionOrder to a non-default order, build Step-1 rows + markdown + document labels, and assert all three orderings match flattenOrderKeys(order) for the selected/non-empty keys.
  </action>
  <verify>
    <automated>node tests/30-export-markdown.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` exits 0 and asserts against a MUTATED (non-default) saved order.
    - `grep -c 'EXPORT_SECTION_ORDER' assets/export-modal.js` is 0 (the static array is removed).
    - buildFilteredSessionMarkdown, buildDocumentSectionLabels, and exportRenderStep1Rows each call App.getSectionOrder (grep-assert).
    - For a mutated order, the ## heading sequence in the markdown equals the Step-1 row sequence equals buildDocumentSectionLabels order (for selected/non-empty keys).
    - No group name (emotionsTech/wrapup or a group title) appears in the emitted markdown.
  </acceptance_criteria>
  <done>All export order consumers read the one saved-order source; the markdown builder iterates that order instead of a hardcoded sequence; group names never leak; the rewritten guard test proves the three-way invariant against a mutated order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Topics/severity split (D-14/D-15) + unrated omission via the PDF-input filter (D-21)</name>
  <files>assets/export-modal.js, tests/47-severity-unrated.test.js</files>
  <read_first>
    - assets/export-modal.js L485-540 (emotionsBlockIncluded, exportRenderStep1Rows, EXPORT_DEFAULT_CHECKED, sectionHasData) — the Step-1 row/selection machinery to extend into the topics + dependent-suboption pattern
    - assets/export-modal.js L722-773 (buildRenderInputs — L744 builds the issues[] payload forwarded to the PDF severity block; THIS is the single site the unrated filter is applied at)
    - assets/pdf-export.js L1437 (`severityIssues = sessionData.issues || []`) + L1830 drawSeverityBlock (early-returns ONLY on an empty array, then draws a row per topic regardless of rating) — the reason the filter is applied at the INPUT so the render loop stays untouched (that loop is 47-09's concern)
    - .planning/phases/47-session-section-reordering/47-02-SUMMARY.md (export.section.topics, export.suboption.includeSeverity keys)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md `## SUPERSESSION NOTE` (the — pill is gone; unrated topics omitted from every output incl. the PDF bar block) + Interaction 8a (export-omission INTENT stands, keyed on unrated; never render a literal —/N/A/dash) + Copywriting (Export Step-1 topics checkbox)
    - .planning/phases/47-session-section-reordering/47-CONTEXT.md D-21 (PDF block filters to ≥1 numeric rating and omits when none qualify; partial keeps its row) + CONTEXT-SEED.md (the locked D-14 split design)
  </read_first>
  <behavior>
    - Step-1 renders "Session topics" as a main checkbox (label = the in-session section title via export.section.topics) and "Include severity before/after" as a dependent indented sub-option that is DISABLED unless topics is checked (severity-alone impossible by construction).
    - The topics checkbox keeps today's pre-selected opt-out default: checked iff issue data exists (the shipped EXPORT_DEFAULT_CHECKED issues rule). The severity sub-option defaults checked when issue data exists, unchecked otherwise; RE-checking topics after an uncheck restores that same default (not the last manual state); both reset each export open (never persisted) (G-12).
    - With topics checked, the filtered markdown emits a "Session topics" section at the issues slot — heading via getSectionLabel("issues", exportDefaultI18nKey("issues")), body = the topic names as a plain list, one per line; NO rating text lines ever appear in the filtered markdown (numeric ratings stay PDF-structural bars); the afterSeverity slot emits nothing in markdown (G-2).
    - When App.isSectionEnabled('afterSeverity') is false, the sub-option is hidden (or disabled+unchecked) regardless of topics state, and the filtered builder + the PDF payload exclude severity entirely — an old session with stored numeric ratings gets no PDF bars and no severity lines while the switch is off (G-3). (The clipboard builder honors the same gate in 47-09.)
    - The issues[] forwarded to the PDF severity block is filtered to topics with at least one numeric rating: a topic whose before AND after are both non-numeric (null/unrated) is dropped; a partially-rated topic (one numeric side) keeps its row. The topic NAME still exports under Session topics regardless of rating.
    - If no topic has a numeric before/after, the filtered issues[] is empty and the severity block is omitted via the existing empty-array early-return even when the sub-option is checked; no literal "—"/"N/A"/dash is ever emitted.
  </behavior>
  <action>
    In exportRenderStep1Rows, render the topics/severity split: a "Session topics" main checkbox (label App.t("export.section.topics") — identical to the in-session title, HE mismatch already fixed in 47-02) and, nested/indented beneath it, an "Include severity before/after" sub-option (App.t("export.suboption.includeSeverity")) that is enabled only while the topics checkbox is checked (toggle its disabled state on the topics checkbox change; when topics unchecks, uncheck+disable the sub-option; when topics RE-checks, restore the sub-option to its DEFAULT — checked iff issue data exists — not its last manual state, G-12). The topics checkbox itself keeps today's pre-selected opt-out default: checked iff the session has issue data (the shipped EXPORT_DEFAULT_CHECKED issues && hasData rule — G-12). Default the sub-option checked only when the session has issue data (reuse sectionHasData), and RESET both per export open — do not persist (D-15). Wire the app-level severity switch (G-3): read App.isSectionEnabled('afterSeverity') at export open — when false, do not offer the sub-option at all (hide the row or render it disabled+unchecked) AND force severity exclusion in the filtered builder + the PDF payload here (the clipboard buildSessionMarkdown honors the same gate in 47-09): no PDF bars, no severity lines, even for old sessions whose stored issues carry numeric ratings (hidden before-scales keep their values in the DB — the switch must gate the OUTPUT, not rely on empty data). Pin the topics-without-severity document representation (G-2): when topics is selected, buildFilteredSessionMarkdown emits at the issues slot a section headed by stripRequired(App.getSectionLabel("issues", exportDefaultI18nKey("issues"))) whose body lists the topic names (one per line, plain text); it never emits rating text lines (ratings are PDF-structural bars gated by the sub-option); the afterSeverity slot emits nothing in markdown. Update emotionsBlockIncluded (and the severity payload path) so severity is included only when topics is selected AND the sub-option is checked AND the afterSeverity switch is on. Apply the D-21 unrated filter at ONE site — the buildRenderInputs issues assembly (L744): after computing the issues payload, filter it to topics with at least one numeric rating (keep a topic when `typeof topic.before === "number" || typeof topic.after === "number"`; drop a topic whose before AND after are both non-numeric) BEFORE returning it as renderInputs.issues, so a fully-unrated topic contributes no bar row and, when zero topics qualify, the resulting empty array trips drawSeverityBlock's existing empty-array early-return and the block is omitted (do not render an empty block or any literal —/N/A/dash). A partially-rated topic keeps its row (one filled bar is real information). Do NOT touch pdf-export.js drawSeverityBlock — the filter is at the input; the render loop stays untouched (its placement rewrite is 47-09). The topic NAME still exports under Session topics. Create tests/47-severity-unrated.test.js asserting: (a) topics checked + sub-option unchecked → no severity in export, but the markdown still carries the Session-topics section with the topic names at the issues slot; (b) a mix of numeric + fully-unrated topics → the PDF payload keeps only the numeric-rated topics; (c) all-unrated → the PDF payload is empty so the block is omitted even with the sub-option checked; (d) a partially-rated topic (before numeric, after null) is KEPT in the PDF payload; (e) the filtered markdown/PDF payload never contains a literal "—"/"N/A"/dash placeholder for an unrated topic; (f) with App.isSectionEnabled('afterSeverity') stubbed false, the sub-option is not offered and a session WITH stored numeric ratings exports no severity; (g) unchecking then re-checking topics restores the sub-option default.
  </action>
  <verify>
    <automated>node tests/47-severity-unrated.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-unrated.test.js` exits 0.
    - The severity sub-option element is disabled when the topics checkbox is unchecked (assert the disabled state toggles with topics); re-checking topics restores the sub-option to its data-derived default (G-12).
    - With topics checked and the sub-option unchecked, the built export contains no severity bar/line BUT the filtered markdown contains the Session-topics heading (via getSectionLabel) followed by the topic names at the issues slot (G-2).
    - The filtered markdown never contains a numeric rating text line for a topic, and emits no section at the afterSeverity slot (G-2).
    - With App.isSectionEnabled('afterSeverity') false, the sub-option is not offered and a session with stored numeric before/after ratings exports zero severity output from the filtered builder + PDF payload (G-3).
    - The buildRenderInputs issues payload drops a fully-unrated topic (both sides non-numeric) and keeps a partially-rated topic; an all-unrated set yields an empty payload so the block is omitted even with the sub-option checked (D-21).
    - The emitted markdown/PDF payload contains no literal placeholder token for an unrated topic (assert absence of a dash-only "N/A"-style severity value).
  </acceptance_criteria>
  <done>Export Step-1 offers topics with a dependent severity sub-option (severity-alone impossible, both defaults pinned and reset per export, re-check restores the default), topics-without-severity emits a topic-name Session-topics section at the issues slot (ratings stay PDF-structural, afterSeverity slot silent in markdown), the app-level severity switch hides the sub-option and forces severity out of the filtered builder + PDF payload when off, the buildRenderInputs unrated filter drops fully-unrated topics (partial kept) so an all-unrated set omits the block, and no literal placeholder leaks into any export. The clipboard builder + severity-block placement follow in 47-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data → export document | Issue ratings (numeric or unrated/null) and section values flow into the filtered markdown + the PDF. |
| saved order → export order | App.getSectionOrder() drives filtered-builder emission order + the PDF-payload assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06 | Tampering/Information Disclosure | An unrated topic leaking empty bars, a literal "—"/"N/A"/dash, into an exported PDF | medium | mitigate | The issues[] forwarded to the PDF severity block is filtered at buildRenderInputs to topics with ≥1 numeric rating (Task 2); a fully-unrated topic is dropped and an all-unrated set omits the block via the existing empty-array early-return — no empty bars, no dash. (The clipboard NaN/unrated case is 47-09.) |
| T-47-07 | Tampering | Divergent order sources (the 260615 class) reintroducing form≠export | high | mitigate | The filtered builder + Step-1 + labels read the single App.getSectionOrder() source; the rewritten guard test asserts the three-way invariant against a mutated order (Task 1). |
| T-47-08 | Information Disclosure | Document-title demotion (title dropped from label set) mis-branding PDF chrome | low | mitigate | buildDocumentSectionLabels keeps the document title in the label set (Task 1), preserving the existing L485-495 WARNING guarantee. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/30-export-markdown.test.js && node tests/47-severity-unrated.test.js` both exit 0; `npm test` full suite green (esp. severity PDF + export tests).
- Manual (real-device gate before /gsd-verify-work): topics-without-severity (topic-name section present, no rating lines) verified in a real opened PDF and .md; a session with a fully-unrated topic exports with NO empty bar row and NO dash; an all-unrated session omits the severity block entirely; with the Issue-severity switch off, an OLD rated session exports with no bars. (Severity-block PLACEMENT at a moved slot + the clipboard-copy clean-unrated case are verified in 47-09.)
- Comment hygiene: no planning IDs in export-modal.js comments.
</verification>

<success_criteria>
The filtered export builder + Step-1 + labels read the saved order from one page-pinned source (three-way invariant holds against a mutated order; no mid-edit divergence), the topics/severity split makes severity a dependent sub-option with pinned defaults, topics-without-severity emits the pinned topic-name section, the app-level severity switch gates the sub-option and the filtered builder + PDF payload, and the buildRenderInputs unrated filter drops fully-unrated topics (partial kept) so an all-unrated set omits the PDF block (no empty bars, no dash). Both tests pass; existing severity/export suites green. The clipboard builder + edit-aware severity-block placement land in 47-09.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-05-SUMMARY.md` when done. Record the final Step-1 topics/sub-option element ids/selectors, the emotionsBlockIncluded gating hooks (topics + sub-option + afterSeverity switch), and the buildRenderInputs PDF-input unrated-filter site (so 47-09 layers the clipboard builder + severity-block placement onto the same file without conflict).
</output>
