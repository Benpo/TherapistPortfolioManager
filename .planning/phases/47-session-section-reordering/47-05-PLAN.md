---
phase: 47-session-section-reordering
plan: 05
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - assets/export-modal.js
  - assets/pdf-export.js
  - tests/30-export-markdown.test.js
  - tests/47-severity-position.test.js
  - tests/47-severity-skip.test.js
autonomous: true
requirements: [ORDR-04, ORDR-07]
must_haves:
  truths:
    - "The export Step-1 checkbox list, BOTH markdown builders (buildFilteredSessionMarkdown AND the clipboard buildSessionMarkdown), and the document-section labels ALL read section order from App.getSectionOrder() — one source (D-13); on add-session.html that is the page-pinned snapshot the form captured at open, so a peer-tab order change mid-edit can never make form and export diverge (G-5); the 260615 three-way invariant (form == Step-1 == export) holds against a MUTATED saved order."
    - "Export Step-1 shows 'Session topics' as a main checkbox named identically to the in-session section title, with 'Include severity before/after' as a dependent, indented sub-option enabled only when topics is checked; the sub-option defaults checked when issue data exists, RE-checking topics restores that default, the topics checkbox keeps today's pre-selected opt-out default (checked iff issue data exists), and both reset per export (D-14/D-15/G-12)."
    - "With topics selected but severity excluded, the filtered markdown emits a 'Session topics' section (via getSectionLabel('issues',…)) listing the topic names at the issues slot; numeric ratings stay PDF-structural (bars), never text lines in the filtered markdown; the afterSeverity slot emits NOTHING in markdown (the PDF renders the bars there) (G-2)."
    - "When App.isSectionEnabled('afterSeverity') is false, the 'Include severity before/after' sub-option is hidden/disabled and BOTH builders exclude severity entirely — no PDF bars even for old sessions that carry ratings (D-08/G-3)."
    - "A topic rated '— (skip)' contributes NO severity to any export (no PDF bar, no markdown line, no clipboard line); if every topic is skip, the severity block is omitted even when the sub-option is checked (Interaction 8a / ORDR-07)."
    - "The clipboard-copy builder buildSessionMarkdown emits in saved order with the same topics/sub-option/switch gating, and a skip-rated topic copies as its name only — no skip-marker rating line and no NaN change line ever reach the clipboard (G-1)."
    - "The PDF severity block renders at the saved-order slot of the afterSeverity item, positioned against the section headings ACTUALLY PRESENT in the edited Step-2 text (saved order ∩ parsed editor headings) so a manual heading deletion cannot land the block at the wrong slot (G-8); the Phase-23 Hebrew bidi render loop is unchanged."
  artifacts:
    - "assets/export-modal.js — order-driven emission (EXPORT_SECTION_ORDER removed) across BOTH builders incl. the clipboard buildSessionMarkdown (G-1), topics/severity split with the pinned Session-topics emission (G-2), App.isSectionEnabled('afterSeverity') switch wiring (G-3), edit-aware deriveSeverityAfterSections (G-8), skip-omission, pinned Step-1 defaults (G-12)."
    - "assets/pdf-export.js — severity-block placement fed by the new severityAfterSections; render loop untouched."
    - "tests/30-export-markdown.test.js (rewritten three-way invariant), tests/47-severity-position.test.js, tests/47-severity-skip.test.js (incl. clipboard-builder skip/gating cases)."
  key_links:
    - "All order reads go through App.getSectionOrder().flatKeys() (47-01) — the SAME source the form (47-04) reads; on add-session.html the form pins the snapshot at open (App.pinSectionOrder) and export reads that pin, so both consumers see one identical order per page load (G-5)."
    - "severityAfterSections is now the ordinal of the afterSeverity slot among the section headings ACTUALLY PRESENT in the edited Step-2 text (saved order ∩ parsed editor headings) — the ONLY input change to the untouched pdf-export severity loop (G-8)."
    - "App.isSectionEnabled('afterSeverity') (toggle owned by 47-03) gates the sub-option AND both builders' severity output (G-3)."
    - "Skip omission branches on App.SEV_SKIP (47-01); export never renders a literal '—'/'N/A' value."
---

<objective>
Repoint both export builders to the saved order and fix the topics/severity selection. Today export-modal.js has three order consumers (EXPORT_SECTION_ORDER, buildDocumentSectionLabels, the Step-1 renderer) plus TWO builders with hardcoded in-line emission orders — buildFilteredSessionMarkdown (L379-475) AND the clipboard-copy buildSessionMarkdown (L189-329) — the exact fragmentation that produced the 260615 bug. All must read App.getSectionOrder() (the page-pinned snapshot on add-session.html, G-5). Then implement the D-14/D-15 topics/severity split (topics main checkbox + dependent severity sub-option with pinned defaults, G-12), the pinned topics-without-severity document representation (G-2), the app-level severity-switch wiring (App.isSectionEnabled('afterSeverity') gates the sub-option and both builders, G-3), the Interaction-8a skip omission in BOTH builders (incl. the clipboard copy, G-1), and re-derive severityAfterSections as an edit-aware ordinal (saved order ∩ parsed editor headings, G-8) so the PDF severity block lands at the right slot with the Phase-23 render loop untouched.

Purpose: ORDR-04 (atomic 260615 rewrite) + the folded 46.1 export bug (D-14). The three-way invariant (form == Step-1 == export) must hold against a mutated order, and severity must render exactly once at the right place.

Output: order-driven export-modal.js; pdf-export.js severity placement fed by the new count; a rewritten three-way guard test + two new behavior tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/CONTEXT-SEED.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-01-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from drift checks):
- `deriveSeverityAfterSections(orderedKeys, presentKeys)` — pure ordinal helper in export-modal.js (replaces the heart-shield-label heading match); presentKeys = section headings actually present in the edited Step-2 text (G-8)
- Topics main-checkbox + dependent severity sub-option controls in the Step-1 renderer
- The pinned Session-topics markdown emission (topic-name list at the issues slot via getSectionLabel) in buildFilteredSessionMarkdown (G-2)
- Order-driven + skip-aware + gated rewrite of the clipboard buildSessionMarkdown (G-1)
- Removal of `EXPORT_SECTION_ORDER` (export-modal.js L343) — replaced by App.getSectionOrder().flatKeys()
- tests/30-export-markdown.test.js (rewritten), tests/47-severity-position.test.js, tests/47-severity-skip.test.js
- Consumes (does NOT create): App.getSectionOrder / pinSectionOrder pin semantics / flattenOrderKeys / SEV_SKIP (47-01), App.isSectionEnabled('afterSeverity') (47-01/47-03), export.section.topics / export.suboption.includeSeverity (47-02), afterSeverity key (47-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order-driven emission — all export order consumers read App.getSectionOrder() (three-way invariant)</name>
  <files>assets/export-modal.js, tests/30-export-markdown.test.js</files>
  <read_first>
    - assets/export-modal.js L325-360 (EXPORT_DEFAULT_CHECKED, EXPORT_SECTION_ORDER, exportDefaultI18nKey) and L379-475 (buildFilteredSessionMarkdown — the HARDCODED in-line emission order L434-470) and L485-540 (buildDocumentSectionLabels, emotionsBlockIncluded, exportRenderStep1Rows)
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.getSectionOrder().flatKeys() / flattenOrderKeys — the ordered form-only key list, groups flattened, no group names per D-03)
    - tests/30-export-markdown.test.js — the existing 260615 guard suite to REWRITE (must assert against a MUTATED saved order, not static DOM)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pitfall 1 + `## Code Examples` (order-driven markdown emission replacing L434-470)
  </read_first>
  <behavior>
    - buildFilteredSessionMarkdown emits ## sections in App.getSectionOrder().flatKeys() order (only selected + non-empty keys), not a hardcoded sequence; group NAMES never appear (D-03); on add-session.html this order is the page-pinned snapshot the form captured at open (G-5).
    - The "issues" key emits the Session-topics section at its slot (representation pinned in Task 2); the "afterSeverity" key emits NO markdown text section (its content is the PDF-structural severity block).
    - buildDocumentSectionLabels returns labels in saved order (plus the document title, preserved for the PDF chrome classification).
    - exportRenderStep1Rows renders rows in saved order.
    - For a MUTATED saved order, the Step-1 row order, the markdown ## heading order, and buildDocumentSectionLabels order are identical (three-way invariant).
  </behavior>
  <action>
    Remove EXPORT_SECTION_ORDER (L343) and replace every consumer with App.getSectionOrder().flatKeys() (the flattened, form-only ordered key list from 47-01). Rewrite buildFilteredSessionMarkdown so instead of the hardcoded L434-470 sequence it iterates the ordered key list: for each key, if selected and its per-key value is non-empty, push `""` then `## ${stripRequired(App.getSectionLabel(key, exportDefaultI18nKey(key)))}` then the value — reusing the EXISTING per-key readers (heartShield yes/no line, heartShieldEmotions, trapped, insights, limitingBeliefs, additionalTech, comments, nextSession note+date) PLUS the "issues" key, which emits the pinned Session-topics representation defined in Task 2 (topic-name list under the getSectionLabel("issues",…) heading); the "afterSeverity" key emits NO markdown section — severity stays PDF-structural (G-2). Keep the heartShield yes/no export wording and the nextSession note-OR-date gate exactly as today; only the ORDER of emission changes. Do NOT add a separate order read for export: App.getSectionOrder() already returns the page-pinned snapshot on add-session.html (the form pins at open, 47-04/47-01) — export must not call refreshSectionOrderCache or otherwise bypass the pin (G-5). Rewrite buildDocumentSectionLabels to map App.getSectionOrder().flatKeys() to labels (then push the document title — it MUST stay in the set so the PDF chrome branch does not demote the title, per the L485-495 WARNING). Rewrite exportRenderStep1Rows to iterate the saved order. Add exportDefaultI18nKey cases for any key not already present (afterSeverity is handled via the split in Task 2, not as a normal content section). Do NOT emit group names anywhere (D-03). When removing the old static array, do not leave a tombstone comment that names the removed identifier (keep comments clean). Rewrite tests/30-export-markdown.test.js to assert the three-way invariant against a MUTATED saved order: stub App.getSectionOrder to a non-default order, build Step-1 rows + markdown + document labels, and assert all three orderings match flattenOrderKeys(order) for the selected/non-empty keys.
  </action>
  <verify>
    <automated>node tests/30-export-markdown.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` exits 0 and asserts against a MUTATED (non-default) saved order.
    - `grep -c 'EXPORT_SECTION_ORDER' assets/export-modal.js` is 0 (the static array is removed).
    - buildFilteredSessionMarkdown, buildDocumentSectionLabels, and exportRenderStep1Rows each call App.getSectionOrder (grep-assert).
    - For a mutated order, the ## heading sequence in the markdown equals the Step-1 row sequence equals buildDocumentSectionLabels order (for selected/non-empty keys).
    - No group name (emotionsTech/wrapup or a group title) appears in the emitted markdown.
  </acceptance_criteria>
  <done>All export order consumers read the one saved-order source; the markdown builder iterates that order instead of a hardcoded sequence; group names never leak; the rewritten guard test proves the three-way invariant against a mutated order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Topics/severity split (D-14/D-15) + skip-value export omission (8a)</name>
  <files>assets/export-modal.js, tests/47-severity-skip.test.js</files>
  <read_first>
    - assets/export-modal.js L485-540 (emotionsBlockIncluded, exportRenderStep1Rows, EXPORT_DEFAULT_CHECKED, sectionHasData) — the Step-1 row/selection machinery to extend into the topics + dependent-suboption pattern
    - assets/export-modal.js issues payload path (where issues[] with before/after feed the PDF severity block) so skip omission is applied to the payload
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.SEV_SKIP) and 47-02-SUMMARY.md (export.section.topics, export.suboption.includeSeverity keys)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Copywriting (Export Step-1 topics checkbox) + Interaction 8a (skip excluded from export; all-skip omits block; never render a literal —/N/A)
    - .planning/phases/47-session-section-reordering/CONTEXT-SEED.md (the locked D-14 split design)
  </read_first>
  <behavior>
    - Step-1 renders "Session topics" as a main checkbox (label = the in-session section title via export.section.topics) and "Include severity before/after" as a dependent indented sub-option that is DISABLED unless topics is checked (severity-alone impossible by construction).
    - The topics checkbox keeps today's pre-selected opt-out default: checked iff issue data exists (the shipped EXPORT_DEFAULT_CHECKED issues rule). The severity sub-option defaults checked when issue data exists, unchecked otherwise; RE-checking topics after an uncheck restores that same default (not the last manual state); both reset each export open (never persisted) (G-12).
    - With topics checked, the filtered markdown emits a "Session topics" section at the issues slot — heading via getSectionLabel("issues", exportDefaultI18nKey("issues")), body = the topic names as a plain list, one per line; NO rating text lines ever appear in the filtered markdown (numeric ratings stay PDF-structural bars); the afterSeverity slot emits nothing in markdown (G-2).
    - When App.isSectionEnabled('afterSeverity') is false, the sub-option is hidden (or disabled+unchecked) regardless of topics state, and BOTH builders exclude severity entirely — an old session with stored numeric ratings gets no PDF bars and no severity lines while the switch is off (G-3).
    - A topic whose before OR after equals App.SEV_SKIP contributes no severity bar/line; the topic name still exports if topics is selected.
    - If every topic's rating is skip (or there are no numeric ratings), the severity block is omitted even when the sub-option is checked; no literal "—"/"N/A" is ever emitted.
  </behavior>
  <action>
    In exportRenderStep1Rows, render the topics/severity split: a "Session topics" main checkbox (label App.t("export.section.topics") — identical to the in-session title, HE mismatch already fixed in 47-02) and, nested/indented beneath it, an "Include severity before/after" sub-option (App.t("export.suboption.includeSeverity")) that is enabled only while the topics checkbox is checked (toggle its disabled state on the topics checkbox change; when topics unchecks, uncheck+disable the sub-option; when topics RE-checks, restore the sub-option to its DEFAULT — checked iff issue data exists — not its last manual state, G-12). The topics checkbox itself keeps today's pre-selected opt-out default: checked iff the session has issue data (the shipped EXPORT_DEFAULT_CHECKED issues && hasData rule — G-12). Default the sub-option checked only when the session has issue data (reuse sectionHasData), and RESET both per export open — do not persist (D-15). Wire the app-level severity switch (G-3): read App.isSectionEnabled('afterSeverity') at export open — when false, do not offer the sub-option at all (hide the row or render it disabled+unchecked) AND force severity exclusion in BOTH builders (buildFilteredSessionMarkdown/PDF payload here, buildSessionMarkdown in Task 4): no PDF bars, no severity lines, even for old sessions whose stored issues carry numeric ratings (hidden before-scales keep their values in the DB — the switch must gate the OUTPUT, not rely on empty data). Pin the topics-without-severity document representation (G-2): when topics is selected, buildFilteredSessionMarkdown emits at the issues slot a section headed by stripRequired(App.getSectionLabel("issues", exportDefaultI18nKey("issues"))) whose body lists the topic names (one per line, plain text); it never emits rating text lines (ratings are PDF-structural bars gated by the sub-option); the afterSeverity slot emits nothing in markdown. Update emotionsBlockIncluded (and the severity payload path) so severity is included only when topics is selected AND the sub-option is checked AND the afterSeverity switch is on. Apply skip omission (8a): when building the issues[] payload forwarded to the PDF severity block, drop any before/after equal to App.SEV_SKIP (a skip contributes no bar); if the resulting set has no numeric before/after at all, omit the severity block entirely (do not render an empty block or a literal —/N/A). The topic NAME still exports under Session topics. Create tests/47-severity-skip.test.js asserting: (a) topics checked + sub-option unchecked → no severity in export, but the markdown still carries the Session-topics section with the topic names at the issues slot; (b) a mix of numeric + skip topics → only numeric contribute; (c) all-skip → severity block omitted even with the sub-option checked; (d) export output never contains a literal "—" or "N/A" as a severity value; (e) with App.isSectionEnabled('afterSeverity') stubbed false, the sub-option is not offered and a session WITH stored numeric ratings exports no severity; (f) unchecking then re-checking topics restores the sub-option default.
  </action>
  <verify>
    <automated>node tests/47-severity-skip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-skip.test.js` exits 0.
    - The severity sub-option element is disabled when the topics checkbox is unchecked (assert the disabled state toggles with topics); re-checking topics restores the sub-option to its data-derived default (G-12).
    - With topics checked and the sub-option unchecked, the built export contains no severity bar/line BUT the filtered markdown contains the Session-topics heading (via getSectionLabel) followed by the topic names at the issues slot (G-2).
    - The filtered markdown never contains a numeric rating text line for a topic, and emits no section at the afterSeverity slot (G-2).
    - With App.isSectionEnabled('afterSeverity') false, the sub-option is not offered and a session with stored numeric before/after ratings exports zero severity output from both builders (G-3).
    - An all-skip issue set produces no severity block even with the sub-option checked.
    - The emitted markdown/PDF payload contains no literal "—"/"N/A" severity token (grep/assert absence).
  </acceptance_criteria>
  <done>Export Step-1 offers topics with a dependent severity sub-option (severity-alone impossible, both defaults pinned and reset per export, re-check restores the default), topics-without-severity emits a topic-name Session-topics section at the issues slot (ratings stay PDF-structural, afterSeverity slot silent in markdown), the app-level severity switch hides the sub-option and forces severity out of both builders when off, skip-rated topics are excluded from severity output, all-skip omits the block, and no literal skip token leaks into any export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: severityAfterSections re-derived as an EDIT-AWARE saved-order ordinal; PDF block placement (loop untouched)</name>
  <files>assets/export-modal.js, assets/pdf-export.js, tests/47-severity-position.test.js</files>
  <read_first>
    - assets/export-modal.js L750-775 (the current severityAfterSections derivation — the heart-shield-label heading match producing 0/1; this is REPLACED. Note its L757-771 comment: the shipped mechanism honours MANUAL Step-2 edits by counting headings in the edited text — that edit-awareness is KEPT, per Ben's G-8 call)
    - assets/pdf-export.js L1425-1501 (drawSeverityBlock loop: it inserts the two-bar block when sectionHeadingsSeen === severityAfterSections — the loop STAYS UNCHANGED, only its input number changes; L1489-1497 counts headings in the EDITED text)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pattern 3 (deriveSeverityAfterSections ordinal) + Open Question 2 (keep the combined before/after block as ONE unit at the afterSeverity slot; do not duplicate a before-bar at the topics slot) — confirmed approach, amended by G-8 to be edit-aware
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.getSectionOrder().flatKeys(); the afterSeverity key)
  </read_first>
  <behavior>
    - deriveSeverityAfterSections(orderedKeys, presentKeys) returns the count of PRESENT sections preceding the afterSeverity slot in saved order; if afterSeverity is absent from orderedKeys it returns presentKeys.length (block at end).
    - presentKeys is derived at EXPORT time from the section headings actually present in the edited Step-2 text (saved order ∩ parsed editor headings), NOT from the build-time emitted keys — deleting a section heading in Step 2 shifts the block correctly (G-8, preserves the shipped edit-awareness).
    - For varied saved orders (afterSeverity early, middle, late) and for edited texts with headings removed, the returned ordinal matches the number of headings actually preceding the slot.
    - The PDF inserts the severity two-bar block after exactly that many sections; with the Phase-23 Hebrew bidi pipeline unchanged.
  </behavior>
  <action>
    Replace the L750-771 severityAfterSections computation with a call to a new pure helper deriveSeverityAfterSections(orderedKeys, presentKeys): orderedKeys = App.getSectionOrder().flatKeys(); presentKeys = the section keys whose headings are ACTUALLY PRESENT in the edited Step-2 text at export time — parse the editor content for the known section-heading lines (match each orderedKey's stripRequired(getSectionLabel(...)) heading string against the edited text, the same label set buildDocumentSectionLabels produces) and keep saved order (saved order ∩ parsed editor headings). This KEEPS the shipped Step-2 edit-awareness (the current mechanism counts headings in the EDITED text — L757-771 comment, pdf-export.js L1489-1497): a user who deletes a section heading in Step 2 still gets the severity block at the right slot, per Ben's G-8 decision — do NOT derive from build-time emitted keys. Return orderedKeys.indexOf("afterSeverity") === -1 ? presentKeys.length : the count of orderedKeys before the afterSeverity index that are in presentKeys. Feed this number into the existing buildRenderInputs return (severityAfterSections) exactly as today. In pdf-export.js, do NOT change the drawSeverityBlock render loop (L1425-1501) — it already inserts the block when sectionHeadingsSeen === severityAfterSections; only the input number now comes from the ordinal. Keep severity rendered EXACTLY ONCE at the afterSeverity slot (the combined before/after two-bar block stays one unit; the topics/Session-topics section shows topic names only, no duplicated before-bar — RESEARCH Open Question 2). Preserve the Phase-23 Hebrew bidi pipeline untouched. Create tests/47-severity-position.test.js (pure-fn) asserting deriveSeverityAfterSections for several orders: afterSeverity first (0), afterSeverity after N present sections (N), afterSeverity absent (presentKeys.length), afterSeverity present in order but with some preceding sections not present (count only present), AND an edit-awareness case: given an edited text with one preceding heading deleted, the derived presentKeys shrinks and the ordinal drops by one (G-8).
  </action>
  <verify>
    <automated>node tests/47-severity-position.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-position.test.js` exits 0.
    - deriveSeverityAfterSections(["issues","afterSeverity","comments"], ["issues","afterSeverity","comments"]) === 1.
    - deriveSeverityAfterSections with afterSeverity first returns 0; with afterSeverity absent returns presentKeys.length.
    - Preceding-but-not-present sections are NOT counted (only headings actually in the edited text before the slot).
    - presentKeys is computed from the EDITED Step-2 text at export time (grep-assert the call site parses the editor content, not the build-time emission list) — a heading deleted in Step 2 moves the block up one slot (G-8).
    - The pdf-export.js drawSeverityBlock loop body is unchanged (diff shows only the input value path changed, not the render loop) — grep-assert the loop's insertion condition string is intact.
    - `npm test` severity PDF tests (34-severity-bars, 34-severity-unmeasured, quick-260702-bg4) stay green.
  </acceptance_criteria>
  <done>severityAfterSections is the saved-order ordinal of the afterSeverity slot among the headings actually present in the edited Step-2 text (edit-awareness kept, G-8); the PDF block renders once at that slot with the Phase-23 render loop untouched; the ordinal test passes and existing severity PDF tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Clipboard-copy builder buildSessionMarkdown — saved-order emission, skip omission, same gating</name>
  <files>assets/export-modal.js, tests/47-severity-skip.test.js</files>
  <read_first>
    - assets/export-modal.js L189-329 (buildSessionMarkdown — the clipboard-copy builder: its HARDCODED section order and its per-issue lines built via issue.before !== null checks at L211-222; the numeric before/after/change lines it emits today)
    - The Task-1/Task-2 rewrites in this same file (the ordered-key iteration, the topics/sub-option/switch gating, the skip-omission payload logic — reuse, do not re-implement)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Interaction 8a (export never renders a literal skip value) + 47-CONTEXT.md D-13 (export order == saved order applies to EVERY export output)
  </read_first>
  <behavior>
    - buildSessionMarkdown emits its sections in App.getSectionOrder().flatKeys() order (the same page-pinned source as the filtered builder), not the old hardcoded sequence; group names never appear (D-03).
    - It honors the SAME topics/sub-option/switch gating as the filtered builder: topics unselected → no topic content; topics selected + severity excluded (sub-option unchecked OR afterSeverity switch off) → topic names only, no rating lines.
    - With severity included, a numeric-rated topic keeps its existing before/after/change text lines; a topic whose before OR after is App.SEV_SKIP emits its name only — no rating line for the skip side and no change line (the L211-222 issue.before !== null checks are replaced with numeric-type checks so the skip marker can never be string-concatenated into a rating line or produce an arithmetic NaN in the change line).
    - The copied text never contains the skip marker as a rating value nor a NaN-bearing change line.
  </behavior>
  <action>
    Rewrite buildSessionMarkdown (L189-329) to iterate the same ordered key list Task 1 established (App.getSectionOrder().flatKeys()) instead of its hardcoded sequence, reusing the per-key readers and the Session-topics emission from Tasks 1-2 where they apply to the copy format. Apply the SAME selection gating as the filtered builder (Task 2): the topics main checkbox gates topic content; the severity sub-option AND the App.isSectionEnabled('afterSeverity') switch gate all severity lines (G-3 applies to the clipboard copy too). Replace the L211-222 per-issue line builders' issue.before !== null guards with strict numeric checks (typeof value === "number" after the App.SEV_SKIP branch): a skip-valued side emits no before/after line, and the change line is emitted ONLY when both before and after are numbers — so a skip can never be string-interpolated into a rating line nor subtracted into a NaN change value (G-1). Topic names still emit when topics is selected. Keep the copy format's existing wording/structure for numeric-rated topics unchanged (only the order source, the gating, and the skip handling change). Extend tests/47-severity-skip.test.js with clipboard-builder cases: (a) a skip-rated topic copies as its name only — assert the copied text contains no skip-marker rating line and no NaN change line; (b) a mutated saved order changes the copied section order to match; (c) severity sub-option unchecked (or switch off) → the copy carries topic names but zero rating lines.
  </action>
  <verify>
    <automated>node tests/47-severity-skip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-skip.test.js` exits 0 (now covering the clipboard builder too).
    - buildSessionMarkdown calls App.getSectionOrder (grep-assert) and no hardcoded section-order array remains in it.
    - For a skip-rated topic, the copied output contains the topic name, no rating line carrying the skip marker, and no "NaN" anywhere (assert on the built string).
    - For a mutated saved order, the copied section sequence equals flattenOrderKeys(order) for the selected/non-empty keys.
    - With the severity sub-option unchecked or App.isSectionEnabled('afterSeverity') false, the copy contains zero before/after/change lines while topic names remain.
  </acceptance_criteria>
  <done>The clipboard-copy builder reads the same saved-order source, honors the same topics/sub-option/switch gating as the other builders, and a skip-rated topic copies as its name only — no skip-marker rating line and no NaN change line can reach the clipboard. The extended skip test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data → export document | Issue ratings (incl. the new skip marker) and section values flow into markdown + PDF. |
| saved order → export order | App.getSectionOrder() drives emission order + severity placement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06 | Tampering/Information Disclosure | Skip marker leaking a phantom value, a literal "—"/"N/A", or a NaN arithmetic artifact into an exported/copied clinical document | medium | mitigate | Skip omission drops SEV_SKIP before/after from both builders (Task 2) AND the clipboard copy replaces null-checks with strict numeric checks so no skip-marker line or NaN change line can be emitted (Task 4). |
| T-47-07 | Tampering | Divergent order sources (the 260615 class) reintroducing form≠export | high | mitigate | All export consumers read the single App.getSectionOrder() source; the rewritten guard test asserts the three-way invariant against a mutated order (Task 1). |
| T-47-08 | Information Disclosure | Document-title demotion (title dropped from label set) mis-branding PDF chrome | low | mitigate | buildDocumentSectionLabels keeps the document title in the label set (Task 1), preserving the existing L485-495 WARNING guarantee. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/30-export-markdown.test.js && node tests/47-severity-skip.test.js && node tests/47-severity-position.test.js` all exit 0; `npm test` full suite green (esp. severity PDF + export tests).
- Manual (real-device gate before /gsd-verify-work): export a session with severity moved to a non-default slot → PDF severity block appears at that slot; Hebrew RTL PDF still correct; topics-without-severity (topic-name section present, no rating lines) and severity-omitted-when-all-skip verified in a real opened PDF and .md; clipboard copy of a skip-rated session verified clean; with the Issue-severity switch off, an OLD rated session exports with no bars.
- Comment hygiene: no planning IDs in export-modal.js / pdf-export.js comments.
</verification>

<success_criteria>
BOTH export builders — filtered AND clipboard — read the saved order from one page-pinned source (three-way invariant holds against a mutated order; no mid-edit divergence), the topics/severity split makes severity a dependent sub-option with pinned defaults, topics-without-severity emits the pinned topic-name section, the app-level severity switch gates the sub-option and all severity output, skip-rated topics are excluded everywhere (all-skip omits the block, no literal marker, no NaN in the copy), and the PDF severity block lands at the edit-aware saved-order slot with the Phase-23 loop untouched. All three tests pass; existing severity/export suites green.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-05-SUMMARY.md` when done. Record the deriveSeverityAfterSections signature (with the presentKeys edit-aware derivation), the final Step-1 topics/sub-option element ids/selectors, and the buildSessionMarkdown gating hooks.
</output>
