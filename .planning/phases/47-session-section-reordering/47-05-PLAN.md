---
phase: 47-session-section-reordering
plan: 05
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - assets/export-modal.js
  - assets/pdf-export.js
  - tests/30-export-markdown.test.js
  - tests/47-severity-position.test.js
  - tests/47-severity-skip.test.js
autonomous: true
requirements: [ORDR-04, ORDR-07]
must_haves:
  truths:
    - "The export Step-1 checkbox list, the markdown builder, and the document-section labels ALL read section order from App.getSectionOrder() — one source (D-13); the 260615 three-way invariant (form == Step-1 == export) holds against a MUTATED saved order."
    - "Export Step-1 shows 'Session topics' as a main checkbox named identically to the in-session section title, with 'Include severity before/after' as a dependent, indented sub-option enabled only when topics is checked; the sub-option defaults checked when issue data exists and resets per export (D-14/D-15)."
    - "A topic rated '— (skip)' contributes NO severity to any export (no PDF bar, no markdown line); if every topic is skip, the severity block is omitted even when the sub-option is checked (Interaction 8a / ORDR-07)."
    - "The PDF severity block renders at the saved-order slot of the afterSeverity item; the Phase-23 Hebrew bidi render loop is unchanged."
  artifacts:
    - "assets/export-modal.js — order-driven emission (EXPORT_SECTION_ORDER removed), topics/severity split, deriveSeverityAfterSections ordinal, skip-omission."
    - "assets/pdf-export.js — severity-block placement fed by the new severityAfterSections; render loop untouched."
    - "tests/30-export-markdown.test.js (rewritten three-way invariant), tests/47-severity-position.test.js, tests/47-severity-skip.test.js."
  key_links:
    - "All order reads go through App.getSectionOrder().flatKeys() (47-01) — the SAME source the form (47-04) reads."
    - "severityAfterSections is now the ordinal of the afterSeverity slot among EMITTED sections in saved order — the ONLY input change to the untouched pdf-export severity loop."
    - "Skip omission branches on App.SEV_SKIP (47-01); export never renders a literal '—'/'N/A' value."
---

<objective>
Repoint both export builders to the saved order and fix the topics/severity selection. Today export-modal.js has three order consumers (EXPORT_SECTION_ORDER, buildDocumentSectionLabels, the Step-1 renderer) plus buildFilteredSessionMarkdown's hardcoded in-line emission order — the exact fragmentation that produced the 260615 bug. All must read App.getSectionOrder(). Then implement the D-14/D-15 topics/severity split (topics main checkbox + dependent severity sub-option), the Interaction-8a skip omission, and re-derive severityAfterSections as an ordinal so the PDF severity block lands at the saved-order slot with the Phase-23 render loop untouched.

Purpose: ORDR-04 (atomic 260615 rewrite) + the folded 46.1 export bug (D-14). The three-way invariant (form == Step-1 == export) must hold against a mutated order, and severity must render exactly once at the right place.

Output: order-driven export-modal.js; pdf-export.js severity placement fed by the new count; a rewritten three-way guard test + two new behavior tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/CONTEXT-SEED.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-01-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from drift checks):
- `deriveSeverityAfterSections(orderedKeys, emittedKeys)` — pure ordinal helper in export-modal.js (replaces the heart-shield-label heading match)
- Topics main-checkbox + dependent severity sub-option controls in the Step-1 renderer
- Removal of `EXPORT_SECTION_ORDER` (export-modal.js L343) — replaced by App.getSectionOrder().flatKeys()
- tests/30-export-markdown.test.js (rewritten), tests/47-severity-position.test.js, tests/47-severity-skip.test.js
- Consumes (does NOT create): App.getSectionOrder / flattenOrderKeys / SEV_SKIP (47-01), export.section.topics / export.suboption.includeSeverity (47-02), afterSeverity key (47-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order-driven emission — all export order consumers read App.getSectionOrder() (three-way invariant)</name>
  <files>assets/export-modal.js, tests/30-export-markdown.test.js</files>
  <read_first>
    - assets/export-modal.js L325-360 (EXPORT_DEFAULT_CHECKED, EXPORT_SECTION_ORDER, exportDefaultI18nKey) and L379-475 (buildFilteredSessionMarkdown — the HARDCODED in-line emission order L434-470) and L485-540 (buildDocumentSectionLabels, emotionsBlockIncluded, exportRenderStep1Rows)
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.getSectionOrder().flatKeys() / flattenOrderKeys — the ordered form-only key list, groups flattened, no group names per D-03)
    - tests/30-export-markdown.test.js — the existing 260615 guard suite to REWRITE (must assert against a MUTATED saved order, not static DOM)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pitfall 1 + `## Code Examples` (order-driven markdown emission replacing L434-470)
  </read_first>
  <behavior>
    - buildFilteredSessionMarkdown emits ## sections in App.getSectionOrder().flatKeys() order (only selected + non-empty keys), not a hardcoded sequence; group NAMES never appear (D-03).
    - buildDocumentSectionLabels returns labels in saved order (plus the document title, preserved for the PDF chrome classification).
    - exportRenderStep1Rows renders rows in saved order.
    - For a MUTATED saved order, the Step-1 row order, the markdown ## heading order, and buildDocumentSectionLabels order are identical (three-way invariant).
  </behavior>
  <action>
    Remove EXPORT_SECTION_ORDER (L343) and replace every consumer with App.getSectionOrder().flatKeys() (the flattened, form-only ordered key list from 47-01). Rewrite buildFilteredSessionMarkdown so instead of the hardcoded L434-470 sequence it iterates the ordered key list: for each key, if selected and its per-key value is non-empty, push `""` then `## ${stripRequired(App.getSectionLabel(key, exportDefaultI18nKey(key)))}` then the value — reusing the EXISTING per-key readers (heartShield yes/no line, heartShieldEmotions, trapped, insights, limitingBeliefs, additionalTech, comments, nextSession note+date). Keep the heartShield yes/no export wording and the nextSession note-OR-date gate exactly as today; only the ORDER of emission changes. Rewrite buildDocumentSectionLabels to map App.getSectionOrder().flatKeys() to labels (then push the document title — it MUST stay in the set so the PDF chrome branch does not demote the title, per the L485-495 WARNING). Rewrite exportRenderStep1Rows to iterate the saved order. Add exportDefaultI18nKey cases for any key not already present (afterSeverity is handled via the split in Task 2, not as a normal content section). Do NOT emit group names anywhere (D-03). When removing the old static array, do not leave a tombstone comment that names the removed identifier (keep comments clean). Rewrite tests/30-export-markdown.test.js to assert the three-way invariant against a MUTATED saved order: stub App.getSectionOrder to a non-default order, build Step-1 rows + markdown + document labels, and assert all three orderings match flattenOrderKeys(order) for the selected/non-empty keys.
  </action>
  <verify>
    <automated>node tests/30-export-markdown.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/30-export-markdown.test.js` exits 0 and asserts against a MUTATED (non-default) saved order.
    - `grep -c 'EXPORT_SECTION_ORDER' assets/export-modal.js` is 0 (the static array is removed).
    - buildFilteredSessionMarkdown, buildDocumentSectionLabels, and exportRenderStep1Rows each call App.getSectionOrder (grep-assert).
    - For a mutated order, the ## heading sequence in the markdown equals the Step-1 row sequence equals buildDocumentSectionLabels order (for selected/non-empty keys).
    - No group name (emotionsTech/wrapup or a group title) appears in the emitted markdown.
  </acceptance_criteria>
  <done>All export order consumers read the one saved-order source; the markdown builder iterates that order instead of a hardcoded sequence; group names never leak; the rewritten guard test proves the three-way invariant against a mutated order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Topics/severity split (D-14/D-15) + skip-value export omission (8a)</name>
  <files>assets/export-modal.js, tests/47-severity-skip.test.js</files>
  <read_first>
    - assets/export-modal.js L485-540 (emotionsBlockIncluded, exportRenderStep1Rows, EXPORT_DEFAULT_CHECKED, sectionHasData) — the Step-1 row/selection machinery to extend into the topics + dependent-suboption pattern
    - assets/export-modal.js issues payload path (where issues[] with before/after feed the PDF severity block) so skip omission is applied to the payload
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.SEV_SKIP) and 47-02-SUMMARY.md (export.section.topics, export.suboption.includeSeverity keys)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Copywriting (Export Step-1 topics checkbox) + Interaction 8a (skip excluded from export; all-skip omits block; never render a literal —/N/A)
    - .planning/phases/47-session-section-reordering/CONTEXT-SEED.md (the locked D-14 split design)
  </read_first>
  <behavior>
    - Step-1 renders "Session topics" as a main checkbox (label = the in-session section title via export.section.topics) and "Include severity before/after" as a dependent indented sub-option that is DISABLED unless topics is checked (severity-alone impossible by construction).
    - The severity sub-option defaults checked when issue data exists, unchecked otherwise; the choice resets each export open (never persisted).
    - A topic whose before OR after equals App.SEV_SKIP contributes no severity bar/line; the topic name still exports if topics is selected.
    - If every topic's rating is skip (or there are no numeric ratings), the severity block is omitted even when the sub-option is checked; no literal "—"/"N/A" is ever emitted.
  </behavior>
  <action>
    In exportRenderStep1Rows, render the topics/severity split: a "Session topics" main checkbox (label App.t("export.section.topics") — identical to the in-session title, HE mismatch already fixed in 47-02) and, nested/indented beneath it, an "Include severity before/after" sub-option (App.t("export.suboption.includeSeverity")) that is enabled only while the topics checkbox is checked (toggle its disabled state on the topics checkbox change; when topics unchecks, uncheck+disable the sub-option). Default the sub-option checked only when the session has issue data (reuse sectionHasData), and RESET both per export open — do not persist (D-15). Update emotionsBlockIncluded (and the severity payload path) so severity is included only when BOTH topics is selected AND the sub-option is checked. Apply skip omission (8a): when building the issues[] payload forwarded to the PDF severity block AND the markdown severity contribution, drop any before/after equal to App.SEV_SKIP (a skip contributes no bar and no line); if the resulting set has no numeric before/after at all, omit the severity block entirely (do not render an empty block or a literal —/N/A). The topic NAME still exports under Session topics. Create tests/47-severity-skip.test.js asserting: (a) topics checked + sub-option unchecked → no severity in export; (b) a mix of numeric + skip topics → only numeric contribute; (c) all-skip → severity block omitted even with the sub-option checked; (d) export output never contains a literal "—" or "N/A" as a severity value.
  </action>
  <verify>
    <automated>node tests/47-severity-skip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-skip.test.js` exits 0.
    - The severity sub-option element is disabled when the topics checkbox is unchecked (assert the disabled state toggles with topics).
    - With topics checked and the sub-option unchecked, the built export contains no severity bar/line.
    - An all-skip issue set produces no severity block even with the sub-option checked.
    - The emitted markdown/PDF payload contains no literal "—"/"N/A" severity token (grep/assert absence).
  </acceptance_criteria>
  <done>Export Step-1 offers topics with a dependent severity sub-option (severity-alone impossible, resets per export), skip-rated topics are excluded from severity output, all-skip omits the block, and no literal skip token leaks into any export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: severityAfterSections re-derived as saved-order ordinal; PDF block placement (loop untouched)</name>
  <files>assets/export-modal.js, assets/pdf-export.js, tests/47-severity-position.test.js</files>
  <read_first>
    - assets/export-modal.js L750-775 (the current severityAfterSections derivation — the heart-shield-label heading match producing 0/1; this is REPLACED)
    - assets/pdf-export.js L1425-1501 (drawSeverityBlock loop: it inserts the two-bar block when sectionHeadingsSeen === severityAfterSections — the loop STAYS UNCHANGED, only its input number changes)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pattern 3 (deriveSeverityAfterSections ordinal) + Open Question 2 (keep the combined before/after block as ONE unit at the afterSeverity slot; do not duplicate a before-bar at the topics slot) — confirmed approach
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.getSectionOrder().flatKeys(); the afterSeverity key)
  </read_first>
  <behavior>
    - deriveSeverityAfterSections(orderedKeys, emittedKeys) returns the count of EMITTED sections preceding the afterSeverity slot in saved order; if afterSeverity is absent it returns emittedKeys.length (block at end).
    - For varied saved orders (afterSeverity early, middle, late), the returned ordinal matches the number of sections emitted before it.
    - The PDF inserts the severity two-bar block after exactly that many sections; with the Phase-23 Hebrew bidi pipeline unchanged.
  </behavior>
  <action>
    Replace the L750-771 severityAfterSections computation with a call to a new pure helper deriveSeverityAfterSections(orderedKeys, emittedKeys): orderedKeys = App.getSectionOrder().flatKeys(); emittedKeys = the keys actually emitted into the document body (selected + non-empty, in saved order). Return orderedKeys.indexOf("afterSeverity") === -1 ? emittedKeys.length : the count of orderedKeys before the afterSeverity index that are in emittedKeys. Feed this number into the existing buildRenderInputs return (severityAfterSections) exactly as today. In pdf-export.js, do NOT change the drawSeverityBlock render loop (L1425-1501) — it already inserts the block when sectionHeadingsSeen === severityAfterSections; only the input number now comes from the ordinal. Keep severity rendered EXACTLY ONCE at the afterSeverity slot (the combined before/after two-bar block stays one unit; the topics/Session-topics section shows topic names only, no duplicated before-bar — RESEARCH Open Question 2). Preserve the Phase-23 Hebrew bidi pipeline untouched. Create tests/47-severity-position.test.js (pure-fn) asserting deriveSeverityAfterSections for several orders: afterSeverity first (0), afterSeverity after N emitted sections (N), afterSeverity absent (emittedKeys.length), and afterSeverity present in order but with some preceding sections not emitted (count only emitted).
  </action>
  <verify>
    <automated>node tests/47-severity-position.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-position.test.js` exits 0.
    - deriveSeverityAfterSections(["issues","afterSeverity","comments"], ["issues","afterSeverity","comments"]) === 1.
    - deriveSeverityAfterSections with afterSeverity first returns 0; with afterSeverity absent returns emittedKeys.length.
    - Preceding-but-not-emitted sections are NOT counted (only emitted keys before the slot).
    - The pdf-export.js drawSeverityBlock loop body is unchanged (diff shows only the input value path changed, not the render loop) — grep-assert the loop's insertion condition string is intact.
    - `npm test` severity PDF tests (34-severity-bars, 34-severity-unmeasured, quick-260702-bg4) stay green.
  </acceptance_criteria>
  <done>severityAfterSections is the saved-order ordinal of the afterSeverity slot among emitted sections; the PDF block renders once at that slot with the Phase-23 render loop untouched; the ordinal test passes and existing severity PDF tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data → export document | Issue ratings (incl. the new skip marker) and section values flow into markdown + PDF. |
| saved order → export order | App.getSectionOrder() drives emission order + severity placement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06 | Tampering/Information Disclosure | Skip marker leaking a phantom value or a literal "—"/"N/A" into an exported clinical document | medium | mitigate | Skip omission drops SEV_SKIP before/after from both builders; all-skip omits the block; export never renders the marker literally (Task 2). |
| T-47-07 | Tampering | Divergent order sources (the 260615 class) reintroducing form≠export | high | mitigate | All export consumers read the single App.getSectionOrder() source; the rewritten guard test asserts the three-way invariant against a mutated order (Task 1). |
| T-47-08 | Information Disclosure | Document-title demotion (title dropped from label set) mis-branding PDF chrome | low | mitigate | buildDocumentSectionLabels keeps the document title in the label set (Task 1), preserving the existing L485-495 WARNING guarantee. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/30-export-markdown.test.js && node tests/47-severity-skip.test.js && node tests/47-severity-position.test.js` all exit 0; `npm test` full suite green (esp. severity PDF + export tests).
- Manual (real-device gate before /gsd-verify-work): export a session with severity moved to a non-default slot → PDF severity block appears at that slot; Hebrew RTL PDF still correct; topics-without-severity and severity-omitted-when-all-skip verified in a real opened PDF and .md.
- Comment hygiene: no planning IDs in export-modal.js / pdf-export.js comments.
</verification>

<success_criteria>
Both export builders read the saved order from one source (three-way invariant holds against a mutated order), the topics/severity split makes severity a dependent sub-option, skip-rated topics are excluded (all-skip omits the block, no literal marker), and the PDF severity block lands at the saved-order slot with the Phase-23 loop untouched. All three tests pass; existing severity/export suites green.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-05-SUMMARY.md` when done. Record the deriveSeverityAfterSections signature and the final Step-1 topics/sub-option element ids/selectors.
</output>
