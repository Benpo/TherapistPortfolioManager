---
phase: 47-session-section-reordering
plan: 09
type: execute
wave: 3
depends_on: ["47-05"]
files_modified:
  - assets/export-modal.js
  - assets/pdf-export.js
  - tests/47-severity-position.test.js
  - tests/47-severity-unrated.test.js
autonomous: true
requirements: [ORDR-04, ORDR-07]
must_haves:
  truths:
    - "The PDF severity block renders at the saved-order slot of the afterSeverity item, positioned against the section headings ACTUALLY PRESENT in the edited Step-2 text (saved order ∩ parsed editor headings) so a manual heading deletion cannot land the block at the wrong slot (G-8); the Phase-23 Hebrew bidi render loop is unchanged."
    - "The clipboard-copy builder buildSessionMarkdown emits in saved order (App.flattenOrderKeys(App.getSectionOrder()) — the same page-pinned source the filtered builder reads) with the same topics/sub-option/switch gating as the filtered builder, and group names never appear (D-03/G-1)."
    - "A topic with no numeric rating (an unrated before/after side, value null) copies as its name only — no rating line for the unrated side and no NaN change line ever reaches the clipboard (the L211-222 issue.before !== null checks become strict typeof === 'number' checks, D-21/G-1)."
    - "severityAfterSections is now the ordinal of the afterSeverity slot among the section headings ACTUALLY PRESENT in the edited Step-2 text (saved order ∩ parsed editor headings) — the ONLY input change to the untouched pdf-export severity loop (G-8)."
  artifacts:
    - "assets/export-modal.js — edit-aware deriveSeverityAfterSections(orderedKeys, presentKeys) feeding buildRenderInputs.severityAfterSections; order-driven + gated + unrated-safe rewrite of the clipboard buildSessionMarkdown (G-1)."
    - "assets/pdf-export.js — severity-block placement fed by the new severityAfterSections; drawSeverityBlock render loop untouched (only the input number path changes)."
    - "tests/47-severity-position.test.js (deriveSeverityAfterSections edit-aware ordinal); tests/47-severity-unrated.test.js EXTENDED with the clipboard-builder cases (saved-order + gating + unrated-copies-as-name-only, no NaN)."
  key_links:
    - "This plan is wave 3, depends_on 47-05 (wave 2) — the two executors that edit export-modal.js run SEQUENTIALLY, never concurrently; 47-05 owns the filtered builder / Step-1 / labels / PDF-input filter, 47-09 layers the clipboard builder + severity-block placement onto the same file afterward."
    - "All order reads go through App.flattenOrderKeys(App.getSectionOrder()) (47-01) — the SAME page-pinned source the filtered builder (47-05) and the form (47-04) read on add-session.html (G-5)."
    - "severityAfterSections is the ordinal of the afterSeverity slot among the headings ACTUALLY PRESENT in the edited Step-2 text (saved order ∩ parsed editor headings) — the ONLY input change to the untouched pdf-export severity loop (G-8)."
    - "The clipboard builder honors the same emotionsBlockIncluded gating (topics + sub-option + App.isSectionEnabled('afterSeverity') switch) that 47-05 wired (G-3); unrated omission is by strict numeric-type checks, never a marker branch (D-21)."
---

<objective>
Complete the export rewrite that 47-05 began, on the same file, in a later wave so the two executors never touch export-modal.js concurrently. Two remaining concerns: (1) re-derive severityAfterSections as an EDIT-AWARE saved-order ordinal (saved order ∩ parsed editor headings, G-8) so the PDF severity block lands at the afterSeverity slot with the Phase-23 drawSeverityBlock loop untouched; (2) repoint the clipboard-copy builder buildSessionMarkdown (export-modal.js L189-329) to the saved order with the same topics/sub-option/switch gating the filtered builder uses, and make an unrated topic copy as its name only (strict numeric-type checks replace the null-checks so no unrated line and no NaN change line can reach the clipboard, D-21/G-1).

Purpose: ORDR-04 (the clipboard copy is also an export output — D-13 saved-order applies) + D-21 (unrated omitted from EVERY output, including the clipboard) + G-8 (the edit-aware PDF placement Ben locked at plan review). Split from 47-05 (Ben's context-quality directive) so each export-modal.js executor gets a fresh 2-task context and sequential file access.

Output: edit-aware deriveSeverityAfterSections + PDF block placement (loop untouched); order-driven + gated + unrated-safe clipboard buildSessionMarkdown; a new ordinal test + the extended unrated-omission test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/CONTEXT-SEED.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-01-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-02-SUMMARY.md
@.planning/phases/47-session-section-reordering/47-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from drift checks):
- `deriveSeverityAfterSections(orderedKeys, presentKeys)` — pure ordinal helper in export-modal.js (replaces the heart-shield-label heading match); presentKeys = section headings actually present in the edited Step-2 text (G-8)
- Order-driven + gated + unrated-safe rewrite of the clipboard buildSessionMarkdown (G-1)
- tests/47-severity-position.test.js (new); tests/47-severity-unrated.test.js EXTENDED (clipboard cases)
- Consumes (does NOT create): App.getSectionOrder / pinSectionOrder pin semantics / flattenOrderKeys (47-01), App.isSectionEnabled('afterSeverity') (47-01/47-03), the filtered-builder order iteration + emotionsBlockIncluded gating + the buildRenderInputs PDF-input unrated filter + the Session-topics emission (47-05), afterSeverity key (47-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: severityAfterSections re-derived as an EDIT-AWARE saved-order ordinal; PDF block placement (loop untouched)</name>
  <files>assets/export-modal.js, assets/pdf-export.js, tests/47-severity-position.test.js</files>
  <read_first>
    - assets/export-modal.js L722-773 (buildRenderInputs — 47-05 filtered the L744 issues payload to ≥1-numeric-rating topics for the PDF; THIS task replaces the L760-771 severityAfterSections derivation — the heart-shield-label heading match producing 0/1. Note its comment: the shipped mechanism honours MANUAL Step-2 edits by counting headings in the edited text — that edit-awareness is KEPT, per Ben's G-8 call)
    - assets/pdf-export.js L1425-1501 (drawSeverityBlock loop: it inserts the two-bar block when sectionHeadingsSeen === severityAfterSections — the loop STAYS UNCHANGED, only its input number changes; L1489-1497 counts headings in the EDITED text)
    - .planning/phases/47-session-section-reordering/47-05-SUMMARY.md (the finalized order iteration, the Session-topics emission — afterSeverity never emits a markdown heading, and the buildRenderInputs PDF-input filter site) and 47-01-SUMMARY.md (App.flattenOrderKeys(App.getSectionOrder()); the afterSeverity key)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pattern 3 (deriveSeverityAfterSections ordinal) + Open Question 2 (keep the combined before/after block as ONE unit at the afterSeverity slot; do not duplicate a before-bar at the topics slot) — confirmed approach, amended by G-8 to be edit-aware
  </read_first>
  <behavior>
    - deriveSeverityAfterSections(orderedKeys, presentKeys) returns the count of PRESENT sections preceding the afterSeverity slot in saved order; if afterSeverity is absent from orderedKeys it returns presentKeys.length (block at end).
    - presentKeys is derived at EXPORT time from the section headings actually present in the edited Step-2 text (saved order ∩ parsed editor headings), NOT from the build-time emitted keys — deleting a section heading in Step 2 shifts the block correctly (G-8, preserves the shipped edit-awareness).
    - For varied saved orders (afterSeverity early, middle, late) and for edited texts with headings removed, the returned ordinal matches the number of headings actually preceding the slot.
    - The PDF inserts the severity two-bar block after exactly that many sections; with the Phase-23 Hebrew bidi pipeline unchanged.
  </behavior>
  <action>
    Replace the L760-771 severityAfterSections computation in buildRenderInputs with a call to a new pure helper deriveSeverityAfterSections(orderedKeys, presentKeys): orderedKeys = App.flattenOrderKeys(App.getSectionOrder()); presentKeys = the section keys whose headings are ACTUALLY PRESENT in the edited Step-2 text at export time — parse the editor content for the known section-heading lines (match each orderedKey's stripRequired(getSectionLabel(...)) heading string against the edited text, the same label set buildDocumentSectionLabels produces) and keep saved order (saved order ∩ parsed editor headings). This KEEPS the shipped Step-2 edit-awareness (the current mechanism counts headings in the EDITED text — the buildRenderInputs comment + pdf-export.js L1489-1497): a user who deletes a section heading in Step 2 still gets the severity block at the right slot, per Ben's G-8 decision — do NOT derive from build-time emitted keys. Return orderedKeys.indexOf("afterSeverity") === -1 ? presentKeys.length : the count of orderedKeys before the afterSeverity index that are in presentKeys. Feed this number into the existing buildRenderInputs return (severityAfterSections) exactly as today; leave the 47-05 PDF-input issues filter (L744) untouched. In pdf-export.js, do NOT change the drawSeverityBlock render loop (L1425-1501) — it already inserts the block when sectionHeadingsSeen === severityAfterSections; only the input number now comes from the ordinal. Keep severity rendered EXACTLY ONCE at the afterSeverity slot (the combined before/after two-bar block stays one unit; the topics/Session-topics section shows topic names only, no duplicated before-bar — RESEARCH Open Question 2). Preserve the Phase-23 Hebrew bidi pipeline untouched. Create tests/47-severity-position.test.js (pure-fn) asserting deriveSeverityAfterSections for several orders: afterSeverity first (0), afterSeverity after N present sections (N), afterSeverity absent (presentKeys.length), afterSeverity present in order but with some preceding sections not present (count only present), AND an edit-awareness case: given an edited text with one preceding heading deleted, the derived presentKeys shrinks and the ordinal drops by one (G-8).
  </action>
  <verify>
    <automated>node tests/47-severity-position.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-position.test.js` exits 0.
    - deriveSeverityAfterSections(["issues","afterSeverity","comments"], ["issues","comments"]) === 1 (afterSeverity never appears in presentKeys — its heading is never emitted to the Step-2 text per G-2; only the "issues" heading before the slot counts) (R2-3).
    - deriveSeverityAfterSections with afterSeverity first returns 0; with afterSeverity absent returns presentKeys.length.
    - Preceding-but-not-present sections are NOT counted (only headings actually in the edited text before the slot).
    - presentKeys is computed from the EDITED Step-2 text at export time (grep-assert the call site parses the editor content, not the build-time emission list) — a heading deleted in Step 2 moves the block up one slot (G-8).
    - The pdf-export.js drawSeverityBlock loop body is unchanged (diff shows only the input value path changed, not the render loop) — grep-assert the loop's insertion condition string is intact.
    - `npm test` severity PDF tests (34-severity-bars, 34-severity-unmeasured, quick-260702-bg4) stay green.
  </acceptance_criteria>
  <done>severityAfterSections is the saved-order ordinal of the afterSeverity slot among the headings actually present in the edited Step-2 text (edit-awareness kept, G-8); the PDF block renders once at that slot with the Phase-23 render loop untouched; the ordinal test passes and existing severity PDF tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clipboard-copy builder buildSessionMarkdown — saved-order emission, unrated omission, same gating</name>
  <files>assets/export-modal.js, tests/47-severity-unrated.test.js</files>
  <read_first>
    - assets/export-modal.js L189-329 (buildSessionMarkdown — the clipboard-copy builder: its HARDCODED section order and its per-issue lines built via issue.before !== null checks at L211-222; the numeric before/after/change lines it emits today)
    - .planning/phases/47-session-section-reordering/47-05-SUMMARY.md (the ordered-key iteration, the topics/sub-option/switch gating via emotionsBlockIncluded, and the Session-topics emission the filtered builder established — reuse, do not re-implement)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md `## SUPERSESSION NOTE` + Interaction 8a (export never renders a literal unrated placeholder; the — pill is gone, omission keys on unrated/null) + 47-CONTEXT.md D-13 (export order == saved order applies to EVERY export output) + D-21 (unrated omitted everywhere)
  </read_first>
  <behavior>
    - buildSessionMarkdown emits its sections in App.flattenOrderKeys(App.getSectionOrder()) order (the same page-pinned source as the filtered builder), not the old hardcoded sequence; group names never appear (D-03).
    - It honors the SAME topics/sub-option/switch gating as the filtered builder: topics unselected → no topic content; topics selected + severity excluded (sub-option unchecked OR afterSeverity switch off) → topic names only, no rating lines.
    - With severity included, a numeric-rated topic keeps its existing before/after/change text lines; a topic whose before OR after is unrated (null) emits its name only — no rating line for the unrated side and no change line (the L211-222 issue.before !== null checks are replaced with strict typeof === "number" checks so a null side can never be string-concatenated into a rating line or produce an arithmetic NaN in the change line).
    - The copied text never contains an unrated placeholder as a rating value nor a NaN-bearing change line.
  </behavior>
  <action>
    Rewrite buildSessionMarkdown (L189-329) to iterate the same ordered key list 47-05 established (App.flattenOrderKeys(App.getSectionOrder())) instead of its hardcoded sequence, reusing the per-key readers and the Session-topics emission from 47-05 where they apply to the copy format. Apply the SAME selection gating as the filtered builder (via the emotionsBlockIncluded hook 47-05 wired): the topics main checkbox gates topic content; the severity sub-option AND the App.isSectionEnabled('afterSeverity') switch gate all severity lines (G-3 applies to the clipboard copy too). Replace the L211-222 per-issue line builders' issue.before !== null guards with strict numeric checks (typeof value === "number"): an unrated side (null) emits no before/after line, and the change line is emitted ONLY when both before and after are numbers — so a null side can never be string-interpolated into a rating line nor subtracted into a NaN change value (G-1/D-21). Topic names still emit when topics is selected. Keep the copy format's existing wording/structure for numeric-rated topics unchanged (only the order source, the gating, and the unrated handling change). When removing the old hardcoded section-order array, do not leave a tombstone comment naming the removed identifier (keep comments clean). Extend tests/47-severity-unrated.test.js with clipboard-builder cases: (a) an unrated topic copies as its name only — assert the copied text contains no rating line for the unrated side and no NaN change line; (b) a mutated saved order changes the copied section order to match flattenOrderKeys(order); (c) severity sub-option unchecked (or switch off) → the copy carries topic names but zero rating lines.
  </action>
  <verify>
    <automated>node tests/47-severity-unrated.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-unrated.test.js` exits 0 (now covering the clipboard builder too).
    - buildSessionMarkdown calls App.getSectionOrder (grep-assert) and no hardcoded section-order array remains in it.
    - For an unrated topic, the copied output contains the topic name, no rating line for the unrated side, and no "NaN" anywhere (assert on the built string).
    - For a mutated saved order, the copied section sequence equals flattenOrderKeys(order) for the selected/non-empty keys.
    - With the severity sub-option unchecked or App.isSectionEnabled('afterSeverity') false, the copy contains zero before/after/change lines while topic names remain.
  </acceptance_criteria>
  <done>The clipboard-copy builder reads the same saved-order source, honors the same topics/sub-option/switch gating as the filtered builder, and an unrated topic copies as its name only — no unrated rating line and no NaN change line can reach the clipboard. The extended unrated test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data → clipboard document | Issue ratings (numeric or unrated/null) and section values flow into the copied markdown. |
| saved order → export order | App.getSectionOrder() drives clipboard emission order + PDF severity placement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06b | Tampering/Information Disclosure | An unrated (null) rating string-concatenated into a copied rating line, or a NaN arithmetic artifact in the change line | medium | mitigate | The clipboard copy replaces null-checks with strict typeof === "number" checks so no unrated line and no NaN change line can be emitted (Task 2). |
| T-47-07b | Tampering | Divergent order sources (the 260615 class) reintroducing copy≠form order | high | mitigate | The clipboard builder reads the single App.getSectionOrder() source (the same page-pinned snapshot the form + filtered builder use); the extended test asserts the copied order against a mutated saved order (Task 2). |
| T-47-08b | Tampering | PDF severity block landing at the wrong slot after a manual Step-2 heading edit | low | mitigate | severityAfterSections is derived from the headings actually present in the edited Step-2 text (Task 1); the drawSeverityBlock render loop is untouched. |

**No package installs this phase (zero-npm/zero-build vanilla JS); no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/47-severity-position.test.js && node tests/47-severity-unrated.test.js` both exit 0; `npm test` full suite green (esp. severity PDF + export tests).
- Manual (real-device gate before /gsd-verify-work): export a session with severity moved to a non-default slot → PDF severity block appears at that slot; delete a preceding section heading in Step 2 → block moves up one slot; Hebrew RTL PDF still correct; clipboard copy of a session with an unrated topic verified clean (name only, no NaN); a mutated saved order reorders the copied sections.
- Comment hygiene (CONVENTIONS.md §Comments): no planning IDs (D-NN, G-NN, ORDR-NN, R2-N) written into export-modal.js / pdf-export.js comments.
</verification>

<success_criteria>
The clipboard-copy builder reads the saved order from the same page-pinned source as the filtered builder and form (order matches against a mutated order), honors the same topics/sub-option/switch gating, and an unrated topic copies as its name only (no unrated line, no NaN). The PDF severity block lands at the edit-aware saved-order slot with the Phase-23 loop untouched. Both tests pass; existing severity/export suites green.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-09-SUMMARY.md` when done. Record the deriveSeverityAfterSections signature (with the presentKeys edit-aware derivation) and the buildSessionMarkdown gating hooks so the docs pass (47-08) describes the shipped behavior.
</output>
</content>
</invoke>
