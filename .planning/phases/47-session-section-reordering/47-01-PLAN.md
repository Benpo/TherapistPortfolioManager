---
phase: 47-session-section-reordering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/db.js
  - assets/app.js
  - tests/47-order-sentinel.test.js
  - tests/47-order-sanitize.test.js
  - tests/47-severity-marker.test.js
autonomous: true
requirements: [ORDR-05, ORDR-07]
must_haves:
  truths:
    - "A per-therapist section order persists in the therapistSettings store as a sentinel row keyed sectionOrder (D-05 group-as-data shape)."
    - "Form and export code can read the saved order synchronously via App.getSectionOrder() after page load (D-16 snapshot)."
    - "An illegal order (afterSeverity before Session topics) is silently corrected by one shared validator, unknown/garbage keys are dropped, and any missing KNOWN section/group is appended at its D-02 default slot so no enabled section is silently hidden (Interaction 11 / V5 / G-7)."
    - "A page-pinned order snapshot (App.pinSectionOrder) freezes the order a form opened with, so the form and its same-page exports never diverge even if a peer tab changes the order mid-edit (D-16 / G-5)."
    - "One groupId→default-title-key mapping (App.GROUP_DEFAULT_TITLE_KEYS: emotionsTech→session.accordion.emotions, wrapup→session.group.wrapup) is the single source the Settings list, ✎-revert, and form headers consume (G-4)."
    - "The severity scale exposes an 11th '— (skip)' value that is a distinct third state from unselected."
  artifacts:
    - "assets/db.js — sectionOrder registered in _SENTINEL_KEYS; _writeTherapistSentinel carries an items/version payload for it; a reader returns the row."
    - "assets/app.js — DEFAULT_SECTION_ORDER (D-02), App.GROUP_DEFAULT_TITLE_KEYS (groupId→default title key, G-4), App.getSectionOrder() sync cache + App.pinSectionOrder() page-pinned snapshot (G-5), App.sanitizeOrder() (clamp + allowlist + append-missing, G-7), App.deriveSeverityAfterSections not here (owned by 47-05), App.SEV_SKIP marker + extended createSeverityScale/getSeverityValue."
    - "tests/47-order-sentinel.test.js, tests/47-order-sanitize.test.js, tests/47-severity-marker.test.js"
  key_links:
    - "db.js#_SENTINEL_KEYS must stay in lock-step with backup.js#ALLOWED_SENTINEL_KEYS (47-06) or restore silently drops the order."
    - "App.getSectionOrder cache must invalidate on the same 'sessions-garden-settings' BroadcastChannel 'therapist-settings-changed' message that already refreshes _sectionLabelCache, else a peer tab renders stale order."
    - "App.sanitizeOrder is the single clamp+allowlist+append consulted by Settings drag/arrows (47-03), saved-order load (here), and backup restore (47-06)."
    - "App.pinSectionOrder() captures the page snapshot at form open (47-04); getSectionOrder() then returns that pin so the form (47-04) and export (47-05) on add-session.html read one identical order for the page's lifetime (G-5)."
    - "App.GROUP_DEFAULT_TITLE_KEYS is the one group-title mapping consumed by 47-03 (Settings headers + ✎-revert) and 47-04 (form headers) — no consumer falls back to session.form.comments.title (G-4)."
---

<objective>
Build the shared data foundation the whole phase composes on: (1) a per-therapist section-order sentinel in IndexedDB modeling groups as DATA (D-05), (2) a synchronous App.getSectionOrder() cache mirroring the shipped _sectionLabelCache pattern, (3) ONE shared App.sanitizeOrder() move-validator that enforces the "Issue severity never precedes Session topics" clamp (Interaction 11) and drops unknown keys, and (4) the 11th "— (skip)" severity value (D-09) as a distinct third state on the shared severity widget.

Purpose: Every downstream consumer (Settings reorder UI, session form, both export builders, backup restore) reads order from ONE cache and validates through ONE function. Divergent sources are exactly the 260615 bug class this phase must not reintroduce.

Output: db.js sentinel path + reader; app.js default order, order cache, validator, and severity-skip marker; three behavior tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from drift/source-grounding checks):
- `sectionOrder` — sentinel sectionKey string (db.js _SENTINEL_KEYS member; backup.js ALLOWED_SENTINEL_KEYS member in 47-06)
- `PortfolioDB.getSectionOrderRecord` (or equivalently-named reader) — returns the raw sentinel row or null
- `App.DEFAULT_SECTION_ORDER` — the D-02 default items[] structure
- `App.getSectionOrder()` — sync reader returning a defensive copy (plain array) of the resolved order
- `App.flattenOrderKeys(order)` — free function returning the ordered section-key list, groups flattened (the ONLY flattening API — no `.flatKeys()` method is attached; consumers call `App.flattenOrderKeys(App.getSectionOrder())`)
- `App.pinSectionOrder()` — captures a page-scoped order snapshot; getSectionOrder returns it once pinned (G-5)
- `App.GROUP_DEFAULT_TITLE_KEYS` — groupId→default-title i18n key map (emotionsTech→session.accordion.emotions, wrapup→session.group.wrapup) (G-4)
- `App.sanitizeOrder(items)` — pure validator/clamp (drops unknown keys, clamps Interaction 11, appends missing knowns at default slots, G-7)
- `App.refreshSectionOrderCache()` — async reload used by the BroadcastChannel handler (does not touch the pin)
- `App.SEV_SKIP` — the "— (skip)" marker constant (string value `"skip"`)
- `afterSeverity` — new orderable section key (introduced in DEFAULT_SECTION_ORDER here; consumed by 47-03/04/05/06/07)
Other phase-wide new section keys/artifacts are produced by sibling plans (47-03..47-08).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order sentinel persistence in db.js (register + write payload + reader)</name>
  <files>assets/db.js, tests/47-order-sentinel.test.js</files>
  <read_first>
    - assets/db.js L910-999 (the sentinel region: _SENTINEL_KEYS, _writeTherapistSentinel, setTherapistSetting, getAllTherapistSettings) — the exact pattern and the lock-step comment
    - tests/25-10-snippets-sentinel-roundtrip.test.js — the sentinel test model to copy structure from
    - tests/_helpers/mock-portfolio-db.js — the IDB mock harness used by sentinel tests
  </read_first>
  <behavior>
    - Writing a sectionOrder record then reading it back returns the same items[] and version.
    - _writeTherapistSentinel rejects a sectionKey not in _SENTINEL_KEYS (unchanged guard).
    - A sectionOrder record with a non-array items is coerced to an empty items[] on write (type-guard, mirrors the deletedIds array coercion at db.js L955-956).
    - getAllTherapistSettings still returns the sectionOrder row alongside section rows; section-row walkers can filter it by sectionKey === "sectionOrder".
  </behavior>
  <action>
    Add the string "sectionOrder" to `_SENTINEL_KEYS` (db.js ~L920), keeping the existing DELETED_SEEDS_KEY member. Generalize `_writeTherapistSentinel` so it persists the payload shape each sentinel needs WITHOUT losing the existing snippetsDeletedSeeds behavior: for "sectionOrder" it must `store.put` a row of shape `{ sectionKey:"sectionOrder", version:<number>, items:<array> }`; for the existing DELETED_SEEDS_KEY it must keep writing `{ sectionKey, deletedIds }` exactly as today. Type-guard on write: coerce a non-array `items` to `[]` and a non-number `version` to `1`, using the same defensive discipline as the existing `Array.isArray(record.deletedIds) ? … : []` line. Add a dedicated reader `getSectionOrderRecord()` that returns the sectionOrder row (or null if absent) — read it from the existing `getAllTherapistSettings()` result filtered by `sectionKey === "sectionOrder"` so no new store/transaction is introduced. Do NOT route the order through `setTherapistSetting` (it coerces to {customLabel, enabled} and destroys sentinel semantics — the reason the dedicated path exists, per the L947 header comment). Keep the L917-919 lock-step comment accurate: note the twin registration in backup.js#ALLOWED_SENTINEL_KEYS is added in plan 47-06. Create tests/47-order-sentinel.test.js modeled on 25-10-snippets-sentinel-roundtrip.test.js asserting the write→read round-trip, the sectionKey guard, and the non-array items coercion.
  </action>
  <verify>
    <automated>node tests/47-order-sentinel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-order-sentinel.test.js` exits 0.
    - `grep -c 'sectionOrder' assets/db.js` returns ≥ 1 and the string appears inside the `_SENTINEL_KEYS` Set literal.
    - Writing `{sectionKey:"sectionOrder", version:1, items:[{type:"section",key:"issues"}]}` then reading via getSectionOrderRecord returns items length 1 with key "issues".
    - Writing a record with `items: "notArray"` reads back `items: []` (not the string).
    - `setTherapistSetting` is unchanged (still writes {sectionKey, customLabel, enabled}).
  </acceptance_criteria>
  <done>The sectionOrder sentinel writes and reads through the dedicated path with type-guarded items[]/version, the DELETED_SEEDS_KEY path is byte-for-byte unchanged, and the round-trip test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default order (D-02) + getSectionOrder cache + sanitizeOrder validator in app.js</name>
  <files>assets/app.js, tests/47-order-sanitize.test.js</files>
  <read_first>
    - assets/app.js L36-115 (_sectionLabelCache/_snippetCache, getSectionLabel, isSectionEnabled, getSnippets — the eager-load + sync-read + defensive-copy pattern to mirror)
    - assets/app.js L840-875 (initCommon eager-load of snippets + the BroadcastChannel "sessions-garden-settings" handler that rebuilds _sectionLabelCache on "therapist-settings-changed")
    - assets/settings.js L35-49 (LOCKED_RENAME + the canonical 9-row SECTION_DEFS keys — the exact section keys the default order must reference)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pattern 1 + Pattern 2 (the proposed sentinel shape and sanitizeOrder — [ASSUMED], confirm shape here) and Open Question 1 (D-07 vs Interaction 11 reconciliation: Interaction 11 is authoritative)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Interaction 11 (the clamp is enforced by affordance, sanitized on load AND restore)
  </read_first>
  <behavior>
    - sanitizeOrder returns items unchanged when afterSeverity already sits after issues (Session topics).
    - sanitizeOrder moves afterSeverity to immediately after issues when it precedes issues.
    - sanitizeOrder drops top-level items and group members whose key is not a known section key, and drops empty groups (all members dropped).
    - sanitizeOrder given null/non-array/malformed input returns a deep copy of DEFAULT_SECTION_ORDER.
    - sanitizeOrder appends any KNOWN section/group missing from a partial input at its DEFAULT_SECTION_ORDER default slot, so no enabled section is silently hidden (G-7).
    - getSectionOrder() returns DEFAULT_SECTION_ORDER (sanitized) when no sentinel is cached, and a defensive copy (mutating the result never corrupts the cache).
    - pinSectionOrder() snapshots the current resolved order; after pinning, getSectionOrder() returns that snapshot even if refreshSectionOrderCache later runs (page-pinned invariant, D-16 / G-5).
    - flattenOrderKeys(order) yields the section keys in top-level-then-member order, groups flattened, group names absent (D-03).
  </behavior>
  <action>
    Declare `const DEFAULT_SECTION_ORDER` as the D-02 structure: item 1 `{type:"section", key:"issues"}` (Session topics, carries the before-rating); item 2 `{type:"group", id:"emotionsTech", titleOverride:null, members:["heartShield","heartShieldEmotions","trapped","insights","limitingBeliefs","additionalTech"]}` — members in Ben's D-02 order (Heart-Wall toggle, Emotions in the Heart-Wall, Trapped Emotions Released, Physical Imbalance, Limiting Beliefs, Additional Techniques and Tools; map those to the existing keys heartShield, heartShieldEmotions, trapped, insights, limitingBeliefs, additionalTech respectively); item 3 `{type:"section", key:"afterSeverity"}` (NEW freely-orderable end-of-session severity item — a NEW key, distinct from "issues" per RESEARCH A2); item 4 `{type:"group", id:"wrapup", titleOverride:null, members:["comments","nextSession"]}`. Add a module-level `const GROUP_DEFAULT_TITLE_KEYS = { emotionsTech: "session.accordion.emotions", wrapup: "session.group.wrapup" }` mapping each group id to its default title i18n key (emotionsTech reuses the existing `session.accordion.emotions`; wrapup uses the NEW `session.group.wrapup` from 47-02) — consumed by the Settings group-header render + ✎-revert (47-03) and the form group headers (47-04) whenever `titleOverride` is null; no consumer falls back to session.form.comments.title (G-4). Add a module-level `const KNOWN_SECTION_KEYS` set = the 9 SECTION_DEFS keys plus "afterSeverity". Add `let _sectionOrderCache = null;` as a sibling of `_sectionLabelCache`. Implement pure `sanitizeOrder(items)`: (a) if input is not a non-empty array, return a deep copy of DEFAULT_SECTION_ORDER; (b) filter every item — sections dropped if key not in KNOWN_SECTION_KEYS; groups keep only members in KNOWN_SECTION_KEYS and are dropped entirely if left with zero members; unknown item shapes dropped; (c) APPEND any KNOWN section/group missing from the filtered result at its DEFAULT_SECTION_ORDER slot — walk DEFAULT_SECTION_ORDER in order and, for any top-level section key or group id absent from the candidate (and, within a surviving group, any default member key absent from it), insert it at the position it holds in the default — so a partial / crafted / future-version order can never silently hide an enabled section from the form or export (G-7); (d) enforce the Interaction-11 clamp: find the top-level "issues" section index and the top-level "afterSeverity" section index in the FLATTENED-top-level ordering — if afterSeverity precedes issues, splice afterSeverity out and reinsert it immediately after issues; (e) return the sanitized array. Add `let _pinnedSectionOrder = null;` (page-scoped snapshot). Implement `pinSectionOrder()` that sets `_pinnedSectionOrder = sanitizeOrder(_sectionOrderCache || DEFAULT_SECTION_ORDER)` — the page-pinned snapshot a form open captures so the form and its same-page exports never diverge mid-edit (D-16 / G-5). Implement `getSectionOrder()` returning a deep copy of `_pinnedSectionOrder` when a pin is set, otherwise a deep copy of `sanitizeOrder(_sectionOrderCache || DEFAULT_SECTION_ORDER)` (sync, never awaits — mirrors getSnippets returning a copy). Implement `flattenOrderKeys(order)` as a FREE FUNCTION returning the ordered section-key list with groups flattened and group identities absent — this is the only flattening API; do NOT attach a `.flatKeys()` method to the returned arrays (getSectionOrder returns a plain array; consumers call `App.flattenOrderKeys(App.getSectionOrder())` — the convention 47-04/47-05 are written against). Implement async `refreshSectionOrderCache()` that reads `PortfolioDB.getSectionOrderRecord()` and sets `_sectionOrderCache = record ? record.items : null`. Call it in `initCommon` right beside the existing eager snippet load (app.js ~L842) so the order is synchronously readable before first render (D-16 snapshot). In the existing BroadcastChannel "therapist-settings-changed" handler (app.js ~L858-864), ALSO await refreshSectionOrderCache() so one message invalidates both caches; keep the existing _sectionLabelCache rebuild. The handler refreshes `_sectionOrderCache` ONLY — it MUST NOT touch `_pinnedSectionOrder`, so a page that already pinned (an open form) keeps its snapshot even when a peer tab changes the order (G-5). Export getSectionOrder, pinSectionOrder, sanitizeOrder, flattenOrderKeys, refreshSectionOrderCache, DEFAULT_SECTION_ORDER, KNOWN_SECTION_KEYS, GROUP_DEFAULT_TITLE_KEYS on the window.App object. Create tests/47-order-sanitize.test.js (pure-fn, load app.js via the existing jsdom/app-stub harness or require the exported fn) asserting the behaviors above, including a MUTATED order where afterSeverity precedes issues and gets clamped AND a PARTIAL order (missing afterSeverity and a whole group) where sanitizeOrder appends the missing knowns at their default slots (G-7).
  </action>
  <verify>
    <automated>node tests/47-order-sanitize.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-order-sanitize.test.js` exits 0.
    - sanitizeOrder([{type:"section",key:"afterSeverity"},{type:"section",key:"issues"}]) returns issues before afterSeverity.
    - sanitizeOrder([{type:"section",key:"__evil__"},{type:"section",key:"issues"}]) drops the unknown key.
    - sanitizeOrder({}) and sanitizeOrder(null) each return an array deep-equal to DEFAULT_SECTION_ORDER.
    - sanitizeOrder([{type:"section",key:"issues"}]) returns an order that still contains afterSeverity and both the emotionsTech and wrapup groups (missing knowns appended at their default slots, G-7).
    - Mutating the array returned by getSectionOrder() does not change a subsequent getSectionOrder() result.
    - After App.pinSectionOrder(), calling App.refreshSectionOrderCache() with a different stored record leaves App.getSectionOrder() equal to the pinned snapshot (G-5).
    - `grep -n 'afterSeverity' assets/app.js` shows it inside DEFAULT_SECTION_ORDER and KNOWN_SECTION_KEYS.
    - The BroadcastChannel "therapist-settings-changed" handler awaits both the label-cache rebuild and refreshSectionOrderCache, and does not overwrite the pin.
  </acceptance_criteria>
  <done>App.getSectionOrder() reads a sanitized D-02-default-or-saved order synchronously, App.sanitizeOrder() is the single clamp+allowlist function (clamps afterSeverity-after-issues, drops unknown keys, appends missing knowns at default slots, defaults on garbage), App.pinSectionOrder() freezes a page snapshot for form/export consistency, App.GROUP_DEFAULT_TITLE_KEYS maps group ids to default title keys, the cache eager-loads in initCommon and invalidates cross-tab, and the sanitize test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The "— (skip)" severity value on the shared severity widget</name>
  <files>assets/app.js, tests/47-severity-marker.test.js</files>
  <read_first>
    - assets/app.js L1205-1244 (createSeverityScale + getSeverityValue — the exact widget to extend; note dataset.value === "" means unselected)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Interaction 8 + 8a + the Color section (the — pill is language-free, neutral grey via --color-sev-default-border, FIRST in the scale immediately before 0, and is the FIRST Tab stop)
    - .planning/phases/47-session-section-reordering/47-RESEARCH.md Pattern 4 + Pitfall 3 (— must be a THIRD state distinct from null; readers branch on it)
  </read_first>
  <behavior>
    - createSeverityScale renders 12 buttons total: the "—" pill FIRST, then 0 through 10, in that DOM/Tab order.
    - Selecting the "—" pill sets dataset.value to the skip marker and marks it active.
    - getSeverityValue returns App.SEV_SKIP for a skip selection, a Number 0-10 for a numeric selection, and null for unselected.
    - Passing App.SEV_SKIP as the initialValue to createSeverityScale pre-selects the "—" pill.
    - An unknown/garbage dataset.value coerces to null (unselected), never a phantom number.
  </behavior>
  <action>
    Add `const SEV_SKIP = "skip";` and export it as `App.SEV_SKIP`. In `createSeverityScale(initialValue, onChange)`: before the 0-10 loop, prepend a skip button (`type="button"`, class `severity-button` plus a modifier class `severity-skip` for styling, textContent the en-dash "—", and NO --sev-color so it stays outside the multi-hue ramp — 47-03 styles it neutral grey). Clicking it sets `wrap.dataset.value = SEV_SKIP`, clears is-active on siblings, adds is-active to itself, and calls onChange with SEV_SKIP. Keep the existing 0-10 buttons and their behavior unchanged. Initialize: if `String(initialValue) === SEV_SKIP` mark the skip pill active and set dataset.value = SEV_SKIP; otherwise keep the existing numeric init. In `getSeverityValue(wrapper)`: return null for "" / undefined (unchanged); return SEV_SKIP when `wrapper.dataset.value === SEV_SKIP`; otherwise `Number.parseInt` and if the result is NaN return null (defensive — an unknown marker must not become a phantom number, Pitfall 3). Do NOT change any reader in add-session/export here — those branch on SEV_SKIP in plans 47-07 and 47-05. Create tests/47-severity-marker.test.js asserting the five behaviors (button count/order, skip selection value, numeric value, initial skip pre-select, garbage → null).
  </action>
  <verify>
    <automated>node tests/47-severity-marker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-severity-marker.test.js` exits 0.
    - A freshly created scale has exactly 12 .severity-button elements and the first is the "—" pill.
    - getSeverityValue on a scale with the skip pill selected === App.SEV_SKIP ("skip").
    - getSeverityValue on a scale with "7" selected === 7 (Number).
    - getSeverityValue on a scale with dataset.value forced to "banana" === null.
    - `grep -c 'SEV_SKIP' assets/app.js` ≥ 3.
  </acceptance_criteria>
  <done>The shared severity widget carries a distinct third "— (skip)" state (first in DOM/Tab order, neutral, language-free), getSeverityValue returns SEV_SKIP for it and null for garbage, and the marker test passes. Downstream readers are repointed in 47-05 (export omission) and 47-07 (form validation/auto-hide).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB record → in-memory cache | A sectionOrder row (possibly from a crafted/old backup, written by 47-06) is read into _sectionOrderCache and drives form/export rendering. |
| Severity widget dataset → reader | dataset.value is DOM-mutable; a smuggled non-numeric value must not become a phantom severity number. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering/DoS | Order sentinel with non-array `items` or wrong types (db.js write, app.js read) | medium | mitigate | Type-guard on write (non-array items → []; non-number version → 1, Task 1) AND on read (sanitizeOrder returns DEFAULT_SECTION_ORDER on malformed input, Task 2). |
| T-47-02 | Tampering | Illegal order (afterSeverity before issues) or unknown section keys reaching the form/export | high | mitigate | Single shared App.sanitizeOrder clamp + KNOWN_SECTION_KEYS allowlist (Task 2); consulted by load here and by drag/arrows (47-03) and backup restore (47-06). |
| T-47-03 | Tampering | "—" skip marker smuggled into a numeric severity field | low | mitigate | getSeverityValue returns null for any unknown dataset.value (Task 3); only the exact SEV_SKIP constant yields the skip state. |

**No package installs this phase (zero-npm/zero-build vanilla JS — RESEARCH Package Legitimacy Audit: N/A); no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/47-order-sentinel.test.js && node tests/47-order-sanitize.test.js && node tests/47-severity-marker.test.js` all exit 0.
- `npm test` full suite stays green (no regression in existing db/app/severity tests, especially 25-10-snippets-sentinel-roundtrip, 34-severity-bars, 34-severity-unmeasured).
- Comment hygiene (CONVENTIONS.md §Comments): no planning IDs (D-NN, ORDR-NN, task IDs) written into assets/db.js or assets/app.js comments.
</verification>

<success_criteria>
The order sentinel persists and round-trips through db.js; App.getSectionOrder() is a synchronous sanitized read backed by a cross-tab-invalidated cache; App.sanitizeOrder() is the one clamp+allowlist; the severity widget has a distinct "— (skip)" third state. All three behavior tests pass and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-01-SUMMARY.md` when done. Record the FINAL confirmed sentinel shape and the exact exported App.* symbol names (getSectionOrder, pinSectionOrder, sanitizeOrder, flattenOrderKeys, SEV_SKIP, DEFAULT_SECTION_ORDER, GROUP_DEFAULT_TITLE_KEYS) and the groupId→default-title-key mapping so 47-03/04/05/06/07 consume the real names.
</output>
