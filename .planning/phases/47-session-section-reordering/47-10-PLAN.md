---
phase: 47-session-section-reordering
plan: 10
type: execute
wave: 2
depends_on: []
files_modified:
  - assets/sessions.js
  - assets/overview.js
  - tests/47-view-unrated.test.js
autonomous: true
requirements: [ORDR-07]
must_haves:
  truths:
    - "In the Sessions History table issues cell and the client-overview expanded-session row, a fully-unrated topic (both before AND after null/undefined) renders its NAME ONLY — no '(- -> -)' / '(-→-)' rating suffix (D-23)."
    - "A partially-rated topic (exactly one of before/after is numeric) renders exactly as today, with '-' standing in for the unrated side; a fully-rated topic is unchanged."
    - "The client-overview session averages loop (overview.js ~L1063-1075) is UNCHANGED — it already excludes null before/after from beforeCount/afterCount (D-23: averages already correct)."
  artifacts:
    - "assets/sessions.js — the issues-cell render (~L246-258) emits the topic name only for a fully-unrated topic; the numeric/partial path keeps today's '(before -> after)' suffix."
    - "assets/overview.js — the expanded-row issues string (~L825-832) emits the topic name only for a fully-unrated topic; the averages loop (~L1063-1075) untouched."
    - "tests/47-view-unrated.test.js — jsdom fixtures (unrated / partial / full topics) asserting the suffix-presence rules on both surfaces + the averages-unchanged guard."
  key_links:
    - "Both surfaces share the 'name (before -> after)' convention and cross-reference each other in comments (sessions.js ~L250, overview.js ~L821-824) — the name-only rule must apply IDENTICALLY to both so the two pages agree."
    - "Pure display logic on existing session records — no dependency on the order sentinel (47-01) or i18n keys (47-02); files (sessions.js, overview.js) are disjoint from every other Phase-47 plan."
---

<objective>
Close D-23: hide the empty severity suffix in the two read-only session views. Today both the Sessions History issues cell (sessions.js ~L246-258) and the client-overview expanded-session row (overview.js ~L825-832) render every topic as `name (before -> after)`, substituting `-` for a null side — so a fully-unrated topic reads `Topic (- -> -)`, visual noise Ben flagged. After this plan a fully-unrated topic renders its name only; a partially-rated topic (one numeric side) still shows the suffix with `-` for the unrated side (one real number is worth showing); a fully-rated topic is unchanged. The client-overview averages already exclude null and stay untouched.

Purpose: ORDR-07 (unrated omitted from every output, including the History / overview views — D-23). This is the display-side complement to the export omission (47-05/47-09) and the form auto-hide (47-07); it operates purely on existing stored data with no new dependencies.

Output: name-only rendering for fully-unrated topics in sessions.js and overview.js; a jsdom behavior test covering both surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
</context>

<artifacts_this_phase_produces>
New symbols/artifacts created by THIS plan (exclude from drift checks):
- The fully-unrated → name-only branch in the sessions.js issues-cell render (~L246-258)
- The fully-unrated → name-only branch in the overview.js expanded-row issues map (~L825-832)
- tests/47-view-unrated.test.js
- Note: session records, issue.name/before/after shapes, and the overview averages loop are EXISTING — this plan only changes how a fully-unrated topic is displayed.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sessions History issues cell — name-only for a fully-unrated topic</name>
  <files>assets/sessions.js, tests/47-view-unrated.test.js</files>
  <read_first>
    - assets/sessions.js L241-258 (the issues-cell render: the per-issue loop building `${issueName} (${before} -> ${after})` with `-` substituted for a null/undefined before/after)
    - tests/31-sessions-render-hardening.test.js — the jsdom harness model for a sessions-render test (how it loads sessions.js, builds a session fixture, and asserts on the rendered cell)
    - .planning/phases/47-session-section-reordering/47-CONTEXT.md D-23 (fully-unrated topic renders NAME ONLY; partially-rated renders as today; averages unchanged)
  </read_first>
  <behavior>
    - A topic whose before AND after are both null/undefined renders its name only — the issue-line textContent is exactly the topic name, with no `(` … `)` rating suffix.
    - A topic with a numeric before OR after (partial) renders as today: `name (before -> after)` with `-` in place of the unrated side.
    - A fully-rated topic renders unchanged.
    - The empty-issues fallback (`-` when a session has no issues) is unchanged.
    - Rendering stays textContent-only (no innerHTML — the shipped render-hardening lock).
  </behavior>
  <action>
    In the per-issue loop (sessions.js ~L246-253), branch on whether the topic is fully unrated: if BOTH `issue.before` and `issue.after` are null or undefined, set `issueLine.textContent` to the topic name only (`issue.name || "-"`) — no rating suffix. Otherwise keep TODAY's exact behavior: compute `before`/`after` with the existing `!== null && !== undefined ? value : "-"` substitution and set `issueLine.textContent = \`${issueName} (${before} -> ${after})\``. Do not change the empty-issues branch (`issuesCell.textContent = "-"`) or the `.issue-list` / `.issue-line` structure, and keep it textContent-only (no innerHTML). Create tests/47-view-unrated.test.js (jsdom, modeled on 31-sessions-render-hardening.test.js) with a session fixture carrying three topics — fully-unrated (before:null, after:null), partial (before:5, after:null), fully-rated (before:8, after:3) — render the issues cell and assert: the unrated line's text equals the topic name with no "(" character; the partial line contains "(5 -> -)"; the full line contains "(8 -> 3)".
  </action>
  <verify>
    <automated>node tests/47-view-unrated.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-view-unrated.test.js` exits 0.
    - A fully-unrated topic's rendered issue line contains no "(" character and equals the topic name.
    - A partially-rated topic renders "(5 -> -)"; a fully-rated topic renders "(8 -> 3)".
    - The render path uses textContent only (grep-assert no new innerHTML on the issue line).
  </acceptance_criteria>
  <done>The Sessions History issues cell hides the empty rating suffix — a fully-unrated topic shows its name only, while partial and full topics keep today's suffix. The sessions half of the view test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client-overview expanded row — name-only for a fully-unrated topic; averages untouched</name>
  <files>assets/overview.js, tests/47-view-unrated.test.js</files>
  <read_first>
    - assets/overview.js L818-842 (the expanded-session issues string: the `.map` building `${name} (${before}→${after})` joined with ", ") and L1058-1077 (the averages loop that already excludes null before/after — MUST stay untouched)
    - tests/31-overview-render-hardening.test.js — the jsdom harness model for an overview-render test
    - .planning/phases/47-session-section-reordering/47-CONTEXT.md D-23 (name-only for fully-unrated; overview averages already exclude unrated — leave untouched)
  </read_first>
  <behavior>
    - In the expanded-session row, a topic whose before AND after are both null/undefined maps to its name only (no `(` … `)` suffix); the join with ", " is otherwise unchanged.
    - A partially-rated or fully-rated topic maps as today (`name (before→after)` with `-` for a null side).
    - The `overview.sessions.none` fallback (no issues) is unchanged.
    - The averages loop (beforeSum/beforeCount/afterSum/afterCount) is UNCHANGED — it already skips null before/after, so unrated topics never affect the averages.
    - Rendering stays textContent-only (the 31-overview-render-hardening lock).
  </behavior>
  <action>
    In the expanded-row issues `.map` (overview.js ~L825-832), branch the same way as Task 1: if BOTH `issue.before` and `issue.after` are null or undefined, return the topic name only (`issue.name || "-"`); otherwise keep today's `${name} (${before}→${after})` with the existing `-` substitution. Leave the `.join(", ")`, the `overview.sessions.none` fallback, and the textContent-only assignment unchanged. Do NOT touch the averages loop (~L1063-1075) — it already excludes null before/after and must keep doing so (a fully-unrated topic still counts toward issueCount but contributes to no average, exactly as today). Extend tests/47-view-unrated.test.js with an overview fixture (same three-topic shape) asserting: the joined issues string omits any "(-→-)" for the fully-unrated topic (its entry is the name with no "(" ), the partial topic renders "(5→-)", the full topic renders "(8→3)", AND (averages guard) a fixture with one fully-unrated and one fully-rated topic yields the same average-before/average-after as the fully-rated topic alone (unrated excluded).
  </action>
  <verify>
    <automated>node tests/47-view-unrated.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-view-unrated.test.js` exits 0 (now covering the overview surface too).
    - In the overview expanded row, the fully-unrated topic's entry is its name with no "(" character; the partial entry is "(5→-)"; the full entry is "(8→3)".
    - The averages guard passes: a fixture with a fully-unrated + a fully-rated topic reports the fully-rated topic's own before/after as the averages (unrated excluded, loop unchanged).
    - The averages loop code is unchanged (grep-assert the beforeCount/afterCount null-guards are intact).
  </acceptance_criteria>
  <done>The client-overview expanded row hides the empty rating suffix for a fully-unrated topic while keeping partial/full suffixes and leaving the (already-null-excluding) averages untouched. The overview half of the view test passes and both surfaces agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session record → read-only view | Stored issue.before/after (numeric or null) drive a textContent-only string in the History table and the overview row. No user input crosses here; no persistence, no new render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-14 | Information Disclosure | A display change accidentally hiding a topic that DOES carry a rating (data loss to the eye) | low | mitigate | The name-only branch fires ONLY when BOTH before AND after are null/undefined (Task 1/2); a partially-rated topic keeps its suffix — the test asserts the partial case renders its numeric side. |
| T-47-15 | Tampering (XSS) | Topic name rendered into the view | low | accept | Rendering stays textContent-only per the shipped render-hardening lock (31-sessions/31-overview) — no new innerHTML introduced. |

**No package installs this phase (zero-npm/zero-build vanilla JS); no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/47-view-unrated.test.js` passes; `npm test` full suite green (esp. 31-sessions-render-hardening, 31-overview-render-hardening — the render path stays textContent-only).
- Manual (light real-device check before /gsd-verify-work): a session with a fully-unrated topic shows the topic name with no "(- -> -)" in both the History table and the client-overview expanded row; a partially-rated topic still shows its numeric side.
- Comment hygiene (CONVENTIONS.md §Comments): no planning IDs (D-NN, G-NN, ORDR-NN) written into sessions.js / overview.js comments.
</verification>

<success_criteria>
A fully-unrated topic renders its name only in both the Sessions History issues cell and the client-overview expanded row; partially-rated and fully-rated topics are unchanged; the overview averages (already null-excluding) are untouched. The two-surface view test passes and the render-hardening suite stays green.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-10-SUMMARY.md` when done. Confirm the two render sites changed (sessions.js issues cell, overview.js expanded row) and that the averages loop was left untouched.
</output>
