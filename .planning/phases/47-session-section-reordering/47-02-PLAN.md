---
phase: 47-session-section-reordering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements: [ORDR-06, ORDR-07, ORDR-08]
user_setup: []
must_haves:
  truths:
    - "Every new UI string the reorder/severity surfaces reference exists in all four locale dictionaries (EN/DE/HE/CS), so no consumer renders a raw i18n key."
    - "The Hebrew export label for Session topics now matches the in-session section title (the D-14 HE mismatch is fixed at the string layer)."
    - "The wrapup group header has an i18n home (session.group.wrapup = 'Session Wrap-up') and the Emotions & Techniques group reuses session.accordion.emotions; no group header falls back to session.form.comments.title (G-4)."
    - "The Session-topics section title reads 'Session topics' in EN/DE/CS (HE already correct), propagated to the Settings row title, form heading, and export topics checkbox by key reuse (G-15); the topics-row Settings description names no severity (G-6)."
  artifacts:
    - "assets/i18n-en.js — new keys for reorder aria-labels, Reset order / Reset names, Issue severity row + form header, Severity at start, the ⓘ severity explainer, the skipped-rating hint, the export topics/severity-split copy, session.group.wrapup (G-4), the severity-free settings.row.issues.description rewrite (G-6), and the session.form.issuesHeading rename to 'Session topics' (G-15)."
    - "assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js — same keys, translated; HE keeps דרגת חומרה terminology."
  key_links:
    - "Key names defined here are the contract the Wave-2 feature plans (47-03 settings, 47-04 form, 47-05 export, 47-07 severity) call via App.t(); they must not invent parallel keys."
---

<objective>
Author the full i18n UI-string contract for Phase 47 up front, in all four locales, so the parallel Wave-2 feature plans consume stable keys instead of each editing the shared dictionaries (which would force them out of the same wave). Strings are taken verbatim from the approved UI-SPEC Copywriting Contract.

Purpose: The i18n-*.js dictionaries are shared files; concentrating all new keys in one Wave-1 plan removes the only file-conflict that would otherwise serialize the four feature plans. It also lets the docs/copy pass (47-08) reference finalized strings.

Output: New/changed keys in i18n-en.js (corpus source) plus i18n-he.js / i18n-de.js / i18n-cs.js, including the D-14 Hebrew export-label fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New i18n keys created by THIS plan (the exact final key names are the executor's choice within the app's dot-notation convention; record them in the SUMMARY). Intended keys:
- `settings.reorder.dragHandle.aria` (parameterized with the section name), `settings.reorder.moveUp.aria`, `settings.reorder.moveDown.aria`
- `settings.reset.order.label`, `settings.reset.names.label`, `settings.reset.names.confirm`
- `settings.row.afterSeverity.label` (= "Issue severity"), `settings.row.afterSeverity.info` (the ⓘ explainer text)
- `session.form.afterSeverityTitle` (REPLACE existing value → "Issue severity — end of session"), `session.form.severityAtStart` (topic-row rating label, replaces "Severity before")
- `export.section.topics` (main checkbox, named identically to the in-session section title per locale — HE mismatch fixed), `export.suboption.includeSeverity` (dependent indented sub-option)
- `session.group.wrapup` (= "Session Wrap-up"; the Emotions & Techniques group reuses the existing `session.accordion.emotions`, no new key) (G-4)
- `settings.row.issues.description` — REWRITE severity-free (remove "and their before/after severity"; UI-SPEC "severity is named once") (G-6)
- `session.form.issuesHeading` — RENAME EN value → "Session topics" (DE/CS natural equivalents; HE already correct); propagate to the Settings row title + `export.section.topics` by key reuse (G-15)
- `changelog.*` / `help.*` keys are OWNED by 47-08 (docs), NOT this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EN keys (corpus of record) + HE export-label mismatch fix</name>
  <files>assets/i18n-en.js, assets/i18n-he.js</files>
  <read_first>
    - assets/i18n-en.js — locate the existing session.form.* and settings.row.* / settings.reset.* namespaces to slot the new keys consistently (dot-notation convention)
    - assets/i18n-he.js — locate the current Session-topics section title (session.form.issuesHeading) and the current export label so the D-14 mismatch is fixed by making the export topics label equal the in-session title
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md `## Copywriting Contract` (every EN string, verbatim) + the Interaction Contract items 6/7/8/8a for the ⓘ and skip hint wording
  </read_first>
  <action>
    In i18n-en.js add the keys listed in this plan's artifacts section using the EN copy from the UI-SPEC Copywriting Contract table verbatim: the ⓘ severity explainer full sentence ("One switch for all severity ratings: when on, each topic gets a rating at the start and this end-of-session section appears; when off, both disappear — the topics themselves stay. Drag this row to choose where the end-of-session ratings appear in the form."), the Reset order / Reset names labels, the Reset names confirm ("Replace all custom names with the defaults? Names you typed will be lost."), the reorder aria-labels (Reorder {section} / Move {section} up / Move {section} down — parameterized), the "Issue severity" settings row name, the "Issue severity — end of session" form header (REPLACE the existing session.form.afterSeverityTitle value), "Severity at start" topic-row label, and the export "Include severity before/after" sub-option. Do NOT add a skipped-rating hint key — the "— (skip)" pill design is dropped (D-19); tap-again-to-clear (D-20) needs no hint string. For the export topics MAIN checkbox, the EN label must equal the in-session Session-topics section title (do not introduce a divergent "Emotions before/after" phrasing for topics — that clarified label belongs only to any legacy severity row, not the topics checkbox). In i18n-he.js, add the same keys translated (keep דרגת חומרה for severity terminology per UI-SPEC) AND fix D-14: set the Hebrew export topics-checkbox label to the SAME string as the Hebrew in-session Session-topics title so the two match. ALSO: (G-4) add `session.group.wrapup` = "Session Wrap-up" (EN) as the wrapup group header key, and note that the Emotions & Techniques group header reuses the existing `session.accordion.emotions` (no new key) — neither group header falls back to session.form.comments.title. (G-6) REWRITE `settings.row.issues.description` severity-free in EN — drop "and their before/after severity" (e.g. "The topics you addressed in this session") so the topics row names no severity (UI-SPEC: severity is named once, on the Issue severity row). (G-15) RENAME the EN `session.form.issuesHeading` value to "Session topics *" (was "Issues addressed in this session *"; the trailing " *" required marker STAYS — it is the form's required affordance, and export paths strip it via stripRequired), set `export.section.topics` (EN) to the STRIPPED value "Session topics" (no marker), and leave the Settings row rendering the key as it does today; HE already means Session topics, so its value is unchanged (R2-1). Render nothing new to the DOM here — keys only.
  </action>
  <verify>
    <automated>node -e "require('./assets/i18n-en.js'); require('./assets/i18n-he.js'); console.log('loaded')" 2>/dev/null || node --check assets/i18n-en.js && node --check assets/i18n-he.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/i18n-en.js` and `node --check assets/i18n-he.js` both succeed (no syntax error).
    - The EN dictionary contains a key whose value is the full ⓘ explainer sentence.
    - No skipped-rating hint key is added (the "— (skip)" design is dropped, D-19).
    - `session.form.afterSeverityTitle` in i18n-en.js resolves to "Issue severity — end of session".
    - The Hebrew export topics-checkbox label equals the stripRequired-normalized Hebrew session.form.issuesHeading title (D-14 mismatch fixed) — assert by comparing the stripRequired-normalized values (the form title keeps its trailing " *" marker; the export label carries none) (R2-1).
    - `session.form.issuesHeading` (EN) resolves to "Session topics *" (required marker kept) and `export.section.topics` (EN) equals its stripRequired-normalized value "Session topics" (G-15/R2-1).
    - `settings.row.issues.description` (EN) contains no severity / before / after wording (grep-assert the words "severity"/"before"/"after" are absent from that value) (G-6).
    - `session.group.wrapup` (EN) resolves to "Session Wrap-up"; no group-header key resolves to session.form.comments.title (G-4).
    - No changelog.* or help.* keys are added by this task (owned by 47-08).
  </acceptance_criteria>
  <done>EN carries every new UI string verbatim from the UI-SPEC; the Hebrew export/topics label mismatch (D-14) is corrected so the two Hebrew labels are identical; both dictionaries parse.</done>
</task>

<task type="auto">
  <name>Task 2: DE + CS translations of the same key set</name>
  <files>assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-de.js and assets/i18n-cs.js — the parallel key layout, to add the identical key set with German/Czech translations
    - assets/i18n-en.js — the finalized EN key set from Task 1 (the source of truth to mirror key-for-key)
  </read_first>
  <action>
    Add the exact same key set introduced in Task 1 to i18n-de.js and i18n-cs.js with natural German and Czech translations of each string (reorder aria-labels, Reset order/names + confirm, Issue severity row name, "Issue severity — end of session" form header replacement, "Severity at start", the ⓘ explainer, export topics checkbox label = each locale's in-session Session-topics title, and the Include-severity sub-option). There is NO skipped-rating hint key (dropped per D-19). Include Task 1's added/changed keys: `session.group.wrapup` (natural DE/CS "Session Wrap-up") (G-4), the severity-free `settings.row.issues.description` rewrite (no before/after severity mention) (G-6), and the `session.form.issuesHeading` rename to the DE/CS natural equivalent of "Session topics" keeping the trailing " *" marker, with `export.section.topics` set to the stripped (marker-free) value (G-15/R2-1). Keys must match Task 1's names exactly so every locale resolves the same lookups. Keep terminology consistent with each locale's existing session/severity vocabulary already in the file.
  </action>
  <verify>
    <automated>node --check assets/i18n-de.js && node --check assets/i18n-cs.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check assets/i18n-de.js` and `node --check assets/i18n-cs.js` both succeed.
    - Every new key present in i18n-en.js after Task 1 also exists in i18n-de.js and i18n-cs.js (assert by diffing the key set; zero missing).
    - `session.form.afterSeverityTitle` in both DE and CS is the translated "Issue severity — end of session" equivalent, not the old value.
    - `session.group.wrapup` exists in both DE and CS (G-4); `settings.row.issues.description` in DE and CS names no severity (G-6); `session.form.issuesHeading` in DE and CS is the natural "Session topics" equivalent (marker kept), matching each locale's `export.section.topics` after stripRequired normalization (G-15/R2-1).
  </acceptance_criteria>
  <done>DE and CS carry the full Phase-47 UI-string key set matching EN's key names, so all four locales resolve every reorder/severity/export string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds static translation strings only; no user input, no persistence, no new rendering path. Strings are still rendered by consumers via .textContent/.value (existing XSS contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02b | Tampering (XSS) | New i18n strings rendered downstream | low | accept | Strings are developer-authored constants, not user input; consumers render via .textContent/.value per the existing settings.js/app.js contract. No new boundary introduced here. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- All four dictionaries pass `node --check`.
- `npm test` stays green (existing i18n coverage tests — e.g. the 15-03 key-completeness pass — must still see 4-locale parity for the new keys).
- Comment hygiene: no planning IDs in any i18n-*.js comment.
</verification>

<success_criteria>
Every Phase-47 UI string exists in EN/DE/HE/CS under one shared key set, the D-14 Hebrew label mismatch is fixed, and the feature plans in Wave 2 can call App.t() against stable keys without touching the shared dictionaries.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-02-SUMMARY.md` when done, listing the FINAL key names so 47-03/04/05/07 reference the real keys.
</output>
