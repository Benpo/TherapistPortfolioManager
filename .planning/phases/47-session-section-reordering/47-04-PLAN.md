---
phase: 47-session-section-reordering
plan: 04
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - add-session.html
  - assets/add-session.js
  - tests/47-form-order.test.js
autonomous: true
requirements: [ORDR-03]
must_haves:
  truths:
    - "The session form ships the D-02 default structure: Session topics (bare, first), Emotions & Techniques (group), Issue severity (bare), Session Wrap-up (group) — no single-field pseudo-groups (D-01/D-02)."
    - "The add/edit session form renders its sections in the therapist's SAVED order (App.getSectionOrder()): top-level items in order, group members in member order (D-03/D-16)."
    - "A group whose members are all disabled disappears from the form until a member is re-enabled (D-04); disabled sections don't render but keep their Settings slot (D-11)."
    - "The Phase-41 guided-tour anchors still resolve after the restructure (no dead tour step)."
  artifacts:
    - "add-session.html — D-02 restructured section containers with data-section-key on every section incl. the new afterSeverity block; group wrapper elements; re-pointed data-tour anchors."
    - "assets/add-session.js — order-driven render that arranges [data-section-key] containers into groups per App.getSectionOrder() with empty-group hide."
    - "tests/47-form-order.test.js — asserts form section DOM order == saved order and empty-group hide."
  key_links:
    - "The form reads order from App.getSectionOrder() (47-01) — the SAME source the export builders (47-05) read, so form order == export order (the 260615 three-way invariant)."
    - "The afterSeverity block gets data-section-key='afterSeverity' so its enable state (App.isSectionEnabled) and order slot are recognized; severity — pill + column-hide behavior lands in 47-07."
    - "data-tour='session-heart' re-points to the Emotions & Techniques group container so the Phase-41 tour survives."
---

<objective>
Ship the one-time D-02 form restructure and make the session form order-driven. The static 4-accordion form (add-session.html L205-340) becomes: Session topics (bare, first, carries the before-rating rows), an Emotions & Techniques group (Heart-Wall toggle, Emotions in the Heart-Wall, Trapped Emotions Released, Physical Imbalance, Limiting Beliefs, Additional Techniques and Tools), an Issue severity bare section (the end-of-session ratings), and a Session Wrap-up group (Session Notes, Information for Next Session). Then add-session.js arranges these containers at render time to match App.getSectionOrder(), hides empty groups (D-04), and preserves the guided-tour anchors.

Purpose: ORDR-03 — the saved order must drive the form the therapist actually fills in. This plan builds the structure and the order-driven arrangement; the severity value semantics (— skip pill, after-rating auto-hide, severity-off column hide) land in 47-07, and the export side in 47-05.

Output: restructured add-session.html; order-driven render in add-session.js; a form-order behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-session-section-reordering/47-CONTEXT.md
@.planning/phases/47-session-section-reordering/47-UI-SPEC.md
@.planning/phases/47-session-section-reordering/47-RESEARCH.md
@.planning/phases/47-session-section-reordering/47-01-SUMMARY.md
@.planning/sketches/010-section-groups-concept/index.html
</context>

<artifacts_this_phase_produces>
New symbols/artifacts created by THIS plan (exclude from drift checks):
- `afterSeverity` data-section-key on the end-of-session severity container in add-session.html (the block currently wrapping #issueSummaryList)
- Group wrapper elements for `emotionsTech` and `wrapup` (accordion/group containers the render engine fills)
- An order-driven render function in add-session.js (e.g. `applySectionOrder()`) that arranges [data-section-key] containers + group wrappers per App.getSectionOrder() and hides empty groups
- tests/47-form-order.test.js
- Note: heartShield / heartShieldEmotions / trapped / insights / limitingBeliefs / additionalTech / issues / comments / nextSession are EXISTING keys; only afterSeverity is new here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: D-02 form restructure in add-session.html (groups + afterSeverity data-section-key + tour re-point)</name>
  <files>add-session.html</files>
  <read_first>
    - add-session.html L200-345 (the current 4 accordions: heart-shield {heartShield, issues, heartShieldEmotions}, emotions {trapped, insights, limitingBeliefs, additionalTech} with data-tour="session-heart", severity {#issueSummaryList}, notes {comments, nextSession}) and L51-70 (the session-setup form-grid, data-tour="session-setup") and L360-370 (data-tour="session-save")
    - .planning/sketches/010-section-groups-concept/index.html (the approved Model A structure + D-02 default layout, EN/HE)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md `## Visual Hierarchy` (Session topics first; section headers are the anchors) and Interaction 5 (group header is the reorder unit)
    - assets/tour.js L131-142 (the anchor contract: session-setup / session-heart / session-save must still resolve)
  </read_first>
  <action>
    Restructure the session form markup to the D-02 default: (1) Session topics — a bare section (its own header, data-section-key="issues", label session.form.issuesHeading) FIRST, containing #issueList (the topic rows carrying the before-rating); (2) Emotions & Techniques — a GROUP container (accordion) with header session.accordion.emotions, containing member sections in D-02 order: heartShield (Heart-Wall toggle), heartShieldEmotions (Emotions in the Heart-Wall), trapped (Trapped Emotions Released), insights (Physical Imbalance), limitingBeliefs (Limiting Beliefs), additionalTech (Additional Techniques and Tools) — each keeping its existing data-section-key and inner fields; (3) Issue severity — a bare section wrapping #issueSummaryList, and ADD data-section-key="afterSeverity" to this block plus header session.form.afterSeverityTitle (now "Issue severity — end of session" from 47-02); (4) Session Wrap-up — a GROUP container with header session.form.comments.title (or a wrapup group header) containing comments and nextSession. Preserve every existing input id (#issueList, #issueSummaryList, #trappedEmotions, #sessionInsights, #limitingBeliefs, #additionalTech, #heartShieldToggle, #heartShieldEmotions, #sessionComments, #customerSummary, #nextSessionDate) — no input renaming (avoids breaking add-session.js and populateSession). Re-point data-tour="session-heart" onto the Emotions & Techniques group container (it now holds the heart-wall content). Keep data-tour="session-setup" (setup grid) and data-tour="session-save" (save button) unchanged — they sit outside the reorderable section list. Give each group container a stable identifier (e.g. data-group-id="emotionsTech" / "wrapup") the render engine (Task 2) targets. Do NOT wrap any single field in a group (D-01 — no single-field pseudo-groups).
  </action>
  <verify>
    <automated>grep -q 'data-section-key="afterSeverity"' add-session.html && grep -q 'data-group-id="emotionsTech"' add-session.html && grep -q 'data-group-id="wrapup"' add-session.html && grep -q 'data-tour="session-setup"' add-session.html && grep -q 'data-tour="session-save"' add-session.html && grep -q 'data-tour="session-heart"' add-session.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The grep verify prints OK: afterSeverity data-section-key, both group ids, and all three surviving tour anchors are present.
    - Session topics (data-section-key="issues") is the FIRST section container in source order and is a bare section (not inside a group container).
    - The Emotions & Techniques group container holds exactly heartShield, heartShieldEmotions, trapped, insights, limitingBeliefs, additionalTech as member sections; Session Wrap-up holds comments + nextSession.
    - Every original input id still exists exactly once (grep each of #issueList, #issueSummaryList, #trappedEmotions, #sessionInsights, #limitingBeliefs, #additionalTech, #heartShieldToggle, #heartShieldEmotions, #sessionComments, #customerSummary, #nextSessionDate).
    - No section container wraps a single field alone (no single-field pseudo-group).
  </acceptance_criteria>
  <done>The form markup is the D-02 default structure with stable group containers, the afterSeverity block carries its data-section-key, every input id is preserved, and the session-setup / session-heart / session-save tour anchors still resolve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Order-driven form render (arrange sections per saved order + empty-group hide)</name>
  <files>assets/add-session.js, tests/47-form-order.test.js</files>
  <read_first>
    - assets/add-session.js L997-1045 (applySectionVisibility + applySectionLabels — the existing [data-section-key] walkers to extend; disabled → is-hidden logic that must stay for D-11)
    - assets/add-session.js — the form init path (where applySectionVisibility/applySectionLabels are called on load) so the new order-arrange runs alongside them
    - .planning/phases/47-session-section-reordering/47-01-SUMMARY.md (App.getSectionOrder() shape, flattenOrderKeys / .flatKeys(), group ids, DEFAULT_SECTION_ORDER)
    - .planning/phases/47-session-section-reordering/47-UI-SPEC.md Interaction 4/9 (disabled rows keep slot in Settings but don't render on form; order takes effect on next form open — no live reshuffle) and D-04 empty-group hide
  </read_first>
  <behavior>
    - After render, the top-level section/group containers appear in the same order as App.getSectionOrder() top-level items.
    - Within the Emotions & Techniques group, member section containers appear in the saved member order.
    - A group whose members are all disabled has its container hidden (D-04); re-enabling a member (in the same page load's data) would show it (verified via a getSectionOrder + enable-state fixture).
    - A mutated saved order (e.g. afterSeverity moved before the wrapup group, issues still first) reorders the form DOM to match.
    - Disabled sections do not render in the form but their container stays in the DOM (keeps slot; is-hidden), matching the existing applySectionVisibility contract (D-11).
  </behavior>
  <action>
    Add an order-arrange function (e.g. applySectionOrder) that runs on form init BEFORE/with applySectionVisibility. It reads App.getSectionOrder() and physically reorders the section containers: for each top-level item in order, append its container to the sections host in order; for group items, append the group container then append its member [data-section-key] containers inside the group body in member order. Use the data-group-id hooks from Task 1 to find group containers, and [data-section-key] to find sections (both already in the DOM from the static markup — this MOVES nodes, it does not recreate them, so all inputs/listeners survive). After arranging, hide any group container whose members are ALL disabled (App.isSectionEnabled false for every member) per D-04; keep the existing applySectionVisibility behavior for individual disabled sections (D-11 — hidden but present). The order snapshot is read ONCE on form open (D-16 — no live reshuffle if Settings changes mid-edit). Keep applySectionLabels working (custom labels + group titleOverride if the group header shows a custom title). Do not touch severity value logic here (— pill, after-hide → 47-07). Create tests/47-form-order.test.js (jsdom): stub App.getSectionOrder to return a MUTATED order, run the arrange function against the real add-session.html fixture (or a DOM fixture of the section containers), and assert the resulting [data-section-key] order equals flattenOrderKeys(order) for enabled sections, and that an all-disabled group container is hidden.
  </action>
  <verify>
    <automated>node tests/47-form-order.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/47-form-order.test.js` exits 0.
    - For a mutated saved order, the resulting [data-section-key] sequence (enabled sections, groups flattened) equals App.flattenOrderKeys(order).
    - Session topics (issues) renders at the top-level slot the order specifies; group members render in member order.
    - A group with all members disabled has its container hidden; a group with ≥1 enabled member is visible.
    - Node MOVE semantics preserved: after arrange, every input id still exists exactly once and its value/listeners are intact (assert an input value set before arrange survives).
  </acceptance_criteria>
  <done>The session form renders sections in the therapist's saved order (top-level items + group members), empty groups hide, disabled sections keep their slot without rendering, and moving nodes preserves inputs/listeners. The form-order test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved order → form DOM | App.getSectionOrder() (already sanitized in 47-01) drives which containers render and in what order. No untrusted input is introduced here; order was sanitized at load/persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05 | Tampering | Order-driven render consuming a malformed order | low | mitigate | Render reads App.getSectionOrder(), which returns a sanitized copy (47-01); an unknown key simply has no matching container and is skipped. No innerHTML used — nodes are moved, not rebuilt from strings. |

**No package installs this phase; no supply-chain checkpoint required.**
</threat_model>

<verification>
- `node tests/47-form-order.test.js` passes; `npm test` full suite green (existing add-session tests, e.g. populateSession/revert, still pass after the restructure).
- Manual (real-device gate before /gsd-verify-work): open add-session with a reordered saved order → sections appear in that order; empty group hidden; run the guided tour end-to-end and confirm the session-heart step still anchors (Pitfall 5).
- Comment hygiene: no planning IDs in add-session.html / add-session.js comments.
</verification>

<success_criteria>
The form ships the D-02 structure and renders in the saved order with empty-group hide and disabled-keep-slot behavior, all input ids preserved, and the guided-tour anchors intact. Form order reads from the same App.getSectionOrder() source the export builders use (three-way invariant foundation). Form-order test passes; tour verified on device.
</success_criteria>

<output>
Create `.planning/phases/47-session-section-reordering/47-04-SUMMARY.md` when done. Record the render-function name, the group-container selectors (data-group-id), and confirm the afterSeverity container id/selector that 47-07 (severity column-hide) will target.
</output>