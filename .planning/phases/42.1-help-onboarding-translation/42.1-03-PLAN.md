---
phase: 42.1-help-onboarding-translation
plan: 03
type: execute
wave: 2
depends_on: ["42.1-01"]
files_modified:
  - assets/help-content-he.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "assets/help-content-he.js registers window.HELP_CONTENT_HE with all 12 sections translated into Hebrew (house register), structurally identical to help-content-en.js."
    - "Every {ui:} token is byte-identical to EN and resolves against window.I18N.he; no clinical terms; no emoji; RTL-safe."
  artifacts:
    - assets/help-content-he.js
  key_links:
    - "help-content-he.js keys on the same section ids as EN so help.js localeSections() (Plan 08) merges per-section with EN fallback."
    - "HELP_DEEPLINKS is defined ONLY in help-content-en.js — the HE file must NOT redefine it."
---

<objective>
Author assets/help-content-he.js — the Hebrew mirror of assets/help-content-en.js. Translate only the human prose (section title, topic title, body text and steps) into Hebrew in the house register; keep every structural field, {ui:} token, and array order byte-identical to EN. This is the largest single translation surface (~2,400–2,900 words). Register the global window.HELP_CONTENT_HE. Target: pass tests/42_1-help-integrity.test.js for locale he.

Purpose: L10N-01 — a Hebrew-first practitioner reads the help center natively.
Output: assets/help-content-he.js.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan creates the global `window.HELP_CONTENT_HE` and the file assets/help-content-he.js (new symbol — not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Specifically: do NOT redefine window.HELP_DEEPLINKS (EN-only); do NOT add/remove/reorder sections or topics; do NOT translate {ui:} tokens, ids, covers, group, featured, priority, or glyph names; pre-v1.3 ASSUMED strings and help.entry.replayWelcome are OUT.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/help-content-en.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author assets/help-content-he.js (Hebrew help body, EN-parity structure)</name>
  <files>assets/help-content-he.js</files>
  <read_first>
    - assets/help-content-en.js (the ENTIRE source: 12 sections, ~30 topics, header schema comment lines 14-45; section ids in order: make-it-yours[featured,session-loop], adding-a-client, starting-a-session, capturing-emotions, heart-wall, severity, review-export, overview, backups[technical], installing[technical], license[technical], troubleshooting[technical]; the IIFE + window.HELP_CONTENT_EN registration ~lines 47-556; HELP_DEEPLINKS ~560)
    - assets/i18n-he.js (shipped Hebrew UI vocabulary — the {ui:} tokens resolve here; ground approved terms: client = לקוח, Heart-Wall = חומת הלב; brand "Sessions Garden" stays Latin)
    - tests/42_1-help-integrity.test.js (the gate this file must pass for locale he — structure parity, {ui:} resolution, forbidden clinical terms, no emoji, no control/bidi chars)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (help-content locale-sibling section; Shared Pattern 2 {ui:} tokens; register reminders)
    - .planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md (Surface A; Pitfall 6 RTL; register conventions)
  </read_first>
  <action>
    Create assets/help-content-he.js as a strict structural clone of assets/help-content-en.js with an IIFE that registers window.HELP_CONTENT_HE (NOT _EN). Copy the section array element-for-element preserving, byte-identical to EN: every section `id`, `group`, `featured`; every topic `id`, `priority`, `covers[]`; every body node `type` and glyph `name`; every `{ui:...}` token exactly as written; and the exact order of sections, topics, and body nodes. Do NOT redefine window.HELP_DEEPLINKS.
    TRANSLATE into Hebrew, in the locked HE house register (infinitive / plural-imperative / noun-gerund; NO imperative singular "you"; RTL): each section `title`, each topic `title`, and each body node's `text` and each `steps[]` item. Around a {ui:} token, translate only the surrounding prose and leave the token untouched — it auto-resolves to the live Hebrew label at render time (D-23).
    Terminology (approved, from shipped he vocab): client → לקוח; Heart-Wall → חומת הלב; keep "Sessions Garden", "PDF" and other brand/format tokens in Latin, ensuring they read correctly amid RTL prose (follow the Phase 37-11 / 38-11 bidi precedent — no innerHTML, no manual bidi markup; the renderer is dir-aware). Never use the clinical patient/treatment Hebrew terms that tests/42_1-help-integrity.test.js forbids.
    No emojis anywhere (D-10). No control characters other than the ordinary spaces/newlines the EN file uses; never introduce NUL or bidi-control code points.
  </action>
  <verify>
    <automated>node tests/42_1-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42_1-help-integrity.test.js` now PASSES the locale-he assertions (structure parity with EN, every {ui:} token present+resolving against window.I18N.he, no forbidden clinical term, no emoji, no control/bidi chars). de/cs may still be RED until their plans land.
    - The integrity test's vm-load of assets/help-content-he.js succeeds (no syntax error) and the registered Hebrew help global is a 12-element array.
    - Loading ONLY assets/help-content-he.js in a fresh sandbox (without the EN file) leaves the deep-links global undefined — the locale file does not redefine deep-links (they live only in the EN file).
    - After that isolated load, the Hebrew help global is defined and the EN content global is undefined — the locale file registers the HE global only.
    - Section-id order in help-content-he.js matches help-content-en.js exactly (the integrity test's parity assertion proves this).
  </acceptance_criteria>
  <done>window.HELP_CONTENT_HE exists as a full Hebrew, EN-structurally-identical help corpus that passes the he half of the integrity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help-content-he.js strings → help page DOM | Executable JS shipped to clients; strings flow through help.js textContent render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-06 | Tampering | Hebrew RTL/bidi-control chars in translated strings | medium | mitigate | Integrity gate rejects U+202A–202E / U+2066–2069 and C0/C1 control chars; renderer stays on textContent (no innerHTML) |
| T-42.1-07 | Tampering | clinical term leaks into Hebrew copy | medium | mitigate | Per-locale forbidden-clinical-term regex (Plan 01) + native-speaker gate (Plan 09) + Sapir read (Plan 10) |
| T-42.1-08 | Tampering | {ui:} token translated/broken → wrong or missing label | low | mitigate | Integrity gate asserts every EN {ui:} token present byte-identical and resolving against I18N.he |
</threat_model>

<verification>
- tests/42_1-help-integrity.test.js passes for he.
- `node tests/run-all.js` shows no NEW failures attributable to help-content-he.js (structure/emoji/token checks green for he).
</verification>

<success_criteria>
A Hebrew help corpus that is structurally EN-identical, register-correct, token-faithful, and passes the he integrity gate — ready for the native-speaker gate and Sapir's read.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-03-SUMMARY.md` when done.
</output>
