---
phase: 42.1-help-onboarding-translation
plan: 06
type: execute
wave: 2
depends_on: ["42.1-01"]
files_modified:
  - assets/changelog-content-he.js
  - assets/changelog-content-de.js
  - assets/changelog-content-cs.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "assets/changelog-content-{he,de,cs}.js each register window.CHANGELOG_CONTENT_<LOC> with the FULL 4-entry history (v1.3, v1.2, v1.1, v1.0 origin) translated natively."
    - "version/anchor/order/category-keys are byte-identical to EN; only lede/highlights/category strings + the localized month word differ."
    - "Heart-Wall renders as חומת הלב in HE; no business-internal framing; no emoji."
  artifacts:
    - assets/changelog-content-he.js
    - assets/changelog-content-de.js
    - assets/changelog-content-cs.js
  key_links:
    - "changelog locale files key on entry `version` so changelog.js localeEntries() and whats-new.js entries() (Plan 08) merge per-entry with EN fallback."
    - "The v1.0 entry keeps origin:true with no highlights/categories."
---

<objective>
Author the three changelog locale siblings — assets/changelog-content-{he,de,cs}.js — translating the FULL history (all 4 entries) natively per Ben decision #1, so the changelog page AND the What's-New popup read natively in every locale. Keep version/anchor/order/category-keys byte-identical to EN; localize the month word only (D-07). Register window.CHANGELOG_CONTENT_<LOC>. Target: pass tests/42_1-changelog-integrity-locale.test.js.

Purpose: L10N-01 — the whole changelog reads natively; no mixed-language page.
Output: assets/changelog-content-he.js, -de.js, -cs.js.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan creates globals `window.CHANGELOG_CONTENT_HE/DE/CS` and the three files (new symbols — not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Do NOT translate `version`, `anchor`, category keys (new/improved/fixed), or `origin`; do NOT reorder or add/remove entries; do NOT re-introduce any business-internal/commercial framing (the D-04 approval already reworded the v1.1 lede to drop such an opener — keep the calm, benefit-led garden register in every locale).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/changelog-content-en.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author assets/changelog-content-he.js (Hebrew, full history)</name>
  <files>assets/changelog-content-he.js</files>
  <read_first>
    - assets/changelog-content-en.js (entire file — 4 entries: 1.3.0/v1-3/"July 2026", 1.2.0/v1-2/"July 2026", 1.1.0/v1-1/"June 2026", 1.0.0/v1-0/"May 2026" origin:true; schema header lines 16-57; D-04 approval note lines 43-57)
    - assets/i18n-he.js (shipped Hebrew vocab; Heart-Wall = חומת הלב; client = לקוח; Hebrew month names as established in the app)
    - tests/42_1-changelog-integrity-locale.test.js (the he half of the gate — version/anchor/order parity, category shape, non-empty date, no cross-locale date equality, no emoji, forbidden clinical terms)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (changelog locale-sibling section; A1 date rule)
  </read_first>
  <action>
    Create assets/changelog-content-he.js: an IIFE registering window.CHANGELOG_CONTENT_HE as a 4-entry array structurally identical to CHANGELOG_CONTENT_EN. Keep byte-identical to EN: each entry `version`, `anchor`, `origin` flag, and the present category keys and their order; the v1.0 entry stays { version:"1.0.0", anchor:"v1-0", date:<localized>, lede:<HE>, origin:true } with NO highlights and NO categories.
    TRANSLATE into Hebrew (house register, RTL): each `lede`, each `highlights[]` string (2–4 per content entry, same count as EN), and each category string array (new/improved/fixed). Heart-Wall → חומת הלב; "Session Format", client/session per shipped he vocab; "Sessions Garden", "PDF", "Safari" stay Latin.
    `date`: localize the month word to the established Hebrew month rendering (e.g. the app's Hebrew form of July/June/May 2026) — the factual date (year, month referent) stays the same; only the language of the month word changes (D-07). Never assert nor require it equals EN.
    No emojis (D-10); no control/bidi-control chars; never use clinical patient/treatment Hebrew terms.
  </action>
  <verify>
    <automated>node tests/42_1-changelog-integrity-locale.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42_1-changelog-integrity-locale.test.js passes the he assertions: version/anchor/order parity with EN, category-key parity, v1.0 origin-only, 2–4 highlights per content entry, non-empty date, no emoji/control chars, no clinical term.
    - After loading only assets/changelog-content-he.js in a fresh sandbox, the Hebrew changelog global is a 4-element array and the EN changelog global is undefined (the locale file registers the HE global only).
    - `grep -c "חומת הלב" assets/changelog-content-he.js` ≥ 1 (Heart-Wall rendered in the established HE term where the EN entry mentions Heart-Wall).
  </acceptance_criteria>
  <done>window.CHANGELOG_CONTENT_HE holds the full 4-entry native Hebrew history, passing the he changelog gate.</done>
</task>

<task type="auto">
  <name>Task 2: Author assets/changelog-content-de.js (German Sie form, full history)</name>
  <files>assets/changelog-content-de.js</files>
  <read_first>
    - assets/changelog-content-en.js (entire file — same 4-entry structure)
    - assets/i18n-de.js (shipped German vocab; Heart-Wall term as shipped in de; client/session register; German month words)
    - tests/42_1-changelog-integrity-locale.test.js (de half of the gate)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (changelog locale-sibling section)
  </read_first>
  <action>
    Create assets/changelog-content-de.js registering window.CHANGELOG_CONTENT_DE — 4-entry array structurally identical to EN (same version/anchor/origin/category-keys/order; v1.0 origin-only). TRANSLATE lede/highlights/category strings into German using the Sie form; use the shipped German Heart-Wall term and client/session vocabulary from assets/i18n-de.js; keep brand/format tokens Latin. Localize the month word to German (year/date referent unchanged, D-07). No emojis; no control chars; never use Patient/Behandlung clinical terms; no business-internal framing.
  </action>
  <verify>
    <automated>node tests/42_1-changelog-integrity-locale.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42_1-changelog-integrity-locale.test.js passes the de assertions (structure/category/date-present/no-emoji/no-clinical).
    - After loading only assets/changelog-content-de.js in a fresh sandbox, the German changelog global is a 4-element array and the EN changelog global is undefined (the locale file registers the DE global only).
  </acceptance_criteria>
  <done>window.CHANGELOG_CONTENT_DE holds the full native German history passing the de changelog gate.</done>
</task>

<task type="auto">
  <name>Task 3: Author assets/changelog-content-cs.js (Czech formal, full history)</name>
  <files>assets/changelog-content-cs.js</files>
  <read_first>
    - assets/changelog-content-en.js (entire file)
    - assets/i18n-cs.js (shipped Czech vocab; Heart-Wall term as shipped in cs; client/session register; Czech month words)
    - tests/42_1-changelog-integrity-locale.test.js (cs half of the gate)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (changelog locale-sibling section; CS accepted-risk)
  </read_first>
  <action>
    Create assets/changelog-content-cs.js registering window.CHANGELOG_CONTENT_CS — 4-entry array structurally identical to EN (same version/anchor/origin/category-keys/order; v1.0 origin-only). TRANSLATE lede/highlights/category strings into Czech (formal register); use the shipped Czech Heart-Wall term and client/session vocabulary from assets/i18n-cs.js; keep brand/format tokens Latin. Localize the month word to Czech (D-07). No emojis; no control chars; never use pacient/léčba/léčení clinical terms; no business-internal framing. CS ships on the accepted-risk precedent (agent gate only).
  </action>
  <verify>
    <automated>node tests/42_1-changelog-integrity-locale.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42_1-changelog-integrity-locale.test.js passes the cs assertions; with he+de+cs authored the changelog gate exits 0.
    - After loading only assets/changelog-content-cs.js in a fresh sandbox, the Czech changelog global is a 4-element array and the EN changelog global is undefined (the locale file registers the CS global only).
  </acceptance_criteria>
  <done>window.CHANGELOG_CONTENT_CS holds the full native Czech history; the per-locale changelog gate is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changelog-content-*.js strings → changelog page + What's-New popup DOM | Executable JS shipped to clients; strings render via textContent in both surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-15 | Tampering | business-internal/commercial framing re-introduced in a locale lede | medium | mitigate | NOT-list forbids it; native-speaker gate (Plan 09) + Sapir read (Plan 10) review every lede; D-04 register is the reference |
| T-42.1-16 | Tampering | clinical term / emoji / control char in changelog strings | medium | mitigate | Per-locale changelog integrity gate (Plan 01 Task 2) rejects them |
| T-42.1-17 | Tampering | structural drift (reordered/renamed version/anchor) breaks merge + deep-links | medium | mitigate | Integrity gate asserts version/anchor/order parity with EN |
</threat_model>

<verification>
- tests/42_1-changelog-integrity-locale.test.js passes for he, de, cs.
- `node tests/run-all.js` shows no new failures attributable to the changelog locale files.
</verification>

<success_criteria>
Three full-history changelog locale siblings, structurally EN-identical, natively translated, month-word localized, passing the per-locale changelog gate — feeding both the changelog page and the What's-New popup once Plan 08 wires the loaders.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-06-SUMMARY.md` when done.
</output>
