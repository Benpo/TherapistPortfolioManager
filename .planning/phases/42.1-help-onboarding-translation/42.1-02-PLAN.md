---
phase: 42.1-help-onboarding-translation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/41-tour-i18n-parity.test.js
  - tests/40-i18n-parity.test.js
  - tests/39-help-precache.test.js
  - tests/42-precache.test.js
  - tests/42-whats-new-gating.test.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "The tour parity gate now asserts the \\n\\n paragraph break survives in help.tour.step.help.body in every locale — RED for any locale that drops it."
    - "The i18n parity gate now requires help.welcome.subtitle2 to be non-empty in every locale — RED while he/de/cs hold empty stubs."
    - "The precache gates require all 6 new locale content files in sw.js PRECACHE_URLS — RED until sw.js is wired."
    - "A whats-new render assertion requires the popup to surface a localized highlight — RED until whats-new.js entries() is made locale-aware (Pitfall 1)."
  artifacts:
    - tests/41-tour-i18n-parity.test.js
    - tests/40-i18n-parity.test.js
    - tests/39-help-precache.test.js
    - tests/42-precache.test.js
    - tests/42-whats-new-gating.test.js
  key_links:
    - "help.tour.step.help.body \\n\\n token is a placeholder-class constraint like {screen}/{n}/{total} — asserted via the existing requireToken idiom."
    - "help.welcome.subtitle2 added to the 40-parity NEW_KEYS list closes the empty-stub gap from Phase 40-08."
---

<objective>
Extend five existing gates RED-first so the translation and wiring plans have falsifiable targets for the highest-risk mechanical failures: the dropped \n\n paragraph break, the empty welcome.subtitle2 stubs, the missing offline precache entries, and the What's-New popup locale asymmetry (Pitfall 1). Each extension goes RED now and turns GREEN when the corresponding translation/wiring plan lands.

Purpose: L10N-01 per-locale integrity plus the RESEARCH Wave 0 Gaps that live in existing test files (not new files).
Output: extended tour parity, i18n parity, two precache gates, and one whats-new render gate.
</objective>

## Artifacts this phase produces

See Plan 01's "Artifacts this phase produces" — the same phase-wide symbol list applies. This plan specifically adds: a `\n\n` token assertion in tests/41-tour-i18n-parity.test.js, `help.welcome.subtitle2` in the tests/40-i18n-parity.test.js NEW_KEYS array, six locale-file entries in the precache gates, and a localized-highlight assertion in a whats-new render test.

## Out of scope (NOT-list)

Same NOT-list as Plan 01 (pre-v1.3 ASSUMED strings incl. Phase 37-06 F5/F4 OUT; help.entry.replayWelcome RETIRED — no task; no new EN corpus; docs hard-gate is Phase 43; rich-text/email-push/per-screen panels OUT; CS external review OUT).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the \n\n paragraph-break parity assertion to tests/41-tour-i18n-parity.test.js</name>
  <files>tests/41-tour-i18n-parity.test.js</files>
  <read_first>
    - tests/41-tour-i18n-parity.test.js (LOCALES ~line 42; the requireToken helper + existing {screen}/{n}/{total} guards ~168-182)
    - assets/i18n-en.js (help.tour.step.help.body at line 632 — the EN value already contains a literal \n\n paragraph break)
    - .planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md (Pitfall 2)
  </read_first>
  <action>
    In tests/41-tour-i18n-parity.test.js, inside the existing "Placeholder tokens survive in every locale" test (the one that already calls requireToken for {screen}/{n}/{total}), add one more requireToken assertion: help.tour.step.help.body must contain the two-newline paragraph-break token in every locale in LOCALES (en/he/de/cs). Reuse the existing requireToken(key, token) helper; the token argument is a string of two newline characters. Because the values are stored as JS string literals, the assertion checks the parsed runtime value contains a newline immediately followed by a newline (the pre-line paragraph break). Do NOT loosen the assertion to a single newline.
  </action>
  <verify>
    <automated>node tests/41-tour-i18n-parity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The test now references help.tour.step.help.body in a requireToken call for the two-newline break: `grep -c "help.tour.step.help.body" tests/41-tour-i18n-parity.test.js` ≥ 1.
    - Running the test PASSES for EN (its value already carries the break) and will FAIL for any locale whose value drops it — confirm by temporarily noting the EN assertion passes today (the he/de/cs re-derivation in Plan 07 supplies the break).
    - The existing {screen}/{n}/{total} assertions remain intact (still present in the file).
  </acceptance_criteria>
  <done>The tour parity gate enforces the \n\n break for help.tour.step.help.body across all four locales.</done>
</task>

<task type="auto">
  <name>Task 2: Add help.welcome.subtitle2 to the tests/40-i18n-parity.test.js NEW_KEYS list (RED-first)</name>
  <files>tests/40-i18n-parity.test.js</files>
  <read_first>
    - tests/40-i18n-parity.test.js (NEW_KEYS array lines 66-80; the "non-empty strings in every locale" test ~105; presence-parity test ~126; emoji scan ~146)
    - assets/i18n-en.js (help.welcome.subtitle2 at line 602 — EN value already authored)
    - assets/i18n-he.js / i18n-de.js / i18n-cs.js (line 598 — currently empty-string stubs "")
    - .planning/phases/42.1-help-onboarding-translation/DEFERRED-FROM-40.md (subtitle/subtitle2 EN targets)
  </read_first>
  <action>
    Add the string 'help.welcome.subtitle2' to the NEW_KEYS array in tests/40-i18n-parity.test.js. The existing "all keys exist and are non-empty strings in every locale" test uses a non-empty-after-trim check, so adding this key makes the suite RED immediately (he/de/cs hold ""), which is the correct RED-first signal — it turns GREEN when Plan 07 authors subtitle2. Do not change the non-empty assertion logic; just extend the guarded key set. The count comment in the test name ("All 14 new keys...") should be updated to the new count if the test name hardcodes it.
  </action>
  <verify>
    <automated>node tests/40-i18n-parity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help.welcome.subtitle2" tests/40-i18n-parity.test.js` ≥ 1 and it is a member of the NEW_KEYS array.
    - `node tests/40-i18n-parity.test.js` exits 1 (RED) today because he/de/cs subtitle2 are empty stubs; the failure names help.welcome.subtitle2.
    - The emoji scan and presence-parity tests still run over the extended NEW_KEYS (no regression to existing keys).
  </acceptance_criteria>
  <done>The i18n parity gate now fails until subtitle2 is authored non-empty in all four locales.</done>
</task>

<task type="auto">
  <name>Task 3: Extend precache gates for the 6 locale files + add a localized-highlight assertion to a whats-new render test</name>
  <files>tests/39-help-precache.test.js, tests/42-precache.test.js, tests/42-whats-new-gating.test.js</files>
  <read_first>
    - tests/39-help-precache.test.js (PRECACHE_URLS isolation ~44-52; the forEach asset check ~49)
    - tests/42-precache.test.js (separate PRECACHE_URLS vs PRECACHE_HTML region isolation ~61-67; check() ~52)
    - tests/42-whats-new-gating.test.js (harness that seeds win.CHANGELOG_CONTENT_EN ~79; entry() fixture ~88; render path via whats-new.js)
    - assets/whats-new.js (entries() EN-only read ~73-77 — the Pitfall 1 source; render highlights via textContent ~143-148; lang from portfolioLang ~53)
    - sw.js (PRECACHE_URLS block with help-content-en.js ~95 and changelog-content-en.js ~111)
    - .planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md (Pitfalls 1 and 5)
  </read_first>
  <action>
    Three RED-first extensions:
    (1) tests/39-help-precache.test.js: extend the PRECACHE_URLS forEach asset list to also require '/assets/help-content-he.js', '/assets/help-content-de.js', '/assets/help-content-cs.js' — string presence in the isolated PRECACHE_URLS region.
    (2) tests/42-precache.test.js: extend its PRECACHE_URLS region check to require '/assets/changelog-content-he.js', '/assets/changelog-content-de.js', '/assets/changelog-content-cs.js'. Keep the existing PRECACHE_HTML-vs-PRECACHE_URLS region isolation intact — assert the changelog content siblings are in PRECACHE_URLS, not PRECACHE_HTML.
    (3) tests/42-whats-new-gating.test.js: add ONE test that seeds win.CHANGELOG_CONTENT_HE (a fixture entry for APP_VERSION whose highlights contain a distinctive Hebrew marker string), sets portfolioLang='he' in the harness localStorage, drives the popup render, and asserts the rendered highlights list contains the Hebrew marker — NOT the EN highlight. This stays RED until Plan 08 makes whats-new.js entries() locale-aware. Preserve the existing gating tests (T-42-V1/V2/V3) unchanged; reuse the same seeding helper, adding a CHANGELOG_CONTENT_HE seed alongside the EN one.
  </action>
  <verify>
    <automated>node tests/39-help-precache.test.js; node tests/42-precache.test.js; node tests/42-whats-new-gating.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help-content-he.js" tests/39-help-precache.test.js` ≥ 1 and de/cs siblings also present; the assertions target the isolated PRECACHE_URLS region.
    - `grep -c "changelog-content-he.js" tests/42-precache.test.js` ≥ 1 and de/cs siblings present; the region-isolation (URLS vs HTML) is unchanged.
    - Both precache tests exit 1 (RED) now because sw.js does not yet list the 6 files.
    - tests/42-whats-new-gating.test.js contains a new test seeding CHANGELOG_CONTENT_HE + portfolioLang='he' and asserting a Hebrew highlight renders; it exits RED now (whats-new.js still EN-only). Existing T-42-V1/V2/V3 still pass in isolation of the new RED test (run and confirm only the new test fails).
  </acceptance_criteria>
  <done>Offline-precache and What's-New-popup locale gates are RED-first and pinned to the 6 new files + the entries() locale fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translated string → tour/popup DOM | \n\n and {ui:}/placeholder tokens are structural; dropping them corrupts rendered layout/labels |
| new content file → service worker cache | A file missing from PRECACHE_URLS breaks help/changelog offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-03 | Tampering | dropped \n\n / placeholder tokens | medium | mitigate | Tour parity gate extended here asserts token survival in every locale |
| T-42.1-04 | Denial of Service (offline) | missing precache entries | medium | mitigate | Precache gates extended here require all 6 locale files in PRECACHE_URLS |
| T-42.1-05 | Tampering | popup silently stays English | low | mitigate | whats-new localized-highlight render gate authored here (turns green with Plan 08 fix) |
</threat_model>

<verification>
- All five extended gates run without throwing and are RED for their intended reason (missing content / empty stub / missing precache / EN-only popup).
- `node tests/run-all.js` completes; the extended gates show as failing until their partner plans land (expected Wave 1 RED).
</verification>

<success_criteria>
Five existing gates now enforce the \n\n break, subtitle2 non-emptiness, 6-file precache coverage, and popup localization — all RED-first with a clear GREEN target.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-02-SUMMARY.md` when done.
</output>
