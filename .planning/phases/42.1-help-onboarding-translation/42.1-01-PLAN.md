---
phase: 42.1-help-onboarding-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/42_1-help-integrity.test.js
  - tests/42_1-changelog-integrity-locale.test.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "A per-locale help-integrity gate exists that FAILS (RED) until help-content-{he,de,cs}.js are authored with EN-parity structure, resolving {ui:} tokens, no forbidden clinical terms, and no emoji."
    - "A per-locale changelog-integrity gate exists that FAILS (RED) until changelog-content-{he,de,cs}.js exist with EN schema/anchor/order parity and no emoji."
    - "Both gates auto-join the suite via tests/run-all.js with zero registration."
  artifacts:
    - tests/42_1-help-integrity.test.js
    - tests/42_1-changelog-integrity-locale.test.js
  key_links:
    - "help integrity test loads i18n-<loc>.js + help-content-<loc>.js in a vm sandbox and cross-checks structure against help-content-en.js (keyed on section id)."
    - "changelog integrity test cross-checks version/anchor/order against changelog-content-en.js but MUST NOT assert cross-locale date equality (D-07 localized month)."
---

<objective>
Author the two NEW per-locale integrity gates RED-first, before any translation lands. These are the machine contracts every translated content file must satisfy: {ui:key} resolution against the live locale dict, structure parity with EN, locale-appropriate forbidden clinical terms, no emoji, and no control characters. Authoring them first (Wave 0) guarantees the translation plans have a falsifiable target and cannot pass vacuously.

Purpose: L10N-01 requires per-locale integrity tests mirroring tests/39-help-integrity.test.js. This plan delivers the two new-file gates; Plan 02 extends the existing gates.
Output: tests/42_1-help-integrity.test.js (param he/de/cs) + tests/42_1-changelog-integrity-locale.test.js, both RED until content lands.
</objective>

## Artifacts this phase produces

The whole phase (all plans) creates these NEW symbols — treat them as newly-created, not drift:
- New globals: `window.HELP_CONTENT_HE`, `window.HELP_CONTENT_DE`, `window.HELP_CONTENT_CS`, `window.CHANGELOG_CONTENT_HE`, `window.CHANGELOG_CONTENT_DE`, `window.CHANGELOG_CONTENT_CS`.
- New functions: `localeSections()` in assets/help.js; a rewritten locale-aware `entries()` in assets/whats-new.js.
- New files: assets/help-content-{he,de,cs}.js, assets/changelog-content-{he,de,cs}.js, tests/42_1-help-integrity.test.js, tests/42_1-changelog-integrity-locale.test.js.
- Extended tests: tests/41-tour-i18n-parity.test.js (adds a `\n\n` token assertion), tests/40-i18n-parity.test.js (adds `help.welcome.subtitle2` + the new v1.3 chrome/screen keys below to its parity coverage), tests/42-i18n-parity.test.js (adds `{V}`/`{X.Y}` token-survival assertions), tests/39-help-precache.test.js + tests/42-precache.test.js (add 6 locale files), tests/42-whats-new-gating.test.js (adds a localized-highlight assertion + a cross-page script-tag shape assertion).
- New i18n keys (v1.3 chrome + AT strings lifted into the i18n layer — see note below): `help.welcome.subtitle2` becomes non-empty in he/de/cs; and these NEW keys are authored in ALL FOUR locales (EN value byte-identical to today's hardcoded string; he/de/cs translated):
  - `help.chrome.startHere`, `help.chrome.sessionLoop`, `help.chrome.technicalBits`, `help.chrome.spine`, `help.chrome.techBand`, `help.chrome.intro`, `help.chrome.jumpToSection`, `help.chrome.contactTitle`, `help.chrome.contactBody`, `help.chrome.reportHint` (visible help-page chrome — help.js LABELS + help.html static shell)
  - `help.chrome.ariaSections`, `help.chrome.ariaSearch`, `help.chrome.ariaShowTopics` (AT-facing aria-labels)
  - `help.tour.screen.overview`, `help.tour.screen.settings`, `help.tour.screen.session`, `help.tour.screen.sessions` (tour fallback screen-name interpolation — replaces tour.js hardcoded English screenNames)
- Re-keyed CSS: assets/help.css `[dir=rtl]` help-body override is removed / re-keyed off an EN-fallback marker so translated sections render RTL (BLOCKER 2).
- Popup-locale delivery: the 3 `changelog-content-{he,de,cs}.js` `<script>` tags are added to ALL nine pages that load whats-new.js (index/sessions/add-client/add-session/settings/reporting/report + help/changelog) so the What's-New popup renders native content on whichever chrome page it fires (BLOCKER 1).

**Note on the new chrome/screen keys (NOT new EN copy):** these keys only LIFT existing hardcoded v1.3 chrome/AT strings out of help.js / help.html / tour.js into the i18n layer so they can be translated. The EN value of each new key is byte-identical to the string it replaces — no new EN copy is authored. This is required by the phase goal (every v1.3-authored user-facing string is translated); it is NOT the "frozen EN corpus" the NOT-list protects.

## Out of scope (NOT-list) — carried in every plan

- Pre-v1.3 ASSUMED strings are OUT: specifically Phase 37-06 F5+F4 i18n (Personalization tab / date-format picker option labels) — "he/de/cs ASSUMED, pending native review" per STATE.md; NOT part of 42.1. Any other pre-Phase-39 draft/ASSUMED i18n values are OUT.
- `help.entry.replayWelcome` is RETIRED (removed Phase 41-12, commit 67285b1; grep returns nothing). NO task re-translates it. DEFERRED-FROM-40.md item 3 is stale.
- No new help content / tour steps / changelog entries — the EN body/tour/changelog PROSE corpus is FROZEN; this phase only translates it. (This does NOT exclude lifting the existing hardcoded v1.3 chrome/AT strings — the help.js LABELS, help.html shell chrome, and tour.js screenNames — into i18n keys with EN byte-identical values so they can be translated; that i18n-enablement is IN scope per the phase goal.)
- IN scope (do NOT NOT-list these — every one is goal-forced): the What's-New popup rendering native on all nine chrome pages (BLOCKER 1); removing/re-keying the interim `[dir=rtl]` help-body LTR suppression in help.css so translated prose renders RTL (BLOCKER 2); the v1.3 help chrome + AT aria strings and the tour screen-name strings (BLOCKER 3 + WARNING 3).
- The docs hard-gate (GATE-01..04) is Phase 43, not here.
- Rich-text, email/push announcements, per-screen help panels — v2/out of scope.
- CS external human review — CS ships on the Phase-37 accepted-risk precedent (agent gate only).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/42_1-help-integrity.test.js (parameterized he/de/cs), RED-first</name>
  <files>tests/42_1-help-integrity.test.js</files>
  <read_first>
    - tests/39-help-integrity.test.js (full skeleton to mirror — vm-sandbox loader ~lines 33-52, custom test() harness ~96-106, {ui:key} resolution check ~108-125, EMOJI_RE ~92, EN patient/treatment forbidden regex ~212)
    - tests/42-changelog-integrity.test.js (the fs.existsSync RED-guard skeleton ~46-52 for files that do not exist yet)
    - assets/help-content-en.js (canonical structure: 12 sections; ids make-it-yours, adding-a-client, starting-a-session, capturing-emotions, heart-wall, severity, review-export, overview, backups, installing, license, troubleshooting; featured=make-it-yours; groups session-loop + technical; HELP_DEEPLINKS lives ONLY here)
    - assets/i18n-he.js / i18n-de.js / i18n-cs.js (shipped client/session vocabulary to ground the approved-terms half of the terminology check)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (Shared Pattern 4 loader; Pitfall 3 forbidden-word design)
  </read_first>
  <action>
    Create tests/42_1-help-integrity.test.js. Loop LOCALES = ['he','de','cs']. For each locale, load assets/i18n-<loc>.js + assets/help-content-<loc>.js AND assets/help-content-en.js + assets/i18n-en.js into a fresh vm.createContext sandbox (mirror the tests/39-help-integrity.test.js loader idiom exactly). Guard each locale file with fs.existsSync FIRST and, when the file is absent, report a RED failure with message "assets/help-content-<loc>.js not found" (mirror tests/42-changelog-integrity.test.js RED-guard) — never throw, so the gate is a live RED, not a crash.
    Assert per locale, comparing the locale HELP_CONTENT_<LOC> array against HELP_CONTENT_EN:
    (a) Structure parity: identical section `id`s in identical order; each section's `group`, `featured` flag, topic `id`s, topic `priority`, and topic `covers[]` are identical to EN; exactly ONE featured section; both groups session-loop and technical present. Only `title` and body `text`/`steps[]` may differ from EN.
    (b) {ui:key} resolution: every `{ui:...}` token that appears in the locale body text resolves to a non-empty string in window.I18N.<loc> (mirror tests/39-help-integrity.test.js resolution check). Also assert every `{ui:...}` token present in the EN body is present byte-identical in the locale body (tokens are never translated, D-23).
    (c) Locale-appropriate forbidden clinical terms (Pitfall 3 — do NOT reuse EN /\bpatient\b/): the terminology check must flag the clinical patient/treatment equivalents in each locale's script (HE clinical patient/treatment words; DE Patient/Behandlung family; CS pacient/léčba family). Ground the APPROVED client/session terms in the shipped i18n-<loc>.js vocabulary. Positively encode the forbidden regexes in the test so it fails if a clinical term appears in any locale body.
    (d) No emoji: reuse the EMOJI_RE from tests/39-help-integrity.test.js against every locale title/body string.
    (e) No control characters other than the allowed `\n` (guard against NUL-truncation / bidi-control injection per Phase 29 precedent): reject C0/C1 control chars and U+202A–U+202E / U+2066–U+2069 bidi-control code points in any locale string.
    Use the tests/run-all.js contract: console.log PASS/FAIL per assertion, process.exit(failed === 0 ? 0 : 1). One parameterized file over he/de/cs (RESEARCH Open Q 3 — preferred over three siblings).
    Do NOT include the emoji glyphs or the forbidden clinical literals anywhere except inside the regex definitions in this test file.
  </action>
  <verify>
    <automated>node tests/42_1-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42_1-help-integrity.test.js` executes without throwing and exits 1 (RED), reporting "assets/help-content-he.js not found" (and de/cs) — proving the gate is live, not vacuous.
    - `grep -c "fs.existsSync" tests/42_1-help-integrity.test.js` returns ≥ 3 (one guard per locale) — verify with `grep -v '^\s*//' tests/42_1-help-integrity.test.js | grep -c "fs.existsSync"`.
    - The file's LOCALES array equals ['he','de','cs'] (EN is loaded as the parity baseline, not iterated as a target).
    - The terminology check contains distinct forbidden regexes for HE, DE, and CS (not a single shared EN patient/treatment regex): `grep -c "Behandlung" tests/42_1-help-integrity.test.js` ≥ 1 and a HE clinical term and a CS clinical term each appear in a regex.
    - `node tests/run-all.js` auto-discovers the new file (it appears in the run output).
  </acceptance_criteria>
  <done>The help per-locale integrity gate exists, auto-joins the suite, and is RED for the correct reason (locale content files absent).</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/42_1-changelog-integrity-locale.test.js (he/de/cs), RED-first</name>
  <files>tests/42_1-changelog-integrity-locale.test.js</files>
  <read_first>
    - tests/42-changelog-integrity.test.js (schema checks + fs.existsSync RED-guard ~46-52 to mirror)
    - assets/changelog-content-en.js (canonical: 4 entries — version/anchor pairs 1.3.0/v1-3, 1.2.0/v1-2, 1.1.0/v1-1, 1.0.0/v1-0; v1.0 is origin:true with no highlights/categories; reverse-chronological order; category keys new/improved/fixed)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (changelog locale-sibling section; A1 date rule)
  </read_first>
  <action>
    Create tests/42_1-changelog-integrity-locale.test.js. Loop LOCALES = ['he','de','cs']. For each, fs.existsSync-guard assets/changelog-content-<loc>.js (RED "assets/changelog-content-<loc>.js not found" when absent), then load it plus assets/changelog-content-en.js into a vm sandbox. Assert, comparing CHANGELOG_CONTENT_<LOC> against CHANGELOG_CONTENT_EN:
    (a) Schema/order parity: same number of entries; identical `version` values in identical reverse-chronological order; identical `anchor` values; the entry whose version is 1.0.0 carries `origin: true` and has NO highlights and NO categories; content entries each have a non-empty `lede` and a `highlights` array of length 2–4.
    (b) Category shape parity: for each content entry, the set of present category keys (new/improved/fixed) is identical to EN and each present category is a non-empty string array (empty categories omitted, D-11). Category keys are never translated.
    (c) date: assert each entry HAS a non-empty `date` string, but DO NOT assert cross-locale `date` equality — the month word is localized per D-07 (A1). Assert the `version`/`anchor` are byte-identical to EN.
    (d) No emoji (reuse an EMOJI_RE) and no C0/C1 control or bidi-control chars in any lede/highlight/category string.
    (e) Locale-appropriate forbidden clinical terms (same design as Task 1 — HE/DE/CS clinical patient/treatment equivalents), grounded in shipped vocab.
    tests/run-all.js contract: console.log PASS/FAIL, process.exit(0/1).
  </action>
  <verify>
    <automated>node tests/42_1-changelog-integrity-locale.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/42_1-changelog-integrity-locale.test.js` exits 1 (RED) reporting the three missing locale files — no throw.
    - The test asserts version+anchor parity but contains NO assertion of cross-locale `date` string equality (grep the file: no `date` equality comparison across locales).
    - The origin-entry check keys on version 1.0.0 and asserts absence of highlights/categories.
    - `grep -v '^\s*//' tests/42_1-changelog-integrity-locale.test.js | grep -c "fs.existsSync"` ≥ 3.
    - `node tests/run-all.js` auto-discovers the new file.
  </acceptance_criteria>
  <done>The changelog per-locale integrity gate exists, auto-joins the suite, and is RED because the locale files are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translated .js content file → client DOM | Locale content files are executable JS shipped to clients; their strings flow into the help/changelog/popup render path |
| translator/agent → shipped copy | Copy corruption (clinical or business-internal framing, emoji, control chars) can leak into client-shipped strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-01 | Tampering | translated help/changelog strings | medium | mitigate | These very integrity gates: no-emoji + control-char + bidi-control rejection in every locale string (this plan authors the assertions) |
| T-42.1-02 | Tampering | clinical/business-internal framing in shipped copy | medium | mitigate | Per-locale forbidden clinical-term regexes authored here; the native-speaker gate (Plan 09) + Sapir read (Plan 10) are the human backstop |
| T-42.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); nothing to verify |
</threat_model>

<verification>
- `node tests/42_1-help-integrity.test.js` and `node tests/42_1-changelog-integrity-locale.test.js` both run without throwing and exit RED for the missing-locale-file reason.
- `node tests/run-all.js` still completes and now discovers the two new files (they show as failing until content lands — expected Wave 0 RED).
</verification>

<success_criteria>
Two live, non-vacuous per-locale integrity gates exist and are RED-first, giving the translation plans a falsifiable target that mirrors tests/39-help-integrity.test.js.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-01-SUMMARY.md` when done.
</output>
