---
phase: 42.1-help-onboarding-translation
plan: 04
type: execute
wave: 2
depends_on: ["42.1-01"]
files_modified:
  - assets/help-content-de.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "assets/help-content-de.js registers window.HELP_CONTENT_DE with all 12 sections translated into German (Sie form), structurally identical to help-content-en.js."
    - "Every {ui:} token is byte-identical to EN and resolves against window.I18N.de; no Patient/Behandlung clinical terms; no emoji."
  artifacts:
    - assets/help-content-de.js
  key_links:
    - "help-content-de.js keys on the same section ids as EN so help.js localeSections() (Plan 08) merges per-section with EN fallback."
    - "HELP_DEEPLINKS is defined ONLY in help-content-en.js — the DE file must NOT redefine it."
---

<objective>
Author assets/help-content-de.js — the German mirror of assets/help-content-en.js. Translate only the human prose into German using the Sie form; keep every structural field, {ui:} token, and array order byte-identical to EN. Register window.HELP_CONTENT_DE. Target: pass tests/42_1-help-integrity.test.js for locale de.

Purpose: L10N-01 — German-speaking practitioners read the help center natively.
Output: assets/help-content-de.js.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan creates the global `window.HELP_CONTENT_DE` and the file assets/help-content-de.js (new symbol — not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Do NOT redefine window.HELP_DEEPLINKS; do NOT alter structure/ids/covers/tokens/order; pre-v1.3 ASSUMED strings and help.entry.replayWelcome are OUT.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/help-content-en.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author assets/help-content-de.js (German help body, Sie form, EN-parity structure)</name>
  <files>assets/help-content-de.js</files>
  <read_first>
    - assets/help-content-en.js (entire source — same section/topic map as listed in Plan 03; IIFE + window.HELP_CONTENT_EN; HELP_DEEPLINKS EN-only)
    - assets/i18n-de.js (shipped German UI vocabulary — {ui:} tokens resolve here; ground approved client/session terms in this file; Heart-Wall term as used in shipped de copy; "Sessions Garden" stays Latin)
    - tests/42_1-help-integrity.test.js (the de half of the gate — structure parity, {ui:} resolution against I18N.de, forbidden Patient/Behandlung family, no emoji, no control/bidi chars)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (help-content locale-sibling section; Shared Pattern 2)
    - .planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md (Surface A; Pitfall 3 German forbidden terms)
  </read_first>
  <action>
    Create assets/help-content-de.js as a strict structural clone of assets/help-content-en.js registering window.HELP_CONTENT_DE (NOT _EN). Preserve byte-identical to EN: every section `id`, `group`, `featured`; every topic `id`, `priority`, `covers[]`; every body node `type`/glyph `name`; every `{ui:...}` token; and all ordering. Do NOT redefine window.HELP_DEEPLINKS.
    TRANSLATE into German using the Sie form (formal address) consistently: each section `title`, each topic `title`, each body node `text`, each `steps[]` item. Leave every {ui:} token untouched (D-23 resolves it to the live German label). Use the shipped German client/session vocabulary from assets/i18n-de.js; keep "Sessions Garden", "PDF" and format tokens in Latin. Never use the clinical Patient/Behandlung terms that tests/42_1-help-integrity.test.js forbids — use the app's client (Klient) / session (Sitzung) register as shipped.
    No emojis (D-10); no control/NUL/bidi-control characters.
  </action>
  <verify>
    <automated>node tests/42_1-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42_1-help-integrity.test.js PASSES the locale-de assertions (structure parity, {ui:} resolution against I18N.de, no Patient/Behandlung, no emoji, no control/bidi chars).
    - The integrity test's vm-load of assets/help-content-de.js succeeds; the registered German help global is a 12-element array with EN-identical section-id order.
    - Loading ONLY assets/help-content-de.js in a fresh sandbox leaves the deep-links global undefined — the locale file does not redefine deep-links.
    - After that isolated load, the German help global is defined and the EN content global is undefined — the locale file registers the DE global only.
  </acceptance_criteria>
  <done>window.HELP_CONTENT_DE exists as a full German (Sie-form), EN-structurally-identical help corpus passing the de integrity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help-content-de.js strings → help page DOM | Executable JS shipped to clients; strings flow through help.js textContent render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-09 | Tampering | clinical Patient/Behandlung leak into German copy | medium | mitigate | Per-locale forbidden-term regex (Plan 01) + native-speaker gate (Plan 09) |
| T-42.1-10 | Tampering | control/emoji injection in German strings | low | mitigate | Integrity gate no-emoji + control-char rejection; textContent-only render path |
| T-42.1-11 | Tampering | {ui:} token translated/broken | low | mitigate | Integrity gate asserts EN {ui:} tokens present byte-identical + resolving against I18N.de |
</threat_model>

<verification>
- tests/42_1-help-integrity.test.js passes for de.
- `node tests/run-all.js` shows no new failures attributable to help-content-de.js.
</verification>

<success_criteria>
A German (Sie-form) help corpus, structurally EN-identical, token-faithful, passing the de integrity gate.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-04-SUMMARY.md` when done.
</output>
