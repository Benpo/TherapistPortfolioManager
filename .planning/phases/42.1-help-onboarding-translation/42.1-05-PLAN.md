---
phase: 42.1-help-onboarding-translation
plan: 05
type: execute
wave: 2
depends_on: ["42.1-01"]
files_modified:
  - assets/help-content-cs.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "assets/help-content-cs.js registers window.HELP_CONTENT_CS with all 12 sections translated into Czech (formal register), structurally identical to help-content-en.js."
    - "Every {ui:} token is byte-identical to EN and resolves against window.I18N.cs; no pacient/léčba clinical terms; no emoji."
  artifacts:
    - assets/help-content-cs.js
  key_links:
    - "help-content-cs.js keys on the same section ids as EN so help.js localeSections() (Plan 08) merges per-section with EN fallback."
    - "HELP_DEEPLINKS is defined ONLY in help-content-en.js — the CS file must NOT redefine it."
---

<objective>
Author assets/help-content-cs.js — the Czech mirror of assets/help-content-en.js. Translate only the human prose into Czech in the formal register; keep every structural field, {ui:} token, and array order byte-identical to EN. Register window.HELP_CONTENT_CS. CS ships on the Phase-37 accepted-risk precedent (agent gate only, no external human review). Target: pass tests/42_1-help-integrity.test.js for locale cs.

Purpose: L10N-01 — Czech-speaking practitioners read the help center natively.
Output: assets/help-content-cs.js.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan creates the global `window.HELP_CONTENT_CS` and the file assets/help-content-cs.js (new symbol — not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Do NOT redefine window.HELP_DEEPLINKS; do NOT alter structure/ids/covers/tokens/order; pre-v1.3 ASSUMED strings and help.entry.replayWelcome are OUT. CS external human review is explicitly OUT (accepted risk).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/help-content-en.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author assets/help-content-cs.js (Czech help body, formal register, EN-parity structure)</name>
  <files>assets/help-content-cs.js</files>
  <read_first>
    - assets/help-content-en.js (entire source — same section/topic map as Plan 03; IIFE + window.HELP_CONTENT_EN; HELP_DEEPLINKS EN-only)
    - assets/i18n-cs.js (shipped Czech UI vocabulary — {ui:} tokens resolve here; ground approved client/session terms; "Sessions Garden" stays Latin)
    - tests/42_1-help-integrity.test.js (the cs half of the gate — structure parity, {ui:} resolution against I18N.cs, forbidden pacient/léčba/léčení family, no emoji, no control/bidi chars)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (help-content locale-sibling section; Shared Pattern 2)
    - .planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md (Surface A; Pitfall 3 Czech forbidden terms; CS accepted-risk)
  </read_first>
  <action>
    Create assets/help-content-cs.js as a strict structural clone of assets/help-content-en.js registering window.HELP_CONTENT_CS (NOT _EN). Preserve byte-identical to EN: every section `id`, `group`, `featured`; every topic `id`, `priority`, `covers[]`; every body node `type`/glyph `name`; every `{ui:...}` token; all ordering. Do NOT redefine window.HELP_DEEPLINKS.
    TRANSLATE into Czech using the formal register consistently: each section `title`, topic `title`, body node `text`, each `steps[]` item. Leave every {ui:} token untouched (D-23). Use the shipped Czech client/session vocabulary from assets/i18n-cs.js (client = klient, session as shipped); keep "Sessions Garden", "PDF" and format tokens in Latin. Never use the clinical pacient/léčba/léčení terms that tests/42_1-help-integrity.test.js forbids.
    No emojis (D-10); no control/NUL/bidi-control characters.
  </action>
  <verify>
    <automated>node tests/42_1-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42_1-help-integrity.test.js PASSES the locale-cs assertions (structure parity, {ui:} resolution against I18N.cs, no pacient/léčba, no emoji, no control/bidi chars). With he+de+cs all authored, the gate exits 0.
    - The integrity test's vm-load of assets/help-content-cs.js succeeds; the registered Czech help global is a 12-element array with EN-identical section-id order.
    - Loading ONLY assets/help-content-cs.js in a fresh sandbox leaves the deep-links global undefined — the locale file does not redefine deep-links.
    - After that isolated load, the Czech help global is defined and the EN content global is undefined — the locale file registers the CS global only.
  </acceptance_criteria>
  <done>window.HELP_CONTENT_CS exists as a full Czech (formal), EN-structurally-identical help corpus passing the cs integrity gate; the full per-locale help gate is now green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help-content-cs.js strings → help page DOM | Executable JS shipped to clients; strings flow through help.js textContent render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-12 | Tampering | clinical pacient/léčba leak into Czech copy | medium | mitigate | Per-locale forbidden-term regex (Plan 01) + native-speaker gate (Plan 09); CS accepted-risk means agent gate is the ceiling |
| T-42.1-13 | Tampering | control/emoji injection in Czech strings | low | mitigate | Integrity gate no-emoji + control-char rejection; textContent-only render path |
| T-42.1-14 | Tampering | {ui:} token translated/broken | low | mitigate | Integrity gate asserts EN {ui:} tokens present byte-identical + resolving against I18N.cs |
</threat_model>

<verification>
- tests/42_1-help-integrity.test.js passes for cs (and now for all three locales).
- `node tests/run-all.js` shows no new failures attributable to help-content-cs.js.
</verification>

<success_criteria>
A Czech (formal) help corpus, structurally EN-identical, token-faithful, passing the cs integrity gate; the per-locale help integrity gate is fully green.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-05-SUMMARY.md` when done.
</output>
