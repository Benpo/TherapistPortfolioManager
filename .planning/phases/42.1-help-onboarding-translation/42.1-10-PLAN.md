---
phase: 42.1-help-onboarding-translation
plan: 10
type: execute
wave: 4
depends_on: ["42.1-08", "42.1-09"]
files_modified:
  - assets/help-content-he.js
  - assets/changelog-content-he.js
  - assets/i18n-he.js
autonomous: false
requirements: [L10N-01]
user_setup: []
must_haves:
  truths:
    - "The HE help page and changelog page have been rendered in a real browser (headless Chrome + WebKit) with real Hebrew content in RTL, and verified: Hebrew renders, dir=rtl, no bidi breakage on mixed Latin/Hebrew lines, and the tour help-step \\n\\n renders as two paragraphs."
    - "Sapir has read the Hebrew (help + changelog + welcome + tour) and either approved it or her corrections have been applied."
    - "After any corrections, the full suite is green and the browser render re-verified."
  artifacts:
    - "screenshots/render evidence for the HE help + changelog pages (scratchpad or phase dir)"
  key_links:
    - "This is the locked pipeline's terminal human gate — Sapir's Hebrew read is BLOCKING and must not be skipped or softened."
    - "UI-SPEC was intentionally skipped (reuse-only); this real-browser render verification is the agreed exchange (first time non-EN content renders on these pages; jsdom has no layout)."
---

<objective>
Close the phase with the two mandated convergence gates: (1) real-browser render verification of the Hebrew help page and changelog page (headless Chrome screenshots + Playwright WebKit probe) — the agreed exchange for skipping UI-SPEC, since this is the first time non-EN content renders on these pages and jsdom has no layout; and (2) Sapir's Hebrew read — a BLOCKING human checkpoint from the locked pipeline. Apply any corrections Sapir raises, then re-verify (full suite + render).

Purpose: L10N-01 — the Hebrew-first practitioner's surfaces are verified natively in a real RTL browser and signed off by a human native speaker.
Output: render evidence, Sapir sign-off, and any correction commits.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan produces render evidence (screenshots) and may apply small correction edits to the HE files; it introduces no new code symbols.

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Corrections are limited to Hebrew prose refinements Sapir raises — do NOT change structure/tokens/anchors/the \n\n break; do NOT expand scope to DE/CS re-review (CS is accepted-risk; DE passed the agent gate); do NOT bump APP_VERSION here (the semver bump is decided at phase close per project convention, not inside these plans). Pre-v1.3 ASSUMED strings remain OUT.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-browser render verification of the HE help + changelog pages (headless Chrome + WebKit)</name>
  <files>help.html, changelog.html, assets/help.js, assets/whats-new.js</files>
  <read_first>
    - help.html / changelog.html (the pages under test; the script-tag load order from Plan 08)
    - assets/help.js (localeSections() merge — how App.getLanguage() selects HE)
    - assets/whats-new.js (locale-aware entries())
    - .planning/STATE.md (memory references: headless-Chrome UI verification harness + the ~500px Chrome min-viewport crop caveat; Playwright WebKit probe for Safari-only bugs; RTL logical-vs-physical coordinate pitfalls)
    - .planning/phases/42.1-help-onboarding-translation/42.1-VALIDATION.md (Manual-Only Verifications — real-browser render requirement)
  </read_first>
  <action>
    Serve the repo over a local static HTTP server and drive it with a real browser engine (headless Chrome for screenshots; Playwright WebKit for the Safari-engine probe, per the project precedent that Chromium-only gates miss WebKit-only bugs). Before loading, set localStorage portfolioLang='he' (plus any license/terms gate keys the pages require to render) so App.getLanguage() resolves Hebrew. Load /help and /changelog and assert, programmatically where possible and via screenshot otherwise:
    (a) the rendered help page shows Hebrew section/topic titles and body prose (not EN), and document/root dir is rtl;
    (b) the changelog page shows the Hebrew full-history entries with localized month words, and dir is rtl;
    (c) mixed Latin/Hebrew lines (e.g. "Sessions Garden", "PDF", "חומת הלב") do not bidi-flip incorrectly — spot-check with getBoundingClientRect physical coordinates (RTL logical-vs-physical pitfall);
    (d) the tour help-step body renders its \n\n as two visible paragraphs (white-space: pre-line) — trigger the tour or render that step to confirm the first-ever non-EN pre-line render.
    Capture screenshots to the scratchpad (or phase dir) as evidence for Sapir's checkpoint. Record any rendering defect found and FIX it (the fix may touch assets/help-content-he.js / changelog-content-he.js / i18n-he.js or, if a wiring defect, coordinate a fix) before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>node tests/run-all.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Screenshots exist for the HE /help and /changelog pages showing Hebrew content in RTL (evidence attached/linked in the SUMMARY).
    - A programmatic assertion confirms document dir=rtl and at least one known Hebrew string (e.g. חומת הלב or a section title) is present in the rendered DOM of each page.
    - The tour help-step \n\n renders as two paragraphs (screenshot or measured two-block layout).
    - No incorrect bidi flip on a mixed Latin/Hebrew line (spot-checked); any defect found is fixed and re-verified.
    - `node tests/run-all.js` full suite green (including after any render-defect fix).
  </acceptance_criteria>
  <done>The HE help + changelog pages are proven to render natively in RTL in a real Chromium and WebKit engine, with the \n\n paragraph break visible — evidence ready for Sapir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sapir's Hebrew read (BLOCKING human checkpoint)</name>
  <files>assets/i18n-he.js, assets/help-content-he.js, assets/changelog-content-he.js (Hebrew surfaces under human review)</files>
  <action>BLOCKING human checkpoint — this is the locked pipeline's terminal human gate for Hebrew and must not be skipped, softened, or auto-approved. Present the rendered Hebrew surfaces (help page, changelog page, What's-New popup, welcome overlay, guided tour) to Sapir using the render evidence from Task 1 and the live pages, and pause until she signs off. Do not proceed to Task 3 until a resume signal is received.</action>
  <what-built>
    The Hebrew help center, changelog page, What's-New popup, welcome overlay, and guided tour now read in Hebrew (RTL). The prior tasks passed the automated per-locale integrity + parity gates, the native-speaker register gate, and a real-browser render verification (screenshots attached in the SUMMARY / scratchpad).
  </what-built>
  <how-to-verify>
    1. Open the Hebrew help page (set language to Hebrew, open Help from any page) and read several sections — the prose should read as natural Hebrew in the house register (noun/infinitive, no imperative "you"), terminology consistent: לקוח for client, חומת הלב for Heart-Wall, "Sessions Garden" left in Latin.
    2. Open the Changelog page and read the full history (v1.3, v1.2, v1.1, v1.0) — native Hebrew, localized month words, calm benefit-led tone, no business-internal/commercial framing, no clinical patient/treatment wording.
    3. Trigger the What's-New popup and confirm its highlights are Hebrew (not English).
    4. Open the welcome overlay (onboarding) and read both paragraphs (value-first P1 + privacy P2) — natural Hebrew.
    5. Run the guided tour and read the steps — especially the final "You're ready / Help" step, which should show two paragraphs.
    Note anything that reads unnaturally, uses the wrong term, or feels off-register.
  </how-to-verify>
  <resume-signal>Type "approved" to sign off the Hebrew, or list the specific corrections (which page/section, what to change).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply Sapir's corrections (if any) and re-verify</name>
  <files>assets/help-content-he.js, assets/changelog-content-he.js, assets/i18n-he.js</files>
  <read_first>
    - assets/help-content-he.js / assets/changelog-content-he.js / assets/i18n-he.js (the HE files to correct)
    - tests/42_1-help-integrity.test.js / tests/42_1-changelog-integrity-locale.test.js / tests/40-i18n-parity.test.js / tests/41-tour-i18n-parity.test.js (gates that must stay green)
  </read_first>
  <action>
    If Sapir approved with no changes, record the sign-off in the SUMMARY and skip edits. If she raised corrections, apply each to the relevant HE file(s) — refining only the Hebrew prose she flagged, never touching structure, {ui:}/placeholder tokens, or the \n\n break. Re-run the HE integrity + parity gates and the full suite; if a correction touched a rendered surface, re-capture the render evidence for the changed page. Record in the SUMMARY: Sapir's verdict, the list of corrections applied (or "none"), and confirmation the full suite is green.
  </action>
  <verify>
    <automated>node tests/42_1-help-integrity.test.js; node tests/42_1-changelog-integrity-locale.test.js; node tests/40-i18n-parity.test.js; node tests/41-tour-i18n-parity.test.js; node tests/run-all.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records Sapir's sign-off (approved) and the corrections applied (or explicitly "none").
    - After any corrections, all HE integrity + parity gates and `node tests/run-all.js` are green.
    - No structural/token/\n\n changes were introduced by the corrections (gates prove this).
  </acceptance_criteria>
  <done>Sapir's Hebrew read is signed off, corrections applied and re-verified, full suite green — the phase's human gate is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| first non-EN render → real browser | jsdom has no layout; RTL/bidi + pre-line rendering only observable in a real engine |
| human sign-off → ship | Sapir's read is the human backstop for Hebrew copy quality before Phase 43 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-27 | Tampering | RTL bidi flip / broken pre-line render only visible in a real browser | medium | mitigate | Headless Chrome + WebKit render verification (Task 1) with physical-coordinate bidi spot-check |
| T-42.1-28 | Repudiation | Hebrew ships without a human native-speaker sign-off | high | mitigate | BLOCKING human-verify checkpoint (Task 2) — never auto-approved; correction loop (Task 3) |
| T-42.1-29 | Tampering | a correction drops a token / the \n\n break | low | mitigate | Task 3 re-runs integrity + parity gates after every correction |
</threat_model>

<verification>
- Render evidence (Chromium + WebKit) for HE /help and /changelog attached.
- Sapir sign-off recorded; corrections (if any) applied and re-verified.
- `node tests/run-all.js` full suite green.
</verification>

<success_criteria>
The Hebrew surfaces render natively and correctly in a real RTL browser, Sapir has read and signed off (with any corrections applied and re-verified), and the full suite is green — L10N-01 is complete and the translated corpus is ready for Phase 43's docs hard-gate.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-10-SUMMARY.md` when done.
</output>
