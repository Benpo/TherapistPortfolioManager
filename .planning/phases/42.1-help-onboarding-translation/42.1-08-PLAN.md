---
phase: 42.1-help-onboarding-translation
plan: 08
type: execute
wave: 3
depends_on: ["42.1-03", "42.1-04", "42.1-05", "42.1-06", "42.1-07"]
files_modified:
  - assets/help.js
  - assets/help.css
  - assets/whats-new.js
  - assets/tour.js
  - help.html
  - changelog.html
  - index.html
  - sessions.html
  - add-client.html
  - add-session.html
  - settings.html
  - reporting.html
  - report.html
  - sw.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "The help page renders the active locale's content via a new localeSections() merge with per-section EN fallback; switching language re-renders natively (already wired to app:language)."
    - "The help BODY prose renders RTL under [dir=rtl] for translated sections — the interim help.css [dir=rtl]→LTR suppression is removed/re-keyed off a per-section EN-fallback marker so only EN-fallback bodies stay LTR (BLOCKER 2)."
    - "The What's-New popup renders the active locale's highlights via a locale-aware entries() with per-entry EN fallback (Pitfall 1 closed)."
    - "The 3 changelog-content-{he,de,cs}.js siblings load on ALL nine pages that load whats-new.js, so the popup renders native content wherever it fires — not just help/changelog (BLOCKER 1)."
    - "All 6 new locale content files load as static <script> tags and are precached in sw.js — help/changelog work offline in every locale."
    - "The v1.3 help chrome (help.js LABELS + help.html shell strings + AT aria-labels) and the tour fallback screen names resolve through the new help.chrome.* / help.tour.screen.* keys via App.t() — a Hebrew user sees Hebrew chrome, no hardcoded English (BLOCKER 3 + WARNING 3)."
  artifacts:
    - assets/help.js
    - assets/help.css
    - assets/whats-new.js
    - assets/tour.js
    - help.html
    - changelog.html
    - index.html
    - sessions.html
    - add-client.html
    - add-session.html
    - settings.html
    - reporting.html
    - report.html
    - sw.js
  key_links:
    - "localeSections() keys on section id; entries() keys on version — both mirror changelog.js localeEntries() with EN fallback (D-16)."
    - "localeSections() sets a per-section EN-fallback marker that help.css keys the [dir=rtl] LTR override off of (BLOCKER 2 depends on this marker)."
    - "The nine whats-new.js pages must each load CHANGELOG_CONTENT_{HE,DE,CS} for the popup entries() locale lookup to resolve (BLOCKER 1)."
    - "help.chrome.* / help.tour.screen.* keys are authored in Plan 07 (wave 2); this plan only WIRES them (App.t() / data-i18n) — key names are stable regardless of Plan 09's parallel value refinements."
    - "sw.js PRECACHE_URLS must list all 6 files; CACHE_NAME auto-rolls via INTEGRITY_TOKEN (NO manual bump)."
---

<objective>
Wire the loaders, offline delivery, RTL rendering, and chrome i18n so the translated content actually reaches users. Add help.js localeSections() (mirroring changelog.js localeEntries(), keyed on section id) with a per-section EN-fallback marker, source render() from it, and re-key the interim help.css [dir=rtl]→LTR help-body suppression off that marker so translated prose renders RTL (BLOCKER 2); make whats-new.js entries() locale-aware with per-entry EN fallback (closing Pitfall 1); add the changelog locale siblings as static <script> tags on ALL nine pages that load whats-new.js so the popup renders native wherever it fires (BLOCKER 1), add the help-content siblings to help.html, and add all 6 to sw.js PRECACHE_URLS for offline; and convert the hardcoded v1.3 help chrome (help.js LABELS + help.html shell + AT aria-labels) and the tour fallback screen names to the new help.chrome.* / help.tour.screen.* keys via App.t()/data-i18n (BLOCKER 3 + WARNING 3). The keys themselves are authored in Plan 07 (wave 2); this plan only wires them.

Purpose: L10N-01 — the help page, changelog page, What's-New popup, help chrome, and tour fallback serve native content, RTL-correct, online and offline.
Output: locale-aware help + popup loaders, the RTL css re-key, per-page popup-locale script tags, precache entries, and chrome/screen-name i18n wiring.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan adds the new function `localeSections()` (with an EN-fallback marker) in assets/help.js and rewrites `entries()` in assets/whats-new.js to be locale-aware; re-keys the help.css [dir=rtl] override; and wires help.js/help.html/tour.js to the Plan-07 chrome/screen keys. The `localeSections()` + `entries()` symbols are new/changed this phase (not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Do NOT author or reword any i18n VALUE here — the help.chrome.* / help.tour.screen.* keys are authored in Plan 07; this plan only references them by key name (if a key is missing, that is a Plan 07 gap, not a reason to inline English). Do NOT introduce innerHTML for RTL or any reason (textContent render path is the XSS boundary T-39-06/T-42-01). Do NOT fork a popup-only changelog data file (CHLG-03 — one source drives page + popup). Do NOT manually bump CACHE_NAME (it auto-rolls via INTEGRITY_TOKEN). Do NOT add dynamic import/fetch — static <script> tags mirror the always-loaded i18n-*.js.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/changelog.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localeSections() (with EN-fallback marker) to help.js, source render() from it, and re-key the help.css [dir=rtl] override (BLOCKER 2)</name>
  <files>assets/help.js, assets/help.css</files>
  <read_first>
    - assets/help.js (the EN-hardwired read at render() ~lines 349-351 `var sections = window.HELP_CONTENT_EN`; buildCard() ~144-184 where the card + card-body are built; the {ui:} resolver interpolateUiLabels ~84-89; the app:language re-render listener ~455-463; any existing lang() helper)
    - assets/changelog.js (the canonical localeEntries() merge lines 59-67 and lang() helper lines 32-36 — the exact shape to mirror, keyed on version → adapt to key on section id)
    - assets/help.css (the interim [dir=rtl] help-body LTR suppression at lines 370-382 — its own comment says to re-key off the loaded content language when translated content lands; the `.card-body`, `.card-head h2`, `.page-head > p` selectors)
    - assets/help-content-he.js / -de.js / -cs.js (the locale globals now registered as window.HELP_CONTENT_HE/DE/CS)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (help.js loader section — the localeSections() body keyed on s.id with per-section EN fallback)
  </read_first>
  <action>
    (1) localeSections() + render source: In assets/help.js add a localeSections() helper modeled exactly on changelog.js localeEntries() (lines 59-67) but keyed on section `id` instead of `version`: read window.HELP_CONTENT_EN as the canonical, always-complete ordered array; resolve the active language via App.getLanguage() (add/reuse a lang() helper matching changelog.js:32-36); look up window["HELP_CONTENT_" + lang.toUpperCase()]; if that is not an array, return EN; otherwise map each EN section to its same-id localized counterpart when present, else keep the EN section (per-section EN fallback). Replace the direct `var sections = window.HELP_CONTENT_EN` at the top of render() with `var sections = localeSections()`. Do NOT change the resolver, the search re-apply, or the app:language listener.
    (2) EN-fallback marker: so the CSS can tell a translated card-body from an EN-fallback one, mark each rendered card (or its card-body) when the section it rendered came from the EN fallback rather than the active locale array — e.g. in buildCard() add a class like `is-en-fallback` when this section's id was NOT present in the active locale array (compute from the same merge: a section is fallback iff the active locale is non-EN AND no same-id entry existed in the locale array). When the active language is EN, the marker is irrelevant (dir is ltr anyway). Pick the exact class/attribute after reading buildCard + help.css.
    (3) help.css re-key (BLOCKER 2): remove the blanket `[dir="rtl"] .help-root .card-body, [dir="rtl"] .help-root .card-head h2, [dir="rtl"] .help-root .page-head > p { direction: ltr; text-align: start; }` override so translated card bodies, card headings, and the (now-translated) page-head intro render RTL under [dir=rtl]. Replace it with a NARROW rule that forces LTR ONLY for EN-fallback bodies keyed off the Task-1(2) marker (e.g. `[dir="rtl"] .help-root .help-card.is-en-fallback .card-body { direction: ltr; text-align: start; }`) — so a per-section EN-fallback still reads correctly LTR while native sections read RTL. Update the interim comment to describe the shipped mechanism.
  </action>
  <verify>
    <automated>node tests/39-help-render.test.js; node tests/39-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "localeSections" assets/help.js` ≥ 2 (definition + call site in render()).
    - In render(), the sections array is assigned from localeSections() (not the direct EN global); render + integrity tests stay green with EN behavior preserved.
    - help.js sets a per-section EN-fallback marker (class/attribute) on cards that rendered EN-fallback content; `grep -c "is-en-fallback" assets/help.js` ≥ 1 (or the chosen marker name, matched in help.css too).
    - assets/help.css no longer forces `.card-body` / `.card-head h2` / `.page-head > p` to LTR for ALL [dir=rtl] — the LTR override is now scoped to the EN-fallback marker only (`grep -n "direction: ltr" assets/help.css` shows the override guarded by the fallback marker, not a blanket [dir=rtl] .card-body rule).
    - tests/39-help-render.test.js and tests/39-help-integrity.test.js stay green (EN behavior unchanged when no locale global is present). The RTL-renders-correctly behavior is proven in a real browser at Plan 10 Task 1 (getComputedStyle direction === 'rtl' on a Hebrew card body).
  </acceptance_criteria>
  <done>help.js render() sources locale-merged sections with an EN-fallback marker; the help.css override no longer forces translated Hebrew prose LTR — native sections render RTL, EN-fallback sections stay LTR.</done>
</task>

<task type="auto">
  <name>Task 2: Make assets/whats-new.js entries() locale-aware (close Pitfall 1)</name>
  <files>assets/whats-new.js</files>
  <read_first>
    - assets/whats-new.js (the EN-only entries() ~lines 73-77; entryFor(v) ~78-85 which iterates entries(); lang resolution via lsGet('portfolioLang') in t() ~53; highlights render via textContent ~143-148)
    - assets/changelog.js (localeEntries() lines 59-67 — the per-entry version-keyed EN fallback to mirror)
    - assets/changelog-content-he.js / -de.js / -cs.js (the locale globals CHANGELOG_CONTENT_HE/DE/CS)
    - tests/42-whats-new-gating.test.js (the Plan 02 Task 3 localized-highlight RED test seeding CHANGELOG_CONTENT_HE + portfolioLang='he')
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (whats-new.js section — the locale-aware entries() body)
  </read_first>
  <action>
    Rewrite assets/whats-new.js entries() to be locale-aware with per-entry EN fallback (mirror changelog.js localeEntries()): resolve the active language from lsGet('portfolioLang') (falling back to window.I18N_DEFAULT then 'en') the way this file's own t() already does; read window['CHANGELOG_CONTENT_' + lang.toUpperCase()] and window.CHANGELOG_CONTENT_EN; use EN as the canonical base array; when a locale array exists, map each EN entry to its same-version localized entry when present, else keep EN. entryFor(v) already iterates entries() and inherits the fix with no change. Do NOT change the render path (highlights stay textContent). Do NOT fork a popup-only data file.
  </action>
  <verify>
    <automated>node tests/42-whats-new-gating.test.js; node tests/42-whats-new-dismiss.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42-whats-new-gating.test.js PASSES including the new localized-highlight test (the popup renders the Hebrew highlight when portfolioLang='he'); the RED gate from Plan 02 Task 3 flips green.
    - Existing gating tests (T-42-V1/V2/V3) and tests/42-whats-new-dismiss.test.js stay green (EN fallback preserves behavior when no locale global).
    - `grep -c "CHANGELOG_CONTENT_' + " assets/whats-new.js` ≥ 1 (locale lookup present) and the entries() body references CHANGELOG_CONTENT_EN as the fallback base.
    - No innerHTML introduced: `grep -c "innerHTML" assets/whats-new.js` is unchanged from HEAD (still 0 for content).
  </acceptance_criteria>
  <done>The What's-New popup reads the active locale's highlights with EN fallback; Pitfall 1 is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add locale <script> tags on all nine whats-new.js pages (BLOCKER 1) + help-content siblings on help.html + 6 sw.js precache entries</name>
  <files>help.html, changelog.html, index.html, sessions.html, add-client.html, add-session.html, settings.html, reporting.html, report.html, sw.js</files>
  <read_first>
    - help.html (content script tags — changelog-content-en.js at line 139, help-content-en.js at line 150; the 4-locale i18n block lines 122-125 as the ordering precedent)
    - changelog.html (changelog-content-en.js at line 96; i18n block lines 72-75)
    - index.html:366, sessions.html:122, add-client.html:201, add-session.html:597, settings.html:380, reporting.html:109, report.html:114 (each has the changelog-content-en.js tag before whats-new.js — the anchor for the 3 locale siblings)
    - sw.js (PRECACHE_URLS with '/assets/help-content-en.js' ~line 95 and '/assets/changelog-content-en.js' ~line 111; CACHE_NAME derivation from INTEGRITY_TOKEN ~line 26)
    - assets/whats-new.js (entries() reads window['CHANGELOG_CONTENT_' + lang] after Task 2 — so the locale globals must be LOADED on every page the popup can fire on)
    - tests/39-help-precache.test.js / tests/42-precache.test.js / tests/42-whats-new-gating.test.js (the Plan 02 RED precache + cross-page shape assertions)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (html + sw.js sections — EN loads first, locales after)
  </read_first>
  <action>
    (1) Popup-locale siblings on ALL NINE whats-new.js pages (BLOCKER 1 — the popup is an AttentionCoordinator surface that fires on whichever chrome page opens first; without the locale globals a Hebrew user on index.html gets an English popup): on EACH of index.html, sessions.html, add-client.html, add-session.html, settings.html, reporting.html, report.html, help.html, changelog.html, immediately after that page's existing `./assets/changelog-content-en.js` tag add three static tags `./assets/changelog-content-he.js`, `-de.js`, `-cs.js`. EN must remain loaded before its locale siblings (canonical fallback); relative order among locales is free. This mirrors the always-loaded 4-locale i18n-*.js precedent.
    (2) help-content siblings on help.html: after the existing help-content-en.js tag (line 150) add ./assets/help-content-he.js, -de.js, -cs.js (help.html is the only page that loads the help corpus).
    (3) sw.js: add all 6 new files to PRECACHE_URLS — the 3 help-content siblings alongside '/assets/help-content-en.js' and the 3 changelog-content siblings alongside '/assets/changelog-content-en.js', using the '/assets/...' path form the existing entries use. Do NOT touch CACHE_NAME (it auto-rolls via the deploy-stamped INTEGRITY_TOKEN — no manual bump).
    This is mechanical mirror work (the same three-line insertion per page); the reasoning cost is low despite the file count.
  </action>
  <verify>
    <automated>node tests/39-help-precache.test.js; node tests/42-precache.test.js; node tests/42-whats-new-gating.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 02 cross-page shape test (tests/42-whats-new-gating.test.js) PASSES: all nine whats-new.js pages reference changelog-content-he.js/-de.js/-cs.js.
    - tests/39-help-precache.test.js and tests/42-precache.test.js both PASS (the 6 files present in the isolated PRECACHE_URLS region; URLS-vs-HTML separation preserved).
    - `grep -lc "changelog-content-he.js" index.html sessions.html add-client.html add-session.html settings.html reporting.html report.html help.html changelog.html` shows all nine; and de/cs siblings likewise present on each.
    - `grep -c "help-content-he.js" help.html` ≥ 1 (and de/cs).
    - `grep -c "help-content-he.js\|help-content-de.js\|help-content-cs.js\|changelog-content-he.js\|changelog-content-de.js\|changelog-content-cs.js" sw.js` == 6.
    - CACHE_NAME line in sw.js is unchanged from HEAD (`git diff sw.js` shows no CACHE_NAME edit).
  </acceptance_criteria>
  <done>The changelog locale siblings load on every page the popup can fire on (native popup everywhere), the help corpus siblings load on help.html, and all 6 files are precached — help + changelog + popup render native content offline in every locale.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the v1.3 help chrome + tour screen names to the Plan-07 keys via App.t()/data-i18n (BLOCKER 3 + WARNING 3)</name>
  <files>assets/help.js, help.html, assets/tour.js</files>
  <read_first>
    - assets/help.js (LABELS object 52-58 + its six render sites: startHere 159, sessionLoop 374, technicalBits 388, techBand aria 414, techBand label 416, spine 408; the "Show topics" aria at 197; the existing `t()` helper 72-74; render() already sets helpTitle + search placeholders via t() at 358-364 and re-renders on app:language 455-463)
    - help.html (intro <p> 61; jump-strip <summary> 88 with its ▾ <span aria-hidden>; contact band 100-103; aria-labels 68 / 71 / 85 / 89)
    - assets/tour.js (STEPS screenName literals 116-127; the fallback loc line `t('help.tour.fallbackBody').replace('{screen}', step.screenName)` at 446; the app:language re-render guard)
    - assets/app.js (applyTranslations 23-32 — data-i18n sets textContent, data-i18n-placeholder sets placeholder; NO aria-label handling → aria-labels are set in JS via App.t(), the app-wide pattern e.g. overview.js:766)
    - assets/i18n-en.js (the help.chrome.* / help.tour.screen.* keys authored by Plan 07 — reference by name only)
  </read_first>
  <action>
    Reference Plan-07 keys ONLY (do not author/reword values). Three wirings:
    (1) help.js LABELS → t(): replace each LABELS.* usage with a t("help.chrome.…") call at the render site — startHere→help.chrome.startHere, sessionLoop→help.chrome.sessionLoop, technicalBits→help.chrome.technicalBits (both the rail label AND the tech-band aria-label), spine→help.chrome.spine, techBand→help.chrome.techBand; and the rail-toggle aria-label "Show topics" (line 197)→t("help.chrome.ariaShowTopics"). Remove the now-dead LABELS object (or repoint it to t() lookups). These already re-render on app:language because render() is the re-render path.
    (2) help.js aria-labels on the static shell: in render(), set the aria-labels that applyTranslations cannot (they are attributes): #rail nav aria-label→t("help.chrome.ariaSections"); the two search inputs (searchDesk/searchMob) aria-label→t("help.chrome.ariaSearch") — extend the existing placeholder-setting loop at 361-364; the #jumpNav aria-label→t("help.chrome.jumpToSection"). (Setting them in render() means they flip live on language switch, same as the placeholders.)
    (3) help.html static visible chrome → data-i18n (app.js applyTranslations sets textContent + re-applies on language switch): intro <p> (61)→data-i18n="help.chrome.intro"; contact <h2> (100)→data-i18n="help.chrome.contactTitle"; contact <p> (101)→data-i18n="help.chrome.contactBody". For the <summary> (88), wrap the label text in a `<span data-i18n="help.chrome.jumpToSection">` and LEAVE the ▾ `<span aria-hidden>` a separate sibling (data-i18n replaces textContent, so the text must be its own span to keep the caret). For the report-hint <p> (103), apply data-i18n="help.chrome.reportHint" to the whole <p>: this renders the hint as one translatable sentence and INTENTIONALLY drops the inner `<strong>` emphasis (a translatable whole sentence beats a fragmented one whose word order breaks in RTL; the EN text stays byte-identical). Do NOT also i18n the "What's new" see-also link (line 111) — it already carries data-i18n="whatsNew.menuRow".
    (4) tour.js screen names → t(): add a small lookup mapping each English screenName ('Overview','Settings','Session','Sessions') to its key (help.tour.screen.overview/settings/session/sessions), and at line 446 substitute `t(lookup[step.screenName] || '')` (falling back to the literal step.screenName if the lookup/key is missing) into {screen}. Leave the STEPS screenName literals as-is (they are the lookup input). The tour already re-renders on app:language.
    No innerHTML anywhere; all dynamic text via textContent / setAttribute / data-i18n.
  </action>
  <verify>
    <automated>node tests/39-help-render.test.js; node tests/39-help-integrity.test.js; node tests/40-i18n-parity.test.js; node tests/41-tour-i18n-parity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - help.js no longer renders any hardcoded English chrome string for the five LABELS + "Show topics": `grep -c "help.chrome." assets/help.js` ≥ 6 and the render sites call t() (the LABELS object is removed or repointed to t()).
    - help.html carries the new data-i18n hooks: `grep -c "help.chrome.intro\|help.chrome.contactTitle\|help.chrome.contactBody\|help.chrome.jumpToSection\|help.chrome.reportHint" help.html` ≥ 5; the ▾ caret span survives (jump-strip summary still shows the caret).
    - The three static aria-labels are set from t() in help.js render (`grep -c "help.chrome.ariaSections\|help.chrome.ariaSearch\|help.chrome.jumpToSection" assets/help.js` ≥ 3).
    - tour.js resolves screenName through the keys: `grep -c "help.tour.screen." assets/tour.js` ≥ 4 and the {screen} substitution uses t() with a literal fallback.
    - render + integrity + 40/41 parity gates stay green; no innerHTML introduced (`grep -c "innerHTML" assets/help.js` unchanged from HEAD except the pre-existing compile-time SVG usages).
    - Full RTL/native rendering of this chrome is proven in a real browser at Plan 10 Task 1.
  </acceptance_criteria>
  <done>Every v1.3 help-page chrome string, AT aria-label, and tour fallback screen name resolves through the localized keys — a Hebrew user sees Hebrew chrome and a Hebrew screen name mid-sentence, with EN fallback intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale global → help/popup render | localeSections()/entries() select which locale array renders; a broken merge could leak wrong-locale or crash |
| page `<script>` tags → popup locale data | A page missing the changelog locale sibling makes the popup render English regardless of the entries() fix (BLOCKER 1) |
| EN-fallback marker → RTL css | The css keys LTR-only-for-fallback off the marker; a wrong marker flips a native section LTR or an EN-fallback section RTL (BLOCKER 2) |
| hardcoded chrome string → user | Any chrome string left hardcoded ships English to a Hebrew user (BLOCKER 3 / WARNING 3) |
| new content file → service worker precache | Missing precache entry breaks offline help/changelog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-21 | Tampering | innerHTML introduced for RTL/formatting | high | mitigate | Task rule forbids innerHTML; acceptance asserts innerHTML count unchanged (content stays textContent) (T-39-06/T-42-01) |
| T-42.1-22 | Denial of Service (offline) | file missing from PRECACHE_URLS | medium | mitigate | Precache gates (Plan 02) require all 6; verified green here |
| T-42.1-23 | Tampering | merge returns wrong-locale/undefined → render break | low | mitigate | Per-section/per-entry EN fallback (mirrors tested changelog.js); render + integrity tests stay green |
| T-42.1-30 | Tampering | popup ships English on a page missing the locale `<script>` tag (BLOCKER 1) | high | mitigate | Task 3 adds siblings on all nine pages; Plan 02 cross-page shape gate proves it |
| T-42.1-31 | Tampering | Hebrew help prose forced LTR by the interim css override (BLOCKER 2) | high | mitigate | Task 1 re-keys the override to the EN-fallback marker; Plan 10 asserts getComputedStyle direction==='rtl' on a Hebrew card body |
| T-42.1-32 | Tampering | a v1.3 chrome / AT aria / tour screen-name string left hardcoded English (BLOCKER 3 / WARNING 3) | high | mitigate | Task 4 wires every one to a help.chrome.*/help.tour.screen.* key; 40-parity + render gates + Plan 10 render prove native |
</threat_model>

<verification>
- tests/39-help-render.test.js, 39-help-integrity.test.js, 40-i18n-parity.test.js, 41-tour-i18n-parity.test.js, 42-whats-new-gating.test.js, 42-whats-new-dismiss.test.js, 39-help-precache.test.js, 42-precache.test.js all green.
- `node tests/run-all.js` — full suite green (all previously-RED Wave 1 gates now flip green with content + wiring in place).
</verification>

<success_criteria>
The help page, changelog page, and What's-New popup serve locale-native content online and offline on every page the popup can fire on; the help chrome, AT aria-labels, and tour screen names read natively; translated help prose renders RTL (EN-fallback stays LTR) — all with EN fallback and live language switching on the textContent render path, no innerHTML, no manual cache bump.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-08-SUMMARY.md` when done.
</output>
