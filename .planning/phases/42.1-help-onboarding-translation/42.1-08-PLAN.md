---
phase: 42.1-help-onboarding-translation
plan: 08
type: execute
wave: 3
depends_on: ["42.1-03", "42.1-04", "42.1-05", "42.1-06"]
files_modified:
  - assets/help.js
  - assets/whats-new.js
  - help.html
  - changelog.html
  - sw.js
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "The help page renders the active locale's content via a new localeSections() merge with per-section EN fallback; switching language re-renders natively (already wired to app:language)."
    - "The What's-New popup renders the active locale's highlights via a locale-aware entries() with per-entry EN fallback (Pitfall 1 closed)."
    - "All 6 new locale content files load as static <script> tags and are precached in sw.js — help/changelog work offline in every locale."
  artifacts:
    - assets/help.js
    - assets/whats-new.js
    - help.html
    - changelog.html
    - sw.js
  key_links:
    - "localeSections() keys on section id; entries() keys on version — both mirror changelog.js localeEntries() with EN fallback (D-16)."
    - "sw.js PRECACHE_URLS must list all 6 files; CACHE_NAME auto-rolls via INTEGRITY_TOKEN (NO manual bump)."
---

<objective>
Wire the loaders and offline delivery so the translated content actually reaches users. Add help.js localeSections() (mirroring changelog.js localeEntries(), keyed on section id) and source render() from it; make whats-new.js entries() locale-aware with per-entry EN fallback (closing the confirmed Pitfall 1 popup asymmetry); add the 6 new locale content files as static <script> tags in help.html + changelog.html; and add all 6 to sw.js PRECACHE_URLS for offline. No renderer logic changes beyond the merge — the render paths already resolve everything via App.t()/App.getLanguage() and re-render on app:language.

Purpose: L10N-01 — the help page, changelog page, and What's-New popup serve native content, online and offline.
Output: locale-aware help + popup loaders, script tags, and precache entries.
</objective>

## Artifacts this phase produces

See Plan 01's phase-wide list. This plan adds the new function `localeSections()` in assets/help.js and rewrites `entries()` in assets/whats-new.js to be locale-aware; both are new/changed symbols this phase introduces (not drift).

## Out of scope (NOT-list)

Same NOT-list as Plan 01. Do NOT introduce innerHTML for RTL or any reason (textContent render path is the XSS boundary T-39-06/T-42-01). Do NOT fork a popup-only changelog data file (CHLG-03 — one source drives page + popup). Do NOT manually bump CACHE_NAME (it auto-rolls via INTEGRITY_TOKEN). Do NOT add dynamic import/fetch — static <script> tags mirror the always-loaded i18n-*.js.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-help-onboarding-translation/42.1-RESEARCH.md
@.planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md
@assets/changelog.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localeSections() to assets/help.js and source render() from it</name>
  <files>assets/help.js</files>
  <read_first>
    - assets/help.js (the EN-hardwired read at render() ~lines 349-351 `var sections = window.HELP_CONTENT_EN`; the {ui:} resolver interpolateUiLabels ~84-89; the app:language re-render listener ~455-463; any existing lang() helper)
    - assets/changelog.js (the canonical localeEntries() merge lines 59-67 and lang() helper lines 32-36 — the exact shape to mirror, keyed on version → adapt to key on section id)
    - assets/help-content-he.js / -de.js / -cs.js (the locale globals now registered as window.HELP_CONTENT_HE/DE/CS)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (help.js loader section — the localeSections() body keyed on s.id with per-section EN fallback)
  </read_first>
  <action>
    In assets/help.js add a localeSections() helper modeled exactly on changelog.js localeEntries() (lines 59-67) but keyed on section `id` instead of `version`: read window.HELP_CONTENT_EN as the canonical, always-complete ordered array; resolve the active language via App.getLanguage() (add/reuse a lang() helper matching changelog.js:32-36); look up window["HELP_CONTENT_" + lang.toUpperCase()]; if that is not an array, return EN; otherwise map each EN section to its same-id localized counterpart when present, else keep the EN section (per-section EN fallback). Replace the direct `var sections = window.HELP_CONTENT_EN` at the top of render() with `var sections = localeSections()`. Do NOT change the resolver, the search re-apply, or the app:language listener — they already work once render() sources from the merge.
  </action>
  <verify>
    <automated>node tests/39-help-render.test.js; node tests/39-help-integrity.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "localeSections" assets/help.js` ≥ 2 (definition + call site in render()).
    - In render(), the sections array is assigned from the new localeSections() helper rather than read directly from the EN global — confirm a localeSections() call exists inside render() and the prior direct EN assignment no longer executes (render + integrity tests stay green with EN behavior preserved).
    - The merge keys on section id and falls back per-section to the EN array (the localeSections body compares section `.id` values and returns the EN section when no locale match exists).
    - tests/39-help-render.test.js and tests/39-help-integrity.test.js stay green (EN behavior unchanged when no locale global is present).
  </acceptance_criteria>
  <done>help.js render() sources locale-merged sections; a Hebrew user sees Hebrew help, with per-section EN fallback and live language switching intact.</done>
</task>

<task type="auto">
  <name>Task 2: Make assets/whats-new.js entries() locale-aware (close Pitfall 1)</name>
  <files>assets/whats-new.js</files>
  <read_first>
    - assets/whats-new.js (the EN-only entries() ~lines 73-77; entryFor(v) ~78-85 which iterates entries(); lang resolution via lsGet('portfolioLang') in t() ~53; highlights render via textContent ~143-148)
    - assets/changelog.js (localeEntries() lines 59-67 — the per-entry version-keyed EN fallback to mirror)
    - assets/changelog-content-he.js / -de.js / -cs.js (the locale globals CHANGELOG_CONTENT_HE/DE/CS)
    - tests/42-whats-new-gating.test.js (the Plan 02 Task 3 localized-highlight RED test seeding CHANGELOG_CONTENT_HE + portfolioLang='he')
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (whats-new.js section — the locale-aware entries() body)
  </read_first>
  <action>
    Rewrite assets/whats-new.js entries() to be locale-aware with per-entry EN fallback (mirror changelog.js localeEntries()): resolve the active language from lsGet('portfolioLang') (falling back to window.I18N_DEFAULT then 'en') the way this file's own t() already does; read window['CHANGELOG_CONTENT_' + lang.toUpperCase()] and window.CHANGELOG_CONTENT_EN; use EN as the canonical base array; when a locale array exists, map each EN entry to its same-version localized entry when present, else keep EN. entryFor(v) already iterates entries() and inherits the fix with no change. Do NOT change the render path (highlights stay textContent). Do NOT fork a popup-only data file.
  </action>
  <verify>
    <automated>node tests/42-whats-new-gating.test.js; node tests/42-whats-new-dismiss.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/42-whats-new-gating.test.js PASSES including the new localized-highlight test (the popup renders the Hebrew highlight when portfolioLang='he'); the RED gate from Plan 02 Task 3 flips green.
    - Existing gating tests (T-42-V1/V2/V3) and tests/42-whats-new-dismiss.test.js stay green (EN fallback preserves behavior when no locale global).
    - `grep -c "CHANGELOG_CONTENT_' + " assets/whats-new.js` ≥ 1 (locale lookup present) and the entries() body references CHANGELOG_CONTENT_EN as the fallback base.
    - No innerHTML introduced: `grep -c "innerHTML" assets/whats-new.js` is unchanged from HEAD (still 0 for content).
  </acceptance_criteria>
  <done>The What's-New popup reads the active locale's highlights with EN fallback; Pitfall 1 is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add 6 locale <script> tags (help.html + changelog.html) and 6 sw.js precache entries</name>
  <files>help.html, changelog.html, sw.js</files>
  <read_first>
    - help.html (content script tags — changelog-content-en.js at line 139, help-content-en.js at line 150; the 4-locale i18n block lines 122-125 as the ordering precedent)
    - changelog.html (changelog-content-en.js at line 96; i18n block lines 72-75)
    - sw.js (PRECACHE_URLS with '/assets/help-content-en.js' ~line 95 and '/assets/changelog-content-en.js' ~line 111; CACHE_NAME derivation from INTEGRITY_TOKEN ~line 26)
    - tests/39-help-precache.test.js / tests/42-precache.test.js (the Plan 02 Task 3 RED precache assertions requiring the 6 files)
    - .planning/phases/42.1-help-onboarding-translation/42.1-PATTERNS.md (html + sw.js sections — EN loads first, locales after)
  </read_first>
  <action>
    (1) help.html: after the existing help-content-en.js tag (line 150) add static <script src> tags for ./assets/help-content-he.js, -de.js, -cs.js; and after the existing changelog-content-en.js tag (line 139) add ./assets/changelog-content-he.js, -de.js, -cs.js (help.html loads changelog content for the What's-New popup — the locale siblings must be added HERE too). EN must remain loaded before its locale siblings (canonical fallback); relative order among locales is free.
    (2) changelog.html: after the changelog-content-en.js tag (line 96) add ./assets/changelog-content-he.js, -de.js, -cs.js.
    (3) sw.js: add all 6 new files to PRECACHE_URLS — the 3 help-content siblings alongside '/assets/help-content-en.js' and the 3 changelog-content siblings alongside '/assets/changelog-content-en.js', using the '/assets/...' path form the existing entries use. Do NOT touch CACHE_NAME (it auto-rolls via the deploy-stamped INTEGRITY_TOKEN — no manual bump).
  </action>
  <verify>
    <automated>node tests/39-help-precache.test.js; node tests/42-precache.test.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/39-help-precache.test.js and tests/42-precache.test.js both PASS (the 6 files are present in the isolated PRECACHE_URLS region; the URLS-vs-HTML region separation is preserved).
    - `grep -c "help-content-he.js" help.html` ≥ 1 (and de/cs); `grep -c "changelog-content-he.js" help.html` ≥ 1 (and de/cs); `grep -c "changelog-content-he.js" changelog.html` ≥ 1 (and de/cs).
    - `grep -c "help-content-he.js\|help-content-de.js\|help-content-cs.js\|changelog-content-he.js\|changelog-content-de.js\|changelog-content-cs.js" sw.js` == 6.
    - CACHE_NAME line in sw.js is unchanged from HEAD (`git diff sw.js` shows no CACHE_NAME edit).
  </acceptance_criteria>
  <done>All 6 locale content files load as static scripts and are precached; help + changelog render native content offline in every locale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale global → help/popup render | localeSections()/entries() select which locale array renders; a broken merge could leak wrong-locale or crash |
| new content file → service worker precache | Missing precache entry breaks offline help/changelog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42.1-21 | Tampering | innerHTML introduced for RTL/formatting | high | mitigate | Task rule forbids innerHTML; acceptance asserts innerHTML count unchanged (0 for content); render stays textContent (T-39-06/T-42-01) |
| T-42.1-22 | Denial of Service (offline) | file missing from PRECACHE_URLS | medium | mitigate | Precache gates (Plan 02) require all 6; verified green here |
| T-42.1-23 | Tampering | merge returns wrong-locale/undefined → render break | low | mitigate | Per-section/per-entry EN fallback (mirrors tested changelog.js); render + integrity tests stay green |
</threat_model>

<verification>
- tests/39-help-render.test.js, 39-help-integrity.test.js, 42-whats-new-gating.test.js, 42-whats-new-dismiss.test.js, 39-help-precache.test.js, 42-precache.test.js all green.
- `node tests/run-all.js` — full suite green (all previously-RED Wave 1 gates now flip green with content + wiring in place).
</verification>

<success_criteria>
The help page, changelog page, and What's-New popup serve locale-native content online and offline, with EN fallback and live language switching, on the existing textContent render path — no innerHTML, no manual cache bump.
</success_criteria>

<output>
Create `.planning/phases/42.1-help-onboarding-translation/42.1-08-SUMMARY.md` when done.
</output>
