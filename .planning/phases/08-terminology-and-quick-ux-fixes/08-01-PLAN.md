---
phase: 08-terminology-and-quick-ux-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-he.js
  - assets/i18n-en.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Every visible string in Hebrew uses לקוח/לקוחות instead of מטופל/מטופלים"
    - "The Hebrew subtitle reads 'תיעוד ומעקב אחר מפגשים אנרגטיים' (not טיפוליים)"
    - "The session form fields read 'נושא למפגש' and 'טכניקות וכלים נוספים' in Hebrew"
    - "The Hebrew quote containing מטפל/מטופל is replaced with a different quote"
    - "EN session.form.additionalTech no longer says 'Treatment'"
    - "No language file uses clinical treatment/patient terminology for UI labels"
  artifacts:
    - path: "assets/i18n-he.js"
      provides: "Hebrew translations with לקוח terminology"
      contains: "לקוח"
    - path: "assets/i18n-en.js"
      provides: "English translations without treatment/patient"
    - path: "assets/i18n-de.js"
      provides: "German translations without Behandlung/Patient"
    - path: "assets/i18n-cs.js"
      provides: "Czech translations without pacient/lecba"
  key_links:
    - from: "assets/i18n-he.js"
      to: "App.t() calls throughout app"
      via: "i18n key lookup"
      pattern: "App\\.t\\("
---

<objective>
Update practitioner-appropriate terminology across all 4 language files, replacing clinical "treatment/patient" language with energy healing practice language ("session/client").

Purpose: The app serves Emotion Code / Body Code practitioners, not medical clinicians. Terminology should reflect energy healing practice, not clinical treatment.
Output: All 4 i18n files updated with correct terminology.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-terminology-and-quick-ux-fixes/08-CONTEXT.md
@assets/i18n-he.js
@assets/i18n-en.js
@assets/i18n-de.js
@assets/i18n-cs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hebrew terminology sweep</name>
  <files>assets/i18n-he.js</files>
  <action>
Replace all ~35 occurrences of מטופל/מטופלים with לקוח/לקוחות throughout the file. Specific locked changes per user decisions:

1. Every "מטופל" (singular) -> "לקוח"
2. Every "מטופלים" (plural) -> "לקוחות"
3. "app.subtitle": change "טיפוליים" to "אנרגטיים" — full value: "תיעוד ומעקב אחר מפגשים אנרגטיים"
4. "session.form.issueName": change "נושא לטיפול" to "נושא למפגש"
5. "session.form.additionalTech": change "טכניקות ושיטות טיפול נוספות" to "טכניקות וכלים נוספים"
6. "client.form.referral.colleague": replace "מטפל" with a neutral term (e.g., "המלצה מקולגה/מטפל.ת אחר.ת" or just "המלצה מקולגה") — use discretion
7. The quote at the end containing "הקשר בין מטפל למטופל הוא קרקע פורייה לצמיחה" — replace with a different inspirational quote about healing/growth that does NOT use מטפל or מטופל. Choose a quote that fits the energy healing / garden growth theme of the app.

Do NOT change legal/disclaimer text in i18n-disclaimer.js — "treatment" is appropriate legal language there.
  </action>
  <verify>
    <automated>grep -c "מטופל" assets/i18n-he.js | grep -q "^0$" && echo "PASS: no מטופל found" || echo "FAIL: מטופל still present"</automated>
  </verify>
  <done>Zero occurrences of מטופל/מטופלים remain in i18n-he.js. Subtitle uses אנרגטיים. Session form fields updated. Quote replaced.</done>
</task>

<task type="auto">
  <name>Task 2: EN/DE/CS terminology review and update</name>
  <files>assets/i18n-en.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
Review all 3 language files for any remaining "treatment" or "patient" references in UI labels (not legal text) and update to non-clinical alternatives.

Known issue in EN:
- "session.form.additionalTech": "Additional Treatment Techniques and Methods" -> "Additional Techniques and Tools" (aligns with Hebrew "טכניקות וכלים נוספים")

For DE: scan for "Behandlung" (treatment), "Patient" (patient) in UI labels. Replace with "Sitzung" (session) / "Klient" (client) equivalents as appropriate. DE already uses "Klient" so changes likely minimal.

For CS: scan for "pacient" (patient), "lecba/osetreni" (treatment) in UI labels. Replace with "klient" / "sezeni" equivalents. CS already uses "Klient" so changes likely minimal.

Guiding principle: avoid implying formal medical/clinical treatment. This is energy healing practice software. Use discretion for natural-sounding translations.
  </action>
  <verify>
    <automated>grep -in "treatment\|patient" assets/i18n-en.js | grep -v "disclaimer\|legal\|i18n-disclaimer" | grep -v "^$" && echo "FAIL: treatment/patient found in EN" || echo "PASS: EN clean"; grep -in "Behandlung\|Patient" assets/i18n-de.js | grep -v "disclaimer\|legal" && echo "FAIL: found in DE" || echo "PASS: DE clean"; grep -in "pacient\|léčba\|ošetření" assets/i18n-cs.js | grep -v "disclaimer\|legal" && echo "FAIL: found in CS" || echo "PASS: CS clean"</automated>
  </verify>
  <done>EN "Additional Treatment Techniques" updated. DE and CS reviewed and any treatment/patient UI labels replaced with non-clinical alternatives.</done>
</task>

</tasks>

<verification>
1. Open app in Hebrew — verify subtitle, nav labels, form fields, client table, toasts, and quotes all use לקוח/מפגש terminology
2. Open app in English — verify no "treatment" or "patient" in UI (except legal disclaimer)
3. Switch to DE and CS — verify no clinical terminology in UI
4. `grep -rn "מטופל" assets/i18n-he.js` returns zero results
</verification>

<success_criteria>
- Zero occurrences of מטופל/מטופלים in i18n-he.js
- Zero occurrences of "treatment"/"patient" in i18n-en.js UI labels
- DE and CS files reviewed and aligned with non-clinical framing
- Hebrew subtitle says "אנרגטיים" not "טיפוליים"
- The problematic Hebrew quote is replaced
</success_criteria>

<output>
After completion, create `.planning/phases/08-terminology-and-quick-ux-fixes/08-01-SUMMARY.md`
</output>
