---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/tokens.css
  - assets/app.css
  - assets/fonts/Rubik-Regular.woff2
  - assets/fonts/Rubik-SemiBold.woff2
  - assets/fonts/Rubik-Bold.woff2
  - index.html
  - add-client.html
  - add-session.html
  - sessions.html
  - reporting.html
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "Every color value in app.css references a CSS variable — no hardcoded hex or rgba colors remain outside tokens.css primitives"
    - "The app loads Rubik from local WOFF2 files with zero external network requests"
    - "All 5 HTML pages link tokens.css before app.css in <head>"
    - "Dark mode activates correctly when data-theme=\"dark\" is set on <html>"
    - "Nunito is fully removed from the codebase — no references in CSS or HTML"
  artifacts:
    - path: "assets/tokens.css"
      provides: "Two-tier CSS token architecture with primitives + semantic tokens + dark mode overrides + @font-face declarations"
      min_lines: 60
    - path: "assets/fonts/Rubik-Regular.woff2"
      provides: "Self-hosted Rubik weight 400"
    - path: "assets/fonts/Rubik-SemiBold.woff2"
      provides: "Self-hosted Rubik weight 600"
    - path: "assets/fonts/Rubik-Bold.woff2"
      provides: "Self-hosted Rubik weight 700"
  key_links:
    - from: "index.html (and 4 other HTML files)"
      to: "assets/tokens.css"
      via: "<link rel=\"stylesheet\"> loaded before app.css"
      pattern: "tokens\\.css"
    - from: "assets/app.css"
      to: "assets/tokens.css"
      via: "CSS var() references for every color"
      pattern: "var\\(--color-"
    - from: "assets/tokens.css"
      to: "assets/fonts/Rubik-*.woff2"
      via: "@font-face url() declarations"
      pattern: "@font-face"
---

<objective>
Create the CSS design token system and self-host the Rubik font, eliminating all hardcoded colors and the Google Fonts CDN dependency.

Purpose: Unblocks Phase 2 visual work. Phase 2 needs token variables to exist before it can apply the garden theme. Self-hosting eliminates the external network request that breaks offline use.

Output: assets/tokens.css (two-tier token system + @font-face), assets/fonts/ (3 WOFF2 files), updated app.css (zero hardcoded colors, zero CDN import), all 5 HTML files linking tokens.css first.
</objective>

<execution_context>
@/Users/sapir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sapir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from existing codebase -->

From assets/app.css (line 1 — the CDN import to REMOVE):
```css
@import url("https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&family=Rubik:wght@400;600;700&display=swap");
```

From assets/app.css :root block (current variable names to RENAME):
```css
:root {
  --page-bg: #f6f3fb;       /* → --color-background */
  --card-bg: #ffffff;       /* → --color-surface */
  --text-main: #2f2d38;     /* → --color-text */
  --text-muted: #5f5c72;    /* → --color-text-muted */
  --accent: #7c66ff;        /* → --color-primary */
  --accent-soft: #efeafe;   /* → --color-primary-soft */
  --success: #2fb37d;       /* → --color-success */
  --danger: #ea4b4b;        /* → --color-danger */
  --shadow-soft: 0 18px 40px rgba(36, 24, 72, 0.08);  /* move to tokens.css */
  --shadow-card: 0 12px 26px rgba(36, 24, 72, 0.1);   /* move to tokens.css */
}
```

From index.html <head> (pattern for all 5 HTML files):
```html
<link rel="stylesheet" href="./assets/app.css" />
```
Must become:
```html
<link rel="stylesheet" href="./assets/tokens.css" />
<link rel="stylesheet" href="./assets/app.css" />
```

Known hardcoded colors in app.css OUTSIDE :root (full list from research):
- #faf8ff, #f2effb (body radial-gradient)
- rgba(86, 78, 120, 0.2), rgba(86, 78, 120, 0.1), rgba(86, 78, 120, 0.08) (borders/backgrounds)
- #f7f4ff (session item background)
- #3a3452 (session meta text)
- rgba(124, 102, 255, 0.35), rgba(124, 102, 255, 0.28) (brand/button shadows)
- rgba(234, 75, 75, 0.28) (danger button shadow)
- #4a4166 (dark purple, multiple components)
- #f3efff (secondary button background)
- #f8f6ff, #efeafe, #ede8ff (toggle/issue/spotlight backgrounds)
- linear-gradient(135deg, #8b7cff, #c6b9ff) (placeholder gradient)
- rgba(26, 24, 35, 0.4) (modal overlay)
- rgba(36, 24, 72, 0.2) (modal card shadow)
- #f5f2ff (issue summary readonly background)
- #fbf9ff (table even row background)
- rgba(86, 78, 120, 0.12), rgba(86, 78, 120, 0.25) (dividers/ghost button border)
- #2e2a38 (toast background)
- #f3f0ff (modal-close and issue-remove background)
- #f0ecff (client avatar background)
- #e6defc (issue-remove hover)
- #f9f7ff (disabled input background)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens.css with two-tier token system, dark mode, and @font-face</name>
  <files>assets/tokens.css, assets/fonts/Rubik-Regular.woff2, assets/fonts/Rubik-SemiBold.woff2, assets/fonts/Rubik-Bold.woff2</files>
  <action>
Download Rubik WOFF2 files (weights 400, 600, 700) from Google Web Fonts Helper at gwfh.mranftl.com/fonts/rubik and save them as:
- assets/fonts/Rubik-Regular.woff2 (weight 400)
- assets/fonts/Rubik-SemiBold.woff2 (weight 600)
- assets/fonts/Rubik-Bold.woff2 (weight 700)

Create assets/tokens.css with this exact structure (order matters: @font-face first, then primitives, then semantics, then dark mode):

```css
/* === FONT FACE === */
@font-face {
  font-family: 'Rubik';
  src: url('./fonts/Rubik-Regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'Rubik';
  src: url('./fonts/Rubik-SemiBold.woff2') format('woff2');
  font-weight: 600;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'Rubik';
  src: url('./fonts/Rubik-Bold.woff2') format('woff2');
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

/* === PRIMITIVE TOKENS === */
/* Raw values — only referenced by semantic tokens, never used directly in app CSS */
:root {
  --color-purple-600: #7c66ff;
  --color-purple-500: #8b7cff;
  --color-purple-400: #c6b9ff;
  --color-purple-200: #efeafe;
  --color-purple-100: #f3efff;
  --color-purple-50: #f8f6ff;
  --color-purple-dark: #4a4166;
  --color-purple-deeper: #3a3452;
  --color-purple-border: rgba(86, 78, 120, 0.2);
  --color-purple-border-soft: rgba(86, 78, 120, 0.1);
  --color-purple-border-faint: rgba(86, 78, 120, 0.08);
  --color-purple-border-subtle: rgba(86, 78, 120, 0.12);
  --color-purple-border-ghost: rgba(86, 78, 120, 0.25);
  --color-cream-50: #f6f3fb;
  --color-cream-100: #faf8ff;
  --color-cream-200: #f2effb;
  --color-cream-300: #f5f2ff;
  --color-cream-400: #fbf9ff;
  --color-cream-500: #f7f4ff;
  --color-cream-600: #f0ecff;
  --color-cream-700: #ede8ff;
  --color-cream-800: #f9f7ff;
  --color-cream-900: #e6defc;
  --color-white: #ffffff;
  --color-dark-900: #2f2d38;
  --color-dark-800: #2e2a38;
  --color-dark-700: #1a1825;
  --color-dark-600: #5f5c72;
  --color-green-500: #2fb37d;
  --color-red-500: #ea4b4b;
  --color-primary-shadow: rgba(124, 102, 255, 0.35);
  --color-primary-shadow-soft: rgba(124, 102, 255, 0.28);
  --color-danger-shadow: rgba(234, 75, 75, 0.28);
  --color-modal-overlay: rgba(26, 24, 35, 0.4);
  --color-modal-shadow: rgba(36, 24, 72, 0.2);
}

/* === SEMANTIC TOKENS (light mode default) === */
:root {
  --color-primary: var(--color-purple-600);
  --color-primary-soft: var(--color-purple-200);
  --color-primary-dark: var(--color-purple-dark);
  --color-primary-deeper: var(--color-purple-deeper);
  --color-background: var(--color-cream-50);
  --color-background-gradient-start: var(--color-cream-100);
  --color-background-gradient-end: var(--color-cream-200);
  --color-surface: var(--color-white);
  --color-surface-alt: var(--color-cream-500);
  --color-surface-hover: var(--color-cream-400);
  --color-surface-subtle: var(--color-cream-300);
  --color-surface-muted: var(--color-cream-800);
  --color-surface-avatar: var(--color-cream-600);
  --color-surface-media: var(--color-cream-700);
  --color-surface-disabled: var(--color-cream-800);
  --color-surface-secondary-btn: var(--color-purple-100);
  --color-surface-toggle: var(--color-purple-50);
  --color-surface-modal-close: var(--color-purple-100);
  --color-text: var(--color-dark-900);
  --color-text-muted: var(--color-dark-600);
  --color-text-meta: var(--color-purple-deeper);
  --color-success: var(--color-green-500);
  --color-danger: var(--color-red-500);
  --color-border: var(--color-purple-border);
  --color-border-soft: var(--color-purple-border-soft);
  --color-border-faint: var(--color-purple-border-faint);
  --color-border-subtle: var(--color-purple-border-subtle);
  --color-border-ghost: var(--color-purple-border-ghost);
  --color-toast-bg: var(--color-dark-800);
  --color-modal-overlay-bg: var(--color-modal-overlay);
  --color-placeholder-gradient: linear-gradient(135deg, var(--color-purple-500), var(--color-purple-400));
  --shadow-soft: 0 18px 40px rgba(36, 24, 72, 0.08);
  --shadow-card: 0 12px 26px rgba(36, 24, 72, 0.1);
  --shadow-brand: 0 8px 24px var(--color-primary-shadow);
  --shadow-button: 0 4px 12px var(--color-primary-shadow-soft);
  --shadow-danger: 0 4px 12px var(--color-danger-shadow);
  --shadow-modal: 0 20px 40px var(--color-modal-shadow);
}

/* === DARK MODE === */
/* Phase 1 placeholder values — Phase 2 will replace with garden palette */
[data-theme="dark"] {
  --color-background: #1a1825;
  --color-background-gradient-start: #1a1825;
  --color-background-gradient-end: #1f1c2e;
  --color-surface: #252235;
  --color-surface-alt: #2a2640;
  --color-surface-hover: #2e2a3e;
  --color-surface-subtle: #2a2640;
  --color-surface-muted: #2a2640;
  --color-surface-avatar: #332f4a;
  --color-surface-media: #332f4a;
  --color-surface-disabled: #2a2640;
  --color-surface-secondary-btn: #2e2850;
  --color-surface-toggle: #2e2850;
  --color-surface-modal-close: #2e2850;
  --color-text: #f0eeff;
  --color-text-muted: #a8a0c8;
  --color-text-meta: #a8a0c8;
  --color-primary: #9b88ff;
  --color-primary-soft: #2e2850;
  --color-primary-dark: #c6b9ff;
  --color-primary-deeper: #a8a0c8;
  --color-border: rgba(160, 150, 200, 0.2);
  --color-border-soft: rgba(160, 150, 200, 0.1);
  --color-border-faint: rgba(160, 150, 200, 0.08);
  --color-border-subtle: rgba(160, 150, 200, 0.12);
  --color-border-ghost: rgba(160, 150, 200, 0.25);
  --color-toast-bg: #1a1825;
  --shadow-soft: 0 18px 40px rgba(0, 0, 0, 0.3);
  --shadow-card: 0 12px 26px rgba(0, 0, 0, 0.35);
  --shadow-brand: 0 8px 24px rgba(155, 136, 255, 0.35);
  --shadow-button: 0 4px 12px rgba(155, 136, 255, 0.28);
  --shadow-modal: 0 20px 40px rgba(0, 0, 0, 0.5);
}
```

Notes for token-to-hardcoded mappings (will be needed in Task 2):
- Body gradient: `radial-gradient(circle at top, var(--color-background-gradient-start) 0%, var(--color-background) 55%, var(--color-background-gradient-end) 100%)`
- Modal overlay rgba → `var(--color-modal-overlay-bg)`
- Toast background → `var(--color-toast-bg)`
- Placeholder gradient → `var(--color-placeholder-gradient)`
  </action>
  <verify>
    <automated>ls /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/fonts/Rubik-Regular.woff2 /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/fonts/Rubik-SemiBold.woff2 /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/fonts/Rubik-Bold.woff2 && grep -c "@font-face" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/tokens.css && grep -c "data-theme" /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/tokens.css</automated>
  </verify>
  <done>Three WOFF2 files exist in assets/fonts/. tokens.css exists with @font-face blocks, primitive tokens, semantic tokens, and [data-theme="dark"] override block. No Google Fonts URL anywhere in tokens.css.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize app.css and update all 5 HTML files</name>
  <files>assets/app.css, index.html, add-client.html, add-session.html, sessions.html, reporting.html</files>
  <action>
**In assets/app.css:**

1. Remove line 1: `@import url("https://fonts.googleapis.com/css2?...")` — delete this entire line.

2. Remove the entire `:root { ... }` block (lines 3-21 approximately) — all variables now live in tokens.css.

3. Replace all remaining hardcoded color values throughout app.css with token references. Every instance of a hex color or rgba() that represents a color must become a `var(--token-name)` call. Use the mappings established in Task 1. Key replacements:
   - Body `radial-gradient(circle at top, #faf8ff 0%, var(--page-bg) 55%, #f2effb 100%)` → `radial-gradient(circle at top, var(--color-background-gradient-start) 0%, var(--color-background) 55%, var(--color-background-gradient-end) 100%)`
   - All former `--page-bg` references → `var(--color-background)`
   - All former `--card-bg` references → `var(--color-surface)`
   - All former `--text-main` references → `var(--color-text)`
   - All former `--text-muted` references → `var(--color-text-muted)`
   - All former `--accent` references → `var(--color-primary)`
   - All former `--accent-soft` references → `var(--color-primary-soft)`
   - All former `--success` references → `var(--color-success)`
   - All former `--danger` references → `var(--color-danger)`
   - All former `--shadow-soft` / `--shadow-card` references → `var(--shadow-soft)` / `var(--shadow-card)`
   - `rgba(86, 78, 120, 0.2)` → `var(--color-border)`
   - `rgba(86, 78, 120, 0.1)` → `var(--color-border-soft)`
   - `rgba(86, 78, 120, 0.08)` → `var(--color-border-faint)`
   - `rgba(86, 78, 120, 0.12)` → `var(--color-border-subtle)`
   - `rgba(86, 78, 120, 0.25)` → `var(--color-border-ghost)`
   - `#f7f4ff` → `var(--color-surface-alt)`
   - `#3a3452` → `var(--color-text-meta)`
   - `rgba(124, 102, 255, 0.35)` → `var(--color-primary-shadow)` (or use `var(--shadow-brand)` if in a box-shadow context)
   - `rgba(124, 102, 255, 0.28)` → `var(--color-primary-shadow-soft)` (or `var(--shadow-button)`)
   - `rgba(234, 75, 75, 0.28)` → `var(--color-danger-shadow)` (or `var(--shadow-danger)`)
   - `#4a4166` → `var(--color-primary-dark)`
   - `#f3efff` → `var(--color-surface-secondary-btn)`
   - `#f8f6ff` → `var(--color-surface-toggle)`
   - `#efeafe` (hardcoded, not the variable) → `var(--color-primary-soft)`
   - `#ede8ff` → `var(--color-surface-media)`
   - `linear-gradient(135deg, #8b7cff, #c6b9ff)` → `var(--color-placeholder-gradient)`
   - `rgba(26, 24, 35, 0.4)` → `var(--color-modal-overlay-bg)`
   - `rgba(36, 24, 72, 0.2)` → `var(--shadow-modal)` context or `var(--color-modal-shadow)`
   - `#f5f2ff` → `var(--color-surface-subtle)`
   - `#fbf9ff` → `var(--color-surface-hover)`
   - `#2e2a38` → `var(--color-toast-bg)`
   - `#f3f0ff` → `var(--color-surface-modal-close)`
   - `#f0ecff` → `var(--color-surface-avatar)`
   - `#e6defc` → `var(--color-cream-900)` (issue-remove hover — add `--color-hover-destructive: var(--color-cream-900)` to tokens.css if cleaner)
   - `#f9f7ff` → `var(--color-surface-disabled)`

4. Replace both `font-family` declarations:
   - `"Nunito", "Avenir", "Trebuchet MS", sans-serif` → `Rubik, system-ui, sans-serif`
   - `"Rubik", "Noto Sans Hebrew", sans-serif` → `Rubik, system-ui, sans-serif`

**In all 5 HTML files** (index.html, add-client.html, add-session.html, sessions.html, reporting.html):

Replace the single CSS link:
```html
<link rel="stylesheet" href="./assets/app.css" />
```
With two links (tokens.css FIRST):
```html
<link rel="stylesheet" href="./assets/tokens.css" />
<link rel="stylesheet" href="./assets/app.css" />
```

After making changes, run the grep checks to verify zero hardcoded colors remain in app.css.
  </action>
  <verify>
    <automated>grep -c '#[0-9a-fA-F]' /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/app.css; grep -c 'rgba(' /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/app.css; grep -c 'fonts.googleapis.com' /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/assets/app.css; grep -c 'tokens.css' /Users/sapir/Claude/Portfolio_Manager/TherapistPortfolioManager/index.html</automated>
  </verify>
  <done>app.css has 0 hardcoded hex colors (grep #[0-9a-fA-F] returns 0), 0 rgba() color values, 0 Google Fonts references, 0 Nunito references. All 5 HTML files have tokens.css linked before app.css. App visually renders the same as before in the browser (same purple theme — colors unchanged, just now via variables).</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Zero hardcoded colors in app.css
grep -c '#[0-9a-fA-F]' assets/app.css
grep -c 'rgba(' assets/app.css

# Zero Google Fonts anywhere in assets/
grep -r 'fonts.googleapis.com' assets/

# Zero Nunito references
grep -r 'Nunito' assets/

# All 5 HTML pages link tokens.css
grep -l 'tokens.css' *.html | wc -l

# Rubik font files present
ls assets/fonts/Rubik-*.woff2
```

Manual: Open index.html in browser (file:// or local server). Open Network tab — confirm 0 requests to fonts.googleapis.com or fonts.gstatic.com. Verify app renders with correct purple styling. Set `document.documentElement.setAttribute('data-theme', 'dark')` in devtools console — verify dark background appears.
</verification>

<success_criteria>
1. assets/tokens.css exists with @font-face (3 weights), primitive tokens, semantic tokens, and [data-theme="dark"] override block
2. Three Rubik WOFF2 files exist in assets/fonts/
3. assets/app.css contains zero hardcoded hex colors and zero rgba() color calls (all replaced by var() references)
4. The Google Fonts @import is gone from app.css
5. All 5 HTML pages load tokens.css before app.css
6. Nunito is completely removed from CSS and HTML
7. App renders visually identical to before in the browser (same colors, now via tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
