---
phase: 40-first-run-welcome-onboarding-coordinator
plan: 04
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - assets/app.js
  - tests/40-app-wiring.test.js
autonomous: true
requirements: [ONBD-02, ONBD-03]
must_haves:
  truths:
    - "initCommon() invokes AttentionCoordinator.run() exactly once (guarded by a typeof check) in place of the former unconditional showFirstLaunchSecurityNote() call (ONBD-03)."
    - "The security note is registered as the coordinator surface id 'security-note'; its eligible() enforces the existing gates (license activated, 7-day securityGuidanceDismissed cadence) AND returns false when #security-guidance-container is absent (D-05/D-08)."
    - "Its show() calls the existing showFirstLaunchSecurityNote() renderer unchanged (same copy, same 7-day cadence, Overview-only container) (D-05)."
    - "initHelpEntry mounts a 'Replay welcome' row (data-i18n help.entry.replayWelcome) after 'Help center' and before 'Contact us' (D-17)."
    - "Clicking 'Replay welcome' calls AttentionCoordinator.showWelcome(true) — a direct open that never re-arms sg.welcomeSeen or the session marker (ONBD-02)."
  artifacts:
    - "Edited assets/app.js: initCommon run() call, security-note surface registration, extended initHelpEntry item schema + Replay-welcome row"
    - "tests/40-app-wiring.test.js"
  key_links:
    - "AttentionCoordinator (Plan 02) must be defined before app.js init; the coordinator <script> is loaded before app.js by Plan 05."
    - "The security-note registration must run before the run() call inside initCommon so it participates in arbitration."
  prohibitions:
    - statement: "The backup-reminder banner (checkBackupReminder) and footer integrity nudge remain independent — they are NOT routed through the coordinator (D-04)."
      status: resolved
      verification: "grep of app.js shows checkBackupReminder still called directly (not registered as a coordinator surface); no register() call wraps the backup banner or footer nudge."
    - statement: "The 'Replay welcome' row is an action row (button role=menuitem), not an href anchor, and does not re-arm the one-shot flag or session marker (D-17/Pitfall 5)."
      status: resolved
      verification: "tests/40-app-wiring.test.js asserts the row is a button invoking showWelcome(true) and that sg.welcomeSeen / session marker are unchanged by the click."
    - statement: "The existing initHelpEntry app:language re-translate wiring stays intact for the new action row (it carries data-label-key)."
      status: resolved
      verification: "The new row has a data-label-key attribute; the existing re-translate listener updates .help-entry-item textContent."
---

<objective>
Wire the coordinator into the app: replace the unconditional first-launch security-note call in initCommon with a guarded AttentionCoordinator.run(), register the existing security note as a governed surface (behavior unchanged, D-05), and extend the Phase 39 "?" popover to mount a "Replay welcome" action row that reopens the welcome overlay directly (never re-arming first-run state).

Purpose: completes ONBD-03 (the coordinator now actually governs the real security note and mounts on every page via initCommon) and ONBD-02 (re-open the welcome from "?", never auto-re-fires). This plan edits only app.js, so it runs in parallel with Plans 03 and 05 (no shared files).

ACCEPTED EXCLUSION (architect-gate, source-verified): report.html never calls initCommon — assets/report.js boots its own init() on DOMContentLoaded (report.js:390) — so the coordinator does NOT run there even though Plan 05 adds the script tag. By design, not a gap: report.html is a print-oriented leaf page; a session starting there simply defers governed surfaces to the next navigation (session marker stays unset, no stacking risk). D-08's "fires on whichever page the session starts" is thereby scoped to every app.js-initCommon page; report.html deferred by design. Do NOT add an initCommon call or a separate run() invocation to report.js.
Output: edited `assets/app.js` + a wiring behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-CONTEXT.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-PATTERNS.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md
@assets/app.js
@assets/attention-coordinator.js
</context>

## Artifacts this phase produces (this plan)

Edited `assets/app.js`:
- initCommon: `AttentionCoordinator.run()` (guarded) replacing the direct `showFirstLaunchSecurityNote()` at ~line 892
- New `security-note` surface registration (wraps the unchanged `showFirstLaunchSecurityNote()` renderer)
- Extended `initHelpEntry` items schema (adds `action` rows alongside `href` rows) + the "Replay welcome" row calling `AttentionCoordinator.showWelcome(true)`

New test: `tests/40-app-wiring.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Author RED behavior test tests/40-app-wiring.test.js</name>
  <files>tests/40-app-wiring.test.js</files>
  <read_first>
    - tests/39-help-entry.test.js (the harness + how initHelpEntry is driven as a test seam and how the popover items are asserted)
    - assets/app.js (initHelpEntry 477–566; the items array 515–528; the app:language re-translate listener 552–565; showFirstLaunchSecurityNote 1356–1383; initCommon call-site 891–893)
    - assets/attention-coordinator.js (register/run/showWelcome API from Plan 02 — the test injects a fake window.AttentionCoordinator to observe calls)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Pattern 5 replay row; Code Example security-note registration; Pitfall 5)
  </read_first>
  <action>
    Author tests/40-app-wiring.test.js (jsdom, mirror tests/39-help-entry.test.js). Boot real assets/app.js into an isolated window with I18N.en seeded (so help.entry.replayWelcome resolves) and a fake window.AttentionCoordinator exposing register (records registrations by id), run (records it was called), and showWelcome (records its isReplay arg).

    Pin: (a) driving initHelpEntry mounts a '.help-entry-item' row whose textContent equals t('help.entry.replayWelcome'), positioned after the 'Help center' row and before the 'Contact us' row, rendered as a button with role=menuitem (an action row, not an <a href>), carrying data-label-key='help.entry.replayWelcome'; (b) clicking that row calls the fake AttentionCoordinator.showWelcome with isReplay truthy, and does NOT change localStorage 'sg.welcomeSeen' nor the session marker (Pitfall 5); (c) an app:language dispatch re-translates the new row's textContent (re-translate wiring intact); (d) after initCommon's boot path, the fake AttentionCoordinator.register was called with id 'security-note' and run() was called (the security note is now a governed surface, not a direct call); (e) the registered security-note eligible() returns false when #security-guidance-container is absent from the document (D-08) and true when the container is present with license activated + no recent dismissal (D-05). If initCommon's full async boot is heavy to drive, exercise the security-note registration through the smallest real seam that runs it (document the seam), consistent with how 39-help-entry drives initHelpEntry directly.

    Authored RED (app.js not yet edited). Exit 0 on pass, 1 otherwise.
  </action>
  <verify>
    <automated>node tests/40-app-wiring.test.js; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exits NON-zero before Task 2/3 — falsifiable RED.
    - It asserts the Replay-welcome row (button/menuitem, correct label, correct position, data-label-key) and the showWelcome(true) click without re-arming.
    - It asserts security-note is registered as a coordinator surface and run() replaces the direct call, plus the D-08 container-absent eligible()=false gate.
    - Uses only the jsdom harness + an injected fake coordinator (zero new dependencies).
  </acceptance_criteria>
  <done>tests/40-app-wiring.test.js exists, is RED, and pins the run() swap, security-note registration + D-08 gate, and the non-re-arming Replay-welcome row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: initCommon → AttentionCoordinator.run() + register the security-note surface</name>
  <files>assets/app.js</files>
  <behavior>
    - initCommon calls AttentionCoordinator.run() once (guarded by typeof) instead of showFirstLaunchSecurityNote().
    - A 'security-note' surface is registered whose eligible() = container present AND license activated AND (no securityGuidanceDismissed within 7 days), and whose show() = showFirstLaunchSecurityNote() (unchanged renderer).
    - Registration happens before run() so it participates in arbitration.
  </behavior>
  <read_first>
    - assets/app.js (initCommon 760/891–893; requestPersistentStorage() then showFirstLaunchSecurityNote() then initPersistentSecuritySection(); the renderer showFirstLaunchSecurityNote 1356–1383 with its license/7-day/container gates)
    - assets/attention-coordinator.js (register API shape from Plan 02)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Code Example: security-note surface registration lines ~412–425)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Winning-but-unrenderable rule D-08; security note unchanged D-05)
  </read_first>
  <action>
    In assets/app.js, register a governed surface {id:'security-note', eligible, show} with the coordinator. eligible() reproduces the existing gates as a boolean (return false early rather than rendering): false when document.getElementById('security-guidance-container') is absent (D-08 — so an unrenderable winner never consumes the session slot on a non-Overview page), false when localStorage 'portfolioLicenseActivated' !== '1', and false when 'securityGuidanceDismissed' is a timestamp within the last 7 days (mirror the existing daysSince<7 logic; the legacy '1' value counts as expired → show). show() calls the EXISTING showFirstLaunchSecurityNote() renderer unchanged (D-05 — same copy, cadence, container, dismissal write). Register this before the run() call. Then at the initCommon call-site (~line 892) replace the unconditional showFirstLaunchSecurityNote() call with a guarded invocation: if (typeof AttentionCoordinator !== 'undefined') AttentionCoordinator.run(); (Pitfall 7 — the guard tolerates demo.html and any page that does not load the coordinator). Do NOT route checkBackupReminder or the footer integrity nudge through the coordinator (D-04 — they stay independent, called as they are today). Leave requestPersistentStorage() and initPersistentSecuritySection() in place.
  </action>
  <verify>
    <automated>node tests/40-app-wiring.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "showFirstLaunchSecurityNote()" assets/app.js` shows the direct initCommon call is gone (the renderer is now invoked only via the surface show()); `grep -c "AttentionCoordinator.run" assets/app.js` >= 1 with the typeof guard present.
    - The security-note registration + D-08 container-absent eligible()=false assertions in `node tests/40-app-wiring.test.js` pass.
    - `grep -c "checkBackupReminder" assets/app.js` is unchanged from baseline and it is not wrapped in a register() call (D-04).
    - `node tests/run-all.js` stays green (existing security-note behavior preserved).
  </acceptance_criteria>
  <done>initCommon arbitrates via AttentionCoordinator.run(); the security note is a governed surface with the D-08 container gate and unchanged renderer; backup/footer nudge stay independent; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend initHelpEntry with the "Replay welcome" action row</name>
  <files>assets/app.js</files>
  <behavior>
    - The initHelpEntry items array supports an `action` handler alternative to `href`.
    - A 'Replay welcome' row (label help.entry.replayWelcome) mounts after 'Help center' and before 'Contact us', as a button role=menuitem.
    - Clicking it calls AttentionCoordinator.showWelcome(true) (direct open, no re-arm).
    - The new row carries data-label-key and re-translates on app:language.
  </behavior>
  <read_first>
    - assets/app.js (initHelpEntry 477–566: the items array 515–518, the forEach that builds each <a href> 519–528, the _listenerInstalled app:language re-translate block 552–565)
    - assets/attention-coordinator.js (showWelcome(isReplay) signature from Plan 02)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Pattern 5 — extend the items loop to support action rows; A1 assumption that the extension is additive)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md ("Replay welcome" menu row row — reuses .help-entry-item verbatim, neutral, noun-phrase label)
  </read_first>
  <action>
    In initHelpEntry, extend the items array to include, between the existing 'help.entry.center' and 'help.entry.contact' entries, a new item { labelKey: 'help.entry.replayWelcome', action: function () { if (typeof AttentionCoordinator !== 'undefined') AttentionCoordinator.showWelcome(true); } } (D-17 position). Extend the forEach loop so that: if item.href is present → build the current <a href> row (unchanged path); else if item.action is present → build a <button class="help-entry-item" role="menuitem"> whose click handler calls item.action() (a direct open that bypasses run() and never re-arms sg.welcomeSeen or the session marker — Pitfall 5). Both branches set data-label-key = item.labelKey and textContent = t(item.labelKey), and preserve the popover's keyboard/focus semantics used by the existing rows. Confirm the existing app:language re-translate listener (._listenerInstalled block) updates .help-entry-item textContent for the new row via data-label-key (it iterates all .help-entry-item, so the button row is covered — verify, do not duplicate the listener). Phase 41 later appends its own "Take the tour" action row the same way (no rename churn).
  </action>
  <verify>
    <automated>node tests/40-app-wiring.test.js && node tests/39-help-entry.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/40-app-wiring.test.js` exits 0 (Replay row present, correct position/label/element, showWelcome(true) on click, no re-arm, re-translate works).
    - `node tests/39-help-entry.test.js` stays green (the existing two day-one rows and idempotency are unbroken by the schema extension).
    - The Replay row is a `<button>` (action), not an `<a href>`; it carries `data-label-key="help.entry.replayWelcome"`.
    - `node tests/run-all.js` green.
  </acceptance_criteria>
  <done>The "?" popover mounts a Replay-welcome action row in the right position that reopens the welcome via showWelcome(true) without re-arming, with re-translate intact and the Phase 39 rows unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator API → app.js | app.js calls into window.AttentionCoordinator (defined by a separately-loaded script); a missing definition must not throw. |
| menu action → welcome overlay | The Replay row triggers a direct overlay open bypassing arbitration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04-REF | Denial of service (load error) | initCommon run() call | low | mitigate | `typeof AttentionCoordinator !== 'undefined'` guard tolerates pages/demo that do not load the coordinator (Pitfall 7). |
| T-40-04-ARM | Tampering (state corruption) | Replay row | low | mitigate | Replay calls showWelcome(true), which writes none of sg.welcomeSeen / last-seen-version / session marker (test-gated). |
| T-40-04-XSS | Tampering | new menu row DOM | low | mitigate | Row label via textContent/data-i18n; no interpolated innerHTML. |
| T-40-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed this phase; nothing to vet. |
</threat_model>

<verification>
- `node tests/40-app-wiring.test.js`, `node tests/39-help-entry.test.js`, `node tests/run-all.js` all green.
- initCommon direct security-note call replaced by guarded run(); security note registered with the D-08 container gate; renderer unchanged (D-05).
- Replay row is a non-re-arming action button in the D-17 position; backup/footer nudge remain independent (D-04).
</verification>

<success_criteria>
The coordinator governs the real security note and mounts on every initCommon app page via a guarded initCommon run() (report.html is an accepted exclusion — its own boot never calls initCommon; surfaces defer to the next navigation), and the "?" popover can replay the welcome any time without ever auto-re-firing or corrupting first-run state — all pinned green, with the Phase 39 popover behavior preserved.
</success_criteria>

<output>
Create `.planning/phases/40-first-run-welcome-onboarding-coordinator/40-04-SUMMARY.md` when done.
</output>
