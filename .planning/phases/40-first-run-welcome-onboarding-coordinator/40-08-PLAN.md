---
phase: 40-first-run-welcome-onboarding-coordinator
plan: 08
type: execute
wave: 2
depends_on: ["40-06", "40-07"]
files_modified:
  - assets/i18n-en.js
  - assets/attention-coordinator.js
  - assets/app.css
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - .planning/phases/40-first-run-welcome-onboarding-coordinator/DEFERRED-FROM-40.md
autonomous: true
gap_closure: true
requirements: [ONBD-01, ONBD-02]
must_haves:
  truths:
    - "The EN welcome overlay reads as TWO warm paragraphs — value/experience first, privacy softened second — beyond the old flat privacy sentence"
    - "The '?' menu label reads naturally as 'Onboarding screen' (EN)"
    - "Non-EN locales show no broken/empty second paragraph; the standing 4-locale parity gates stay green"
  artifacts:
    - "assets/i18n-en.js — help.welcome.subtitle (P1) rewritten + help.welcome.subtitle2 (P2) added, exact approved copy; help.entry.replayWelcome renamed"
    - "assets/attention-coordinator.js — showWelcome() renders a second subtitle <p> via data-i18n + textContent"
    - "assets/i18n-he.js / i18n-de.js / i18n-cs.js — empty-string subtitle2 parity stub (structural only; translation deferred to 42.1)"
  key_links:
    - "showWelcome() second <p> data-i18n=help.welcome.subtitle2 → textContent (never innerHTML — T-40-03)"
    - "second <p> mounts ONLY when the resolved value is a non-empty string ≠ the key (empty stubs render nothing)"
---

<objective>
Close two MINOR content gaps on the welcome overlay (40-UAT.md test 1), using Ben's
APPROVED Option-C copy (recorded in
.planning/phases/40-first-run-welcome-onboarding-coordinator/APPROVED-WELCOME-COPY-EN.md):

1. The subtitle was one flat privacy sentence. Ben approved TWO paragraphs in the calm garden
   voice: P1 = the smooth day-to-day value of working in the app (beyond privacy), P2 = privacy
   softened. Deliver P1 in the existing help.welcome.subtitle key and P2 in a NEW
   help.welcome.subtitle2 key, rendered as a second `<p>` (data-i18n + textContent — the T-40-03
   XSS boundary; never innerHTML). Layout + CTAs stay intact (ONBD-01).
2. The "?" menu row "Replay welcome" reads awkwardly — rename its EN label to "Onboarding
   screen" (ONBD-02).

## Why he/de/cs are touched here (surfaced constraint — read before executing)
The confirmed two-key shape adds a NEW EN key (subtitle2). Two STANDING parity gates —
tests/25-11-i18n-parity.js ("every EN key exists in he/de/cs") and
tests/33-i18n-de-cs-completion.js (exact bidirectional de/cs↔en key parity) — make an EN-only
new key impossible without turning the suite red. So subtitle2 is added to i18n-he/de/cs as an
EMPTY-string parity STUB (structural only, ZERO translation). The second `<p>` renders only when
the resolved value is a non-empty string, so non-EN locales show no second paragraph until the
REAL translation lands in Phase 42.1. This is the minimal touch that keeps the suite green; the
actual HE/DE/CS copy work remains deferred.

Purpose: a warmer, value-first welcome (EN) and a natural menu label, without breaking i18n parity.
Output: approved EN copy live, second `<p>` rendered, empty non-EN stubs, DEFERRED doc updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative approved EN strings — use these EXACT values
@.planning/phases/40-first-run-welcome-onboarding-coordinator/APPROVED-WELCOME-COPY-EN.md
# showWelcome() builds the subtitle <p> at ~L157-162 (mirror it for the second <p>)
@assets/attention-coordinator.js
# EN dict — subtitle (~L602), replayWelcome (~L599)
@assets/i18n-en.js
# .welcome-subtitle rule (~L4823) for the two-paragraph spacing
@assets/app.css
# Deferred-translation tracker to update with the confirmed key shape
@.planning/phases/40-first-run-welcome-onboarding-coordinator/DEFERRED-FROM-40.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the approved EN copy + rename the menu label</name>
  <files>assets/i18n-en.js</files>
  <action>
    Read the EXACT strings from APPROVED-WELCOME-COPY-EN.md (that file is authoritative — if any
    wording differs from the quotes below, use the file). In assets/i18n-en.js:

    - Replace the help.welcome.subtitle value (around line 602) with paragraph 1:
      "Sessions Garden brings your whole practice into one calm, simple space — clients, sessions,
      and notes, all where you expect them. It's built to feel effortless day to day, so the tool
      disappears and the work comes forward."
    - Immediately AFTER it, add a NEW key help.welcome.subtitle2 with paragraph 2:
      "Everything you enter stays private on this device. No accounts, no cloud, no one else in
      the room — just your practice, kept safely in your own hands."
    - Rename the help.entry.replayWelcome value (around line 599) from its current wording to
      exactly: "Onboarding screen".

    Do not touch help.welcome.title (stays "Welcome to your garden"). Preserve JS string escaping
    (em-dash and apostrophes are plain characters — no emoji). Keep the surrounding comments/keys
    intact.
  </action>
  <verify>
    <automated>node -e "global.window={};require('./assets/i18n-en.js');const e=window.I18N.en;const A=(k,s)=>{if(!e[k]||e[k].trim()==='')throw new Error('empty '+k);if(s&&e[k].indexOf(s)===-1)throw new Error('unexpected '+k+': '+e[k])};A('help.welcome.subtitle','effortless day to day');A('help.welcome.subtitle2','private on this device');if(e['help.entry.replayWelcome']!=='Onboarding screen')throw new Error('label not renamed: '+e['help.entry.replayWelcome']);console.log('EN copy OK')" && node tests/40-i18n-parity.test.js</automated>
  </verify>
  <done>help.welcome.subtitle = P1, help.welcome.subtitle2 = P2 (exact approved copy), help.entry.replayWelcome = "Onboarding screen"; 40-i18n-parity still green.</done>
</task>

<task type="auto">
  <name>Task 2: Render the second subtitle paragraph (textContent, conditional)</name>
  <files>assets/attention-coordinator.js, assets/app.css</files>
  <action>
    In showWelcome() (assets/attention-coordinator.js, where the existing subtitle `sub` <p> is
    built at ~L157-162), add a second paragraph MIRRORING the existing one: create a `<p>` with
    className 'welcome-subtitle', setAttribute('data-i18n','help.welcome.subtitle2'), and set its
    textContent from t('help.welcome.subtitle2') — via textContent ONLY, never innerHTML (the
    overlay's XSS trust boundary, T-40-03). Append it to `copy` BETWEEN the existing subtitle
    `sub` and the `actions` block (so paragraph order is title → P1 → P2 → CTAs).

    Guard the mount: only append the second `<p>` when the resolved value is a NON-EMPTY string
    AND is not the key itself (t() returns the raw key when a locale lacks the entry). This keeps
    non-EN locales — whose subtitle2 is an empty parity stub — from mounting an empty or
    literal-key paragraph. Resolve the value once (same t() the existing sub uses) and branch on
    it. Do not change the existing subtitle `sub`, the CTAs, the focus trap, or dismiss logic.

    In assets/app.css, give the two subtitle paragraphs a comfortable gap: add a rule
    `.welcome-subtitle + .welcome-subtitle { margin-block-start: <existing spacing token>; }` using
    an existing spacing custom-property (logical property only — no physical margin-top, no literal
    length/hex), so exactly the second paragraph gets top spacing while the first keeps margin 0.
  </action>
  <verify>
    <automated>node tests/40-welcome-overlay.test.js && node -e "const s=require('fs').readFileSync('assets/attention-coordinator.js','utf8');if(s.indexOf('help.welcome.subtitle2')===-1)throw new Error('subtitle2 not rendered');if(!/\.welcome-subtitle\s*\+\s*\.welcome-subtitle/.test(require('fs').readFileSync('assets/app.css','utf8')))throw new Error('paragraph spacing rule missing');console.log('render + spacing OK')" && node tests/run-all.js</automated>
    <human-check>Fresh profile / replay from "?": the welcome overlay shows two distinct paragraphs (value first, privacy second) with a comfortable gap, in light AND dark, Chromium AND Safari.</human-check>
  </verify>
  <done>showWelcome() renders a second data-i18n=help.welcome.subtitle2 paragraph via textContent, conditional on a non-empty resolved value; the two paragraphs have proper spacing; welcome-overlay behavior tests + full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Empty-string subtitle2 parity stubs (he/de/cs) + update the deferred tracker</name>
  <files>assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, .planning/phases/40-first-run-welcome-onboarding-coordinator/DEFERRED-FROM-40.md</files>
  <action>
    In each of assets/i18n-he.js, i18n-de.js, and i18n-cs.js, add the key help.welcome.subtitle2
    with an EMPTY-string value ("") adjacent to that file's existing help.welcome.subtitle entry.
    This is a STRUCTURAL parity stub only — do NOT translate P2 here (that is Phase 42.1). Mark
    each stub with a short JS comment noting it is a Phase 42.1 translation-pending empty parity
    stub. IMPORTANT: do NOT use the literal marker "// TODO i18n" — tests/33-i18n-de-cs-completion.js
    fails on that exact string; use different wording (e.g. "Phase 42.1: translation pending").
    Change nothing else in these files (all other HE/DE/CS strings — including the current
    single-sentence subtitle and the old replay-welcome label — stay as-is until 42.1).

    Update DEFERRED-FROM-40.md to record the now-CONFIRMED state so Phase 42.1 has an exact
    to-do: (a) the EN key shape is finalized as help.welcome.subtitle (P1) + help.welcome.subtitle2
    (P2), and help.entry.replayWelcome is now "Onboarding screen" in EN; (b) HE/DE/CS currently
    hold an EMPTY subtitle2 stub plus the OLD single-sentence subtitle and OLD replay-welcome
    wording; (c) 42.1 must re-translate subtitle to the P1 value-first wording, author subtitle2
    (P2 privacy) in each locale, and re-translate the "Onboarding screen" label — per the existing
    HE noun/infinitive / DE Sie / CS formal register reminders in that file.
  </action>
  <verify>
    <automated>node tests/25-11-i18n-parity.test.js && node tests/33-i18n-de-cs-completion.test.js && node -e "for(const l of ['he','de','cs']){global.window={};delete require.cache[require.resolve('./assets/i18n-'+l+'.js')];require('./assets/i18n-'+l+'.js');if(!('help.welcome.subtitle2' in window.I18N[l]))throw new Error(l+' missing subtitle2 stub')}console.log('parity stubs OK')" && node tests/run-all.js</automated>
  </verify>
  <done>he/de/cs each define help.welcome.subtitle2 as an empty stub; tests/25-11 + tests/33 stay green; no "// TODO i18n" marker introduced; DEFERRED-FROM-40.md records the confirmed key shape and the exact 42.1 to-do; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dict → welcome overlay DOM | The subtitle2 string crosses from the I18N dict into the mounted overlay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-03 | Tampering/Injection | showWelcome() second subtitle `<p>` | medium | mitigate | The second paragraph is set via textContent + data-i18n exactly like the existing subtitle — never innerHTML. A hostile/future translation can only ever become inert text, not markup. The conditional mount also prevents an empty/key-echo string from rendering. |
| T-40-08-01 | Denial of Service | empty subtitle2 stubs collapsing the overlay | low | mitigate | The non-empty guard means an empty stub renders nothing (no empty `<p>`), so the overlay layout is unchanged for non-EN locales. |

No package-manager installs in this plan (no supply-chain checkpoint required).
</threat_model>

<verification>
- `node tests/40-welcome-overlay.test.js` green (overlay structure/behavior unbroken by the added paragraph).
- `node tests/40-i18n-parity.test.js`, `node tests/25-11-i18n-parity.test.js`, `node tests/33-i18n-de-cs-completion.test.js` all green (subtitle2 stubs satisfy parity; no forbidden marker).
- `node tests/run-all.js` green.
- Human check: two-paragraph EN overlay renders correctly (light/dark, Chromium/Safari); non-EN overlays show a single subtitle (no empty gap).
</verification>

<success_criteria>
The EN welcome overlay shows two warm paragraphs (value first, privacy softened), the "?" menu label reads "Onboarding screen", non-EN locales show no broken second paragraph, and all standing i18n parity gates stay green — with the real HE/DE/CS translation cleanly deferred to Phase 42.1.
</success_criteria>

<output>
Create `.planning/phases/40-first-run-welcome-onboarding-coordinator/40-08-SUMMARY.md` when done.
</output>
