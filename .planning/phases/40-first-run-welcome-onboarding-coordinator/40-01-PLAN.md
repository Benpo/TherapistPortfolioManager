---
phase: 40-first-run-welcome-onboarding-coordinator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/40-i18n-parity.test.js
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-04]
must_haves:
  truths:
    - "All 15 new i18n keys resolve to a non-empty string in each of en/he/de/cs (welcome ×4, replayWelcome ×1, install ×7, mobileHint ×3)."
    - "EN canonical values match the 40-UI-SPEC Copywriting Contract verbatim (help.welcome.* inherited from 26-UI-SPEC; onboard.* draft copy)."
    - "The four locale files carry an identical set of the new keys (structural parity), guarded by tests/40-i18n-parity.test.js."
  artifacts:
    - "tests/40-i18n-parity.test.js (new static parity gate)"
    - "New key blocks appended to assets/i18n-en.js / i18n-he.js / i18n-de.js / i18n-cs.js"
  key_links:
    - "help.welcome.* consumed by the welcome overlay in attention-coordinator.js (Plan 02)."
    - "onboard.install.* / onboard.mobileHint.* consumed by the install-nudge / mobile-hint surfaces (Plan 03)."
    - "help.entry.replayWelcome consumed by the initHelpEntry 'Replay welcome' row (Plan 04)."
  prohibitions:
    - statement: "No emoji appears in any new copy value across the four locale files (the deleted iOS banner's 📤 does not carry forward)."
      status: resolved
      verification: "grep for the emoji code point / any emoji in the new key values across i18n-*.js returns none."
    - statement: "No new label is a bare single-word imperative (Install!/Back/OK) — labels are nouns / noun-phrases so HE renders natural noun/infinitive forms (Phase 24 D-05)."
      status: resolved
      verification: "Manual + native-speaker agent review; EN values equal the noun-phrase strings in the UI-SPEC table."
---

<objective>
Author every new user-facing UI-chrome string this phase introduces — the welcome overlay copy, the "Replay welcome" menu label, the install-nudge card, and the mobile expectation hint — in all four locales (EN/HE/DE/CS), and ship a structural parity gate so no locale drifts a key.

Per CONTEXT language scope: these are UI chrome (not help body content), so they ship in all 4 locales in THIS phase under the standing all-language rule — Phase 42.1 (L10N-01) covers only the help BODY corpus, not these strings.

Purpose: Every surface built in Plans 02–04 is `data-i18n`-keyed; those keys must exist first (none do yet — verified: only `help.entry.label/center/contact` exist today). Authoring them in one isolated plan lets the coordinator/welcome/install work proceed against a stable key contract.
Output: 15 new keys × 4 locales + a parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-CONTEXT.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md
@.planning/SessionsGarden-DNA-EN.md
</context>

## Artifacts this phase produces (this plan)

New i18n keys (EN canonical shown; HE/DE/CS authored in this plan):
- `help.welcome.title`, `help.welcome.subtitle`, `help.welcome.ctaTour`, `help.welcome.ctaExplore`
- `help.entry.replayWelcome`
- `onboard.install.title`, `onboard.install.body`, `onboard.install.ctaInstall`, `onboard.install.safariHint`, `onboard.install.safariLink`, `onboard.install.dismiss`, `onboard.install.laterHint`
- `onboard.mobileHint.body`, `onboard.mobileHint.link`, `onboard.mobileHint.dismiss`

New file: `tests/40-i18n-parity.test.js` (static parity gate).

<tasks>

<task type="auto">
  <name>Task 1: Author the RED parity gate tests/40-i18n-parity.test.js</name>
  <files>tests/40-i18n-parity.test.js</files>
  <read_first>
    - tests/39-help-integrity.test.js (Phase 39 i18n integrity harness — mirror its require/eval-into-node pattern and exit-0/1 contract)
    - tests/39-help-precache.test.js (fs source-scan + check(name,cond) + process.exit(failed===0?0:1) idiom)
    - assets/i18n-en.js (confirm the module export shape so the test can load each locale's key map)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Copywriting Contract — the exact key names)
  </read_first>
  <action>
    Create tests/40-i18n-parity.test.js as a zero-dependency node test (fs + the project's existing locale-load idiom, mirroring tests/39-help-integrity.test.js). Define the canonical list of the 15 new keys: help.welcome.title, help.welcome.subtitle, help.welcome.ctaTour, help.welcome.ctaExplore, help.entry.replayWelcome, onboard.install.title, onboard.install.body, onboard.install.ctaInstall, onboard.install.safariHint, onboard.install.safariLink, onboard.install.dismiss, onboard.install.laterHint, onboard.mobileHint.body, onboard.mobileHint.link, onboard.mobileHint.dismiss. Load each of the four locale maps (en/he/de/cs) the same way the existing help-integrity test loads I18N, and assert for every locale × every new key: the key exists AND its value is a non-empty trimmed string. Assert cross-locale parity: the set of new keys present is identical across all four files. Add a no-emoji assertion: none of the new key VALUES contains an emoji (test against a code-point range covering pictographic emoji, and explicitly against the U+1F4E4 outbox code point the deleted banner used). This test is authored RED (keys absent until Task 2) — do NOT weaken it to green. Exit 0 only when all assertions pass, 1 otherwise.
  </action>
  <verify>
    <automated>node tests/40-i18n-parity.test.js; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/40-i18n-parity.test.js` exits NON-zero before Task 2 (keys absent) — proving the gate is falsifiable.
    - The test enumerates all 15 key strings listed above (welcome 4 + replayWelcome 1 + install 7 + mobileHint 3 = 15 keys total) and checks presence + non-empty + cross-locale parity + no-emoji.
    - The test uses only node built-ins + the existing locale-load idiom (zero new dependencies).
  </acceptance_criteria>
  <done>tests/40-i18n-parity.test.js exists, is falsifiable-RED, and pins presence, non-empty, parity, and no-emoji for the 15 new keys.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 15 new keys across all four locale files</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <read_first>
    - assets/i18n-en.js (insert near the existing "help.entry.*" block at ~line 596–598; match the surrounding quoting/trailing-comma style exactly)
    - assets/i18n-he.js (Hebrew register — noun/infinitive forms per Phase 24 D-05; match existing help.entry.* HE style)
    - assets/i18n-de.js (German Sie register; match existing help.entry.* DE style)
    - assets/i18n-cs.js (Czech formal register; match existing help.entry.* CS style)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Copywriting Contract table — EN canonical values are VERBATIM there)
    - .planning/SessionsGarden-DNA-EN.md (voice: calm, warm, garden-branded — for the HE/DE/CS tone)
  </read_first>
  <action>
    Add the 15 keys to each locale file. EN canonical values are copied VERBATIM from the 40-UI-SPEC Copywriting Contract: help.welcome.title = "Welcome to your garden"; help.welcome.subtitle = "Sessions Garden keeps your client work private, on this device, and organized around how you actually run a session."; help.welcome.ctaTour = "Take the guided tour"; help.welcome.ctaExplore = "I'll explore myself"; help.entry.replayWelcome = "Replay welcome"; onboard.install.title = "Add Sessions Garden to your computer"; onboard.install.body = "Install it for a calm full-screen window and one-click access — it still works fully offline, and your data stays on this device."; onboard.install.ctaInstall = "Install app"; onboard.install.safariHint = "On Safari, open the File menu and choose Add to Dock."; onboard.install.safariLink = "See install help"; onboard.install.dismiss = "No thanks"; onboard.install.laterHint = "You can install any time from Help."; onboard.mobileHint.body = "Sessions Garden is designed for computers. On a phone your data lives on each device separately — it won't move between your phone and your computer."; onboard.mobileHint.link = "How Sessions Garden works on mobile"; onboard.mobileHint.dismiss = "Got it". Author HE/DE/CS translations that preserve meaning and register: Hebrew uses noun/infinitive forms (no bare imperatives, D-05), German uses Sie, Czech uses the formal register — mirroring the existing help.entry.* translations already in each file. No emojis in any value. Place the welcome/replayWelcome keys adjacent to the existing help.entry.* block; place the onboard.* keys in a clearly-commented "Onboarding surfaces — UI chrome (Phase 40)" group. Flag the HE/DE/CS values for the milestone native-speaker agent review (this authoring is the input to that review, not a substitute for it).
  </action>
  <verify>
    <automated>node tests/40-i18n-parity.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/40-i18n-parity.test.js` exits 0 (all 15 keys present, non-empty, parity, no-emoji across en/he/de/cs).
    - `grep -c "onboard.install.title" assets/i18n-en.js assets/i18n-he.js assets/i18n-de.js assets/i18n-cs.js` shows the key once per file.
    - EN values for the four `help.welcome.*` keys and the 15 keys equal the 40-UI-SPEC Copywriting Contract strings character-for-character.
    - `node tests/run-all.js` stays green (no regression in the existing suite).
  </acceptance_criteria>
  <done>All 15 keys exist in all four locale files with EN canonical values verbatim from the UI-SPEC and register-appropriate HE/DE/CS; the parity gate is green; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n data → DOM | Locale string values are later rendered by consuming surfaces; if a value carried markup and were injected via innerHTML it could execute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01-DATA | Tampering (stored XSS vector) | new onboard.*/help.welcome.* values | low | mitigate | Values are plain sentences (no markup); consuming surfaces (Plans 02–04) render them via `textContent`/`data-i18n`, never interpolated innerHTML. |
| T-40-SC | Tampering (supply chain) | package installs | low | accept | No packages installed this phase (zero-npm vanilla PWA); nothing to vet. |
</threat_model>

<verification>
- `node tests/40-i18n-parity.test.js` exits 0.
- `node tests/run-all.js` green.
- EN canonical values match the 40-UI-SPEC Copywriting Contract verbatim.
- HE/DE/CS authored to register conventions and flagged for the milestone native-speaker agent review before ship.
</verification>

<success_criteria>
All 15 new UI-chrome keys exist in en/he/de/cs, are non-empty, parity-checked, emoji-free, with EN canonical verbatim from the UI-SPEC; the new parity gate and the full suite are green.
</success_criteria>

<output>
Create `.planning/phases/40-first-run-welcome-onboarding-coordinator/40-01-SUMMARY.md` when done.
</output>
