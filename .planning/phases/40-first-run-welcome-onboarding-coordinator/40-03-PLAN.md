---
phase: 40-first-run-welcome-onboarding-coordinator
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - assets/attention-coordinator.js
  - assets/app.css
  - tests/40-install-nudge.test.js
autonomous: true
requirements: [ONBD-04]
must_haves:
  truths:
    - "The install-nudge surface registers as PRECEDENCE id 'install-nudge' and is eligible only on a non-standalone, non-mobile browser that has not dismissed it AND that can render correct copy: a deferredPrompt was captured (Chromium branch) OR the browser is actually macOS Safari (pointer branch) — any other browser/no-event case is NOT eligible and the slot passes to the next surface (per-browser-aware, D-12/D-13)."
    - "On Chromium (a captured beforeinstallprompt exists), the nudge card shows a real [Install app] button whose click calls the deferred event's prompt() exactly once, then clears it (Pitfall 2)."
    - "The 'File → Add to Dock' pointer line + 'See install help' link render ONLY on actual macOS Safari (UA/platform-detected) — never on desktop Firefox or on Chromium whose event hasn't fired yet; those cases are ineligible so the one-ask is never burned on wrong copy (D-12/D-14, Pitfall 1) — and never a dead/fake Install button."
    - "The nudge is never eligible when matchMedia('(display-mode: standalone)').matches (already installed) (D-12)."
    - "Dismissing the nudge sets a persistent localStorage flag so it never re-appears (gone forever, D-14)."
    - "The mobile-hint surface registers as PRECEDENCE id 'mobile-hint' (lowest tier), is eligible only on phone-class devices that have not dismissed it, and is one-shot / dismissed-forever (D-16)."
    - "Phone-class detection uses capability probing (uaMobile OR (pointer:coarse AND max-width:820px)), not iOS-only UA sniffing (D-16)."
  artifacts:
    - "install-nudge surface impl + registration in assets/attention-coordinator.js"
    - "mobile-hint surface impl + registration in assets/attention-coordinator.js"
    - "install-card and mobile-hint CSS in assets/app.css"
    - "tests/40-install-nudge.test.js"
  key_links:
    - "Both surfaces read the deferredPrompt / display-mode state captured at eval time by Plan 02's coordinator core."
    - "The mobile-hint link targets the Phase 39 mobile-expectations help topic; the Safari install link targets the Phase 39 install topic."
  prohibitions:
    - statement: "The mobile expectation hint uses neutral surface styling only — NO --color-primary fill and no warning tone (it is not the deleted iOS banner's accent bar) (D-16)."
      status: resolved
      verification: "grep of the mobile-hint CSS block shows no --color-primary background fill; styling uses --color-surface / --color-text / soft border."
    - statement: "Dismissals persist in localStorage, NOT sessionStorage (opposite storage class from the session marker) (Pitfall 4)."
      status: resolved
      verification: "The install/mobile dismissal reads/writes localStorage keys; tests assert the nudge stays gone across a fresh coordinator load."
    - statement: "No fake universal Install button is rendered when no beforeinstallprompt was captured, and Safari copy never shows on a non-Safari browser; the platform branch is real-capability + real-browser gated (D-12)."
      status: resolved
      verification: "tests/40-install-nudge.test.js asserts the macOS-Safari branch renders the pointer + help link with no prompt()-calling button, AND a Firefox-like no-event non-Safari env makes eligible() false (nothing shown, nothing dismissed, session slot not consumed)."
    - statement: "Install-nudge copy and hint copy are set via textContent/data-i18n, never variable-interpolated innerHTML."
      status: resolved
      verification: "grep of the added coordinator code shows no variable-interpolated innerHTML for copy."
---

<objective>
Add the two lowest-precedence governed surfaces to the coordinator: the computer-focused install nudge (real [Install app] button on Chromium via the captured beforeinstallprompt; File → Add to Dock pointer on macOS Safari) and the all-mobile expectation hint that succeeds the deleted iOS banner. Both are one-ask / dismissed-forever, and both register into the Plan 02 coordinator so `run()` arbitrates them at the bottom of the precedence order.

Purpose: delivers ONBD-04 (one friendly, dismissable, non-nagging, per-browser-aware install affordance; dismissal remembered) and the mobile successor to the retired banner. The install nudge naturally never wins launch 1 (welcome wins), so it is eligible from the second session on (D-13) with no extra bookkeeping.
Output: two registered surfaces in `attention-coordinator.js`, their CSS in `app.css`, and a behavior test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-CONTEXT.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-PATTERNS.md
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md
@assets/attention-coordinator.js
@assets/tokens.css
</context>

## Artifacts this phase produces (this plan)

Added to `assets/attention-coordinator.js`:
- `install-nudge` surface (eligible/show) + registration
- `mobile-hint` surface (eligible/show) + registration
- Phone-class detection helper (capability probe)
- `isMacSafari()` browser-detection helper (per-browser install-copy gate, D-12)
- New localStorage keys: `sg.installNudgeDismissed`, `sg.mobileHintDismissed`

Added to `assets/app.css`: install-nudge card CSS + mobile expectation-hint bar CSS.

New test: `tests/40-install-nudge.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Author RED behavior test tests/40-install-nudge.test.js</name>
  <files>tests/40-install-nudge.test.js</files>
  <read_first>
    - tests/39-help-entry.test.js (jsdom harness to mirror)
    - tests/_helpers/mock-navigator-share.js (example of mocking navigator capabilities in this suite)
    - assets/attention-coordinator.js (the coordinator + capture seam authored in Plan 02 — confirm how the install-nudge surface will read the deferred prompt and how to inject a synthetic one for the test)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Pattern 3/4; Pitfalls 1/2/3/4; Validation Architecture → 40-install-nudge test map)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Install nudge + Mobile expectation hint contract rows)
  </read_first>
  <action>
    Author tests/40-install-nudge.test.js (zero-dep jsdom, mirror tests/39-help-entry.test.js). Provide mocks the surfaces read: a controllable window.matchMedia (so '(display-mode: standalone)', '(pointer: coarse)', and '(max-width: 820px)' can each be toggled), a settable navigator.userAgentData.mobile, a settable navigator.userAgent (so macOS-Safari vs Firefox-like vs Chromium UA strings can each be simulated), and a way to inject a synthetic captured beforeinstallprompt object exposing preventDefault(), a prompt() spy, and a userChoice promise.

    Pin install-nudge: (a) eligible() is FALSE when display-mode standalone matches (already installed); (b) eligible() is FALSE when the persistent dismissal flag 'sg.installNudgeDismissed' is set; (c) eligible() is FALSE on phone-class (mobile gets the hint instead); (c2) PER-BROWSER GATE (D-12) — eligible() is FALSE in a Firefox-like desktop env (no captured beforeinstallprompt AND a non-Safari UA): nothing is shown, 'sg.installNudgeDismissed' is NOT written, and the session slot is not consumed (a lower surface / next session can still compete) — this also covers Chromium when run() beats the late-firing event (Pitfall 1: the nudge simply retries a later session); (d) with a captured deferredPrompt on a desktop non-standalone browser, eligible() is TRUE and show() renders a card containing a real [Install app] button (textContent = onboard.install.ctaInstall) whose click calls the injected prompt() spy exactly once; a second click does not call prompt() again (Pitfall 2); (e) with NO captured deferredPrompt but an actual macOS Safari UA (e.g. 'Macintosh' + 'Safari/' without Chrome/Chromium/CriOS/Edg/OPR), eligible() is TRUE and show() renders the 'Add to Dock' pointer (onboard.install.safariHint) + a 'See install help' link (onboard.install.safariLink) targeting the Phase 39 install help topic, and renders NO button that calls prompt(); (f) clicking dismiss ('No thanks') sets localStorage 'sg.installNudgeDismissed' and, on a fresh coordinator load, eligible() is now false (persists — Pitfall 4).

    Pin mobile-hint: (g) eligible() is TRUE on phone-class (matchMedia coarse+narrow OR userAgentData.mobile) and not-yet-dismissed, FALSE otherwise; (h) show() renders the hint body (onboard.mobileHint.body) + topic link (onboard.mobileHint.link) → Phase 39 mobile-expectations help topic + 'Got it' dismiss; (i) dismiss sets localStorage 'sg.mobileHintDismissed' and it stays gone on a fresh load (one-shot forever).

    Authored RED (surfaces not yet implemented). Exit 0 on pass, 1 otherwise.
  </action>
  <verify>
    <automated>node tests/40-install-nudge.test.js; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exits NON-zero before Task 2/3 (surfaces unimplemented) — falsifiable RED.
    - It covers install eligibility gates (standalone / dismissed / phone-class / per-browser: Firefox-like no-event non-Safari → NOT eligible, nothing shown, nothing dismissed, slot not consumed), the Chromium prompt()-once branch, the macOS-Safari-only pointer branch, and persistent dismissal.
    - It covers mobile-hint phone-class eligibility, structure, and one-shot persistent dismissal.
    - Uses only the jsdom harness + injected mocks (zero new dependencies).
  </acceptance_criteria>
  <done>tests/40-install-nudge.test.js exists, is RED, and pins the per-browser platform branches (Chromium / macOS Safari / ineligible-otherwise), standalone/dismissed/phone-class gates, prompt()-once, and gone-forever dismissal for both surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the install-nudge surface in attention-coordinator.js</name>
  <files>assets/attention-coordinator.js</files>
  <behavior>
    - eligible(): false if display-mode standalone, false if localStorage 'sg.installNudgeDismissed' set, false on phone-class, false if NEITHER a deferredPrompt was captured NOR the browser is actual macOS Safari (per-browser gate, D-12); otherwise true.
    - show() with a captured deferredPrompt: render the card with a real [Install app] button calling deferredPrompt.prompt() once then nulling it; on macOS Safari (no deferredPrompt): render the File → Add to Dock pointer + See-install-help link (no button). No other show() branch exists — any other env is filtered by eligible().
    - Dismiss ('No thanks') writes the persistent localStorage flag; card removed.
  </behavior>
  <read_first>
    - assets/attention-coordinator.js (the deferredPrompt capture + registry API from Plan 02 — register this surface the same way the welcome surface registered; reuse the try/catch storage helpers)
    - assets/app.js (matchMedia usage precedent for '(display-mode: standalone)' at ~index.html:369; try/catch storage idiom 1379–1382)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Pattern 3 capture+prompt, Pitfalls 1/2/3)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Install nudge contract row — copy keys, accent = single [Install app] button only, dismiss neutral)
    - tests/40-install-nudge.test.js (contract to satisfy)
  </read_first>
  <action>
    In assets/attention-coordinator.js register {id:'install-nudge', eligible, show}. Add an isMacSafari() helper: /Macintosh/.test(navigator.userAgent) AND /Safari\//.test(navigator.userAgent) AND NOT /Chrome|Chromium|CriOS|Edg|OPR/.test(navigator.userAgent) — the only environment where the Add-to-Dock pointer copy is correct. eligible(): return false if window.matchMedia('(display-mode: standalone)').matches (already installed), false if localStorage 'sg.installNudgeDismissed' is set (try/catch), false if the phone-class helper (added in Task 3) reports phone-class, and false if NEITHER a deferredPrompt was captured NOR isMacSafari() — otherwise true. The per-browser gate (D-12) means: desktop Firefox (event never fires, not Safari) → ineligible; Chromium where run() beats the late-firing beforeinstallprompt (Pitfall 1) → ineligible THIS session, the slot passes to the next surface and the nudge competes again a later session — since dismissal is gone-forever (D-14), the one ask is never burned on wrong copy or a dead button. Note D-13 needs no session bookkeeping: because 'install-nudge' sits below 'welcome' in PRECEDENCE, welcome naturally wins launch 1 and the nudge only surfaces from a later session.

    show(): build a dismissable card (bottom inline-end anchored, class per Task 3 CSS). Card title data-i18n onboard.install.title, body data-i18n onboard.install.body, all copy via textContent/data-i18n (no interpolated innerHTML). Platform branch on the captured deferredPrompt (from Plan 02): if present, render a single filled [Install app] button (data-i18n onboard.install.ctaInstall, the ONLY accent element) whose click calls deferredPrompt.prompt() exactly once, then nulls the module deferredPrompt and removes/disables the button (Pitfall 2); otherwise (which, given the eligible() gate, means actual macOS Safari) render the Safari pointer line (data-i18n onboard.install.safariHint) + a neutral 'See install help' link (data-i18n onboard.install.safariLink) href ./help.html#installing (the Phase 39 install section — confirm the resolvable anchor id in help.js/help-content-en.js and prefer the topic id topic-install-safari if it resolves) — never a fake button. Also render a neutral dismiss ('No thanks', data-i18n onboard.install.dismiss) + a muted reassurance line (data-i18n onboard.install.laterHint). Dismiss writes localStorage 'sg.installNudgeDismissed'='1' (persistent, D-14, try/catch) and removes the card.
  </action>
  <verify>
    <automated>node tests/40-install-nudge.test.js && node tests/40-coordinator.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The install-nudge portions of `node tests/40-install-nudge.test.js` pass (standalone/dismissed/phone-class gates, the (c2) Firefox-like no-event non-Safari → ineligible case, prompt()-once Chromium branch, macOS-Safari-only pointer branch, persistent dismissal).
    - `node tests/40-coordinator.test.js` stays green (registration did not break arbitration).
    - The install card's only accent element is the [Install app] button; dismiss + Safari link are neutral.
    - The Safari branch renders a link to the Phase 39 install help topic and no prompt()-calling button; the Safari pointer copy never renders on a non-Safari UA (eligible() gate).
  </acceptance_criteria>
  <done>The install-nudge surface is registered and green: real Chromium button (prompt-once), macOS-Safari-only pointer branch, ineligible everywhere else (no-event non-Safari passes the slot), standalone/dismissed/phone-class gating, and persistent gone-forever dismissal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the mobile-hint surface + phone-class helper + install/hint CSS</name>
  <files>assets/attention-coordinator.js, assets/app.css</files>
  <behavior>
    - Phone-class helper: returns true for uaMobile OR (pointer:coarse AND max-width:820px).
    - mobile-hint eligible(): phone-class AND not localStorage 'sg.mobileHintDismissed'.
    - mobile-hint show(): neutral bottom bar with body + topic link + 'Got it'; dismiss writes the persistent flag.
    - CSS: install card (bottom inline-end, --shadow-card, max-width ≈380px) and mobile hint (bottom full-width, neutral surface, no accent fill) — logical properties, tokens only.
  </behavior>
  <read_first>
    - assets/attention-coordinator.js (register the mobile-hint surface + add the shared phone-class helper used by both this surface and the install-nudge eligibility)
    - assets/app.css (existing bottom-anchored / banner surface idioms e.g. `.backup-reminder-banner` ~2476, `.security-guidance-note` ~2779; token comment style)
    - assets/tokens.css (semantic tokens — the only allowed values)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-RESEARCH.md (Pattern 4 phone-class; Open Question 1 threshold 820px)
    - .planning/phases/40-first-run-welcome-onboarding-coordinator/40-UI-SPEC.md (Mobile expectation hint contract row — neutral surface, NO --color-primary fill, no warning tone; Install nudge placement)
  </read_first>
  <action>
    In assets/attention-coordinator.js add a phone-class helper: isPhoneClass() returns (navigator.userAgentData && navigator.userAgentData.mobile) OR (window.matchMedia('(pointer: coarse)').matches AND window.matchMedia('(max-width: 820px)').matches) — capability probe, not iOS-only UA sniffing (D-16); document the 820px choice inline. Wire this helper into the install-nudge eligible() (Task 2) so the two surfaces are mutually exclusive by device class in practice (desktop → nudge, phone → hint). Register {id:'mobile-hint', eligible, show}: eligible() = isPhoneClass() AND not localStorage 'sg.mobileHintDismissed' (try/catch). show(): build a bottom-anchored full-width neutral hint bar — body data-i18n onboard.mobileHint.body, a topic link data-i18n onboard.mobileHint.link href ./help.html#installing (the Phase 39 mobile-expectations topic; prefer the resolvable topic id topic-install-mobile-note if it anchors — confirm in help.js/help-content-en.js), and a neutral 'Got it' dismiss data-i18n onboard.mobileHint.dismiss. Dismiss writes localStorage 'sg.mobileHintDismissed'='1' (persistent, one-shot forever, D-16) and removes the bar. Copy via textContent/data-i18n only.

    In assets/app.css add two token-only, logical-property blocks: (1) install-nudge card — position fixed, inset-block-end + inset-inline-end anchored, --color-surface bg, --shadow-card, --color-border outline, max-width ≈380px, md/lg internal spacing, 44×44 targets, focus-visible 2px solid var(--color-primary)+2px; the single [Install app] button filled --color-primary, dismiss/link neutral. (2) mobile expectation hint — position fixed, inset-block-end anchored, full-width bar, --color-surface bg, --color-text copy, soft top border/shadow, NO --color-primary fill and no warning tone (it must NOT reuse the deleted banner's accent bar), topic link as a plain underlined text link, 'Got it' neutral, 44×44 targets + focus outline. Both blocks: logical properties only (inset-inline/inset-block, border-inline/border-block), weights 400/700, dark-aware via token auto-resolution.
  </action>
  <verify>
    <automated>node tests/40-install-nudge.test.js && node tests/run-all.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/40-install-nudge.test.js` exits 0 (both install + mobile-hint sections green).
    - `node tests/run-all.js` green.
    - The mobile-hint CSS block contains no `--color-primary` background/fill (grep the block) — neutral surface only.
    - Both new CSS blocks use only `var(--*)` tokens and logical properties (no literal hex, no physical left/right).
    - The phone-class helper references `pointer: coarse` + `max-width: 820px` + `userAgentData` (capability probe, not `/iP(hone|ad|od)/` UA sniffing).
  </acceptance_criteria>
  <done>The mobile-hint surface + shared phone-class helper are registered and green; install-card and neutral mobile-hint CSS are token-only, logical, accessible; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser install engine → page | The deferred `beforeinstallprompt` is re-fired by the page's [Install app] button; the native dialog is browser-mediated (page cannot style/overlay it). |
| copy/i18n → DOM | Nudge/hint copy set into the DOM. |
| navigator/matchMedia capability → eligibility | Phone-class + standalone detection drives which surface shows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-03-XSS | Tampering | nudge/hint DOM build | medium | mitigate | Copy via `textContent`/`data-i18n`; no variable-interpolated innerHTML (grep-gated). |
| T-40-03-CLICK | Spoofing | native install prompt | low | accept | `beforeinstallprompt.prompt()` is a browser-mediated dialog the page cannot overlay; no extra control needed. |
| T-40-03-STOR | Tampering | dismissal flags | low | accept | Single-user local app; clearing a flag only re-offers the surface; try/catch guards. |
| T-40-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed this phase; nothing to vet. |
</threat_model>

<verification>
- `node tests/40-install-nudge.test.js` exits 0; `node tests/run-all.js` green.
- Install card: single accent [Install app] button; pointer + help link render only on actual macOS Safari; a no-event non-Safari env (desktop Firefox, or Chromium before its event fires) is ineligible — nothing shown, nothing dismissed, slot passes on.
- Mobile hint: neutral surface, no `--color-primary` fill; one-shot persistent dismissal.
- Phone-class helper is a capability probe (pointer/width/userAgentData), not iOS UA sniffing.
- Real Chromium (real Install button) + real WebKit/mobile visual checks deferred to phase UAT.
</verification>

<success_criteria>
The install nudge offers one honest one-click install on Chromium and an Add-to-Dock pointer only on actual macOS Safari — never a fake button, never wrong-browser copy (any other no-event env is ineligible and retries a later session), never when installed, dismissed-forever; the mobile hint sets calm expectations on all phones once and stays gone — both governed at the bottom of the precedence order, all pinned green.
</success_criteria>

<output>
Create `.planning/phases/40-first-run-welcome-onboarding-coordinator/40-03-SUMMARY.md` when done.
</output>
