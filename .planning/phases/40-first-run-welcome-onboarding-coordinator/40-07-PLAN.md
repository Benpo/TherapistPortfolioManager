---
phase: 40-first-run-welcome-onboarding-coordinator
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/app.js
  - assets/app.css
  - tests/39-help-entry.test.js
autonomous: true
gap_closure: true
requirements: [ONBD-02]
must_haves:
  truths:
    - "The header shows only the '?' help entry — the duplicate green 'Help' nav pill is gone"
    - "The '?' menu opens with NO row appearing preselected; <button> rows match <a> rows in light AND dark"
  artifacts:
    - "assets/app.js — renderNav() with the redundant Help <a> removed"
    - "assets/app.css — .help-entry-item carries a native-<button> reset"
    - "tests/39-help-entry.test.js — removal-regression guard (renderNav emits no data-nav=help anchor)"
  key_links:
    - "renderNav() → no a[data-nav=\"help\"] anchor emitted"
    - ".help-entry-item reset applies identically to <a> and <button> rows (:hover still reads on both)"
---

<objective>
Close two UAT visual gaps in the Phase 39 "?" help entry (D-09 popover):

1. MINOR — renderNav() hardcodes an `<a href=./help.html data-nav=help>Help</a>` (app.js:148,
   added in Phase 39 alongside the "?" entry) that duplicates the "?" icon button. Ben wants
   only the "?" icon. Remove the redundant nav pill; keep the "?" entry.
2. COSMETIC — the menu's action row is a native `<button>` sharing `.help-entry-item` with the
   `<a>` link rows, but the class never resets native button chrome, so it renders as a grey
   box with a border ("preselected") in both light and dark (40-UAT.md test 2). Reset the
   button chrome so `<button>` rows are visually identical to `<a>` rows.

Both fixes are within the "?" help-entry surface (ONBD-02 — re-open help/tour from "?").

Purpose: the header shows a single, clean help affordance and the menu opens with nothing
looking preselected.
Output: nav pill removed (+ regression guard), `.help-entry-item` button reset.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# renderNav (nav pill at ~L148) + initHelpEntry (the "?" menu, <a> vs <button> rows)
@assets/app.js
# .help-entry-item rule (~L216) that needs the button reset
@assets/app.css
# The test that currently PINS the Help nav anchor (test #1 must become a removal guard)
@tests/39-help-entry.test.js
@.planning/phases/40-first-run-welcome-onboarding-coordinator/40-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the redundant Help nav pill + convert its test to a removal guard</name>
  <files>assets/app.js, tests/39-help-entry.test.js</files>
  <action>
    In renderNav() (assets/app.js, the template around line 148), delete the single Help nav
    anchor line — the `<a>` with href ./help.html, data-nav help, and data-i18n nav.help. Leave
    every other nav entry untouched. Do NOT change the active-state loop below it (it iterates
    all a[data-nav] generically and simply will no longer find a help entry — no help-specific
    active handling exists to remove). The "?" entry button keeps its OWN is-active handling in
    initHelpEntry() — do not touch that.

    Do NOT remove the nav.help i18n key from any locale file. It stays defined (now unused) in
    all four locales — removing it would require editing i18n-he/de/cs, which is out of scope
    this phase (locale translation work is deferred to Phase 42.1). An unused-but-present key is
    harmless and keeps the standing 4-locale parity gates green. Confirm nav.help has no
    remaining consumer other than the (now-removed) anchor: grep the assets + html for nav.help
    and verify only the four locale definitions remain.

    Update tests/39-help-entry.test.js: the current test #1 ("renderNav() emits a Help anchor …",
    around lines 83-94) asserts the pill's PRESENCE and will now fail. Replace its body with a
    REMOVAL-regression guard: call env.App.renderNav() and assert that
    document.querySelector('#nav-placeholder a[data-nav="help"]') is null (renderNav must NOT
    emit a Help nav anchor). Rename the test title accordingly (e.g. "renderNav() emits NO Help
    nav anchor — the '?' entry is the only help affordance"). Update the file's header-comment
    bullet 1 to describe the new guard. Leave tests #2–#6 (the "?" button mount, popover rows,
    is-active, idempotency, toggle) exactly as they are — they still guard the surviving "?"
    entry.
  </action>
  <verify>
    <automated>grep -c 'data-nav="help"' assets/app.js | grep -qx 0 && node tests/39-help-entry.test.js && node tests/run-all.js</automated>
  </verify>
  <done>renderNav() emits no Help nav anchor; app.js contains no `data-nav="help"`; tests/39-help-entry.test.js passes with test #1 now guarding the removal; nav.help remains defined in all four locales; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Reset native button chrome on .help-entry-item</name>
  <files>assets/app.css</files>
  <action>
    In the `.help-entry-item` rule (assets/app.css around line 216), add a native-control reset
    so `<button>` rows render identically to the `<a>` rows and no longer show the UA's grey box
    + border ("preselected"). Add these declarations to the rule: appearance none AND
    -webkit-appearance none (the -webkit- prefix is required so Safari/WebKit also drops native
    button chrome — project memory: Chromium-only checks have missed Safari-only rendering bugs),
    background transparent, border 0, width 100%, cursor pointer, and font inherit. CRITICAL
    ordering: place `font: inherit;` BEFORE the existing `font-size: 0.9rem;` (re-declare
    font-size AFTER font:inherit if needed) so both `<a>` and `<button>` end up at 0.9rem — the
    `font` shorthand would otherwise reset size to the inherited value and make button rows a
    different size than anchor rows. Keep ALL existing declarations (display block, padding,
    border-radius, color, text-decoration none, text-align start, white-space nowrap) and keep
    the existing `.help-entry-item:hover` rule unchanged so hover still reads on BOTH element
    types. Token-only / logical properties only (no literal hex, no physical left/right) — match
    the file's conventions.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('assets/app.css','utf8');const m=c.match(/\.help-entry-item\s*\{[^}]*\}/);if(!m)throw new Error('rule not found');const r=m[0];['appearance: none','-webkit-appearance: none','background: transparent','border: 0','width: 100%','font: inherit','cursor: pointer'].forEach(d=>{if(r.indexOf(d)===-1)throw new Error('missing reset decl: '+d)});console.log('button reset present')" && node tests/run-all.js</automated>
    <human-check>Open the "?" menu in light AND dark mode, in Chromium AND Safari (WebKit): the action row no longer looks grey/preselected, matches the anchor rows, and :hover highlights identically on both a link row and the button row.</human-check>
  </verify>
  <done>.help-entry-item carries the full button reset (appearance/-webkit-appearance/background/border/width/font/cursor) with font-size preserved at 0.9rem; button rows are visually indistinguishable from anchor rows; :hover reads on both; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan removes a static nav anchor and adds CSS reset declarations — no new untrusted-input path, no new markup source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07-01 | Tampering | renderNav() template edit | low | accept | The nav template is a compile-time literal with no interpolation; removing one static anchor introduces no injection surface. Guarded by the updated removal test. |
| T-40-07-02 | Information Disclosure | .help-entry-item CSS reset | low | accept | Pure presentational reset; no data or behavior change. |

No package-manager installs in this plan (no supply-chain checkpoint required).
</threat_model>

<verification>
- `grep -c 'data-nav="help"' assets/app.js` returns 0.
- `node tests/39-help-entry.test.js` — green, with test #1 guarding the removal.
- The `.help-entry-item` rule contains the full button reset; `node tests/run-all.js` green.
- Human on-device check (light/dark, Chromium/Safari) confirms the row no longer looks preselected.
</verification>

<success_criteria>
The header shows only the "?" help affordance (no green Help pill), and the "?" menu opens with no row looking preselected — button rows match anchor rows in light and dark, with :hover intact.
</success_criteria>

<output>
Create `.planning/phases/40-first-run-welcome-onboarding-coordinator/40-07-SUMMARY.md` when done.
</output>
