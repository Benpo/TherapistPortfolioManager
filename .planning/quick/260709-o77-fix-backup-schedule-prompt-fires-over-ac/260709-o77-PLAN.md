---
phase: 260709-o77-fix-backup-schedule-prompt-fires-over-ac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/quick-260709-o77-backup-schedule-tour-guard.test.js
  - assets/backup.js
autonomous: true
requirements: ["260709-o77"]

must_haves:
  truths:
    - "With the tour overlay active on this page, a DUE backup schedule check does NOT open the Backup & Restore modal."
    - "On the tour-suppressed path, portfolioBackupSchedulePromptedAt is NOT written, so the reminder re-fires naturally after the tour closes (mirrors the CR-01 successful-prompt-only write gate)."
    - "With the tour inactive, or on a page where window.Tour is undefined, a DUE schedule check opens the modal exactly as before (no regression)."
  artifacts:
    - "tests/quick-260709-o77-backup-schedule-tour-guard.test.js"
    - "assets/backup.js (checkBackupSchedule tour-active guard)"
  key_links:
    - "checkBackupSchedule() -> window.Tour.isActive() (typeof-guarded read of the tour public surface; overlay-mounted signal, not sg.tourResume persisted state)"
---

<objective>
Fix the Phase 41 release blocker: `checkBackupSchedule()` (assets/backup.js, D-17 foreground schedule check) opens the Backup & Restore modal on top of an active onboarding tour when a long-idle tab fires `visibilitychange` and the schedule prompt is due.

Add a minimal tour-active guard inside `checkBackupSchedule()` that suppresses the prompt while the tour overlay is currently mounted on this page, WITHOUT consuming the 1-hour debounce key — so the reminder re-fires at the next opportunity after the tour closes.

Purpose: Prevent the broken-looking modal-over-tour collision (screenshot on file) before release.
Output: One RED behavior test (falsifiable) + one surgical guard in backup.js. No app.js, no attention-coordinator.js, no sw.js change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/todos/pending/2026-07-09-backup-schedule-prompt-fires-over-active-onboarding-tour.md

# The code to guard — checkBackupSchedule() and the CR-01 successful-prompt-only write gate:
@assets/backup.js

# The tour public surface — window.Tour = { start, resume, isActive, ... }.
# isActive() (line 657) returns the in-memory `active` flag: true when render()
# mounts the overlay (line 282), false on teardown/close (529/582). This is the
# "overlay currently mounted on THIS page" signal — the correct guard signal, and
# distinct from the persisted sg.tourResume cross-page resume tier.
@assets/tour.js

# Test harness conventions + the closest existing idiom to copy (vm sandbox that
# loads backup.js and drives checkBackupSchedule({ now })):
@.planning/codebase/TESTING.md
@tests/25-09-schedule-debounce-no-modal.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing behavior test for the tour-active backup-prompt guard</name>
  <files>tests/quick-260709-o77-backup-schedule-tour-guard.test.js</files>
  <action>
Create a new self-contained Node test following the vm-sandbox idiom in tests/25-09-schedule-debounce-no-modal.test.js (Node built-in `assert`, hand-rolled `test()` runner, `process.exit(failed===0?0:1)`, no jsdom). Adapt its `makeSandbox` factory and its `seedScheduleReadyToFire` helper (schedule=daily, password acked, lastExport 25h ago, debounce stamp 2h ago) so the schedule check is DUE and only the trailing prompt block remains.

Extend the sandbox factory to accept a tour-state argument and, when supplied, set `window.Tour = { isActive: function () { return tourActive; } }` on the sandbox window BEFORE calling `checkBackupSchedule`. When the argument is omitted, leave `window.Tour` undefined (models a page that does not load tour.js).

Assert exactly these falsifiable cases, each with a fresh sandbox and a spy that counts `window.openBackupModal` calls:
  - Case A (tour ACTIVE, modal available, pathname=/index.html): after `checkBackupSchedule({ now })`, openBackupModal call count MUST be 0, `portfolioBackupSchedulePromptedAt` MUST remain the seeded 2h-ago value (NOT advanced to ~now), and location.href MUST be unchanged. This is the case that currently FAILS (the bug).
  - Case B (tour INACTIVE — window.Tour.isActive returns false, modal available, pathname=/index.html): openBackupModal call count MUST be exactly 1 AND the stamp MUST advance to ~now (regression guard: fires as before).
  - Case C (window.Tour UNDEFINED, modal available, pathname=/index.html): openBackupModal call count MUST be exactly 1 (proves the typeof-guard passes through on pages without tour.js — no throw, no behavior change).

Add a header comment stating this is the RED test for quick task 260709-o77 and that Case A is expected to FAIL against the unpatched backup.js. Use `Math.abs(stampAfter - now) < 5000` for the "advanced to ~now" checks, matching the 25-09 idiom. Do NOT write fenced pseudo-code that duplicates the guard string into the file; assert behavior only.
  </action>
  <verify>
    <automated>node tests/quick-260709-o77-backup-schedule-tour-guard.test.js; test $? -ne 0</automated>
  </verify>
  <done>The test file exists, is auto-discovered by tests/run-all.js (top-level tests/*.test.js), and currently EXITS NON-ZERO because Case A fails against the unpatched backup.js (modal is called + stamp advances). Cases B and C pass. This proves the test is a real RED, not vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the typeof-guarded tour-active skip in checkBackupSchedule()</name>
  <files>assets/backup.js</files>
  <behavior>
    - Tour active + prompt due: openBackupModal NOT called; portfolioBackupSchedulePromptedAt NOT written (re-fires after tour closes).
    - Tour inactive + prompt due: modal opens and stamp advances (unchanged).
    - window.Tour undefined (page without tour.js): typeof-guard passes through; modal opens as before, no throw.
  </behavior>
  <action>
In `checkBackupSchedule(opts)` (assets/backup.js, ~line 1500), add an early return that fires when the tour overlay is currently mounted on this page. Position it BEFORE the debounce-stamp write block (the CR-01 comment + `window.openBackupModal()` attempt at ~1524-1561) so that no code path can write `portfolioBackupSchedulePromptedAt` on the suppressed run — placing it at the top of the function body (right after `now`/`intervalMs` are resolved, or immediately before the modal-open block) both satisfy this; choose the top for readability.

Detect the tour via the real public surface `window.Tour.isActive()`. This MUST be fully typeof-guarded because backup.js loads on pages that never load tour.js: guard on `typeof window !== 'undefined'` AND `window.Tour` truthy AND `typeof window.Tour.isActive === 'function'`, then call it inside a try/catch (matching the file's defensive-storage idiom) and return only when it yields truthy; treat any throw as "not active" (fall through to the normal prompt path). Use `isActive()` — the mounted-overlay signal — NOT the persisted sg.tourResume state; the guard suppresses whenever the tour OVERLAY is visible on this page.

Add a one-line comment explaining WHY (Phase 41 escape: the interval-end modal prompt must not open over the active tour overlay; suppressed path intentionally skips the debounce write so the reminder re-fires post-tour, mirroring the CR-01 successful-prompt-only write gate). Do NOT route through AttentionCoordinator and do NOT touch assets/attention-coordinator.js or assets/app.js — the guard lives entirely inside checkBackupSchedule.
  </action>
  <verify>
    <automated>node tests/quick-260709-o77-backup-schedule-tour-guard.test.js && node tests/25-09-schedule-debounce-no-modal.test.js && node tests/run-all.js</automated>
    <automated>test -z "$(git diff --name-only -- assets/attention-coordinator.js assets/app.js sw.js)"</automated>
  </verify>
  <done>The new quick test now EXITS ZERO (Case A passes: tour active -> modal not called + stamp untouched). tests/25-09 still passes (CR-01 debounce logic intact). Full suite (node tests/run-all.js) is green. The guard change is confined to assets/backup.js — git diff shows no change to attention-coordinator.js, app.js, or sw.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkBackupSchedule -> window.Tour | First-party same-origin app code reads a boolean seam. No untrusted input crosses; risk is availability/robustness, not injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o77-01 | Denial of Service | checkBackupSchedule reading window.Tour.isActive | low | mitigate | Full typeof-guard (window defined, window.Tour truthy, isActive is a function) + try/catch; any throw is treated as "tour inactive" so the schedule prompt still works — a broken/absent tour global can never disable the backup reminder. |
| T-o77-02 | Tampering (stuck-state) | tour `active` flag never clearing -> reminder permanently suppressed | low | accept | `active` is cleared on tour teardown/close (tour.js 529/582); the suppressed path also skips the debounce write, so the reminder re-fires the next opportunity after the overlay unmounts. No persistence, no cross-page stickiness. |

No package-manager installs in this task — no supply-chain (T-*-SC) threat row required.
</threat_model>

<verification>
- RED proof: on the unpatched backup.js, `node tests/quick-260709-o77-backup-schedule-tour-guard.test.js` exits non-zero (Case A fails).
- GREEN proof: after the guard, the same test exits zero and `node tests/run-all.js` is fully green.
- Regression: `node tests/25-09-schedule-debounce-no-modal.test.js` still passes (CR-01 write-only-on-prompt gate unchanged).
- Scope containment: `git diff --name-only` touches only `assets/backup.js` and the new test file — NOT attention-coordinator.js, app.js, or sw.js.
</verification>

<success_criteria>
- Tour overlay active + schedule due: Backup & Restore modal does NOT open and `portfolioBackupSchedulePromptedAt` is NOT written.
- Tour inactive, or page without tour.js: schedule prompt fires exactly as before.
- Reminder re-fires naturally after the tour closes (no consumed debounce on the suppressed path).
- Test-first (RED then GREEN) satisfied; full suite green; fix confined to backup.js + one new (non-shipped) test file.
</success_criteria>

<output>
Create `.planning/quick/260709-o77-fix-backup-schedule-prompt-fires-over-ac/260709-o77-SUMMARY.md` when done.
</output>