---
phase: quick-260626-h5j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/snippets.js
  - assets/settings.js
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - tests/quick-260626-h5j-recall-smart-commit.test.js
  - tests/quick-260626-h5j-trigger-autoconvert.test.js
autonomous: true
requirements: [QUICK-260626-h5j]

must_haves:
  truths:
    - "Recall: typing `;physical ` (space) expands `physical-trauma` because exactly ONE trigger prefix-matches `physical`; `;low ` expands `low-self-esteem`."
    - "Recall: `;anger ` STILL expands the exact single-word trigger `anger` — the exact-match terminator path is byte-for-byte unchanged (53/60 seeds depend on it)."
    - "Recall: `;s ` (2+ prefix matches) does NOT auto-commit, and `;xyz ` (0 matches) does nothing — neither returns a 'match' result."
    - "Recall: a query that prefix-matches only a snippet's TAG (no trigger prefix-match) does NOT auto-commit — the single-candidate set is TRIGGER-prefix-matches ONLY; tag-fallback stays popover-only (seam dd78aff preserved)."
    - "Recall: sole-candidate auto-commit is case-insensitive (`;PHYSICAL `/`;Physical ` -> physical-trauma) and Unicode-aware (a non-ASCII hyphenated trigger auto-commits), while the candidate computation stays single-pass so the ReDoS <50ms guard holds (seams 80dc91d/92a07ad preserved)."
    - "Recall: when the sole-candidate auto-commit fires, the boundary char the user typed is left in place (NOT consumed) and the expansion + caret land exactly as the exact-match path does — no doubled or swallowed space."
    - "Editor: typing a space in the trigger input live-converts to a regular hyphen-minus U+002D (never em U+2014 / en U+2013 dash), so `physical trauma` becomes `physical-trauma` and saves with no format error."
    - "Editor: the same space->hyphen conversion is applied defensively in handleSave before validation, so a space can never reach the format error via paste or programmatic set."
    - "Editor: the `snippets.editor.trigger.error.format` copy in all four locales (en/he/de/cs) now guides the user to use a hyphen instead of a space, with `physical-trauma` as the example."
    - "Two falsifiable Node behavior tests exist that are RED before implementation and GREEN after; existing snippet tests (24-04, 260619-okw) still pass."
  artifacts:
    - path: "assets/snippets.js"
      provides: "detectTrigger single-candidate auto-commit branch (boundary present + no exact match + exactly one trigger prefix-match)"
    - path: "assets/settings.js"
      provides: "Pure space->hyphen helper exported on window.__SnippetEditorHelpers; live trigger-input listener + defensive handleSave conversion"
    - path: "assets/i18n-en.js"
      provides: "Reworded snippets.editor.trigger.error.format (no-spaces-use-a-hyphen)"
    - path: "assets/i18n-he.js"
      provides: "Reworded snippets.editor.trigger.error.format (raw-Hebrew style)"
    - path: "assets/i18n-de.js"
      provides: "Reworded snippets.editor.trigger.error.format (\\uXXXX-escaped style)"
    - path: "assets/i18n-cs.js"
      provides: "Reworded snippets.editor.trigger.error.format (\\uXXXX-escaped style)"
    - path: "tests/quick-260626-h5j-recall-smart-commit.test.js"
      provides: "Falsifiable detectTrigger behavior test (sole-candidate / exact / ambiguous / none)"
      min_lines: 80
    - path: "tests/quick-260626-h5j-trigger-autoconvert.test.js"
      provides: "Falsifiable space->hyphen helper + isValidTrigger behavior test"
      min_lines: 60
  key_links:
    - from: "detectTrigger hasBoundary && no-exact branch (assets/snippets.js ~L109-119)"
      to: "the returned 'match' result consumed by handleInput -> insertExpansion (assets/snippets.js ~L399-403)"
      via: "single-candidate match MUST reuse the exact-match start/end/boundary (end = matchEnd - boundary.length) so insertExpansion replaces prefix+partial only and the boundary char stays"
    - from: "trigger input live listener + handleSave (assets/settings.js)"
      to: "isValidTrigger / TRIGGER_REGEX validation"
      via: "both paths route the raw value through the new space->hyphen helper BEFORE validation"
---

<objective>
Two aligned snippet-trigger improvements so a typed space never dead-ends the user — on BOTH the editor (creation) side and the recall (expansion) side. This is a behavior + copy change, NOT a Czech bug fix (investigation proved there is no Czech-specific bug; the seed convention for multi-word emotions is hyphenation).

1. Recall smart-commit: in `detectTrigger`, when the user types a boundary char after `<prefix><partial>` and there is no exact trigger, auto-expand iff EXACTLY ONE trigger prefix-matches the partial (`;physical ` -> `physical-trauma`). Ambiguous (2+) and no-match (0) cases stay unchanged. The sacred single-word exact-match terminator (`;anger `) is untouched.
2. Editor auto-convert + clearer error: live-convert spaces to a regular hyphen-minus `-` (U+002D) in the trigger input and defensively in handleSave, and reword the format error in all four locales to say "no spaces — use a hyphen, e.g. physical-trauma".

Purpose: Multi-word emotions (`physical-trauma`, `low-self-esteem`) become both creatable and recallable when the user instinctively types a space, while the 53 single-word seed triggers that rely on space-as-terminator keep working exactly as today.
Output: Modified `assets/snippets.js` + `assets/settings.js` + 4 i18n files; 2 new falsifiable behavior tests.
</objective>

<coverage_note>
## Recall path coverage (confirmed — do NOT go hunting for more call sites)

- `detectTrigger` (assets/snippets.js) is the SINGLE recall chokepoint. There are ZERO call sites of detectTrigger / insertExpansion / resolveExpansion / commitSelected / showPopover / getSnippetMap outside assets/snippets.js. Fixing detectTrigger fixes recall everywhere.
- ONE engine reached from TWO bind sites: (a) `Snippets.init()` on DOMContentLoaded binds every `[data-snippets="true"]` session textarea; (b) `add-session.js:1469` re-binds the EXPORT EDITOR via `Snippets.bindTextarea(editor)`. The detectTrigger change covers BOTH automatically — do NOT edit add-session.js.
- No keystroke interference: the only keydown handlers are modal-level (Escape) and the popover's handleKeyDown (Arrow/Enter/Tab/Esc — never space). Nothing preventDefaults the space. Co-listeners (autoGrow, export-preview onEditorInput) already react via the synthetic `input` event insertExpansion dispatches — unchanged.
- Known-fragile seams the new branch sits on: detection-regex/validation-regex alignment (commits 80dc91d, 92a07ad) and the tag-trigger fallback (dd78aff). The single-candidate boundary auto-commit must NOT disturb either — guarded by the Task-1 regression assertions and the 24-04 / 260619-okw regression gate.
</coverage_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260626-h5j-snippet-triggers-auto-convert-spaces-to-/260626-h5j-CONTEXT.md
@.planning/STATE.md
@assets/snippets.js
@assets/settings.js
@tests/24-04-trigger-regex.test.js
@tests/quick-260619-okw-trigger-unicode.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recall smart-commit for the sole prefix-matching trigger</name>
  <files>assets/snippets.js, tests/quick-260626-h5j-recall-smart-commit.test.js</files>
  <behavior>
    detectTrigger(textBeforeCaret, prefix, snippetsByTrigger) — boundary-present cases, using an in-test Map mirroring the seed triggers. The map MUST include: single-word `anger` and `shame`; `physical-trauma`, `low-self-esteem`, `heart-shock`; 3+ triggers starting with `s` (e.g. `sadness`, `shame`, `shock`, `sorrow`) for the ambiguous case; one non-ASCII hyphenated trigger for the Unicode case (e.g. a Czech `ztráta-důvěry` or Hebrew slug — exactly one prefix-match for its partial); and one snippet with NO trigger prefix-match for the query but a TAG that prefix-matches it (for the tag-only case):
    - Test 1 (sole candidate): `'note ;physical '` -> result.type === 'match' and result.snippet.trigger === 'physical-trauma'.
    - Test 2 (sole candidate): `'note ;low '` -> match, snippet.trigger === 'low-self-esteem'.
    - Test 3 (exact wins, UNCHANGED): `'note ;anger '` -> match, snippet.trigger === 'anger' (exact lookup, never the prefix branch). Also `'note ;shame '` -> exact match `shame` (single-word seed terminator, byte-for-byte today's behavior).
    - Test 4 (ambiguous 2+): `'note ;s '` -> result is NOT a 'match' (assert result === null || result.type !== 'match'); no expansion, popover semantics unchanged.
    - Test 5 (no match): `'note ;xyz '` -> result === null.
    - Test 6 (placement/boundary contract): for `'note ;physical '` assert result.start/result.end cover exactly the `;physical` span (prefix+partial) and EXCLUDE the trailing space, i.e. result.boundary === ' ' and textBeforeCaret.slice(result.end) === ' '. Guarantees insertExpansion leaves the typed space in place.
    - Test 7 (partial still works, UNCHANGED): `'note ;physi'` (no boundary) -> result.type === 'partial'.
    REGRESSION assertions (the new branch sits on the fragile detection-regex/tag-fallback seams — these MUST be explicit and falsifiable):
    - Test 8 (case-insensitive sole candidate): `'note ;PHYSICAL '` AND `'note ;Physical '` -> match, snippet.trigger === 'physical-trauma', with result.end still excluding the boundary (result.boundary === ' '). Typed space survives exactly like the `;anger ` exact path.
    - Test 9 (tag-only NEVER auto-commits): pick a query that prefix-matches a snippet's TAG but NO trigger, type `'note ;<tagquery> '` -> result is NOT a 'match' (null or partial/popover). The single-candidate set is TRIGGER-prefix-matches ONLY; tag matches never auto-expand.
    - Test 10 (Unicode sole candidate): type `<prefix><unicode-partial><space>` for the non-ASCII trigger that has exactly one prefix-match -> match for that trigger; confirms the boundary branch stays Unicode-aware like the partial branch.
    - Test 11 (ReDoS preserved): the adversarial `';'.repeat(9999) + 'a'` input, run a few iterations, completes < 50ms — the candidate computation must iterate the snippet map AT MOST ONCE per input (no nested scan).
    Load real assets/snippets.js in a vm sandbox; read detectTrigger via window.Snippets.__testExports (same pattern as tests/24-04-trigger-regex.test.js). Exit 0 all-pass, 1 on any failure.
  </behavior>
  <action>
    FIRST write tests/quick-260626-h5j-recall-smart-commit.test.js per the behavior block and run it — confirm RED (Tests 1, 2, 6 fail because detectTrigger currently returns null whenever a boundary is present and there is no exact trigger). Mirror the vm-sandbox + __testExports harness of tests/24-04-trigger-regex.test.js exactly (minimal window/document/localStorage stubs, fixture snip() helper, makeMap()).

    THEN implement in assets/snippets.js detectTrigger, inside the existing `if (hasBoundary)` branch (~L109): keep the exact-match lookup and its return identical. Only when `snippet` is falsy (no exact match), compute the set of snippets whose trigger prefix-matches `triggerText` with a SINGLE pass over the snippet map (reuse the SAME `trigger.startsWith(triggerText)` test already used in the partial branch — extract a tiny pure helper if it reads cleaner; one loop, no nested scan, to keep the ReDoS budget). Do NOT include tag-fallback matches — a tag match is not an unambiguous trigger, so TRIGGER prefix-matches ONLY. If that set has length EXACTLY 1, return a match result for that single snippet using the EXACT same shape as the exact path: { type: "match", start: matchStart, end: matchEnd - m[3].length, boundary: m[3], snippet: <the one> }. If the set length is 0 or >= 2, return null (today's behavior — preserves popover/no-op semantics; do NOT change the 2+ case, do NOT keep the popover open). The branch must stay Unicode-aware (the regex already supplies a `\p{L}\p{N}-` triggerText; `startsWith` on lowercased triggers is correct for non-ASCII slugs). Add no new exports — detectTrigger is already exported.

    Why `end = matchEnd - m[3].length`: this excludes the boundary char from the replaced range so handleInput's insertExpansion(..., false) (~L402) replaces only `prefix+partial` and the typed space survives — identical caret/space outcome to the sacred exact-match terminator path. Do not append a space (appendSpace stays false on the auto-match path). Touch nothing in the partial branch, resolveExpansion, insertExpansion, or handleInput.
  </action>
  <verify>
    <automated>node tests/quick-260626-h5j-recall-smart-commit.test.js && node tests/24-04-trigger-regex.test.js && node tests/quick-260619-okw-trigger-unicode.test.js</automated>
  </verify>
  <done>The new recall test passes (all of Tests 1–11, including case-insensitive sole-candidate, tag-only-no-commit, 2+-no-commit, Unicode sole-candidate, and ReDoS <50ms). The 24-04 and 260619-okw regression suites still pass GREEN (exact-match, partial-popover, locale-fallback, and tag-fallback outcomes unchanged). `;anger ` and `;shame ` (single-word seeds — 53 of 60) expand byte-for-byte unchanged, and the boundary char survives on every auto-commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor space->hyphen auto-convert + reworded format error in 4 locales</name>
  <files>assets/settings.js, assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js, tests/quick-260626-h5j-trigger-autoconvert.test.js</files>
  <behavior>
    A new pure helper on window.__SnippetEditorHelpers (suggested name hyphenateSpaces; final name at discretion) plus the existing isValidTrigger:
    - Test 1: hyphenateSpaces('physical trauma') === 'physical-trauma'.
    - Test 2 (no fancy dash): the result of hyphenateSpaces('physical trauma') contains NO U+2014 (em dash) and NO U+2013 (en dash); the inserted separator char code is 0x2D.
    - Test 3 (round-trip valid): isValidTrigger(hyphenateSpaces('physical trauma')) === true (a space-containing input becomes hyphenated and then valid).
    - Test 4 (leading/trailing/space-only handled sanely): hyphenateSpaces(' a b ') trimmed has no spaces; assert the helper output, once trimmed, matches /^[\p{L}\p{N}-]+$/u (no whitespace remains).
    - Test 5 (idempotent on already-hyphenated): hyphenateSpaces('low-self-esteem') === 'low-self-esteem'.
    - Test 6 (unicode unaffected): hyphenateSpaces('כעס') === 'כעס' and isValidTrigger('כעס') === true (no regression vs 260619-okw).
    Load real assets/settings.js in a vm sandbox; read helpers via window.__SnippetEditorHelpers (same pattern as tests/quick-260619-okw-trigger-unicode.test.js). Exit 0 all-pass, 1 on any failure.
  </behavior>
  <action>
    FIRST write tests/quick-260626-h5j-trigger-autoconvert.test.js per the behavior block and run it — confirm RED (the helper is not yet exported, so the harness guard fails). Mirror the vm-sandbox harness of tests/quick-260619-okw-trigger-unicode.test.js exactly.

    THEN in assets/settings.js, near isValidTrigger (~L885): add the pure helper that replaces whitespace runs with a single regular hyphen-minus `-` (U+002D) — use `String(value).replace(/\s+/g, "-")`. It MUST NEVER emit an em-dash or en-dash; only U+002D, which is the only dash TRIGGER_REGEX accepts. Export it by adding it to the existing window.__SnippetEditorHelpers object literal (~L863) alongside isValidTrigger.

    Wire LIVE conversion: in the editor wiring block where the modal controls are bound (near where snippetEditorSave is wired, ~L1904), get triggerInput = $("snippetEditorTrigger") and add an `input` listener that, when the field value contains whitespace, captures selectionStart, sets triggerInput.value to the hyphenated value, then restores the caret via setSelectionRange clamped to the new length. Bind it once here (this block runs once; the input element persists across openEditor calls). Assign to triggerInput.value only (never innerHTML) — preserves the no-innerHTML grep gate at settings.js ~L710.

    Wire DEFENSIVE conversion in handleSave (~L1511): change the trigger derivation so the raw value passes through the helper before .trim().toLowerCase(), so any space surviving paste/programmatic-set is converted before isValidTrigger runs. The reworded format error stays as the fallback for genuinely invalid input (e.g. punctuation).

    THEN reword `snippets.editor.trigger.error.format` in all four locale files to convey "Triggers can't contain spaces — use a hyphen, like physical-trauma" (exact wording at discretion; MUST mention no-spaces + hyphen + the example physical-trauma). Match each file's existing escaping style: en is plain ASCII; he is RAW Hebrew text (i18n-he.js L515 is unescaped Hebrew); de and cs use \uXXXX escapes for non-ASCII (i18n-de.js L528, i18n-cs.js L528). The literal example `physical-trauma` is ASCII in every locale and uses a plain hyphen-minus. Do not touch the adjacent ...error.duplicate keys.
  </action>
  <verify>
    <automated>node tests/quick-260626-h5j-trigger-autoconvert.test.js && node tests/quick-260619-okw-trigger-unicode.test.js</automated>
  </verify>
  <done>The new auto-convert test passes (all 6), the 260619-okw isValidTrigger regression still passes, the helper is exported, the live + handleSave conversions are wired through it, and all four locale format strings name the no-space/use-a-hyphen rule with the physical-trauma example in each file's native escaping style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes -> trigger input field -> IndexedDB -> recall insertion into textarea.value | User-authored trigger/expansion text is the only untrusted input; it round-trips through the editor, storage, and the recall engine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h5j-01 | Tampering | trigger-input auto-convert + handleSave (assets/settings.js) | low | mitigate | Conversion writes only to element.value (never innerHTML); validation regex `/^[\p{L}\p{N}-]{2,32}$/u` still gates save — existing no-innerHTML grep gate (settings.js ~L710) preserved. |
| T-h5j-02 | Tampering | detectTrigger single-candidate expansion (assets/snippets.js) | low | accept | Expansion text is inserted via textarea.value assignment only (insertExpansion, unchanged); no new DOM sink, no markup parsing introduced. |
| T-h5j-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this task — zero new dependencies. |
</threat_model>

<verification>
- `node tests/quick-260626-h5j-recall-smart-commit.test.js` exits 0 (Tests 1–11, incl. case-insensitive, tag-only-no-commit, Unicode, ReDoS).
- `node tests/quick-260626-h5j-trigger-autoconvert.test.js` exits 0 (6/6).
- Regression gate (must be GREEN): `node tests/24-04-trigger-regex.test.js` and `node tests/quick-260619-okw-trigger-unicode.test.js` both exit 0.
- Manual sanity (optional, not a gate): in the snippet editor type `physical trauma` -> field self-corrects to `physical-trauma`; in a session textarea type `;physical ` -> expands to the physical-trauma text; `;anger ` -> still expands anger.
</verification>

<success_criteria>
- detectTrigger auto-commits ONLY when exactly one trigger prefix-matches a boundary-terminated partial; exact-match and 0/2+ paths are byte-for-byte unchanged.
- The boundary char is preserved on auto-commit (no doubled/swallowed space; caret matches the exact-match path).
- Trigger input live-converts spaces to U+002D and handleSave converts defensively; no em/en dash ever produced.
- All four locale format errors guide the user to use a hyphen with the physical-trauma example, in each file's native escaping style.
- Both new behavior tests were RED before implementation and GREEN after; no regressions in 24-04 or 260619-okw.
</success_criteria>

<output>
Create `.planning/quick/260626-h5j-snippet-triggers-auto-convert-spaces-to-/260626-h5j-SUMMARY.md` when done.
</output>
