---
quick_id: 260703-mp6
slug: phase37-mainpage-selects-df-label
description: "Phase 37 ext — main-page dropdowns get modern-select style + date-picker neutral sample label"
phase_ref: 37
status: complete
date: 2026-07-03
---

# Quick Task 260703-mp6 — Phase 37 reuse-only extension

Scoped extension to Phase 37 (date-consistency + Personalization), per handoff
`.claude/context/session-prompts/2026-07-03_phase37-extend-mainpage-dropdowns-datepicker.md`.
**Phase 37 stays PENDING** — Ben verifies later. Two atomic commits.

## Task 1 [feat(37)] — main-page controls adopt the shipped modern-select style

Reuse the already-built, already-shipped classes `.select-modern` / `.input-pill`
(`assets/app.css:1041`) via the exact **dual-class** pattern already live on
`#dateFormatSelect`, `#scheduleFrequencySelect` (settings.html) and `#sessionDate`
(add-session.html). Keep the existing base class, ADD the modern class. **Zero new CSS.**

- **files:** `index.html`, `sessions.html`
- **action:**
  - `index.html`: add `select-modern` to `#clientTypeFilter` (~119), `#clientHeartShieldFilter` (~129), `#clientYearFilter` (~137), `#clientSortSelect` (~143) → `class="select-field select-modern"`.
  - `sessions.html`: add `select-modern` to `#sessionClientFilter` (~55), `#sessionTypeFilter` (~69) → `class="select-field select-modern"`; add `input-pill` to `#sessionDateFrom` (~61), `#sessionDateTo` (~65) → `class="input input-pill"`.
- **verify:** grep confirms all 6 selects carry `select-field select-modern` and both date inputs carry `input input-pill`; no other `.select-field` element (add-client `#clientReferralSource`, add-session `#editClientReferralSource`) touched. Filter rows still `.filter-field` flex-columns (`min-width:180px`); `.select-modern` `max-inline-size:100%` keeps them inside the bar on mobile; RTL handled by existing `html[dir="rtl"] .select-modern` rule.
- **done:** both pages render the rounded controls consistent with settings/add-session; no layout regression.

## Task 2 [fix(37)] — date-format picker labels stop showing a near-today date

`assets/settings.js:822` `REFERENCE_DATE = "2026-07-02"` formats each picker option
label via `window.DateFormat.format(...)`, so the options read like a date that
looks like *today / the build date* (confusing; leaks the enhancement date).

- **files:** `assets/settings.js`, `tests/37-personalization.test.js`
- **action:**
  - Change `REFERENCE_DATE` → `"2000-01-31"` (neutral, clearly non-recent; day=31 > 12 so the day slot is unambiguous in every format; day/month/year all distinct). Update the inline comment to explain WHY (was leaking build date / looked like today; now a neutral unambiguous sample). Labels now read `Jan 31, 2000` / `31 Jan 2000` / `01/31/2000` / `31/01/2000` / `2000-01-31`.
  - **SEAM preserved:** labels still come from `window.DateFormat.format` (never hand-typed); `auto` keeps its static i18n label; option `value`s unchanged.
  - Add a test asserting the engine-filled labels reflect the neutral sample (contain `2000`) and do NOT contain the current year (`2026`) or a near-today month — without weakening the existing value/SEAM-order contract tests.
- **verify:** `npm test` green (baseline 121/0, +new assertions); new test fails if `REFERENCE_DATE` regresses to a recent date.
- **done:** picker labels are neutral & unambiguous; SEAM intact.

## Constraints
- Honor no-innerHTML-with-user-data contract (untouched here — only class attrs + a constant + a test).
- Atomic `feat(37)` (Task 1) + `fix(37)` (Task 2) commits; hooks ON; no `--no-verify`.
- Do NOT touch ROADMAP/REQUIREMENTS or mark Phase 37 complete — it stays PENDING.
- Post: delete the handoff file; note completion in dashboard.
