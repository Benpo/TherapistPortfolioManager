---
phase: quick-260702-bg4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/pdf-export.js
  - tests/quick-260702-bg4-pdf-severity-long-name-wrap.test.js
autonomous: true
requirements: [QUICK-260702-bg4]

must_haves:
  truths:
    - "In the PDF 'Severity — before & after' section, a long emotion name wraps onto multiple lines constrained to the name column width and never draws across / under the before-after bar unit (the reported overlap is gone)."
    - "The FULL emotion name text is preserved across the wrapped lines — no truncation, no ellipsis, no font shrinking."
    - "Wrapping and non-overlap hold in BOTH LTR (uiLang 'en') and RTL (uiLang 'he') layouts."
    - "The severity row height is dynamic — max(wrapped-name block, two-bar unit) + row padding — so a single-line name renders byte-identically to today and subsequent rows / page breaks are not corrupted by a taller row."
    - "A falsifiable Node behavior test exists that FAILS on the current fixed-width / single-baseline name draw and PASSES after the wrap + dynamic-row-height fix, in both LTR and RTL."
  artifacts:
    - path: "assets/pdf-export.js"
      provides: "drawSeverityBlock() wrapping issue.name via doc.splitTextToSize to the name-column width, with a per-row dynamic rowHeight"
      contains: "splitTextToSize"
    - path: "tests/quick-260702-bg4-pdf-severity-long-name-wrap.test.js"
      provides: "Falsifiable behavior test: long severity name wraps within the name column (no bar-unit overlap) and preserves full text, LTR + RTL"
      min_lines: 120
  key_links:
    - from: "drawSeverityBlock() name draw (assets/pdf-export.js ~line 1716)"
      to: "the fixed-x before/after bar unit (unitLeftX / barUnitW, ~lines 1651-1667)"
      via: "wrap issue.name to nameColW so each drawn name line stays inside the name column, never crossing into the bar unit"
      pattern: "splitTextToSize"
    - from: "per-row dynamic rowHeight"
      to: "ensureRoom(rowHeight) page-break check + `y = rowTop + rowHeight` advance"
      via: "rowHeight = max(nameLines.length * nameLineH, BAR_LINE_H*2) + ROW_PAD computed before ensureRoom each iteration"
      pattern: "ensureRoom\\(rowHeight\\)"
---

<objective>
Fix the PDF export "Severity — before & after" section so a long emotion name
(`issue.name`) WRAPS onto multiple lines constrained to the name column, instead
of drawing straight across / under the fixed-position before-after bar unit (the
reported overlap). The full name text must be preserved (no truncation, no
ellipsis, no font shrinking), the row height must grow to fit the taller of
{wrapped name, two-bar unit}, and it must work in BOTH LTR and RTL.

Purpose: `drawSeverityBlock` draws `issue.name` at a bare anchor with NO width
constraint while every other section in the module wraps text via
`doc.splitTextToSize`. Because the before/after bar unit sits at a fixed x
hugging the trailing edge, a long name overruns into it. This is the ONLY
section that does not wrap.

Output: `drawSeverityBlock` (assets/pdf-export.js) wraps `issue.name` to the
name-column width and advances each row by a dynamic height; a new falsifiable
Node test proves the fix RED→GREEN in LTR and RTL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@assets/pdf-export.js
@tests/_helpers/jsdom-pdf-env.js
@tests/34-severity-bars.test.js

# Reference (do NOT edit) — the established doc.text-baseline spy pattern this
# plan's test mirrors, via the shared helper's { onJsPDF } hook:
@tests/quick-260620-q8m-pdf-paragraph-linebreaks.test.js
</context>

<constraints>
- The ONLY production file that changes is `assets/pdf-export.js`. The other
  changed file is the NEW test.
- LOCKED fix direction (do NOT revisit or offer alternatives): WRAP the emotion
  name to multiple lines constrained to the name-column width, grow the row
  height to fit the taller of {wrapped name block, two-bar unit}. Preserve the
  full text — NO truncation, NO font shrinking, NO ellipsis. Must work in BOTH
  LTR and RTL.
- Single-line names MUST keep today's exact layout (no visual regression): the
  dynamic row height must reduce to the current fixed 44pt when the name is one
  line (see Task 2 for why the formula guarantees this).
- Runtime-behavior fix → a falsifiable behavior test is MANDATORY before the
  implementation (project rule feedback-behavior-verification). Grep/shape checks
  alone are NOT sufficient.
- Version/cache: NOT a task. `CACHE_NAME` auto-rolls from the deploy git-SHA
  (decision 34-01), so no manual SW bump is required. An optional `APP_VERSION`
  patch bump in `assets/version.js` (currently `1.2.3`) can be folded into the
  next phase/milestone close — do NOT bump it here.
</constraints>

<geometry_reference>
Constants confirmed in source (lines 756-843 + 1587-1667). All values in pt.

    PAGE_W = 595   MARGIN_X = 71   GAP = 6   NAME_SIZE = BODY_SIZE = 11.5
    captionW = 42  TRACK_W = 118   numeralW = 22
    barUnitW = captionW + GAP + TRACK_W + GAP + numeralW = 194
    BAR_LINE_H = 16   ROW_PAD = 12
    current fixed rowHeight = BAR_LINE_H*2 + ROW_PAD = 44

Per-docDir slot anchors (computed once, constant across rows):
  LTR: unitLeftX = PAGE_W - MARGIN_X - barUnitW = 330 ; nameX = MARGIN_X = 71 (left anchor)
       name column spans [MARGIN_X, unitLeftX] = [71, 330]
  RTL: unitLeftX = MARGIN_X = 71 (bar unit spans [71, 265]) ; nameX = PAGE_W - MARGIN_X = 524 (right anchor)
       name column spans [unitLeftX + barUnitW, PAGE_W - MARGIN_X] = [265, 524]

Name column width (symmetric, ~253pt both directions), leaving a GAP gutter so
wrapped lines never touch the bars:
  LTR: nameColW = unitLeftX - MARGIN_X - GAP
  RTL: nameColW = (PAGE_W - MARGIN_X) - (unitLeftX + barUnitW) - GAP
</geometry_reference>

<tasks>

<task type="auto">
  <name>Task 1: Author the failing behavior test (RED) — long severity name wraps, no bar-unit overlap, LTR + RTL</name>
  <files>tests/quick-260702-bg4-pdf-severity-long-name-wrap.test.js</files>
  <action>
Create a Node behavior test (no framework — plain `assert` + `process.exit`,
matching the sibling PDF tests) that builds a real PDF via the SHARED helper
`require('./_helpers/jsdom-pdf-env')` and captures per-draw geometry with the
helper's `{ onJsPDF }` hook.

Harness setup: call `buildJsdomEnv({ onJsPDF: function (doc) { ... } })`. Inside
the hook, wrap the instance's own `text` method (jsPDF installs `text` per
instance, not on the prototype — see the helper's WrappedJsPDF note) so every
`doc.text(str, x, y, opts)` records an entry on a module array:
`{ str: String(str), x: x, y: y, w: doc.getTextWidth(String(str)) }`. `w` is the
drawn width at the font active during that call (name draws are Heebo normal
NAME_SIZE, exactly what we want to measure). Never let the spy throw (wrap in
try/catch), and always return the original `text.apply(this, arguments)`.
NOTE: the 34-severity-bars harness injects `<script>` DEP_STUBS to stop jsdom
hanging on `loadScriptOnce`; the shared helper already eval's every dep AND
`assets/branding/icon-512-base64.js` and short-circuits ensureDeps, so no stub
injection is needed when going through `buildJsdomEnv` (confirm the build resolves
and does not hang — if it ever hangs, mirror 34-severity-bars' injectDepStubs).

Fixtures — two builds, both with a DELIBERATELY LONG emotion name that exceeds
nameColW (~253pt at 11.5pt) so the old code overflows badly and the new code
must wrap to >=2 lines:
  - LTR build: `{ uiLang: 'en' }`, sessionData with
    `issues: [{ name: <long EN phrase>, before: 8, after: 3 }]`. Use a phrase of
    ~60+ chars, e.g. "Persistent overwhelming anticipatory performance anxiety and dread before sessions".
    Also include `markdown` with at least two `## ` section headings and set
    `severityAfterSections` so the block renders inside the body (or rely on the
    end-of-body fallback — either is fine; the goal is that drawSeverityBlock runs).
  - RTL build: `{ uiLang: 'he' }`, a long Hebrew name (RTL), e.g.
    "חרדה מתמשכת ועזה במיוחד לפני מפגשים חברתיים גדולים ולחץ נלווה מתמיד".

Geometry constants in the test (hard-code, mirror geometry_reference):
    PAGE_W=595, MARGIN_X=71, GAP=6, barUnitW=194
    LTR: unitLeftX=330, nameX=71 (left anchor)  ; barUnit near edge = unitLeftX = 330
    RTL: unitLeftX=71,  nameX=524 (right anchor) ; barUnit far edge = unitLeftX+barUnitW = 265
Identify NAME draws: the text calls whose `str` (or its whitespace-joined tokens)
belongs to the long emotion name — filter by matching a distinctive substring/word
from the fixture that appears in NO heading/caption/footer (e.g. "anticipatory"
for EN; a distinctive Hebrew word for HE). Group name draws by rounded baseline y
(helper `roundY` pattern) → one group per wrapped name line.

Assertions (numbered; label which are the RED gates):
  A. [RED gate] WRAP HAPPENED: for the long name there are >= 2 distinct name
     baselines (>= 2 wrapped lines). On current code the whole name is one
     doc.text call at one baseline -> 1 group -> FAILS.
  B. [RED gate] NO BAR-UNIT OVERLAP, per name line:
       LTR (left anchor): assert `x + w <= unitLeftX + TOL` (TOL ~1pt) for every
         name line — i.e. the name's right edge never crosses the bar unit's near
         edge (330). On current code the single line's w (~400+pt) makes
         x+w (~471) >> 330 -> FAILS.
       RTL (right anchor at nameX): assert `x - w >= (unitLeftX + barUnitW) - TOL`
         for every name line — the name's left edge never crosses the bar unit's
         far edge (265). On current code the single overflowing line breaches this
         -> FAILS.
  C. FULL TEXT PRESERVED: concatenating the name-line strings (in baseline order;
     join with a single space and normalize runs of whitespace) contains EVERY
     word of the original fixture name (no word dropped, no ellipsis). Assert each
     source word is present. (splitTextToSize may re-split on spaces; assert on
     words, not exact byte-equality.)
  D. [correctness guard, passes after fix] SUBSEQUENT ROW NOT CORRUPTED: add a
     second build (or reuse a two-issue build) with
     `issues: [{ name:<long>, before:8, after:3 }, { name:'Anger', before:5, after:2 }]`.
     Locate the second issue's name baseline(s) and the first issue's name
     baseline(s). Assert `min(baseline of issue[1] name) > max(baseline of issue[0]
     name)` AND the gap `>= nameLineH` (use 16) — proving the dynamic row height
     pushed row 2 below row 1's LAST wrapped line rather than overlapping it.

Print `[PASS]/[FAIL]` per check and `process.exit(failed === 0 ? 0 : 1)`. Wrap
`main()` in `.catch` that logs FATAL and exits 1. Include a top-of-file docblock
stating the bug, the RED expectation, and the run command.
  </action>
  <verify>
    <automated>node tests/quick-260702-bg4-pdf-severity-long-name-wrap.test.js; test $? -ne 0 && echo "RED-AS-EXPECTED (fails before fix)"</automated>
  </verify>
  <done>
The test runs against the CURRENT (unfixed) code and FAILS on gates A and B in
both the LTR and RTL builds (single overflowing name line -> 1 baseline and
x+w / x-w breaching the bar-unit edge). Exit code is non-zero. The failure
messages clearly name the overlap and the missing wrap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap issue.name + dynamic per-row height in drawSeverityBlock (GREEN)</name>
  <files>assets/pdf-export.js</files>
  <action>
Edit `drawSeverityBlock(issues)` (lines ~1584-1735). Do NOT touch the bar-unit
slot math (unitLeftX / nameX / captionX / trackX / numeralX at ~1651-1667) or
`drawBar` — the bar unit STAYS at its fixed x, top-aligned.

1. Compute the name-column width ONCE, after the slot-layout block (after ~1667),
   using `unitLeftX` / `barUnitW` already in scope — symmetric per docDir per the
   geometry_reference:
     RTL: nameColW = (PAGE_W - MARGIN_X) - (unitLeftX + barUnitW) - GAP
     LTR: nameColW = unitLeftX - MARGIN_X - GAP
   Define `var nameLineH = BAR_LINE_H;` (16) so wrapped name lines share the
   bar-line rhythm.

2. Remove the module-scope fixed `var rowHeight = BAR_LINE_H * 2 + ROW_PAD;`
   (line ~1599). rowHeight becomes per-row.

3. Inside the per-issue loop, BEFORE `ensureRoom(...)`:
   - Set the name font/size so splitTextToSize measures correctly:
     `doc.setFont('Heebo','normal'); doc.setFontSize(NAME_SIZE);`
   - Wrap the LOGICAL name (splitTextToSize measures the unshaped logical string;
     drawAt shapes each line at draw time — the same wrap-then-shape order the
     paragraph/list renderers use):
     `var nameLines = doc.splitTextToSize(String(issue.name || ''), nameColW);`
     Guard against an empty result: if `nameLines.length === 0` set it to `['']`.
   - Compute the dynamic height:
     `var nameBlockH = nameLines.length * nameLineH;`
     `var barBlockH  = BAR_LINE_H * 2;`
     `var rowHeight   = Math.max(nameBlockH, barBlockH) + ROW_PAD;`
     (For a single-line name: nameBlockH = 16 < barBlockH = 32 -> rowHeight = 44,
     byte-identical to today. This is why single-line rows do not regress.)

4. `ensureRoom(rowHeight)` now uses the dynamic value; keep `var rowTop = y;`
   AFTER ensureRoom (so it reads the post-page-break y).

5. Replace the single name draw (line ~1716-1717) with a loop over `nameLines`,
   drawing each line at its own baseline, top-aligned to match the bars:
     for each nl in 0..nameLines.length-1:
       drawAt(nameLines[nl], nameX, rowTop + 10 + nl * nameLineH, nameAlign);
   Keep the bar draws unchanged at `rowTop + 10` and `rowTop + 10 + BAR_LINE_H`
   (bar unit stays top-aligned).

6. Advance with the dynamic height: `y = rowTop + rowHeight;` (the inter-row
   separator line at `y - 6` stays as-is and remains correct).

Do NOT add truncation, ellipsis, or font-size reduction. Preserve all existing
comments/behavior for the bars, numerals, unmeasured '–' handling, and the
docDir anchoring.
  </action>
  <verify>
    <automated>node tests/quick-260702-bg4-pdf-severity-long-name-wrap.test.js</automated>
    <automated>node tests/34-severity-bars.test.js && node tests/34-severity-unmeasured.test.js</automated>
    <automated>npm test</automated>
  </verify>
  <done>
The new bg4 test passes all checks (A-D) in both LTR and RTL: the long name wraps
to >=2 lines, no name line's horizontal extent crosses the bar-unit edge, the
full text is preserved, and the second row sits below the first row's last wrapped
line. `tests/34-severity-bars.test.js` and `tests/34-severity-unmeasured.test.js`
still pass (single-line geometry unchanged). The full `npm test` suite is green
(previously 118 passing + the new file). No test was weakened.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data (user-entered emotion names) → PDF renderer | `issue.name` is arbitrary user text of unbounded length crossing into layout code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-bg4-01 | Denial of Service | drawSeverityBlock name wrapping | low | mitigate | Width is bounded by `nameColW` and height by `nameLines.length * nameLineH`; `splitTextToSize` is a single bounded pass with no loop over the fix — an extremely long name produces more lines / pages (bounded by `ensureRoom` pagination), never a hang. No new network, storage, or eval surface. |
| T-bg4-02 | Tampering | test dependencies | low | accept | No new npm/pip/cargo installs; the test reuses the existing jsdom devDependency via the shared helper. |
</threat_model>

<verification>
- RED first: Task 1's test FAILS (non-zero exit) against unfixed code on gates A
  (wrap) and B (no overlap), LTR and RTL.
- GREEN after: Task 2 makes the bg4 test pass all checks; the two existing
  severity tests pass; `npm test` is fully green.
- No-regression: single-line names still produce the 44pt row (dynamic formula
  reduces to the old constant), so 34-severity-bars / 34-severity-unmeasured and
  the golden-baseline PDF suites are unaffected.
</verification>

<success_criteria>
- Long emotion names wrap within the name column and never overlap the before/after
  bar unit, in LTR and RTL.
- Full name text preserved — no truncation, ellipsis, or font shrinking.
- Row height is dynamic; page breaks and subsequent rows are correct.
- A falsifiable RED→GREEN test proves it; full suite green; only
  `assets/pdf-export.js` changed in production.
</success_criteria>

<output>
Create `.planning/quick/260702-bg4-pdf-export-severity-section-long-emotion/260702-bg4-SUMMARY.md` when done.
</output>
