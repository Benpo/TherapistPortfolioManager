---
phase: quick-260516-g7p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/add-session.js
  - add-session.html
  - assets/snippets-seed.js
  - assets/overview.js
  - index.html
  - tests/quick-260516-g7p-save-returns-to-session.test.js
  - tests/quick-260516-g7p-export-editor-snippets.test.js
  - tests/quick-260516-g7p-missing-birth-filter.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Saving a session returns the therapist to that same session in reading mode (not the overview homepage)"
    - "Typing a snippet trigger in the export modal's live editor expands it, exactly like the session-form textareas"
    - "The two seed snippets read 'Effort Unreceived' and 'Love Unreceived' (words reordered, spelling unchanged)"
    - "The overview missing-birth-year warning has a clickable control that filters the client table to exactly those clients"
  artifacts:
    - path: "assets/add-session.js"
      provides: "Post-save redirect to ./add-session.html?sessionId=<id>"
    - path: "add-session.html"
      provides: "exportEditor textarea bindable by Snippets"
    - path: "assets/snippets-seed.js"
      provides: "Corrected seed.unreceived-effort / seed.unreceived-love English text"
    - path: "assets/overview.js"
      provides: "Missing-birth filter wired into existing applyFiltersAndSort pipeline"
    - path: "index.html"
      provides: "Clickable filter trigger inside #missingBirthBanner"
  key_links:
    - from: "sessionForm submit handler (add-session.js)"
      to: "./add-session.html?sessionId=<id> read-mode load path (line ~1735-1743)"
      via: "window.location.href with id from addSession() return or editingSession.id"
      pattern: "add-session\\.html\\?sessionId="
    - from: "openExportDialog (add-session.js)"
      to: "window.Snippets.bindTextarea"
      via: "explicit bind of #exportEditor when the modal opens"
      pattern: "Snippets\\.bindTextarea"
    - from: "#missingBirthBanner control (index.html)"
      to: "applyFiltersAndSort (overview.js)"
      via: "module-level missing-birth filter flag honored in the existing filter predicate"
      pattern: "missingBirth"
---

<objective>
Fix 4 small, independent UI bugs in the Sessions Garden app. Each bug is its
own atomic task with its own atomic commit so the git history clearly states
what was fixed where (explicit user requirement).

Purpose: Polish before launch — these are friction points the therapist hits
daily (save flow, snippet expansion, mislabeled seed data, undiscoverable
missing-data warning).

Output: 4 commits, one per bug, plus 3 falsifiable behavior tests (bugs #1,
#2, #4 are runtime-behavior changes; bug #3 is pure seed text — no test, per
MEMORY:feedback-behavior-verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<notes>
- Static HTML/JS app, no framework. No build step. Tests are node-runnable
  vm-sandbox scripts under tests/ that `process.exit(0|1)`. Pattern reference:
  tests/quick-260516-1f5-backup-trio-parity.test.js (most recent quick task)
  and tests/24-05-list-filter.test.js (pure-helper sandbox load).
- Project convention (MEMORY:feedback-behavior-verification): runtime-behavior
  fixes get a FALSIFIABLE behavior test that fails BEFORE and passes AFTER.
  Grep gates verify shape, not behavior — do not rely on them alone for #1/#2/#4.
- Pre-commit hook may skip the sw.js CACHE_NAME bump
  (MEMORY:reference-pre-commit-sw-bump). None of these 4 fixes change
  precached asset *URLs* (only file contents of already-precached files), so
  no manual CACHE_NAME chore commit is required — but if the hook prints a
  warning about sw.js, it is safe to ignore for this plan.
- Lemon Squeezy store rule (./CLAUDE.md): irrelevant here — no LS code touched.
</notes>

<interfaces>
Bug #1 — save handler & read-mode load path (assets/add-session.js):
- Line ~1680: `await PortfolioDB.addSession({...})` — RETURNS the new numeric
  session id (db.js addSession → addRecord returns the IndexedDB key).
- Line ~1660: existing-session branch uses `editingSession` (has `.id`).
- Line ~1698-1701 (CURRENT, the bug):
    formSaving = true;
    setTimeout(() => { window.location.href = "./index.html"; }, 600);
- Line ~1735-1743: `if (sessionId && Number.isInteger(sessionId)) { editingSession =
  await PortfolioDB.getSession(sessionId); ... setReadMode(true); }`
  → navigating to ./add-session.html?sessionId=<id> AUTOMATICALLY opens the
  session in reading mode. This is the existing, proven entry point used by
  sessions.js line 149.

Bug #2 — snippet binding (assets/snippets.js, public API window.Snippets):
- `init()` binds only `document.querySelectorAll('textarea[data-snippets="true"]')`
  at DOMContentLoaded.
- `bindTextarea(el)` is public, idempotent (guarded by internal `_bound`
  WeakMap), safe to call repeatedly.
- Session-form textareas (add-session.html lines 245-344) all carry
  `data-snippets="true"` — that is why snippets work there.
- add-session.html line 460: `<textarea id="exportEditor" class="textarea
  export-editor" dir="auto" spellcheck="true"></textarea>` — NO
  data-snippets attribute → never bound → snippets dead in the export editor.
- openExportDialog() is at assets/add-session.js line ~1389; it reads
  `document.getElementById("exportEditor")` at line ~1403.

Bug #3 — seed records (assets/snippets-seed.js):
- Line 303-307: rec("seed.unreceived-effort", "unreceived-effort", he, en, cs, de)
  en (line 305): "Unreceived Effort — When work, achievement, or effort is not
  accepted, recognized, or considered good enough. ..."
- Line 308-312: rec("seed.unreceived-love", "unreceived-love", he, en, cs, de)
  en (line 311): "Unreceived Love — A feeling that love has been rejected. ..."
- Only the leading TWO English words before " — " are reordered. id, trigger,
  he/cs/de expansions, em-dash separator, and the rest of the paragraph are
  UNCHANGED. Spelling is already correct in the seed ("Unreceived", not
  "Unrecieved") — preserve that spelling, only swap word order per the user's
  intent ("X unreceived" word order).

Bug #4 — overview filter pipeline (assets/overview.js):
- `updateMissingBirthBanner(clients)` line ~252: computes
  `missing = clients.filter(c => !c.birthDate && !c.age).length`, sets
  #missingBirthBannerText, toggles #missingBirthBanner .is-hidden.
- `applyFiltersAndSort()` line ~110: the single client-table filter pipeline.
  Filters `_allClients` (module var, set line ~218) then calls
  renderClientRows(filtered, _sessionsByClient).
- `clearFiltersBtn` handler line ~176 resets all filters; `updateClearButton()`
  line ~160 controls its visibility.
- #missingBirthBanner markup is index.html lines 151-154 (span icon + span
  text). #clientTableBody is line 165.
- Existing "missing birth" client predicate is `!c.birthDate && !c.age`
  (reuse this exact predicate for the filter so banner count == filtered count).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return to the same session in reading mode after Save</name>
  <files>assets/add-session.js, tests/quick-260516-g7p-save-returns-to-session.test.js</files>
  <behavior>
    Behavior test (FALSIFIABLE — fails before, passes after):
    - Source-contract test on assets/add-session.js: assert the session-form
      submit handler's post-save redirect targets
      `./add-session.html?sessionId=` and does NOT redirect to `./index.html`.
      (The redirect string + branching are deterministic enough to assert via
      a vm-sandbox load OR a precise source-shape assertion that fails on the
      current `window.location.href = "./index.html"`.)
    - New-session branch: the id used in the redirect comes from the value
      RETURNED by `PortfolioDB.addSession(...)` (capture into a `const`).
    - Existing-session branch: the id used is `editingSession.id`.
    Model the broken-world vs fixed-world as in
    tests/quick-260516-1f5-backup-trio-parity.test.js so the test proves the
    redirect target is a FUNCTION of the saved id, not a constant homepage URL.
  </behavior>
  <action>
    In the `sessionForm` submit handler (assets/add-session.js, around lines
    1618-1702): capture the saved session id. For the NEW-session branch,
    change `await PortfolioDB.addSession({...})` to assign its return value
    (e.g. `const newId = await PortfolioDB.addSession({...});`). For the
    existing-session branch, use `editingSession.id`. Replace the post-save
    navigation at lines ~1698-1701 so that instead of
    `window.location.href = "./index.html"` it navigates to
    `./add-session.html?sessionId=${savedId}` (keep the existing
    `formSaving = true;` guard and the ~600ms setTimeout/toast timing exactly
    as-is — only the URL and the id capture change). Reuse the SAME entry
    point sessions.js already uses (line 149) and the read-mode auto-load at
    add-session.js line ~1735-1743 — no new read-mode code needed; loading
    with ?sessionId auto-enters reading mode. Do NOT change the delete-button
    handler (line ~1716 → ./sessions.html stays).
  </action>
  <verify>
    <automated>node tests/quick-260516-g7p-save-returns-to-session.test.js</automated>
    Also: grep proves no homepage redirect remains in the save handler:
    `grep -n 'index.html' assets/add-session.js` must not show the save-handler
    line; `grep -c 'add-session.html?sessionId=' assets/add-session.js` >= 1.
  </verify>
  <done>
    Behavior test exits 0. After saving a new OR existing session, the browser
    lands on ./add-session.html?sessionId=<that session's id> which renders the
    session in reading mode. Saving no longer returns to the overview homepage.
    Commit: `fix(quick-260516-g7p): return to session reading mode after save`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make snippets work in the export modal's live editor</name>
  <files>add-session.html, assets/add-session.js, tests/quick-260516-g7p-export-editor-snippets.test.js</files>
  <behavior>
    Behavior test (FALSIFIABLE):
    - Structural-but-load-bearing: add-session.html `<textarea id="exportEditor">`
      MUST carry `data-snippets="true"` (parity with the 7 session-form
      textareas at lines 245-344). Fails BEFORE (attribute absent).
    - Behavior: load assets/snippets.js in a vm sandbox (pattern:
      tests/24-05-list-filter.test.js). Prove `window.Snippets.bindTextarea`
      is a function and that a textarea bound via it expands a known trigger
      via the public API / __testExports.resolveExpansion — i.e. the export
      editor, once bound, behaves identically to a session-form textarea.
      Assert that an UNBOUND textarea does NOT expand (proves binding is the
      causal fix, not a constant).
  </behavior>
  <action>
    Root cause: `window.Snippets.init()` binds only
    `textarea[data-snippets="true"]` at DOMContentLoaded; the export modal's
    `#exportEditor` (add-session.html line 460) lacks that attribute and is
    therefore never wired, while the session-form textareas (lines 245-344)
    have it and work. Two-part fix: (1) add `data-snippets="true"` to the
    `<textarea id="exportEditor">` in add-session.html (keep its existing
    class, id, dir, spellcheck attributes). (2) In `openExportDialog()`
    (assets/add-session.js ~line 1403, right where it does
    `const editor = document.getElementById("exportEditor")`), after the
    editor element is obtained, defensively call
    `if (window.Snippets && editor) window.Snippets.bindTextarea(editor);`
    — this is idempotent (Snippets guards via its internal `_bound` WeakMap)
    and guarantees binding even though the static editor is technically
    present at DOMContentLoaded (belt-and-suspenders against future modal
    re-rendering). Do not modify snippets.js.
  </action>
  <verify>
    <automated>node tests/quick-260516-g7p-export-editor-snippets.test.js</automated>
    Also: `grep -n 'id="exportEditor"' add-session.html` shows
    `data-snippets="true"`; `grep -c 'Snippets.bindTextarea' assets/add-session.js`
    >= 1.
  </verify>
  <done>
    Behavior test exits 0. Typing the snippet prefix + a known trigger in the
    export modal's live editor expands the snippet exactly as it does in the
    session-form textareas.
    Commit: `fix(quick-260516-g7p): enable snippet expansion in export modal editor`
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the two mislabeled seed snippet texts</name>
  <files>assets/snippets-seed.js</files>
  <action>
    Pure seed-text reorder — no behavior test (data-only change, per
    MEMORY:feedback-behavior-verification: behavior tests are for runtime
    behavior, not static seed strings). In assets/snippets-seed.js, edit ONLY
    the leading English emotion-name (the words before the " — " em-dash
    separator) of two records, preserving id, trigger, the he/cs/de
    expansions, the separator, and the entire meaning paragraph:
      - Line ~305, record `seed.unreceived-effort`: change the English
        expansion's leading "Unreceived Effort" to "Effort Unreceived"
        (result: "Effort Unreceived — When work, achievement, or effort is
        not accepted, ...").
      - Line ~311, record `seed.unreceived-love`: change the English
        expansion's leading "Unreceived Love" to "Love Unreceived"
        (result: "Love Unreceived — A feeling that love has been rejected.
        ...").
    Keep the existing (correct) spelling — only the two leading words are
    swapped in order. Do NOT touch the `id` ("seed.unreceived-effort" /
    "seed.unreceived-love") or `trigger` ("unreceived-effort" /
    "unreceived-love") — those are deterministic migration keys; changing
    them would duplicate seeds on re-run.
  </action>
  <verify>
    <automated>grep -c 'Effort Unreceived —' assets/snippets-seed.js</automated>
    Must be >= 1. Also: `grep -c 'Love Unreceived —' assets/snippets-seed.js`
    >= 1; `grep -c '"Unreceived Effort —\|"Unreceived Love —' assets/snippets-seed.js`
    == 0 (old order gone from the English expansions); `grep -c
    'seed.unreceived-effort\|seed.unreceived-love' assets/snippets-seed.js`
    unchanged (ids/triggers preserved — still 4 matches: 2 ids + 2 triggers).
  </automated></verify>
  <done>
    The two seed snippets' English text now reads "Effort Unreceived — ..."
    and "Love Unreceived — ...". IDs/triggers and he/cs/de text unchanged.
    Commit: `fix(quick-260516-g7p): correct word order in unreceived-effort/love seed text`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Make the missing-birth-year warning filter the client table</name>
  <files>index.html, assets/overview.js, tests/quick-260516-g7p-missing-birth-filter.test.js</files>
  <behavior>
    Behavior test (FALSIFIABLE) — pure-helper sandbox like
    tests/24-05-list-filter.test.js: load assets/overview.js in a vm sandbox,
    exercise the filter predicate / applyFiltersAndSort path with a fixture
    client list (some with birthDate, some with age, some with neither).
    Assert:
      - With the missing-birth filter ACTIVE, only clients matching
        `!c.birthDate && !c.age` are rendered (count == the banner's
        `missing` count from updateMissingBirthBanner over the same list).
      - With it INACTIVE (default), all clients render.
      - Clearing filters resets the missing-birth flag (filtered == all).
    The equality "filtered count == banner count" proves the link shows
    EXACTLY the warned clients (not an approximation / constant).
  </behavior>
  <action>
    Chosen approach (per task guidance — reuse the existing, lower-risk,
    discoverable filter mechanism rather than a new modal/list): turn the
    warning into a click-to-filter on the EXISTING client table pipeline.

    index.html: inside `#missingBirthBanner` (lines 151-154), add a clickable
    control after the text span — a `<button type="button"
    id="missingBirthFilterBtn" class="button ghost">` with a
    `data-i18n="overview.missingBirth.showThem"` label. Add the i18n key
    `overview.missingBirth.showThem` to all four locale files
    (assets/i18n-en.js, i18n-he.js, i18n-de.js, i18n-cs.js) near the existing
    `overview.missingBirth.notice` key (line ~56) — EN: "Show them", DE:
    "Anzeigen", HE: "הצג אותם", CS: "Zobrazit". (i18n files are not listed in
    files_modified intentionally-add the key; it is additive and locale-safe.)

    assets/overview.js: introduce a module-level boolean
    `_missingBirthFilterActive = false` alongside `_allClients`. In
    `applyFiltersAndSort()` (line ~110) add one predicate to the existing
    `_allClients.filter(...)`: `if (_missingBirthFilterActive && (c.birthDate
    || c.age)) return false;` — reuse the EXACT existing missing predicate
    (`!c.birthDate && !c.age`) so the filtered set equals the banner count.
    Wire `#missingBirthFilterBtn` click to set
    `_missingBirthFilterActive = true`, call `applyFiltersAndSort()`,
    `updateClearButton()`, and scroll the client table (#clientTableBody /
    its containing section) into view so the therapist immediately sees the
    filtered list. Include `_missingBirthFilterActive` in the
    `updateClearButton()` "hasFilters" check (line ~160) and reset it to
    false in the `clearFiltersBtn` handler (line ~176) so the existing Clear
    Filters control also clears this filter. Keep `updateMissingBirthBanner`
    behavior (banner still shows the count) — only add the actionable button.
  </action>
  <verify>
    <automated>node tests/quick-260516-g7p-missing-birth-filter.test.js</automated>
    Also: `grep -c 'missingBirthFilterBtn' index.html assets/overview.js`
    (>=1 each); `grep -c 'overview.missingBirth.showThem' assets/i18n-en.js
    assets/i18n-he.js assets/i18n-de.js assets/i18n-cs.js` (>=1 each);
    `grep -c '_missingBirthFilterActive' assets/overview.js` >= 3 (declare,
    filter predicate, clear-reset).
  </verify>
  <done>
    Behavior test exits 0. The overview missing-birth warning shows a
    clickable "Show them" control; clicking it filters the existing client
    table to exactly the clients with no birth year (count matches the
    warning), scrolls the table into view, and Clear Filters resets it.
    Commit: `fix(quick-260516-g7p): make missing-birth warning filter the client table`
  </done>
</task>

</tasks>

<verification>
- Bug #1: `node tests/quick-260516-g7p-save-returns-to-session.test.js` exits 0;
  no save-handler redirect to ./index.html remains.
- Bug #2: `node tests/quick-260516-g7p-export-editor-snippets.test.js` exits 0;
  #exportEditor has data-snippets="true" + bindTextarea call present.
- Bug #3: grep gates green (new word order present, old absent, ids/triggers
  intact).
- Bug #4: `node tests/quick-260516-g7p-missing-birth-filter.test.js` exits 0;
  filtered count equals banner count; Clear Filters resets the new flag.
- Exactly 4 commits, one per bug, with the specified message stubs — verify
  with `git log --oneline -4` after execution.
</verification>

<success_criteria>
- All 4 bugs fixed, each in its own atomic commit with a clear conventional
  message naming exactly what was fixed.
- 3 falsifiable behavior tests added (bugs #1, #2, #4) following the project's
  node-vm-sandbox convention; bug #3 is a data-only change with grep gates.
- No regression to the delete-session flow, the session-form snippet
  behavior, the existing overview filters, or seed migration idempotency
  (ids/triggers unchanged).
- No Lemon Squeezy code touched.
</success_criteria>

<output>
After completion, create
`.planning/quick/260516-g7p-four-small-ui-bugs/260516-g7p-SUMMARY.md`
recording: the 4 commit SHAs, root cause + fix for each bug, and the chosen
bug-#4 approach (reused existing client-table filter pipeline, not a new
modal/list).
</output>
