---
quick_id: 260702-q5f
phase: quick-260702-q5f
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - assets/i18n-en.js
  - assets/i18n-he.js
  - assets/i18n-de.js
  - assets/i18n-cs.js
  - assets/overview.js
  - assets/app.css
requirements: [UAT-F3]

must_haves:
  truths:
    - "The home-overview past-sessions toggle reads as a labelled pill: eye icon + a visible word (en 'Sessions')."
    - "The pill toggles aria-expanded between 'false' and 'true' as the detail row opens/closes."
    - "All four languages (en/he/de/cs) render the label without overflow, clipping, or wrap; Hebrew renders RTL."
    - "npm test stays green — no overview test regresses."
  artifacts:
    - "assets/overview.js — detailButton with eye SVG, a data-i18n label span, aria-expanded"
    - "assets/app.css — .table .row-toggle rewritten as an auto-width pill"
    - "assets/i18n-{en,he,de,cs}.js — new overview.table.viewSessions key"
  key_links:
    - "detailButton click handler ↔ aria-expanded state must stay in sync with detailRow.style.display"
    - "label span data-i18n='overview.table.viewSessions' ↔ the new key in all 4 locale files"
---

<objective>
Replace the icon-only clock button that expands a client's past sessions on the home
overview with a clearer, labelled pill: a Feather "eye" icon plus a visible word
("Sessions"), and add proper `aria-expanded` state. Therapist UAT feedback F3: the current
clock button is "a small button with a clock. Not easy to find or self explanatory."

All design decisions are LOCKED (icon = eye, label = new `overview.table.viewSessions` key,
font = inherited Rubik, a11y = add `aria-expanded`). This plan is the HOW only — do not
revisit the WHAT and do not expand scope beyond the six files listed.

Purpose: make the "view previous sessions" control self-explanatory.
Output: a labelled pill control, four new i18n values, and pill CSS — no behavior change to
the toggle itself beyond the aria-expanded addition.

## Execution constraints (READ FIRST)
- This runs **NON-worktree, directly on `main`**. There is no isolated worktree base.
- The executor must **NOT commit any docs** (this PLAN, any SUMMARY, or STATE.md). The
  orchestrator owns all doc/state commits. Commit **only** the six source files above.
- Scope fence: touch ONLY the six files in `files_modified`. Nothing else.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# The authoritative handoff (full grounded context):
@.claude/context/session-prompts/2026-07-02_f3-view-sessions-label-HANDOFF.md

# Files being changed (line numbers may have drifted — re-verify with grep before editing):
@assets/overview.js
@assets/app.css
@assets/i18n-en.js

# Grounding notes (verified during planning, 2026-07-02, Phase 36 complete):
# - overview.js:438-443  detailButton (className row-toggle): title + aria-label from
#     overview.table.previousSessions, innerHTML = clock SVG (circle r=10 + polyline).
# - overview.js:444-455  sibling quickAddButton (row-quick-add, textContent "+"), both
#     appended into actionsWrap (.row-actions).
# - overview.js:561-564  the click handler toggling detailRow.style.display none↔table-row.
# - app.css:492-503  .table .row-toggle — fixed 34px circle (border-radius:50%).
# - app.css:505-510  .row-actions — flex, justify-content:flex-end, gap:0.5rem.
# - app.css:1286-1298 mobile 44px min tap-target rule includes ".row-actions button".
# - app.css:1301-1361 max-width:640px card layout (td become blocks; action cell last).
# - app.css:1371 html[dir="rtl"] .row-actions{justify-content:flex-start}.
# - PRECEDENT: app.css:1453-1465 .edit-button is already an inline-flex pill
#     (gap, padding, border-radius:999px, font-size:0.85rem, font-weight:700) using
#     .button-label + .button-icon spans (overview.js:532-541). Mirror it.
# - i18n key overview.table.previousSessions sits at i18n-en.js:26 (adjacent in he/de/cs:26).
# - NO test is coupled to the clock button: grep of tests/ for row-toggle / previousSessions
#     / the clock polyline / clock / viewSessions returns nothing. The 31-overview-render-
#     hardening test asserts only on .edit-button .button-label/.button-icon (the detail-row
#     "View" button) — untouched here. The render path must still succeed, so keep the
#     label on textContent + static SVG innerHTML (do NOT introduce interpolated innerHTML).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the overview.table.viewSessions i18n key in all four locales</name>
  <files>assets/i18n-en.js, assets/i18n-he.js, assets/i18n-de.js, assets/i18n-cs.js</files>
  <action>
Add a NEW key `overview.table.viewSessions` immediately after the existing
`overview.table.previousSessions` entry (the line ~26 block) in each of the four locale
files. Do not touch or rename `previousSessions` — the new key is additive and reused for
the visible pill label. Values (LOCKED, Decision 2):
- assets/i18n-en.js → "Sessions"
- assets/i18n-he.js → "מפגשים"
- assets/i18n-de.js → "Sitzungen"
- assets/i18n-cs.js → "Sezení"
Match the surrounding key/quote/comma/indentation style exactly (two-space indent, double
quotes, trailing comma). Task 2 references this key via App.t, so land it first so the
label renders real text rather than the raw key fallback.
  </action>
  <verify>
    <automated>node -e "for (const l of ['en','he','de','cs']) { const m={en:'Sessions',he:'מפגשים',de:'Sitzungen',cs:'Sezení'}; const s=require('fs').readFileSync('assets/i18n-'+l+'.js','utf8'); const re=new RegExp('\"overview\\\\.table\\\\.viewSessions\"\\\\s*:\\\\s*\"'+m[l]+'\"'); if(!re.test(s)){console.error('MISSING/'+l);process.exit(1)} } console.log('all 4 viewSessions keys present')"</automated>
  </verify>
  <done>All four locale files contain `overview.table.viewSessions` with the exact values above; `previousSessions` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Swap clock→eye, add labelled span + aria-expanded, drop redundant title</name>
  <files>assets/overview.js</files>
  <action>
Edit the `detailButton` construction (currently ~overview.js:438-443) and its click handler
(~561-564). Re-grep for `row-toggle` and `previousSessions` first; line numbers may drift.

Markup changes (Decisions 1, 2, 4):
1. Swap the clock SVG for the Feather "eye" icon. Set the button's icon via the SAME static
   innerHTML assignment used today (no i18n interpolation — keep it XSS-safe and keep the
   31-hardening intent intact). Keep width/height 18 and the exact icon attribute style:
   `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
   stroke-linecap="round" stroke-linejoin="round"`. The eye markup is the two shapes: the
   almond path `M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z` and `<circle cx="12" cy="12"
   r="3"/>`. Remove the old clock shapes entirely (the r=10 circle and the hour-hand
   polyline) — the button must no longer contain that polyline.
2. After the icon innerHTML, append a NEW label `<span>` created via createElement:
   className `row-toggle-label`, set `data-i18n="overview.table.viewSessions"`, and
   `textContent = App.t("overview.table.viewSessions")`. Use textContent (NOT innerHTML) so
   no interpolated-i18n innerHTML site is introduced. appendChild it to detailButton so the
   icon sits before the label (natural reading order; RTL flips via dir automatically).
3. Add `detailButton.setAttribute("aria-expanded", "false")` at creation.
4. Keep the existing `aria-label` = App.t("overview.table.previousSessions") (preserves the
   descriptive accessible name; do NOT regress screen-reader semantics). DROP the now-
   redundant `detailButton.title = ...` line — the visible label plus aria-label make the
   tooltip noise unnecessary (Decision 4, planner's call: title removed).
5. Do NOT touch the sibling `quickAddButton` ("+") — it must stay appended after
   detailButton in `.row-actions`, unchanged.

Click-handler change (Decision 4): in the detailButton click listener, after toggling
`detailRow.style.display`, set `detailButton.setAttribute("aria-expanded", String(!isOpen))`
where `isOpen` is the pre-toggle state already computed there (so aria-expanded reflects the
NEW open/closed state: "true" when the detail row is now shown).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('assets/overview.js','utf8'); const checks=[[/M1 12s4-8 11-8/,'eye path'],[/circle cx=\"12\" cy=\"12\" r=\"3\"/,'eye pupil'],[/aria-expanded/,'aria-expanded attr'],[/String\(!isOpen\)/,'handler updates aria-expanded'],[/data-i18n=\"overview\.table\.viewSessions\"/,'label span data-i18n'],[/textContent = App\.t\(\"overview\.table\.viewSessions\"\)/,'label textContent']]; let ok=true; for(const [re,name] of checks){ if(!re.test(s)){console.error('MISSING: '+name);ok=false} } if(/12 6 12 12 16 14/.test(s)){console.error('STALE clock polyline still present');ok=false} if(/detailButton\.title\s*=/.test(s)){console.error('redundant title not dropped');ok=false} process.exit(ok?0:1)"</automated>
  </verify>
  <done>detailButton renders the eye icon + a data-i18n label span, initializes aria-expanded="false", updates it in the click handler, keeps aria-label, drops title; the clock polyline is gone; quickAddButton untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle .table .row-toggle from fixed circle to auto-width pill</name>
  <files>assets/app.css</files>
  <action>
Rewrite the `.table .row-toggle` rule (currently ~app.css:492-503) from a fixed 34px circle
to an auto-width pill, mirroring the existing `.edit-button` pill (app.css:1453-1465) so the
two controls read consistently (Decisions 1, 3). Replace `width:34px; border-radius:50%;
padding:0; display:grid; place-items:center` with:
- `display:inline-flex; align-items:center; gap:0.35rem;`
- `padding:0 0.6rem;` and keep a comfortable `height:34px;`
- `border-radius:999px;`
- KEEP `border:0; background:var(--color-primary-soft); color:var(--color-text);
  cursor:pointer;`
- Add label typography that the span inherits (Decision 3 — no new font, Rubik inherited):
  `font-size:0.85rem; font-weight:700;`
- Add `white-space:nowrap;` so the German "Sitzungen" never wraps inside the pill.
The 18px eye SVG is a direct flex child and centers via align-items; no icon-span wrapper or
extra selector is needed. Do NOT change `.row-quick-add`, `.row-actions`, the mobile 44px
tap-target rule, the 640px card layout, or the RTL `.row-actions` rule — inline-flex + gap +
the existing dir-aware rules already handle RTL (icon/label order flips) and mobile (the
min-height:44px tap rule still applies; the wider pill simply exceeds min-width:44px, which
is fine). Verify those layouts hold; only edit the `.table .row-toggle` block.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('assets/app.css','utf8'); const m=s.match(/\.table \.row-toggle\s*\{[^}]*\}/); if(!m){console.error('rule not found');process.exit(1)} const b=m[0]; const need=[/display:\s*inline-flex/,/align-items:\s*center/,/border-radius:\s*999px/,/gap:/,/white-space:\s*nowrap/]; let ok=true; for(const re of need){ if(!re.test(b)){console.error('missing in .row-toggle: '+re);ok=false} } if(/border-radius:\s*50%/.test(b)){console.error('still a circle (border-radius:50%)');ok=false} if(/width:\s*34px/.test(b)){console.error('still fixed 34px width');ok=false} process.exit(ok?0:1)"</automated>
  </verify>
  <done>.table .row-toggle is an inline-flex pill (999px radius, gap, nowrap, height ~34px, primary-soft bg); no border-radius:50% or fixed width remains; sibling/mobile/RTL rules untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| i18n data → DOM | Locale label string rendered into the button |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q5f-01 | Tampering (XSS) | detailButton label | low | mitigate | Label uses `textContent` (not innerHTML); icon SVG is a static, app-controlled string with no interpolation — no injection surface introduced. |
| T-q5f-02 | Info-disclosure | a11y semantics | low | accept | aria-label retained + aria-expanded added; visible label added — screen-reader semantics improve, not regress. |
</threat_model>

<verification>
## Whole-plan gates (run after all three tasks)
1. `npm test` — full suite stays green; specifically `tests/31-overview-render-hardening.test.js`
   must still pass (it asserts `.edit-button` internals and drives the render path — the eye
   swap and label must not break rendering).
2. All three per-task automated greps above pass.

## Human visual check (supplementary — UI change, cannot be fully automated)
Serve the app (`python3 -m http.server 8001 --directory .` → http://127.0.0.1:8001/, seed a
client) and confirm on the home overview:
- Desktop: the pill (eye + "Sessions") and the "+" button align cleanly on the row, no
  overflow/wrap; clicking the pill expands/collapses past sessions and the DevTools
  aria-expanded flips false↔true.
- Narrow / mobile card layout (≤640px): the control stays readable and non-clipped; tap
  target is comfortable (≥44px tall).
- All 4 languages render the label without clipping — German "Sitzungen" (widest) fits;
  Hebrew "מפגשים" renders RTL with icon/label in a natural order.
</verification>

<success_criteria>
- Home-overview past-sessions toggle is a labelled eye+word pill with working aria-expanded.
- `overview.table.viewSessions` exists in all four locales with the locked values.
- No clock polyline remains; no interpolated-i18n innerHTML introduced; quickAddButton and
  mobile/RTL rules unchanged.
- `npm test` green; no overview test regressed.
- Only the six source files changed; no docs/state committed by the executor.
</success_criteria>

<output>
This is a quick task. Do NOT write or commit a SUMMARY, PLAN, or STATE file — the
orchestrator handles all doc/state commits. Commit only the six source files
(assets/i18n-{en,he,de,cs}.js, assets/overview.js, assets/app.css) as atomic changes, then
return control to the orchestrator.
</output>
